# PR #5149: [KEP-2941]: Supporting Dynamic Resource Allocations in Kueue

**Summary**: [KEP-2941]: Supporting Dynamic Resource Allocations in Kueue

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5149

**Last updated**: 2025-06-25T16:02:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alaypatel07](https://github.com/alaypatel07)
- **Branch**: `dra-kep` → `main`
- **Created**: 2025-05-02T19:55:53Z
- **Updated**: 2025-06-25T16:02:31Z
- **Merged**: 2025-06-25T16:02:30Z
- **Closed**: 2025-06-25T16:02:30Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 2   **+801 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Implement DRA support in Kueue

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
KEP for https://github.com/kubernetes-sigs/kueue/issues/2941

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-02T19:56:03Z

Hi @alaypatel07. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-02T19:56:12Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | dc4fd9ae77152e8ab1ea739a69d8d2b6c929f9c6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/685c1510507d970008f42604 |

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-02T20:00:39Z

replaces #3071

Thanks @kannon92 for getting us started on this design.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-05-03T01:24:28Z

/ok-to-test

### Comment by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-03T15:10:21Z

/cc

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T18:26:30Z

@nojnhuh thanks for the review, I have pushed an update addressing all the comments, PTAL

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-08T19:42:40Z

This design was discussed in wg-batch meeting, on 05/08. Some important items to look at from the conversation is as follows:

- [x] need to explore if the definition of quota can be put in ResourceFlavor rather than in ClusterQuota
- [x] address a potential race condition that could happen between a workload being admitted and its resourceclaims being created. One solution could be to pull in the resource claims in the workload object, Kueue will keep track of what was admitted
- [ ] how to handle resource claims that are already allocated

### Comment by [@kannon92](https://github.com/kannon92) — 2025-05-08T19:45:28Z

cc @mwielgus @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-30T06:43:39Z

@alaypatel07 thank you for addressing the comments, please make sure the verify passes.

Sorry, I was busy finalizing the 0.12 release + some follow ups. I'm aiming to book time next week for a more detailed review.

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-04T05:22:42Z

@mimowo gentle ping, can you please take a look, I have updated the design to include all the feedback I have received so far.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-04T18:22:44Z

Ok, let me start with some exploratory questions. They might be naive, but I'm learning DRA as we go with the review:

1. do we require that the Job's `resourceClaims.name` matches one of resources listed in `ClusterQueue.ResourceGroups.Flavors.CoveredResources`?

If so, this is probably fine, but it might be worth noting in Notes section, because it might be constraining to users migrating Jobs into Kueue, because IIUC there is no such requirement in plain DRA (they could freely rename `resourceClaims.name` as xyz as long as it matches `resources.claims.name`. 

2. Why do we introduce ResourceFlavor.dynamicResources.deviceClassNames?

Currently Kueue operates under the assumption that the sets of nodes corresponding to different flavors are disjoint by setting `ResourceFlavor.nodeLabels`. Is this an attempt to relax this assumption in case of DRA, or it is needed also under this assumption?

3. Assuming we introduce deviceClassNames to support overlapping nodes, why is this a list?

IIUC this is introduced to separate counting of quota on overlapping nodes, say I have nodes with devices A and B, then `deviceClassName` should be enough. Is this API built for use cases when RF1 says nodes with devices "A", while RF says devices B or C?

4. Could quota counted at the device level not be enough due to CELs for ResourceClaims?

Say I have a lot of devices A, and quota for device A, but I have two Jobs which create ResouceClaims pointing (with the CEL expressions) to a specific subset of the device A. Then, the two jobs may not be able to run at the same time, right?

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-04T19:38:10Z

@mimowo  answering the questions inline:

> do we require that the Job's resourceClaims.name matches one of resources listed in ClusterQueue.ResourceGroups.Flavors.CoveredResources?
If so, this is probably fine, but it might be worth noting in Notes section, because it might be constraining to users migrating Jobs into Kueue, because IIUC there is no such requirement in plain DRA (they could freely rename resourceClaims.name as xyz as long as it matches resources.claims.name.

No, job's resourceClaims.name is not supposed to be listed in `ClusterQueue.ResourceGroups.Flavors.CoveredResources`. The job.ResourceClaims of the job will all resolve to their respective deviceClass. The ResourceFlavor's `dynamicResources` field maps these device class names to canonical names, and the canonical name that contains the requested device class in its device class names should be present in the `ClusterQueue.ResourceGroups.Flavors.CoveredResources`.

> Why do we introduce ResourceFlavor.dynamicResources.deviceClassNames?
Currently Kueue operates under the assumption that the sets of nodes corresponding to different flavors are disjoint by setting ResourceFlavor.nodeLabels. Is this an attempt to relax this assumption in case of DRA, or it is needed also under this assumption?

This is not an attempt to relax the disjoint nodes assumption. It's needed because unlike core resources (CPU, memory) which are well-defined standard units, DRA resources represent abstract user requests that can vary dramatically - from "give me all GPUs on a node" to "give me a small GPU slice." Device classes serve as blueprints that define the characteristics of these varied requests. Since different device classes can represent similar quota consumption patterns (e.g., different implementations of shared GPUs), the deviceClassNames field allows cluster admins to group these varied device class blueprints under canonical names that form an abstract resource in Kueue for consistent quota enforcement.

> Assuming we introduce deviceClassNames to support overlapping nodes, why is this a list?
IIUC this is introduced to separate counting of quota on overlapping nodes, say I have nodes with devices A and B, then deviceClassName should be enough. Is this API built for use cases when RF1 says nodes with devices "A", while RF says devices B or C?

Actually, `deviceClassNames` is not primarily introduced to support overlapping nodes. 

In DRA, users can provide configuration along with their device request, specifying different consumption patterns for the same underlying resource - like requesting 10 timeslices vs 10 space partitions of a GPU. Even though both consume the same physical resource, they're represented by different device classes (e.g., gpu-timeslice.example.com vs gpu-spatial.example.com).

The deviceClassNames list allows grouping these different consumption patterns under a single canonical name for quota purposes, since they should count against the same quota pool despite having different device classes. This supports the DRA flexibility where the how of resource consumption can vary even when the what (underlying resource) is the same.
If cluster admins prefer single-entry lists as a best practice, they can enforce that constraint through admission policies rather than limiting the API design.

> Could quota counted at the device level not be enough due to CELs for ResourceClaims?
Say I have a lot of devices A, and quota for device A, but I have two Jobs which create ResouceClaims pointing (with the CEL expressions) to a specific subset of the device A. Then, the two jobs may not be able to run at the same time, right?

@mimowo I think we need to define what you mean by subset here, taking different meaning of subsets,

If "subset" means different physical devices of class A:
Job 1: CEL targets devices A1, A2, A3
Job 2: CEL targets devices A4, A5, A6
No conflict - both jobs can run simultaneously since they target different physical devices

If "subset" means overlapping device selections:
Job 1: CEL targets devices with memory >= 40GB
Job 2: CEL targets devices with compute >= 20GB
Potential conflict - if some devices match both criteria, scheduler may not be able to satisfy both

The fundamental issue is that Kueue cannot pre-determine which specific devices will satisfy the CEL expressions before scheduler allocation, so it counts conservatively at the device class level. Whether this causes actual conflicts depends on whether the CEL expressions create overlapping or mutually exclusive device selections.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:42:25Z

> > do we require that the Job's resourceClaims.name matches one of resources listed in ClusterQueue.ResourceGroups.Flavors.CoveredResources?
> > If so, this is probably fine, but it might be worth noting in Notes section, because it might be constraining to users migrating Jobs into Kueue, because IIUC there is no such requirement in plain DRA (they could freely rename resourceClaims.name as xyz as long as it matches resources.claims.name.
> 
> No, job's resourceClaims.name is not supposed to be listed in `ClusterQueue.ResourceGroups.Flavors.CoveredResources`. The job.ResourceClaims of the job will all resolve to their respective deviceClass. The ResourceFlavor's `dynamicResources` field maps these device class names to canonical names, and the canonical name that contains the requested device class in its device class names should be present in the `ClusterQueue.ResourceGroups.Flavors.CoveredResources`.

So IIUC, Kueue, instead of looking into Job's `resourceClaims.name` will follow the ResourceClaimTemplate (separete object instance), and we will get the device class name from `resourceClassName`, then we will resolve the canonical resource name by "reverse" lookup inside the ResourceFlavor's dynamicResources. One issue I see if there are multiple dynamicResources inside the RF using the same device class name, eg.:

```yaml
dynamicResources:
- name: res-a1
  deviceClassNames: ["a"]
- name: res-a2
  deviceClassNames: ["a"]
```
are there any use cases for this setup? If no, we may block it at the validation level. Then I see how the lookup could work.

At some point we should clarify the nuances in the API comments for the relevant fields.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:46:28Z

> > Why do we introduce ResourceFlavor.dynamicResources.deviceClassNames?
> > Currently Kueue operates under the assumption that the sets of nodes corresponding to different flavors are disjoint by setting ResourceFlavor.nodeLabels. Is this an attempt to relax this assumption in case of DRA, or it is needed also under this assumption?
> 
> This is not an attempt to relax the disjoint nodes assumption. It's needed because unlike core resources (CPU, memory) which are well-defined standard units, DRA resources represent abstract user requests that can vary dramatically - from "give me all GPUs on a node" to "give me a small GPU slice." Device classes serve as blueprints that define the characteristics of these varied requests. Since different device classes can represent similar quota consumption patterns (e.g., different implementations of shared GPUs), the deviceClassNames field allows cluster admins to group these varied device class blueprints under canonical names that form an abstract resource in Kueue for consistent quota enforcement.

Sure, but if we have 1:1 mapping between dynamicResources.name and deviceClassName (singleton), then the same consumption patterns could be expressed at the level of `dynamicResources.name`. 

Still, I see the need to have deviceClassNames related to the lookup for canonical resource names, based on the device class name declared in ResourceClaimTemplate, related to the previous comment. So, I see the need for declaring `deviceClassName(s)`.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:50:22Z

> > Could quota counted at the device level not be enough due to CELs for ResourceClaims?
> > Say I have a lot of devices A, and quota for device A, but I have two Jobs which create ResouceClaims pointing (with the CEL expressions) to a specific subset of the device A. Then, the two jobs may not be able to run at the same time, right?
> 
> @mimowo I think we need to define what you mean by subset here, taking different meaning of subsets,
> 
> If "subset" means different physical devices of class A: Job 1: CEL targets devices A1, A2, A3 Job 2: CEL targets devices A4, A5, A6 No conflict - both jobs can run simultaneously since they target different physical devices
> 
> If "subset" means overlapping device selections: Job 1: CEL targets devices with memory >= 40GB Job 2: CEL targets devices with compute >= 20GB Potential conflict - if some devices match both criteria, scheduler may not be able to satisfy both
> 
> The fundamental issue is that Kueue cannot pre-determine which specific devices will satisfy the CEL expressions before scheduler allocation, so it counts conservatively at the device class level. Whether this causes actual conflicts depends on whether the CEL expressions create overlapping or mutually exclusive device selections.

I see. What I meant implicitly by "subset" here is the subset of nodes which are in the comment overlap, so "If "subset" means overlapping device selections: Job". 

However, I think this is limitation of Kueue model of counting quota. It also exists outside of DRA. For example, currently a user may have two workloads which are assigned to the same flavor (say zone-a), but they cannot both run (even though quota allowed), because the user used some very specific node selector, say `nodeSelector: rack=zone-a-rack1`.

I believe this problem can be tackled by Kueue when we have TopologyAwareScheduling, which is actually taking into account the physical capacity, not just quota.

So, I'm ok with this limitation in the current proposal, this is not a new limitation conceptually in the model Kueue is using.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T13:01:06Z

> > Assuming we introduce deviceClassNames to support overlapping nodes, why is this a list?
> > IIUC this is introduced to separate counting of quota on overlapping nodes, say I have nodes with devices A and B, then deviceClassName should be enough. Is this API built for use cases when RF1 says nodes with devices "A", while RF says devices B or C?
> 
> Actually, `deviceClassNames` is not primarily introduced to support overlapping nodes.
> 
> In DRA, users can provide configuration along with their device request, specifying different consumption patterns for the same underlying resource - like requesting 10 timeslices vs 10 space partitions of a GPU. Even though both consume the same physical resource, they're represented by different device classes (e.g., gpu-timeslice.example.com vs gpu-spatial.example.com).
> 
> The deviceClassNames list allows grouping these different consumption patterns under a single canonical name for quota purposes, since they should count against the same quota pool despite having different device classes. This supports the DRA flexibility where the how of resource consumption can vary even when the what (underlying resource) is the same. If cluster admins prefer single-entry lists as a best practice, they can enforce that constraint through admission policies rather than limiting the API design.

I see, so potentially we may have "gpu" as the canonical name resource, but represented as two distinct "deviceClasses", say gpu-time-sliced and gpu-mig-sliced. Then, the admin may want to count both usage patterns under "gpu" resource. This sounds like a plausible use case. If it does not cost much from maintenance PoV. I guess I will need to see in the actual implementation PR, but tentatively we can assume supporting it (IMO).

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-05T13:39:55Z

> I see, so potentially we may have "gpu" as the canonical name resource, but represented as two distinct "deviceClasses", say gpu-time-sliced and gpu-mig-sliced. Then, the admin may want to count both usage patterns under "gpu" resource. This sounds like a plausible use case. If it does not cost much from maintenance PoV. I guess I will need to see in the actual implementation PR, but tentatively we can assume supporting it (IMO).

@mimowo Correct, this is not possible if you have a mapping with single deviceClassName.

This is the POC [commit](https://github.com/kubernetes-sigs/kueue/pull/5151/commits/de0dfde000792f26eaa62631e8716a714c6ffa22) that moved this design from single deviceClassName to a list of deviceClassNames. I am sure the actual implementation will be more nuanced, but it gives you a rough idea of the implementation.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-06T08:12:18Z

@alaypatel07 please also look back and resolve addressed comments without open questions, to avoid distractions.

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-10T19:15:02Z

> LGTM overall. I think the remaining open question is if the API starts with "Alpha" or "Beta". I would probably start with Alpha since the feature is expected to evolve as the core DRA evolves.

It was an oversight on my side, the API should start with alpha, good catch @mimowo I will update the document.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-13T08:59:50Z

no release note needed since this is KEP
/remove-kind feature
/kind documentation
/release-note-edit
```release-note
NONE
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-20T19:39:56Z

LGTM label has been added.  <details>Git tree hash: 00b347b5851adacd3ab80d2ee72dbe268121ce20</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T04:28:45Z

> /lgtm /approve Thank you 👍 I think this is good enough to kick off the implementation as Alpha.
> 
> /hold To give more time for review / feedback for @tenzen-y and others.

ACK, let me check this one, today.

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T14:18:55Z

@tenzen-y Please take another run at reviewing this PR, I have addressed all your comments.

### Comment by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T17:56:55Z

fixed the nits @mimowo  👍

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-25T10:57:15Z

/lgtm
Thanks for the updates after the latest comments. Keeping on hold for @tenzen-y possibly additional comments.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-25T10:57:21Z

LGTM label has been added.  <details>Git tree hash: 6db49499ec4a146a0eb52f77588831186f83c719</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-25T13:50:03Z

> The most important question is whether or not we should care the order in the `.spec.resources` and `.spec.resources[*].deviceClassNames`.

I don't think we need to be careful about the order. I'm ok with using map.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T15:23:07Z

@alaypatel07 If you apply `set` patch strategy, we can merge this one.
Otherwise LGTM.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-25T15:29:36Z

LGTM label has been added.  <details>Git tree hash: ad8e8c62e4a1dbcfb481bee7dd50f03896dff790</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-25T15:29:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5149#" title="Author self-approved">alaypatel07</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5149#pullrequestreview-2947131895" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5149#pullrequestreview-2958666747" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-05-06T20:06:04Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T15:53:35Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
```

  ```suggestion
     namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
  ```

- **keps/2941-DRA-Structured-Parameters/README.md:262** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T15:56:22Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
```

  ```suggestion
     1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
  ```

- **keps/2941-DRA-Structured-Parameters/README.md:391** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T15:57:07Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing. 
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### Resource Claim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota. 
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
+
+### Using devices in ResourceSlice to Count
+
+DRA drivers publish resources for each node, which could be used as a mechanism for counting resources. However, in DRA
+implementation, ResourceSlices are used for driver/scheduler communication. The only way users can request dynamic 
+resources is via ResourceClaims. ResourceClaims does not have the notion of what devices will be allocated apriori. 
```

  ```suggestion
  resources is via ResourceClaims. ResourceClaims does not have the notion of what devices will be allocated a priori. 
  ```

- **keps/2941-DRA-Structured-Parameters/README.md:190** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T16:27:52Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
```

  Could Kueue theoretically track the number of devices in each resource pool and assume the worst-case scenario is that it lands on the pool with the most devices (if that's less than the max for a claim)? Is that at least something that could be done later without causing compatibility issues?
  
  Can we also mention exactly where the Kubernetes control plane defines "the max number of devices that could allocated to a single claim" so it's easier to update here if that changes?

- **keps/2941-DRA-Structured-Parameters/README.md:185** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T18:19:15Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
```

  ```suggestion
     counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
  ```

- **keps/2941-DRA-Structured-Parameters/README.md:189** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T18:20:07Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
```

  ```suggestion
  3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
  ```

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T18:25:51Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  Does this also need to include `kind: DeviceClass`?

- **keps/2941-DRA-Structured-Parameters/README.md:200** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T18:34:12Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
```

  ```suggestion
      // +featureGate=KueueDynamicResourceAllocation
  ```

- **keps/2941-DRA-Structured-Parameters/README.md:207** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T18:35:32Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
```

  ```suggestion
      // +featureGate=KueueDynamicResourceAllocation
  ```

- **keps/2941-DRA-Structured-Parameters/README.md:212** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T18:40:58Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
```

  How likely is it that declaring quota for DRA resources will expand beyond device class, such as by matching specific pools or attributes? Is the API described here amenable enough to those kinds of changes?
  
  I suppose Kueue admins could create their own DeviceClasses to more flexibly scope quotas. If that's the intent behind this KEP, can we note that?
  
  Creating custom device classes isn't ideal though since that would require workloads to know about which Kueue admin-provided DeviceClasses exist ahead of time to declare those in ResourceClaims.

- **keps/2941-DRA-Structured-Parameters/README.md:213** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T18:43:37Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
```

  Since `DeviceField` isn't a "kind" per se like `DeviceClass`, is there another name we could choose for the field to avoid that confusion?

- **keps/2941-DRA-Structured-Parameters/README.md:201** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T18:54:04Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
```

  Can we make this field an enum type and list all the valid values?

- **keps/2941-DRA-Structured-Parameters/README.md:237** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T19:03:47Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
```

  How does a DRA device's `capacity` fit in here? If my driver declares GPUs with a `memory` capacity, can the `nominalQuota` operate against that? e.g. if an admin wants to enforce that "this namespace can use 100GB of GPU memory" no matter how many GPUs or which specific model of GPU that would take.

- **keps/2941-DRA-Structured-Parameters/README.md:259** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T19:07:10Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
```

  Unfortunately this second line appears on the same line as the first one in the rendered view. Is there a way we can rearrange this to look a bit better there?

- **keps/2941-DRA-Structured-Parameters/README.md:287** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T19:33:02Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
```

  These comments in the previous KEP made mapping back to the steps listed above easier for me but now that's lost. Can we update the comments here to make it easier to follow the current steps described above?

- **keps/2941-DRA-Structured-Parameters/README.md:357** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T19:36:13Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
```

  The Kubernetes test driver is purpose-built for the e2e framework there (it doesn't even run in a Pod), so it probably won't be easy to integrate into a separate project like Kueue. I'd recommend going as far as possible with the dra-example-driver before trying the test driver.

- **keps/2941-DRA-Structured-Parameters/README.md:385** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T19:41:51Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing. 
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### Resource Claim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota. 
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
```

  I'm not familiar with the details here, but how is this different from how pods currently declare cpu/memory/etc. requests? Pods are namespace-scoped, so why is it an issue that ResourceClaims are namespace-scoped?

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T19:49:00Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  What exactly does this nominalQuota represent for DeviceClasses?

- **keps/2941-DRA-Structured-Parameters/README.md:293** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T19:50:35Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
```

  How will references to ResourceClaims be accounted differently from references to ResourceClaimTemplates?

- **keps/2941-DRA-Structured-Parameters/README.md:185** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-06T20:01:44Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
```

  Is "Kubernetes ResourceQuota" here referring to the [core/v1 ResourceQuota](https://kubernetes.io/docs/reference/kubernetes-api/policy-resources/resource-quota-v1/) type?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-06T22:52:46Z

- **keps/2941-DRA-Structured-Parameters/README.md:190** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-06T22:52:46Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
```

  I raised something like this in the wg-device-management meeting, the ResourceQuota implementation in k8s does not do this because its more work.  However, unlike ResourceQuota, kueue is not an admission time quota decision, Kueue could make the worse case scenario more narrow, but it will still be an overestimation for a moment. Kueue can correct the usage once the DRA allocation is made. 
  
  > Could Kueue theoretically track the number of devices in each resource pool and assume the worst-case scenario is that it lands on the pool with the most devices (if that's less than the max for a claim)? Is that at least something that could be done later without causing compatibility issues?
  
  The max size is here: https://github.com/kubernetes/kubernetes/blob/1cc003ae4880c040e3ec47f5afcb0bf013d6a0e3/pkg/apis/resource/types.go#L1211, will update the doc with it.
  
  > Can we also mention exactly where the Kubernetes control plane defines "the max number of devices that could allocated to a single claim" so it's easier to update here if that changes?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-06T22:55:13Z

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-06T22:55:13Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  good catch, last line seems to have got dropped, will update

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-07T17:36:30Z

- **keps/2941-DRA-Structured-Parameters/README.md:212** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T17:36:30Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
```

  > How likely is it that declaring quota for DRA resources will expand beyond device class, such as by matching specific pools or attributes? Is the API described here amenable enough to those kinds of changes?
  
  > I suppose Kueue admins could create their own DeviceClasses to more flexibly scope quotas. If that's the intent behind this KEP, can we note that?
  
  The intent in the KEP is for admins to create their own DeviceClasses. There could be an evolution to the API where instead of specifying device class names, admins can provide a `deviceClassLabelSelector` to make it easier for users to discover the Kueue admin-provided Deviceclasses.
  
  > How likely is it that declaring quota for DRA resources will expand beyond device class, such as by matching specific pools or attributes? Is the API described here amenable enough to those kinds of changes?
  
  Looking at the current DRA APIs that are in beta and are planned for in GA, I do not see quota management to be possible for anything except deviceclasses. Reason being, for quota management system to work, users should be able to request the resources for the workloads.  The only concrete way to request resources in DRA that a system like kueue can track is through a device class. As an example, say kueue wants to track based on an attribute `foo` the value for which is `1` for a device `foo-1` as advertised by the driver in resource slice. The attribute and its value will only be associated to a workload when the dra scheduler assigns a device to it. For kueue to admit the workload, this discovery needs to happen well before dra scheduler assigns a device to the workload.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-07T17:37:25Z

- **keps/2941-DRA-Structured-Parameters/README.md:213** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T17:37:25Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
```

  it is a typo, I meant this to be `field is configured to be DeviceClass`

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-07T17:41:50Z

- **keps/2941-DRA-Structured-Parameters/README.md:237** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T17:41:50Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
```

  > How does a DRA device's capacity fit in here? If my driver declares GPUs with a memory capacity, can the nominalQuota operate against that? 
  
  IMO this is not possible until workload/DRA APIs have a declarative way to request Capacity that system like kueue can read.  Currently with DRA APIs, this is achieved by writing a CEL expression encapsulated in a DeviceClass which makes it hard for a system like kueue to track and tally.
  
  More detailed explanation is in this comment: https://github.com/kubernetes-sigs/kueue/pull/5149#discussion_r2078147309

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-07T17:57:56Z

- **keps/2941-DRA-Structured-Parameters/README.md:385** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T17:57:56Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing. 
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### Resource Claim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota. 
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
```

  Even if pods are namespace-scoped, but the resources requested by pods which are tallied for quota are expressed on cluster-scoped node objects. Because ResourceClaims/ResourceClaimTemplates are namespace-scoped, they could be defined by the user to take any shape/form. In order to affectively manage quota's a cluster-admin should have control over what kind of quotas needs to be defined and set up for each namespace/set of namespaces.
  
  @kannon92 please add more thoughts if I missed anything here.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-07T18:04:16Z

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T18:04:16Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  nominal quota represents the count of devices that got allocated for any of the device classes specified in deviceClassNames.  I am trying to think what would be the right place in the KEP to clarify this?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-07T18:07:24Z

- **keps/2941-DRA-Structured-Parameters/README.md:293** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T18:07:24Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
```

  Good question, from user perspective, it shouldnt matter, they are the same.  From implementation perspective, I think I need to add necessary steps to prevent double counting resource claims that are already accounted for by a running workload.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-07T18:07:42Z

- **keps/2941-DRA-Structured-Parameters/README.md:185** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T18:07:42Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
```

  yes.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-07T18:17:39Z

- **keps/2941-DRA-Structured-Parameters/README.md:201** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T18:17:39Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
```

  We can definitely do that if more `Kind` will need to be supported.  I am almost on the fence of needing this field at all.  The presence of `deviceClassNames` itself can signify this is a DRA resource. Will let the maintainers chime in here for guidance on the API

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-07T18:21:01Z

- **keps/2941-DRA-Structured-Parameters/README.md:357** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-07T18:21:01Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
```

  This is most likely an implementation detail as to what is easier/better.  I know atleast one other project https://github.com/kubevirt/enhancements/pull/11/files#diff-ca56d7cf7c1e00e1a9f18d95a66ad56535d734121d47fcddb0831dca511cc506R734 that requires a test driver.

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-05-07T18:49:59Z

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-07T18:49:59Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  Cool, that seems like a reasonable way to account for DRA resources.
  
  I wonder if in the first paragraph under "Proposal" might be a good place to mention this? Since that's pretty fundamental to understanding the rest of the KEP, it should probably be as close to the top as possible. Or at least near where implementation details start being discussed.

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-05-07T20:04:03Z

- **keps/2941-DRA-Structured-Parameters/README.md:385** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-07T20:04:03Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing. 
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### Resource Claim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota. 
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
```

  Maybe I'm not looking at things quite right but it seems like DRA resources and core resources are comparable in this way:
  
  > Even if pods are namespace-scoped, but the resources requested by pods which are tallied for quota are expressed on cluster-scoped node objects.
  
  Likewise, DRA resources requested by pods are expressed on cluster-scoped ResourceSlice objects.
  
  > Because ResourceClaims/ResourceClaimTemplates are namespace-scoped, they could be defined by the user to take any shape/form.
  
  To me, this seems to be the same as for pods requesting regular resources like cpu and memory.
  
  > In order to affectively manage quota's a cluster-admin should have control over what kind of quotas needs to be defined and set up for each namespace/set of namespaces.
  
  Isn't this fulfilled by Kueue's ClusterQuota resource?
  
  (To clarify, I'm not trying to advocate for this approach, just looking to understand the limitation.)

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-05-07T20:21:35Z

- **keps/2941-DRA-Structured-Parameters/README.md:212** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-07T20:21:35Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
```

  Ah, I think I understand now. Thanks!

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-05-07T20:27:36Z

- **keps/2941-DRA-Structured-Parameters/README.md:237** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-07T20:27:36Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
```

  Makes sense. It looks like the missing piece is in [KEP-5075: DRA: Consumable Capacity](https://github.com/kubernetes/enhancements/pull/5104) which will [modify ResourceClaims](https://github.com/kubernetes/enhancements/blob/a31f4e54094d27d5f7b30b4ccdc8ebcce04b372e/keps/sig-scheduling/5075-dra-consumable-capacity/README.md#resourceclaimspecs-devicerequest) to make that possible.

### Review by [@johnbelamaric](https://github.com/johnbelamaric) (commented) — 2025-05-07T20:32:10Z

- **keps/2941-DRA-Structured-Parameters/README.md:60** by [@johnbelamaric](https://github.com/johnbelamaric) — 2025-05-07T20:32:09Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
```

  We should also be sure to think about multi-node resources, which will be shared by pods running on potentially different nodes. They could be network-accessible devices or logical devices. One example is TPU subslicing, see: https://github.com/kubernetes/enhancements/tree/master/keps/sig-scheduling/4815-dra-partitionable-devices#multi-host-tensor-processing-unit-tpu-scheduling
  
  In the past @klueska has considered using this type of resource for NVIDIA IMEX channels but he ultimately chose a different route that doesn't use multi-node resources. Nonetheless, we need to be sure to support it in Kueue.
  
  For these multi-node resources, we are also looking into adding support for Resource Claim Templates for LWS and JobSet, such that for each statefulset (LWS) or replicated job (JobSet), you would instantiate one resource claim and inject that in the claims list of every pod in the workload.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-07T20:35:01Z

- **keps/2941-DRA-Structured-Parameters/README.md:60** by [@kannon92](https://github.com/kannon92) — 2025-05-07T20:35:00Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
```

  IMO maybe we can consider this as a separate KEP once LWS and JobSet have this implementation drafted.
  
  In general, I imagine that this ResourceClaim would still point to a deviceClass though? So I think this should be fine.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-07T20:37:28Z

- **keps/2941-DRA-Structured-Parameters/README.md:154** by [@kannon92](https://github.com/kannon92) — 2025-05-07T20:37:28Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
```

  Is this non goal correct? You mention support for this later on in the doc.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-07T20:40:13Z

- **keps/2941-DRA-Structured-Parameters/README.md:357** by [@kannon92](https://github.com/kannon92) — 2025-05-07T20:40:13Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
```

  Let's just use dra-example-driver. Thanks to @nojnhuh, there is a helm chart to install this and it may be easier to support this than the e2e framework.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-07T20:41:12Z

- **keps/2941-DRA-Structured-Parameters/README.md:381** by [@kannon92](https://github.com/kannon92) — 2025-05-07T20:41:11Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  Can you fill this out?
  
  Will we graduate this feature once DRA structured paramters is ga?

### Review by [@johnbelamaric](https://github.com/johnbelamaric) (commented) — 2025-05-07T20:42:00Z

- **keps/2941-DRA-Structured-Parameters/README.md:60** by [@johnbelamaric](https://github.com/johnbelamaric) — 2025-05-07T20:42:00Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
```

  Yes, it will still point to a device class. So, in some sense it may look similar to other "shared claims". We can take it piece-by-piece; just be aware that multi-node resourceslices have been part of the API from day one.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-07T20:43:25Z

- **keps/2941-DRA-Structured-Parameters/README.md:385** by [@kannon92](https://github.com/kannon92) — 2025-05-07T20:43:25Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing. 
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### Resource Claim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota. 
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
```

  > Isn't this fulfilled by Kueue's ClusterQuota resource?
  
  Yes. The "ResourceQuota" api is part of the ClusterQueue resource for Kueue.
  
  ```yaml
  ---
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "cluster-queue"
  spec:
    namespaceSelector: {} # match all.
    resourceGroups:
      - coveredResources: ["cpu", "memory", "single-gpus"]
        flavors:
          - name: "default-flavor"
            resources:
              - name: "cpu"
                nominalQuota: 9
              - name: "memory"
                nominalQuota: "200Mi"
              - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
                deviceClassNames: ["gpu.example.com"]
                nominalQuota: 2
                kind: "DeviceClass"
  ```

### Review by [@johnbelamaric](https://github.com/johnbelamaric) (commented) — 2025-05-07T20:45:12Z

- **keps/2941-DRA-Structured-Parameters/README.md:381** by [@johnbelamaric](https://github.com/johnbelamaric) — 2025-05-07T20:45:12Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  FYI target for DRA structured parameters GA is still 1.34. Most other KEPs will remain at their current stage (beta for resource claim status which you likely don't care about, alpha for everything else). The one exception may be the admin claim KEP may move to beta, we will discuss next week at the WG meeting. If it plays out how I expect/hope, 1.34 will look like: GA of structured parameters, continue resource claim status in beta, admin access to beta, and a whole bunch of continuing & new alphas.
  
  From a Kueue perspective, the GA matters, and the fact that admin access will be a default on beta matters.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-07T20:45:26Z

- **keps/2941-DRA-Structured-Parameters/README.md:385** by [@kannon92](https://github.com/kannon92) — 2025-05-07T20:45:26Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing. 
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### Resource Claim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota. 
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
```

  ClusterQueue is a cluster scoped resource setting up quota limits. And users bind to that resource via a LocalQueue.
  
  An Admin sets cluster quota limits on the ClusterQueue. So that is why we are putting deviceClassName here.
  
  I was told that a ResourceSlice is targeted for a dra device driver.  It didn't make sure to limit resources on a ResourceSlice. 
  
  cc @pohly @johnbelamaric @klueska

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-07T20:50:43Z

- **keps/2941-DRA-Structured-Parameters/README.md:381** by [@kannon92](https://github.com/kannon92) — 2025-05-07T20:50:42Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  The challenge with all these projects (LWS, JobSet, Kueue) is that they are supported on all supported k8s versions (1.32, 1.33, 1.34). And in the wild we even have people running Kueue on older versions of K8s than that (last I heard was 1.28/1.29). So I'd like the code to be smart enough to detect these kind of skews if we use something like DRA?

### Review by [@johnbelamaric](https://github.com/johnbelamaric) (commented) — 2025-05-07T20:51:18Z

- **keps/2941-DRA-Structured-Parameters/README.md:385** by [@johnbelamaric](https://github.com/johnbelamaric) — 2025-05-07T20:51:18Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing. 
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### Resource Claim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota. 
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
```

  I am not sure I really understand the question here, but maybe this clarifies things:
  
  1) Agreed, namespace-scoped resource claims consume from cluster-scoped resource slices, analogous to standard pod resources consuming from cluster-scoped nodes.
  2) Quota in DRA today is per device class, that's all we know how to do right now (https://github.com/kubernetes/enhancements/issues/4840).
  3) Resource slices are only writeable by on-node DRA drivers generally, though for certain types of resources there may be a controller instead of the on-node driver. But in general they are not writeable by ordinary users.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-08T00:32:57Z

- **keps/2941-DRA-Structured-Parameters/README.md:60** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-08T00:32:57Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
```

  IMO, as long as multi-node resources can be uniquely counted via a device class configured by the admin, it could potentially be supported by this design. 
  
  The implementation will have to make sure there is no double counting for multiple pods using the shared multi-node resource, but it is no different than preventing double counting for multiple pod sharing a resource claim for resources in the same node.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-08T00:36:16Z

- **keps/2941-DRA-Structured-Parameters/README.md:154** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-08T00:36:16Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
```

  My intention was to clarify that this design has the path to support these features once they make it to beta, without any API changes.
  
  I am unsure if Kueue can support alpha features given that there is a (very small indeed) chance that the API/implementation could potentially change once they are in beta.
  
  But I am open to ideas if there is a better way of dealing with these features.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-08T01:04:48Z

- **keps/2941-DRA-Structured-Parameters/README.md:381** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-08T01:04:48Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  > Can you fill this out?
  
  > Will we graduate this feature once DRA structured paramters is ga?
  
  I will take a stab at it, it seems to me that support of DRA in kueue should be independent of DRA structured parameters going GA.
  
  > From a Kueue perspective, the GA matters, and the fact that admin access will be a default on beta matters.
  
  @johnbelamaric Is the expectation here that when Structured Parameters goes to GA, it should be supported in Kueue out of the box (iow this feature in Kueue should be in beta at minimum by then)?
  
  > The challenge with all these projects (LWS, JobSet, Kueue) is that they are supported on all supported k8s versions (1.32, 1.33, 1.34). And in the wild we even have people running Kueue on older versions of K8s than that (last I heard was 1.28/1.29). So I'd like the code to be smart enough to detect these kind of skews if we use something like DRA?
  
  @kannon92 this is a tall ask.  
  
  - 1.30 had DRA APIs in v1alpha2, which was removed in 1.31: https://pkg.go.dev/k8s.io/kubernetes@v1.30.2/pkg/apis/resource#section-directories
  - 1.31 has DRA APIs in v1alpha3: https://pkg.go.dev/k8s.io/kubernetes@v1.31.2/pkg/apis/resource#section-directories
  - 1.32 has DRA APIs in v1alpha3 and v1beta1: https://pkg.go.dev/k8s.io/kubernetes@v1.32.2/pkg/apis/resource#section-directories
  - 1.33 has DRA APIs in v1alpha3, v1beta1 and v1beta2: https://pkg.go.dev/k8s.io/kubernetes@v1.33.0/pkg/apis/resource#section-directories
  
  So Kueue can potentially import the v1alpha3, https://github.com/kubernetes-sigs/kueue/pull/5151/files#diff-d6b37b9e32fc2e214d8cb094a9c9f1607df91930402addb50f8691f888888843R27, to make sure the code can run in all three versions (1.31, 1.32, 1.33). This would mean only the core structured parameters APIs will be available, all the other features (for example partitionable devices) which are in alpha now will not have the API available in v1alpha3 for kueue to support. 
  
  Alternatively, queue can use https://pkg.go.dev/k8s.io/client-go/dynamic with unstructured type to make sure kueue can read any/all the versions of DRA API in the cluster, but this will make the implementation much more complex.

### Review by [@everpeace](https://github.com/everpeace) (commented) — 2025-05-08T01:31:11Z

- **keps/2941-DRA-Structured-Parameters/README.md:208** by [@everpeace](https://github.com/everpeace) — 2025-05-08T01:27:39Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
```

  Should we support setting quota by device class AND selector because `ResourceClaim` can specify them?
  
  IIUC, with this current KEP that supports only deviceclasses in ResourceQuota, Kueue admin might have to create new deviceclass for narrowing set of target devices in existing device class for setting quota. Moreover, when existing users use the old device classes, they might have to migrate to the new device class. For example,
  
  - Assume:
    - `gpu.example.com` deviceclass exists, and each device has device attribute "memory"
    - there are existing users who have resourceclaims with the deviceclass and selector like this:
      ```yaml
      kind: ResourceClaim
      name: one-large-gpu
      spec:
        requests:
        - name: gpu-large
          deviceClassName: gpu.example.com
          selectors:
          - cel:
              expression: device.attributes["memory"] >= 80g
      ```
  - In this situation, if Kueue admin wants to set quota for `gpu.example.com` devices with `device.attribute["memory"]>=80g`, Kueue admin might have to create a device class and use the new device class in clusterqueue:
    ```yaml
    kind: DeviceClass
    name: large-gpu.example.com
    spec:
      selectors:
      - cel:
          expression: device.driver == "gpu.example.com" && device.attributes["memory"] >= 80g
    ---
    kind: ClusterQueue
    ...
        flavors:
          - name: "default-flavor"
            resources:
              - name: 'large-gpus'
                kind: "DeviceClass"
                deviceClassNames:
                - "large-gpu.example.com"
                nominalQuota: 2  
    ```
  - then, existing users might have to migrate/switch their ResourceClaim with `large-gpu.example.com` device class from existing one.
  
  Is this my understanding correct or am I missing some discussion context? If correct, for minimizing user impact, how about supporting setting quota by device class AND selector like this? 
  
  ```yaml
  # in ResoruceQuota
  ...
  # using "Device" instead of "DeviceClass"
  # (kubernetes does not have "kind: Device", though...)
  kind: Device
  nominalQuota: 2
  devices:
    # We might be able to extend this object to support 
    # partitionable devices, etc. in the future??
  - className: gpu.example.com
    selectors:
    - cel:
        expression: device.attributes["memory"] >= 80g
  
  # Alternative Idea
  # kind: ResourceClaim
  # nominalQuota: 2
  # claims:
  # - className: gpu.example.com
  #   selectors:
  #   - cel:
  #       expression: device.attributes["memory"] >= 80g
  ```
  
  And, I personally think that quota configuration for DRA(Structured Params) should be symmetric to device requests in `ResourceClass` as much as possible.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-08T01:41:56Z

- **keps/2941-DRA-Structured-Parameters/README.md:381** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-08T01:41:56Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  > So Kueue can potentially import the v1alpha3, https://github.com/kubernetes-sigs/kueue/pull/5151/files#diff-d6b37b9e32fc2e214d8cb094a9c9f1607df91930402addb50f8691f888888843R27, to make sure the code can run in all three versions (1.31, 1.32, 1.33). This would mean only the core structured parameters APIs will be available, all the other features (for example partitionable devices) which are in alpha now will not have the API available in v1alpha3 for kueue to support.
  
  Actually I might have misstated this, if Kueue uses v1alpha3, in 1.33 and 1.32, all the new features should be completely roundtrippable from API point of view, hence will be available in v1alpha3. With that Kueue should be able to support 1.31+ k8s versions and determine based on API fields if a particular DRA feature is requested by user or not.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-08T02:10:14Z

- **keps/2941-DRA-Structured-Parameters/README.md:208** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-08T02:10:14Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
```

  > Is this my understanding correct?? If correct, for minimizing user impact, how about supporting setting quota by device class AND selector like this?
  
  yes this understanding is correct. 
  
  ```
  # in ResoruceQuota
  ...
  # using "Device" instead of "DeviceClass"
  # (kubernetes does not have "kind: Device", though...)
  - name: large-gpus
     kind: Device
     devices:
     - className: gpu.example.com
       selectors:
       - cel:
           expression: device.attributes["memory"] >= 80g
  - name: small-gpus
     kind: Device
     devices:
     - className: gpu.example.com
       selectors:
       - cel:
           expression: device.attributes["memory"] < 80g    
  ```
  
  @everpeace can you please provide the workload YAML (say a job object) that Kueue will uniqueuly count against `large-gpus` quota with the above quota configuration?
  
  I think this idea indeed improves the user experience, but it seems to me that with a cel expression like that, whether a device having attributes that evaluates the cel expression to true or not, will only be available after the scheduler selects the device for the claim. Kueue needs to know the device and count it before scheduler makes this decision. But I could be missing something, having a job.yaml will help make it clear.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-08T02:20:34Z

- **keps/2941-DRA-Structured-Parameters/README.md:381** by [@kannon92](https://github.com/kannon92) — 2025-05-08T02:20:33Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  TBH I think its okay to have DRA support >= 1.33 (v1beta2 or v1 for 1.34).
  
  We may just have to think through the failure case if DRA does not have v1beta2 or v1.
  
  For an alpha feature I think we can roll this out in a more conservative way.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-08T02:20:48Z

- **keps/2941-DRA-Structured-Parameters/README.md:381** by [@kannon92](https://github.com/kannon92) — 2025-05-08T02:20:47Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  I think we should discuss our plan in the KEP though.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-08T02:22:35Z

- **keps/2941-DRA-Structured-Parameters/README.md:154** by [@kannon92](https://github.com/kannon92) — 2025-05-08T02:22:35Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
```

  This sounds good to me.

### Review by [@everpeace](https://github.com/everpeace) (commented) — 2025-05-08T08:07:59Z

- **keps/2941-DRA-Structured-Parameters/README.md:208** by [@everpeace](https://github.com/everpeace) — 2025-05-08T08:07:59Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
```

  @alaypatel07 Thanks for your feedback. I think I got your point.
  
  > @everpeace can you please provide the workload YAML (say a job object) that Kueue will uniqueuly count against large-gpus quota with the above quota configuration?
  > I think this idea indeed improves the user experience, but it seems to me that with a cel expression like that, whether a device having attributes that evaluates the cel expression to true or not, will only be available after the scheduler selects the device for the claim. Kueue needs to know the device and count it before scheduler makes this decision. But I could be missing something, having a job.yaml will help make it clear.
  
  I now understand the difficulty on this. As we can not assume any inclusion relationship between two boolean formulae in ResourceClaim and ClusterQueue. So, we have to wait for the device allocation result to decide whether the allocation result consumes the quota or not. For example, assume the following ResourceClaim and ClusterQueue exist. In this situation, it is clear that there could be both cases where the allocation result consumes and does not consume the quota (i.e. this means we have to wait for the allocation result). 
  
  ```yaml
  kind: ResourceClaim
  name: one-mid-or-large-gpu
  spec:
    requests:
    - name: middle-or-large-gpu
      deviceClassName: gpu.example.com
      selectors:
      - cel:
          expression: 50g < device.attributes["memory"] and device.attributes["memory"] <= 100g
  ---
  kind: ClusterQueue
  ...
  - name: middle-or-small-gpus
     kind: Device
     devices:
     - className: gpu.example.com
       selectors:
       - cel:
           expression: device.attributes["memory"] <= 80g
  ```
  
  
  Thus, I now understand the current KEP (supporting only DeviceClass in ResourceQuota object) would be a reasonable design decision. 
  
  > Kueue admin might have to create a device class for narrowing the set of devices in the existing device class and use the new device class in clusterqueue.
  > then, existing users might have to migrate/switch their ResourceClaim with the new device class from the existing one.
  
  I think this limitation and user impact should be clearly explained in the documents.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-08T14:24:01Z

- **keps/2941-DRA-Structured-Parameters/README.md:208** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-08T14:24:01Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
```

  @everpeace +1, I will added it to Alternatives section with the reasoning.

### Review by [@johnbelamaric](https://github.com/johnbelamaric) (commented) — 2025-05-08T16:00:35Z

- **keps/2941-DRA-Structured-Parameters/README.md:154** by [@johnbelamaric](https://github.com/johnbelamaric) — 2025-05-08T16:00:35Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
```

  Yeah, I don't think it is worth supporting alpha features.

### Review by [@johnbelamaric](https://github.com/johnbelamaric) (commented) — 2025-05-08T16:13:10Z

- **keps/2941-DRA-Structured-Parameters/README.md:381** by [@johnbelamaric](https://github.com/johnbelamaric) — 2025-05-08T16:13:10Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  > @johnbelamaric Is the expectation here that when Structured Parameters goes to GA, it should be supported in Kueue out of the box (iow this feature in Kueue should be in beta at minimum by then)?
  
  That would be awesome, yes.

### Review by [@johnbelamaric](https://github.com/johnbelamaric) (commented) — 2025-05-08T16:13:57Z

- **keps/2941-DRA-Structured-Parameters/README.md:381** by [@johnbelamaric](https://github.com/johnbelamaric) — 2025-05-08T16:13:57Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  > TBH I think its okay to have DRA support >= 1.33 (v1beta2 or v1 for 1.34).
  
  Yes, I think that's reasonable too. If it's a big effort to support v1beta2 (I don't imagine it is?), we could even just say v1.

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-05-08T19:48:39Z

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-08T19:20:53Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  (This might be getting too specific to discuss here, happy to pick this up elsewhere!)
  
  I see the NVIDIA DRA driver's `ComputeDomain` controller creates ResourceClaimTemplates for IMEX channels that look like this:
  ```yaml
  apiVersion: resource.k8s.io/v1beta1
  kind: ResourceClaimTemplate
  metadata:
    labels:
      resource.nvidia.com/computeDomain: 62407f0c-8627-4b55-8caa-bf496ae6f730
      resource.nvidia.com/computeDomainTarget: Workload
    name: imex-channel-0
  spec:
    spec:
      devices:
        config:
        - opaque:
            driver: compute-domain.nvidia.com
            parameters:
              apiVersion: resource.nvidia.com/v1beta1
              domainID: 62407f0c-8627-4b55-8caa-bf496ae6f730
              kind: ComputeDomainChannelConfig
          requests:
          - channel
        requests:
        - allocationMode: ExactCount
          count: 1
          deviceClassName: compute-domain-default-channel.nvidia.com
          name: channel
  ```
  
  If there is only 1 IMEX channel at play here for every ResourceClaim spawned from this ResourceClaimTemplate, is there a way to express a quota of a particular number of IMEX channels (or ComputeDomains) if several claims against the `compute-domain-default-channel.nvidia.com` DeviceClass actually all map to the same IMEX channel? Or am I missing where that might not make sense to do in the first place?

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-08T19:46:39Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  Will DRA integration intersect with ToplogyAwareScheduling in interesting ways that might require extra testing?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-09T06:41:24Z

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@mimowo](https://github.com/mimowo) — 2025-05-09T06:41:24Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  I think it is definitely worth testing. At least some sanity check in a simple case as a starting point (alpha). 
  
  Ultimately I think it would be ideal if the feature supports TAS (and vice versa). , and actually, I expect TAS may not work in some scenarios without adjustments, because when assigning workloads to nodes TAS is currently unaware of the mapping between device classes and nodes. 
  
  So, I would at least add the TAS testing & support as a graduation requirement for Beta.
  
  cc @mwielgus do we have some specific scenarios we suspect as problematic and worth covering?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-09T10:34:57Z

- **keps/2941-DRA-Structured-Parameters/README.md:203** by [@mwielgus](https://github.com/mwielgus) — 2025-05-09T10:34:57Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
```

  Each resource should have the very same meaning across all queues with that ResourceFlavor. Consider moving this definition to ResourceFlavor or other shared or referencable, class scoped object.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-09T10:37:57Z

- **keps/2941-DRA-Structured-Parameters/README.md:252** by [@mwielgus](https://github.com/mwielgus) — 2025-05-09T10:37:57Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
```

  Workload object should contain a snapshot of the total amount of resources the job requested at admission time. If, at any point in time, the real resource request changes, the workload object should be re-evaluated, re-created and re-admitted.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-09T10:39:41Z

- **keps/2941-DRA-Structured-Parameters/README.md:151** by [@mwielgus](https://github.com/mwielgus) — 2025-05-09T10:39:41Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
```

  This design doesn't work with Topology Aware Scheduling. Making DRA compatible with TAS requires significant additional amount of work and that should be mentioned explicitly here.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-15T18:15:36Z

- **keps/2941-DRA-Structured-Parameters/README.md:203** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-15T18:15:35Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
```

  @mwielgus I went through the existing cluster-scoped APIs and unfortunately none of them feel the right fit for declaring the quota for DRA devices. Following is my thoughts on each of them:
  
  1. ResourceFlavor API: This in intended to express the different types of nodes (containing different types resources). Kueue adds the node label selector and tolerations to workloads upon admission.  IMO this API is focused towards defining the set of nodes for workloads, rather than defining meaning of a resource.
  
  2. Cohort API: This is intended to express the collection of ClusterQueues that can share quotas. Defining the resource here means another cohort could also define the same resource.
  
  Can you please help understand the usecases of defining the resource once and referencing them in different clusterqueues?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-15T19:14:20Z

- **keps/2941-DRA-Structured-Parameters/README.md:252** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-15T19:14:20Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
```

  Currently, in the POC, if this is the clusterqueue configuration: https://github.com/kubernetes-sigs/kueue/blob/714537bfd7fc4e190bfe74acf50ddfead0a89052/keps/2941-DRA-Structured-Parameters/examples/single-gpus/queue-dc-rct-setup.yaml
  
  And this is the workload: https://github.com/kubernetes-sigs/kueue/blob/714537bfd7fc4e190bfe74acf50ddfead0a89052/keps/2941-DRA-Structured-Parameters/examples/single-gpus/single-gpus-workload.yaml
  
  Then the workload status captures this info:
  
  ```
  status:
    admission:
      clusterQueue: single-gpus-cluster-queue
      podSetAssignments:
      - count: 2
        flavors:
          cpu: default-flavor
          memory: default-flavor
          single-gpus: default-flavor
        name: main
        resourceUsage:
          cpu: "2"
          memory: 400Mi
          single-gpus: "2"
    ``` 
    
  Is this sufficient, or do we need to capture, resourceclaimtemplate/resourceclaim and deviceclass specs?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-16T08:14:57Z

- **keps/2941-DRA-Structured-Parameters/README.md:203** by [@mwielgus](https://github.com/mwielgus) — 2025-05-16T08:14:57Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
```

  My comment was not about declaring quota but defining a resource in just one place. For example, the proposed api allows two ClusterQueues to  have significantly different understaning what a resource is. One may say that `"fancygpu"` covers device classes A and B, the other that C, D, E. I want to avoid this situation and have a single place where `"fancygpu"` is defined (for example, in ResourceFlavor). The quota for `"fancygpu"` then would be declared in each of CQs separately.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-16T08:17:39Z

- **keps/2941-DRA-Structured-Parameters/README.md:252** by [@mwielgus](https://github.com/mwielgus) — 2025-05-16T08:17:39Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
```

  That will be enough, thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-16T08:20:17Z

- **keps/2941-DRA-Structured-Parameters/README.md:203** by [@mimowo](https://github.com/mimowo) — 2025-05-16T08:20:17Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
```

  I agree the ResourceFlavor is the desired API for the use case for commonizing the configuration. At least we should explore this option more to challenge the intuition.
  
  >  This in intended to express the different types of nodes (containing different types resources). Kueue adds the node label selector and tolerations to workloads upon admission. IMO this API is focused towards defining the set of nodes for workloads, rather than defining meaning of a resource.
  
  Indeed, so far it was used to specify node labels and taints directly, but I think it is ok to reuse for specify the DRA class details. Similarly as we use it to specify the topology via the spec.topologyName field.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-16T08:20:50Z

- **keps/2941-DRA-Structured-Parameters/README.md:252** by [@mimowo](https://github.com/mimowo) — 2025-05-16T08:20:50Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
```

  sgtm

### Review by [@klueska](https://github.com/klueska) (commented) — 2025-05-16T08:31:38Z

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@klueska](https://github.com/klueska) — 2025-05-16T08:31:38Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  Putting quota on the `compute-domain-default-channel.nvidia.com` would essentially be equivalent to putting a quota on the number of NVLink-capable nodes. Yes it's the same channel number, but technically a unique device being allocated from each node.

### Review by [@klueska](https://github.com/klueska) (commented) — 2025-05-16T14:53:33Z

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@klueska](https://github.com/klueska) — 2025-05-16T14:53:33Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  Independent of `kueue`, DRA does not (and has no current plans to) integrate with TopologyWareScheduling

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-16T14:56:05Z

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T14:56:05Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  The TopologyAwareScheduling is the Kueue core feature (https://kueue.sigs.k8s.io/docs/concepts/topology_aware_scheduling/). So, the support is required, IMO.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-16T15:05:19Z

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@mimowo](https://github.com/mimowo) — 2025-05-16T15:05:19Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  I think it is required, but I think we can defer it for later when kubescheduler exposes some helper functions we could easily use. I would not block the initial dra support on that. We can add this to the requirements for second Beta or GA.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-16T15:07:28Z

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T15:07:28Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  I didn't want to say that we should support it in the alpha stage. Yes, we can consider it as gates for Beta or GA stage. However, in general, we should add additional feature before GA.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-16T15:11:00Z

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@mimowo](https://github.com/mimowo) — 2025-05-16T15:11:00Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  yes, totally

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-16T15:15:42Z

- **keps/2941-DRA-Structured-Parameters/README.md:203** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T15:15:42Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
```

  I have an additional question. Do we want to dynamically change ( or allow) device class and resource name mapping? If yes, the ResourceFlavor is a good place to store it. If not, we can consider storing it in Configurateion API like ConfigurableResourceTransformation feature. 
  
  If we store it in ResourceFlavor, I guess that we need to consider the way how to map between ResourceFlavor and other resources like Cohort or else  since those has resources section as well.
  If we can store it in Configuration API, we do not need to care it since we can just load the resource - device class mapping from the static configuration.

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-05-16T16:04:17Z

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-16T16:04:17Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  @klueska Is that "technically a unique device being allocated from each node" part implemented within the DRA driver? If Kueue can only see "N pods will each make one claim for a `compute-domain-default-channel.nvidia.com`" then I think it assumes N quota will be used, i.e. number of pods instead of number of nodes. Or could Kueue derive the number of nodes some other way before the pods are scheduled?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-16T16:39:02Z

- **keps/2941-DRA-Structured-Parameters/README.md:203** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-16T16:39:02Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
```

  > I have an additional question. Do we want to dynamically change ( or allow) device class and resource name mapping?
  
  This is one of the TODOs left to address from my side.  I am not sure if allowing users to change the device class and resource name mapping is a good idea. As soon as the mapping changes, the quota for all the existing workloads that were admitted will have to be re-calculated as noted by this comment: https://github.com/kubernetes-sigs/kueue/pull/5149#discussion_r2081412975.  This might not be desirable.

### Review by [@klueska](https://github.com/klueska) (commented) — 2025-05-16T17:04:14Z

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@klueska](https://github.com/klueska) — 2025-05-16T17:04:14Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  Exactly one default-channel is advertised from each node by the DRA driver running there. So if you have a group of pods each asking for one -- then each of them will be assigned to a different node (because there is only one available per node).

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-05-16T18:10:44Z

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-16T18:10:44Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  Ah, so a workload comprised of a ComputeDomain + Deployment, with the pods all referencing the ComputeDomain's ResourceClaimTemplate, will end up with each pod on a separate node, and the driver implements the necessary magic for those pods in the ComputeDomain to all be interconnected via IMEX?
  
  I didn't catch the correlation between the ComputeDomain's `spec.numNodes` and number of pods in the [examples](https://github.com/NVIDIA/k8s-dra-driver-gpu/tree/dc61b93a9b0a65261309981e062ac416e3730629/demo/specs/imex) but that makes sense.

### Review by [@klueska](https://github.com/klueska) (commented) — 2025-05-16T19:27:46Z

- **keps/2941-DRA-Structured-Parameters/README.md:243** by [@klueska](https://github.com/klueska) — 2025-05-16T19:27:46Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
```

  Yes, this is basically how it works.

### Review by [@klueska](https://github.com/klueska) (commented) — 2025-05-16T19:32:37Z

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@klueska](https://github.com/klueska) — 2025-05-16T19:32:37Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  I think the way I worded it was probably too harsh -- what I meant to say is that there is no current proposal for DRA to integrate with TopologyAwareScheduling in the short-term. I don't think anyone would be opposed to supporting it if someone had a concrete proposal on how to do it.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-16T20:42:36Z

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-16T20:42:36Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  > Ultimately I think it would be ideal if the feature supports TAS (and vice versa). , and actually, I expect TAS may not work in some scenarios without adjustments, because when assigning workloads to nodes TAS is currently unaware of the mapping between device classes and nodes.
  
  Just clarifying this for my own understanding, the mapping between device class names and nodes is not determined until a ResourceClaim is processed by kube-scheduler.  Technically, kueue's TAS will continue to work as long as the node that Kueue's TAS selects for the workload, has the resources to suffice device classes that is requested by the workload.  For example in case of a homogeneous cluster where each nodes has the same kind of accelerators, we should expect TAS to work with DRA devices.
  
  The only cases where it will not work is when Kueue's TAS guides the workload to a particular node which kube-scheduler couldnt find resources to assign for the ResourceClaim.
  
  @mimowo @tenzen-y is that correct understanding?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-19T13:09:28Z

- **keps/2941-DRA-Structured-Parameters/README.md:377** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-19T13:09:28Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+    - coveredResources: ["cpu", "memory", "single-gpus"]
+      flavors:
+        - name: "default-flavor"
+          resources:
+            - name: "cpu"
+              nominalQuota: 9
+            - name: "memory"
+              nominalQuota: "200Mi"
+            - name: "single-gpu"    #4.ii) lookup the cannonical name of the collection of deviceClassNames and count quota against it
+              deviceClassNames: ["gpu.example.com"]
+              nominalQuota: 2
+              kind: "DeviceClass"
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
```

  > Ultimately I think it would be ideal if the feature supports TAS (and vice versa). , and actually, I expect TAS may not work in some scenarios without adjustments, because when assigning workloads to nodes TAS is currently unaware of the mapping between device classes and nodes.
  
  At a high level, there needs to be some kind of agreement between kueue scheduler and dra scheduler on the set of nodes the workload will be guided towards. 
  
  Since this might be a big chunk of work, while it would be nice to have it in beta, I think having a separate feature gate/feature lifecycle for TAS with DRA devices might be a better approach in de-risking the lifecycle of this feature.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-19T16:25:10Z

- **keps/2941-DRA-Structured-Parameters/README.md:151** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-19T16:25:10Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
```

  I will update it based on the discussion in this thread https://github.com/kubernetes-sigs/kueue/pull/5149#discussion_r2081034068

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-19T21:18:08Z

- **keps/2941-DRA-Structured-Parameters/README.md:203** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-19T21:18:08Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
```

  > My comment was not about declaring quota but defining a resource in just one place. For example, the proposed api allows two ClusterQueues to have significantly different understaning what a resource is. One may say that "fancygpu" covers device classes A and B, the other that C, D, E. I want to avoid this situation and have a single place where "fancygpu" is defined (for example, in ResourceFlavor). The quota for "fancygpu" then would be declared in each of CQs separately.
  
  @mwielgus @mimowo it seems to me that moving the definition of resource to ResourceFlavor can still lead to conflicting understanding of the resource. As an example:
  
  ```
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ResourceFlavor
  metadata:
    name: fancy-gpu-ab
  spec:
    resources:
    - name: "fancygpu"
      kind: "DeviceClass"
      deviceClassNames:
      - "deviceclass-a"
      - "deviceclass-b"
    nodeLabels:
      instance-type: gpu-optimized
  ---
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ResourceFlavor
  metadata:
    name: fancy-gpu-cde
  spec:
    resources:
    - name: "fancygpu"
      kind: "DeviceClass"
      deviceClassNames:
      - "deviceclass-c"
      - "deviceclass-d"
      - "deviceclass-e"
    nodeLabels:
      accelerator-type: fancy-gpu-premium
      instance-type: gpu-premium
  ```
  
  is API change expected to be complemented with some kind of webhook validation that checks for `resourceflavor.spec.resource.name[*]` to be unique across all resourceflavors?

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-05-19T23:43:03Z

- **keps/2941-DRA-Structured-Parameters/README.md:73** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-19T20:11:23Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
```

  nit, for consistency with the other items in the list:
  ```suggestion
  - ResourceClaims
  ```
  
  Is it also worth mentioning ResourceClaimTemplates in this list?

- **keps/2941-DRA-Structured-Parameters/README.md:385** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-19T20:30:32Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing. 
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### Resource Claim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota. 
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
```

  @alaypatel07 helped me understand this more. Going back to the main idea of this alternative:
  
  > Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota.
  
  The main reason not to count resource claims that I understand is that a quota of "one resource claim" could mean anything from "one request for the smallest allocatable piece of one device" to "several requests for entire pools" so tracking quota that way isn't very effective. Tracking a count of devices like this doc currently proposes is definitely a step in the right direction from that. I think I led myself a bit astray misinterpreting the namespace/cluster scoped part.
  
  @alaypatel07 I think you mentioned you planned to clarify that here?

- **keps/2941-DRA-Structured-Parameters/README.md:138** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-19T23:02:36Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
```

  nit: this statement seems to conflict with the list in the "Background" section above.

- **keps/2941-DRA-Structured-Parameters/README.md:265** by [@nojnhuh](https://github.com/nojnhuh) — 2025-05-19T23:39:10Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
```

  Is it possible and desirable to add an extra check here for ResourceClaims that the claim is not already allocated?
  
  e.g. If one workload references a claim, then it should count against the quota since it's asking for new devices. If another workload references the same claim which is now allocated, I would expect that claim not to count against the quota since no additional devices are being allocated.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-21T17:50:51Z

- **keps/2941-DRA-Structured-Parameters/README.md:265** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-21T17:50:51Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
```

  Thinking more about this, what happens if the resourceclaim that was allocated counted against quota from different ClusterQueue?
  
  I think we have talked about this in the design review meeting that Kueue needs to support ResourceClaims that could be shared between workloads, but there is a grey area on how to count them against the right quota.
  
  IMO, the correct thing to do here is: for all workloads that are using the allocated claim, count it against their respective clusterqueue quota only once.
  
  cc @kannon92 @tardieu

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-21T19:10:01Z

- **keps/2941-DRA-Structured-Parameters/README.md:138** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-21T19:10:01Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
```

  removed the statement.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-21T19:14:24Z

- **keps/2941-DRA-Structured-Parameters/README.md:385** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-21T19:14:24Z
```diff
@@ -0,0 +1,394 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance that Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode all: worse case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=DynamicResourceStructuredParameters
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=DynamicResourceStructuredParameters
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceField, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using
+   1. The name of the ResourceClaimTemplate or
+   2. The name of the ResourceClaim
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> cannonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #a) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # b) use the name in resources.claim
+        resourceClaimTemplateName: single-gpu # c) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # d) the name of the device class
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing. 
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### Resource Claim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota. 
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
```

  @nojnhuh Updated the alternative section with more accurate explanation on why counting ResourceClaims is not an ideal solution

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-21T19:21:00Z

- **keps/2941-DRA-Structured-Parameters/README.md:72** by [@kannon92](https://github.com/kannon92) — 2025-05-21T19:21:00Z
```diff
@@ -0,0 +1,518 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
```

  ```suggestion
  DRA has four APIs that are relevant for a Kueue:
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-21T19:22:02Z

- **keps/2941-DRA-Structured-Parameters/README.md:164** by [@kannon92](https://github.com/kannon92) — 2025-05-21T19:22:02Z
```diff
@@ -0,0 +1,518 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
```

  ```suggestion
  borrowing and preemptable scheduling. 
  This includes modifying the `ResourceQuota` struct to allow configuring the field
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-21T19:23:01Z

- **keps/2941-DRA-Structured-Parameters/README.md:165** by [@kannon92](https://github.com/kannon92) — 2025-05-21T19:23:01Z
```diff
@@ -0,0 +1,518 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
```

  At a high level you are really modifying the ClusterQueue API. ResourceQuota is more of a field within a ClusterQueue.
  
  Plus ResourceQuota means something different in Kubernetes.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-21T19:25:56Z

- **keps/2941-DRA-Structured-Parameters/README.md:265** by [@kannon92](https://github.com/kannon92) — 2025-05-21T19:25:56Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
```

  How is this handled with DRA's work for ResourceQuota?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-21T20:15:40Z

- **keps/2941-DRA-Structured-Parameters/README.md:165** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-21T20:15:40Z
```diff
@@ -0,0 +1,518 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
```

  This is about to change, I am working on a design change move the api changes from ClusterQueue to ResourceFlavor as requested here https://github.com/kubernetes-sigs/kueue/pull/5149#discussion_r2081408930.
  
  I have the POC ready for it here: https://github.com/kubernetes-sigs/kueue/pull/5151/files#diff-68c00ab8f58f96837628e878290e5d22cae1aade4661409f11a3cac04369e9e5R44 
  
  There are some corner cases that needs to be addressed, planning on pushing an update in couple hours.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-05-21T22:18:19Z

- **keps/2941-DRA-Structured-Parameters/README.md:203** by [@alaypatel07](https://github.com/alaypatel07) — 2025-05-21T22:18:19Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
```

  @mimowo @mwielgus I have updated the design document to reflect this exploration about moving the device class mapping in the ResourceFlavor API.
  
  PTAL. I am running into a corner case, I will plan to share the corner cases in tomorrow's wg-batch call at 9:00 am EST if there is enough quorum of maintainers.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-30T17:41:40Z

- **keps/2941-DRA-Structured-Parameters/README.md:442** by [@kannon92](https://github.com/kannon92) — 2025-05-30T17:41:40Z
```diff
@@ -0,0 +1,549 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpu"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+  1. the name of the ResourceClaimTemplate or
+  2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+  1. For the workload a deviceClassMap will be created by:
+    1. Retrieving the LocalQueue and ClusterQueue for the workload
+      1. Getting all ResourceFlavors used in the ClusterQueue
+        1. For each ResourceFlavor, extracting its DynamicResources section
+          1. Creating a map from DeviceClass names to the Kueue resource names
+  2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'             # 4.1.1.1) get the canonical name for default-gpu-flavor
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com                # 4.1.1.1.1) create a map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        single-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1"                # 5) selected device count is reflected here in workload status
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
```

  We should explain when we can graduate this feature.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-30T17:42:31Z

- **keps/2941-DRA-Structured-Parameters/README.md:454** by [@kannon92](https://github.com/kannon92) — 2025-05-30T17:42:31Z
```diff
@@ -0,0 +1,549 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpu"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+  1. the name of the ResourceClaimTemplate or
+  2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+  1. For the workload a deviceClassMap will be created by:
+    1. Retrieving the LocalQueue and ClusterQueue for the workload
+      1. Getting all ResourceFlavors used in the ClusterQueue
+        1. For each ResourceFlavor, extracting its DynamicResources section
+          1. Creating a map from DeviceClass names to the Kueue resource names
+  2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'             # 4.1.1.1) get the canonical name for default-gpu-flavor
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com                # 4.1.1.1.1) create a map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        single-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1"                # 5) selected device count is reflected here in workload status
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing.
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
```

  We should probably add the original design here.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-04T05:20:33Z

- **keps/2941-DRA-Structured-Parameters/README.md:454** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-04T05:20:33Z
```diff
@@ -0,0 +1,549 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpu"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+  1. the name of the ResourceClaimTemplate or
+  2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+  1. For the workload a deviceClassMap will be created by:
+    1. Retrieving the LocalQueue and ClusterQueue for the workload
+      1. Getting all ResourceFlavors used in the ClusterQueue
+        1. For each ResourceFlavor, extracting its DynamicResources section
+          1. Creating a map from DeviceClass names to the Kueue resource names
+  2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'             # 4.1.1.1) get the canonical name for default-gpu-flavor
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com                # 4.1.1.1.1) create a map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        single-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1"                # 5) selected device count is reflected here in workload status
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing.
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
```

  updated.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-04T05:21:08Z

- **keps/2941-DRA-Structured-Parameters/README.md:442** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-04T05:21:08Z
```diff
@@ -0,0 +1,549 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpu"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+  1. the name of the ResourceClaimTemplate or
+  2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+  1. For the workload a deviceClassMap will be created by:
+    1. Retrieving the LocalQueue and ClusterQueue for the workload
+      1. Getting all ResourceFlavors used in the ClusterQueue
+        1. For each ResourceFlavor, extracting its DynamicResources section
+          1. Creating a map from DeviceClass names to the Kueue resource names
+  2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'             # 4.1.1.1) get the canonical name for default-gpu-flavor
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com                # 4.1.1.1.1) create a map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        single-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1"                # 5) selected device count is reflected here in workload status
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+We will introduce a KueueDynamicResourceAllocation feature gate.
+
+This feature gate will go beta depending on community feedback of functionality in alpha.
+
+The goal will be limit changes only if this feature gate is enabled in combination with the DRA feature.
```

  @kannon92 can you please take a look at the Graduation Criteria, I took a stab at defining it, will like to get additional thoughts.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-05T01:33:59Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-05T01:33:59Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  @mwielgus @tenzen-y @mimowo 
  
  This was discussed at length in `20250522` wg-batch meeting, https://youtu.be/S-aDbRvcLEk?si=2ymByQ1BCsWHgzkV
  
  PTAL at the summary

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-05T01:35:04Z

- **keps/2941-DRA-Structured-Parameters/README.md:254** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-05T01:35:04Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
+
+A problematic scenario can arise when multiple ResourceFlavors map the same device class to different canonical names within the same ClusterQueue. This creates ambiguity at admission time about which quota should be consumed.
+
+**Problematic Configuration Example:**
+
+```yaml
+# ResourceFlavor 1: Zone A configuration
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-a-flavor
+spec:
+  nodeLabels:
+    zone: zone-a
+  dynamicResources:
+  - name: zone-a-gpus
+    deviceClassNames:
+    - gpus.example.com  # Maps to zone-a-gpus
+
+---
+# ResourceFlavor 2: Zone B configuration  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-b-flavor
+spec:
+  nodeLabels:
+    zone: zone-b
+  dynamicResources:
+  - name: zone-b-gpus
+    deviceClassNames:
+    - gpus.example.com  # CONFLICT: Same device class mapped to zone-b-gpus
+
+---
+# ClusterQueue using both flavors
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "zone-a-gpus", "zone-b-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 2
+      - name: zone-b-gpus
+        nominalQuota: 0
+    - name: zone-b-flavor  
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 0
+      - name: zone-b-gpus
+        nominalQuota: 3
+```
+
+**The Problem:**
+When a workload requests `gpus.example.com`, Kueue faces ambiguity at admission time:
+- Should it count against `zone-a-gpus` quota (from zone-a-flavor)?
+- Should it count against `zone-b-gpus` quota (from zone-b-flavor)?
+
+This ambiguity exists **before** the scheduler performs node selection, so ResourceFlavor node labels cannot resolve it during admission.
+
+**Potential Issues:**
+1. **Incorrect quota accounting**: Workloads might be admitted against the wrong ResourceFlavor's quota  
+1. **Resource waste**: One flavor's quota might be exhausted while another remains unused
+1. **Scheduling failures**: Workloads might be admitted but fail to schedule if counted against the wrong flavor
+
+#### Same Canonical name with different deviceClassNames list
```

  @tenzen-y this was brought up in the wg-batch meeting, I have documented it here, PTAL if this addresses your concerns

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T08:16:35Z

Just a couple editorial comments for kep.yaml.

- **keps/2941-DRA-Structured-Parameters/kep.yaml:32** by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:06:32Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
+approvers:
+  - TBD
+  - "tbd"
+
+see-also:
+  - "dra kep in upstream"
+  - "/keps/2345-everyone-gets-a-kep"
+replaces:
+  - "/keps/3456-replaced-kep"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.9"
+  beta: "v0.10"
+  stable: "v0.11"
```

  ```suggestion
  latest-milestone: "v0.13"
  
  # The milestone at which this feature was, or is targeted to be, at each stage.
  milestone:
    alpha: "v0.13"
    beta:
    stable: 
  ```

- **keps/2941-DRA-Structured-Parameters/kep.yaml:42** by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:07:08Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
+approvers:
+  - TBD
+  - "tbd"
+
+see-also:
+  - "dra kep in upstream"
+  - "/keps/2345-everyone-gets-a-kep"
+replaces:
+  - "/keps/3456-replaced-kep"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.9"
+  beta: "v0.10"
+  stable: "v0.11"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDynamicResourceAllocation
+disable-supported: true
+
+# The following PRR answers are required at beta release
+metrics:
+  - my_feature_metric
```

  ```suggestion
  # metrics:
  #  - my_feature_metric
  ```
  not needed for alpha, just comment out

- **keps/2941-DRA-Structured-Parameters/kep.yaml:11** by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:07:34Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
+approvers:
+  - TBD
```

  Feel free to add me, and @tenzen-y

- **keps/2941-DRA-Structured-Parameters/kep.yaml:9** by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:07:58Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
```

  Please update.

- **keps/2941-DRA-Structured-Parameters/kep.yaml:6** by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:08:51Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
```

  nit: does not need to be super accurate, but I see the PR open in May 2, 2025

- **keps/2941-DRA-Structured-Parameters/kep.yaml:18** by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:16:07Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
+approvers:
+  - TBD
+  - "tbd"
+
+see-also:
+  - "dra kep in upstream"
+  - "/keps/2345-everyone-gets-a-kep"
+replaces:
+  - "/keps/3456-replaced-kep"
```

  Refresh this section:
  - link to upstream DRA
  - add "https://github.com/kubernetes-sigs/kueue/pull/3071" to see also or "replaces"
  - drop the dummy links from the template

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T08:26:37Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:26:37Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  I'm not clear why this is an issue, because Kueue already accounts quota against [`FlavorResource`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/resources/resource.go#L28) which is already a pair (Flavor, Resource). See, for example, [clusterqueue.updateWorkloadUsage](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/clusterqueue.go#L481-L491).
  
  And so, if we are consistent with the model we would count the quota against (zone-a-flavor, zone-a-gpus), and (zone-b-flavor, zone-b-gpus). I would like to first understand if there is any issue (or gap) with following the current model of accounting for quota, in the context of DRA.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-05T13:34:26Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-05T13:34:26Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  @mimowo essentially this is the same issue as you mentioned here: https://github.com/kubernetes-sigs/kueue/pull/5149#issuecomment-2943275997 
  
  The issue is, how can cluster-admins deterministically know what quota will Kueue use to admit the workload in case of this ambiguity?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-05T13:36:10Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:32** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-05T13:36:10Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
+approvers:
+  - TBD
+  - "tbd"
+
+see-also:
+  - "dra kep in upstream"
+  - "/keps/2345-everyone-gets-a-kep"
+replaces:
+  - "/keps/3456-replaced-kep"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.9"
+  beta: "v0.10"
+  stable: "v0.11"
```

  updated, will resolve

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-05T13:36:19Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:11** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-05T13:36:19Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
+approvers:
+  - TBD
```

  done, thanks!

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-05T13:36:54Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:9** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-05T13:36:54Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
```

  added @kannon92, if anyone else wants to be added to the list here, please let me know.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-05T13:37:17Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:18** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-05T13:37:16Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
+approvers:
+  - TBD
+  - "tbd"
+
+see-also:
+  - "dra kep in upstream"
+  - "/keps/2345-everyone-gets-a-kep"
+replaces:
+  - "/keps/3456-replaced-kep"
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T14:10:25Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-05T14:10:25Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  Got it, bringing up the example from the other comment:
  ```yaml
  dynamicResources:
  - name: res-a1
    deviceClassNames: ["a"]
  - name: res-a2
    deviceClassNames: ["a"]
  ```
  I'm leaning to say, we have validation that there are no two resources under the same Flavor with the same set of deviceClassNames. This would enable the quota counting model as we currently have (Flavor, resource), as this pair would be non-ambiguous. Such validation is very easy to add using kubebuilder markers. At least I'm tempted for Alpha, for simplicity.
  
  Are you aware of some important use cases which would be blocked by this assumption?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T14:29:45Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-05T14:29:45Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  or to make things even simpler to fit under the current Kueue quota model, we would have validation that two dynamic resources, under the same flavor, cannot have overlapping device class name.
  
  I think counting quota other than (flavor, resource) might be possible but will be confusing to the users of Kueue, so Im exploring limiting the expressiveness of the API to make the model simple without losing any important use cases.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-05T15:04:59Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-05T15:04:59Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  > Are you aware of some important use cases which would be blocked by this assumption?
  
  @mimowo The only interesting usecase I came up with is, assuming a cluster admin is managing two separate tenants in the cluster and wants isolate nodes using node labels. zone-a and zone-b being the separate node labels.  Both of these tenants have the same device class that the cluster admin has to declare on their respective RF objects.
  
  However, if Kueue enforces the validation, the cluster admin can create separate device classes for zone-a and zone-b, even though both the device class have the same spec. The only negative thing about this, is that tenant workloads on will have to use the new device class instead of the generic one.
  
  On the other hand keeping it unvalidated will cause issues with improper accounting may be even hard-to-debug corner cases.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T15:28:33Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-05T15:28:33Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  > @mimowo The only interesting usecase I came up with is, assuming a cluster admin is managing two separate tenants in the cluster and wants isolate nodes using node labels. zone-a and zone-b being the separate node labels. Both of these tenants have the same device class that the cluster admin has to declare on their respective RF objects.
  
  Let me try to explore if this is indeed an issue for the model, consider the following setup:
  
  Two separate pools of devices per zone:
  ```yaml
  kind: ResourceFlavor
  apiVersion: kueue.x-k8s.io/v1beta1
  metadata:
    name: "flavor-a"
  spec:
    nodeLabels: 
      provider.com/zone: a
    dynamicResources:
    - name: fency-resource
      deviceClassNames: ["fency-device"]
  ---
  kind: ResourceFlavor
  apiVersion: kueue.x-k8s.io/v1beta1
  metadata:
    name: "flavor-b"
  spec:
    nodeLabels: 
      provider.com/zone: b
    dynamicResources:
    - name: fency-resource
      deviceClassNames: ["fency-device"]
  ---
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "cq-tentant-a"
  spec:
    resourceGroups:
    - coveredResources: [ "fency-resource"]
      flavors:
      - name: "flavor-a"
        resources:
        - name: "fency-resource"
          nominalQuota: 80
  ---
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "cq-tentant-b"
  spec:
    resourceGroups:
    - coveredResources: [ "fency-resource"]
      flavors:
      - name: "flavor-b"
        resources:
        - name: "fency-resource"
          nominalQuota: 80
  ---
  ```
  more over we could for example introduce borrowing, say, tenant a has no nominal quota in zone-b, but can borrow 20 from tentan't b zone:
  
  ```yaml
  ---
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "cq-tentant-a"
  spec:
    resourceGroups:
    - coveredResources: [ "fency-resource"]
      flavors:
      - name: "flavor-a"
        resources:
        - name: "fency-resource"
          nominalQuota: 80
      - name: "flavor-b"
        resources:
        - name: "fency-resource"
          nominalQuota: 0
          borrowingLimit: 20
  ---
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "cq-tentant-b"
  spec:
    resourceGroups:
    - coveredResources: [ "fency-resource"]
      flavors:
      - name: "flavor-b"
        resources:
        - name: "fency-resource"
          nominalQuota: 80
  ---
  ```
  So in this case, we have one device mapped 1:1 to resource, called "fency-resource". However, because the quota model is (flavor, resource), then I think use case is covered, unless I'm missing something. Let me know what you think.

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-06-05T15:43:50Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:9** by [@nojnhuh](https://github.com/nojnhuh) — 2025-06-05T15:43:50Z
```diff
@@ -0,0 +1,42 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2024-08-30
+reviewers:
+  - tbd
+  - "tbd"
```

  @alaypatel07 Could you please list me here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T15:58:53Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:35** by [@mimowo](https://github.com/mimowo) — 2025-06-05T15:58:53Z
```diff
@@ -0,0 +1,40 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2025-05-02
+reviewers:
+  - "@kannon92"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+see-also:
+  - "https://github.com/kubernetes/enhancements/blob/master/keps/sig-node/4381-dra-structured-parameters/README.md"
+replaces:
+  - "https://github.com/kubernetes-sigs/kueue/pull/3071"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.13"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.13"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDynamicResourceAllocation
```

  ```suggestion
    - name: DynamicResourceAllocation
  ```
  I think "Kueue" is redundant in this context.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T15:59:50Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:35** by [@mimowo](https://github.com/mimowo) — 2025-06-05T15:59:50Z
```diff
@@ -0,0 +1,40 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2025-05-02
+reviewers:
+  - "@kannon92"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+see-also:
+  - "https://github.com/kubernetes/enhancements/blob/master/keps/sig-node/4381-dra-structured-parameters/README.md"
+replaces:
+  - "https://github.com/kubernetes-sigs/kueue/pull/3071"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.13"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.13"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDynamicResourceAllocation
```

  I'm also considering `DRAIntegration`, but I think `DynamicResourceAllocation` is my slight preference. Any preference from you @tenzen-y ?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-06T02:28:45Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-06T02:28:44Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  @mimowo You are right, in a two tenant system that is represented by two separate cluster queues and two separate resource flavors, the ambiguity issue will not exist. 
  
  However, the borrowing example you provided, I think the ambiguity still exists. When kueue sees the workload and finds the device class. It will do reverse mapping of the dynamicResourceName, it has two options, "flavor-a/fency-resource" or "flavor-b/fency-resource". This ambiguity will somehow have to be resolved.
  
  Additionally, I can provide an example of a single tenant system with multiple zones (zone-a and zone-b), where the exact ambiguity will be more obvious.
  
  ```yaml
  # Zone A Resource Flavor
  kind: ResourceFlavor
  apiVersion: kueue.x-k8s.io/v1beta1
  metadata:
    name: "zone-a-flavor"
  spec:
    nodeLabels: 
      topology.kubernetes.io/zone: zone-a
    dynamicResources:
    - name: fency-resource
      deviceClassNames: ["fency-device"]
  
  ---
  # Zone B Resource Flavor  
  kind: ResourceFlavor
  apiVersion: kueue.x-k8s.io/v1beta1
  metadata:
    name: "zone-b-flavor"
  spec:
    nodeLabels: 
      topology.kubernetes.io/zone: zone-b
    dynamicResources:
    - name: fency-resource
      deviceClassNames: ["fency-device"]
  
  ---
  # Single ClusterQueue for the tenant that can use both zones
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "single-tenant-multi-zone-cq"
  spec:
    resourceGroups:
    - coveredResources: ["cpu", "memory", "fency-resource"]
      flavors:
      - name: "zone-a-flavor"
        resources:
        - name: "cpu"
          nominalQuota: 100
        - name: "memory"
          nominalQuota: "500Gi"
        - name: "fency-resource"
          nominalQuota: 50  # 50 devices in zone-a
      - name: "zone-b-flavor"
        resources:
        - name: "cpu"
          nominalQuota: 80
        - name: "memory"
          nominalQuota: "400Gi"
        - name: "fency-resource"
          nominalQuota: 30  # 30 devices in zone-b
  ---
  ```
  
  How will kueue resolve the ambiguity? will it traverse the resource flavors list in the cluster queue in top down order and find the first (flavor, dynamicResource) that fits the workload requirement?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T06:08:33Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-06T06:08:33Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  > However, the borrowing example you provided, I think the ambiguity still exists. When kueue sees the workload and finds the device class. It will do reverse mapping of the dynamicResourceName, it has two options, "flavor-a/fency-resource" or "flavor-b/fency-resource". This ambiguity will somehow have to be resolved.
  
  Yes, but the same exists for the common resources like cpu or memory. 
  
  > How will kueue resolve the ambiguity? will it traverse the resource flavors list in the cluster queue in top down order and find the first (flavor, dynamicResource) that fits the workload requirement?
  
  Yes, traversing flavors in the declaration order is exactly what Kueue does. The choice of the flavor is recorded in the Workload `status.admission.podSetAssignments[].flavors`, so that the information is not lost on reboot of Kueue, for example fragment of Workload status:
  ```yaml
  status:
    admission:
      clusterQueue: cluster-queue
      podSetAssignments:
      - count: 3
        flavors:
          cpu: default-flavor                       
          memory: default-flavor
        name: main
  ```
  
  Now, this is a powerful abstraction in Kueue which allows us to provide the same resources like "gpu", "cpu", "memory", via the same devices or machine types, but at different prices, for example as some quota may come from reservation, then spot, and provisioning. For example, slightly modifying your zone example:
  ```yaml
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "single-tenant-multi-provsioning"
  spec:
    resourceGroups:
    - coveredResources: ["cpu", "memory", "fency-resource"]
      flavors:
      - name: "reservation"
        resources:
        - name: "cpu"
          nominalQuota: 100
        - name: "memory"
          nominalQuota: "500Gi"
        - name: "fency-resource"
          nominalQuota: 50  # 50 devices which can be used in reservation
      - name: "spot"
        resources:
        - name: "cpu"
          nominalQuota: 80
        - name: "memory"
          nominalQuota: "400Gi"
        - name: "fency-resource"
          nominalQuota: 30  # 30 devices which can be created using spot
      - name: "on-demand"
        resources:
        - name: "cpu"
          nominalQuota: 80
        - name: "memory"
          nominalQuota: "400Gi"
        - name: "fency-resource"
          nominalQuota: 30  # 30 devices which can be provisioned
  ```
  In other words Flavor is not always "machine type". Flavor denotes a set of machines with common characteristics, but the only difference between flavors might be the type of provisioning.
  
  Kueue currently uses `(Flavor, Resource)` as the quota domain identity. 
  
  Yes, it is possible to define the quota unit differently in the context of DRA, for example:
  1. (Flavor, Resource, DeviceClassName)
  2. (Resource, DeviceClassName) 
  
  However, I think (2.) would not support the above important use-case. Maybe (1.) supports them all, but it is a complication of the model we have so far, so I would like to go there only if we see important use-cases which cannot be supported in the standard model of Kueue. |
  
  I believe the use-cases discussed so far can be covered with the standard (Flavor, Resource) model.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T08:11:11Z

- **keps/2941-DRA-Structured-Parameters/README.md:563** by [@mimowo](https://github.com/mimowo) — 2025-06-06T08:02:11Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+    - [Same Canonical Name Across Different Device Classes](#same-canonical-name-across-different-device-classes)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
+
+A problematic scenario can arise when multiple ResourceFlavors map the same device class to different canonical names within the same ClusterQueue. This creates ambiguity at admission time about which quota should be consumed.
+
+**Problematic Configuration Example:**
+
+```yaml
+# ResourceFlavor 1: Zone A configuration
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-a-flavor
+spec:
+  nodeLabels:
+    zone: zone-a
+  dynamicResources:
+  - name: zone-a-gpus
+    deviceClassNames:
+    - gpus.example.com  # Maps to zone-a-gpus
+
+---
+# ResourceFlavor 2: Zone B configuration  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-b-flavor
+spec:
+  nodeLabels:
+    zone: zone-b
+  dynamicResources:
+  - name: zone-b-gpus
+    deviceClassNames:
+    - gpus.example.com  # CONFLICT: Same device class mapped to zone-b-gpus
+
+---
+# ClusterQueue using both flavors
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "zone-a-gpus", "zone-b-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 2
+      - name: zone-b-gpus
+        nominalQuota: 0
+    - name: zone-b-flavor  
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 0
+      - name: zone-b-gpus
+        nominalQuota: 3
+```
+
+**The Problem:**
+When a workload requests `gpus.example.com`, Kueue faces ambiguity at admission time:
+- Should it count against `zone-a-gpus` quota (from zone-a-flavor)?
+- Should it count against `zone-b-gpus` quota (from zone-b-flavor)?
+
+This ambiguity exists **before** the scheduler performs node selection, so ResourceFlavor node labels cannot resolve it during admission.
+
+**Potential Issues:**
+1. **Resource waste**: One flavor's quota might be exhausted while another remains unused
+
+**Mitigation:**
+To avoid this scenario:
+- ensure that within a single ClusterQueue, each device class name maps to exactly one canonical name across a
+  ResourceFlavors. 
+- cluster admins should create separate deviceclasses to account for the dynamic resources that are being requested
+  in the resource flavor.
+
+#### Same Canonical Name Across Different Device Classes
+
+The following configuration is **NOT** problematic because each device class maps to exactly one ResourceFlavor/canonical-name combination:
+
+```yaml
+# ResourceFlavor 1: fancy-gpu-flavor maps gpu.example.com → GPUs
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: fancy-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: fancy
+  dynamicResources:
+  - name: GPUs  # canonical name
+    deviceClassNames:
+    - gpu.example.com  # device class maps to this flavor's "GPUs"
+
+---
+# ResourceFlavor 2: new-gpu-flavor maps new-gpu.example.com → GPUs  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: new-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: new
+  dynamicResources:
+  - name: GPUs  # same canonical name, different device class
+    deviceClassNames:
+    - new-gpu.example.com  # different device class maps to this flavor's "GPUs"
+```
+
+When a workload requests `gpu.example.com`, Kueue can unambiguously determine the mapping path:
+1. **Device class lookup**: `gpu.example.com` appears only in fancy-gpu-flavor's deviceClassNames
+2. **ResourceFlavor identification**: fancy-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from fancy-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against fancy-gpu-flavor's "GPUs" quota
+
+Similarly, when a workload requests `new-gpu.example.com`:
+1. **Device class lookup**: `new-gpu.example.com` appears only in new-gpu-flavor's deviceClassNames  
+2. **ResourceFlavor identification**: new-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from new-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against new-gpu-flavor's "GPUs" quota
+
+The fact that both flavors use the same canonical name "GPUs" doesn't create ambiguity because the device class name uniquely identifies which ResourceFlavor (and therefore which quota pool) should be used.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpu"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+  1. the name of the ResourceClaimTemplate or
+  2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+  1. For the workload a deviceClassMap will be created by:
+    1. Retrieving the LocalQueue and ClusterQueue for the workload
+      1. Getting all ResourceFlavors used in the ClusterQueue
+        1. For each ResourceFlavor, extracting its DynamicResources section
+           1. Creating a map from DeviceClass names to the Kueue resource names
+              1. For every ResourceClaim that does not have an owner reference to a resourceclaimtemplate, the name/uuid
+                  of the ResourceClaim will be stored. This resourceclaim will not be counted again and workloads using it
+                  will automatically be admitted
+  2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'             # 4.1.1.1) get the canonical name for default-gpu-flavor
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com                # 4.1.1.1.1) create a map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        single-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1"                # 5) selected device count is reflected here in workload status
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
```

  Move this section to "Design Details".

- **keps/2941-DRA-Structured-Parameters/README.md:565** by [@mimowo](https://github.com/mimowo) — 2025-06-06T08:02:37Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+    - [Same Canonical Name Across Different Device Classes](#same-canonical-name-across-different-device-classes)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
+
+A problematic scenario can arise when multiple ResourceFlavors map the same device class to different canonical names within the same ClusterQueue. This creates ambiguity at admission time about which quota should be consumed.
+
+**Problematic Configuration Example:**
+
+```yaml
+# ResourceFlavor 1: Zone A configuration
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-a-flavor
+spec:
+  nodeLabels:
+    zone: zone-a
+  dynamicResources:
+  - name: zone-a-gpus
+    deviceClassNames:
+    - gpus.example.com  # Maps to zone-a-gpus
+
+---
+# ResourceFlavor 2: Zone B configuration  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-b-flavor
+spec:
+  nodeLabels:
+    zone: zone-b
+  dynamicResources:
+  - name: zone-b-gpus
+    deviceClassNames:
+    - gpus.example.com  # CONFLICT: Same device class mapped to zone-b-gpus
+
+---
+# ClusterQueue using both flavors
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "zone-a-gpus", "zone-b-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 2
+      - name: zone-b-gpus
+        nominalQuota: 0
+    - name: zone-b-flavor  
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 0
+      - name: zone-b-gpus
+        nominalQuota: 3
+```
+
+**The Problem:**
+When a workload requests `gpus.example.com`, Kueue faces ambiguity at admission time:
+- Should it count against `zone-a-gpus` quota (from zone-a-flavor)?
+- Should it count against `zone-b-gpus` quota (from zone-b-flavor)?
+
+This ambiguity exists **before** the scheduler performs node selection, so ResourceFlavor node labels cannot resolve it during admission.
+
+**Potential Issues:**
+1. **Resource waste**: One flavor's quota might be exhausted while another remains unused
+
+**Mitigation:**
+To avoid this scenario:
+- ensure that within a single ClusterQueue, each device class name maps to exactly one canonical name across a
+  ResourceFlavors. 
+- cluster admins should create separate deviceclasses to account for the dynamic resources that are being requested
+  in the resource flavor.
+
+#### Same Canonical Name Across Different Device Classes
+
+The following configuration is **NOT** problematic because each device class maps to exactly one ResourceFlavor/canonical-name combination:
+
+```yaml
+# ResourceFlavor 1: fancy-gpu-flavor maps gpu.example.com → GPUs
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: fancy-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: fancy
+  dynamicResources:
+  - name: GPUs  # canonical name
+    deviceClassNames:
+    - gpu.example.com  # device class maps to this flavor's "GPUs"
+
+---
+# ResourceFlavor 2: new-gpu-flavor maps new-gpu.example.com → GPUs  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: new-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: new
+  dynamicResources:
+  - name: GPUs  # same canonical name, different device class
+    deviceClassNames:
+    - new-gpu.example.com  # different device class maps to this flavor's "GPUs"
+```
+
+When a workload requests `gpu.example.com`, Kueue can unambiguously determine the mapping path:
+1. **Device class lookup**: `gpu.example.com` appears only in fancy-gpu-flavor's deviceClassNames
+2. **ResourceFlavor identification**: fancy-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from fancy-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against fancy-gpu-flavor's "GPUs" quota
+
+Similarly, when a workload requests `new-gpu.example.com`:
+1. **Device class lookup**: `new-gpu.example.com` appears only in new-gpu-flavor's deviceClassNames  
+2. **ResourceFlavor identification**: new-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from new-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against new-gpu-flavor's "GPUs" quota
+
+The fact that both flavors use the same canonical name "GPUs" doesn't create ambiguity because the device class name uniquely identifies which ResourceFlavor (and therefore which quota pool) should be used.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpu"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+  1. the name of the ResourceClaimTemplate or
+  2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+  1. For the workload a deviceClassMap will be created by:
+    1. Retrieving the LocalQueue and ClusterQueue for the workload
+      1. Getting all ResourceFlavors used in the ClusterQueue
+        1. For each ResourceFlavor, extracting its DynamicResources section
+           1. Creating a map from DeviceClass names to the Kueue resource names
+              1. For every ResourceClaim that does not have an owner reference to a resourceclaimtemplate, the name/uuid
+                  of the ResourceClaim will be stored. This resourceclaim will not be counted again and workloads using it
+                  will automatically be admitted
+  2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'             # 4.1.1.1) get the canonical name for default-gpu-flavor
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com                # 4.1.1.1.1) create a map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        single-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1"                # 5) selected device count is reflected here in workload status
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
+
+A new feature gate KueueDynamicResourceAllocation will be introduced, allowing users to test it in dev environments
```

  ```suggestion
  A new feature gate `DynamicResourceAllocation` will be introduced, allowing users to test it in dev environments
  ```

- **keps/2941-DRA-Structured-Parameters/README.md:561** by [@mimowo](https://github.com/mimowo) — 2025-06-06T08:10:59Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+    - [Same Canonical Name Across Different Device Classes](#same-canonical-name-across-different-device-classes)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
+
+A problematic scenario can arise when multiple ResourceFlavors map the same device class to different canonical names within the same ClusterQueue. This creates ambiguity at admission time about which quota should be consumed.
+
+**Problematic Configuration Example:**
+
+```yaml
+# ResourceFlavor 1: Zone A configuration
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-a-flavor
+spec:
+  nodeLabels:
+    zone: zone-a
+  dynamicResources:
+  - name: zone-a-gpus
+    deviceClassNames:
+    - gpus.example.com  # Maps to zone-a-gpus
+
+---
+# ResourceFlavor 2: Zone B configuration  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-b-flavor
+spec:
+  nodeLabels:
+    zone: zone-b
+  dynamicResources:
+  - name: zone-b-gpus
+    deviceClassNames:
+    - gpus.example.com  # CONFLICT: Same device class mapped to zone-b-gpus
+
+---
+# ClusterQueue using both flavors
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "zone-a-gpus", "zone-b-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 2
+      - name: zone-b-gpus
+        nominalQuota: 0
+    - name: zone-b-flavor  
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 0
+      - name: zone-b-gpus
+        nominalQuota: 3
+```
+
+**The Problem:**
+When a workload requests `gpus.example.com`, Kueue faces ambiguity at admission time:
+- Should it count against `zone-a-gpus` quota (from zone-a-flavor)?
+- Should it count against `zone-b-gpus` quota (from zone-b-flavor)?
+
+This ambiguity exists **before** the scheduler performs node selection, so ResourceFlavor node labels cannot resolve it during admission.
+
+**Potential Issues:**
+1. **Resource waste**: One flavor's quota might be exhausted while another remains unused
+
+**Mitigation:**
+To avoid this scenario:
+- ensure that within a single ClusterQueue, each device class name maps to exactly one canonical name across a
+  ResourceFlavors. 
+- cluster admins should create separate deviceclasses to account for the dynamic resources that are being requested
+  in the resource flavor.
+
+#### Same Canonical Name Across Different Device Classes
+
+The following configuration is **NOT** problematic because each device class maps to exactly one ResourceFlavor/canonical-name combination:
+
+```yaml
+# ResourceFlavor 1: fancy-gpu-flavor maps gpu.example.com → GPUs
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: fancy-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: fancy
+  dynamicResources:
+  - name: GPUs  # canonical name
+    deviceClassNames:
+    - gpu.example.com  # device class maps to this flavor's "GPUs"
+
+---
+# ResourceFlavor 2: new-gpu-flavor maps new-gpu.example.com → GPUs  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: new-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: new
+  dynamicResources:
+  - name: GPUs  # same canonical name, different device class
+    deviceClassNames:
+    - new-gpu.example.com  # different device class maps to this flavor's "GPUs"
+```
+
+When a workload requests `gpu.example.com`, Kueue can unambiguously determine the mapping path:
+1. **Device class lookup**: `gpu.example.com` appears only in fancy-gpu-flavor's deviceClassNames
+2. **ResourceFlavor identification**: fancy-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from fancy-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against fancy-gpu-flavor's "GPUs" quota
+
+Similarly, when a workload requests `new-gpu.example.com`:
+1. **Device class lookup**: `new-gpu.example.com` appears only in new-gpu-flavor's deviceClassNames  
+2. **ResourceFlavor identification**: new-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from new-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against new-gpu-flavor's "GPUs" quota
+
+The fact that both flavors use the same canonical name "GPUs" doesn't create ambiguity because the device class name uniquely identifies which ResourceFlavor (and therefore which quota pool) should be used.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpu"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+  1. the name of the ResourceClaimTemplate or
+  2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+  1. For the workload a deviceClassMap will be created by:
+    1. Retrieving the LocalQueue and ClusterQueue for the workload
+      1. Getting all ResourceFlavors used in the ClusterQueue
+        1. For each ResourceFlavor, extracting its DynamicResources section
+           1. Creating a map from DeviceClass names to the Kueue resource names
+              1. For every ResourceClaim that does not have an owner reference to a resourceclaimtemplate, the name/uuid
+                  of the ResourceClaim will be stored. This resourceclaim will not be counted again and workloads using it
+                  will automatically be admitted
+  2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'             # 4.1.1.1) get the canonical name for default-gpu-flavor
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com                # 4.1.1.1.1) create a map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        single-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1"                # 5) selected device count is reflected here in workload status
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  I would suggest to align this section with the template, and analoguus section in the core k8s KEPS. Eg. https://github.com/kubernetes/enhancements/blob/master/keps/sig-node/4381-dra-structured-parameters/README.md#graduation-criteria.
  
  So, move the "Feature Gate" section under "Design Details".
  
  Here, add (as proposal under discussion, I'm ok to adjust, but 
  ```
  #### Alpha
  
  - the implementation behind the [feature gate](add link to the section) flag in alpha
  - support Beta API of the core k8s
  - initial e2e tests for baseline scenario
  
  #### Beta
  
  - the feature gate in Beta
  - all known bugs are fixed
  - support integration with v1 API of DRA in core k8s
  - e2e tests
  - re-evaluate the support for TopologyAwareScheduling (might be moved to GA)
  
  #### GA
  
  - the feature gate in stable
  - integration with TopologyAwareScheduling
  ```

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-06T13:43:49Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-06T13:43:49Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  @mimowo Thanks for exploration of these ideas, it agree with you (Flavor, Resource) model is apt abstraction for this design. For this model, one of the advantages of core resources like cpu and memory is a generic, cluster-wide understanding of what cpu means in the cluster. 
  
  With dynamicResources, this meaning could differ between resource flavors in the same cluster, for example:
  
  ```
  RF1 -> dynamicResource: "gpu" -> deviceClassNames -> gpu-A.example.com
  RF2 -> dynamicResource: "gpu" -> deviceClassNames -> gpu-B.example.com
  ```
  
  This leads to confusing definition of CQ:
  
  ```yaml
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "single-tenant-multi-provsioning"
  spec:
    resourceGroups:
    - coveredResources: ["cpu", "memory", "fency-resource"]
      flavors:
      - name: "rf1"
        resources:
        - name: "gpu"
          nominalQuota: 50   # Admin thinks: 50 GPUs
      - name: "rf2"
        resources:
        - name: "gpu"
          nominalQuota: 30   # Admin thinks: 30 GPUs, total = 80 GPUs
  ```
  
  Admin believes they're offering 80 identical "gpu" resources, but they're actually offering 50 gpu-A + 30 gpu-B GPUs (different capabilities).
  
  @mimowo do you see this problematic?  I am willing to explore a cluster scoped CRD "DeviceClassMapping" which can provide a unique, commonly understood semantic name of a dynamic resource avoiding the above confusion.  It seems to me that moving the definition out of resourceflavor into a separate CRD provides a lot of simplicity to the design.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T14:11:28Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-06T14:11:28Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  > With dynamicResources, this meaning could differ between resource flavors in the same cluster, for example:
  
  I see what you mean
  
  > @mimowo do you see this problematic?
  
  it would be better to avoid indeed. 
  
  > I am willing to explore a cluster scoped CRD "DeviceClassMapping"
  
  Something like:
  ```yaml
  kind: DeviceClassMapping
  spec:
    - resourceName: "gpu-a"
      deviceClassNames: ["gpu-A.example.com"]
    - resourceName: "gpu-b"
      deviceClassNames: ["gpu-B.example.com"]
  ```
  , plus then resource flavors would just have
  ```yaml
  kind: ResourceFlavor
  apiVersion: kueue.x-k8s.io/v1beta1
  metadata:
    name: "flavor-a"
  spec:
    nodeLabels: 
      provider.com/zone: x
    dynamicResources:
    - name: "gpu-a"
  ```
  Is this the idea? We could also have it in the config map as, eg:
  
  ```yaml
  dra:
    deviceClassMappings:
    - resourceName: "gpu-a"
      deviceClassNames: ["gpu-A.example.com"]
    - resourceName: "gpu-b"
      deviceClassNames: ["gpu-B.example.com"]
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T14:12:20Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-06T14:12:20Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  if we are on the same page, then the remaining choice is new CRD DeviceClassMapping vs. global config map.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-06T14:16:26Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-06T14:16:26Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  ```yaml
  kind: DeviceClassMapping
  spec:
    - resourceName: "gpu-a"
      deviceClassNames: ["gpu-A.example.com"]
    - resourceName: "gpu-b"
      deviceClassNames: ["gpu-B.example.com"]
  ```
  
  @mimowo yes, this is the idea, once we have a CR like this, do we need this to be embedded in RF? I was thinking that admins can directly use the DCM in CQ, like this:
  
  ```yaml
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "single-tenant-multi-provsioning"
  spec:
    resourceGroups:
    - coveredResources: ["cpu", "memory", "gpu-a", "gpu-b"]
      flavors:
      - name: "rf1"
        resources:
        - name: "gpu-a"
          nominalQuota: 50
      - name: "rf2"
        resources:
        - name: "gpu-b"
          nominalQuota: 30
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T14:24:06Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-06T14:24:06Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  Yes, you are right actually. We can simplify the ResourceFlavor API.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T14:33:04Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-06T14:33:04Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  So we have on the table two options:
  
  1. DeviceClassMapping API
  ```yaml
  kind: DeviceClassMapping
  spec:
    mapping:
    - resourceName: "gpu-a"
      deviceClassNames: ["gpu-A.example.com"]
    - resourceName: "gpu-b"
      deviceClassNames: ["gpu-B.example.com"]
  ```
  2. Extending the global config map:
  ```yaml
  dra:
    deviceClassMappings:
    - resourceName: "gpu-a"
      deviceClassNames: ["gpu-A.example.com"]
    - resourceName: "gpu-b"
      deviceClassNames: ["gpu-B.example.com"]
  ```
  
  Here is my short summary of pros & cons for (1.):
  
  pros:
  - allows to add new devices without Kueue restart
  - could be Alpha API, so no need for backwards-compatibility
  
  cons:
  - more complicated implementation
  - validation is harder to detect if we have multiple instances
  - not clear if we ever need more than one instance
  
  I'm ok with either for Alpha.  Maybe we could mitigate the cons by reading data only from the instance named "default", and have webhook which does not allow to create instances with other names?
  
  @alaypatel07 @tenzen-y do you have more suggestions or preferences?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-06T14:37:02Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-06T14:37:02Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  thanks @mimowo. I'll update the design accordingly.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-06T15:10:11Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-06T15:10:10Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  > I'm ok with either for Alpha. Maybe we could mitigate the cons by reading data only from the instance named "default", and have webhook which does not allow to create instances with other names?
  
  +1, I agree, we can have a singleton CR that declares the mapping. There are couple more pros to the approach:
  
  Pros for 1:
  1. if the mapping declaration is incorrect, with the API approach, the webhook simply denies and user can retry. If the mapping declaration is incorrect, with configmap, the debug cycle will involve waiting for pod to crashloop, getting the logs, figuring out issue and fixing it. This will also mean that an alpha feature mis-configuration is causing service outages to all the kueue features.
  2. the API allows for typed data with doc comments, which is lacking in configmap
  
  With the above, I have a slight preference to using API types, but I am okay with either.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T15:15:44Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-06T15:15:43Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  sgtm, I can support the singleton CRD, in alpha, idea. 
  
  Also, the configmap is already beta API, so higher expectations on API backwards-compatibility. Also, the CRD is more consistent with the long term plan of allowing updates without restarting Kueue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T15:17:48Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-06T15:17:48Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  We didn't think of the "singleton" CRD, when designing the ResourceTransformation API, but looking at this back, it would also be a good fit there, as we now require Kueue restart. cc @dgrove-oss

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (commented) — 2025-06-06T15:28:10Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-06-06T15:28:10Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  Using a singleton CRD for ResourceTransformation seems plausible to me.  Should we open a separate issue to track it?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-06T15:29:06Z

- **keps/2941-DRA-Structured-Parameters/README.md:265** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-06T15:29:06Z
```diff
@@ -0,0 +1,416 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Resource Claim By Count](#resource-claim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has three APIs that are relevant for a Kueue:
+
+- Resource Claims
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for workloads.
+Kueue does not need to be aware of these resources.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying the `ResourceQuota` struct to allow configuring the field
+to distinguish a core resource from a dynamic resource.
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use the DRA API to provide more control over the scheduling of devices.
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type ResourceQuota struct {
+    // kind is used to configure if this is for a DRA Device. Its value will be DeviceClass.
+    // +featureGate=KueueDynamicResourceAllocation
+    Kind *string `json:"kind,omitempty"`
+    
+    // deviceClassNames lists the names of all the device classes that will count against
+    // the quota defined in this resource quota object.
+    // +listType=atomic
+    // +optional
+    // +featureGate=KueueDynamicResourceAllocation
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames,omitempty"`
+}
+```
+
+Kind field in ResourceQuota allows Kueue to distinguish between a Core resource and a DeviceClass. When the value of kind
+field is configured to be DeviceClass, the `name` becomes a canonical name of the collection of resources that can be 
+provisioned for each device class present in `deviceClassNames` field.
+
+
+With this, a ClusterQueue could be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "cluster-queue"
+spec:
+  namespaceSelector: {} # match all.
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "gpu.example.com"]
+    flavors:
+    - name: "default-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "200Mi"
+      - name: "single-gpu"
+        deviceClassNames: ["gpu.example.com"]
+        nominalQuota: 2
+        kind: "DeviceClass"
+      - name: "shared-gpus"
+        deviceClassNames: 
+        - "ts-shared.gpu.example.com"
+        - "sp-shared.gpu.example.com"
+        nominalQuota: 2
+        kind: "DeviceClass"
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class. This allows for quota to be 
+configured in Kueue without any modifications. 
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+     1. the name of the ResourceClaimTemplate or
+     2. the name of the ResourceClaim
+   
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at 
+   1. For the workload a deviceClassMap will be created, which is map of deviceClass -> canonical name in cluster queue
+   2. for each device class the canonical quota name will be looked up and resource will be counted against it.
```

  @kannon92 this seems to be a kueue only problem.
  
  For alpha, we will go with the approach that within a single clusterqueue, if the resource claim is already allocated, we will skip counting it against the quota.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T15:31:33Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@mimowo](https://github.com/mimowo) — 2025-06-06T15:31:33Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  Yeah, let's do so to offload the discussion here. As the motivation we could provide avoiding kueue restarts on update. I'm not sure how important this is in that case, or how to migrate, but we can put details later, it is not urgent by any means.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-06-06T15:53:11Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:35** by [@kannon92](https://github.com/kannon92) — 2025-06-06T15:53:11Z
```diff
@@ -0,0 +1,40 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2025-05-02
+reviewers:
+  - "@kannon92"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+see-also:
+  - "https://github.com/kubernetes/enhancements/blob/master/keps/sig-node/4381-dra-structured-parameters/README.md"
+replaces:
+  - "https://github.com/kubernetes-sigs/kueue/pull/3071"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.13"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.13"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDynamicResourceAllocation
```

  Part of the issue is DRA is really an idea and not an exact feature. So DRAIntegration can get a bit stale over time.
  
  Maybe we can call this DeviceClassIntegration?
  
  DRA has something like 15-20 features getting proposed so I worry that if we call this DRA we will forever be adding to this feature as new DRA features roll out.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T16:00:27Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:35** by [@mimowo](https://github.com/mimowo) — 2025-06-06T16:00:27Z
```diff
@@ -0,0 +1,40 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2025-05-02
+reviewers:
+  - "@kannon92"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+see-also:
+  - "https://github.com/kubernetes/enhancements/blob/master/keps/sig-node/4381-dra-structured-parameters/README.md"
+replaces:
+  - "https://github.com/kubernetes-sigs/kueue/pull/3071"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.13"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.13"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDynamicResourceAllocation
```

  Good point, with the new features added to core DRA we may need to have new extensions in Kueue, potentially guarded by different feature gates.
  
  We had a similar case for TAS, and we have one global gate [TopologyAwareScheduling](https://github.com/kubernetes-sigs/kueue/blob/30239528fb59f0d99d5be0195c55665fea6503c0/pkg/features/kube_features.go#L97), but for smaller sub-features we group them by feature gates prefixed with "TAS" [examples](https://github.com/kubernetes-sigs/kueue/blob/30239528fb59f0d99d5be0195c55665fea6503c0/pkg/features/kube_features.go#L137-L147), wdyt?
  
  I think similarly we could later have "DRAMultiNodeScheduling" or whatever.
  
  I still prefer "DynamicResourceAllocation" as the main global gate, rather than DeviceClassIntegration, bacause it will be much more "discoverable" to admins.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-06-06T18:17:44Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@kannon92](https://github.com/kannon92) — 2025-06-06T18:17:44Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  +1 on CRD. I really want to avoid putting more things in global config map.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-07T03:13:36Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:35** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-07T03:13:35Z
```diff
@@ -0,0 +1,40 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2025-05-02
+reviewers:
+  - "@kannon92"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+see-also:
+  - "https://github.com/kubernetes/enhancements/blob/master/keps/sig-node/4381-dra-structured-parameters/README.md"
+replaces:
+  - "https://github.com/kubernetes-sigs/kueue/pull/3071"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.13"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.13"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDynamicResourceAllocation
```

  +1 to DynamicResourceAllocation, will update accordingly. Keeping this thread open if @tenzen-y wants to add any additional opinions

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-07T03:14:10Z

- **keps/2941-DRA-Structured-Parameters/README.md:186** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-07T03:14:10Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
```

  Resolving this thread, @mimowo @tenzen-y @kannon92 the design has been updated to reflect the discussion here, PTAL

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-07T03:14:51Z

- **keps/2941-DRA-Structured-Parameters/README.md:254** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-07T03:14:51Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
+
+A problematic scenario can arise when multiple ResourceFlavors map the same device class to different canonical names within the same ClusterQueue. This creates ambiguity at admission time about which quota should be consumed.
+
+**Problematic Configuration Example:**
+
+```yaml
+# ResourceFlavor 1: Zone A configuration
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-a-flavor
+spec:
+  nodeLabels:
+    zone: zone-a
+  dynamicResources:
+  - name: zone-a-gpus
+    deviceClassNames:
+    - gpus.example.com  # Maps to zone-a-gpus
+
+---
+# ResourceFlavor 2: Zone B configuration  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-b-flavor
+spec:
+  nodeLabels:
+    zone: zone-b
+  dynamicResources:
+  - name: zone-b-gpus
+    deviceClassNames:
+    - gpus.example.com  # CONFLICT: Same device class mapped to zone-b-gpus
+
+---
+# ClusterQueue using both flavors
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "zone-a-gpus", "zone-b-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 2
+      - name: zone-b-gpus
+        nominalQuota: 0
+    - name: zone-b-flavor  
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 0
+      - name: zone-b-gpus
+        nominalQuota: 3
+```
+
+**The Problem:**
+When a workload requests `gpus.example.com`, Kueue faces ambiguity at admission time:
+- Should it count against `zone-a-gpus` quota (from zone-a-flavor)?
+- Should it count against `zone-b-gpus` quota (from zone-b-flavor)?
+
+This ambiguity exists **before** the scheduler performs node selection, so ResourceFlavor node labels cannot resolve it during admission.
+
+**Potential Issues:**
+1. **Incorrect quota accounting**: Workloads might be admitted against the wrong ResourceFlavor's quota  
+1. **Resource waste**: One flavor's quota might be exhausted while another remains unused
+1. **Scheduling failures**: Workloads might be admitted but fail to schedule if counted against the wrong flavor
+
+#### Same Canonical name with different deviceClassNames list
```

  This is not an issue anymore with new design, resolving the thread.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-07T03:15:11Z

- **keps/2941-DRA-Structured-Parameters/README.md:561** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-07T03:15:10Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+    - [Same Canonical Name Across Different Device Classes](#same-canonical-name-across-different-device-classes)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
+
+A problematic scenario can arise when multiple ResourceFlavors map the same device class to different canonical names within the same ClusterQueue. This creates ambiguity at admission time about which quota should be consumed.
+
+**Problematic Configuration Example:**
+
+```yaml
+# ResourceFlavor 1: Zone A configuration
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-a-flavor
+spec:
+  nodeLabels:
+    zone: zone-a
+  dynamicResources:
+  - name: zone-a-gpus
+    deviceClassNames:
+    - gpus.example.com  # Maps to zone-a-gpus
+
+---
+# ResourceFlavor 2: Zone B configuration  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-b-flavor
+spec:
+  nodeLabels:
+    zone: zone-b
+  dynamicResources:
+  - name: zone-b-gpus
+    deviceClassNames:
+    - gpus.example.com  # CONFLICT: Same device class mapped to zone-b-gpus
+
+---
+# ClusterQueue using both flavors
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "zone-a-gpus", "zone-b-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 2
+      - name: zone-b-gpus
+        nominalQuota: 0
+    - name: zone-b-flavor  
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 0
+      - name: zone-b-gpus
+        nominalQuota: 3
+```
+
+**The Problem:**
+When a workload requests `gpus.example.com`, Kueue faces ambiguity at admission time:
+- Should it count against `zone-a-gpus` quota (from zone-a-flavor)?
+- Should it count against `zone-b-gpus` quota (from zone-b-flavor)?
+
+This ambiguity exists **before** the scheduler performs node selection, so ResourceFlavor node labels cannot resolve it during admission.
+
+**Potential Issues:**
+1. **Resource waste**: One flavor's quota might be exhausted while another remains unused
+
+**Mitigation:**
+To avoid this scenario:
+- ensure that within a single ClusterQueue, each device class name maps to exactly one canonical name across a
+  ResourceFlavors. 
+- cluster admins should create separate deviceclasses to account for the dynamic resources that are being requested
+  in the resource flavor.
+
+#### Same Canonical Name Across Different Device Classes
+
+The following configuration is **NOT** problematic because each device class maps to exactly one ResourceFlavor/canonical-name combination:
+
+```yaml
+# ResourceFlavor 1: fancy-gpu-flavor maps gpu.example.com → GPUs
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: fancy-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: fancy
+  dynamicResources:
+  - name: GPUs  # canonical name
+    deviceClassNames:
+    - gpu.example.com  # device class maps to this flavor's "GPUs"
+
+---
+# ResourceFlavor 2: new-gpu-flavor maps new-gpu.example.com → GPUs  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: new-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: new
+  dynamicResources:
+  - name: GPUs  # same canonical name, different device class
+    deviceClassNames:
+    - new-gpu.example.com  # different device class maps to this flavor's "GPUs"
+```
+
+When a workload requests `gpu.example.com`, Kueue can unambiguously determine the mapping path:
+1. **Device class lookup**: `gpu.example.com` appears only in fancy-gpu-flavor's deviceClassNames
+2. **ResourceFlavor identification**: fancy-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from fancy-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against fancy-gpu-flavor's "GPUs" quota
+
+Similarly, when a workload requests `new-gpu.example.com`:
+1. **Device class lookup**: `new-gpu.example.com` appears only in new-gpu-flavor's deviceClassNames  
+2. **ResourceFlavor identification**: new-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from new-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against new-gpu-flavor's "GPUs" quota
+
+The fact that both flavors use the same canonical name "GPUs" doesn't create ambiguity because the device class name uniquely identifies which ResourceFlavor (and therefore which quota pool) should be used.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpu"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+  1. the name of the ResourceClaimTemplate or
+  2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+  1. For the workload a deviceClassMap will be created by:
+    1. Retrieving the LocalQueue and ClusterQueue for the workload
+      1. Getting all ResourceFlavors used in the ClusterQueue
+        1. For each ResourceFlavor, extracting its DynamicResources section
+           1. Creating a map from DeviceClass names to the Kueue resource names
+              1. For every ResourceClaim that does not have an owner reference to a resourceclaimtemplate, the name/uuid
+                  of the ResourceClaim will be stored. This resourceclaim will not be counted again and workloads using it
+                  will automatically be admitted
+  2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'             # 4.1.1.1) get the canonical name for default-gpu-flavor
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com                # 4.1.1.1.1) create a map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        single-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1"                # 5) selected device count is reflected here in workload status
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
```

  updated.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-07T03:16:22Z

- **keps/2941-DRA-Structured-Parameters/README.md:563** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-07T03:16:22Z
```diff
@@ -0,0 +1,709 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+    - [Same Canonical Name Across Different Device Classes](#same-canonical-name-across-different-device-classes)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Resource Quota API](#resource-quota-api)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Feature Gate](#feature-gate)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices will not be
+    included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Device Class Mapping Ambiguity
+
+A problematic scenario can arise when multiple ResourceFlavors map the same device class to different canonical names within the same ClusterQueue. This creates ambiguity at admission time about which quota should be consumed.
+
+**Problematic Configuration Example:**
+
+```yaml
+# ResourceFlavor 1: Zone A configuration
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-a-flavor
+spec:
+  nodeLabels:
+    zone: zone-a
+  dynamicResources:
+  - name: zone-a-gpus
+    deviceClassNames:
+    - gpus.example.com  # Maps to zone-a-gpus
+
+---
+# ResourceFlavor 2: Zone B configuration  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: zone-b-flavor
+spec:
+  nodeLabels:
+    zone: zone-b
+  dynamicResources:
+  - name: zone-b-gpus
+    deviceClassNames:
+    - gpus.example.com  # CONFLICT: Same device class mapped to zone-b-gpus
+
+---
+# ClusterQueue using both flavors
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "zone-a-gpus", "zone-b-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 2
+      - name: zone-b-gpus
+        nominalQuota: 0
+    - name: zone-b-flavor  
+      resources:
+      - name: zone-a-gpus
+        nominalQuota: 0
+      - name: zone-b-gpus
+        nominalQuota: 3
+```
+
+**The Problem:**
+When a workload requests `gpus.example.com`, Kueue faces ambiguity at admission time:
+- Should it count against `zone-a-gpus` quota (from zone-a-flavor)?
+- Should it count against `zone-b-gpus` quota (from zone-b-flavor)?
+
+This ambiguity exists **before** the scheduler performs node selection, so ResourceFlavor node labels cannot resolve it during admission.
+
+**Potential Issues:**
+1. **Resource waste**: One flavor's quota might be exhausted while another remains unused
+
+**Mitigation:**
+To avoid this scenario:
+- ensure that within a single ClusterQueue, each device class name maps to exactly one canonical name across a
+  ResourceFlavors. 
+- cluster admins should create separate deviceclasses to account for the dynamic resources that are being requested
+  in the resource flavor.
+
+#### Same Canonical Name Across Different Device Classes
+
+The following configuration is **NOT** problematic because each device class maps to exactly one ResourceFlavor/canonical-name combination:
+
+```yaml
+# ResourceFlavor 1: fancy-gpu-flavor maps gpu.example.com → GPUs
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: fancy-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: fancy
+  dynamicResources:
+  - name: GPUs  # canonical name
+    deviceClassNames:
+    - gpu.example.com  # device class maps to this flavor's "GPUs"
+
+---
+# ResourceFlavor 2: new-gpu-flavor maps new-gpu.example.com → GPUs  
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: new-gpu-flavor
+spec:
+  nodeLabels:
+    gpu-tier: new
+  dynamicResources:
+  - name: GPUs  # same canonical name, different device class
+    deviceClassNames:
+    - new-gpu.example.com  # different device class maps to this flavor's "GPUs"
+```
+
+When a workload requests `gpu.example.com`, Kueue can unambiguously determine the mapping path:
+1. **Device class lookup**: `gpu.example.com` appears only in fancy-gpu-flavor's deviceClassNames
+2. **ResourceFlavor identification**: fancy-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from fancy-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against fancy-gpu-flavor's "GPUs" quota
+
+Similarly, when a workload requests `new-gpu.example.com`:
+1. **Device class lookup**: `new-gpu.example.com` appears only in new-gpu-flavor's deviceClassNames  
+2. **ResourceFlavor identification**: new-gpu-flavor
+3. **Canonical name determination**: "GPUs" (from new-gpu-flavor's dynamicResources)
+4. **Quota accounting**: Count against new-gpu-flavor's "GPUs" quota
+
+The fact that both flavors use the same canonical name "GPUs" doesn't create ambiguity because the device class name uniquely identifies which ResourceFlavor (and therefore which quota pool) should be used.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes ResourceQuota is
+   namespace scoped. As a result, admin users can account for quota independent of user workloads. On the contrary, with 
+   Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using devices in admin namespace
+   if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could allocated 
+   to a single claim will be used against quota.
+
+
+## Design Details
+
+### Resource Quota API
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpu"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Workloads
+
+When a user submits a workload and KueueDynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+  1. the name of the ResourceClaimTemplate or
+  2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+  1. For the workload a deviceClassMap will be created by:
+    1. Retrieving the LocalQueue and ClusterQueue for the workload
+      1. Getting all ResourceFlavors used in the ClusterQueue
+        1. For each ResourceFlavor, extracting its DynamicResources section
+           1. Creating a map from DeviceClass names to the Kueue resource names
+              1. For every ResourceClaim that does not have an owner reference to a resourceclaimtemplate, the name/uuid
+                  of the ResourceClaim will be stored. This resourceclaim will not be counted again and workloads using it
+                  will automatically be admitted
+  2. for each device class the canonical quota name will be looked up and resource will be counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu. #1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name from read from #1
+        resourceClaimTemplateName: single-gpu # 2.i) the name for resource claim templates 
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'             # 4.1.1.1) get the canonical name for default-gpu-flavor
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com                # 4.1.1.1.1) create a map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        single-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1"                # 5) selected device count is reflected here in workload status
+```
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+I am not sure if we can test DRA functionality (requiring alpha features enabled) at the integration level.
+
+DRA requires a kubelet plugin so this may not be a good candidate for an integration test.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Feature Gate
```

  couldnt find the Feature Gate section in Design Details, I have updated the document to reflect the feature gate there, PTAL

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-10T18:45:45Z

LGTM overall. I think the remaining open question is if the API starts with "Alpha" or "Beta". I would probably start with Alpha since the feature is expected to evolve as the core DRA evolves.

- **keps/2941-DRA/README.md:171** by [@mimowo](https://github.com/mimowo) — 2025-06-10T18:36:42Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
```

  ```suggestion
  1. Introduce API to allow creating a mapping canonical name -> device class names list
  ```
  since we migrate to use CRD for the mapping.

- **keps/2941-DRA/README.md:216** by [@mimowo](https://github.com/mimowo) — 2025-06-10T18:37:44Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  I would suggest naming the CRD more generically like "DynamicResourceAllocation" so that we have natural place to expand the configuration in the future.
  
  We also discussed this with @tenzen-y recently offline

- **keps/2941-DRA/README.md:251** by [@mimowo](https://github.com/mimowo) — 2025-06-10T18:38:59Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  No strong view, but maybe we call it "CanonicalName" since we use it in the doc in many places, and when talking?

- **keps/2941-DRA/README.md:220** by [@mimowo](https://github.com/mimowo) — 2025-06-10T18:41:00Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
```

  ```suggestion
  // DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
  // used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the
  // CRD instance.
  ```

- **keps/2941-DRA/README.md:256** by [@mimowo](https://github.com/mimowo) — 2025-06-10T18:42:14Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping
+metadata:
+  name: "cluster-device-mapping"  # Fixed name - singleton CR, only one instance allowed
```

  ```suggestion
    name: "default"  # Fixed name - singleton CR, only one instance allowed
  ```
  for simplicity

- **keps/2941-DRA/README.md:253** by [@mimowo](https://github.com/mimowo) — 2025-06-10T18:42:32Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
```

  I would suggest to start with "Alpha" as the API is expected to grow.

- **keps/2941-DRA/README.md:540** by [@mimowo](https://github.com/mimowo) — 2025-06-10T18:43:38Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping
+metadata:
+  name: "cluster-device-mapping"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DeviceClassMapping CR. This creates
+uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DeviceClassMapping
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+   1. the name of the ResourceClaimTemplate or
+   2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+   1. For the workload a deviceClassMap will be created by:
+      1. Retrieving the singleton DeviceClassMapping resource (named "cluster-device-mapping")
+      2. Creating a map from DeviceClass names to canonical resource names using the DeviceClassMapping.spec.dynamicResources
+         entries
+      3. For resource claims that are already allocated, Kueue will maintain a mapping called `admittedResourceClaims`
+         where the key follows the format `{clusterQueueName}/{resourceClaimNamespace}/{resourceClaimName}`
+      4. If a workload is using an already allocated resource claim and the key
+         `{targetClusterQueueName}/{resourceClaimNamespace}/{resourceClaimName}` exists in the `admittedResourceClaims`
+         map, Kueue will skip counting that claim against quota and automatically admit the workload
+      5. This approach ensures that within a single cluster queue, reusing allocated resource claims doesn't consume
+         additional quota, while still enforcing quota boundaries between different cluster queues
+   2. For each device class the canonical quota name will be looked up from the deviceClassMap and resource will be
+      counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change.
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu # 1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name read from #1
+        resourceClaimTemplateName: single-gpu # 2.1) the name for resource claim template
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping # 4.1.1) Singleton CR that defines all device class mappings
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com # 4.1.2) Creating map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus' # 4.2) The canonical name from DeviceClassMapping
+        nominalQuota: 2
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No dynamicResources field - mapping is now in DeviceClassMapping CR
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        whole-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1" # 5) selected device count is reflected here in workload status
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+Integration tests in Kueue use controller-runtime's envtest framework, which provides a real Kubernetes API server
+without requiring kubelet or other cluster components. While DRA device allocation requires kubelet plugins, the core
+DRA integration functionality for Kueue can be tested at the integration level by:
+
+- Testing DeviceClassMapping CRD creation and validation
+- Verifying workload admission logic with DRA resource claims
+- Testing quota enforcement against device class mappings
+- Validating resource counting and flavor assignment for DRA resources
+
+The integration tests would focus on Kueue's quota management and admission logic rather than actual device allocation,
+using mock ResourceClaims and DeviceClasses to simulate DRA workloads.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Alpha
+
+- the implementation behind the [feature gate](add link to the section) flag in alpha
```

  ```suggestion
  - the implementation behind the feature gate flag in alpha
  ```

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-10T19:18:48Z

- **keps/2941-DRA/README.md:216** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-10T19:18:48Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  > I would suggest naming the CRD more generically like "DynamicResourceAllocation" so that we have natural place to expand the configuration in the future.
  
  With this name my concern is that we are tying the kueue abstractions too closely with the k8s feature. In essence, what are introducing is a Kueue abstraction, a ResourceMapping, that could be extended in the future for example for non-dra use-cases, say the ResourceTransformations with device-plugins could be housed here in this API.
  
  For that reason, I wanted to name this CRD generically, how about `ResourceMapping`? However I am open to be convinced otherwise.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-10T19:27:02Z

- **keps/2941-DRA/README.md:251** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-10T19:27:02Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  I have considered something like this, but it has the following problem:
  ```yaml
  apiVersion: kueue.x-k8s.io/v1alpha1
  kind: DeviceClassMapping
  metadata:
    name: "default" 
  spec:
    dynamicResources:
    - canonnicalName: gpus
      deviceClassNames:
      - gpu.example.com
  ---
  apiVersion: kueue.x-k8s.io/v1beta1
  kind: ClusterQueue
  metadata:
    name: "gpus-cluster-queue"
  spec:
    resourceGroups:
    - coveredResources: ["cpu", "memory", "gpus"]
      flavors:
      - name: "default-gpu-flavor"
        resources:
        - name: "gpus"                                        # <--- the fields here do not match
          nominalQuota: 9
  ```
  
  From UX point of view, keeping the field names consistent when cross-referencing in clusterqueue seem the most natural, DRA feature in k8s does this in its APIs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-10T19:33:23Z

- **keps/2941-DRA/README.md:216** by [@mimowo](https://github.com/mimowo) — 2025-06-10T19:33:23Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  I see the point, we could then use it to host also out parameters, currently hosted in the global configmap which are related to resources, like excludeResourcePrefixes or resourceTransformations.
  
  It could host DRA for now, but also other resource-related params in the future. Note that hosting them in one CRD might be useful to perform validation with CEL rules. 
  
  So I'm ok with both:
  1. specific DynamicResourceAllocation
  2. generic ResourcesConfiguation (or ResourceMapping)
  
  I'm leaning towards 2. but it is only a slight preference. wdyt @tenzen-y?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-10T19:34:42Z

- **keps/2941-DRA/README.md:216** by [@mimowo](https://github.com/mimowo) — 2025-06-10T19:34:42Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  In any case since this is going to be Alpha, then we can relatively easily change our minds in that phase

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-06-10T19:36:25Z

- **keps/2941-DRA/README.md:251** by [@nojnhuh](https://github.com/nojnhuh) — 2025-06-10T19:36:25Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  I tend to agree, I like the symmetry of having both fields called `name`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-10T19:39:12Z

- **keps/2941-DRA/README.md:251** by [@mimowo](https://github.com/mimowo) — 2025-06-10T19:39:12Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  got it, I'm ok as is, just poining out as a potential thing to consider for @tenzen-y too

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-06-10T21:49:08Z

LGTM overall from a DRA perspective. Left one small question about another edge case I just thought of.

- **keps/2941-DRA/README.md:360** by [@nojnhuh](https://github.com/nojnhuh) — 2025-06-10T21:33:46Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DeviceClassMapping CR. This creates
+uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DeviceClassMapping
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
```

  Should we clarify that only the container-level resources are checked and not the [(alpha) pod-level resources](https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/#example-2) (assuming those are unsupported like we're calling the DRA alpha features)?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-11T07:38:56Z

- **keps/2941-DRA/examples/problematic-resourceflavors/problematic-rf-cq.yaml:1** by [@mimowo](https://github.com/mimowo) — 2025-06-11T07:38:56Z
```diff
@@ -0,0 +1,46 @@
+---
```

  Is this file referenced from the body of the text? If not, then drop it, or make sure referenced. Same for other example files. 
  
  If referenced please also refresh their content to align with the recent API.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-11T17:05:33Z

- **keps/2941-DRA/README.md:269** by [@mimowo](https://github.com/mimowo) — 2025-06-11T17:05:33Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
```

  I would probably suggest keeping it under kueue-system namespace. This way it is closer to the ConfigMap model, which is intrinsically namespaced. Maybe using helm one could imagine two separete installations of Kueue. It is very unlikely, but possible.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-13T09:00:56Z

- **keps/2941-DRA/README.md:216** by [@mimowo](https://github.com/mimowo) — 2025-06-13T09:00:56Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  cc @mwielgus also for opinion

### Review by [@jackfrancis](https://github.com/jackfrancis) (commented) — 2025-06-16T19:30:32Z

- **keps/2941-DRA/examples/problematic-resourceflavors/problematic-rf-cq.yaml:1** by [@jackfrancis](https://github.com/jackfrancis) — 2025-06-16T19:30:32Z
```diff
@@ -0,0 +1,46 @@
+---
```

  +1, I vote to keep the examples in place that reflect the current agreed upon API, and to link to them (something like [more examples here](./examples/) at the bottom of the `### Workloads` section should suffice.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-17T02:17:58Z

- **keps/2941-DRA/README.md:216** by [@mimowo](https://github.com/mimowo) — 2025-06-17T02:17:58Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  @alaypatel07  I had a chance to discuss this with @tenzen-y.
  
  TLDR: we agreed to go with DynamicResourceAllocation CRD in alpha. 
  
  The main reason for using CRD instead of configmap is that configmap is already beta, so we would need to be more careful about API changes. We expect DRA to evolve in the coming releases of k8s, so it is preferred to go via Alpha in Kueue.
  
  Regarding the generic "ResourcesConfigurations" CRD we prefer specific, because sometimes the boundaries between features and fields are fuzzy. For example, MultiKueue may want to introduce some specific resource names, and it would not be clear where to declare the parameters.
  
  Going forward, we will consider moving the configmap to CRD https://github.com/kubernetes-sigs/kueue/issues/5646. If this happens before moving DRA to Beta, then we could merge the configs. If this does not happen by then, then we may end up with two different configuration CRDs (global and DRA), we are ok with both possibilities.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-17T02:36:59Z

- **keps/2941-DRA/README.md:216** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-17T02:36:59Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  @mimowo de-coupling current configmap API and DRA and having a possibility to merge configs in bet makes a lot of sense to me, thanks for the summary. I will re-work the design to name this CRD DynamicResourceAllocation. 
  
  Although I have one small concern, DynamicResourceAllocation is a K8s concept, the CRD this design is introducing is purely a kueue concept, are the abstractions little ambiguous for users?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-17T02:48:07Z

- **keps/2941-DRA/README.md:216** by [@mimowo](https://github.com/mimowo) — 2025-06-17T02:48:07Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  I think this is ok, because the API is inside the Kueue Group, and the instance will be inside the kueue-system namespace. 
  
  Also, I think it is actually beneficial for discover ability, if we name it say ResourceMapping users may not know immediately this is for DRA configuration. Moreover the name may not fit some sub features of DRA which are not related directly to resource mapping.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-17T07:41:13Z

- **keps/2941-DRA/README.md:216** by [@mimowo](https://github.com/mimowo) — 2025-06-17T07:41:13Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  I had one more chance to discuss this with @tenzen-y during KubeCon
  
  The only nit adjustment which came out is to call it  DynamicResourceAllocationConfig as the suffix would align it with the naming with ProvisioningRequestConfig CRD we already have.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-20T02:19:51Z

- **keps/2941-DRA/README.md:269** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-20T02:19:51Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
```

  > I would probably suggest keeping it under kueue-system namespace. This way it is closer to the ConfigMap model, which is intrinsically namespaced. Maybe using helm one could imagine two separete installations of Kueue. It is very unlikely, but possible.
  
  If there are multiple Kueue installations in a cluster, how do they coordinate on what ClusterQueues are owned by each installation?
  
  Having a namespaces CRD will mean that modelling permissions with RBAC, ClusterRole vs Role will change.  Considering that, I would really prefer a cluster-scoped CRD but not a very strong opinion.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-20T02:26:43Z

- **keps/2941-DRA/examples/problematic-resourceflavors/problematic-rf-cq.yaml:1** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-20T02:26:43Z
```diff
@@ -0,0 +1,46 @@
+---
```

  updated.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-20T02:41:46Z

- **keps/2941-DRA/README.md:360** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-20T02:41:46Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DeviceClassMapping CR. This creates
+uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DeviceClassMapping
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
```

  +1 will be good to clarify. Out of curiosity, does DRA plan on support claims in pod level resources?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-20T02:47:24Z

- **keps/2941-DRA/README.md:360** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-20T02:47:24Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DeviceClassMapping CR. This creates
+uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DeviceClassMapping
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
```

  thinking about this more, Kueue doesnt care if the claims are referenced in container level resources or pod level within the podSpec. As per this design, Kueue will be looking at pod.spec.resourceClaims to count it against the quota.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-20T02:48:05Z

- **keps/2941-DRA/README.md:360** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-20T02:48:05Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DeviceClassMapping CR. This creates
+uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DeviceClassMapping
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
```

  updated.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-20T02:49:19Z

- **keps/2941-DRA/README.md:216** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-20T02:49:19Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  +1, DynamicResourceAllocationConfig sgtm, updated the design.

### Review by [@nojnhuh](https://github.com/nojnhuh) (commented) — 2025-06-20T03:52:52Z

- **keps/2941-DRA/README.md:360** by [@nojnhuh](https://github.com/nojnhuh) — 2025-06-20T03:52:52Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DeviceClassMapping CR. This creates
+uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DeviceClassMapping
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
```

  I see now that claims actually are disallowed in pod-level resources: https://github.com/kubernetes/kubernetes/blob/2689185671dbb42d338a36ce66c0b316b9cd4706/pkg/apis/core/validation/validation.go#L4364-L4366
  
  I also don't see any KEPs that look like they will make that possible, so probably safe to ignore for now. Maybe mentioning it at all will raise more questions than answers. If the implementation can handle that case transparently without any hacks if that does become possible someday though that's great!

### Review by [@jackfrancis](https://github.com/jackfrancis) (commented) — 2025-06-20T04:46:32Z

- **keps/2941-DRA/README.md:360** by [@jackfrancis](https://github.com/jackfrancis) — 2025-06-20T04:46:32Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DeviceClassMapping CR. This creates
+uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DeviceClassMapping
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DeviceClassMapping
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from resources.claims in the PodTemplateSpec.
```

  Just a pedantic nit that pod-level resources will be beta in 1.34. Though I concur that there are is no mention of resource claim (or DRA generally) in the updated pod-level resources KEP that was updated today for 1.34:
  
  https://github.com/kubernetes/enhancements/pull/5362
  
  cc @ndixita @pohly @johnbelamaric

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-20T06:03:52Z

- **keps/2941-DRA/README.md:216** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-20T06:03:52Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  @mimowo Thank you for adding our discussion result and thank you @alaypatel07 for reflecting my opinion to this KEP.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-20T06:25:46Z

- **keps/2941-DRA/README.md:269** by [@mimowo](https://github.com/mimowo) — 2025-06-20T06:25:46Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
```

  > If there are multiple Kueue installations in a cluster, how do they coordinate on what ClusterQueues are owned by each installation?
  
  It is not supported currently, I was just making a point about potential future when we may want to split the load into 2 Kueue instances running in parallel.
  
  > Having a namespaces CRD will mean that modelling permissions with RBAC, ClusterRole vs Role will change. 
  
  In the folder `main/config/components/rbac/` we have examples of both namespace-scoped and cluster-scoped resources. In particular `leader_election_role.yaml` is kueue-system scoped. So, yes, the RBAC would be different, but it does not seem more challenging.
  
  > Considering that, I would really prefer a cluster-scoped CRD but not a very strong opinion.
  
  Let me summarize the arguments I was thinking about:
  1. deploying multiple Kueue instances in the same cluster (potential future)
  2. make it clear to users that DynamicResourceAllocationConfig is  Kueue-specific, not code k8s. 
  3. similarity with the current ConfigMap where the configuration is in the kueue-system ns
  
  However, (1) is not going to happen any time soon and if we do so, we could have some sort of naming convention like `default-nsName`; (2.) would anyway be clear by the CRD groupName; (3.) ConfigMap has to be ns-scoped to scope permissions. So, none of the arguments are particularly strong. 
  
  So, I'm ok to go with global DynamicResourceAllocationConfig if @tenzen-y is also ok with that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-20T19:35:12Z

- **keps/2941-DRA/README.md:373** by [@mimowo](https://github.com/mimowo) — 2025-06-20T19:35:12Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from podSpec.resourceClaims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+   1. the name of the ResourceClaimTemplate or
+   2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+   1. For the workload a deviceClassMap will be created by:
+      1. Retrieving the singleton DynamicResourceAllocationConfig resource (named "default")
+      2. Creating a map from DeviceClass names to canonical resource names using the
+          DynamicResourceAllocationConfig.spec.dynamicResources entries
+      3. For resource claims that are already allocated, Kueue will maintain a mapping called `admittedResourceClaims`
+         where the key follows the format `{clusterQueueName}/{resourceClaimNamespace}/{resourceClaimName}`
```

  nit: this format of the key sounds like a technical detail. Actually in the cache.go we already have clustequeue caches so we could just keep the key per CQ cache. So, I consider it a technical detail which may change as we progress with the implementation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-20T19:39:50Z

/lgtm
/approve
Thank you 👍 I think this is good enough to kick off the  implementation as Alpha. 

/hold
To give more time for review / feedback for @tenzen-y and others.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-23T16:50:51Z

Thank you for your continuously work.

- **keps/2941-DRA/README.md:62** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T09:00:49Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
```

  ```suggestion
  [Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/) is a major effort to improve device support in Kubernetes.
  ```

- **keps/2941-DRA/README.md:85** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T09:03:41Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
```

  Isn't `DeviceTaintRule` related to Kueue?

- **keps/2941-DRA/README.md:219** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T14:54:18Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
```

  This will introduce a cluster-scoped CRD.
  In that case, how can we support multiple kueue-controller-manager in a single cluster?
  
  Currently, admins can do it to mitigate kueue scheduling performance which allows admins to shard managed namespaces based on label selectors.

- **keps/2941-DRA/README.md:233** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T14:56:11Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
```

  Do we have any limitations for the number of deviceClassMapping?

- **keps/2941-DRA/README.md:238** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T15:09:49Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
```

  What does this `Workload status` mean?

- **keps/2941-DRA/README.md:237** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T15:18:49Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
```

  Why not `DynamicResource` instead of `DeviceClassMapping`?

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T16:49:15Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  In the context of cq, the name is located under the `resources` field. However, in the DynamicResourceAllocationConfig, there is no context for `resources`.
  
  So, I would recommend the following APIs
  
  (a. Put all fields under the `.spec.resources`.
  
  ```yaml
  apiVersion: kueue.x-k8s.io/v1alpha1
  kind: DynamicResourceAllocationConfig
  metadata:
    name: "default" 
  spec:
    resources:
    - name: gpus
      deviceClassNames:
      - gpu.example.com
  ```
  
  (b. Put all fields under the `.spec.requests.resources`.
  
  ```yaml
  apiVersion: kueue.x-k8s.io/v1alpha1
  kind: DynamicResourceAllocationConfig
  metadata:
    name: "default" 
  spec:
    requests:
      resources:
      - name: gpus
        deviceClassNames:
        - gpu.example.com
  ```
  
  (c. Put all fields under the `.spec.deviceRequests.resources`.
  
  ```yaml
  apiVersion: kueue.x-k8s.io/v1alpha1
  kind: DynamicResourceAllocationConfig
  metadata:
    name: "default" 
  spec:
    deviceRequests:
      resources:
      - name: gpus
        deviceClassNames:
        - gpu.example.com
  ```
  
  
  TBH, I'm leaning to (c since it allows us to add additional deviceRequest related parameters under the the `deviceRequests` and also allows us to add non deviceRequests related and more global parameters under the spec.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-23T16:55:10Z

- **keps/2941-DRA/README.md:269** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T16:55:10Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
```

  As I posted in https://github.com/kubernetes-sigs/kueue/pull/5149#discussion_r2161830900, I'm leaning toward to (3). The cluster admin can select which cq can manage which namespaces based on namespaceSelector in cq.
  
  After we introduce TAS to kueue, kueue-controller-manager retrieves all node information as a dedicated cache separate from the informer cache, which increases the memory footprint and kueue scheduling duration.
  
  The sharding kueue-controller-manager concept would mitigate this problem.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-23T17:03:10Z

- **keps/2941-DRA/README.md:752** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T17:03:10Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from podSpec.resourceClaims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+   1. the name of the ResourceClaimTemplate or
+   2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+   1. For the workload a deviceClassMap will be created by:
+      1. Retrieving the singleton DynamicResourceAllocationConfig resource (named "default")
+      2. Creating a map from DeviceClass names to canonical resource names using the
+          DynamicResourceAllocationConfig.spec.dynamicResources entries
+      3. For resource claims that are already allocated, Kueue will maintain a mapping called `admittedResourceClaims`
+         where the key follows the format `{clusterQueueName}/{resourceClaimNamespace}/{resourceClaimName}`
+      4. If a workload is using an already allocated resource claim and the key
+         `{targetClusterQueueName}/{resourceClaimNamespace}/{resourceClaimName}` exists in the `admittedResourceClaims`
+         map, Kueue will skip counting that claim against quota and automatically admit the workload
+      5. This approach ensures that within a single cluster queue, reusing allocated resource claims doesn't consume
+         additional quota, while still enforcing quota boundaries between different cluster queues
+   2. For each device class the canonical quota name will be looked up from the deviceClassMap and resource will be
+      counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change.
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu # 1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name read from #1
+        resourceClaimTemplateName: single-gpu # 2.1) the name for resource claim template
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig # 4.1.1) Singleton CR that defines all device class mappings
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com # 4.1.2) Creating map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus' # 4.2) The canonical name from DynamicResourceAllocationConfig
+        nominalQuota: 2
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No dynamicResources field - mapping is now in DynamicResourceAllocationConfig CR
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        whole-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1" # 5) selected device count is reflected here in workload status
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+Integration tests in Kueue use controller-runtime's envtest framework, which provides a real Kubernetes API server
+without requiring kubelet or other cluster components. While DRA device allocation requires kubelet plugins, the core
+DRA integration functionality for Kueue can be tested at the integration level by:
+
+- Testing DynamicResourceAllocationConfig CRD creation and validation
+- Verifying workload admission logic with DRA resource claims
+- Testing quota enforcement against device class mappings
+- Validating resource counting and flavor assignment for DRA resources
+
+The integration tests would focus on Kueue's quota management and admission logic rather than actual device allocation,
+using mock ResourceClaims and DeviceClasses to simulate DRA workloads.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Alpha
+
+- the implementation behind the feature gate flag in alpha
+- support Beta API of the core k8s
+- initial e2e tests for baseline scenario
+
+#### Beta
+
+- the feature gate in Beta
+- all known bugs are fixed
+- support integration with v1 API of DRA in core k8s
+- e2e tests
+- re-evaluate the support for TopologyAwareScheduling (might be moved to GA)
+
+#### GA
+
+- the feature gate in stable
+- integration with TopologyAwareScheduling
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing.
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### ResourceClaim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota.
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
+
+Additionally, ResourceClaims capture the intent of the user on what kind of device is request. The request could mean
+anything from one small allocatable device to several devices or entire resource pool. Therefore, tracking the number
+of requests becomes non-intuitive. The need is to count devices going to be allocated to those requests.
+
+### Using devices in ResourceSlice to Count
+
+DRA drivers publish resources for each node, which could be used as a mechanism for counting resources. However, in DRA
+implementation, ResourceSlices are used for driver/scheduler communication. The only way users can request dynamic
+resources is via ResourceClaims. ResourceClaims does not have the notion of what devices will be allocated a priori.
+
+Enforcing quota requires two inputs, 1) user request and 2) system usages. With using ResourceSlice, the first requirement
+is missing.
+
+### Using a CEL expression
+
+Cluster admin might have to create new deviceclass for narrowing set of target devices in existing device class for
+setting quota. Moreover, when existing users use the old device classes, they might have to migrate to the new deviceclass.
+For example, assume gpu.example.com deviceclass exists, and each device has device attribute "memory" there are existing
+users who have resourceclaims with the deviceclass and selector like this:
+```yaml
+kind: ResourceClaim
+name: one-large-gpu
+spec:
+requests:
+- name: gpu-large
+  deviceClassName: gpu.example.com
+  selectors:
+    - cel:
+      expression: device.attributes["memory"] >= 80g
+```
+
+Now, if Kueue admin wants to set quota for gpu.example.com devices with device.attribute["memory"]>=80g, Kueue admin
+might have to create a device class and use the new device class in clusterqueue:
+
+```yaml
+kind: DeviceClass
+name: large-gpu.example.com
+spec:
+  selectors:
+  - cel:
+      expression: device.driver == "gpu.example.com" && device.attributes["memory"] >= 80g
+```
+
+Then, existing users might have to migrate/switch their ResourceClaim with large-gpu.example.com device class from
+existing one.
+
+For minimizing user impact, there could be an API change that allows CEL expression along with deviceclass name in
+defining the Kueue quota, like this:
+```yaml
+kind: Device
+nominalQuota: 2
+devices:
+  # We might be able to extend this object to support
+  # partitionable devices, etc. in the future??
+- className: gpu.example.com
+  selectors:
+  - cel:
+      expression: device.attributes["memory"] >= 80g
+```
+
+This indeed improves the user experience, but with a cel expression like that, whether a device having attributes that
+evaluates the cel expression to true or not, will only be available after the scheduler allocates the device for the
+claim. Kueue needs to know the device and count it before admitting the workload and hence before it hits the
+kube-scheduler. Any inclusion relationship between two boolean formulae in ResourceClaim and ClusterQueue cannot be
+assumed.
+
+For example, assume the following ResourceClaim and ClusterQueue exist. In this situation, it is clear that there could
+be both cases where the allocation result consumes and does not consume the quota (i.e. this means we have to wait for
+the allocation result).
+
+```yaml
+kind: ResourceClaim
+name: one-mid-or-large-gpu
+spec:
+  requests:
+  - name: middle-or-large-gpu
+    deviceClassName: gpu.example.com
+    selectors:
+    - cel:
+        expression: 50g < device.attributes["memory"] and device.attributes["memory"] <= 100g
+---
+kind: Device
+devices:
+- className: gpu.example.com
+  selectors:
+  - cel:
+      expression: device.attributes["memory"] <= 80g
+```
+
+### Defining DeviceClass mapping in ClusterQuota
+
+The definition of what DeviceClasses construct a DRA device could be in ClusterQuota just before declaring the nominal
+count for the device.
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+This presents a problem where the same canonical name could be used to define DeviceClasses A and B in one ClusterQueue
+and DeviceClasses C, D and E in another ClusterQueue leading to conflicts. Since the mapping canonical name to list of
+DeviceClasses is not shared, it is hard to implement borrowing as it becomes very non-deterministic. Hence, this approach
+is not feasible.
+
+### Using ResourceFlavor for DeviceClass Mapping
+
+An earlier design considered embedding device class mappings directly in the ResourceFlavor API instead of creating
+a separate DynamicResourceAllocationConfig CRD:
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+However, this design had a major drawback. The biggest issue was semantic confusion - a dynamicResource like `gpu` in
+ResourceFlavor1 could have deviceClass gpu-a.example.com while the same dynamicResource name in ResourceFlavor2 could
+have the completely different deviceClass gpu-b.example.com. This creates significant confusion for cluster
+administrators because the same resource name would have different meanings depending on which ResourceFlavor was being
+referenced. The singleton DynamicResourceAllocationConfig CRD approach addresses this by providing a single source of
+truth for all device class mappings in the cluster.
+
```

  Could you add another Kueue Config configmap as an alternative?
  I think we can describe that 
  
  (1 The global configmap prevents dynamically Kueue-DRA collaboration parameters change w/o kueue-controller-manager restarting. 
  (2 It could potentially bring us a breaking change to Beta Kueue Config API since DRA is still actively developed in the core Kubernetes.
  
  For (1, could you expand the use case where we want to dynamically change those parameters?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-23T17:04:01Z

- **keps/2941-DRA/README.md:257** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T17:04:01Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
```

  Could you add limitation for the number of deviceClassNames?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-23T17:13:21Z

- **keps/2941-DRA/README.md:373** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T17:13:21Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
```

  What if ResourceClaim / ResourceClaimTemplate is changed after Job admission by Kueue?
  Does it occur the quota violation? Or in DRA, are those resources immutable?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-06-23T17:14:58Z

- **keps/2941-DRA/README.md:373** by [@kannon92](https://github.com/kannon92) — 2025-06-23T17:14:58Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
```

  @pohly mentioned to me that these resources are immutable for now.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-23T17:17:33Z

- **keps/2941-DRA/README.md:373** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T17:17:33Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
```

  Thank you for letting me know.
  @alaypatel07, could you mention ResourceClaim immutabilities in the Notes section?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-23T17:19:22Z

- **keps/2941-DRA/README.md:373** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-23T17:19:21Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
```

  +1 yes they are immutable, as part of the discussions in this design we double checked the latest APIs. I'll document it.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-23T17:45:41Z

- **keps/2941-DRA/README.md:269** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-23T17:45:41Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
```

  I think we have 2 votes leaning towards namespaced CRD. I will refactor the design accordingly.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-23T17:48:55Z

- **keps/2941-DRA/README.md:85** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-23T17:48:55Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
```

  related, but its not part of the core KEP. DeviceTaintRule is a part of an alpha feature, behind a feature gate: https://github.com/kubernetes/enhancements/blob/9d1f63cf7d78dcf7affa1b37c6b950cb98dcb022/keps/sig-scheduling/5055-dra-device-taints-and-tolerations/kep.yaml#L18
  
  In alpha version, kueue will not support any alpha level DRA feature, as noted in the KEP

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-23T18:01:44Z

- **keps/2941-DRA/README.md:251** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-23T18:01:43Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  > TBH, I'm leaning to (c since it allows us to add additional deviceRequest related parameters under the the deviceRequests and also allows us to add non deviceRequests related and more global parameters under the spec.
  
  what are examples of other deviceRequests fields that could be anticipated? I am not sure if we need an additional level of indentation if there is no immediate benefit.
  
  What do you think about this?
  
  ```yaml
  apiVersion: kueue.x-k8s.io/v1alpha1 
  kind: DynamicResourceAllocationConfig
  metadata: 
    name: "default"
  spec: 
    coveredResourceMapping: 
    - name: gpus
      deviceClassNames:
      - gpu.example.com
  ```
  1. It has the same naming as cluster queue API,
  1. It has only 1 indentation inside of spec, fairly easy to use
  1. Other configurations related to DRA can be extended by adding new fields under spec

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-23T18:02:31Z

- **keps/2941-DRA/README.md:257** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-23T18:02:31Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
```

  shall we start with max of 8? This limit can be revisited based on user feedback in beta.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T05:40:04Z

- **keps/2941-DRA/README.md:257** by [@mimowo](https://github.com/mimowo) — 2025-06-24T05:40:04Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
```

  8 sgtm to start

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T05:40:40Z

- **keps/2941-DRA/README.md:373** by [@mimowo](https://github.com/mimowo) — 2025-06-24T05:40:40Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
```

  +1 for making immutable

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T05:42:05Z

- **keps/2941-DRA/README.md:85** by [@mimowo](https://github.com/mimowo) — 2025-06-24T05:42:05Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
```

  Yes, I think we need to scope for the first iteration of alpha, otherwise we risk not delivering in 0.13

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T05:47:16Z

- **keps/2941-DRA/README.md:251** by [@mimowo](https://github.com/mimowo) — 2025-06-24T05:47:15Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  This is Alpha so I dont think we need to complicate the API for unknowns. If we discover the new features require different organization we can add it later. I suggest to focus on the current scope and make it easy to use.
  
  My clear preference is option a) from Yuki which is quite clear from the CRD name, so I think no need to repeat 'dynamic' at the field level.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T06:53:25Z

- **keps/2941-DRA/README.md:257** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T06:53:24Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
```

  8 sounds good to me as well, thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T06:54:45Z

- **keps/2941-DRA/README.md:85** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T06:54:45Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
```

  Alright, let's postpone thinking about DeviceTaintRule resource.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T06:55:22Z

- **keps/2941-DRA/README.md:269** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T06:55:22Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DeviceClassMapping
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
```

  Thank you.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T08:45:14Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T08:45:14Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  > > TBH, I'm leaning to (c since it allows us to add additional deviceRequest related parameters under the the deviceRequests and also allows us to add non deviceRequests related and more global parameters under the spec.
  > 
  > what are examples of other deviceRequests fields that could be anticipated? I am not sure if we need an additional level of indentation if there is no immediate benefit.
  
  DRA big features will be coming like ConsumableCapacities, DeviceTaint, PartitinalDevices, PrioritizedList, and so on. So, I proposed (c to make a room for that.
  
  If @mimowo and @alaypatel07 are ok with continuously breaking the API in the future, (a sounds fine.
  The key is that breaking will be "continuously", which means we might need to break multiple times without any backward compatibility.
  
  Are your customers able to accept the multiple times the API breaks? If yes, let's go with the simplest (a solution.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T08:46:30Z

- **keps/2941-DRA/README.md:233** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T08:46:30Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
```

  @alaypatel07 could you propose the assuming number of dynamicResources?
  Is 8 enough? Or do you have stories that need more and more resources?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T08:52:49Z

- **keps/2941-DRA/README.md:163** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T08:52:49Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
```

  Additionally, I'm suspecting the first Kueue with the DRA feature does not have compatibility for the MultiKueue.
  Because the ResourceClaimTemplate and ResourceClaim responsibilities are out of Kueue.
  
  In other words, batch users need access to all clusters and create a ResourceClaimTemplate in their namespaces before they submit Jobs.
  However, in the dynamic cluster creation / deletion environment, it's challenging, I believe.
  
  In the future DRA support, I think we need to consider how to support it for MultiKueue the same as TopologyAwareScheduling.
  
  cc @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T08:57:00Z

- **keps/2941-DRA/README.md:251** by [@mimowo](https://github.com/mimowo) — 2025-06-24T08:57:00Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  It is ok for Alpha users, to accept a breaking API change. I'm not sure what "continuously" breaking changes mean here.
  
  My point is that it is very hard to predict the future, and is not clear to me even if any breaking changes will be needed. Can you confirm that the breaking changes will be needed, then we could consider a more nested API.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T09:00:55Z

- **keps/2941-DRA/README.md:163** by [@mimowo](https://github.com/mimowo) — 2025-06-24T09:00:55Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
```

  Actually, I think MultiKueue may work ootb if users repeat the configuration in management and worker cluster. 
  I'm ok with a note as non-goal, but maybe just a graduation point for beta is enough.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T10:01:57Z

- **keps/2941-DRA/README.md:163** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T10:01:57Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
```

  Let's add MultiKueue to the Beta criteria.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T10:10:20Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T10:10:20Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  I just confirmed if breaking APIs are acceptable for you since someone sometimes requests backward compatibility even for alpha API, even for beta API. Actually, you requested to avoid cq (beta1 -> beta2) and cohort (alpha1 -> beta1) field name changes previously.
  
  As long as we follow the kubernetes API graduation steps, any API change should be acceptable.
  If anyone doesn't request such compabiilities for alpha and beta API, the Alpha API should be (a).
  However, if someone potentially requests backward compabitilities, we should introduce more composable APIs in the alpha level.
  
  Again, this is just confirmation if API break is acceptable even if it change just field name.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T10:19:54Z

- **keps/2941-DRA/README.md:251** by [@mimowo](https://github.com/mimowo) — 2025-06-24T10:19:54Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  Yes, changing Alpha API is ok. Especially alpha1 -> alpha2.
  
  For Alpha -> Beta, and v1beta1 -> v1beta2 it is also ok, but we just need to be more careful. Sometimes renaming a field is just not worth the hassle. I think this is case by case.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T10:26:34Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T10:26:34Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  > Yes, changing Alpha API is ok. Especially alpha1 -> alpha2.
  > 
  > For Alpha -> Beta, and v1beta1 -> v1beta2 it is also ok, but we just need to be more careful. Sometimes, renaming a field is just not worth the hassle. I think this is case by case.
  
  Just a naming change is also worth it since it avoids broken window theory and it helps to construct mental model for users. So, if the naming change makes sense, API and architecture PoV, it should be accepted.
  
  Because, as you mentioned, for now, it's challenging to imagine every use case, which means we might add improper name to the field.
  
  > I think this is case by case.
  
  If name change is case by case even if the naming change makes sense for entire API construction, I would request to introduce more composable object strucuture for now.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T10:53:23Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T10:53:22Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  Based on offline discussion with @mimowo, as long as the field name change is reasonable and follows the K8s API graduation guideline, changes should be acceptable.
  
  So, if @alaypatel07 does not have other concerns, we can go with (a).

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T11:41:11Z

- **keps/2941-DRA/README.md:233** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T11:41:11Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
```

  I think 8 is enough, but I cannot anticipate all the different device classes a user might have. To be safe, we can consider 16.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T12:05:00Z

- **keps/2941-DRA/README.md:233** by [@mimowo](https://github.com/mimowo) — 2025-06-24T12:05:00Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
```

  I'm good with 16 day one, I think with increased adoption of DRA this is likely to happen, but I will leave it to @tenzen-y .

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T12:05:00Z

- **keps/2941-DRA/README.md:251** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T12:05:00Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  @tenzen-y may be in the next community call we can have a discussion about what changes do you expect with these big feature.
  
  > DRA big features will be coming like ConsumableCapacities, DeviceTaint, PartitinalDevices, PrioritizedList, and so on. So, I proposed (c to make a room for that.
  
  IMHO:
  
  1. PartitionableDevices do not change any user-facing API, its change in implementation.
  1. PrioritizedList feature was explored and a potential solution does require design change, just doc changes, I am assuming.
  1. DeviceTaint is a scheduling constraint, it will work at a layer below kueue's quota admission, just like how NodeTaints work for nodes.
  1. ConsumableCapacities is breaking down consumption of a single device into multiple allocations. Supporting this feature will probably require another feature gate in Kueue and a new KEP.
  
  Considering the above, I am okay with going with option (a) as of now.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T12:19:47Z

- **keps/2941-DRA/README.md:233** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T12:19:47Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
```

  https://github.com/kubernetes-sigs/kueue/pull/5149/files#r2163105420
  
  we agreed to starting with 8 and re-consider in beta.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T12:28:16Z

- **keps/2941-DRA/README.md:237** by [@mimowo](https://github.com/mimowo) — 2025-06-24T12:28:16Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
```

  +1

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T12:31:02Z

- **keps/2941-DRA/README.md:233** by [@mimowo](https://github.com/mimowo) — 2025-06-24T12:31:02Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
```

  Actually, these are two different levels, so technically they could be different limits, but probably 8 for both levels to start with is ok.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T12:44:46Z

- **keps/2941-DRA-Structured-Parameters/kep.yaml:35** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T12:44:46Z
```diff
@@ -0,0 +1,40 @@
+title: DRA (Structured Parameters) Support in Kueue
+kep-number: 2941
+authors:
+  - "@alaypatel07"
+status: provisional
+creation-date: 2025-05-02
+reviewers:
+  - "@kannon92"
+approvers:
+  - "@mimowo"
+  - "@tenzen-y"
+
+see-also:
+  - "https://github.com/kubernetes/enhancements/blob/master/keps/sig-node/4381-dra-structured-parameters/README.md"
+replaces:
+  - "https://github.com/kubernetes-sigs/kueue/pull/3071"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.13"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.13"
+  beta:
+  stable:
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: KueueDynamicResourceAllocation
```

  I'm ok with `DynamicResourceAllocation` for the feature gate name.
  @alaypatel07 Feel free to mark this as resolved.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T12:50:20Z

- **keps/2941-DRA/README.md:233** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T12:50:20Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
```

  I'm ok with 16 for resources, and 8 for deviceClassNames.
  I can understand that resources probably should be accommodated more items than deviceClasses.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T13:00:25Z

- **keps/2941-DRA/README.md:238** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T13:00:25Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
```

  ```yaml
        flavors:
          cpu: whole-gpu-flavor
          memory: whole-gpu-flavor
          whole-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
        name: main
        resourceUsage:
          cpu: "1"
          memory: 400Mi
          whole-gpus: "1" # 5) selected device count is reflected here in workload status
   ```
   
   Please look at the workload status in the example, it clarifies how the resource names are referenced.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T13:01:12Z

- **keps/2941-DRA/README.md:237** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T13:01:11Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
```

  modified it to `Resource`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T13:11:42Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T13:11:42Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  @alaypatel07 Thank you for summarizing that. In this KEP, we can take (a) approach in that case.
  
  I guess that PrioritizedList and ConsumableCapacity are the biggest ones for Kueue since PrioritizedList is similar to Flavor fallback mechanism and ConsumableCapacity increase scheduling accuraccy, IIUC.
  
  For PrioritizedList, if we support it, we will have a 2-level fallback mechanism, 1 is ResourceFlavor level, 2 is ResourceClaim level. I think we should discuss how to mitigate increase of complexity and entropy when we support PrioritizedList since it will generate n × m patterns in case of fallback where n is the number of ResourceFlavors in cq and m is the number of device requests in ResourceClaim (Template).
  
  For ConsumableCapacity, I guess that this is needed for TAS, which means TAS should consider ConsumableCapacities in addition to Node allocatable resources when it simulates scheduling.
  When TAS is disabled, I think we can ignore the ConsumableCapacity because the cq quota is just virtual. So Kueue don't care actual resource availabilities in case of non TAS mode.
  
  One unclear point is whether or not cluster-autoscaler can understand ConsumableCapacity when it scale node. This is a bit of an important problem for PrivisioningRequest support in Kueue, IIUC. Because if CA can not recognize the ConsumableCapacity, it might scale out small nodes rather than the expected resources.
  
  In any case, I'm open to discussion for DRA support in Kueue.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T13:20:46Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T13:20:46Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  Additionally, for DeviceTaints, I'm interested in what if users use RF taints (https://kueue.sigs.k8s.io/docs/concepts/resource_flavor/#resourceflavor-taints-for-user-selective-scheduling) and DeviceTaints, together.
  
  Let's say we have foo RF with gpu taints which means only Jobs with gpu tolerations can consume the foo RF.
  In that case, what if the pointed deviceClass has taints rule for DeviceTaints? Do batch users need to add different tolerations to 2 places (Job and ResourceClaimTemplate)? For example, adding a gpu toleration to Job, and a super-device toleration to ResourceClaimTemplate. It looks like it's complicated UX for batch users.
  So, I'm curious about how to mitigate the problem and how to guide usage for RF taints and DeviceTaints features, together.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T13:22:10Z

- **keps/2941-DRA/README.md:233** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T13:22:10Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
```

  updated

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T13:24:18Z

- **keps/2941-DRA/README.md:219** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T13:24:18Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
```

  updated to namespace scoped

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T13:24:40Z

- **keps/2941-DRA/README.md:752** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T13:24:39Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from podSpec.resourceClaims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+   1. the name of the ResourceClaimTemplate or
+   2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+   1. For the workload a deviceClassMap will be created by:
+      1. Retrieving the singleton DynamicResourceAllocationConfig resource (named "default")
+      2. Creating a map from DeviceClass names to canonical resource names using the
+          DynamicResourceAllocationConfig.spec.dynamicResources entries
+      3. For resource claims that are already allocated, Kueue will maintain a mapping called `admittedResourceClaims`
+         where the key follows the format `{clusterQueueName}/{resourceClaimNamespace}/{resourceClaimName}`
+      4. If a workload is using an already allocated resource claim and the key
+         `{targetClusterQueueName}/{resourceClaimNamespace}/{resourceClaimName}` exists in the `admittedResourceClaims`
+         map, Kueue will skip counting that claim against quota and automatically admit the workload
+      5. This approach ensures that within a single cluster queue, reusing allocated resource claims doesn't consume
+         additional quota, while still enforcing quota boundaries between different cluster queues
+   2. For each device class the canonical quota name will be looked up from the deviceClassMap and resource will be
+      counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change.
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu # 1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name read from #1
+        resourceClaimTemplateName: single-gpu # 2.1) the name for resource claim template
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig # 4.1.1) Singleton CR that defines all device class mappings
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com # 4.1.2) Creating map{"gpu.example.com" -> "whole-gpus"}
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus' # 4.2) The canonical name from DynamicResourceAllocationConfig
+        nominalQuota: 2
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No dynamicResources field - mapping is now in DynamicResourceAllocationConfig CR
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-job0-6f46e
+  namespace: gpu-test1
+...
+...
+status:
+  admission:
+    clusterQueue: single-gpus-cluster-queue
+    podSetAssignments:
+    - count: 2
+      flavors:
+        cpu: whole-gpu-flavor
+        memory: whole-gpu-flavor
+        whole-gpus: default-gpu-flavor # 5) selected flavor is reflected here in workload status
+      name: main
+      resourceUsage:
+        cpu: "1"
+        memory: 400Mi
+        whole-gpus: "1" # 5) selected device count is reflected here in workload status
+```
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+TBD
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+Integration tests in Kueue use controller-runtime's envtest framework, which provides a real Kubernetes API server
+without requiring kubelet or other cluster components. While DRA device allocation requires kubelet plugins, the core
+DRA integration functionality for Kueue can be tested at the integration level by:
+
+- Testing DynamicResourceAllocationConfig CRD creation and validation
+- Verifying workload admission logic with DRA resource claims
+- Testing quota enforcement against device class mappings
+- Validating resource counting and flavor assignment for DRA resources
+
+The integration tests would focus on Kueue's quota management and admission logic rather than actual device allocation,
+using mock ResourceClaims and DeviceClasses to simulate DRA workloads.
+
+#### E2E Test
+
+Use existing dra-example-driver or Kubernetes test driver for e2e testing.
+
+### Graduation Criteria
+
+#### Alpha
+
+- the implementation behind the feature gate flag in alpha
+- support Beta API of the core k8s
+- initial e2e tests for baseline scenario
+
+#### Beta
+
+- the feature gate in Beta
+- all known bugs are fixed
+- support integration with v1 API of DRA in core k8s
+- e2e tests
+- re-evaluate the support for TopologyAwareScheduling (might be moved to GA)
+
+#### GA
+
+- the feature gate in stable
+- integration with TopologyAwareScheduling
+
+## Implementation History
+
+- Initial draft on September 16th 2024 by @kannon92
+- Polished on April 25th 2025 by @alaypatel07
+
+## Drawbacks
+
+NA. Kueue should be able to schedule devices following what upstream is proposing.
+The only drawbacks are that workloads will have to fetch the resource claim if they are specifying resource claims.
+
+## Alternatives
+
+### ResourceClaim By Count
+
+Keeping a tally of the resource claims for a given workload could be another mechanism for enforcing quota.
+However, the issue with this is that resource claims are namespaced scoped, to enforce quota usage across namespaces kueue
+need to rely on a cluster-scope resource.
+
+Additionally, ResourceClaims capture the intent of the user on what kind of device is request. The request could mean
+anything from one small allocatable device to several devices or entire resource pool. Therefore, tracking the number
+of requests becomes non-intuitive. The need is to count devices going to be allocated to those requests.
+
+### Using devices in ResourceSlice to Count
+
+DRA drivers publish resources for each node, which could be used as a mechanism for counting resources. However, in DRA
+implementation, ResourceSlices are used for driver/scheduler communication. The only way users can request dynamic
+resources is via ResourceClaims. ResourceClaims does not have the notion of what devices will be allocated a priori.
+
+Enforcing quota requires two inputs, 1) user request and 2) system usages. With using ResourceSlice, the first requirement
+is missing.
+
+### Using a CEL expression
+
+Cluster admin might have to create new deviceclass for narrowing set of target devices in existing device class for
+setting quota. Moreover, when existing users use the old device classes, they might have to migrate to the new deviceclass.
+For example, assume gpu.example.com deviceclass exists, and each device has device attribute "memory" there are existing
+users who have resourceclaims with the deviceclass and selector like this:
+```yaml
+kind: ResourceClaim
+name: one-large-gpu
+spec:
+requests:
+- name: gpu-large
+  deviceClassName: gpu.example.com
+  selectors:
+    - cel:
+      expression: device.attributes["memory"] >= 80g
+```
+
+Now, if Kueue admin wants to set quota for gpu.example.com devices with device.attribute["memory"]>=80g, Kueue admin
+might have to create a device class and use the new device class in clusterqueue:
+
+```yaml
+kind: DeviceClass
+name: large-gpu.example.com
+spec:
+  selectors:
+  - cel:
+      expression: device.driver == "gpu.example.com" && device.attributes["memory"] >= 80g
+```
+
+Then, existing users might have to migrate/switch their ResourceClaim with large-gpu.example.com device class from
+existing one.
+
+For minimizing user impact, there could be an API change that allows CEL expression along with deviceclass name in
+defining the Kueue quota, like this:
+```yaml
+kind: Device
+nominalQuota: 2
+devices:
+  # We might be able to extend this object to support
+  # partitionable devices, etc. in the future??
+- className: gpu.example.com
+  selectors:
+  - cel:
+      expression: device.attributes["memory"] >= 80g
+```
+
+This indeed improves the user experience, but with a cel expression like that, whether a device having attributes that
+evaluates the cel expression to true or not, will only be available after the scheduler allocates the device for the
+claim. Kueue needs to know the device and count it before admitting the workload and hence before it hits the
+kube-scheduler. Any inclusion relationship between two boolean formulae in ResourceClaim and ClusterQueue cannot be
+assumed.
+
+For example, assume the following ResourceClaim and ClusterQueue exist. In this situation, it is clear that there could
+be both cases where the allocation result consumes and does not consume the quota (i.e. this means we have to wait for
+the allocation result).
+
+```yaml
+kind: ResourceClaim
+name: one-mid-or-large-gpu
+spec:
+  requests:
+  - name: middle-or-large-gpu
+    deviceClassName: gpu.example.com
+    selectors:
+    - cel:
+        expression: 50g < device.attributes["memory"] and device.attributes["memory"] <= 100g
+---
+kind: Device
+devices:
+- className: gpu.example.com
+  selectors:
+  - cel:
+      expression: device.attributes["memory"] <= 80g
+```
+
+### Defining DeviceClass mapping in ClusterQuota
+
+The definition of what DeviceClasses construct a DRA device could be in ClusterQuota just before declaring the nominal
+count for the device.
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+This presents a problem where the same canonical name could be used to define DeviceClasses A and B in one ClusterQueue
+and DeviceClasses C, D and E in another ClusterQueue leading to conflicts. Since the mapping canonical name to list of
+DeviceClasses is not shared, it is hard to implement borrowing as it becomes very non-deterministic. Hence, this approach
+is not feasible.
+
+### Using ResourceFlavor for DeviceClass Mapping
+
+An earlier design considered embedding device class mappings directly in the ResourceFlavor API instead of creating
+a separate DynamicResourceAllocationConfig CRD:
+
+```golang
+type DynamicResourceMapping struct {
+	// Name is the canonical name of this mapping. This will be referred in ClusterQueue
+	// and Workload status
+	Name corev1.ResourceName `json:"name"`
+
+	// deviceClassNames lists the names of all the device classes that will count against
+	// the quota defined in this resource
+	// +listType=atomic
+	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+
+type ResourceFlavorSpec struct {
+	// dynamicResources defines Kubernetes Dynamic Resource Allocation resources
+	// +optional
+	// +featureGate=DynamicResourceStructuredParameters
+	// +listType=atomic
+	// +kubebuilder:validation:MaxItems=16
+	DynamicResources []DynamicResourceMapping `json:"dynamicResources,omitempty"`
+}
+```
+
+However, this design had a major drawback. The biggest issue was semantic confusion - a dynamicResource like `gpu` in
+ResourceFlavor1 could have deviceClass gpu-a.example.com while the same dynamicResource name in ResourceFlavor2 could
+have the completely different deviceClass gpu-b.example.com. This creates significant confusion for cluster
+administrators because the same resource name would have different meanings depending on which ResourceFlavor was being
+referenced. The singleton DynamicResourceAllocationConfig CRD approach addresses this by providing a single source of
+truth for all device class mappings in the cluster.
+
```

  updated.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T13:24:55Z

- **keps/2941-DRA/README.md:373** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T13:24:55Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this canonical name
+    // +listType=atomic
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a canonical name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which canonical resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple canonical names
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using canonical names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple canonical names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the canonical names that correspond to the requested device class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota  
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
```

  added it in Notes

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T13:25:05Z

- **keps/2941-DRA/README.md:163** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T13:25:05Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
```

  added.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T13:29:55Z

- **keps/2941-DRA/README.md:216** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T13:29:55Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
```

  marking this Resolved.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T13:35:15Z

- **keps/2941-DRA/README.md:251** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T13:35:15Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  I think all of these are good follow-up discussions for beta.  
  
  To me it seems these changes could be additive to the DynamicResourceAllocationConfig struct instead of "breaking".  So as decided earlier, I have updated the design with option (a)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T17:43:45Z

- **keps/2941-DRA/README.md:266** by [@mimowo](https://github.com/mimowo) — 2025-06-24T17:43:45Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
+}
+
+// Resources defines a mapping from a resource name to a list of device classes
+type Resources struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=8
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a resource name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+  namespace: "kube-system"
```

  ```suggestion
    namespace: "kueue-system"
  ```
  kueue-system is the namespace we use in Kueue

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T17:44:38Z

- **keps/2941-DRA/README.md:442** by [@mimowo](https://github.com/mimowo) — 2025-06-24T17:44:38Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
+}
+
+// Resources defines a mapping from a resource name to a list of device classes
+type Resources struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=8
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a resource name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+  namespace: "kube-system"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of
+example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple resource names
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpus.example.com          # Appears here
+  - name: fast-gpus
+    deviceClassNames:
+    - gpus.example.com          # And also here - creates ambiguity
+---
+# ClusterQueue using resource names from the DynamicResourceAllocationConfig
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: multi-zone-queue
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "fast-gpus"]
+    flavors:
+    - name: zone-a-flavor
+      resources:
+      - name: whole-gpus
+        nominalQuota: 2
+    - name: zone-b-flavor
+      resources:
+      - name: fast-gpus
+        nominalQuota: 3
+```
+
+When a device class like `gpus.example.com` maps to multiple resource names (`whole-gpus` and `fast-gpus`), Kueue must
+determine which quota to use for admission.
+
+Kueue resolves this ambiguity using a top-down flavor matching process:
+
+1. Examine flavors in the order they are listed in the ClusterQueue spec
+2. For each flavor, check if any of its resources match the resource names that correspond to the requested device
+   class
+3. Select the first flavor that contains a resource mapping to the device class and has sufficient quota available
+
+For example, when a workload requests `gpus.example.com`:
+- First check `zone-a-flavor` for available `whole-gpus` quota
+- If `zone-a-flavor` is exhausted, check `zone-b-flavor` for available `fast-gpus` quota
+- If no flavors have available quota, the workload waits in queue
+
+This approach ensures predictable behavior where the ClusterQueue flavor ordering determines quota evaluation priority.
+
+### Workloads
+
+When a user submits a workload and DynamicResourceAllocation feature gate is on, Kueue will do the following:
+
+1. Claims will be read from podSpec.resourceClaims in the PodTemplateSpec.
+2. ResourceClaimSpec will be looked up either by using:
+    1. the name of the ResourceClaimTemplate or
+    2. the name of the ResourceClaim
+
+   Both ResourceClaimTemplate or ResourceClaim will be in the same namespace as the workload.
+3. From the ResourceClaimSpec, the deviceClassName will be read.
+4. Every claim, deviceClassName for each request will be looked at
+    1. For the workload a deviceClassMap will be created by:
+        1. Retrieving the singleton DynamicResourceAllocationConfig resource (named "default")
+        2. Creating a map from DeviceClass names to resource names using the
+           DynamicResourceAllocationConfig.spec.dynamicResources entries
+        3. For resource claims that are already allocated, Kueue will maintain a mapping called `admittedResourceClaims`
+           where the key follows the format `{clusterQueueName}/{resourceClaimNamespace}/{resourceClaimName}`
+        4. If a workload is using an already allocated resource claim and the key
+           `{targetClusterQueueName}/{resourceClaimNamespace}/{resourceClaimName}` exists in the
+           `admittedResourceClaims`
+           map, Kueue will skip counting that claim against quota and automatically admit the workload
+        5. This approach ensures that within a single cluster queue, reusing allocated resource claims doesn't consume
+           additional quota, while still enforcing quota boundaries between different cluster queues
+    2. For each device class the resource name will be looked up from the deviceClassMap and resource will be
+       counted against it.
+5. Once the Kueue counts and admits the workloads, it saves the count in workload status. This does not require any API
+   change.
+
+All the step above are reflect in the YAMLs below:
+```yaml
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu # 1) read the claim from resources.claims
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu # use the name read from #1
+        resourceClaimTemplateName: single-gpu # 2.1) the name for resource claim template
+---
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com # 3) the name of the device class
+---
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig # 4.1.1) Singleton CR that defines all device class mappings
+metadata:
+  name: "cluster-device-mapping"
```

  ```suggestion
    name: "default"
    namespace: kueue-system
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T17:44:56Z

- **keps/2941-DRA/README.md:322** by [@mimowo](https://github.com/mimowo) — 2025-06-24T17:44:56Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
+}
+
+// Resources defines a mapping from a resource name to a list of device classes
+type Resources struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=8
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a resource name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+  namespace: "kube-system"
+spec:
+  dynamicResources:
+  - name: whole-gpus
+    deviceClassNames:
+    - gpu.example.com
+  - name: shared-gpus
+    deviceClassNames:
+    - ts-shard-gpus.example.com
+    - sp-shared-gpus.example.com
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ResourceFlavor
+metadata:
+  name: "default-gpu-flavor"
+spec:
+  # No changed needed here
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "gpus-cluster-queue"
+spec:
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "whole-gpus", "shared-gpus"]
+    flavors:
+    - name: "default-gpu-flavor"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: "1200Mi"
+      - name: 'whole-gpus'
+        nominalQuota: 2
+      - name: 'shared-gpus'
+        nominalQuota: 2
+```
+
+The above ClusterQueue is an example configuration of a queue, with half quota configured for single allocation of
+example
+GPUs, and half quota configured for GPUs that are shared by workloads. Similarly, when DRAPartitionableDevices feature
+is supported in kubernetes, GPUs partitions can be represented by a single device class.
+
+### Device Class Resolution and Ambiguity Handling
+
+#### Device Class Mapping Ambiguity
+
+Ambiguity occurs when a device class appears in multiple mappings within the DynamicResourceAllocationConfig CR. This
+creates uncertainty at admission time about which resource name and corresponding quota should be used.
+
+Example of ambiguous configuration:
+```yaml
+# Configuration allowing device class to map to multiple resource names
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "cluster-device-mapping"
```

  ```suggestion
    name: "default"
    namespace: kueue-system
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T17:45:48Z

LGTM, left just nits

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T18:07:00Z

- **keps/2941-DRA/README.md:241** by [@mimowo](https://github.com/mimowo) — 2025-06-24T18:07:00Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
```

  ```suggestion
      Resources []DynamicResources `json:"resources"`
  ```
  or DRAResources

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T18:07:37Z

- **keps/2941-DRA/README.md:268** by [@mimowo](https://github.com/mimowo) — 2025-06-24T18:07:37Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
+}
+
+// Resources defines a mapping from a resource name to a list of device classes
+type Resources struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=8
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a resource name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+  namespace: "kube-system"
+spec:
+  dynamicResources:
```

  ```suggestion
    resources:
  ```
  to align with the API

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T18:17:07Z

- **keps/2941-DRA/README.md:241** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T18:17:07Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
```

  https://github.com/kubernetes-sigs/kueue/pull/5149#discussion_r2161883129
  
  I thought @tenzen-y wanted alignment in field name and struct name.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-24T18:19:58Z

- **keps/2941-DRA/README.md:268** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-24T18:19:58Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
+}
+
+// Resources defines a mapping from a resource name to a list of device classes
+type Resources struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=8
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a resource name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+  namespace: "kube-system"
+spec:
+  dynamicResources:
```

  Good catch, updated all such instances, found 4 more further down the doc that needed to be fixed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T18:23:52Z

- **keps/2941-DRA/README.md:241** by [@mimowo](https://github.com/mimowo) — 2025-06-24T18:23:52Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
```

  https://github.com/kubernetes-sigs/kueue/pull/5149#discussion_r2161883129
  
  actually suggests DynamicResource as the struct name, which is even better. 
  
  Generally struct names can be more specific as they are global. Field names can be shorter and rely on the context.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-25T10:55:38Z

- **keps/2941-DRA/README.md:268** by [@mimowo](https://github.com/mimowo) — 2025-06-25T10:55:38Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
+}
+
+// Resources defines a mapping from a resource name to a list of device classes
+type Resources struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=8
+    DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+}
+```
+
+The ResourceFlavor spec will have a field called dynamicResource. The cluster admin has to define the list of
+deviceClasses that will be represented by a resource name and can be used to define quotas in cluster queue. With this
+a ClusterQueue with nominalQuota can be defined as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: DynamicResourceAllocationConfig
+metadata:
+  name: "default"  # Fixed name - singleton CR, only one instance allowed
+  namespace: "kube-system"
+spec:
+  dynamicResources:
```

  thx, feel free to resolve

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-25T10:56:11Z

- **keps/2941-DRA/README.md:241** by [@mimowo](https://github.com/mimowo) — 2025-06-25T10:56:11Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
```

  thx, feel free to resolve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-25T12:19:45Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T12:19:45Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DeviceClassMapping API](#deviceclassmapping-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes modifying:
+1. The `ResourceFlavor` API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DeviceClassMapping API
+
+```golang
+// DeviceClassMapping is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas.
+type DeviceClassMapping struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DeviceClassMapping
+    Spec DeviceClassMappingSpec `json:"spec"`
+}
+
+// DeviceClassMappingSpec defines the mappings between device classes and canonical names
+type DeviceClassMappingSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceMapping `json:"dynamicResources"`
+}
+
+// DeviceMapping defines a mapping from a canonical name to a list of device classes
+type DeviceMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
```

  > I think all of these are good follow-up discussions for beta.
  > 
  > To me it seems these changes could be additive to the DynamicResourceAllocationConfig struct instead of "breaking". So as decided earlier, I have updated the design with option (a)
  
  Yes, that's right. I just put for the next discussion points.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-25T12:26:55Z

- **keps/2941-DRA/README.md:238** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T12:26:55Z
```diff
@@ -0,0 +1,729 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+Dynamic Resource Allocation (DRA) is a major effort to improve device support in Kubernetes.
+It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a 
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and running 
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire 
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+  - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+    will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+   addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management, 
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping canonical name -> device class names list
+2. Allowing admins to refer to canonical name defined in resource flavor to define nominalQuota. The nominalQuota here
+   will be applied to workloads requesting devices from any DeviceClasses mentioned in the deviceClassNames list for
+   the specific canonical name. More details are documented in [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users. 
+
+### Notes/Constraints/Caveats (Optional)
+
+- Device classes mapping to multiple canonical names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being 
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a cluster-scoped singleton CRD that maps device classes to canonical names
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between device classes and canonical names
+type DynamicResourceAllocationConfigSpec struct {
+    // DynamicResources is a list of mappings from canonical names to device classes
+    // +listType=atomic
+    DynamicResources []DeviceClassMapping `json:"dynamicResources"`
+}
+
+// DeviceClassMapping defines a mapping from a canonical name to a list of device classes
+type DeviceClassMapping struct {
+    // Name is the canonical name that will be referred to in ClusterQueue and Workload status
```

  I see. In that case, we want to say like "Name is the canonical resource name that will be referred to in ClusterQueue and Workload admission status".

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-25T12:29:36Z

- **keps/2941-DRA/README.md:241** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T12:29:36Z
```diff
@@ -0,0 +1,754 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []Resources `json:"resources"`
```

  > https://github.com/kubernetes-sigs/kueue/pull/5149#discussion_r2161883129
  > 
  > I thought @tenzen-y wanted alignment in field name and struct name.
  
  When I left the comment, the field name was different. So, the comment has already been outdated.
  As @mimowo mentioned, we want to use the API specific name as a Go API prefix to avoid unexpected conflicts with other object.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-25T12:46:26Z

Almost lgtm, thank you!
The most important question is whether or not we should care the order in the `.spec.resources` and `.spec.resources[*].deviceClassNames`.

If the order should be careful, we should mention that in API comment.
Otherwise, we can use the listType=map API annotation for mergePatch.

- **keps/2941-DRA/README.md:225** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T12:38:02Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
```

  ```suggestion
  // +genclient
  // +kubebuilder:object:root=true
  // +kubebuilder:storageversion
  
  // DynamicResourceAllocationConfig is a singleton CRD that maps resource names to device classes
  ```
  
  We should add those markers and fix a typo.

- **keps/2941-DRA/README.md:239** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T12:39:47Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
```

  Why do we need to have `atomic` instead of `map`? Is this `resources` field order important?
  If we can accept this field as any order, we should use the following:
  
  
  ```suggestion
      // +listType=map
      // +listMapKey=name
  ```

- **keps/2941-DRA/README.md:246** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T12:42:21Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
```

  ```suggestion
      // Name is the resource name that will be referred to in ClusterQueue and Workload admission status.
  ```
  
  As I mentioned in another thread, could we say as `admission status`?

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T12:43:08Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  Same as another comment, do we need this as an atomic? Or is this field acceptable in any order?

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-25T13:41:08Z

- **keps/2941-DRA/README.md:239** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-25T13:41:08Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
```

  considering the following:
  
  1. duplicates should not be allowed
  2. order of the items is not important, rather contents of the field is important, this could be a map.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-25T13:45:22Z

- **keps/2941-DRA/README.md:239** by [@mimowo](https://github.com/mimowo) — 2025-06-25T13:45:21Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
```

  +1 for using map, yes, duplicates are not supported, the resource canonical name should be used as the key

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-25T13:47:43Z

- **keps/2941-DRA/README.md:251** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-25T13:47:43Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  Since these are equivalent of string literals, I think `+listType=set` makes the most sense to me.
  
  ref: https://github.com/kubernetes/kube-openapi/blob/8b98d1ed966af811a5b7401560a259e75937e25b/pkg/idl/doc.go#L68
  
  @tenzen-y wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-25T13:48:18Z

- **keps/2941-DRA/README.md:251** by [@mimowo](https://github.com/mimowo) — 2025-06-25T13:48:18Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  I think we can go with map, we should not have duplicates also. I think the validation rule for detecting duplicaes can be added later during impl, but for reference this is how we verify the levels are unique in topology: https://github.com/kubernetes-sigs/kueue/blob/eff2574a0b8215c4416442a70da89b8acbf34f9b/apis/kueue/v1alpha1/topology_types.go#L102C2-L102C132

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-25T13:49:01Z

- **keps/2941-DRA/README.md:239** by [@mimowo](https://github.com/mimowo) — 2025-06-25T13:49:01Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
```

  For detecting duplicates we can probably follow the pattern for [topology](https://github.com/kubernetes-sigs/kueue/blob/eff2574a0b8215c4416442a70da89b8acbf34f9b/apis/kueue/v1alpha1/topology_types.go#L102C2-L102C132)

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-25T13:56:38Z

- **keps/2941-DRA/README.md:251** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-25T13:56:38Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  if we go with map or set, we automatically get validation for unique entries.
  
  My understanding is that with a map, two clients can theoretically hot loop one patching deviceClassNames: [A, B, C] and other patching deviceClassNames: [B, A, C].
  
  I think we can avoid this with a set and get unique entries.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-25T14:50:57Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T14:50:57Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  I'm ok with either typed, `map` / `set`. The point is if we can return the duplication errors to users when they accidentally add duplicated values.
  
  @alaypatel07 Do you know if `set` supports raising validation errors and returning them to users? Or is it just outputting the error log in the kueue-controller-manager?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-25T15:15:56Z

- **keps/2941-DRA/README.md:251** by [@mimowo](https://github.com/mimowo) — 2025-06-25T15:15:56Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  I'm fine with set too. 
  
  tbh I think like the validation can also be determined later during implementation phase, because then we will add a test, and now we rely static analysis.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-25T15:16:50Z

- **keps/2941-DRA/README.md:251** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-25T15:16:50Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  > @alaypatel07 Do you know if set supports raising validation errors and returning them to users? Or is it just outputting the error log in the kueue-controller-manager?
  
  I assumed this is going to be a openapi validation, so client will get the message through validation before it even hits the webhook/kueue stack

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-25T15:22:26Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T15:22:26Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  > > @alaypatel07 Do you know if set supports raising validation errors and returning them to users? Or is it just outputting the error log in the kueue-controller-manager?
  > 
  > I assumed this is going to be a openapi validation, so client will get the message through validation before it even hits the webhook/kueue stack
  
  Thank you for describing that. In that case, we can select `set` instead of `map` which reduce annotations.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-25T15:23:53Z

- **keps/2941-DRA/README.md:251** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-25T15:23:53Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  > tbh I think like the validation can also be determined later during implementation phase, because then we will add a test, and now we rely static analysis.
  
  It looks like the problems has been resolved. So, we can move to the next step.

### Review by [@alaypatel07](https://github.com/alaypatel07) (commented) — 2025-06-25T15:27:48Z

- **keps/2941-DRA/README.md:251** by [@alaypatel07](https://github.com/alaypatel07) — 2025-06-25T15:27:48Z
```diff
@@ -0,0 +1,756 @@
+# KEP-2941: Structured Parameters
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Background](#background)
+    - [DRA Example](#dra-example)
+    - [Workload Example](#workload-example)
+    - [Example Driver Cluster Resources](#example-driver-cluster-resources)
+      - [ResourceSlices](#resourceslices)
+      - [DeviceClasses](#deviceclasses)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [DynamicResourceAllocationConfig API](#dynamicresourceallocationconfig-api)
+  - [Device Class Resolution and Ambiguity Handling](#device-class-resolution-and-ambiguity-handling)
+    - [Device Class Mapping Ambiguity](#device-class-mapping-ambiguity)
+  - [Workloads](#workloads)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [E2E Test](#e2e-test)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [ResourceClaim By Count](#resourceclaim-by-count)
+  - [Using devices in ResourceSlice to Count](#using-devices-in-resourceslice-to-count)
+  - [Using a CEL expression](#using-a-cel-expression)
+  - [Defining DeviceClass mapping in ClusterQuota](#defining-deviceclass-mapping-in-clusterquota)
+  - [Using ResourceFlavor for DeviceClass Mapping](#using-resourceflavor-for-deviceclass-mapping)
+  - [Using ConfigMap for DeviceClass Mapping](#using-configmap-for-deviceclass-mapping)
+<!-- /toc -->
+
+## Summary
+
+[Dynamic Resource Allocation (DRA)](https://kubernetes.io/docs/concepts/scheduling-eviction/dynamic-resource-allocation/)
+is a major effort to improve device support in Kubernetes. It changes how one can request resources in a myriad of ways.
+
+## Motivation
+
+Dynamic Resource Allocation (DRA) provides the groundwork for more sophisticated device allocations to Pods.
+Quota management is about enforcing rules around the use of resources.
+For example, GPUs are resource constrained and a popular request is the ability to enforce fair sharing of GPU
+resources.
+With these devices, many users want access and sometimes some users want the ability to preempt other users if their
+workloads have a higher priority. Kueue provides support for this.
+
+DRA provides a future where users could schedule partitionable GPU devices (MIG) or time slicing. As devices gain a
+more robust way to schedule, it is important to walk through how support of DRA will work with Kueue.
+
+### Background
+
+DRA has four APIs that are relevant for a Kueue:
+
+- ResourceClaims
+- ResourceClaimTemplates
+- DeviceClasses
+- ResourceSlices
+
+#### DRA Example
+
+The easiest way to test DRA is to
+use [dra example driver repository](https://github.com/kubernetes-sigs/dra-example-driver). Cloning that repo and
+running
+`make setup-e2e` will create a Kind cluster with the DRA feature gate and install a mock dra driver. This does not use
+actual GPUs so it is perfect for a test environment for exploring Kueue and DRA integration.
+
+#### Workload Example
+
+An example workload that uses DRA:
+
+```yaml
+---
+
+apiVersion: resource.k8s.io/v1alpha3
+kind: ResourceClaimTemplate
+metadata:
+  namespace: gpu-test1
+  name: single-gpu
+spec:
+  spec:
+    devices:
+      requests:
+      - name: gpu
+        deviceClassName: gpu.example.com
+---
+apiVersion: batch/v1
+kind: Job
+metadata:
+  namespace: gpu-test1
+  name: job0
+  labels:
+    app: job
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  template:
+    spec:
+      restartPolicy: Never
+      containers:
+      - name: ctr0
+        image: ubuntu:22.04
+        command: ["bash", "-c"]
+        args: ["export; sleep 9999"]
+        resources:
+          claims:
+          - name: gpu
+          requests:
+            cpu: 1
+            memory: "200Mi"
+      resourceClaims:
+      - name: gpu
+        resourceClaimTemplateName: single-gpu
+```
+
+#### Example Driver Cluster Resources
+
+The dra-example-driver creates a ResourceSlice for each node and a DeviceClass named `gpu.example.com` for the entire
+cluster.
+
+##### ResourceSlices
+
+ResourceSlices are meant for communication between drivers and the control planes. These are not expected to be used for
+workloads.
+
+##### DeviceClasses
+
+Each driver creates a device class and every resource claim will reference the device class. The dra-example-driver has
+a simple device class named `gpu.example.com`. This will be the way to enforce quota limits.
+
+### Goals
+
+- Users can submit workloads using resource claims and Kueue can monitor the usage.
+- Admins can enforce the quota for number of devices for a given DeviceClass.
+
+
+### Non-Goals
+
+- We are limiting scope for DRA to structured parameters (beta in 1.32 and 1.33)
+    - Support for alpha features like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists and DRAPartitionableDevices
+      will not be included.
+- This design does not work with Topology Aware Scheduling feature of Kueue. It is a significant amount of work, will be
+  addressed in the future with a separate body of work
+
+## Proposal
+
+This proposal is to extend the APIs for allowing workloads using DRA APIs to be tallied against quota management,
+borrowing and preemptable scheduling. This includes:
+1. Introducing a new API to allow creating a mapping of resource name -> device class names list
+2. Allowing admins to refer to resource name defined in DynamicResourceAllocationConfig in ClusterQueue to define
+   nominalQuota. The nominalQuota here will be applied to workloads requesting devices from any DeviceClasses mentioned
+   in the deviceClassNames list for the specific resource name. More details are documented in
+   [Design Details](#design-details)
+
+### User Stories (Optional)
+
+#### Story 1
+
+As a Kueue user, I want to use DRA devices for batch workloads in Kubernetes using Kueue
+
+#### Story 2
+
+As an administrator of Kueue with ClusterQueue, I have a DRA driver installed in the cluster. I would like to enforce
+queuing, quota management and preemptable workloads for cluster users.
+
+### Notes/Constraints/Caveats (Optional)
+
+- The `ResourceClaims` and `ResourceClaimTemplates` APIs for DRA in k8s are immutable.
+- Device classes mapping to multiple resource names can create ambiguity during quota enforcement and requires careful
+  handling.
+- This design does not work with Kueue's Topology Aware Scheduling feature and will be addressed in future work.
+- This implementation focuses on structured parameters (beta in 1.32 and 1.33) and does not support alpha DRA features
+  like DRADeviceTaints, DRAAdminAccess, DRAPrioritizedLists, and DRAPartitionableDevices.
+
+### Risks and Mitigations
+
+With newer DRA features like DRAAdminAccess and DRAPrioritizedLists, there is a risk that
+effective tallying of resources will not be available until after allocation of these resources by kube-scheduler.
+
+In order to mitigate this risk, Kueue can take the following approach:
+1. For DRAPrioritizedLists: all the mentioned device classes in the request will be counted against the quota
+2. For DRAAdminAccess: This feature can only be enabled in admin namespace, therefore it should be skipped for being
+   counted against ClusterQuota. This is a different stance than Kubernetes ResourceQuota because kubernetes
+   ResourceQuota is namespace scoped. As a result, admin users can account for quota independent of user workloads.
+   On the contrary, with Kueue, since quota is part of ClusterQuota a cluster scoped object, admin workloads using
+   devices in admin namespace if counted against quota, will eat up quota meant for user workloads.
+3. For ResourceClaims with allocation mode `All`: worst-case scenario of the max number of devices that could be
+   allocated to a single claim will be used against quota.
+
+
+## Design Details
+
+A new feature gate DynamicResourceAllocation will be introduced, allowing users to test it in dev environments while
+making the changes dormant for production users. The following sections will explain the design in detail.
+
+### DynamicResourceAllocationConfig API
+
+```golang
+// DynamicResourceAllocationConfig is a singleton CRD that maps resource names todevice classes
+// used in ClusterQueue resource quotas. It is singleton as "default" is the only allowed named for the CRD instance in
+// Kueue namespace.
+type DynamicResourceAllocationConfig struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    // Spec defines the desired state of DynamicResourceAllocationConfig
+    Spec DynamicResourceAllocationConfigSpec `json:"spec"`
+}
+
+// DynamicResourceAllocationConfigSpec defines the mappings between resource names and device classes
+type DynamicResourceAllocationConfigSpec struct {
+    // Resources is a list of mappings from resource name to device classes
+    // +listType=atomic
+    // +kubebuilder:validation:MaxItems=16
+    Resources []DynamicResource `json:"resources"`
+}
+
+// DynamicResource defines a mapping from a resource name to a list of device classes
+type DynamicResource struct {
+    // Name is the resource name that will be referred to in ClusterQueue and Workload status
+    Name corev1.ResourceName `json:"name"`
+
+    // DeviceClassNames lists the names of all the device classes that will count against
+    // the quota defined for this resource name
+    // +listType=atomic
```

  updated.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-25T15:29:28Z

@alaypatel07 Awesome work! I'm looking forward to seeing DRA & Kueue collaboration!
Thank you!

/lgtm
/approve

/hold cancel
