# PR #5211: Update KEP-1282 to support WaitForPodsReady metrics for eviction by reason

**Summary**: Update KEP-1282 to support WaitForPodsReady metrics for eviction by reason

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5211

**Last updated**: 2025-05-13T11:24:04Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `docs/update-kep-1282` → `main`
- **Created**: 2025-05-09T08:19:08Z
- **Updated**: 2025-05-13T11:24:04Z
- **Merged**: 2025-05-12T08:17:15Z
- **Closed**: 2025-05-12T08:17:15Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+45 / -3**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Update KEP-1282 to support WaitForPodsReady metrics for eviction by reason.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #5071

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-09T08:19:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b60cc0958b12df6036037ebfbc25a66759156c1a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/681e4922237f460008cbce4e |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-09T08:30:12Z

/retitle Update KEP-1282 to support WaitForPodsReady metrics for eviction by reason

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-09T08:30:22Z

cc @mwielgus

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-09T08:37:28Z

/cc @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-09T09:09:44Z

/lgtm
/approve
/hold
Thanks, I would like to get a look from another pair of eyes, like @mwielgus or @tenzen-y . If people are too busy, I think the proposal is reasonable and will unblock it today probably.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-09T09:09:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5211#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5211#issuecomment-2865784425" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-09T09:09:51Z

LGTM label has been added.  <details>Git tree hash: fb11d54f538b368d17d5e16bd5447e2be7be0249</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-12T08:00:57Z

/lgtm
Thanks @mbobrovskyi!
/unhold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-12T08:01:04Z

LGTM label has been added.  <details>Git tree hash: 1e6e5341ce38951b6997b91ff6fda6da77bc050f</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-12T08:01:42Z

LGTM

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-13T07:30:58Z

@mimowo @tenzen-y ptal https://github.com/kubernetes-sigs/kueue/pull/5238

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T11:24:03Z

@mimowo @mszadkow @PBundyra I will leave any comments on new KEP PR instead of here. Thank you for responding to my comments.

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-09T10:40:58Z

- **keps/1282-pods-ready-requeue-strategy/README.md:221** by [@mwielgus](https://github.com/mwielgus) — 2025-05-09T10:40:58Z
```diff
@@ -208,6 +208,22 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `waitForPodsReadyStats` to the WorkloadStatus struct to track the number of evictions by reason:
+
+```go
+type WorkloadStatus struct {
+    // waitForPodsReadyStats holds statistics for WaitForPodsReady requeues.
+    // +optional
+    WaitForPodsReadyStats *WaitForPodsReadyStats `json:"waitForPodsReadyStats,omitempty"`
+}
+
+type WaitForPodsReadyStats struct {
+    // evictionByReasonCount maps eviction reasons to their counts.
```

  What other fields do you expect to have here in the future?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-09T10:55:56Z

- **keps/1282-pods-ready-requeue-strategy/README.md:221** by [@mimowo](https://github.com/mimowo) — 2025-05-09T10:55:56Z
```diff
@@ -208,6 +208,22 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `waitForPodsReadyStats` to the WorkloadStatus struct to track the number of evictions by reason:
+
+```go
+type WorkloadStatus struct {
+    // waitForPodsReadyStats holds statistics for WaitForPodsReady requeues.
+    // +optional
+    WaitForPodsReadyStats *WaitForPodsReadyStats `json:"waitForPodsReadyStats,omitempty"`
+}
+
+type WaitForPodsReadyStats struct {
+    // evictionByReasonCount maps eviction reasons to their counts.
```

  no specific expectation at the moment, it was proposed as a potential container since WaitForPodsReady is a growing feature we may anticipate more metrics to track.
  
  However, I'm totally ok to inline the field as `WaitForPodsReadyEvictionByReasonCount`. We can always revisit in v1beta2.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-09T16:54:38Z

- **keps/1282-pods-ready-requeue-strategy/README.md:223** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-09T16:54:34Z
```diff
@@ -208,6 +208,22 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `waitForPodsReadyStats` to the WorkloadStatus struct to track the number of evictions by reason:
+
+```go
+type WorkloadStatus struct {
+    // waitForPodsReadyStats holds statistics for WaitForPodsReady requeues.
+    // +optional
+    WaitForPodsReadyStats *WaitForPodsReadyStats `json:"waitForPodsReadyStats,omitempty"`
+}
+
+type WaitForPodsReadyStats struct {
+    // evictionByReasonCount maps eviction reasons to their counts.
+    // +optional
+    EvictionByReasonCount map[string]int32 `json:"evictionByReasonCount,omitempty"`
```

  I still do not understand what is relationship between existing requeueState.
  
  However, at least, I can say this should not be introduced since map will lose any kind of CRD based possibilities.
  If we want to introduce such APIs, we should use a dedicated struct having reason and count.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-09T17:03:41Z

- **keps/1282-pods-ready-requeue-strategy/README.md:223** by [@mimowo](https://github.com/mimowo) — 2025-05-09T17:03:41Z
```diff
@@ -208,6 +208,22 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `waitForPodsReadyStats` to the WorkloadStatus struct to track the number of evictions by reason:
+
+```go
+type WorkloadStatus struct {
+    // waitForPodsReadyStats holds statistics for WaitForPodsReady requeues.
+    // +optional
+    WaitForPodsReadyStats *WaitForPodsReadyStats `json:"waitForPodsReadyStats,omitempty"`
+}
+
+type WaitForPodsReadyStats struct {
+    // evictionByReasonCount maps eviction reasons to their counts.
+    // +optional
+    EvictionByReasonCount map[string]int32 `json:"evictionByReasonCount,omitempty"`
```

  I'm find with count and reason. Maybe more generic
  
  ```yaml
  evictionStats:
     waitForPodsReady:
      - reason: StartupTimeout 
        count: 5
      - reason: RecoveryTimeout
        count: 2
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-09T17:04:53Z

- **keps/1282-pods-ready-requeue-strategy/README.md:223** by [@mimowo](https://github.com/mimowo) — 2025-05-09T17:04:53Z
```diff
@@ -208,6 +208,22 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `waitForPodsReadyStats` to the WorkloadStatus struct to track the number of evictions by reason:
+
+```go
+type WorkloadStatus struct {
+    // waitForPodsReadyStats holds statistics for WaitForPodsReady requeues.
+    // +optional
+    WaitForPodsReadyStats *WaitForPodsReadyStats `json:"waitForPodsReadyStats,omitempty"`
+}
+
+type WaitForPodsReadyStats struct {
+    // evictionByReasonCount maps eviction reasons to their counts.
+    // +optional
+    EvictionByReasonCount map[string]int32 `json:"evictionByReasonCount,omitempty"`
```

  regarding the requeueState, ok n the current form it does not carry the required information currently. It is just a counter without reason, counting not only waitforpodsready

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-05-09T17:17:25Z

- **keps/1282-pods-ready-requeue-strategy/README.md:223** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-09T17:17:25Z
```diff
@@ -208,6 +208,22 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `waitForPodsReadyStats` to the WorkloadStatus struct to track the number of evictions by reason:
+
+```go
+type WorkloadStatus struct {
+    // waitForPodsReadyStats holds statistics for WaitForPodsReady requeues.
+    // +optional
+    WaitForPodsReadyStats *WaitForPodsReadyStats `json:"waitForPodsReadyStats,omitempty"`
+}
+
+type WaitForPodsReadyStats struct {
+    // evictionByReasonCount maps eviction reasons to their counts.
+    // +optional
+    EvictionByReasonCount map[string]int32 `json:"evictionByReasonCount,omitempty"`
```

  What do you think about this?
  
  ```
  evictionStats:
    - type: PodsReadyTimeout 
      reason: StartupTimeout 
      count: 5
    - type: PodsReadyTimeout 
      reason: RecoveryTimeout
      count: 2
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-09T17:40:38Z

- **keps/1282-pods-ready-requeue-strategy/README.md:223** by [@mimowo](https://github.com/mimowo) — 2025-05-09T17:40:38Z
```diff
@@ -208,6 +208,22 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `waitForPodsReadyStats` to the WorkloadStatus struct to track the number of evictions by reason:
+
+```go
+type WorkloadStatus struct {
+    // waitForPodsReadyStats holds statistics for WaitForPodsReady requeues.
+    // +optional
+    WaitForPodsReadyStats *WaitForPodsReadyStats `json:"waitForPodsReadyStats,omitempty"`
+}
+
+type WaitForPodsReadyStats struct {
+    // evictionByReasonCount maps eviction reasons to their counts.
+    // +optional
+    EvictionByReasonCount map[string]int32 `json:"evictionByReasonCount,omitempty"`
```

  sgtn

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-09T17:44:18Z

- **keps/1282-pods-ready-requeue-strategy/README.md:223** by [@mimowo](https://github.com/mimowo) — 2025-05-09T17:44:18Z
```diff
@@ -208,6 +208,22 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `waitForPodsReadyStats` to the WorkloadStatus struct to track the number of evictions by reason:
+
+```go
+type WorkloadStatus struct {
+    // waitForPodsReadyStats holds statistics for WaitForPodsReady requeues.
+    // +optional
+    WaitForPodsReadyStats *WaitForPodsReadyStats `json:"waitForPodsReadyStats,omitempty"`
+}
+
+type WaitForPodsReadyStats struct {
+    // evictionByReasonCount maps eviction reasons to their counts.
+    // +optional
+    EvictionByReasonCount map[string]int32 `json:"evictionByReasonCount,omitempty"`
```

  And to be fair I like this proposal the most so far

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-05-09T18:26:53Z

- **keps/1282-pods-ready-requeue-strategy/README.md:223** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-05-09T18:26:53Z
```diff
@@ -208,6 +208,22 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `waitForPodsReadyStats` to the WorkloadStatus struct to track the number of evictions by reason:
+
+```go
+type WorkloadStatus struct {
+    // waitForPodsReadyStats holds statistics for WaitForPodsReady requeues.
+    // +optional
+    WaitForPodsReadyStats *WaitForPodsReadyStats `json:"waitForPodsReadyStats,omitempty"`
+}
+
+type WaitForPodsReadyStats struct {
+    // evictionByReasonCount maps eviction reasons to their counts.
+    // +optional
+    EvictionByReasonCount map[string]int32 `json:"evictionByReasonCount,omitempty"`
```

  Updated

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-12T12:45:35Z

@mbobrovskyi @mimowo It looks like this API still has a few problems.

- **keps/1282-pods-ready-requeue-strategy/README.md:211** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T12:38:31Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
```

  Let's use the `evictionState` to align with other fields like `requeueState` and `admisssionCheckState`.

- **keps/1282-pods-ready-requeue-strategy/README.md:233** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T12:40:01Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+
+```go
+type WorkloadStatus struct {
+	// evictionStats tracks eviction statistics by type and reason.
+    //
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+}
+
+type Eviction struct {
+    // type identifies the eviction type.
+    //
+    // +required
+    // +kubebuilder:validation:Required
+    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+    // +kubebuilder:validation:MaxLength=316
+    Type string `json:"type"`
+    
```

  Why do we need `type`? Doesn't it have only `reason`?

- **keps/1282-pods-ready-requeue-strategy/README.md:241** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T12:41:34Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+
+```go
+type WorkloadStatus struct {
+	// evictionStats tracks eviction statistics by type and reason.
+    //
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+}
+
+type Eviction struct {
+    // type identifies the eviction type.
+    //
+    // +required
+    // +kubebuilder:validation:Required
+    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+    // +kubebuilder:validation:MaxLength=316
+    Type string `json:"type"`
+    
+    // reason specifies the programmatic identifier for the eviction cause.
+    //
+    // +required
+    // +kubebuilder:validation:Required
+    // +kubebuilder:validation:MaxLength=1024
+    // +kubebuilder:validation:MinLength=1
+    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
+    Reason string `json:"reason"`
```

  Could we use the enum which has `WaitForStart` and `WaitForRecovery`? Because we do not want to generize this one and we should use conditions as much as possible.

- **keps/1282-pods-ready-requeue-strategy/README.md:222** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T12:44:32Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+
+```go
+type WorkloadStatus struct {
+	// evictionStats tracks eviction statistics by type and reason.
+    //
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    EvictionStats []Eviction `json:"evictionStats,omitempty"`
```

  ```suggestion
      EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
  ```
  
  This has the same potential problem as the previous Topology API which means this could potentially occur conflicts. We should use the identity API typed name.

- **keps/1282-pods-ready-requeue-strategy/README.md:225** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T12:44:51Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+
+```go
+type WorkloadStatus struct {
+	// evictionStats tracks eviction statistics by type and reason.
+    //
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+}
+
+type Eviction struct {
```

  ```suggestion
  type WorkloadEvictionState struct {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-12T12:49:35Z

- **keps/1282-pods-ready-requeue-strategy/README.md:211** by [@mimowo](https://github.com/mimowo) — 2025-05-12T12:49:35Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
```

  sgtm

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-13T07:08:16Z

- **keps/1282-pods-ready-requeue-strategy/README.md:233** by [@mszadkow](https://github.com/mszadkow) — 2025-05-13T07:08:16Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+
+```go
+type WorkloadStatus struct {
+	// evictionStats tracks eviction statistics by type and reason.
+    //
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+}
+
+type Eviction struct {
+    // type identifies the eviction type.
+    //
+    // +required
+    // +kubebuilder:validation:Required
+    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+    // +kubebuilder:validation:MaxLength=316
+    Type string `json:"type"`
+    
```

  There was no mention of `type` neither in https://github.com/kubernetes-sigs/kueue/pull/5127 nor in https://github.com/kubernetes-sigs/kueue/issues/5071.
  Except it was mentioned here: https://github.com/kubernetes-sigs/kueue/pull/5211#discussion_r2082134136
  But @mimowo example didn't have the `type` - https://github.com/kubernetes-sigs/kueue/pull/5211#discussion_r2082134136
  
  We can remove it

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-13T07:24:55Z

- **keps/1282-pods-ready-requeue-strategy/README.md:241** by [@mszadkow](https://github.com/mszadkow) — 2025-05-13T07:24:55Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+
+```go
+type WorkloadStatus struct {
+	// evictionStats tracks eviction statistics by type and reason.
+    //
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+}
+
+type Eviction struct {
+    // type identifies the eviction type.
+    //
+    // +required
+    // +kubebuilder:validation:Required
+    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+    // +kubebuilder:validation:MaxLength=316
+    Type string `json:"type"`
+    
+    // reason specifies the programmatic identifier for the eviction cause.
+    //
+    // +required
+    // +kubebuilder:validation:Required
+    // +kubebuilder:validation:MaxLength=1024
+    // +kubebuilder:validation:MinLength=1
+    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
+    Reason string `json:"reason"`
```

  I think it's reasonable to change it this way

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T07:34:15Z

- **keps/1282-pods-ready-requeue-strategy/README.md:233** by [@mimowo](https://github.com/mimowo) — 2025-05-13T07:34:15Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+
+```go
+type WorkloadStatus struct {
+	// evictionStats tracks eviction statistics by type and reason.
+    //
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+}
+
+type Eviction struct {
+    // type identifies the eviction type.
+    //
+    // +required
+    // +kubebuilder:validation:Required
+    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+    // +kubebuilder:validation:MaxLength=316
+    Type string `json:"type"`
+    
```

  Actually, I think the type is useful and we should probably keep it. My previous proposals didn't include "type" as a field explicitly, but included implicitly in the field name "WaitForPodsReadyStats".
  
  I think the type is needed, to split by the feature Preemption / WaitForPodsReady / AdmissionChecks. Otherwise we risk that the reasons are ambiguous, if different features use the same reason. Indeed, this is not the case currently. 
  
  So with type and reason fields we have hierarchy, type determines the feature, and reason exact reason.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T07:38:48Z

- **keps/1282-pods-ready-requeue-strategy/README.md:233** by [@mimowo](https://github.com/mimowo) — 2025-05-13T07:38:48Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+
+```go
+type WorkloadStatus struct {
+	// evictionStats tracks eviction statistics by type and reason.
+    //
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+}
+
+type Eviction struct {
+    // type identifies the eviction type.
+    //
+    // +required
+    // +kubebuilder:validation:Required
+    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+    // +kubebuilder:validation:MaxLength=316
+    Type string `json:"type"`
+    
```

  wdyt @tenzen-y ?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-13T08:31:05Z

- **keps/1282-pods-ready-requeue-strategy/README.md:233** by [@PBundyra](https://github.com/PBundyra) — 2025-05-13T08:31:05Z
```diff
@@ -208,6 +208,47 @@ type RequeueState struct {
 }
 ```
 
+Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+
+```go
+type WorkloadStatus struct {
+	// evictionStats tracks eviction statistics by type and reason.
+    //
+    // +optional
+    // +listType=map
+    // +listMapKey=type
+    // +patchStrategy=merge
+    // +patchMergeKey=type
+    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+}
+
+type Eviction struct {
+    // type identifies the eviction type.
+    //
+    // +required
+    // +kubebuilder:validation:Required
+    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+    // +kubebuilder:validation:MaxLength=316
+    Type string `json:"type"`
+    
```

  I am good with both options
  
  > Otherwise we risk that the reasons are ambiguous, if different features use the same reason. I
  
  If we use a separate enum instead of the same string like in Condition's Reason as [Yuki proposed](https://github.com/kubernetes-sigs/kueue/pull/5211#discussion_r2084589527) we mitigate this risk. Even we didn't do this if this is currently not a problem I wouldn't put too much attention to it. Additionally, we should be aware to not use the same reason in multiple features to avoid the issue
