# PR #5212: KEP-2724: In-Place topology reassignment when nodes are not ready

**Summary**: KEP-2724: In-Place topology reassignment when nodes are not ready

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5212

**Last updated**: 2025-05-28T09:41:36Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `kep_tas_error_handling` → `main`
- **Created**: 2025-05-09T08:22:06Z
- **Updated**: 2025-05-28T09:41:36Z
- **Merged**: 2025-05-20T09:41:16Z
- **Closed**: 2025-05-20T09:41:16Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 1   **+59 / -7**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This is to decide on the shape of the API update needed for TAS error handling improvements.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->


#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-09T08:22:12Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2e5a4a5662a7a64251367a375e6f36b89495fd4c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/68243848e5f0eb000813973f |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-09T08:22:15Z

Hi @pajakd. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:00:08Z

/ok-to-test

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T10:35:02Z

/retitle KEP-2724: In-Place topology reassignment when nodes are not ready

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-15T08:25:47Z

LGTM
@PBundyra @tenzen-y do you have more comments here?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-16T08:05:29Z

/lgtm
From me as well

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-16T08:05:37Z

LGTM label has been added.  <details>Git tree hash: edab20e8a50ed25fca8c43323fda58cd7e01aa3e</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-05-20T08:50:13Z

/lgtm

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-20T09:20:30Z

/approve
I believe this is ready to go 👍 The proposed new annotation is alpha level, if we see it does not cover all cases then we can  easily update the API.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-20T09:20:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5212#issuecomment-2893612563" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5212#" title="Author self-approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-09T16:46:32Z

- **keps/2724-topology-aware-scheduling/README.md:672** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-09T16:45:13Z
```diff
@@ -643,6 +644,18 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
```

  Could you expand this description more something like
  
  1. Which events do we need to address? For example, unschedulable Node and node label changes
  2. How to catch and take action on the event and address that? Daemonset / dedicated controllers
  3. What if there are multiple replacement nodes.
  4. ...

- **keps/2724-topology-aware-scheduling/README.md:827** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-09T16:46:26Z
```diff
@@ -811,6 +824,7 @@ The new validations which are for MVP, but likely will be relaxed in the future:
   or explicit level added by webhook.
 - introduce configuration for setting TAS profiles/algorithms
 - introduce a performance test for TAS [#4634](https://github.com/kubernetes-sigs/kueue/issues/4634)
+- change failed nodes information from Annotation into a field in workload.Status
```

  Sorry, what does that mean?
  As I can see from this enhancement documentation, I can see only a new Annotation.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-12T08:13:05Z

- **keps/2724-topology-aware-scheduling/README.md:827** by [@pajakd](https://github.com/pajakd) — 2025-05-12T08:13:04Z
```diff
@@ -811,6 +824,7 @@ The new validations which are for MVP, but likely will be relaxed in the future:
   or explicit level added by webhook.
 - introduce configuration for setting TAS profiles/algorithms
 - introduce a performance test for TAS [#4634](https://github.com/kubernetes-sigs/kueue/issues/4634)
+- change failed nodes information from Annotation into a field in workload.Status
```

  Sorry for the confusion. Added both changes in the KEP (Annotation and the field in Status). Keeping in mind that the change in Status requires more consideration (what should be the format? is the list of node names enough?), we wanted to temporarily use Annotations and then change it to the field in Status. 
  
  I didn't know if I should document only the initial change or the final one (or both) in KEP.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-12T08:13:32Z

- **keps/2724-topology-aware-scheduling/README.md:672** by [@pajakd](https://github.com/pajakd) — 2025-05-12T08:13:32Z
```diff
@@ -643,6 +644,18 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
```

  Added the description answering the questions.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-12T13:15:46Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:05:01Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  What if multiple workload-scheduled nodes are not ready?
  Do we add only first detected node to this annotation value? Or we will store all nodes as JSON format?

- **keps/2724-topology-aware-scheduling/README.md:675** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:09:13Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
```

  The batch admins with `kubectl` (`cordon $NODE_NAME` / `drain $NODE_NAME`) and some controllers add `node.kubernetes.io/unschedulable:NoSchedule` taints to nodes, and then the node can not be scheduled. 
  
  This is typical pod unschedulable situations. However, in this approach, will we ignore the situation?

- **keps/2724-topology-aware-scheduling/README.md:676** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:11:47Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
```

  Does this `TAS workload` mean running Workload? Or suspended Workload?
  If this means running workload, does this indicate bypass the FIFO?

- **keps/2724-topology-aware-scheduling/README.md:687** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:14:53Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  Shouldn't we store this informaiton in Topology CR? because this allows aggressive users to hack this mechanism to bypass FIFO mechanism. Why do we need to store this in Workload and manage every for Workload?

- **keps/2724-topology-aware-scheduling/README.md:703** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:15:27Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
+)
+```
+Alternatively (for Beta release), this information could be stored in a new field in WorkloadStatus.
+
+```golang
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  ...
+  // nodesToReplace lists the names of failed nodes running pods associated 
+  // with this workload. This field is populated by the node failure controller.
+  // +optional
+  // +listType=set
+  NodesToReplace []string `json:"nodesToReplace,omitempty"`
+}
+```
+
```

  If this is still evaluating, could you move it to alternative section? https://github.com/kubernetes-sigs/kueue/blob/191574c659fe6d75e43f97fd5cc3a5d71f99d01d/keps/2724-topology-aware-scheduling/README.md#alternatives?

- **keps/2724-topology-aware-scheduling/README.md:672** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:15:42Z
```diff
@@ -643,6 +644,18 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
```

  Thx.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-12T13:17:53Z

- **keps/2724-topology-aware-scheduling/README.md:827** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:17:53Z
```diff
@@ -811,6 +824,7 @@ The new validations which are for MVP, but likely will be relaxed in the future:
   or explicit level added by webhook.
 - introduce configuration for setting TAS profiles/algorithms
 - introduce a performance test for TAS [#4634](https://github.com/kubernetes-sigs/kueue/issues/4634)
+- change failed nodes information from Annotation into a field in workload.Status
```

  Thx.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-12T13:44:52Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:44:51Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  Additionally, will this annotation be removed once the Workload is requeued?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-12T16:24:01Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@mimowo](https://github.com/mimowo) — 2025-05-12T16:24:01Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  > because this allows aggressive users to hack this mechanism to bypass FIFO mechanism. 
  
  Could you elaborate on how this information could be used?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-12T16:24:40Z

- **keps/2724-topology-aware-scheduling/README.md:695** by [@mimowo](https://github.com/mimowo) — 2025-05-12T16:24:39Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
+)
+```
+Alternatively (for Beta release), this information could be stored in a new field in WorkloadStatus.
+
+```golang
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  ...
+  // nodesToReplace lists the names of failed nodes running pods associated 
+  // with this workload. This field is populated by the node failure controller.
+  // +optional
+  // +listType=set
+  NodesToReplace []string `json:"nodesToReplace,omitempty"`
+}
+```
+
+
```

  I would also like to add a note that the mechanism will only work for Topologies which specify kubernetes.io/hostname at the lowest level

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-13T07:52:15Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@pajakd](https://github.com/pajakd) — 2025-05-13T07:52:15Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  The idea is to initially handle only a single node failure (with Annotation holding its name). In the case of multiple failures we will evict the workload. 
  
  In the next step we would like to look for replacement in case of multiple failures and store the information about the (multiple) failed nodes in some dedicated field holding a list of node names (or any other format that makes more sense).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-13T08:07:02Z

- **keps/2724-topology-aware-scheduling/README.md:675** by [@pajakd](https://github.com/pajakd) — 2025-05-13T08:07:02Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
```

  You are right, adding taints might make the node unschedulable and we plan to support it. We just thought of the simplest case for the initial version of this. 
  
  Since there is probably more failure scenarios we will want to cover, I decided to only list the ones in KEP with the plan to expand in the future. Is it ok?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-13T08:10:03Z

- **keps/2724-topology-aware-scheduling/README.md:703** by [@pajakd](https://github.com/pajakd) — 2025-05-13T08:10:02Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
+)
+```
+Alternatively (for Beta release), this information could be stored in a new field in WorkloadStatus.
+
+```golang
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  ...
+  // nodesToReplace lists the names of failed nodes running pods associated 
+  // with this workload. This field is populated by the node failure controller.
+  // +optional
+  // +listType=set
+  NodesToReplace []string `json:"nodesToReplace,omitempty"`
+}
+```
+
```

  Done.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-13T08:50:31Z

- **keps/2724-topology-aware-scheduling/README.md:675** by [@PBundyra](https://github.com/PBundyra) — 2025-05-13T08:50:31Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
```

  Let's add to the beta plans that we will reevaluate other scenarios

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-13T09:18:01Z

- **keps/2724-topology-aware-scheduling/README.md:676** by [@PBundyra](https://github.com/PBundyra) — 2025-05-13T09:18:01Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
```

  It's for running Workloads, not suspended ones. Yes it would bypass the FIFO, and use the mechanism that is [in progress here](https://github.com/kubernetes-sigs/kueue/pull/5052)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-13T09:35:36Z

- **keps/2724-topology-aware-scheduling/README.md:676** by [@PBundyra](https://github.com/PBundyra) — 2025-05-13T09:35:35Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
```

  The way I imagine it working is:
  - nodeFailure controller detects failure and sets annotation
  - workload controller sees that and adds the workload to the queue of workloads that require second pass of the scheduler
  - scheduler corrects the assignment, replacing the failed node if possible and removes the annotation.
  - if correction of the assignment is not possible because of the lack of capacity it, it does nothing. We recommend to users they combine TAS with waitForPodsReady and then it timeout is surpassed, the Workload gets evicted and requeued

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T11:08:34Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T11:08:34Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  So, will you want to deprecate this approach in the future release?
  In that case, couldn't we use the `alpha.kueue.x-k8s.io/node-to-replace` annotation?
  
  cc @mimowo

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T11:09:14Z

- **keps/2724-topology-aware-scheduling/README.md:675** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T11:09:14Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
```

  > Let's add to the beta plans that we will reevaluate other scenarios
  
  That sounds good to me. Could you add a future work section?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T11:14:33Z

- **keps/2724-topology-aware-scheduling/README.md:676** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T11:14:33Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
```

  Thank you for clarifications. So, will the second pass of the scheduler does not execute preemption including reclaiming, right? If yes, could you expand "If no replacement is possible, the workload will be evicted." explanation so that we can clarify not to preempt and reclaim other running workloads.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T11:15:08Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@mimowo](https://github.com/mimowo) — 2025-05-13T11:15:08Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  Yes, most likely we will need to drop the annotation in future releases. 
  
  > In that case, couldn't we use the alpha.kueue.x-k8s.io/node-to-replace annotation?
  
  This is a possibility, I think it is also ok to just say that the annotation is alpha-level and will be removed in future releases.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T11:18:48Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T11:18:48Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  > > because this allows aggressive users to hack this mechanism to bypass FIFO mechanism.
  > 
  > Could you elaborate on how this information could be used?
  
  In the general enqueue / dequeue flow, every workloads will be requeued even though there is a node failure. However, this approach bypasses the mechanism, and quota assignments will be kept.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T11:22:50Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T11:22:50Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  > > In that case, couldn't we use the alpha.kueue.x-k8s.io/node-to-replace annotation?
  > 
  > This is a possibility, I think it is also ok to just say that the annotation is alpha-level and will be removed in future releases.
  
  The removal of standard labels and annotations are not easy, IMO. We need to consider how to handle the workload with removed annotations. Techanically, there are the workload with removed annotations in the cluster after the annotations was removed.
  
  If we have an `alpha` prefix, that's why we stop serving the annotations. If not, we need to consider migration plans. The migration plan requires keeping the annotations code base.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T11:29:42Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@mimowo](https://github.com/mimowo) — 2025-05-13T11:29:42Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  Yes, we want to keep quota, just replace the failed node. This will make the failure recovery much faster. Otherwise 1000 other pods need to be re-scheduled.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-13T11:31:17Z

- **keps/2724-topology-aware-scheduling/README.md:675** by [@pajakd](https://github.com/pajakd) — 2025-05-13T11:31:17Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
```

  Added this to the (long) list of plans for beta.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T11:38:52Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T11:38:52Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  Basically, I agree with this use case. However, I'm trying to mitigate some risks:
  
  1. Can evil batch users technically hack this mechanism and bypass the FIFO mechanism? What if batch users add `kueue.x-k8s.io/node-to-replace` annotation to their Workload?
  2. Do we really need to update and expose this information about Workload objects? Why not just add failure node information to Topology CR?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-13T11:40:17Z

- **keps/2724-topology-aware-scheduling/README.md:695** by [@pajakd](https://github.com/pajakd) — 2025-05-13T11:40:17Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
+)
+```
+Alternatively (for Beta release), this information could be stored in a new field in WorkloadStatus.
+
+```golang
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+  ...
+  // nodesToReplace lists the names of failed nodes running pods associated 
+  // with this workload. This field is populated by the node failure controller.
+  // +optional
+  // +listType=set
+  NodesToReplace []string `json:"nodesToReplace,omitempty"`
+}
+```
+
+
```

  Done.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-13T11:43:18Z

- **keps/2724-topology-aware-scheduling/README.md:676** by [@PBundyra](https://github.com/PBundyra) — 2025-05-13T11:43:18Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
```

  Yes, it wouldn't execute preemption, surely not in this version - might be if we gathered some users' feedback. 
  
  >If yes, could you expand "If no replacement is possible, the workload will be evicted." explanation so that we can clarify not to preempt and reclaim other running workloads.
  
  @pajakd Can you clarify that?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T12:04:28Z

- **keps/2724-topology-aware-scheduling/README.md:675** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T12:04:28Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
```

  Thank you

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T12:06:41Z

- **keps/2724-topology-aware-scheduling/README.md:676** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T12:06:41Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
```

  Thank you. My concern here comment was preemptions. That was resolved.
  
  > Yes, it wouldn't execute preemption, surely not in this version - might be if we gathered some users' feedback.
  
  Basically, I think the preemption w/o requeue is very aggressive. So, if we want to introduce such a mechanism, we need to consider more. However, for now, we can postpone it to the future release.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-13T15:19:30Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@PBundyra](https://github.com/PBundyra) — 2025-05-13T15:19:29Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  > Can evil batch users technically hack this mechanism and bypass the FIFO mechanism? What if batch users add kueue.x-k8s.io/node-to-replace annotation to their Workload?
  
  If an evil user sets the annotation, then we would recompute the assignment but it wouldn't result in more resources for Workloads. There's no possibility a Workload ends up with more resources than it reserved in Kueue
  
  > Do we really need to update and expose this information about Workload objects? Why not just add failure node information to Topology CR?
  
  Could you expand on that? How would you expose information about failed node in Topology CR? What ąre the use-cases/problems we try to solve?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-14T06:31:04Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@pajakd](https://github.com/pajakd) — 2025-05-14T06:31:03Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  Added `alpha` prefix (both in code in #5184 and here in the KEP)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T06:50:49Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@mimowo](https://github.com/mimowo) — 2025-05-14T06:50:49Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

   I think as long as we say the annotation is alpha and part of alpha it is fine to drop it. I remember similar discussion in K8s and using the prefixes was discouraged because then promotion becomes hard. 
  
  k8s guideline allows to break alpha apis without notice even.
  
  However in this particular case the annotation is deprecated day one without a plan to promote, so I'm ok with the prefix

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T06:52:49Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@mimowo](https://github.com/mimowo) — 2025-05-14T06:52:49Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  For example in the core k8s for the feature of back off limit per index we introduced the failure count annotation while the feature was alpha, it never had a prefix

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T08:25:09Z

- **keps/2724-topology-aware-scheduling/README.md:676** by [@mimowo](https://github.com/mimowo) — 2025-05-15T08:25:09Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
```

  I agree, we may need to revisit preemptions. I guess we will learn more when we collect more feedback from users, and based on testing. For now this is an alpha feature, behind a dedicated feature gate. So, I think it is ok.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-19T08:56:44Z

- **keps/2724-topology-aware-scheduling/README.md:334** by [@gabesaba](https://github.com/gabesaba) — 2025-05-19T08:56:44Z
```diff
@@ -323,12 +325,13 @@ transition to the `PodsReady=false`, more details in the
 [KEP PR](https://github.com/kubernetes-sigs/kueue/pull/2737). This mechanism
 will also be helpful for regular workloads.
 
-A more involving approach would be to recompute the TopologyAdmission, however,
-until now we don't modify the workload's admission while the workload is
-scheduled, so it would require extra investigation and effort. We will consider
-this before graduation to GA based on investigation if feasible and feedback
-from users.
-
+We also propose to recompute the TopologyAdmission upon node removal and/or 
+failure, to find matching replacements for missing nodes. If no such 
+replacement exists, the workload has to be evicted and rescheduled again.
+This mechanism requires kueue to keep track of any failed or missing nodes 
+affecting the scheduled TAS workloads. We propose to initially handle only
+a single node failure and extend it to multiple depending on the feedback
+from the users.
```

  Suppose our workload has required topology=rack, it requests all the nodes in the rack, and one of the hosts (nodes) in the rack fails. This will require rescheduling the workload to find a new rack, right? As the scope of this repair is just a single node

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-19T10:00:11Z

- **keps/2724-topology-aware-scheduling/README.md:334** by [@pajakd](https://github.com/pajakd) — 2025-05-19T10:00:11Z
```diff
@@ -323,12 +325,13 @@ transition to the `PodsReady=false`, more details in the
 [KEP PR](https://github.com/kubernetes-sigs/kueue/pull/2737). This mechanism
 will also be helpful for regular workloads.
 
-A more involving approach would be to recompute the TopologyAdmission, however,
-until now we don't modify the workload's admission while the workload is
-scheduled, so it would require extra investigation and effort. We will consider
-this before graduation to GA based on investigation if feasible and feedback
-from users.
-
+We also propose to recompute the TopologyAdmission upon node removal and/or 
+failure, to find matching replacements for missing nodes. If no such 
+replacement exists, the workload has to be evicted and rescheduled again.
+This mechanism requires kueue to keep track of any failed or missing nodes 
+affecting the scheduled TAS workloads. We propose to initially handle only
+a single node failure and extend it to multiple depending on the feedback
+from the users.
```

  Yes. We only want to handle the case where we find a replacement without moving anything else in the topology assignment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T09:19:00Z

- **keps/2724-topology-aware-scheduling/README.md:687** by [@mimowo](https://github.com/mimowo) — 2025-05-20T09:19:00Z
```diff
@@ -666,6 +667,42 @@ for [Beta](#beta). The initial approach for the design is left in the
 [Support for ReplicatedJobs in JobSet](#support-for-replicatedjobs-in-jobset)
 section.
 
+#### Node failures
+
+Initially we plan to support node becoming not ready (as indicated in Node 
+`status.conditions.ready` field) and node being deleted. A new controller will 
+monitor nodes and will update each affected TAS workload with the information 
+about the failed nodes. This information will then be consumed by a new mechanism
+in scheduler where we will try to find a new topology assignment and replace the
+failed node(s) (by changing the assignment only on the affected pods). 
+If no replacement is possible, the workload will be evicted. Initially we plan 
+to only replace in the case of a single node failure. 
+
+We propose to introduce a new Annotation at a Workload level:
+
+```golang
+const (
+	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
+	// name of a failed node running at least one pod of this workload.
+	NodeToReplaceAnnotation = "kueue.x-k8s.io/node-to-replace"
```

  > Can evil batch users technically hack this mechanism and bypass the FIFO mechanism? What if batch users add kueue.x-k8s.io/node-to-replace annotation to their Workload?
  
  Adding the annotation will not result in adding more resource, the scheduler will just replace the node. 
  
  > Do we really need to update and expose this information about Workload objects? Why not just add failure node information to Topology CR?
  
  I don't think this annotation belongs to Topology, because topology is meant to just define the hierarchy. The nodes are actually tracked at the ResourceFlavor level. We could track the information about nodes to replace at the ResourceFlavor status level, or in the visibiltity API, however I would put this as potential future improvement (based on user feedback). 
  
  From the point-of-view of the mechanism I think this information belongs conceptually to workload. Not all workloads will have the TopologyAssignment processed atomically. So, once a workload is processed we can remove the annotation indicating it is "done", and find out which workloads are yet to be processed.
