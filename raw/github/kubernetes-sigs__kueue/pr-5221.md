# PR #5221: tas: Wait for TAS reconciliation before starting the scheduling cycles

**Summary**: tas: Wait for TAS reconciliation before starting the scheduling cycles

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5221

**Last updated**: 2026-01-14T15:39:07Z

---

## Metadata

- **State**: closed
- **Author**: [@GonzaloSaez](https://github.com/GonzaloSaez)
- **Branch**: `g/fix_tas_restart` → `main`
- **Created**: 2025-05-10T13:07:06Z
- **Updated**: 2026-01-14T15:39:07Z
- **Merged**: —
- **Closed**: 2025-05-27T13:55:08Z
- **Labels**: `kind/bug`, `size/L`, `release-note-none`, `needs-rebase`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 7   **+216 / -5**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

When TAS is enabled and the kueue pod restarts, the TAS usage in the cache must be correctly initialized before scheduling and other workload-related reconciliations can happen. Currently, such a synchronization does not happen resulting in TAS workloads being assigned to incorrect nodes after a kueue reboot.

This PR is similar to #5104. However, it seems that in that PR we were trying to access the manager cache before the controller starts which does not work. Hence, we have to synchronize the TAS flavor cache in the background after the controllers are started. This also implies that we have to wait in certain controllers and the scheduler before making any scheduling/reconciliation decision. I also added two integration tests that reproduce the reboot without this fix and that pass with the changes in this PR. I ran the test >50 times in my machine and I did not notice any flakiness.

#### Which issue(s) this PR fixes:

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2025-05-10T13:07:10Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: GonzaloSaez  (c0c6377ce8af837742bb6e07d2febf81395786b7, 617ba752b521f83fb37071ede55ba0495606d551, 1bde8f1194edf4a2a0dbf6c072ddd7bdcd7df98f, 2ba7c73bdc393b4b2ecaa4ef80a5824bf60e605d, 2aad8b42c0115f57c1c5afc3357dd45a0f5affa8)</li></ul><!-- Date Modified: 2025-05-12 13:30:09.684222 -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-10T13:07:12Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2aad8b42c0115f57c1c5afc3357dd45a0f5affa8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6821f7dea03bc400087c623f |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-10T13:07:14Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5221#" title="Author self-approved">GonzaloSaez</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-10T13:07:16Z

Hi @GonzaloSaez. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-12T07:39:46Z

Please sign the CLA

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-12T08:47:50Z

>  That said, I think that there's always potential for the controllers and the scheduler racing against each other during boot time. 

This is my understanding, and I remember some discussions under https://github.com/kubernetes-sigs/kueue/issues/3984 indicating the issue exists. However, I agree the issue is more server for TAS if it is not able to "recover" after a while. 

So, I would like to understand if the issue for TAS is:
1. "temporary", and after a while the workloads are accounted properly (as for regular scheduling)
2. "permanent", after startup the workloads continue to be not accounted for

> I'm not very familiar with the kueue HA mode. My understanding is that non-leader elected pods are just idling and they do not run the reconciling cycles. Hence, when they take the leadership they may end up having the same issues at boot time.

The idle replicas are not doing reconciling, but they keep maintaining the cache based on the Create/Update/Delete event handlers. This way they are ready to take over without waiting when the "main replica" fails.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-12T08:57:09Z

> That said, I think that there's always potential for the controllers and the scheduler racing against each other during boot time. Maybe the best solution is to stall scheduling decisions and workload creation for some time during boot time and to make the hierarchy of controllers boot in certain order. 

We may also consider how kube-scheduler deals with that. I'm not an expert myself on kube-scheduler, but I see this code: https://github.com/kubernetes/kubernetes/blob/627ce4946ccd6b67bc18f5a4dda92cf6c583e9b7/cmd/kube-scheduler/app/server.go#L263-L274

which suggests to me we could follow this model, and it would maybe work both for regular quota & TAS.

cc @tenzen-y @sanposhiho  can you maybe confirm if kube-scheduler awaits for synchronization of the cache (based on the Create / Update / Delete) events before scheduling?

### Comment by [@sanposhiho](https://github.com/sanposhiho) — 2025-05-12T09:24:38Z

> cc @tenzen-y @sanposhiho can you maybe confirm if kube-scheduler awaits for synchronization of the cache (based on the Create / Update / Delete) events before scheduling?

Yes, at startup, kube-scheduler waits for all events to be handled and then starts scheduling.
https://github.com/kubernetes/kubernetes/blob/627ce4946ccd6b67bc18f5a4dda92cf6c583e9b7/cmd/kube-scheduler/app/server.go#L269

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-12T11:55:24Z

> Yes, at startup, kube-scheduler waits for all events to be handled and then starts scheduling.

@GonzaloSaez could you check if this approach would work in Kueue (waiting for something like WaitForHandlersSync)? It seems less code and more consistent with the battle tested approach. It would also fix the non-TAS case.

### Comment by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T12:34:37Z

>@GonzaloSaez could you check if this approach would work in Kueue (waiting for something like WaitForHandlersSync)? It seems less code and more consistent with the battle tested approach. It would also fix the non-TAS case.

Sure I'll do that. However, that requires changing all the `managerSetup` and co. since we have to start the scheduler in a separate thread. Another option is to change the (public) interface of `scheduler.Start` or `scheduler.New()` to pass in `mgr` so that we can call `WaitForCacheSync` in a different thread. What would you prefer in this case? Thanks!

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-12T13:26:34Z

> > cc @tenzen-y @sanposhiho can you maybe confirm if kube-scheduler awaits for synchronization of the cache (based on the Create / Update / Delete) events before scheduling?
> 
> Yes, at startup, kube-scheduler waits for all events to be handled and then starts scheduling. https://github.com/kubernetes/kubernetes/blob/627ce4946ccd6b67bc18f5a4dda92cf6c583e9b7/cmd/kube-scheduler/app/server.go#L269

Correct. The difference is kube-scheder uses client-go, directly. However, kueue uses the controller-runtime. So, I'm not sure if we can check the handler synced state.

### Comment by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T14:28:16Z

In theory, `WaitForCacheSync` should wait for all the informers to sync

```
// WaitForCacheSync waits until all the caches have been started and synced.
func (ip *Informers) WaitForCacheSync(ctx context.Context) bool {
	if !ip.waitForStarted(ctx) {
		return false
	}
	return cache.WaitForCacheSync(ctx.Done(), ip.getHasSyncedFuncs()...)
}
```

However, the doc states that it's not enough to call `HasSynced` on the informer.

```
        // HasSynced returns true if the shared informer's store has been
	// informed by at least one full LIST of the authoritative state
	// of the informer's object collection.  This is unrelated to "resync".
	//
	// Note that this doesn't tell you if an individual handler is synced!!
	// For that, please call HasSynced on the handle returned by
	// AddEventHandler.
	HasSynced() bool
```

I'll keep looking into a way to wait for the handlers to be synced

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-12T16:17:33Z

/ok-to-test

### Comment by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-13T06:28:13Z

It seems that controller-runtime does not store the func returned by AddEventHandler so we cannot run `HasSynced` as in the scheduler implementation.

https://github.com/kubernetes-sigs/controller-runtime/blob/bd600cfa7aa35858a53ad9a93540b3cfabdde32f/pkg/internal/source/kind.go#L94
https://github.com/kubernetes-sigs/controller-runtime/blob/bd600cfa7aa35858a53ad9a93540b3cfabdde32f/pkg/source/source.go#L289

I think that the code I had before in `scheduler.go` kind of ensures something similar by making sure that the `workloads` inside kueue's cache are the same as the ones in the k8s client cache. Let me know if I should revert to using that approach.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-13T07:06:20Z

I have not yet decided on any particular approach. Ideally, we would wait for syncing all handlers, this would also solve the issue outside of TAS (I agree less severe).

I'm wondering about `multi_namespace_cache.go` in controller-runtime. Is this something we could use? It seems to maintain the [map of all handlers]( https://github.com/kubernetes-sigs/controller-runtime/blob/bd600cfa7aa35858a53ad9a93540b3cfabdde32f/pkg/cache/multi_namespace_cache.go#L333-L364) and has a helper to check their status. It would be helpful to run integration tests with debugger and vs-code and check what happens underneath.

cc @sbueringer @alvaroaleman, is there any way to await on startup for all handlers to sync?

### Comment by [@sbueringer](https://github.com/sbueringer) — 2025-05-13T08:36:15Z

> @sbueringer @alvaroaleman, is there any way to await on startup for all handlers to sync?

Sorry didn't have time to take a detailed look, just a quick response that might help

Could it be that the underlying assumption is wrong?

As far as I know the cached_reader does block Get/List calls until an informer has synced: https://github.com/kubernetes-sigs/controller-runtime/blob/eef257b1f1788c7a04c8b3b4f6336aa2be0089c2/pkg/cache/internal/informers.go#L317

(not sure if that helps in your case)

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-13T08:46:18Z

Thank you @sbueringer , by "synced" it means that all the objects are already in the informer, or also all the ADDED events are already processed by the registered handlers? We need to make sure in Kueue that all the ADDED events are already processed.

### Comment by [@sbueringer](https://github.com/sbueringer) — 2025-05-13T08:54:44Z

It means that this func returned true: https://github.com/kubernetes/kubernetes/blob/a1809ffae377f3abbae12b137073ca4c473743cd/staging/src/k8s.io/client-go/tools/cache/shared_informer.go#L172-L179

To the best of my knowledge that means the initial list was done

```go
	// HasSynced returns true if the shared informer's store has been
	// informed by at least one full LIST of the authoritative state
	// of the informer's object collection.  This is unrelated to "resync".
	//
	// Note that this doesn't tell you if an individual handler is synced!!
	// For that, please call HasSynced on the handle returned by
	// AddEventHandler.
```

>  or also all the ADDED events are already processed by the registered handlers? We need to make sure in Kueue that all the ADDED events are already processed.

Sounds like that is not the case

### Comment by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-13T14:24:15Z

I've traced down a bit what `WaitForCacheSync` guarantees by printing some stacktraces: 

- `controller-runtime/pkg/cache/internal/informers.go` -> `WaitForCacheSync` -> `return cache.WaitForCacheSync(ctx.Done(), ip.getHasSyncedFuncs()...)`
- `client-go/tools/cache/shared_informer.go` -> `WaitForCacheSync` using `InformerSynced` that comes from `SharedInformer`.
- `client-go/tools/cache/controller.go`-> `HasSynced` -> `return c.config.Queue.HasSynced()`
- `client.go/tools/cache/delta_fifo.go` -> `HasSynced` -> `return f.populated && f.initialPopulationCount == 0`.
- `initialPopulationCount` is decremented with a lock in `Pop` and the lock is not released until `process(...)` is called on the  element of the queue.  Also, `initialPopulationCount` seems to be initialized with a lock when calling `Replace` at the same time `populated` is set to true.
- `process` is calling the respective event handler.

Looking at this stacktrace I think it's safe to say that calling `WaitForCachySync` guarantees that all event handlers are synced but I'd appreciate if someone with more experience in controller-runtime could confirm this. Thanks!

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-13T14:27:56Z

> Looking at this stacktrace I think it's safe to say that calling WaitForCachySync guarantees that all event handlers are synced but I'd appreciate if someone with more experience in controller-runtime could confirm this. Thanks!

If this is the case, then the implementation in Kueue would be fairly simple - add `WaitForCachySync` on startup. Maybe you can give it a try while waiting for confirmation, running the integration test in a loop, say even 500 times. It would be a strong experimental case to support this hypothesis.

### Comment by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-13T14:32:33Z

>If this is the case, then the implementation in Kueue would be fairly simple - add WaitForCachySync on startup. Maybe you can give it a try while waiting for confirmation, running the integration test in a loop, say even 500 times. It would be a strong experimental case to support this hypothesis.

Sure I'll do that soon. That said, this doesn't remove the need of waiting for the TAS cache in the ClusterQueue and Workload controllers before reconciling/creating the objects. I think the only solution to avoid the need of waiting for the TAS cache is to boot and sync the TAS cache before starting the CQ and Workload controllers

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-13T14:35:39Z

> That said, this doesn't remove the need of waiting for the TAS cache in the ClusterQueue and Workload controllers before reconciling/creating the objects. 

This is actually surprising to me, I was expecting the tasCache would be fully initialized while processing the handler events on startup. Can you elaborate why this is not enough?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-13T14:37:19Z

Ahh, because we don't have a guarantee that the events coming for Topology ADDED or RF ADDED will come before the workload ADDED events. So, we need to retry processing of the workload ADDED events.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-14T13:31:36Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-16T07:23:12Z

/assign 
I will try to book today time for investigating the direction of the solution. @GonzaloSaez do you know why the integration tests fail currently?

### Comment by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-16T08:35:23Z

@mimowo I have been investigating in the past days more about the syncing of cache and handlers and it seems that WaitForSyncCache is not enough for the scheduler to boot. I think that there's no good way to ensure that the handlers are synced from within the controller-runtime lib. Maybe we can raise an issue on their end to expose a way from the manager to wait for the handlers to sync but that will take time and it will require kueue to update to the latest version which will also take time.

Regarding the tests failing, it's because some unit/integration tests may enable the the TAS feature after the controllers boot. In that case, since we moved the logic to wait for the TAS cache sync to the `cache` constructor, the TAS cache will not sync. I think we should move it back to the TAS controller initialization logic as it was before.

I'll keep looking a bit more on how to improve the situation over the weekend.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-16T10:51:22Z

Thank you for the summary, it will be helpful to guide also my investigation.

>  it's because some unit/integration tests may enable the the TAS feature after the controllers boot

It is fair to restructure the tests to make sure the feature gate is set before the initialization code, because this would reflect the production use. Every change of the feature gate requires restart of the full binary. Maybe we could set the feature in BeforeAll rather than BeforeEach. I think we should not change the production code just because of how the tests are written. It is fair to change the implementation if we truly believe the other approach is better, but this isn't yet clear to me. It is on my todo to investigate.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-19T07:03:51Z

cc @GonzaloSaez I'm also starting to look into this issue with the initial PR: https://github.com/kubernetes-sigs/kueue/pull/5276. I know it still has gaps, but the main idea is to delay scheduling as long as the CQ is inactive (active=false). IIUC this should be already done here: https://github.com/kubernetes-sigs/kueue/blob/3df4ea2744edf1ee4891d7ce1527f54c2d2df960/pkg/scheduler/scheduler.go#L359C18-L359C42

The other piece is to make sure the CQ is considered inactive as long as there are still some workloads not accounted. 

Then, when tasCache gets populated, re-account for all Workloads.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-20T12:57:33Z

I think I've got it fixed, ptal: https://github.com/kubernetes-sigs/kueue/pull/5276

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-27T13:43:25Z

@GonzaloSaez: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-integration-baseline-main | 2aad8b42c0115f57c1c5afc3357dd45a0f5affa8 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/5221/pull-kueue-test-integration-baseline-main/1921962986600140800) | true | `/test pull-kueue-test-integration-baseline-main`
pull-kueue-test-integration-extended-main | 2aad8b42c0115f57c1c5afc3357dd45a0f5affa8 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/5221/pull-kueue-test-integration-extended-main/1921962986671443968) | true | `/test pull-kueue-test-integration-extended-main`
pull-kueue-verify-main | 2aad8b42c0115f57c1c5afc3357dd45a0f5affa8 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/5221/pull-kueue-verify-main/1921962989007671296) | true | `/test pull-kueue-verify-main`
pull-kueue-test-e2e-main-1-33 | 2aad8b42c0115f57c1c5afc3357dd45a0f5affa8 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/5221/pull-kueue-test-e2e-main-1-33/1927359931204243456) | true | `/test pull-kueue-test-e2e-main-1-33`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=5221). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3AGonzaloSaez). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-27T13:55:04Z

/close
the issue is already fixed by https://github.com/kubernetes-sigs/kueue/pull/5276

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-27T13:55:09Z

@mimowo: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5221#issuecomment-2912610705):

>/close
>the issue is already fixed by https://github.com/kubernetes-sigs/kueue/pull/5276


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alvaroaleman](https://github.com/alvaroaleman) — 2026-01-14T15:39:07Z

> cc @sbueringer @alvaroaleman, is there any way to await on startup for all handlers to sync?

@mimowo please just open a controller-runtime issue the next time. Random pings like this tend to get lost in the flood of GitHub notifications and even if they don't, there is a tendency to ignore pings for issues in third-party projects I am not involved with.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-12T05:45:32Z

Thanks for the PR 👍 
Please try to eliminate any hardcoded delays from the code. 

Aside from the comments to code, let me also ask some exploratory questions to better understand the impact and the required changes:
1. is this also a problem outside of TAS? 
2. if 1 is true, then is it more serious for TAS becuase the cache never gets populated whilst for regular workloads it gets populated with a delay?
3. Can this problem be mitigated by running Kueue in HA mode (multiple replicas)?

- **test/integration/singlecluster/tas/tas_test.go:538** by [@mimowo](https://github.com/mimowo) — 2025-05-12T05:34:17Z
```diff
@@ -532,6 +534,47 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 						},
 					))
 				})
+
+				ginkgo.By("creating wl5, wl6, wl7 before restart", func() {
```

  Please move this scenario to a dedicated test (ginkgo.It)

- **pkg/scheduler/scheduler.go:221** by [@mimowo](https://github.com/mimowo) — 2025-05-12T05:35:38Z
```diff
@@ -173,11 +176,80 @@ func reportSkippedPreemptions(p map[kueue.ClusterQueueReference]int) {
 	}
 }
 
+func (s *Scheduler) waitForTas(ctx context.Context, log logr.Logger) wait.SpeedSignal {
+	for !s.cache.TASCache().SyncedFlavors() {
+		log.V(2).Info("Waiting for the TAS cache to be synced")
+		time.Sleep(1 * time.Second)
+	}
+
+	// All the listed workloads that have reservation should have been added to the cache to ensure
+	// the TAS usage is as correct as possible.
+	var wlsList kueue.WorkloadList
+	err := s.client.List(ctx, &wlsList)
+	if err != nil {
+		return wait.SlowDown
+	}
+	wlsIn := s.cache.GetWorkloads()
+	wls := sets.Set[string]{}
+	for _, wl := range wlsIn {
+		key := fmt.Sprintf("%s/%s", wl.Obj.Namespace, wl.Obj.Name)
+		wls.Insert(key)
+	}
+	wlsListSet := sets.Set[string]{}
+	for _, wl := range wlsList.Items {
+		wlsListSet.Insert(wl.Name)
+	}
+	for _, wl := range wlsList.Items {
+		if !workload.HasQuotaReservation(&wl) || workload.IsFinished(&wl) {
+			continue
+		}
+		key := fmt.Sprintf("%s/%s", wl.Namespace, wl.Name)
+		if !wls.Has(key) {
+			log.V(2).Info("Workload has not received the topology update", "workload", wl)
+			return wait.SlowDown
+		}
+	}
+	log.V(2).Info("TAS cache is synced, proceeding with scheduling")
+	return wait.KeepGoing
+}
+
 func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 	s.schedulingCycle++
 	log := ctrl.LoggerFrom(ctx).WithValues("schedulingCycle", s.schedulingCycle)
 	ctx = ctrl.LoggerInto(ctx, log)
 
+	if ctx.Err() != nil {
```

  Let's move all the waiting-related code to a dedicated function to offload the main routine. Also, this will give us the ability to unit-test it.

- **pkg/controller/tas/controllers.go:50** by [@mimowo](https://github.com/mimowo) — 2025-05-12T05:36:30Z
```diff
@@ -38,5 +43,57 @@ func SetupControllers(mgr ctrl.Manager, queues *queue.Manager, cache *cache.Cach
 	if ctrlName, err := topologyUngater.setupWithManager(mgr, cfg); err != nil {
 		return ctrlName, err
 	}
+
+	// Set up the TAS flavor cache in the background. This is because mgr is configured to list from a cache
+	// and the cache is not populated until the controller is started.
+	go func() {
+		err := syncTasCache(mgr, cache)
```

  I'm not clear why we need this code if we are already waiting inside scheduler.go?

- **pkg/controller/core/clusterqueue_controller.go:182** by [@mimowo](https://github.com/mimowo) — 2025-05-12T05:39:24Z
```diff
@@ -177,6 +177,13 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile ClusterQueue")
 
+	if features.Enabled(features.TopologyAwareScheduling) && !r.cache.TASCache().SyncedFlavors() {
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		return ctrl.Result{
```

  How long in takes in practice? I think it is fair to just return error to avoid hard-coding such delays.

- **pkg/controller/core/clusterqueue_controller.go:332** by [@mimowo](https://github.com/mimowo) — 2025-05-12T05:40:37Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  We already wait for this in workload_controller in Reconcile. I'm not clear if we need in both places.

- **pkg/controller/core/workload_controller.go:166** by [@mimowo](https://github.com/mimowo) — 2025-05-12T05:40:52Z
```diff
@@ -159,6 +159,15 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		return ctrl.Result{}, nil
 	}
 
+	if features.Enabled(features.TopologyAwareScheduling) {
+		if !r.cache.TASCache().SyncedFlavors() {
+			log.V(2).Info("Waiting for TAS cache to be synced")
+			return ctrl.Result{
+				RequeueAfter: 1 * time.Second,
```

  I think it is fair to just return error, it will get retried by controller-runtime

- **test/integration/singlecluster/tas/tas_test.go:569** by [@mimowo](https://github.com/mimowo) — 2025-05-12T05:44:25Z
```diff
@@ -532,6 +534,47 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 						},
 					))
 				})
+
+				ginkgo.By("creating wl5, wl6, wl7 before restart", func() {
+					wl5 = testing.MakeWorkload("wl5", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl5.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					wl6 = testing.MakeWorkload("wl6", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl6.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					wl7 = testing.MakeWorkload("wl7", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl7.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					util.MustCreate(ctx, k8sClient, wl5)
+					util.MustCreate(ctx, k8sClient, wl6)
+					util.MustCreate(ctx, k8sClient, wl7)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl5)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl6)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl7)
+				})
+
+				ginkgo.By("inadmissible wl5,wl6,wl7", func() {
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl5, wl6, wl7)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+				})
+
+				ginkgo.By("restart", func() {
+					fwk.StopManager(ctx)
+					time.Sleep(time.Second * 2)
```

  Avoid using hard coded delays. They don't seem necessary, neither here nor below.

### Review by [@GonzaloSaez](https://github.com/GonzaloSaez) (commented) — 2025-05-12T07:05:17Z

>Aside from the comments to code, let me also ask some exploratory questions to better understand the impact and the required changes:
>
>1. is this also a problem outside of TAS?
>2. if 1 is true, then is it more serious for TAS becuase the cache never gets populated whilst for regular workloads it gets populated with a delay?
>3. Can this problem be mitigated by running Kueue in HA mode (multiple replicas)?

1. I don't think this is a problem outside TAS because it seems that the root cause comes from the lack of TAS usage accounting during boot time because we were not waiting for the TAS controller to sync with the RF. That said, the only way to be sure about this is to add stops and starts in the e2e and integration tests. Adding some random fuzziness to those tests could be useful but it's probably beyond the scope of this PR. I can add the stop and start logic to other places within this PR though.
2. I think that it's less of a problem for non-TAS workloads because the CQ workload utilization is correctly accounted for during boot time. That said, I think that there's always potential for the controllers and the scheduler racing against each other during boot time. Maybe the best solution is to stall scheduling decisions and workload creation for some time during boot time and to make the hierarchy of controllers boot in certain order. The latter would be particular useful to simplify the boot process since currently everything is a bit tangled together.
3. I'm not very familiar with the kueue HA mode. My understanding is that non-leader elected pods are just idling and they do not run the reconciling cycles. Hence, when they take the leadership they may end up having the same issues at boot time.

- **pkg/controller/core/clusterqueue_controller.go:182** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T06:47:02Z
```diff
@@ -177,6 +177,13 @@ func (r *ClusterQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request
 	log := ctrl.LoggerFrom(ctx)
 	log.V(2).Info("Reconcile ClusterQueue")
 
+	if features.Enabled(features.TopologyAwareScheduling) && !r.cache.TASCache().SyncedFlavors() {
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		return ctrl.Result{
```

  It depends on how long it takes the TAS controller to sync the RF. I'll just return an error and let the controller-runtime figure out the retries as suggested

- **pkg/controller/core/clusterqueue_controller.go:332** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T06:50:08Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  The problem is that `AddClusterQueue` will call the following (note the usage of `client.List`, i.e. independent on whether the workloads have been reconciled).
  
  ```
          var workloads kueue.WorkloadList
  	if err := c.client.List(ctx, &workloads, client.MatchingFields{utilindexer.WorkloadClusterQueueKey: cq.Name}); err != nil {
  		return fmt.Errorf("listing workloads that match the queue: %w", err)
  	}
  	for i, w := range workloads.Items {
  		if !workload.HasQuotaReservation(&w) || workload.IsFinished(&w) {
  			continue
  		}
  		c.addOrUpdateWorkload(&workloads.Items[i])
  	}
  ```
  
  which will then call 
  
  ```
  if features.Enabled(features.TopologyAwareScheduling) && wi.IsUsingTAS() {
  		for tasFlavor, tasUsage := range wi.TASUsage() {
  			if tasFlvCache := c.tasFlavorCache(tasFlavor); tasFlvCache != nil {
  				if m == 1 {
  					tasFlvCache.addUsage(tasUsage)
  				}
  				if m == -1 {
  					tasFlvCache.removeUsage(tasUsage)
  				}
  			}
  		}
  	}
  ```
  
  If the TAS RF are not synced, the TAS usage will not be correctly updated.

- **pkg/controller/tas/controllers.go:50** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T06:51:26Z
```diff
@@ -38,5 +43,57 @@ func SetupControllers(mgr ctrl.Manager, queues *queue.Manager, cache *cache.Cach
 	if ctrlName, err := topologyUngater.setupWithManager(mgr, cfg); err != nil {
 		return ctrlName, err
 	}
+
+	// Set up the TAS flavor cache in the background. This is because mgr is configured to list from a cache
+	// and the cache is not populated until the controller is started.
+	go func() {
+		err := syncTasCache(mgr, cache)
```

  This is the code in charge of signaling that the TAS RF cache is up-to-date. Without this we'd have to check in each controller whether it's in sync using something similar to `syncTasCache`. I can move this to `cache` if you prefer. Open to suggestions here.

- **pkg/scheduler/scheduler.go:221** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T06:51:37Z
```diff
@@ -173,11 +176,80 @@ func reportSkippedPreemptions(p map[kueue.ClusterQueueReference]int) {
 	}
 }
 
+func (s *Scheduler) waitForTas(ctx context.Context, log logr.Logger) wait.SpeedSignal {
+	for !s.cache.TASCache().SyncedFlavors() {
+		log.V(2).Info("Waiting for the TAS cache to be synced")
+		time.Sleep(1 * time.Second)
+	}
+
+	// All the listed workloads that have reservation should have been added to the cache to ensure
+	// the TAS usage is as correct as possible.
+	var wlsList kueue.WorkloadList
+	err := s.client.List(ctx, &wlsList)
+	if err != nil {
+		return wait.SlowDown
+	}
+	wlsIn := s.cache.GetWorkloads()
+	wls := sets.Set[string]{}
+	for _, wl := range wlsIn {
+		key := fmt.Sprintf("%s/%s", wl.Obj.Namespace, wl.Obj.Name)
+		wls.Insert(key)
+	}
+	wlsListSet := sets.Set[string]{}
+	for _, wl := range wlsList.Items {
+		wlsListSet.Insert(wl.Name)
+	}
+	for _, wl := range wlsList.Items {
+		if !workload.HasQuotaReservation(&wl) || workload.IsFinished(&wl) {
+			continue
+		}
+		key := fmt.Sprintf("%s/%s", wl.Namespace, wl.Name)
+		if !wls.Has(key) {
+			log.V(2).Info("Workload has not received the topology update", "workload", wl)
+			return wait.SlowDown
+		}
+	}
+	log.V(2).Info("TAS cache is synced, proceeding with scheduling")
+	return wait.KeepGoing
+}
+
 func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 	s.schedulingCycle++
 	log := ctrl.LoggerFrom(ctx).WithValues("schedulingCycle", s.schedulingCycle)
 	ctx = ctrl.LoggerInto(ctx, log)
 
+	if ctx.Err() != nil {
```

  Will do

- **test/integration/singlecluster/tas/tas_test.go:538** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T06:51:45Z
```diff
@@ -532,6 +534,47 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 						},
 					))
 				})
+
+				ginkgo.By("creating wl5, wl6, wl7 before restart", func() {
```

  Will do

- **test/integration/singlecluster/tas/tas_test.go:569** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T06:53:59Z
```diff
@@ -532,6 +534,47 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 						},
 					))
 				})
+
+				ginkgo.By("creating wl5, wl6, wl7 before restart", func() {
+					wl5 = testing.MakeWorkload("wl5", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl5.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					wl6 = testing.MakeWorkload("wl6", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl6.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					wl7 = testing.MakeWorkload("wl7", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl7.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					util.MustCreate(ctx, k8sClient, wl5)
+					util.MustCreate(ctx, k8sClient, wl6)
+					util.MustCreate(ctx, k8sClient, wl7)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl5)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl6)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl7)
+				})
+
+				ginkgo.By("inadmissible wl5,wl6,wl7", func() {
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl5, wl6, wl7)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+				})
+
+				ginkgo.By("restart", func() {
+					fwk.StopManager(ctx)
+					time.Sleep(time.Second * 2)
```

  We need to make sure that the controllers have certainly stopped before starting (this is what the k8s deployment controller would do for us). We also have to make sure that the controllers have ran for a certain amount of time before checking the workload admission. The longer we wait the better to make the tests more robust (i.e. less chances of the workload admission remaining the same due to the controllers not having fully booted up) but at the same time the tests will take longer.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-12T07:34:15Z

- **pkg/controller/core/clusterqueue_controller.go:332** by [@mimowo](https://github.com/mimowo) — 2025-05-12T07:34:15Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  Right, so we need to make sure that `c.addOrUpdateWorkload` is called after the TAS cache is initialized. My point is that the workload already calls `AddOrUpdateWorkload`, so I would assume delaying the call in one place should be enough. 
  
  I'm not 100% sure, but please give it an experiment, if dropping it from here still reproduces the issue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-12T07:38:08Z

- **test/integration/singlecluster/tas/tas_test.go:569** by [@mimowo](https://github.com/mimowo) — 2025-05-12T07:38:08Z
```diff
@@ -532,6 +534,47 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 						},
 					))
 				})
+
+				ginkgo.By("creating wl5, wl6, wl7 before restart", func() {
+					wl5 = testing.MakeWorkload("wl5", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl5.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					wl6 = testing.MakeWorkload("wl6", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl6.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					wl7 = testing.MakeWorkload("wl7", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl7.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					util.MustCreate(ctx, k8sClient, wl5)
+					util.MustCreate(ctx, k8sClient, wl6)
+					util.MustCreate(ctx, k8sClient, wl7)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl5)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl6)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl7)
+				})
+
+				ginkgo.By("inadmissible wl5,wl6,wl7", func() {
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl5, wl6, wl7)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+				})
+
+				ginkgo.By("restart", func() {
+					fwk.StopManager(ctx)
+					time.Sleep(time.Second * 2)
```

  > We need to make sure that the controllers have certainly stopped before starting (this is what the k8s deployment controller would do for us). 
  
  In principle yes, but hard coding `2s` does not make it "certain". Also, I would be pragmatic about the test - if the test reproduces the issues without waiting (which most likely it will), then why to wait the 2s on every build.
  
  > We also have to make sure that the controllers have ran for a certain amount of time before checking the workload admission. 
  
  I see, here I would also be pragmatic, choose the smallest sleep that reproduces the issue "almost always" on your local machine.
  
  > The longer we wait the better to make the tests more robust
  
  right, but we need to balance that with paying the price of long tests for the future. So I prefer to minimize the waiting times.

### Review by [@GonzaloSaez](https://github.com/GonzaloSaez) (commented) — 2025-05-12T08:11:06Z

- **pkg/controller/core/clusterqueue_controller.go:332** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T07:55:20Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  The workload controller calls `AddOrUpdateWorkload` but it usually fails because the CQ is not yet in the cache
  
  ```
  func (c *Cache) addOrUpdateWorkload(w *kueue.Workload) bool {
  	if !workload.HasQuotaReservation(w) {
  		return false
  	}
  
  	clusterQueue := c.hm.ClusterQueue(w.Status.Admission.ClusterQueue)
  	if clusterQueue == nil {
  		return false
  	}
  ```
  
  That's why I had to add these wait cycles in the CQ create action

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-12T08:20:11Z

- **pkg/controller/core/clusterqueue_controller.go:332** by [@mimowo](https://github.com/mimowo) — 2025-05-12T08:20:11Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  Ok, but if CQ does not exist then workload controller would fail and retry soon after with exponential backoff. So, iiuc this new code in cq is not needed for correctess?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-12T08:30:30Z

- **pkg/controller/tas/controllers.go:61** by [@mimowo](https://github.com/mimowo) — 2025-05-12T08:30:29Z
```diff
@@ -38,5 +43,57 @@ func SetupControllers(mgr ctrl.Manager, queues *queue.Manager, cache *cache.Cach
 	if ctrlName, err := topologyUngater.setupWithManager(mgr, cfg); err != nil {
 		return ctrlName, err
 	}
+
+	// Set up the TAS flavor cache in the background. This is because mgr is configured to list from a cache
+	// and the cache is not populated until the controller is started.
+	go func() {
+		err := syncTasCache(mgr, cache)
+		if err != nil {
+			panic(fmt.Sprintf("failed to populate TAS cache: %v", err))
+		}
+	}()
+
 	return "", nil
 }
+
+func syncTasCache(mgr ctrl.Manager, cCache *cache.Cache) error {
+	// Wait for the cache to be synced before listing resources. Else it will fail.
+	ctx, cancel := context.WithTimeout(context.Background(), 60*time.Second)
```

  Instead of creating a custom context here, use context of the invocation

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-12T08:32:52Z

- **pkg/controller/tas/controllers.go:50** by [@mimowo](https://github.com/mimowo) — 2025-05-12T08:32:52Z
```diff
@@ -38,5 +43,57 @@ func SetupControllers(mgr ctrl.Manager, queues *queue.Manager, cache *cache.Cach
 	if ctrlName, err := topologyUngater.setupWithManager(mgr, cfg); err != nil {
 		return ctrlName, err
 	}
+
+	// Set up the TAS flavor cache in the background. This is because mgr is configured to list from a cache
+	// and the cache is not populated until the controller is started.
+	go func() {
+		err := syncTasCache(mgr, cache)
```

  I would probably prefer to have a function like `WaitForTASCache` exposed too which awaits for the caches, by analogy to `mgr.GetCache().WaitForCacheSync(ctx)`. I think it makes sense to create the helper inside the cache.

### Review by [@GonzaloSaez](https://github.com/GonzaloSaez) (commented) — 2025-05-12T09:30:27Z

>"permanent", after startup the workloads continue to be not accounted for

From my observations in my k8s clusters the errors are permanent until the workloads that were present before the reboot are done. This is because `AddOrUpdateWorkflow` does not seem to update the TAS usage after a reboot if we don't wait in the CQ controller for the TAS cache to sync.

>The idle replicas are not doing reconciling, but they keep maintaining the cache based on the Create/Update/Delete event handlers. This way they are ready to take over without waiting when the "main replica" fails.

Then maybe HA mode can deal with this better but there's always the chance of the TAS cache to be out of sync if we are in a crash loop or something like that. It would also force us to use PDB and such and be extremely careful about node upgrades. Besides, I'm not sure if it's easy to test whether HA mode can deal better with this TAS scheduling issues.

- **pkg/controller/core/clusterqueue_controller.go:332** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T09:14:07Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  The problem is that we are calling `AddOrUpdateWorkload` inside the `Create` event so if it fails we don't retry we just keep going. Then in `Reconcile` it seems that we are not accounting for that failure and we may not be calling `AddOrUpdateWorkflow` correctly, so some scheduling decisions are done without the correct TAS usage. That's why we have to block both CQ creation and Workload reconciliation. I'm new to Kueue but it seems that all of this is a bit tangled. It may make more sense to boot the controllers in certain order?

- **pkg/controller/tas/controllers.go:50** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T09:19:45Z
```diff
@@ -38,5 +43,57 @@ func SetupControllers(mgr ctrl.Manager, queues *queue.Manager, cache *cache.Cach
 	if ctrlName, err := topologyUngater.setupWithManager(mgr, cfg); err != nil {
 		return ctrlName, err
 	}
+
+	// Set up the TAS flavor cache in the background. This is because mgr is configured to list from a cache
+	// and the cache is not populated until the controller is started.
+	go func() {
+		err := syncTasCache(mgr, cache)
```

  Done. I'm not running any deduplication on the calls to it though and the context may be wrong since Create events don't have context.

- **test/integration/singlecluster/tas/tas_test.go:569** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-12T09:20:32Z
```diff
@@ -532,6 +534,47 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 						},
 					))
 				})
+
+				ginkgo.By("creating wl5, wl6, wl7 before restart", func() {
+					wl5 = testing.MakeWorkload("wl5", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl5.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					wl6 = testing.MakeWorkload("wl6", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl6.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					wl7 = testing.MakeWorkload("wl7", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					wl7.Spec.PodSets[0].TopologyRequest = &kueue.PodSetTopologyRequest{
+						Required: ptr.To(testing.DefaultRackTopologyLevel),
+					}
+					util.MustCreate(ctx, k8sClient, wl5)
+					util.MustCreate(ctx, k8sClient, wl6)
+					util.MustCreate(ctx, k8sClient, wl7)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl5)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl6)
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl7)
+				})
+
+				ginkgo.By("inadmissible wl5,wl6,wl7", func() {
+					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl5, wl6, wl7)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl1, wl2, wl4)
+				})
+
+				ginkgo.By("restart", func() {
+					fwk.StopManager(ctx)
+					time.Sleep(time.Second * 2)
```

  I changed this to create a a CQ and a workload after the reboot. This ensures that the controllers are up and I'm able to reproduce the TAS issues before this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T14:44:59Z

- **pkg/controller/core/clusterqueue_controller.go:332** by [@mimowo](https://github.com/mimowo) — 2025-05-13T14:44:59Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  > It may make more sense to boot the controllers in certain order?
  
  I think the order of starting the controllers is under controller-runtime. You may check if some control is available, but I think it would be not typical for a distributed system.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T14:55:11Z

- **pkg/controller/core/clusterqueue_controller.go:332** by [@mimowo](https://github.com/mimowo) — 2025-05-13T14:55:11Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  > The problem is that we are calling AddOrUpdateWorkload inside the Create event so if it fails we don't retry we just keep going. Then in Reconcile it seems that we are not accounting for that failure and we may not be calling AddOrUpdateWorkflow correctly
  
  I see, I think generally even handlers should be very fast. Sleeping in event handlers in anti pattern. If we need to retry we should scheduler Reconcile(), and we could retry Reconcile just by returning error. Then, controller runtime will retry with exponential backoff as you did in https://github.com/kubernetes-sigs/kueue/pull/5221/files#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffR162-R165. This may require a bit of refactoring to move the AddXYZ functions into the Reconcile, but I think it is time well spent.

### Review by [@GonzaloSaez](https://github.com/GonzaloSaez) (commented) — 2025-05-13T17:50:46Z

- **pkg/controller/core/clusterqueue_controller.go:332** by [@GonzaloSaez](https://github.com/GonzaloSaez) — 2025-05-13T17:50:46Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  I think that moving it Reconcile can be problematic since the `HasSynced` mechanism does not check for `Reconcile` to succeed. So if we return true from `Create` then `WaitForCacheSync` will succeed before `Reconcile` ends. If we want to avoid the sleeps we need two different controllers, booting TAS + ResourceFlavor in one, waiting for it to sync and then booting the rest. But then we'd have to figure out if that can mess up with the rate limits of the client.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T18:10:23Z

- **pkg/controller/core/clusterqueue_controller.go:332** by [@mimowo](https://github.com/mimowo) — 2025-05-13T18:10:23Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  I see, thanks for explaining, makes sense. 
  
  Still I sense a better solution is possible. Actually, Im wondering if we could write the event handles so that in any order of processing they will initialize correctly. I believe this is what is achieved for regular quota. 
  
  I would like to explore this potential path before we make the decision : identify the orders of handlers which are not supported and see if it is possible to make them supported.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T18:24:46Z

- **pkg/controller/core/clusterqueue_controller.go:332** by [@mimowo](https://github.com/mimowo) — 2025-05-13T18:24:46Z
```diff
@@ -321,6 +328,18 @@ func (r *ClusterQueueReconciler) Create(e event.TypedCreateEvent[*kueue.ClusterQ
 
 	log := r.log.WithValues("clusterQueue", klog.KObj(e.Object))
 	log.V(2).Info("ClusterQueue create event")
+
+	if features.Enabled(features.TopologyAwareScheduling) {
+		// If we don't wait for the flavors to be synced, AddClusterQueue will
+		// add workloads to the cache without the flavors being present which results
+		// in TAS not accounting correctly for the node usage. This sync period should
+		// not take long.
+		log.V(2).Info("Waiting for TAS cache to be synced")
+		for !r.cache.TASCache().SyncedFlavors() {
+			time.Sleep(1 * time.Second)
```

  > So if we return true from Create then WaitForCacheSync will succeed before Reconcile ends.
  
  Right, so it means we can no longer rely on WaitForCacheSync. However, in the current approach we have waitForTas anyway. 
  
  So on a higher level I would like to consider the paths forward
  1. support any order of processing handlers  and rely only on WaitForCacheSync
  2. figure out enforcing the order of processing handlers in controller runtime
  3. move the code from handlers to reconcilers to avoid sleeping and retry by returning error or sleepAfter
  4. accept we sleep in handlers but, but make sure it only sleeps in startup (similar as in the pr)
  
  Let me know if we can already eliminate some of the options. My preference would be 1 or 3, but I see it may not be easy, so we look for compromises when we run out of ideas, but Im still not clear if 1 and 3 are eliminated.
