# PR #5238: [KEP-1282]: Update evictionState of WorkloadStatus

**Summary**: [KEP-1282]: Update evictionState of WorkloadStatus

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5238

**Last updated**: 2025-05-19T15:25:18Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `docs/update-kep-1282-follow-up` → `main`
- **Created**: 2025-05-13T07:30:39Z
- **Updated**: 2025-05-19T15:25:18Z
- **Merged**: 2025-05-19T15:25:16Z
- **Closed**: 2025-05-19T15:25:16Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 2   **+52 / -22**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
A followup on comments after premature merge: https://github.com/kubernetes-sigs/kueue/pull/5211

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #5071 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-13T07:30:44Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9ce3aeb5adbcd365e26c76c2f782e1fc50f7a248 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/682b492bf744be00085f2c40 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-5238--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-5238--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTUyMzgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.Fk7qix6ZGkZ7XFiQlX-ug4JtXeELEJiEe27l00RlLck)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-5238--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-13T07:31:21Z

/cc @tenzen-y

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-13T10:21:46Z

Should this KEP introduce this new metric?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-13T10:24:36Z

> Should this KEP introduce this new metric?

I would suggest yes, because the API is designed for the metric. So, I think it makes more sense to discuss the two together.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T14:53:05Z

@mszadkow Thank you for waiting for our discussion. I left the final decision at https://github.com/kubernetes-sigs/kueue/pull/5238#discussion_r2093201954

Additionally, could you mention in the doc that the `schedulingState` will not be reset since it is the history of the Workload.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-16T15:19:19Z

Updated ptal @tenzen-y @mimowo @mwielgus

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-19T10:26:23Z

Updated ptal

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-19T11:40:58Z

The implementation available here: https://github.com/kubernetes-sigs/kueue/pull/5259
@mimowo @PBundyra @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-19T11:58:34Z

/lgtm
/approve
Thanks 👍 I think all comments are addressed.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-19T11:58:40Z

LGTM label has been added.  <details>Git tree hash: b40e493ac058879d5677e137e7239b5b4f3223fa</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-19T15:18:49Z

/lgtm
leaving final tagging to @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-19T15:18:55Z

LGTM label has been added.  <details>Git tree hash: 4a1ab0f840e44babd78c45d907350627f2a2d6b8</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-19T15:20:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5238#issuecomment-2890743609" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5238#" title="Author self-approved">mszadkow</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5238#pullrequestreview-2851165264" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T07:40:11Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-13T07:40:11Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  I would probably suggest to keep the type, see my point in https://github.com/kubernetes-sigs/kueue/pull/5211#discussion_r2086129161
  cc @tenzen-y @PBundyra 
  
  OTOH, I'm also ok to have just "reason" but we then need to be careful to make sure there are no conflicts.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T07:42:58Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-13T07:42:58Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  Actually, having the "type" and "reason" at the metric level also allows us to easily aggregate the metrics in grafana at both levels of detail.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T07:55:06Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-13T07:55:06Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  Actually, seeing that the proposal migrates to be more generic, I think instead of introducing a new metric like https://github.com/kubernetes-sigs/kueue/pull/5127 `kueue_wait_for_pods_ready_workload_eviction_total` I would suggest to introduce the following metric, adding the "detailedReason".
  
  So:
  - EvictedWorkloadsTotal -> fix to count workloads only once.
  - EvictionsTotal -> would count eviction events as such
  
  Both metrics would have the "detailedReason". In the API:
  - "type" -> "reason"
  - "reason" -> "detailedReason"
  
  ```
  	EvictedWorkloadsTotal = prometheus.NewCounterVec(
  		prometheus.CounterOpts{
  			Subsystem: constants.KueueName,
  			Name:      "evicted_workloads_total",
  			Help: `The number of evicted workloads per 'cluster_queue',
  The label 'reason' can have the following values:
  - "Preempted" means that the workload was evicted in order to free resources for a workload with a higher priority or reclamation of nominal quota.
  - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
  - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
  - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
  - "Deactivated" means that the workload was evicted because spec.active is set to false`,
  		}, []string{"cluster_queue", "reason", "detailed_reason"},
  	)
  ```
  ```
  	EvictionsTotal = prometheus.NewCounterVec(
  		prometheus.CounterOpts{
  			Subsystem: constants.KueueName,
  			Name:      "evictions_total",
  			Help: `The number of evictions per 'cluster_queue',
  The label 'reason' can have the following values:
  - "Preempted" means that the workload was evicted in order to free resources for a workload with a higher priority or reclamation of nominal quota.
  - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
  - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
  - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
  - "Deactivated" means that the workload was evicted because spec.active is set to false`,
  		}, []string{"cluster_queue", "reason", "detailed_reason"},
  	)
  ```

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-13T08:36:57Z

- **keps/1282-pods-ready-requeue-strategy/README.md:211** by [@gabesaba](https://github.com/gabesaba) — 2025-05-13T08:35:40Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
```

  ```suggestion
  Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
  ```

- **keps/1282-pods-ready-requeue-strategy/README.md:233** by [@gabesaba](https://github.com/gabesaba) — 2025-05-13T08:36:17Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
+    // +kubebuilder:validation:Enum=WaitForStart;WaitForRecovery
     Reason string `json:"reason"`
     
-    // count tracks the number of evictions for this type and reason.
+    // count tracks the number of evictions for the reason.
```

  ```suggestion
      // count tracks the number of evictions for this reason.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-13T08:44:29Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@PBundyra](https://github.com/PBundyra) — 2025-05-13T08:44:29Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  I like the idea of generic metric. And if the current metrics have both `reason` and `detailedReason`, I would like to stick with it for the sake of consistency

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-13T10:45:02Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mszadkow](https://github.com/mszadkow) — 2025-05-13T10:45:02Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  I am missing some details here...
  1. Should both metrics count evictions per `cluster_queue` ?
  2. Should the `evictions_total` is `cluster_queue` independent?
  If not, why we need both?
  3. If both are per `cluster_queue` then `EvictedWorkloadsTotal -> fix to count workloads only once.` should rather be a gauge to count up and down and `EvictionsTotal -> would count eviction events as such` - counts only up?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T11:12:31Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-13T11:12:31Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  > Should both metrics count evictions per cluster_queue ?
  
  Yes, we should have the data available so I would use it. It is easy to aggregate data over cluster_queues if someone does not want that detail.
  
  > Should the evictions_total is cluster_queue independent?
  If not, why we need both?
  
  I think it should not be independent.
  
  > If both are per cluster_queue then EvictedWorkloadsTotal -> fix to count workloads only once. should rather be a gauge to count up and down and EvictionsTotal -> would count eviction events as such - counts only up?
  
  I think both metrics should be counters. The pre-existing is a counter, I would not change that. The new metric is a counter too.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T11:29:35Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T11:29:35Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  > Actually, having the "type" and "reason" at the metric level also allows us to easily aggregate the metrics in grafana at both levels of detail.
  
  At prometheus metrics level, I'm ok with including type. However, at the Workload API level, I have some objections.
  The state machine should be defined in a single place, and we should expose only additional information via another state field like `evictionState`. Otherwise, the state machine will be more complex across multiple fields and bring us uncontrollable state handling.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T11:32:02Z

- **keps/1282-pods-ready-requeue-strategy/README.md:243** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T11:31:59Z
```diff
@@ -208,39 +209,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
+    // +kubebuilder:validation:Enum=WaitForStart;WaitForRecovery
     Reason string `json:"reason"`
```

  ```suggestion
      Reason WorkloadEvictionStateReason `json:"reason"`
  ```
  
  @mszadkow could we use the dedicated type to prevent assigning arbitrary reason code base? Do you see any problem to introduce dedicated typed reason?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T11:33:16Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-13T11:33:15Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  I think the "type" could have been misleading. The metric we have already has "reason" as one of the following:
  - "Preempted" means that the workload was evicted in order to free resources for a workload with a higher priority or reclamation of nominal quota.
  - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
  - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
  - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
  - "Deactivated" means that the workload was evicted because spec.active is set to false`,
  
  I think in this feature we need something like "detailedReason" to break down "PodsReadyTimeout" into two subcategories.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-13T11:54:33Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mszadkow](https://github.com/mszadkow) — 2025-05-13T11:54:33Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  OK, then the actual difference is one counts workload evictions per cq and the other counts all evictions per cq?
  What else do we evict apart from workloads?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T12:03:43Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T12:03:42Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  @mimowo Is this feature objective adding human-readable information or machine-readable information or both?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T12:26:30Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-13T12:26:30Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  machine readable mostly, human readable a little bit

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T12:48:55Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T12:48:55Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  > machine readable mostly, human readable a little bit
  
  In that case, shoulnd't we have 2 different reasons instead of a single PodsReadyTimeout?
  What are your expected reasons?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T13:16:16Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-13T13:16:16Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  > In that case, shoulnd't we have 2 different reasons instead of a single PodsReadyTimeout?
  
  For this feature (metric) we want to distinguish StartupTimeout and RecoveryTimeout. I think since the metric is going to be generic then for preemption we would need to distinguish the following: https://github.com/kubernetes-sigs/kueue/blob/4cafd5fbad492983c9e3b8fde2dea2823712d44f/apis/kueue/v1beta1/workload_types.go#L517-L534
  
  Would you like to mesh all the reasons into one big enum, making sure the reasons from different features don't conflict?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-13T14:09:51Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T14:02:36Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  Ah, I imagined that you want to use `PodsReadyTimeout` in the `evictionStates.reason` for WorkloasStatus resource. 
  If we introduce the `detailed_reason` only to Prometheus metrics, I'm fine with that.

- **keps/1282-pods-ready-requeue-strategy/README.md:337** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T14:07:21Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailedReason`.
```

  ```suggestion
  Extend an existing `evicted_workloads_total` metric to include also `detailed_reason`.
  ```

- **keps/1282-pods-ready-requeue-strategy/README.md:345** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-13T14:09:42Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailedReason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
```

  I could not fully understand the differences between `evicted_workloads_total` and `evictions_total`.
  IIUC, the `evicted_workloads_total` is per workload. However, I couldn't catch which resources we will count for `evictions_total`?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-13T14:12:32Z

- **keps/1282-pods-ready-requeue-strategy/README.md:345** by [@mszadkow](https://github.com/mszadkow) — 2025-05-13T14:12:32Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailedReason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
```

  After discussion with Michal I see it like this now:
  Number of evictions total with reason to an eviction per cluster queue. Counts all events.
  Number of evicted workloads per cluster_queue. Count only one event per workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-13T14:19:37Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-13T14:19:36Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  Well, indeed I imagined:
  ```yaml
  evictionState:
  - reason: PodsReadyTimeout
    detailedReason: StartupTimeout
    count: 4
  - reason: Preempted
    detailedReason: InCohortReclamation
    count: 2
  - reason: AdmissionCheck
    detailedReason: // not available yet
    count: 3
  ```
  Then, to put the information into the metrics. It would allow to easily have a metric as the current `EvictedWorkloadsTotal` but just extended by "detailedReason".

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T06:23:05Z

- **keps/1282-pods-ready-requeue-strategy/README.md:243** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T06:23:04Z
```diff
@@ -208,39 +209,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
+    // +kubebuilder:validation:Enum=WaitForStart;WaitForRecovery
     Reason string `json:"reason"`
```

  Could be done, the only counter argument I have, is that we will duplicate existing Workload reasons.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T06:23:40Z

- **keps/1282-pods-ready-requeue-strategy/README.md:345** by [@mimowo](https://github.com/mimowo) — 2025-05-14T06:23:40Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailedReason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
```

  having a second thought on that. maybe it is better to keep the existing metric as is, just add a new one 'evicted_workloads_count_once_total'. WDYT @mszadkow @tenzen-y ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T07:21:58Z

- **keps/1282-pods-ready-requeue-strategy/README.md:359** by [@mimowo](https://github.com/mimowo) — 2025-05-14T07:21:58Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailed_reason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
+
+```go
+	EvictedWorkloadsTotal = prometheus.NewCounterVec(
```

  As mentioned in the other comment, to minimize the diff, and avoid changing the existing metric I would actually propose to keep `evicted_workloads_total`, and just add `evicted_workloads_once_total`.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T07:50:36Z

- **keps/1282-pods-ready-requeue-strategy/README.md:243** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T07:50:35Z
```diff
@@ -208,39 +209,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
+    // +kubebuilder:validation:Enum=WaitForStart;WaitForRecovery
     Reason string `json:"reason"`
```

  https://github.com/kubernetes-sigs/kueue/pull/5211#discussion_r2086246430
  That speaks to me too.
  Let's make it separate enum

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T08:04:33Z

- **keps/1282-pods-ready-requeue-strategy/README.md:359** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T08:04:32Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailed_reason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
+
+```go
+	EvictedWorkloadsTotal = prometheus.NewCounterVec(
```

  I think we should go back to what is the goal of the task.
  User wants to see number of workloads evicted due to a specific reason (initially PodsReady timeouts).
  However we want only to count distinct eviction events per workload.
  
  Let's keep the typed `reason` to avoid having an extra `type` field.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T08:07:43Z

- **keps/1282-pods-ready-requeue-strategy/README.md:345** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T08:07:43Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailedReason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
```

  We want this metric to convey the idea of "once per workload-reason pair", I think it will be just enough.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T08:23:31Z

- **keps/1282-pods-ready-requeue-strategy/README.md:359** by [@mimowo](https://github.com/mimowo) — 2025-05-14T08:23:31Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailed_reason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
+
+```go
+	EvictedWorkloadsTotal = prometheus.NewCounterVec(
```

  So we will not have "reason" and "detailedReason", just "reason"?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T08:43:26Z

- **keps/1282-pods-ready-requeue-strategy/README.md:359** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T08:43:26Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailed_reason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
+
+```go
+	EvictedWorkloadsTotal = prometheus.NewCounterVec(
```

  No, let's have `reason` and `detailed_reason`, one will be typed string the other is just a string.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T08:44:52Z

- **keps/1282-pods-ready-requeue-strategy/README.md:359** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T08:44:52Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailed_reason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
+
+```go
+	EvictedWorkloadsTotal = prometheus.NewCounterVec(
```

  However I  don't have opinion on `detailed_reason` in `EvictedWorkloadsTotal`.
  Maybe it's better to leave it as is

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T08:48:14Z

- **keps/1282-pods-ready-requeue-strategy/README.md:359** by [@mimowo](https://github.com/mimowo) — 2025-05-14T08:48:14Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailed_reason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
+
+```go
+	EvictedWorkloadsTotal = prometheus.NewCounterVec(
```

  Yes, to make the changes minimal I'm ok to do not add the `detailed_reason` label to the pre-existing `EvictedWorkloadsTotal`. My goal was to make sure this is possible in the future, and the API looks consistent. 
  
  For now, I would just keep `EvictedWorkloadsTotal` as is and introduce `EvictedWorkloadsOnceTotal` with "reason" and "detailed_reason".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T10:17:05Z

- **keps/1282-pods-ready-requeue-strategy/README.md:244** by [@mimowo](https://github.com/mimowo) — 2025-05-14T10:17:01Z
```diff
@@ -208,39 +209,39 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionStateReason string
+
+const (
+	Preempted WorkloadEvictionStateReason = "Preempted"
+	PodsReadyTimeout WorkloadEvictionStateReason = "PodsReadyTimeout"
+	AdmissionCheck WorkloadEvictionStateReason = "AdmissionCheck"
+	ClusterQueueStopped WorkloadEvictionStateReason = "ClusterQueueStopped"
+	Deactivated WorkloadEvictionStateReason = "Deactivated"
+)
+
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
-    Reason string `json:"reason"`
+    // +kubebuilder:validation:Enum={Preempted,PodsReadyTimeout,AdmissionCheck,ClusterQueueStopped,Deactivated}
+    Reason WorkloadEvictionStateReason `json:"reason"`
     
-    // count tracks the number of evictions for this type and reason.
+    // count tracks the number of evictions for this reason.
```

  Don't we need also the "DetailedReason" field? I don't see how we will get the required information otherwise.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T11:02:26Z

- **keps/1282-pods-ready-requeue-strategy/README.md:244** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T11:02:25Z
```diff
@@ -208,39 +209,39 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionStateReason string
+
+const (
+	Preempted WorkloadEvictionStateReason = "Preempted"
+	PodsReadyTimeout WorkloadEvictionStateReason = "PodsReadyTimeout"
+	AdmissionCheck WorkloadEvictionStateReason = "AdmissionCheck"
+	ClusterQueueStopped WorkloadEvictionStateReason = "ClusterQueueStopped"
+	Deactivated WorkloadEvictionStateReason = "Deactivated"
+)
+
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
-    Reason string `json:"reason"`
+    // +kubebuilder:validation:Enum={Preempted,PodsReadyTimeout,AdmissionCheck,ClusterQueueStopped,Deactivated}
+    Reason WorkloadEvictionStateReason `json:"reason"`
     
-    // count tracks the number of evictions for this type and reason.
+    // count tracks the number of evictions for this reason.
```

  Yes, done.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T13:00:19Z

- **keps/1282-pods-ready-requeue-strategy/README.md:242** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T13:00:19Z
```diff
@@ -208,39 +209,47 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionStateReason string
+
+const (
+	Preempted WorkloadEvictionStateReason = "Preempted"
+	PodsReadyTimeout WorkloadEvictionStateReason = "PodsReadyTimeout"
+	AdmissionCheck WorkloadEvictionStateReason = "AdmissionCheck"
+	ClusterQueueStopped WorkloadEvictionStateReason = "ClusterQueueStopped"
+	Deactivated WorkloadEvictionStateReason = "Deactivated"
+)
+
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
-    Reason string `json:"reason"`
+    // +kubebuilder:validation:Enum={Preempted,PodsReadyTimeout,AdmissionCheck,ClusterQueueStopped,Deactivated}
+    Reason WorkloadEvictionStateReason `json:"reason"`
```

  to be true it would be much easier to implement  if we could use string instead of enum

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T13:34:43Z

- **keps/1282-pods-ready-requeue-strategy/README.md:345** by [@mimowo](https://github.com/mimowo) — 2025-05-14T13:34:43Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailedReason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
```

  Can you provide example status then, and the example metric update, how how to map them? My idea I summarized in https://github.com/kubernetes-sigs/kueue/pull/5238#discussion_r2086950624, and I see how this could work, but I'm not sure about this proposal TBH.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T17:09:59Z

- **keps/1282-pods-ready-requeue-strategy/README.md:345** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T17:09:59Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailedReason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
```

  Sure let's have it all together here:
  
  Workload.Status.evictionStates:
  - reason: PodsReadyTimeout
    detailedReason: StartupTimeout
    count: 4
  - reason: Preempted
    detailedReason: InCohortReclamation
    count: 2
  - reason: AdmissionCheck
    detailedReason: // not available yet
    count: 3
  
  Metrics:
  
  {"kueue_evicted_workloads_total", cq, "9"},
  
  {"kueue_evicted_workloads_once_total", cq, "PodsReadyTimeout", "StartupTimeoutReason", "1"},
  {"kueue_evicted_workloads_once_total", cq, "Preempted", "InCohortReclamation", "1"},
  {"kueue_evicted_workloads_once_total", cq, "AdmissionCheck", "", "1"},

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-14T17:17:42Z

- **keps/1282-pods-ready-requeue-strategy/README.md:345** by [@mimowo](https://github.com/mimowo) — 2025-05-14T17:17:42Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailedReason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
```

  kueue_evicted_workloads_once_total lgtm
  
  For kueue_evicted_workloads_total we probably have:
  {"kueue_evicted_workloads_total", cq, "PodsReadyTimeout", "4"},
  {"kueue_evicted_workloads_total", cq, "Preempted", "2"},
  {"kueue_evicted_workloads_total", cq, "AdmissionCheck", "3"},
  
  right?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-14T18:04:37Z

- **keps/1282-pods-ready-requeue-strategy/README.md:345** by [@mszadkow](https://github.com/mszadkow) — 2025-05-14T18:04:37Z
```diff
@@ -341,6 +332,41 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
+### Changes to metrics
+
+Extend an existing `evicted_workloads_total` metric to include also `detailedReason`.
+Introduce a new metric `evictions_total` to count total number of evictions per 'cluster_queue'.
```

  right, ofc, sorry

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-15T10:16:02Z

- **keps/1282-pods-ready-requeue-strategy/README.md:250** by [@mszadkow](https://github.com/mszadkow) — 2025-05-15T10:16:02Z
```diff
@@ -208,39 +209,47 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionStateReason string
+
+const (
+	Preempted WorkloadEvictionStateReason = "Preempted"
+	PodsReadyTimeout WorkloadEvictionStateReason = "PodsReadyTimeout"
+	AdmissionCheck WorkloadEvictionStateReason = "AdmissionCheck"
+	ClusterQueueStopped WorkloadEvictionStateReason = "ClusterQueueStopped"
+	Deactivated WorkloadEvictionStateReason = "Deactivated"
+)
+
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
-    Reason string `json:"reason"`
+    // +kubebuilder:validation:Enum={Preempted,PodsReadyTimeout,AdmissionCheck,ClusterQueueStopped,Deactivated}
+    Reason WorkloadEvictionStateReason `json:"reason"`
     
-    // count tracks the number of evictions for this type and reason.
+    // detailedReason specifies a finer-grained explanation that complements the eviction cause.
+    //
+	// Defaults to null.
+	// +optional
+    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
+    // +kubebuilder:validation:MaxLength=316
```

  	// This may be an empty string.
  	//
  	// +required
  	// +kubebuilder:validation:Required
  	// +kubebuilder:validation:MaxLength=316

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-15T10:16:35Z

- **keps/1282-pods-ready-requeue-strategy/README.md:222** by [@mszadkow](https://github.com/mszadkow) — 2025-05-15T10:16:35Z
```diff
@@ -208,39 +209,47 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
```

  	// +optional
  	// +listType=map
  	// +listMapKey=reason
  	// +patchStrategy=merge
  	// +patchMergeKey=reason

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T10:17:55Z

- **keps/1282-pods-ready-requeue-strategy/README.md:242** by [@mimowo](https://github.com/mimowo) — 2025-05-15T10:17:55Z
```diff
@@ -208,39 +209,47 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionStateReason string
+
+const (
+	Preempted WorkloadEvictionStateReason = "Preempted"
+	PodsReadyTimeout WorkloadEvictionStateReason = "PodsReadyTimeout"
+	AdmissionCheck WorkloadEvictionStateReason = "AdmissionCheck"
+	ClusterQueueStopped WorkloadEvictionStateReason = "ClusterQueueStopped"
+	Deactivated WorkloadEvictionStateReason = "Deactivated"
+)
+
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
-    Reason string `json:"reason"`
+    // +kubebuilder:validation:Enum={Preempted,PodsReadyTimeout,AdmissionCheck,ClusterQueueStopped,Deactivated}
+    Reason WorkloadEvictionStateReason `json:"reason"`
```

  That would be my feeling too. I would be ok to start with string and refactor to use enum. cc @tenzen-y @PBundyra

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-15T13:11:13Z

- **keps/1282-pods-ready-requeue-strategy/README.md:242** by [@PBundyra](https://github.com/PBundyra) — 2025-05-15T13:11:13Z
```diff
@@ -208,39 +209,47 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
+type WorkloadEvictionStateReason string
+
+const (
+	Preempted WorkloadEvictionStateReason = "Preempted"
+	PodsReadyTimeout WorkloadEvictionStateReason = "PodsReadyTimeout"
+	AdmissionCheck WorkloadEvictionStateReason = "AdmissionCheck"
+	ClusterQueueStopped WorkloadEvictionStateReason = "ClusterQueueStopped"
+	Deactivated WorkloadEvictionStateReason = "Deactivated"
+)
+
+type WorkloadEvictionState struct {
     // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
-    Reason string `json:"reason"`
+    // +kubebuilder:validation:Enum={Preempted,PodsReadyTimeout,AdmissionCheck,ClusterQueueStopped,Deactivated}
+    Reason WorkloadEvictionStateReason `json:"reason"`
```

  >  I would be ok to start with string and refactor to use enum
  
  I'm fine with this approach

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-15T14:32:02Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-15T14:32:02Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  Let me put the reason why I am keeping urgue not to introduduce those fields to Workload status: https://docs.google.com/presentation/d/1_4PCAne1b1NtggdeemBkFmJvVctr4b_Z5n1rsJsmg-Q/edit?usp=sharing

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T14:39:21Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-15T14:39:21Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  Yes. there is some tranformation needed, but not to the "detailedReason" part, which would be 1:1 taken from the PodsReadyTimeout condition. I think some transformation is done for the preexisting reason in the preexisting metric `evicted_workloads_total`. 
  
  Yuki, what is your proposal?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-15T14:42:09Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-15T14:42:09Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  Before something, could you tell me about the missing capabilities, as I mentioned on page 4?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-15T14:44:43Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-15T14:44:42Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  > Yes. there is some tranformation needed, but not to the "detailedReason" part, which would be 1:1 taken from the PodsReadyTimeout condition.
  
  The problem with multi-time transformations is that they need to be considered to keep consistency between conditions and evictionStetes which considering the behind criteria for each condition.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-16T08:11:00Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@PBundyra](https://github.com/PBundyra) — 2025-05-16T08:11:00Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  Could you expand on how do you understand "two times stretched state" and why is this a problem? The `evictionState` will only changed by increasing counter, adding a new reason when Workloads gets evicted? The interaction is only one way, and I think it's fairly simply
  
  I see two main capabilities of this feature:
  1. Have a metrics that counts how many different workloads have been evicted (in particular, by waitForPodsReady)
  2. From the user perspective, be able to easily find workloads that have been evicted in the past (in particular by waitForPodsReady)
  
  I believe we need status API for that, as keeping the state in memory won't work on restart, and it doesn't meet the 2nd requirement

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-16T08:36:59Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mwielgus](https://github.com/mwielgus) — 2025-05-16T08:36:59Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  I think that the counter field for eviction/attempts will have couple other applications. For example we can store information which RF we tried (and failed) in the past and possibly try a different one the next time. So, most likely, PR is not only for the metric, although this is the most immediate application.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-16T08:38:14Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mwielgus](https://github.com/mwielgus) — 2025-05-16T08:38:14Z
```diff
@@ -208,39 +209,37 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
-    // +listMapKey=type
+    // +listMapKey=reason
     // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    // +patchMergeKey=reason
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type WorkloadEvictionState struct {
```

  Eviction limits the applciation to eviction only. Can we make it a little bit more generic?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-16T08:39:26Z

- **keps/1282-pods-ready-requeue-strategy/README.md:254** by [@mwielgus](https://github.com/mwielgus) — 2025-05-16T08:39:26Z
```diff
@@ -208,39 +209,37 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
-    // +listMapKey=type
+    // +listMapKey=reason
     // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    // +patchMergeKey=reason
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type WorkloadEvictionState struct {
+    // reason specifies the programmatic identifier for the eviction cause.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
     // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
-    
-    // reason specifies the programmatic identifier for the eviction cause.
+    Reason string `json:"reason"`
+
+    // detailedReason specifies a finer-grained explanation that complements the eviction cause.
+    // This may be an empty string.
     //
     // +required
     // +kubebuilder:validation:Required
-    // +kubebuilder:validation:MaxLength=1024
-    // +kubebuilder:validation:MinLength=1
-    // +kubebuilder:validation:Pattern=`^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$`
-    Reason string `json:"reason"`
-    
-    // count tracks the number of evictions for this type and reason.
+    // +kubebuilder:validation:MaxLength=316
+    DetailedReason string `json:"detailedReason"`
+
+    // count tracks the number of evictions for this reason and detailed reason.
     //
```

  I guess we will need LastUpdate soon. Anyway, this can wait until we have a PR that uses it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-16T12:30:00Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mimowo](https://github.com/mimowo) — 2025-05-16T12:30:00Z
```diff
@@ -208,39 +209,37 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
-    // +listMapKey=type
+    // +listMapKey=reason
     // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    // +patchMergeKey=reason
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type WorkloadEvictionState struct {
```

  We can try, but I'm worried we can over-engineer this. Do you have any specific proposal?
  
  I think given the data is going to supply the EvictedWorkloadOnce metric it makes sense to keep the name scoped to "eviction".
  
  We can always revisit the API for v1beta2.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-16T12:40:45Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T12:40:44Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  > Could you expand on how do you understand "two times stretched state" and why is this a problem? The evictionState will only changed by increasing counter, adding a new reason when Workloads gets evicted? The interaction is only one way, and I think it's fairly simply
  
  I agree that each state is simple. However, as I mentioned on slide page 2, we need to have 2 times criteria / state transformations, and the `evictionState` is based on 2 condition types. As a result, the paths for reaching the final `evictionState` will be complex.
  
  Additionally, each `evictionState` is aggregated from 2 conditions, which means we have additional paths in the current condition state machine. This is also a state machine complexity problem.
  
  In conclusion, this new `evictionState` introduces 2 dimensional complexity, (1. 2 times state / criteria transformations and (2. aggregation of multiple conditions into one eviction state.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-16T12:52:20Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mwielgus](https://github.com/mwielgus) — 2025-05-16T12:52:20Z
```diff
@@ -208,39 +209,37 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
-    // +listMapKey=type
+    // +listMapKey=reason
     // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    // +patchMergeKey=reason
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type WorkloadEvictionState struct {
```

  What I'm suggesting is just a minor rename. Instead of calling it eviction state maybe we can call it, for example, scheduling state - almost the same, but leaves other applications open.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-16T12:58:25Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mimowo](https://github.com/mimowo) — 2025-05-16T12:58:24Z
```diff
@@ -208,39 +209,37 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
-    // +listMapKey=type
+    // +listMapKey=reason
     // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    // +patchMergeKey=reason
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type WorkloadEvictionState struct {
```

  but it embeds the list of eviction reasons, directly, since this is `EvictionStates []WorkloadEvictionState`. 
  
  Maybe we could add another layer of nesting to accommodate for it
  ```yaml
  schedulingState:  // this field is meant for future extensions
    // with potential other info
    evictionState:  // this field can be directly transformed into the new metric
    - reason: ...
       detailedReason
       count: 
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-16T13:00:54Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T13:00:54Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  > I think that the counter field for eviction/attempts will have couple other applications. For example we can store information which RF we tried (and failed) in the past and possibly try a different one the next time. So, most likely, PR is not only for the metric, although this is the most immediate application.
  
  I can understand that this feature is useful. The most impactful problem is the `evictionState` has multiple steps as one state like `WaitForStart` -> `PodsReadyTimeout` -> `Eviction`

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-16T13:18:06Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mwielgus](https://github.com/mwielgus) — 2025-05-16T13:18:06Z
```diff
@@ -208,39 +209,37 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
-    // +listMapKey=type
+    // +listMapKey=reason
     // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    // +patchMergeKey=reason
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type WorkloadEvictionState struct {
```

  OK, i understand calling the field evictionState and making it an array. What I'm trying to point out is that the structure has no field very specific to eviction it is reason, details and count. So I was thinking about removing eviction from the struct name to allow future reuse.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-16T13:48:22Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mimowo](https://github.com/mimowo) — 2025-05-16T13:48:22Z
```diff
@@ -208,39 +209,37 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
-    // +listMapKey=type
+    // +listMapKey=reason
     // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    // +patchMergeKey=reason
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type WorkloadEvictionState struct {
```

  I synced with @mwielgus and we concluded on the following option as preferred:
  
  ```yaml
  schedulingState:
    // potential place to include admissionStats, etc.
    evictionStats:  
    - reason: PodsReadyTimeout  // From Evicted.reason
      detailedReason: WaitForStart  // From PodsReady.reason
      count: 3
  ```
  but skipping `schedulingState` is also acceptable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-16T13:57:49Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-05-16T13:57:49Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  >  The most impactful problem is the evictionState has multiple steps as one state like WaitForStart -> PodsReadyTimeout -> Eviction
  
  Could you elaborate on why this is a problem? The implementation PR  is relatively simple.
  
  I would like to see some alternative proposal to satisfy the feature needs. 
  
  I'm not a big fun of this, but in order to make progress we could have:
  ```yaml
  evictionState:
  - reason: WaitForStart
    count: 
  ```
  So in API we would have just one API field, and we would implicitly know that WaitForStart means PodsReadyTimeout when setting the "reason" and "detailed_reason" labels.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-16T14:49:21Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T14:49:21Z
```diff
@@ -208,39 +209,37 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
-    // +listMapKey=type
+    // +listMapKey=reason
     // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    // +patchMergeKey=reason
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type WorkloadEvictionState struct {
```

  I discussed with @mimowo (+ @mwielgus) about this. To structure the eviction between the reason and the background reason, like:
  
  ```yaml
  schedulingState:
    evictionStats:  
    - reason: PodsReadyTimeout  // From Evicted.reason
      underlyingCause: WaitForStart // From PodsReady.reason
      count: 3
  ```
  
  As an alternative, we considered the following more nested API surface to allow us to add more and more identifiers for the reason in the following. However, the beflow API allows us to expand the evictionStats usage, which introduce more complexity. When we grow the evicioonStats, we should have another sub-resource / dedicated resource to store the information, in conclusion.
  
  ```yaml
  schedulingState:
    evictionStats:  
    - reason:
        type: PodsReadyTimeout  // From Evicted.reason
        underlyingCause: WaitForStart // From PodsReady.reason
      count: 3
  ```
  
  As conclusion, we agreed using the following API surface:
  
  
  ```yaml
  schedulingState:
    evictionStats:  
    - reason: PodsReadyTimeout  // From Evicted.reason
      underlyingCause: WaitForStart // From PodsReady.reason
      count: 3
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-16T14:50:24Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T14:50:24Z
```diff
@@ -208,39 +208,29 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field `evictionState` to the WorkloadStatus struct to track the number of evictions by  reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
     // +listMapKey=type
     // +patchStrategy=merge
     // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
-    //
-    // +required
-    // +kubebuilder:validation:Required
-    // +kubebuilder:validation:Pattern=`^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$`
-    // +kubebuilder:validation:MaxLength=316
-    Type string `json:"type"`
```

  The discussion results are stored in https://github.com/kubernetes-sigs/kueue/pull/5238#discussion_r2093201954
  Please see it if anyone is interested in this discussion.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-16T15:02:32Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mimowo](https://github.com/mimowo) — 2025-05-16T15:02:32Z
```diff
@@ -208,39 +209,37 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `evictionState`, to the WorkloadStatus struct to track the number of evictions by reason:
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
+    // evictionState tracks eviction statistics by reason.
     //
     // +optional
     // +listType=map
-    // +listMapKey=type
+    // +listMapKey=reason
     // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+    // +patchMergeKey=reason
+    EvictionStates []WorkloadEvictionState `json:"evictionStates,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type WorkloadEvictionState struct {
```

  I suggest we also say "underlying_cause" in the metric definition

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-16T15:26:49Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T15:25:15Z
```diff
@@ -208,39 +209,46 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub-resource to be a part of the `schedulingState` will be `evictionStates` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingState tracks scheduling statistics (e.g. evictionStates)
+	//
+	// +optional
+	SchedulingState SchedulingState `json:"schedulingState,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingState struct {
+	// evictionState tracks eviction statistics by reason.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
```

  Does this `+patchMergeKey ` and `+listMapKey` work well? I am suspecting we have conflicts in the following:
  Did you check it?
  
  ```yaml
  schedulingState:
    evictionStats:  
    - reason: PodsReadyTimeout  // From Evicted.reason
      underlyingCause: WaitForStart // From PodsReady.reason
      count: 3
    - reason: PodsReadyTimeout  // From Evicted.reason
      underlyingCause: WaitForRecovery // From PodsReady.reason
      count: 3
  ```

- **keps/1282-pods-ready-requeue-strategy/README.md:214** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T15:26:22Z
```diff
@@ -208,39 +209,46 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub-resource to be a part of the `schedulingState` will be `evictionStates` a struct to track the number of evictions by reason.
```

  ```suggestion
  First sub field to be a part of the `schedulingState` will be `evictionStates` a struct to track the number of evictions by reason.
  ```
  
  The `sub-resource` has a dedicated technical term which indicates `status` and `scale`, something else.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-17T10:33:47Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mszadkow](https://github.com/mszadkow) — 2025-05-17T10:33:47Z
```diff
@@ -208,39 +209,46 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub-resource to be a part of the `schedulingState` will be `evictionStates` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingState tracks scheduling statistics (e.g. evictionStates)
+	//
+	// +optional
+	SchedulingState SchedulingState `json:"schedulingState,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingState struct {
+	// evictionState tracks eviction statistics by reason.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
```

  Yes, you are correct I will change it

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-17T11:22:55Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mszadkow](https://github.com/mszadkow) — 2025-05-17T11:22:55Z
```diff
@@ -208,39 +209,46 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub-resource to be a part of the `schedulingState` will be `evictionStates` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingState tracks scheduling statistics (e.g. evictionStates)
+	//
+	// +optional
+	SchedulingState SchedulingState `json:"schedulingState,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingState struct {
+	// evictionState tracks eviction statistics by reason.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
```

  We have basically 2 options here.
  Either we introduce an extra field.g. `uniqueKey` as combination of `reason` and `underlyingCause` or we assume `underlyingCause` is more unique than `reason`.
  wdyt? @mimowo @tenzen-y

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-19T03:27:45Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-19T03:27:45Z
```diff
@@ -208,39 +209,46 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub-resource to be a part of the `schedulingState` will be `evictionStates` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingState tracks scheduling statistics (e.g. evictionStates)
+	//
+	// +optional
+	SchedulingState SchedulingState `json:"schedulingState,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingState struct {
+	// evictionState tracks eviction statistics by reason.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
```

  IIUC, we can specify the multiple map keys in the following. Did you see any generation errors?
  
  https://kubernetes.io/docs/reference/using-api/server-side-apply/#merge-strategy
  
  ```go
  	// +optional
  	// +listType=map
  	// +listMapKey=reason
  	// +listMapkey=underlyingCause
  	// +patchStrategy=merge
  	// +patchMergeKey=reason
  	// +patchMergeKey=underlyingCause
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-19T07:04:41Z

- **keps/1282-pods-ready-requeue-strategy/README.md:232** by [@mszadkow](https://github.com/mszadkow) — 2025-05-19T07:04:41Z
```diff
@@ -208,39 +209,46 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub-resource to be a part of the `schedulingState` will be `evictionStates` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingState tracks scheduling statistics (e.g. evictionStates)
+	//
+	// +optional
+	SchedulingState SchedulingState `json:"schedulingState,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingState struct {
+	// evictionState tracks eviction statistics by reason.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
```

  Nope, no errors

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T07:15:36Z

LGTM, just one remaining comment about the naming of the top-level structure

- **keps/1282-pods-ready-requeue-strategy/README.md:212** by [@mimowo](https://github.com/mimowo) — 2025-05-19T07:15:05Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
```

  Since the API is going to be more generic than the metric (and expanded in the future), let me consider alternative naming:
  1. schedulingState, evictionStates (as currently)
  2. schedulingStats, evictionStats
  3. schedulingEvents, evictionEvents
  4. schedulingHistory, evictionHistory
  5. schedulingLog, evictionLog
  
  
  I'm not a big fan of:
  -  `schedulingState`, because "state" suggests to me this is "current" moment, but the structure is meant for historical data. 
  - `schedulingStats` - because id does not capture the intention - these are not statistics like avg, std dev, these are just data to compute some stats on. 
  - `schedulingEvents` - because events has already a formal meaning in Kubernetes, so could be constraining if we want to use it beyond events.
  
  So, my preference is either `schedulingLog` or `schedulingHistory`. I'm slightly leaning towards `schedulingLog` as a bit more "relaxed".
  
  Let me know what you think @tenzen-y @PBundyra @mwielgus

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-19T08:41:03Z

- **keps/1282-pods-ready-requeue-strategy/README.md:212** by [@PBundyra](https://github.com/PBundyra) — 2025-05-19T08:41:03Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
```

  I'm okay with both 2 and 4

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-19T08:59:45Z

- **keps/1282-pods-ready-requeue-strategy/README.md:212** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-19T08:59:44Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
```

  I think `2` is better since `Log` and `History` indicate the raw record of past scheduling. The `Stats` means the aggregation and summarization of log or history based on some points.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T09:08:04Z

- **keps/1282-pods-ready-requeue-strategy/README.md:212** by [@mimowo](https://github.com/mimowo) — 2025-05-19T09:08:04Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
```

  I'm ok with stats too "Stats". 
  
  However, seeing your point why not History, maybe we want to consider "schedulingLogSummary and sub-field eviction", wdyt @tenzen-y ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-19T09:35:09Z

- **keps/1282-pods-ready-requeue-strategy/README.md:212** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-19T09:35:09Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
```

  In that case, `schedulingStats.eviction` looks better, IMO.
  For sure, the `stats` indicates `statistics` like avg, std dev in the mathematical field.
  However, the word ' stats` means the information based on something, which is not limited in dedicated signs.
  
  For `schedulingLogSummary` means more modified information rather than `statics`. I meant `Summary` indicates analized information by someone (kueue-controller-manager) PoV.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T09:40:59Z

- **keps/1282-pods-ready-requeue-strategy/README.md:212** by [@mimowo](https://github.com/mimowo) — 2025-05-19T09:40:59Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
```

  Ok, let's go with `schedulingStats.eviction`. @mszadkow please update the KEP and implementation accordingly.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-19T09:58:38Z

- **keps/1282-pods-ready-requeue-strategy/README.md:212** by [@mszadkow](https://github.com/mszadkow) — 2025-05-19T09:58:38Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
```

  Sure, I am on it

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-19T10:02:54Z

- **keps/1282-pods-ready-requeue-strategy/README.md:221** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-19T10:02:54Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingState`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingState` will be `evictionStates` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingState tracks scheduling statistics (e.g. evictionStates)
+	//
+	// +optional
+	SchedulingState SchedulingState `json:"schedulingState,omitempty"`
```

  ```suggestion
  	SchedulingState *SchedulingState `json:"schedulingState,omitempty"`
  ```
  
  Let's use a pointer field.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T10:19:38Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mimowo](https://github.com/mimowo) — 2025-05-19T10:19:38Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// eviction tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Eviction []WorkloadEvictionState `json:"eviction,omitempty"`
+}
+
+type WorkloadEvictionState struct {
```

  ```suggestion
  type WorkloadSchedulingStatsEviction struct {
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-19T12:03:37Z

- **keps/1282-pods-ready-requeue-strategy/README.md:234** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-19T12:03:37Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// eviction tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Eviction []WorkloadSchedulingStatsEvicition `json:"eviction,omitempty"`
```

  ```suggestion
  	Evictions []WorkloadSchedulingStatsEvicition `json:"evictions,omitempty"`
  ```
  
  Shouldn't we use the `evictions` since this can accommodate the multiple eviction stats?
  /hold

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-19T14:08:06Z

- **keps/1282-pods-ready-requeue-strategy/README.md:234** by [@mszadkow](https://github.com/mszadkow) — 2025-05-19T14:08:06Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// eviction tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Eviction []WorkloadSchedulingStatsEvicition `json:"eviction,omitempty"`
```

  I thought about that, but I read this like "scheduling statistics related to eviction".
  Then I thought keeping eviction (singular form) was ok here.
  Wdyt @mimowo @PBundyra ?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-19T14:09:02Z

- **keps/1282-pods-ready-requeue-strategy/README.md:234** by [@mszadkow](https://github.com/mszadkow) — 2025-05-19T14:09:02Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// eviction tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Eviction []WorkloadSchedulingStatsEvicition `json:"eviction,omitempty"`
```

  but I can't easily change it

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T14:10:30Z

- **keps/1282-pods-ready-requeue-strategy/README.md:234** by [@mimowo](https://github.com/mimowo) — 2025-05-19T14:10:30Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// eviction tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Eviction []WorkloadSchedulingStatsEvicition `json:"eviction,omitempty"`
```

  I think both singlular and plural have some merit. I would be slightly leaning to Evictions as it is a list.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-19T14:14:00Z

- **keps/1282-pods-ready-requeue-strategy/README.md:234** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-19T14:14:00Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// eviction tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Eviction []WorkloadSchedulingStatsEvicition `json:"eviction,omitempty"`
```

  IIUC, Workload has eviction statistics every for reason and underlyingCause.
  This is because I recommended using `evicitons`. We are going to store the statistics for eviction for each reason.
  In other words, the workload has eviction statistics for PodsReadyTimeout+WaitForStart and statistics for PodsReadyTimeout+WaitForRecovery in the following:
  
  ```yaml
  schedulingStats:
    evictions:  
    - reason: PodsReadyTimeout  // From Evicted.reason
      underlyingCause: WaitForStart // From PodsReady.reason
      count: 3
      lastTime: xyz // Not for Now
      cluster: spot-cluster // Not for Now
    - reason: PodsReadyTimeout  // From Evicted.reason
      underlyingCause: WaitForRecovery // From PodsReady.reason
      count: 3
      lastTime: xyz // Not for Now
      cluster: ondemand-cluster // Not for Now
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T14:21:31Z

- **keps/1282-pods-ready-requeue-strategy/README.md:234** by [@mimowo](https://github.com/mimowo) — 2025-05-19T14:21:30Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// eviction tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Eviction []WorkloadSchedulingStatsEvicition `json:"eviction,omitempty"`
```

  ok, @mszadkow please adjust accordingly

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-19T15:07:42Z

- **keps/1282-pods-ready-requeue-strategy/README.md:234** by [@mszadkow](https://github.com/mszadkow) — 2025-05-19T15:07:42Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// eviction tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Eviction []WorkloadSchedulingStatsEvicition `json:"eviction,omitempty"`
```

  done, please check again @tenzen-y

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T15:12:48Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mimowo](https://github.com/mimowo) — 2025-05-19T15:12:48Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// evictions tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Evictions []WorkloadSchedulingStatsEvicition `json:"evictions,omitempty"`
+}
+
+type WorkloadSchedulingStatsEvicition struct {
```

  nit WorkloadSchedulingStatsEvicitions
  to align with the field

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-19T15:17:25Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-19T15:17:25Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// evictions tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Evictions []WorkloadSchedulingStatsEvicition `json:"evictions,omitempty"`
+}
+
+type WorkloadSchedulingStatsEvicition struct {
```

  Couldn't we keep `WorkloadSchedulingStatsEvicition`? Because evictions are a set of `WorkloadSchedulingStatsEvicition`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T15:18:09Z

- **keps/1282-pods-ready-requeue-strategy/README.md:237** by [@mimowo](https://github.com/mimowo) — 2025-05-19T15:18:09Z
```diff
@@ -208,39 +209,48 @@ type RequeueState struct {
 }
 ```
 
-Add a new field `evictionStats` to the WorkloadStatus struct to track the number of evictions by type and reason:
+Add a new field, `schedulingStats`, to the WorkloadStatus struct to track the number of scheduling statistics.
+SchedulingState serves as a history of the Workload thus will not be reset.
+First sub field to be a part of the `schedulingStats` will be `eviction` a struct to track the number of evictions by reason.
 
 ```go
 type WorkloadStatus struct {
-	// evictionStats tracks eviction statistics by type and reason.
-    //
-    // +optional
-    // +listType=map
-    // +listMapKey=type
-    // +patchStrategy=merge
-    // +patchMergeKey=type
-    EvictionStats []Eviction `json:"evictionStats,omitempty"`
+	// schedulingStats tracks scheduling statistics (e.g. evictions)
+	//
+	// +optional
+	SchedulingStats *SchedulingStats `json:"schedulingStats,omitempty"`
 }
 
-type Eviction struct {
-    // type identifies the eviction type.
+type SchedulingStats struct {
+	// evictions tracks eviction statistics by reason and underlyingCause.
+	//
+	// +optional
+	// +listType=map
+	// +listMapKey=reason
+	// +listMapKey=underlyingCause
+	// +patchStrategy=merge
+	// +patchMergeKey=reason
+	// +patchMergeKey=underlyingCause
+	Evictions []WorkloadSchedulingStatsEvicition `json:"evictions,omitempty"`
+}
+
+type WorkloadSchedulingStatsEvicition struct {
```

  ok, sure we can

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-19T15:20:02Z

@mszadkow Thank you!
/lgtm
/approve
/hold cancel
