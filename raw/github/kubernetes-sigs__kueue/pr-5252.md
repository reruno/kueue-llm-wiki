# PR #5252: Fix Kueue ownership traversal resulting in permission errors

**Summary**: Fix Kueue ownership traversal resulting in permission errors

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5252

**Last updated**: 2025-10-07T09:11:07Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-kueue-permissions` → `main`
- **Created**: 2025-05-15T07:20:02Z
- **Updated**: 2025-10-07T09:11:07Z
- **Merged**: 2025-05-15T17:37:14Z
- **Closed**: 2025-05-15T17:37:14Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `kind/regression`, `approved`, `cncf-cla: yes`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@dgrove-oss](https://github.com/dgrove-oss)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 6   **+200 / -1**

## Description

#### What type of PR is this?

/kind bug
/kind regression
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5235

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix the bug which prevented running Jobs (with queue-name label) owned by other Jobs for which Kueue does not 
have the necessary RBAC permissions (for example kserve or CronJob).
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-15T07:20:09Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 30627abbf77b7298e7bc7493153118bff06495e7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/682620f3e3a33f00082d11b5 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-15T16:46:17Z

@kannon92 @dgrove-oss @tenzen-y This is ready for review I think.

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-05-15T16:52:54Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-15T16:53:01Z

LGTM label has been added.  <details>Git tree hash: 5c3c68c4b63070fa551b3eb437e6b4c1e89d2c34</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-15T17:07:19Z

@dgrove-oss I just reduced the timeouts in e2e tests to LongTimeout. I initially used VeryLongTimeout before I figured out the trick with adjusting the LastScheduleTime. I tested locally in a loop and the job schedules immediately. If it flakes I will bump the timeout, but it is more that needed (from my testing).

PTAL

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-15T17:07:52Z

/cherry-pick release-0.11

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-05-15T17:07:54Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.11` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5252#issuecomment-2884517587):

>/cherry-pick release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-05-15T17:10:01Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-15T17:10:07Z

LGTM label has been added.  <details>Git tree hash: 25a63c8a21a41bdad25f17669896f94b82478a68</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-15T17:22:58Z

LGTM label has been added.  <details>Git tree hash: 6fea3e829d2bf09e262809c5981d252564cc651c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-15T17:22:58Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5252#pullrequestreview-2844490020" title="LGTM">dgrove-oss</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5252#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-05-15T17:37:51Z

@mimowo: #5252 failed to apply on top of branch "release-0.11":
```
Applying: Fix Kueue owner traversal issue / regression
Using index info to reconstruct a base tree...
M	pkg/controller/jobframework/integrationmanager.go
M	pkg/controller/jobframework/reconciler.go
M	pkg/controller/jobs/job/job_webhook_test.go
M	test/e2e/singlecluster/e2e_test.go
M	test/util/util.go
Falling back to patching base and 3-way merge...
Auto-merging test/util/util.go
Auto-merging test/e2e/singlecluster/e2e_test.go
CONFLICT (content): Merge conflict in test/e2e/singlecluster/e2e_test.go
Auto-merging pkg/controller/jobs/job/job_webhook_test.go
Auto-merging pkg/controller/jobframework/reconciler.go
Auto-merging pkg/controller/jobframework/integrationmanager.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Fix Kueue owner traversal issue / regression

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5252#issuecomment-2884517587):

>/cherry-pick release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-15T15:49:49Z

- **test/e2e/singlecluster/e2e_test.go:124** by [@kannon92](https://github.com/kannon92) — 2025-05-15T15:49:48Z
```diff
@@ -118,6 +119,78 @@ var _ = ginkgo.Describe("Kueue", func() {
 			util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 		})
 
+		ginkgo.It("Should allow to schedule Jobs via CronJob", func() {
```

  Great test case! Thanks for adding it.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-15T15:51:22Z

- **pkg/controller/jobframework/integrationmanager.go:216** by [@kannon92](https://github.com/kannon92) — 2025-05-15T15:51:22Z
```diff
@@ -198,6 +199,20 @@ func (m *integrationManager) getList() []string {
 	return ret
 }
 
+func (m *integrationManager) isKnownOwner(ownerRef *metav1.OwnerReference) bool {
+	for _, cbs := range m.integrations {
+		if cbs.matchingOwnerReference(ownerRef) {
+			return true
+		}
+	}
+	for _, jt := range m.externalIntegrations {
+		if ownerReferenceMatchingGVK(ownerRef, jt.GetObjectKind().GroupVersionKind()) {
+			return true
+		}
+	}
+	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == "apps/v1"
```

  The first two blocks of code make sense to me.
  
  The replicaSet does not. I'm sure this is correct but could you add a comment here explaining why?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T15:58:42Z

- **pkg/controller/jobframework/integrationmanager.go:216** by [@mimowo](https://github.com/mimowo) — 2025-05-15T15:58:42Z
```diff
@@ -198,6 +199,20 @@ func (m *integrationManager) getList() []string {
 	return ret
 }
 
+func (m *integrationManager) isKnownOwner(ownerRef *metav1.OwnerReference) bool {
+	for _, cbs := range m.integrations {
+		if cbs.matchingOwnerReference(ownerRef) {
+			return true
+		}
+	}
+	for _, jt := range m.externalIntegrations {
+		if ownerReferenceMatchingGVK(ownerRef, jt.GetObjectKind().GroupVersionKind()) {
+			return true
+		}
+	}
+	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == "apps/v1"
```

  Sure, we need to be able to walk up the ownerhip chain for Pod -> ReplicaSet -> Deployment. 
  
  However, ReplicaSet is "technical detail" rather than an integration a user would like to enable or see in the config.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T16:00:14Z

- **test/e2e/singlecluster/e2e_test.go:124** by [@mimowo](https://github.com/mimowo) — 2025-05-15T16:00:14Z
```diff
@@ -118,6 +119,78 @@ var _ = ginkgo.Describe("Kueue", func() {
 			util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 		})
 
+		ginkgo.It("Should allow to schedule Jobs via CronJob", func() {
```

  Thanks, the only downside is that sometimes the Job gets 1min to be created by the CronJob, because CronJob seems to only create with 1min resolution. I'm wondering if we could trick it: 
  - create CronJob suspended
  - patch CronJob status to indicate the last schedule was over 1min ago
  - resume
  
  I will check if this works, because I don't like wasting 1min (or 30s on average) for this test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T16:27:06Z

- **pkg/controller/jobframework/integrationmanager.go:216** by [@mimowo](https://github.com/mimowo) — 2025-05-15T16:27:06Z
```diff
@@ -198,6 +199,20 @@ func (m *integrationManager) getList() []string {
 	return ret
 }
 
+func (m *integrationManager) isKnownOwner(ownerRef *metav1.OwnerReference) bool {
+	for _, cbs := range m.integrations {
+		if cbs.matchingOwnerReference(ownerRef) {
+			return true
+		}
+	}
+	for _, jt := range m.externalIntegrations {
+		if ownerReferenceMatchingGVK(ownerRef, jt.GetObjectKind().GroupVersionKind()) {
+			return true
+		}
+	}
+	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == "apps/v1"
```

  /hold
  We have had one e2e test for this scenario, but we are missing unit test, I will add.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T16:45:54Z

- **pkg/controller/jobframework/integrationmanager.go:216** by [@mimowo](https://github.com/mimowo) — 2025-05-15T16:45:54Z
```diff
@@ -198,6 +199,20 @@ func (m *integrationManager) getList() []string {
 	return ret
 }
 
+func (m *integrationManager) isKnownOwner(ownerRef *metav1.OwnerReference) bool {
+	for _, cbs := range m.integrations {
+		if cbs.matchingOwnerReference(ownerRef) {
+			return true
+		}
+	}
+	for _, jt := range m.externalIntegrations {
+		if ownerReferenceMatchingGVK(ownerRef, jt.GetObjectKind().GroupVersionKind()) {
+			return true
+		}
+	}
+	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == "apps/v1"
```

  /unhold
  added under "Pod -> ReplicaSet -> Deployment (queue-name) => Deployment". I tested it fails without this line.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (approved) — 2025-05-15T16:52:38Z

lgtm.

- **pkg/controller/jobframework/integrationmanager.go:216** by [@dgrove-oss](https://github.com/dgrove-oss) — 2025-05-15T16:49:09Z
```diff
@@ -198,6 +199,20 @@ func (m *integrationManager) getList() []string {
 	return ret
 }
 
+func (m *integrationManager) isKnownOwner(ownerRef *metav1.OwnerReference) bool {
+	for _, cbs := range m.integrations {
+		if cbs.matchingOwnerReference(ownerRef) {
+			return true
+		}
+	}
+	for _, jt := range m.externalIntegrations {
+		if ownerReferenceMatchingGVK(ownerRef, jt.GetObjectKind().GroupVersionKind()) {
+			return true
+		}
+	}
+	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == "apps/v1"
```

  Makes sense.  Thanks for adding the extra test case.

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (approved) — 2025-05-15T17:09:45Z

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-15T17:10:01Z

- **pkg/controller/jobframework/integrationmanager.go:216** by [@kannon92](https://github.com/kannon92) — 2025-05-15T17:10:01Z
```diff
@@ -198,6 +199,20 @@ func (m *integrationManager) getList() []string {
 	return ret
 }
 
+func (m *integrationManager) isKnownOwner(ownerRef *metav1.OwnerReference) bool {
+	for _, cbs := range m.integrations {
+		if cbs.matchingOwnerReference(ownerRef) {
+			return true
+		}
+	}
+	for _, jt := range m.externalIntegrations {
+		if ownerReferenceMatchingGVK(ownerRef, jt.GetObjectKind().GroupVersionKind()) {
+			return true
+		}
+	}
+	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == "apps/v1"
```

  I meant a comment in the code..

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T17:11:25Z

- **pkg/controller/jobframework/integrationmanager.go:216** by [@mimowo](https://github.com/mimowo) — 2025-05-15T17:11:25Z
```diff
@@ -198,6 +199,20 @@ func (m *integrationManager) getList() []string {
 	return ret
 }
 
+func (m *integrationManager) isKnownOwner(ownerRef *metav1.OwnerReference) bool {
+	for _, cbs := range m.integrations {
+		if cbs.matchingOwnerReference(ownerRef) {
+			return true
+		}
+	}
+	for _, jt := range m.externalIntegrations {
+		if ownerReferenceMatchingGVK(ownerRef, jt.GetObjectKind().GroupVersionKind()) {
+			return true
+		}
+	}
+	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == "apps/v1"
```

  will do soon

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-15T17:14:38Z

- **pkg/controller/jobframework/integrationmanager.go:216** by [@mimowo](https://github.com/mimowo) — 2025-05-15T17:14:38Z
```diff
@@ -198,6 +199,20 @@ func (m *integrationManager) getList() []string {
 	return ret
 }
 
+func (m *integrationManager) isKnownOwner(ownerRef *metav1.OwnerReference) bool {
+	for _, cbs := range m.integrations {
+		if cbs.matchingOwnerReference(ownerRef) {
+			return true
+		}
+	}
+	for _, jt := range m.externalIntegrations {
+		if ownerReferenceMatchingGVK(ownerRef, jt.GetObjectKind().GroupVersionKind()) {
+			return true
+		}
+	}
+	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == "apps/v1"
```

  Done, ptal

### Review by [@dgrove-oss](https://github.com/dgrove-oss) (approved) — 2025-05-15T17:22:52Z

/lgtm

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-15T17:30:01Z

/lgtm

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-16T04:00:30Z

Thank you

- **pkg/controller/jobframework/integrationmanager.go:216** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T03:45:53Z
```diff
@@ -198,6 +199,23 @@ func (m *integrationManager) getList() []string {
 	return ret
 }
 
+func (m *integrationManager) isKnownOwner(ownerRef *metav1.OwnerReference) bool {
+	for _, cbs := range m.integrations {
+		if cbs.matchingOwnerReference(ownerRef) {
+			return true
+		}
+	}
+	for _, jt := range m.externalIntegrations {
+		if ownerReferenceMatchingGVK(ownerRef, jt.GetObjectKind().GroupVersionKind()) {
+			return true
+		}
+	}
+	// ReplicaSet is an interim owner from Pod to Deployment. We call it known
+	// so that the users don't need to list	it explicitly in their configs.
+	// Note that Kueue provides RBAC permissions allowing for traversal over it.
+	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == "apps/v1"
```

  ```suggestion
  	return ownerRef.Kind == "ReplicaSet" && ownerRef.APIVersion == appsv1.SchemeGroupVersion.String()
  ```
  
  Let's use the constants instead of hardcoded value.

- **pkg/controller/jobframework/reconciler_test.go:332** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T03:48:23Z
```diff
@@ -315,10 +319,80 @@ func TestFindAncestorJobManagedByKueue(t *testing.T) {
 				Obj(),
 			wantManaged: testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Obj(),
 		},
+		"Job -> CronJob (external framework, not enabled) -> AppWrapper (queue-name) => AppWrapper": {
+			integrations: []string{"workload.codeflare.dev/appwrapper"},
+			ancestors: []client.Object{
+				testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Queue("test-q").Obj(),
+				&batchv1.CronJob{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "cronjob",
+						Namespace: jobNamespace,
+						OwnerReferences: []metav1.OwnerReference{{
+							Name:       "aw",
+							APIVersion: "workload.codeflare.dev/appwrapper",
```

  ```suggestion
  							APIVersion: awv1beta2.GroupVersion.String(),
  ```

- **pkg/controller/jobframework/reconciler_test.go:333** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T03:48:57Z
```diff
@@ -315,10 +319,80 @@ func TestFindAncestorJobManagedByKueue(t *testing.T) {
 				Obj(),
 			wantManaged: testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Obj(),
 		},
+		"Job -> CronJob (external framework, not enabled) -> AppWrapper (queue-name) => AppWrapper": {
+			integrations: []string{"workload.codeflare.dev/appwrapper"},
+			ancestors: []client.Object{
+				testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Queue("test-q").Obj(),
+				&batchv1.CronJob{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "cronjob",
+						Namespace: jobNamespace,
+						OwnerReferences: []metav1.OwnerReference{{
+							Name:       "aw",
+							APIVersion: "workload.codeflare.dev/appwrapper",
+							Kind:       "AppWrapper",
```

  ```suggestion
  
  							Kind:       awvv1beta2.AppWrapperKind,
  ```

- **pkg/controller/jobframework/reconciler_test.go:355** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T03:49:32Z
```diff
@@ -315,10 +319,80 @@ func TestFindAncestorJobManagedByKueue(t *testing.T) {
 				Obj(),
 			wantManaged: testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Obj(),
 		},
+		"Job -> CronJob (external framework, not enabled) -> AppWrapper (queue-name) => AppWrapper": {
+			integrations: []string{"workload.codeflare.dev/appwrapper"},
+			ancestors: []client.Object{
+				testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Queue("test-q").Obj(),
+				&batchv1.CronJob{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "cronjob",
+						Namespace: jobNamespace,
+						OwnerReferences: []metav1.OwnerReference{{
+							Name:       "aw",
+							APIVersion: "workload.codeflare.dev/appwrapper",
+							Kind:       "AppWrapper",
+							UID:        "aw",
+							Controller: ptr.To(true),
+						}},
+					},
+				},
+			},
+			job: testingjob.MakeJob("job", jobNamespace).UID("job").
+				OwnerReference("cronjob", batchv1.SchemeGroupVersion.WithKind("CronJob")).
+				Obj(),
+		},
+		"Job -> CronJob (external framework, enabled) -> AppWrapper (queue-name) => AppWrapper": {
+			integrations:       []string{"workload.codeflare.dev/appwrapper"},
+			externalFrameworks: []string{"CronJob.v1.batch"},
+			ancestors: []client.Object{
+				testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Queue("test-q").Obj(),
+				&batchv1.CronJob{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "cronjob",
+						Namespace: jobNamespace,
+						OwnerReferences: []metav1.OwnerReference{{
+							Name:       "aw",
+							APIVersion: "workload.codeflare.dev/v1beta2",
```

  ```suggestion
  							APIVersion: awv1beta2.GroupVersion.String(),
  ```

- **pkg/controller/jobframework/reconciler_test.go:356** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T03:49:51Z
```diff
@@ -315,10 +319,80 @@ func TestFindAncestorJobManagedByKueue(t *testing.T) {
 				Obj(),
 			wantManaged: testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Obj(),
 		},
+		"Job -> CronJob (external framework, not enabled) -> AppWrapper (queue-name) => AppWrapper": {
+			integrations: []string{"workload.codeflare.dev/appwrapper"},
+			ancestors: []client.Object{
+				testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Queue("test-q").Obj(),
+				&batchv1.CronJob{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "cronjob",
+						Namespace: jobNamespace,
+						OwnerReferences: []metav1.OwnerReference{{
+							Name:       "aw",
+							APIVersion: "workload.codeflare.dev/appwrapper",
+							Kind:       "AppWrapper",
+							UID:        "aw",
+							Controller: ptr.To(true),
+						}},
+					},
+				},
+			},
+			job: testingjob.MakeJob("job", jobNamespace).UID("job").
+				OwnerReference("cronjob", batchv1.SchemeGroupVersion.WithKind("CronJob")).
+				Obj(),
+		},
+		"Job -> CronJob (external framework, enabled) -> AppWrapper (queue-name) => AppWrapper": {
+			integrations:       []string{"workload.codeflare.dev/appwrapper"},
+			externalFrameworks: []string{"CronJob.v1.batch"},
+			ancestors: []client.Object{
+				testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Queue("test-q").Obj(),
+				&batchv1.CronJob{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "cronjob",
+						Namespace: jobNamespace,
+						OwnerReferences: []metav1.OwnerReference{{
+							Name:       "aw",
+							APIVersion: "workload.codeflare.dev/v1beta2",
+							Kind:       "AppWrapper",
```

  ```suggestion
  							Kind:       awvv1beta2.AppWrapperKind,
  ```

- **pkg/controller/jobframework/reconciler_test.go:378** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T03:50:21Z
```diff
@@ -315,10 +319,80 @@ func TestFindAncestorJobManagedByKueue(t *testing.T) {
 				Obj(),
 			wantManaged: testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Obj(),
 		},
+		"Job -> CronJob (external framework, not enabled) -> AppWrapper (queue-name) => AppWrapper": {
+			integrations: []string{"workload.codeflare.dev/appwrapper"},
+			ancestors: []client.Object{
+				testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Queue("test-q").Obj(),
+				&batchv1.CronJob{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "cronjob",
+						Namespace: jobNamespace,
+						OwnerReferences: []metav1.OwnerReference{{
+							Name:       "aw",
+							APIVersion: "workload.codeflare.dev/appwrapper",
+							Kind:       "AppWrapper",
+							UID:        "aw",
+							Controller: ptr.To(true),
+						}},
+					},
+				},
+			},
+			job: testingjob.MakeJob("job", jobNamespace).UID("job").
+				OwnerReference("cronjob", batchv1.SchemeGroupVersion.WithKind("CronJob")).
+				Obj(),
+		},
+		"Job -> CronJob (external framework, enabled) -> AppWrapper (queue-name) => AppWrapper": {
+			integrations:       []string{"workload.codeflare.dev/appwrapper"},
+			externalFrameworks: []string{"CronJob.v1.batch"},
+			ancestors: []client.Object{
+				testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Queue("test-q").Obj(),
+				&batchv1.CronJob{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "cronjob",
+						Namespace: jobNamespace,
+						OwnerReferences: []metav1.OwnerReference{{
+							Name:       "aw",
+							APIVersion: "workload.codeflare.dev/v1beta2",
+							Kind:       "AppWrapper",
+							UID:        "aw",
+							Controller: ptr.To(true),
+						}},
+					},
+				},
+			},
+			job: testingjob.MakeJob("job", jobNamespace).UID("job").
+				OwnerReference("cronjob", batchv1.SchemeGroupVersion.WithKind("CronJob")).
+				Obj(),
+			wantManaged: testingaw.MakeAppWrapper("aw", jobNamespace).UID("aw").Queue("test-q").Obj(),
+		},
+		"Pod -> ReplicaSet -> Deployment (queue-name) => Deployment": {
+			integrations: []string{"pod", "deployment"},
+			ancestors: []client.Object{
+				testingdeployment.MakeDeployment("deploy", jobNamespace).UID("deploy").Queue("test-q").Obj(),
+				&appsv1.ReplicaSet{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "rs",
+						Namespace: jobNamespace,
+						OwnerReferences: []metav1.OwnerReference{{
+							Name:       "deploy",
+							APIVersion: "apps/v1",
```

  ```suggestion
  							APIVersion:  appsv1.SchemeGroupVersion.String(),
  ```

- **test/e2e/singlecluster/e2e_test.go:176** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-16T04:00:26Z
```diff
@@ -118,6 +121,85 @@ var _ = ginkgo.Describe("Kueue", func() {
 			util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 		})
 
+		ginkgo.It("Should allow to schedule Jobs via CronJob", func() {
+			cronJob := &batchv1.CronJob{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      "test-cronjob",
+					Namespace: ns.Name,
+				},
+				Spec: batchv1.CronJobSpec{
+					Schedule:          "* * * * *",
+					ConcurrencyPolicy: batchv1.ForbidConcurrent,
+					JobTemplate: batchv1.JobTemplateSpec{
+						ObjectMeta: metav1.ObjectMeta{
+							Labels: map[string]string{
+								constants.QueueLabel: localQueue.Name,
+							},
+						},
+						Spec: batchv1.JobSpec{
+							Template: corev1.PodTemplateSpec{
+								Spec: corev1.PodSpec{
+									RestartPolicy: corev1.RestartPolicyNever,
+									Containers: []corev1.Container{
+										{
+											Name:    "c",
+											Image:   util.E2eTestAgnHostImage,
+											Command: util.BehaviorExitFast,
+											Resources: corev1.ResourceRequirements{
+												Requests: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+												Limits: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			}
+			util.MustCreate(ctx, k8sClient, cronJob)
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(util.DeleteAllCronJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Patch the last start time to be in the past so that it starts immediately", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cronJob), cronJob)).To(gomega.Succeed())
+					nextSchedule := cronJob.CreationTimestamp.Add(-2 * time.Minute)
+					cronJob.Status.LastScheduleTime = ptr.To(metav1.Time{Time: nextSchedule})
+					g.Expect(k8sClient.Status().Update(ctx, cronJob)).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
```

  We might want to move CronJob case to integration tests and emulate the kube-controller-manager behavior like https://github.com/kubernetes/kubectl/blob/88bb12ba04025e5b03a141ad26fca1c735d54e11/pkg/cmd/create/create_job.go#L254-L287
  
  And we can expand Appwrapper E2E to verify the unknown owners.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-09T13:57:08Z

- **test/e2e/singlecluster/e2e_test.go:176** by [@mimowo](https://github.com/mimowo) — 2025-06-09T13:57:07Z
```diff
@@ -118,6 +121,85 @@ var _ = ginkgo.Describe("Kueue", func() {
 			util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 		})
 
+		ginkgo.It("Should allow to schedule Jobs via CronJob", func() {
+			cronJob := &batchv1.CronJob{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      "test-cronjob",
+					Namespace: ns.Name,
+				},
+				Spec: batchv1.CronJobSpec{
+					Schedule:          "* * * * *",
+					ConcurrencyPolicy: batchv1.ForbidConcurrent,
+					JobTemplate: batchv1.JobTemplateSpec{
+						ObjectMeta: metav1.ObjectMeta{
+							Labels: map[string]string{
+								constants.QueueLabel: localQueue.Name,
+							},
+						},
+						Spec: batchv1.JobSpec{
+							Template: corev1.PodTemplateSpec{
+								Spec: corev1.PodSpec{
+									RestartPolicy: corev1.RestartPolicyNever,
+									Containers: []corev1.Container{
+										{
+											Name:    "c",
+											Image:   util.E2eTestAgnHostImage,
+											Command: util.BehaviorExitFast,
+											Resources: corev1.ResourceRequirements{
+												Requests: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+												Limits: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			}
+			util.MustCreate(ctx, k8sClient, cronJob)
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(util.DeleteAllCronJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Patch the last start time to be in the past so that it starts immediately", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cronJob), cronJob)).To(gomega.Succeed())
+					nextSchedule := cronJob.CreationTimestamp.Add(-2 * time.Minute)
+					cronJob.Status.LastScheduleTime = ptr.To(metav1.Time{Time: nextSchedule})
+					g.Expect(k8sClient.Status().Update(ctx, cronJob)).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
```

  I would suggest to stay with e2e, for two reasons:
  - emulating `kube-controller-manager` is generally hard
  - it is easier to test RBAC permissions in e2e, I don't think we install them for integration tests, so it means a lot of extra work. 
  
  wdyt @tenzen-y ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-26T10:59:26Z

- **test/e2e/singlecluster/e2e_test.go:176** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-26T10:59:25Z
```diff
@@ -118,6 +121,85 @@ var _ = ginkgo.Describe("Kueue", func() {
 			util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 		})
 
+		ginkgo.It("Should allow to schedule Jobs via CronJob", func() {
+			cronJob := &batchv1.CronJob{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      "test-cronjob",
+					Namespace: ns.Name,
+				},
+				Spec: batchv1.CronJobSpec{
+					Schedule:          "* * * * *",
+					ConcurrencyPolicy: batchv1.ForbidConcurrent,
+					JobTemplate: batchv1.JobTemplateSpec{
+						ObjectMeta: metav1.ObjectMeta{
+							Labels: map[string]string{
+								constants.QueueLabel: localQueue.Name,
+							},
+						},
+						Spec: batchv1.JobSpec{
+							Template: corev1.PodTemplateSpec{
+								Spec: corev1.PodSpec{
+									RestartPolicy: corev1.RestartPolicyNever,
+									Containers: []corev1.Container{
+										{
+											Name:    "c",
+											Image:   util.E2eTestAgnHostImage,
+											Command: util.BehaviorExitFast,
+											Resources: corev1.ResourceRequirements{
+												Requests: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+												Limits: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			}
+			util.MustCreate(ctx, k8sClient, cronJob)
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(util.DeleteAllCronJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Patch the last start time to be in the past so that it starts immediately", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cronJob), cronJob)).To(gomega.Succeed())
+					nextSchedule := cronJob.CreationTimestamp.Add(-2 * time.Minute)
+					cronJob.Status.LastScheduleTime = ptr.To(metav1.Time{Time: nextSchedule})
+					g.Expect(k8sClient.Status().Update(ctx, cronJob)).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
```

  IIUC, this test case does not depend on any CronJob features. So, I think we can move this case to integration test, then could reduce E2E test duration.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-07T08:10:45Z

- **test/e2e/singlecluster/e2e_test.go:176** by [@mimowo](https://github.com/mimowo) — 2025-10-07T08:10:45Z
```diff
@@ -118,6 +121,85 @@ var _ = ginkgo.Describe("Kueue", func() {
 			util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 		})
 
+		ginkgo.It("Should allow to schedule Jobs via CronJob", func() {
+			cronJob := &batchv1.CronJob{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      "test-cronjob",
+					Namespace: ns.Name,
+				},
+				Spec: batchv1.CronJobSpec{
+					Schedule:          "* * * * *",
+					ConcurrencyPolicy: batchv1.ForbidConcurrent,
+					JobTemplate: batchv1.JobTemplateSpec{
+						ObjectMeta: metav1.ObjectMeta{
+							Labels: map[string]string{
+								constants.QueueLabel: localQueue.Name,
+							},
+						},
+						Spec: batchv1.JobSpec{
+							Template: corev1.PodTemplateSpec{
+								Spec: corev1.PodSpec{
+									RestartPolicy: corev1.RestartPolicyNever,
+									Containers: []corev1.Container{
+										{
+											Name:    "c",
+											Image:   util.E2eTestAgnHostImage,
+											Command: util.BehaviorExitFast,
+											Resources: corev1.ResourceRequirements{
+												Requests: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+												Limits: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			}
+			util.MustCreate(ctx, k8sClient, cronJob)
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(util.DeleteAllCronJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Patch the last start time to be in the past so that it starts immediately", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cronJob), cronJob)).To(gomega.Succeed())
+					nextSchedule := cronJob.CreationTimestamp.Add(-2 * time.Minute)
+					cronJob.Status.LastScheduleTime = ptr.To(metav1.Time{Time: nextSchedule})
+					g.Expect(k8sClient.Status().Update(ctx, cronJob)).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
```

  I'm just not sure we verify RBAC in integration tests, and the bug would only surface at the permission layer.
  
  So, we may need to extend / refactor the integration tests to enable that check.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-07T09:11:07Z

- **test/e2e/singlecluster/e2e_test.go:176** by [@mimowo](https://github.com/mimowo) — 2025-10-07T09:11:07Z
```diff
@@ -118,6 +121,85 @@ var _ = ginkgo.Describe("Kueue", func() {
 			util.ExpectAllPodsInNamespaceDeleted(ctx, k8sClient, ns)
 		})
 
+		ginkgo.It("Should allow to schedule Jobs via CronJob", func() {
+			cronJob := &batchv1.CronJob{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      "test-cronjob",
+					Namespace: ns.Name,
+				},
+				Spec: batchv1.CronJobSpec{
+					Schedule:          "* * * * *",
+					ConcurrencyPolicy: batchv1.ForbidConcurrent,
+					JobTemplate: batchv1.JobTemplateSpec{
+						ObjectMeta: metav1.ObjectMeta{
+							Labels: map[string]string{
+								constants.QueueLabel: localQueue.Name,
+							},
+						},
+						Spec: batchv1.JobSpec{
+							Template: corev1.PodTemplateSpec{
+								Spec: corev1.PodSpec{
+									RestartPolicy: corev1.RestartPolicyNever,
+									Containers: []corev1.Container{
+										{
+											Name:    "c",
+											Image:   util.E2eTestAgnHostImage,
+											Command: util.BehaviorExitFast,
+											Resources: corev1.ResourceRequirements{
+												Requests: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+												Limits: corev1.ResourceList{
+													corev1.ResourceCPU: resource.MustParse("1"),
+												},
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			}
+			util.MustCreate(ctx, k8sClient, cronJob)
+			ginkgo.DeferCleanup(func() {
+				gomega.Expect(util.DeleteAllCronJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Patch the last start time to be in the past so that it starts immediately", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(cronJob), cronJob)).To(gomega.Succeed())
+					nextSchedule := cronJob.CreationTimestamp.Add(-2 * time.Minute)
+					cronJob.Status.LastScheduleTime = ptr.To(metav1.Time{Time: nextSchedule})
+					g.Expect(k8sClient.Status().Update(ctx, cronJob)).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
+			})
```

  @tenzen-y I synced with @kaisoz about moving forward with the "simple" its. As mentioned above the moving of the CronJob test is more involving as it requires enabling RBAC checks in the integration tests so that the test could pick up the original issue for which the  test was added. I believe this requires a separate issue on its own as this is not a "quick follow up" imo.
