# PR #5259: [Feature] Create a metric to track the number of workloads evicted due to unique reasons

**Summary**: [Feature] Create a metric to track the number of workloads evicted due to unique reasons

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5259

**Last updated**: 2025-05-20T14:32:21Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/metric-to-track-the-number-of-workloads-evicted-due-to-PodsReady-timeouts-2` → `main`
- **Created**: 2025-05-15T15:45:51Z
- **Updated**: 2025-05-20T14:32:21Z
- **Merged**: 2025-05-20T09:23:16Z
- **Closed**: 2025-05-20T09:23:16Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mykysha](https://github.com/mykysha), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mykysha](https://github.com/mykysha), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 23   **+704 / -40**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Create a metric to track the number of workloads evicted due to unique reasons.
This approach is more generic than limit ourselves to reasons related to PodsReady timeouts.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5071 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
evicted_workloads_once_total - counts number of unique workload evictions.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-15T15:45:58Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9dc4ef770f3df7199382e1956bb3b4dea9037645 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/682c4365e258de00084620ab |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-16T11:26:12Z

/cc @mimowo

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-16T11:26:31Z

I am still fighting with flaky - pull-kueue-test-e2e-customconfigs-main

/test pull-kueue-test-e2e-customconfigs-main

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-16T11:26:52Z

/cc @PBundyra

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-16T11:27:09Z

related to https://github.com/kubernetes-sigs/kueue/pull/5238

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-16T13:17:36Z

> Overall looks great, one minor comment and a question whether it would be possible to add the metric to the `test/e2e/singlecluster/metrics_test.go`?

done

### Comment by [@mykysha](https://github.com/mykysha) — 2025-05-16T13:22:37Z

Thanks!
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-16T13:22:46Z

LGTM label has been added.  <details>Git tree hash: 886ec870cf72d9f0ec03debc564811aa8d2093ac</details>

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-16T15:22:19Z

Will be updated based on https://github.com/kubernetes-sigs/kueue/pull/5238

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-20T08:02:29Z

LGTM, only small-ish comments. 

cc @tenzen-y I think this is almost ready to go.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-20T09:21:40Z

/lgtm
/approve
Thanks 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-20T09:21:46Z

LGTM label has been added.  <details>Git tree hash: 9bf99cf87321d72b612e281cd707b254b2911ee3</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-20T09:21:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5259#issuecomment-2893615859" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5259#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-16T12:52:43Z

Overall looks great, one minor comment and a question whether it would be possible to add the metric to the `test/e2e/singlecluster/metrics_test.go`?

- **pkg/controller/core/workload_controller_test.go:1763** by [@mykysha](https://github.com/mykysha) — 2025-05-16T12:50:54Z
```diff
@@ -1656,21 +1760,21 @@ func TestReconcile(t *testing.T) {
 				Obj(),
 		},
 
-		"admitted workload with max execution time": {
-			workload: utiltesting.MakeWorkload("wl", "ns").
-				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
-				MaximumExecutionTimeSeconds(120).
-				AdmittedAt(true, testStartTime.Add(-time.Minute)).
-				ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "ownername", "owneruid").
-				Obj(),
-			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
-				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
-				MaximumExecutionTimeSeconds(120).
-				AdmittedAt(true, testStartTime.Add(-time.Minute)).
-				ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "ownername", "owneruid").
-				Obj(),
-			wantResult: reconcile.Result{RequeueAfter: time.Minute},
-		},
+		// "admitted workload with max execution time": {
```

  Do we need this commented-out block?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-16T12:56:05Z

- **pkg/controller/core/workload_controller_test.go:1763** by [@mszadkow](https://github.com/mszadkow) — 2025-05-16T12:56:05Z
```diff
@@ -1656,21 +1760,21 @@ func TestReconcile(t *testing.T) {
 				Obj(),
 		},
 
-		"admitted workload with max execution time": {
-			workload: utiltesting.MakeWorkload("wl", "ns").
-				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
-				MaximumExecutionTimeSeconds(120).
-				AdmittedAt(true, testStartTime.Add(-time.Minute)).
-				ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "ownername", "owneruid").
-				Obj(),
-			wantWorkload: utiltesting.MakeWorkload("wl", "ns").
-				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
-				MaximumExecutionTimeSeconds(120).
-				AdmittedAt(true, testStartTime.Add(-time.Minute)).
-				ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "ownername", "owneruid").
-				Obj(),
-			wantResult: reconcile.Result{RequeueAfter: time.Minute},
-		},
+		// "admitted workload with max execution time": {
```

  ups, good catch

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T12:00:18Z

- **test/util/e2e.go:344** by [@mimowo](https://github.com/mimowo) — 2025-05-19T12:00:18Z
```diff
@@ -341,7 +341,7 @@ func GetKueueMetrics(ctx context.Context, cfg *rest.Config, restClient *rest.RES
 	metricsOutput, _, err := KExecute(ctx, cfg, restClient, configapi.DefaultNamespace, curlPodName, curlContainerName, []string{
 		"/bin/sh", "-c",
 		fmt.Sprintf(
-			"curl -s -k -H \"Authorization: Bearer $(cat /var/run/secrets/kubernetes.io/serviceaccount/token)\" https://%s.%s.svc.cluster.local:8443/metrics",
+			"curl --connect-timeout 10 --max-time 30 -s -k -H \"Authorization: Bearer $(cat /var/run/secrets/kubernetes.io/serviceaccount/token)\" https://%s.%s.svc.cluster.local:8443/metrics",
```

  Please move changes in the file to a dedicated PR as we may need to cherry-pick them in the future.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T12:03:24Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@mimowo](https://github.com/mimowo) — 2025-05-19T12:03:24Z
```diff
@@ -536,6 +578,17 @@ const (
 	WorkloadDeactivationTarget = "DeactivationTarget"
 )
 
+// Detailed Reasons for the WorkloadEvicted condition due to PodsReadyTimeout.
+const (
+	// StartupTimeoutReason is the exceedance of the maximum time allowed for a workload
+	// to transition from admission to pods being ready.
+	StartupTimeoutReason string = "StartupTimeout"
```

  I'm wondering about reusing the same reasons as "reasons" of the PodsReady condition: WaitForStart & WaitForRecovery. Would it be more or less work?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T13:37:24Z

- **apis/kueue/v1beta1/workload_types.go:391** by [@mimowo](https://github.com/mimowo) — 2025-05-19T13:37:24Z
```diff
@@ -387,6 +387,48 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// schedulingStats tracks scheduling statistics (e.g. evictionStates)
```

  ```suggestion
  	// schedulingStats tracks scheduling statistics
  ```

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-19T15:11:20Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@mszadkow](https://github.com/mszadkow) — 2025-05-19T15:11:20Z
```diff
@@ -536,6 +578,17 @@ const (
 	WorkloadDeactivationTarget = "DeactivationTarget"
 )
 
+// Detailed Reasons for the WorkloadEvicted condition due to PodsReadyTimeout.
+const (
+	// StartupTimeoutReason is the exceedance of the maximum time allowed for a workload
+	// to transition from admission to pods being ready.
+	StartupTimeoutReason string = "StartupTimeout"
```

  I think they mean the same, we can use it easily.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T15:48:17Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@mimowo](https://github.com/mimowo) — 2025-05-19T15:48:17Z
```diff
@@ -536,6 +578,17 @@ const (
 	WorkloadDeactivationTarget = "DeactivationTarget"
 )
 
+// Detailed Reasons for the WorkloadEvicted condition due to PodsReadyTimeout.
+const (
+	// StartupTimeoutReason is the exceedance of the maximum time allowed for a workload
+	// to transition from admission to pods being ready.
+	StartupTimeoutReason string = "StartupTimeout"
```

  wdyt @tenzen-y , I would probably suggest to use the same values as in the reason, to avoid the unnecessary mapping

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T06:57:11Z

- **apis/kueue/v1beta1/workload_types.go:391** by [@mimowo](https://github.com/mimowo) — 2025-05-20T06:57:11Z
```diff
@@ -387,6 +387,48 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// schedulingStats tracks scheduling statistics (e.g. evictionStates)
```

  I suggested to remove because the field is no longer called evictionStates. However, feel free to adjust the comment according to recent API changes. For example e.g. eviction.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T06:59:14Z

- **test/util/e2e.go:344** by [@mimowo](https://github.com/mimowo) — 2025-05-20T06:59:14Z
```diff
@@ -341,7 +341,7 @@ func GetKueueMetrics(ctx context.Context, cfg *rest.Config, restClient *rest.RES
 	metricsOutput, _, err := KExecute(ctx, cfg, restClient, configapi.DefaultNamespace, curlPodName, curlContainerName, []string{
 		"/bin/sh", "-c",
 		fmt.Sprintf(
-			"curl -s -k -H \"Authorization: Bearer $(cat /var/run/secrets/kubernetes.io/serviceaccount/token)\" https://%s.%s.svc.cluster.local:8443/metrics",
+			"curl --connect-timeout 10 --max-time 30 -s -k -H \"Authorization: Bearer $(cat /var/run/secrets/kubernetes.io/serviceaccount/token)\" https://%s.%s.svc.cluster.local:8443/metrics",
```

  I proposed separate PR because I assumed this is a fix for flakes, but please clarify what this is for. If indeed for flakes, then we merge separately and cherrypick.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T06:59:27Z

- **apis/kueue/v1beta1/workload_types.go:391** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T06:59:27Z
```diff
@@ -387,6 +387,48 @@ type WorkloadStatus struct {
 	//
 	// +optional
 	AccumulatedPastExexcutionTimeSeconds *int32 `json:"accumulatedPastExexcutionTimeSeconds,omitempty"`
+
+	// schedulingStats tracks scheduling statistics (e.g. evictionStates)
```

  Removed

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T06:59:39Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T06:59:39Z
```diff
@@ -536,6 +578,17 @@ const (
 	WorkloadDeactivationTarget = "DeactivationTarget"
 )
 
+// Detailed Reasons for the WorkloadEvicted condition due to PodsReadyTimeout.
+const (
+	// StartupTimeoutReason is the exceedance of the maximum time allowed for a workload
+	// to transition from admission to pods being ready.
+	StartupTimeoutReason string = "StartupTimeout"
```

  done

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T07:01:17Z

- **test/util/e2e.go:344** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T07:01:17Z
```diff
@@ -341,7 +341,7 @@ func GetKueueMetrics(ctx context.Context, cfg *rest.Config, restClient *rest.RES
 	metricsOutput, _, err := KExecute(ctx, cfg, restClient, configapi.DefaultNamespace, curlPodName, curlContainerName, []string{
 		"/bin/sh", "-c",
 		fmt.Sprintf(
-			"curl -s -k -H \"Authorization: Bearer $(cat /var/run/secrets/kubernetes.io/serviceaccount/token)\" https://%s.%s.svc.cluster.local:8443/metrics",
+			"curl --connect-timeout 10 --max-time 30 -s -k -H \"Authorization: Bearer $(cat /var/run/secrets/kubernetes.io/serviceaccount/token)\" https://%s.%s.svc.cluster.local:8443/metrics",
```

  In case of timeout we don't wait 2 minutes rather 30 seconds.
  I will add it in separate PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T07:08:17Z

- **test/util/e2e.go:357** by [@mimowo](https://github.com/mimowo) — 2025-05-20T07:08:17Z
```diff
@@ -354,7 +354,7 @@ func ExpectMetricsToBeAvailable(ctx context.Context, cfg *rest.Config, restClien
 		metricsOutput, err := GetKueueMetrics(ctx, cfg, restClient, curlPodName, curlContainerName)
 		g.Expect(err).NotTo(gomega.HaveOccurred())
 		g.Expect(metricsOutput).Should(utiltesting.ContainMetrics(metrics))
-	}, Timeout).Should(gomega.Succeed())
+	}, LongTimeout).Should(gomega.Succeed())
```

  Is this a fix for flake? If so please move to a separate PR (same with the other change in the file).

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T07:21:10Z

- **test/util/e2e.go:357** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T07:21:10Z
```diff
@@ -354,7 +354,7 @@ func ExpectMetricsToBeAvailable(ctx context.Context, cfg *rest.Config, restClien
 		metricsOutput, err := GetKueueMetrics(ctx, cfg, restClient, curlPodName, curlContainerName)
 		g.Expect(err).NotTo(gomega.HaveOccurred())
 		g.Expect(metricsOutput).Should(utiltesting.ContainMetrics(metrics))
-	}, Timeout).Should(gomega.Succeed())
+	}, LongTimeout).Should(gomega.Succeed())
```

  ok, I am fine with that

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T07:30:55Z

- **test/util/e2e.go:357** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T07:30:54Z
```diff
@@ -354,7 +354,7 @@ func ExpectMetricsToBeAvailable(ctx context.Context, cfg *rest.Config, restClien
 		metricsOutput, err := GetKueueMetrics(ctx, cfg, restClient, curlPodName, curlContainerName)
 		g.Expect(err).NotTo(gomega.HaveOccurred())
 		g.Expect(metricsOutput).Should(utiltesting.ContainMetrics(metrics))
-	}, Timeout).Should(gomega.Succeed())
+	}, LongTimeout).Should(gomega.Succeed())
```

  moved to separate PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T08:01:10Z

- **pkg/controller/core/workload_controller.go:876** by [@mimowo](https://github.com/mimowo) — 2025-05-20T08:01:10Z
```diff
@@ -836,37 +853,40 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.Conf
 		Complete(WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
 }
 
-// admittedNotReadyWorkload returns as a pair of values. The first boolean determines
-// if the workload is currently counting towards the timeout for PodsReady, i.e.
-// it has the Admitted condition True and the PodsReady condition not equal
-// True (False or not set). The second value is the remaining time to exceed the
-// specified timeout counted since max of the LastTransitionTime's for the
-// Admitted and PodsReady conditions.
-func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool, time.Duration) {
-	if r.waitForPodsReady == nil {
-		// the timeout is not configured for the workload controller
-		return false, 0
-	}
-	if !workload.IsAdmitted(wl) {
-		// the workload is not admitted so there is no need to time it out
-		return false, 0
+// admittedNotReadyWorkload checks if a workload counts toward the PodsReady timeout
+// and calculates the remaining timeout duration.
+//
+// It returns two values:
+//  1. A underlyingCause that complements informarion carried by kueue.WorkloadEvictedByPodsReadyTimeout
+//
+// (e.g., WaitForStart, WaitForRecovery, or empty if not applicable).
+//
+//  2. The remaining time (in seconds) until the timeout is exceeded, based on
+//     the maximum of the LastTransitionTime for the Admitted and PodsReady conditions.
+//
+// If the workload is not admitted, PodsReady is true, or no timeout is configured,
+// it returns an empty underlyingCause and zero duration.
+func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (string, time.Duration) {
+	// Return early if timeout is not configured or workload is not admitted
+	if r.waitForPodsReady == nil || !workload.IsAdmitted(wl) {
+		return "", 0
```

  This looks like a useful optimization, but is this necessary for the PR? If all tests continue to pass I would suggest to drop the optimization. Let's focus on the metric, making the diff smaller will give us more confidence into correctness. If this was a bug it warrants a separate PR which we can cherry-pick.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T08:01:42Z

- **pkg/controller/core/workload_controller.go:880** by [@mimowo](https://github.com/mimowo) — 2025-05-20T08:01:42Z
```diff
@@ -836,37 +853,40 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.Conf
 		Complete(WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
 }
 
-// admittedNotReadyWorkload returns as a pair of values. The first boolean determines
-// if the workload is currently counting towards the timeout for PodsReady, i.e.
-// it has the Admitted condition True and the PodsReady condition not equal
-// True (False or not set). The second value is the remaining time to exceed the
-// specified timeout counted since max of the LastTransitionTime's for the
-// Admitted and PodsReady conditions.
-func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool, time.Duration) {
-	if r.waitForPodsReady == nil {
-		// the timeout is not configured for the workload controller
-		return false, 0
-	}
-	if !workload.IsAdmitted(wl) {
-		// the workload is not admitted so there is no need to time it out
-		return false, 0
+// admittedNotReadyWorkload checks if a workload counts toward the PodsReady timeout
+// and calculates the remaining timeout duration.
+//
+// It returns two values:
+//  1. A underlyingCause that complements informarion carried by kueue.WorkloadEvictedByPodsReadyTimeout
+//
+// (e.g., WaitForStart, WaitForRecovery, or empty if not applicable).
+//
+//  2. The remaining time (in seconds) until the timeout is exceeded, based on
+//     the maximum of the LastTransitionTime for the Admitted and PodsReady conditions.
+//
+// If the workload is not admitted, PodsReady is true, or no timeout is configured,
+// it returns an empty underlyingCause and zero duration.
+func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (string, time.Duration) {
+	// Return early if timeout is not configured or workload is not admitted
+	if r.waitForPodsReady == nil || !workload.IsAdmitted(wl) {
+		return "", 0
 	}
 
 	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
-		return false, 0
+		return "", 0
 	}
 
-	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForStart || podsReadyCond.Reason == "PodsReady" {
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForStart || podsReadyCond.Reason == kueue.WorkloadPodsReady {
```

  I suggest to revert any drive-by cleanups and focus on the metric. We can have a separate PR for this.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T08:55:22Z

- **pkg/controller/core/workload_controller.go:880** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T08:55:22Z
```diff
@@ -836,37 +853,40 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.Conf
 		Complete(WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
 }
 
-// admittedNotReadyWorkload returns as a pair of values. The first boolean determines
-// if the workload is currently counting towards the timeout for PodsReady, i.e.
-// it has the Admitted condition True and the PodsReady condition not equal
-// True (False or not set). The second value is the remaining time to exceed the
-// specified timeout counted since max of the LastTransitionTime's for the
-// Admitted and PodsReady conditions.
-func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool, time.Duration) {
-	if r.waitForPodsReady == nil {
-		// the timeout is not configured for the workload controller
-		return false, 0
-	}
-	if !workload.IsAdmitted(wl) {
-		// the workload is not admitted so there is no need to time it out
-		return false, 0
+// admittedNotReadyWorkload checks if a workload counts toward the PodsReady timeout
+// and calculates the remaining timeout duration.
+//
+// It returns two values:
+//  1. A underlyingCause that complements informarion carried by kueue.WorkloadEvictedByPodsReadyTimeout
+//
+// (e.g., WaitForStart, WaitForRecovery, or empty if not applicable).
+//
+//  2. The remaining time (in seconds) until the timeout is exceeded, based on
+//     the maximum of the LastTransitionTime for the Admitted and PodsReady conditions.
+//
+// If the workload is not admitted, PodsReady is true, or no timeout is configured,
+// it returns an empty underlyingCause and zero duration.
+func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (string, time.Duration) {
+	// Return early if timeout is not configured or workload is not admitted
+	if r.waitForPodsReady == nil || !workload.IsAdmitted(wl) {
+		return "", 0
 	}
 
 	podsReadyCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady)
 	if podsReadyCond != nil && podsReadyCond.Status == metav1.ConditionTrue {
-		return false, 0
+		return "", 0
 	}
 
-	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForStart || podsReadyCond.Reason == "PodsReady" {
+	if podsReadyCond == nil || podsReadyCond.Reason == kueue.WorkloadWaitForStart || podsReadyCond.Reason == kueue.WorkloadPodsReady {
```

  ok, I accept that

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T08:55:36Z

- **pkg/controller/core/workload_controller.go:876** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T08:55:36Z
```diff
@@ -836,37 +853,40 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.Conf
 		Complete(WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
 }
 
-// admittedNotReadyWorkload returns as a pair of values. The first boolean determines
-// if the workload is currently counting towards the timeout for PodsReady, i.e.
-// it has the Admitted condition True and the PodsReady condition not equal
-// True (False or not set). The second value is the remaining time to exceed the
-// specified timeout counted since max of the LastTransitionTime's for the
-// Admitted and PodsReady conditions.
-func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (bool, time.Duration) {
-	if r.waitForPodsReady == nil {
-		// the timeout is not configured for the workload controller
-		return false, 0
-	}
-	if !workload.IsAdmitted(wl) {
-		// the workload is not admitted so there is no need to time it out
-		return false, 0
+// admittedNotReadyWorkload checks if a workload counts toward the PodsReady timeout
+// and calculates the remaining timeout duration.
+//
+// It returns two values:
+//  1. A underlyingCause that complements informarion carried by kueue.WorkloadEvictedByPodsReadyTimeout
+//
+// (e.g., WaitForStart, WaitForRecovery, or empty if not applicable).
+//
+//  2. The remaining time (in seconds) until the timeout is exceeded, based on
+//     the maximum of the LastTransitionTime for the Admitted and PodsReady conditions.
+//
+// If the workload is not admitted, PodsReady is true, or no timeout is configured,
+// it returns an empty underlyingCause and zero duration.
+func (r *WorkloadReconciler) admittedNotReadyWorkload(wl *kueue.Workload) (string, time.Duration) {
+	// Return early if timeout is not configured or workload is not admitted
+	if r.waitForPodsReady == nil || !workload.IsAdmitted(wl) {
+		return "", 0
```

  np, done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T14:32:20Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T14:32:20Z
```diff
@@ -536,6 +578,17 @@ const (
 	WorkloadDeactivationTarget = "DeactivationTarget"
 )
 
+// Detailed Reasons for the WorkloadEvicted condition due to PodsReadyTimeout.
+const (
+	// StartupTimeoutReason is the exceedance of the maximum time allowed for a workload
+	// to transition from admission to pods being ready.
+	StartupTimeoutReason string = "StartupTimeout"
```

  > wdyt @tenzen-y , I would probably suggest to use the same values as in the reason, to avoid the unnecessary mapping
  
  +1, I want to avoid the additional cross mappings.
