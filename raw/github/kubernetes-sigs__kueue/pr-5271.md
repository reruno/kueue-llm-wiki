# PR #5271: docs: add object retention page

**Summary**: docs: add object retention page

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5271

**Last updated**: 2025-06-05T10:11:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `docs/setup-object-retention` → `main`
- **Created**: 2025-05-16T12:35:59Z
- **Updated**: 2025-06-05T10:11:13Z
- **Merged**: 2025-06-05T09:12:38Z
- **Closed**: 2025-06-05T09:12:38Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 1   **+216 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add a documentation page for the object retention functionality
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5261 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-16T12:36:02Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-16T12:36:06Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0c52a325ccde375e7099333045b24d2c408e0b5a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68415170909b4200086ce1cf |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-5271--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-5271--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTUyNzEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.7G8QTKPrCRnAYPNOjHZ9RgpboC-1ompJd7j00rgVEUo)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-5271--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-19T13:17:44Z

cc @mwysokin would you like to give the first pass?

### Comment by [@mwysokin](https://github.com/mwysokin) — 2025-05-22T19:53:35Z

Sorry for the delay. I'll have time to review this on Friday (tomorrow).

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-29T08:34:54Z

cc @mszadkow @mbobrovskyi it will be great if you can also give it a pass

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T08:55:47Z

/lgtm
/approve
as the remaining comments are addressed, thank you 👍 
/cherrypick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-05T08:55:49Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5271#issuecomment-2943325294):

>/lgtm
>/approve
>as the remaining comments are addressed, thank you 👍 
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-05T08:55:54Z

LGTM label has been added.  <details>Git tree hash: 820f44fece8d856a465c49a807836474307ff36a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-05T08:55:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5271#issuecomment-2943325294" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5271#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-05T09:13:17Z

@mimowo: new pull request created: #5515

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5271#issuecomment-2943325294):

>/lgtm
>/approve
>as the remaining comments are addressed, thank you 👍 
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-05-16T17:16:35Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:2** by [@kannon92](https://github.com/kannon92) — 2025-05-16T17:16:35Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup object retention"
```

  ```suggestion
  title: "Setup garbage-collection of workloads"
  ```
  
  "Setup object retention" is not very descriptive for user docs.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-19T07:19:47Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:2** by [@mykysha](https://github.com/mykysha) — 2025-05-19T07:19:47Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup object retention"
```

  Sure, have updated the title

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-27T14:22:55Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:42** by [@mimowo](https://github.com/mimowo) — 2025-05-27T14:22:55Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
```

  I don't think we need this. This should be discussed in the referenced part of the doc: (/docs/installation#install-a-custom-configured-released-version

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-27T14:26:12Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:45** by [@mimowo](https://github.com/mimowo) — 2025-05-27T14:26:12Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
+{{% /alert %}}
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
+It contains the following optional fields:
+- `afterFinished`: Duration after which finished Workloads are deleted.
+- `afterDeactivatedByKueue`: Duration after which any Kueue-deactivated Workloads (such as a Job, JobSet, or other custom workload types) are deleted.
+
+
+## Example
+
```

  The example here isn't really example currently. 
  Maybe in this example propose Kueue configuration which is using the delay of 1min, and also configure waitforPodsReady. Then describe 2 scenarios:
  1. workload happily finished and collected
  2. workload which hits waitForPodsReady and gets deactivated. For example, set up Kueue quota bigger than the amount of physical not capacity, to simulate node failure. 
  
  wdyt @mwysokin ?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-27T14:28:40Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:42** by [@mykysha](https://github.com/mykysha) — 2025-05-27T14:28:40Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
```

  Surely, will remove this part. Was using waitforpodsready doc as an example, which has this section as well

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-27T14:32:02Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:42** by [@mimowo](https://github.com/mimowo) — 2025-05-27T14:32:02Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
```

  I see, I guess this is old style, I think it is ok to keep it in one place only

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-27T14:32:18Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:42** by [@mykysha](https://github.com/mykysha) — 2025-05-27T14:32:18Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
```

  Removed the alert from the doc

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-28T10:28:33Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:45** by [@mimowo](https://github.com/mimowo) — 2025-05-28T10:28:33Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
+{{% /alert %}}
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
+It contains the following optional fields:
+- `afterFinished`: Duration after which finished Workloads are deleted.
+- `afterDeactivatedByKueue`: Duration after which any Kueue-deactivated Workloads (such as a Job, JobSet, or other custom workload types) are deleted.
+
+
+## Example
+
```

  Assuming no response from @mwysokin I would structure the example as proposed. Please adjust the doc.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-29T09:24:17Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:12** by [@mszadkow](https://github.com/mszadkow) — 2025-05-29T09:24:17Z
```diff
@@ -0,0 +1,73 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
```

  not sure we have to mention etcd, or maybe make a direct link between Kueue and etc, as this is more indirect

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-29T09:24:50Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:45** by [@mszadkow](https://github.com/mszadkow) — 2025-05-29T09:24:50Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
+{{% /alert %}}
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
+It contains the following optional fields:
+- `afterFinished`: Duration after which finished Workloads are deleted.
+- `afterDeactivatedByKueue`: Duration after which any Kueue-deactivated Workloads (such as a Job, JobSet, or other custom workload types) are deleted.
+
+
+## Example
+
```

  IMO what is missing is the good example as mentioned.
  other than that looks good

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-30T11:29:34Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:45** by [@mykysha](https://github.com/mykysha) — 2025-05-30T11:29:34Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
+{{% /alert %}}
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
+It contains the following optional fields:
+- `afterFinished`: Duration after which finished Workloads are deleted.
+- `afterDeactivatedByKueue`: Duration after which any Kueue-deactivated Workloads (such as a Job, JobSet, or other custom workload types) are deleted.
+
+
+## Example
+
```

  Reworked the example here, wondering whether this is understandable enough or whether I should add yaml for the resources to be created, so that it would be guaranteed to work and demonstrate what is intended?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-05-30T11:30:51Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:12** by [@mykysha](https://github.com/mykysha) — 2025-05-30T11:30:51Z
```diff
@@ -0,0 +1,73 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
```

  Changed the description to "cluster", hope it makes it more understandable and relevant

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-06-02T08:44:32Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:45** by [@mszadkow](https://github.com/mszadkow) — 2025-06-02T08:44:32Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
+{{% /alert %}}
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
+It contains the following optional fields:
+- `afterFinished`: Duration after which finished Workloads are deleted.
+- `afterDeactivatedByKueue`: Duration after which any Kueue-deactivated Workloads (such as a Job, JobSet, or other custom workload types) are deleted.
+
+
+## Example
+
```

  I think scenario B step 2 can be a common pre-step.
  Then for A you can link simple-job.yaml and for B add modifications.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-03T15:43:35Z

LGTM overall, thank you 👍

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:7** by [@mimowo](https://github.com/mimowo) — 2025-06-03T15:40:24Z
```diff
@@ -0,0 +1,134 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
```

  ```suggestion
    by defining retention policies.
  ```

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:32** by [@mimowo](https://github.com/mimowo) — 2025-06-03T15:41:34Z
```diff
@@ -0,0 +1,134 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in cluster
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1m"
+          afterDeactivatedByKueue: "1m"
```

  ```suggestion
            afterFinished: "1h"
            afterDeactivatedByKueue: "1h"
  ```
  I think I would recommend a bit higher values as default.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-04T09:56:22Z

Otherwise LGTM
Thank you

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:38** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T09:49:45Z
```diff
@@ -0,0 +1,134 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in cluster
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1m"
+          afterDeactivatedByKueue: "1m"
+```
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
```

  ```suggestion
  `.objectRetentionPolicies.workloads` field.
  ```
  
  Let's represent the `.` as a root.

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:210** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T09:56:08Z
```diff
@@ -0,0 +1,134 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in cluster
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1m"
+          afterDeactivatedByKueue: "1m"
+```
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
+It contains the following optional fields:
+- `afterFinished`: Duration after which finished Workloads are deleted.
+- `afterDeactivatedByKueue`: Duration after which any Kueue-deactivated Workloads (such as a Job, JobSet, or other custom workload types) are deleted.
+
+
+## Example
+
+---
+
+### Scenario A: Successfully finished Workload
+
+1. **Configure** Kueue with a 1m retention policy:
+
+```yaml
+  objectRetentionPolicies:
+    workloads:
+      afterFinished: "1m"
+```
+
+2. **Submit** a simple Workload that should finish normally:
+
+```bash
+   kubectl apply -f successful-workload.yaml
+   kubectl get workloads --all-namespaces
+```
+
+3. Watch the status transition to `Finished`. After ~1 minute, Kueue will automatically delete it:
+
+```bash
+   # ~1m after Finished
+   kubectl get workloads --all-namespaces
+   # <successful-workload not found>
+```
+
+---
+
+### Scenario B: Evicted Workload via `waitForPodsReady`
+
+1. **Configure** Kueue [deployment](/docs/installation#install-a-custom-configured-released-version) to have more resources available than the node can provide:
+
+```yaml
+        resources:
+          limits:
+            cpu: "100" # or any value greater than the node's capacity
+```
+
+2. **Configure** Kueue with a 1m retention policy and enable [waitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready.md):
+
+```yaml
+  objectRetentionPolicies:
+    workloads:
+      afterDeactivatedByKueue: "1m"
+  waitForPodsReady:
+    enable: true
+    timeout: 2m
+    recoveryTimeout: 1m
+    blockAdmission: true
+    requeuingStrategy:
+      backoffLimitCount: 0
+```
+
+3. **Submit** a Workload that requests more than is available on the node:
+
+```bash
+   kubectl apply -f limited-workload.yaml
+   kubectl get workloads --all-namespaces
+```
+
+4. Without all Pods ready, Kueue evicts and deactivates the Workload:
+
+```bash
+   kubectl get workloads --all-namespaces
+   # NAME                    QUEUE    RESERVED IN   ADMITTED   FINISHED   AGE
+   # limited-workload                               False                 2m
+```
+
+5. ~1 minute after eviction, the deactivated Workload is garbage-collected:
+
+```bash
+   # ~1m after eviction
+   kubectl get workloads --all-namespaces
+   # <limited-workload not found>
+```
+
+## Notes
+
+- `afterDeactivatedByKueue` is the duration to wait after *any* Kueue-managed Workload
+  (such as a Job, JobSet, or other custom workload types)
+  has been marked as deactivated by Kueue before automatically deleting it.
+  Deletion of deactivated workloads may cascade to objects not created by Kueue,
+  since deleting the parent Workload owner (e.g. JobSet) can trigger garbage-collection of dependent resources.
```

  ```suggestion
    since deleting the parent Workload owner (e.g. JobSet) can trigger garbage-collection of dependent resources.
    If you manually / automatically deactivate the Workload by specifying `.spec.active=false`, `afterDeactivatedByKueue` is _NOT_ effective.
  ```
  
  Could you mention that the `afterDeactivatedByKueue` is not effective by disabling the active in the spec field?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-06-05T08:15:07Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:45** by [@mykysha](https://github.com/mykysha) — 2025-06-05T08:15:06Z
```diff
@@ -0,0 +1,83 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in etcd
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1h"
+          afterDeactivatedByKueue: "1h"
+```
+
+{{% alert title="Note" color="primary" %}}
+If you update an existing Kueue installation you may need to restart the
+`kueue-controller-manager` pod in order for Kueue to pick up the updated
+configuration. In that case run:
+
+```shell
+kubectl delete pods --all -n kueue-system
+```
+{{% /alert %}}
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
+It contains the following optional fields:
+- `afterFinished`: Duration after which finished Workloads are deleted.
+- `afterDeactivatedByKueue`: Duration after which any Kueue-deactivated Workloads (such as a Job, JobSet, or other custom workload types) are deleted.
+
+
+## Example
+
```

  Added examples for job creation for both cases, and moved kueue-config to a separate pre-step

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-06-05T08:15:31Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:7** by [@mykysha](https://github.com/mykysha) — 2025-06-05T08:15:31Z
```diff
@@ -0,0 +1,134 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-06-05T08:15:48Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:32** by [@mykysha](https://github.com/mykysha) — 2025-06-05T08:15:48Z
```diff
@@ -0,0 +1,134 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in cluster
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1m"
+          afterDeactivatedByKueue: "1m"
```

  Surely, done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-06-05T08:16:10Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:38** by [@mykysha](https://github.com/mykysha) — 2025-06-05T08:16:10Z
```diff
@@ -0,0 +1,134 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in cluster
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1m"
+          afterDeactivatedByKueue: "1m"
+```
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
```

  Surely, added the dot here

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-06-05T08:16:41Z

- **site/content/en/docs/tasks/manage/setup_object_retention_policy.md:210** by [@mykysha](https://github.com/mykysha) — 2025-06-05T08:16:41Z
```diff
@@ -0,0 +1,134 @@
+---
+title: "Setup garbage-collection of workload"
+date: 2025-05-16
+weight: 11
+description: >
+  Configure automatic garbage-collection of finished and deactivated Workloads
+  by defining retention policies in your KueueConfiguration.
+---
+
+This guide shows you how to enable and configure optional object retention
+policies in Kueue to automatically delete finished or deactivated Workloads
+after a specified time. By default, Kueue leaves all Workload objects in cluster
+indefinitely; with object retention you can free up etcd storage and reduce
+Kueue’s memory footprint.
+
+## Prerequisites
+
+- A running Kueue installation at **v0.12** or newer.
+- The `ObjectRetentionPolicies` feature-gate enabled in the Kueue controller manager. Check the [Installation](/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
+
+## Set up a retention policy
+
+Follow the instructions described
+[here](/docs/installation#install-a-custom-configured-released-version) to
+install a release version by extending the configuration with the following
+fields:
+
+```yaml
+      objectRetentionPolicies:
+        workloads:
+          afterFinished: "1m"
+          afterDeactivatedByKueue: "1m"
+```
+
+### Workload Retention Policy
+
+The retention policy for Workloads is defined in the
+`objectRetentionPolicies.workloads` field.
+It contains the following optional fields:
+- `afterFinished`: Duration after which finished Workloads are deleted.
+- `afterDeactivatedByKueue`: Duration after which any Kueue-deactivated Workloads (such as a Job, JobSet, or other custom workload types) are deleted.
+
+
+## Example
+
+---
+
+### Scenario A: Successfully finished Workload
+
+1. **Configure** Kueue with a 1m retention policy:
+
+```yaml
+  objectRetentionPolicies:
+    workloads:
+      afterFinished: "1m"
+```
+
+2. **Submit** a simple Workload that should finish normally:
+
+```bash
+   kubectl apply -f successful-workload.yaml
+   kubectl get workloads --all-namespaces
+```
+
+3. Watch the status transition to `Finished`. After ~1 minute, Kueue will automatically delete it:
+
+```bash
+   # ~1m after Finished
+   kubectl get workloads --all-namespaces
+   # <successful-workload not found>
+```
+
+---
+
+### Scenario B: Evicted Workload via `waitForPodsReady`
+
+1. **Configure** Kueue [deployment](/docs/installation#install-a-custom-configured-released-version) to have more resources available than the node can provide:
+
+```yaml
+        resources:
+          limits:
+            cpu: "100" # or any value greater than the node's capacity
+```
+
+2. **Configure** Kueue with a 1m retention policy and enable [waitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready.md):
+
+```yaml
+  objectRetentionPolicies:
+    workloads:
+      afterDeactivatedByKueue: "1m"
+  waitForPodsReady:
+    enable: true
+    timeout: 2m
+    recoveryTimeout: 1m
+    blockAdmission: true
+    requeuingStrategy:
+      backoffLimitCount: 0
+```
+
+3. **Submit** a Workload that requests more than is available on the node:
+
+```bash
+   kubectl apply -f limited-workload.yaml
+   kubectl get workloads --all-namespaces
+```
+
+4. Without all Pods ready, Kueue evicts and deactivates the Workload:
+
+```bash
+   kubectl get workloads --all-namespaces
+   # NAME                    QUEUE    RESERVED IN   ADMITTED   FINISHED   AGE
+   # limited-workload                               False                 2m
+```
+
+5. ~1 minute after eviction, the deactivated Workload is garbage-collected:
+
+```bash
+   # ~1m after eviction
+   kubectl get workloads --all-namespaces
+   # <limited-workload not found>
+```
+
+## Notes
+
+- `afterDeactivatedByKueue` is the duration to wait after *any* Kueue-managed Workload
+  (such as a Job, JobSet, or other custom workload types)
+  has been marked as deactivated by Kueue before automatically deleting it.
+  Deletion of deactivated workloads may cascade to objects not created by Kueue,
+  since deleting the parent Workload owner (e.g. JobSet) can trigger garbage-collection of dependent resources.
```

  Great catch, added your suggestion

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-05T10:07:59Z

LGTM
thank you
