# PR #5272: TAS error handling: workload eviction due to multiple failures

**Summary**: TAS error handling: workload eviction due to multiple failures

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5272

**Last updated**: 2025-05-22T12:57:28Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `tas_multiple_errors` → `main`
- **Created**: 2025-05-16T12:41:36Z
- **Updated**: 2025-05-22T12:57:28Z
- **Merged**: 2025-05-22T10:58:35Z
- **Closed**: 2025-05-22T10:58:36Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 7   **+330 / -93**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
When multiple nodes fail we want to (at least for now) evict the workload instead of looking for replacement.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes part of #5230

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-16T12:41:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d390cc5ca1bcbd331742101bfbb418a199e5e943 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/682ee151e0143600085eca71 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-16T12:41:45Z

Hi @pajakd. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-19T12:22:28Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-20T06:26:50Z

@PBundyra @pajakd I would suggest you do "peer buddy review" to each other PRs (https://github.com/kubernetes-sigs/kueue/pull/5287 is the other one).  cc @gabesaba if you could also give a pass.

I will try to look into this later today or tomorrow.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-20T08:12:13Z

/assign

### Comment by [@pajakd](https://github.com/pajakd) — 2025-05-21T13:05:50Z

/test pull-kueue-test-e2e-customconfigs-main
this test is known to be flaky (see https://github.com/kubernetes-sigs/kueue/issues/5302)

### Comment by [@pajakd](https://github.com/pajakd) — 2025-05-21T13:28:00Z

/retest

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-05-21T15:04:50Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-21T15:04:59Z

LGTM label has been added.  <details>Git tree hash: d237e22287bc35d5404c812d090df1d9f6809ed1</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T10:43:58Z

/lgtm
/assign @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-22T10:44:03Z

LGTM label has been added.  <details>Git tree hash: 31146364a8e2193b1708062a8f30b5693b890cd2</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-22T10:56:45Z

/lgtm
/approve
We can handle the nit in a follow up, I don't see any outstanding issues.

I believe merging this will already help to work on the second pass logic in https://github.com/kubernetes-sigs/kueue/pull/5287.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-22T10:56:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5272#issuecomment-2900789753" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5272#" title="Author self-approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-20T08:18:18Z

- **pkg/controller/tas/node_failure_controller.go:247** by [@PBundyra](https://github.com/PBundyra) — 2025-05-20T08:18:18Z
```diff
@@ -214,3 +234,15 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 	}
 	return nil
 }
+
+func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload) error {
+	message := "Workload eviction triggered due to multiple TAS assigned node failures"
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedDueToTASNodeFailures, message)
+	workload.ResetChecksOnEviction(wl, r.clock.Now())
+	err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+	if err == nil {
+		cqName := wl.Status.Admission.ClusterQueue
+		workload.ReportEvictedWorkload(r.recorder, wl, cqName, kueue.WorkloadEvictedDueToTASNodeFailures, message)
+	}
+	return err
```

  ```suggestion
  	if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock); err != nil {
  	      return err
  	}
  	cqName := wl.Status.Admission.ClusterQueue
  	workload.ReportEvictedWorkload(r.recorder, wl, cqName, kueue.WorkloadEvictedDueToTASNodeFailures, message)
  	return nil
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-20T08:20:00Z

- **pkg/controller/tas/node_failure_controller.go:210** by [@PBundyra](https://github.com/PBundyra) — 2025-05-20T08:20:00Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
+		if currentAnnotations == nil {
+			currentAnnotations = make(map[string]string)
+		}
+		existingFailedNode, annotationPresent := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
+		if annotationPresent && existingFailedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+		}
+		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
```

  Do I understand correctly that here we do
  1. PATCH to evict the workload
  2. GET to get the synced version of the workload
  3. PATCH to delete annotation?
  
  If so, can we keep all changes within one PATCH request to reduce number of requests?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-20T08:26:44Z

- **pkg/controller/tas/node_failure_controller_test.go:184** by [@PBundyra](https://github.com/PBundyra) — 2025-05-20T08:26:44Z
```diff
@@ -79,90 +79,117 @@ func TestNodeFailureReconciler(t *testing.T) {
 		Admitted(true).
 		Obj()
 
+	workloadWithTwoNodes := utiltesting.MakeWorkload(wlName, nsName).
+		Finalizers(kueue.ResourceInUseFinalizerName).
+		PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 2).Request(corev1.ResourceCPU, "1").Obj()).
+		ReserveQuota(
+			utiltesting.MakeAdmission("cq").
+				Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+				AssignmentPodCount(2).
+				TopologyAssignment(&kueue.TopologyAssignment{
+					Levels: []string{corev1.LabelHostname},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{nodeName}},
+						{Count: 1, Values: []string{nodeName2}},
+					},
+				}).
+				Obj(),
+		).
+		Admitted(true).
+		Obj()
+
 	basePod := testingpod.MakePod("test-pod", nsName).
 		Annotation(kueuealpha.WorkloadAnnotation, wlName).
 		Label(kueuealpha.TASLabel, "true").
 		NodeName(nodeName).
 		Obj()
 
 	tests := map[string]struct {
-		initObjs       []client.Object
-		req            reconcile.Request
-		advanceClock   time.Duration
-		wantFailedNode string
-		wantRequeue    time.Duration
+		initObjs          []client.Object
+		reconcileRequests []reconcile.Request
+		advanceClock      time.Duration
+		wantFailedNode    string
+		wantRequeue       time.Duration
+		wantEvicted       bool
 	}{
+		"Node Found and Healthy - not marked as unavailable": {
+			initObjs: []client.Object{
+				newNodeTest(nodeName, corev1.ConditionTrue, fakeClock, time.Duration(0)),
+				baseWorkload.DeepCopy(),
+				basePod.DeepCopy(),
+			},
+			reconcileRequests: []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantFailedNode:    "",
+		},
 		"Node Found and Unhealthy (NotReady), delay not passed - not marked as unavailable": {
 			initObjs: []client.Object{
 				newNodeTest(nodeName, corev1.ConditionFalse, fakeClock, time.Duration(0)),
 				baseWorkload.DeepCopy(),
 				basePod.DeepCopy(),
 			},
-			req:            reconcile.Request{NamespacedName: types.NamespacedName{Name: nodeName}},
-			wantFailedNode: "",
-			wantRequeue:    NodeFailureDelay,
+			reconcileRequests: []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantFailedNode:    "",
+			wantRequeue:       NodeFailureDelay,
 		},
 		"Node Found and Unhealthy (NotReady), delay passed - marked as unavailable": {
 			initObjs: []client.Object{
 				newNodeTest(nodeName, corev1.ConditionFalse, fakeClock, NodeFailureDelay),
 				baseWorkload.DeepCopy(),
 				basePod.DeepCopy(),
 			},
-			req:            reconcile.Request{NamespacedName: types.NamespacedName{Name: nodeName}},
-			wantFailedNode: nodeName,
+			reconcileRequests: []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantFailedNode:    nodeName,
 		},
-		"Node Found and Healthy - not marked as unavailable": {
+		"Node Deleted - marked as unavailable": {
 			initObjs: []client.Object{
-				newNodeTest(nodeName, corev1.ConditionTrue, fakeClock, time.Duration(0)),
-				utiltesting.MakeWorkload(wlName, nsName).
-					Finalizers(kueue.ResourceInUseFinalizerName).
-					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
-					Admitted(true).
-					Obj(),
+				baseWorkload.DeepCopy(),
 				basePod.DeepCopy(),
 			},
-			req:            reconcile.Request{NamespacedName: types.NamespacedName{Name: nodeName}},
-			wantFailedNode: "",
+			reconcileRequests: []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantFailedNode:    nodeName,
 		},
-		"Node Deleted - marked as unavailable": {
+		"Two Nodes Unhealthy (NotReady), delay passed - workload evicted": {
 			initObjs: []client.Object{
-				baseWorkload.DeepCopy(),
-				basePod.DeepCopy(),
+				newNodeTest(nodeName, corev1.ConditionFalse, fakeClock, NodeFailureDelay),
+				newNodeTest(nodeName2, corev1.ConditionFalse, fakeClock, NodeFailureDelay),
+				workloadWithTwoNodes.DeepCopy(),
+				testingpod.MakePod("pod1", nsName).Annotation(kueuealpha.WorkloadAnnotation, wlName).Label(kueuealpha.TASLabel, "true").NodeName(nodeName).Obj(),
+				testingpod.MakePod("pod2", nsName).Annotation(kueuealpha.WorkloadAnnotation, wlName).Label(kueuealpha.TASLabel, "true").NodeName(nodeName2).Obj(),
+			},
+			reconcileRequests: []reconcile.Request{
+				{NamespacedName: types.NamespacedName{Name: nodeName}},
+				{NamespacedName: types.NamespacedName{Name: nodeName2}},
 			},
-			req:            reconcile.Request{NamespacedName: types.NamespacedName{Name: nodeName}},
-			wantFailedNode: nodeName,
+			wantFailedNode: "",
+			wantEvicted:    true,
 		},
 	}
-
 	for name, tc := range tests {
 		t.Run(name, func(t *testing.T) {
 			features.SetFeatureGateDuringTest(t, features.TASFailedNodeReplacement, true)
 			ctx := t.Context()
 			fakeClock.SetTime(testStartTime)
-			s := scheme.Scheme
-			if err := kueue.AddToScheme(s); err != nil {
-				t.Fatalf("Failed to add kueue scheme: %v", err)
-			}
-			if err := corev1.AddToScheme(s); err != nil {
-				t.Fatalf("Failed to add kueue scheme: %v", err)
-			}
 
-			clientBuilder := fake.NewClientBuilder().
-				WithScheme(s).
+			clientBuilder := utiltesting.NewClientBuilder().
 				WithObjects(tc.initObjs...).
-				WithStatusSubresource(tc.initObjs...)
+				WithStatusSubresource(tc.initObjs...).
+				WithInterceptorFuncs(interceptor.Funcs{SubResourcePatch: utiltesting.TreatSSAAsStrategicMerge})
```

  Why do we need this function?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-20T08:27:53Z

- **pkg/controller/tas/node_failure_controller_test.go:215** by [@PBundyra](https://github.com/PBundyra) — 2025-05-20T08:27:53Z
```diff
@@ -177,6 +204,19 @@ func TestNodeFailureReconciler(t *testing.T) {
 			if diff := cmp.Diff(tc.wantRequeue, result.RequeueAfter); diff != "" {
 				t.Errorf("Unexpected RequeueAfter (-want/+got):\n%s", diff)
 			}
+
+			if tc.wantEvicted {
+				foundEvicted := false
+				for _, cond := range wl.Status.Conditions {
+					if cond.Type == kueue.WorkloadEvicted && cond.Status == metav1.ConditionTrue && cond.Reason == kueue.WorkloadEvictedDueToTASNodeFailures {
+						foundEvicted = true
+						break
+					}
+				}
```

  Can we use `apimeta.IsStatusConditionTrue`?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-20T08:30:20Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@gabesaba](https://github.com/gabesaba) — 2025-05-20T07:56:57Z
```diff
@@ -580,6 +580,10 @@ const (
 	// because the LocalQueue is Stopped.
 	WorkloadEvictedByLocalQueueStopped = "LocalQueueStopped"
 
+	// WorkloadEvictedDueToTASNodeFailures indicates that the workload was evicted
+	// due to non-recoverable node failures in TAS.
+	WorkloadEvictedDueToTASNodeFailures = "WorkloadEvictedDueToTASNodeFailures"
```

  Is TAS important part of this message, or can we leave it more general?

- **pkg/controller/tas/node_failure_controller.go:194** by [@gabesaba](https://github.com/gabesaba) — 2025-05-20T08:06:21Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
```

  ```suggestion
  		annotations := wl.GetAnnotations()
  ```

- **pkg/controller/tas/node_failure_controller.go:198** by [@gabesaba](https://github.com/gabesaba) — 2025-05-20T08:07:07Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
+		if currentAnnotations == nil {
+			currentAnnotations = make(map[string]string)
+		}
+		existingFailedNode, annotationPresent := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
```

  ```suggestion
  		failedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
  ```

- **pkg/controller/tas/node_failure_controller.go:195** by [@gabesaba](https://github.com/gabesaba) — 2025-05-20T08:11:53Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
```

  line 183: `for wlKey := range workloadsToProcess {`

- **pkg/controller/tas/node_failure_controller.go:209** by [@gabesaba](https://github.com/gabesaba) — 2025-05-20T08:23:37Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
+		if currentAnnotations == nil {
+			currentAnnotations = make(map[string]string)
+		}
+		existingFailedNode, annotationPresent := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
+		if annotationPresent && existingFailedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+		}
```

  please pull this out to a function. something like `evictWorkloadWhenMultipleFailures`

- **pkg/controller/tas/node_failure_controller.go:221** by [@gabesaba](https://github.com/gabesaba) — 2025-05-20T08:24:08Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
+		if currentAnnotations == nil {
+			currentAnnotations = make(map[string]string)
+		}
+		existingFailedNode, annotationPresent := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
+		if annotationPresent && existingFailedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+		}
+		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			switch {
+			case annotationPresent && (workload.IsEvicted(&wl) || existingFailedNode != nodeName):
+				delete(currentAnnotations, kueuealpha.NodeToReplaceAnnotation)
+				wl.SetAnnotations(currentAnnotations)
+				r.log.V(4).Info("Removing unavailable node from workload annotation", "workload", wlKey, "nodeName", existingFailedNode)
+			case annotationPresent && existingFailedNode == nodeName:
 				return false, nil
+			default:
+				currentAnnotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(currentAnnotations)
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", nodeName)
```

  please pull this out to a function. something like `updateWorklaodAnnotation`

- **pkg/controller/tas/node_failure_controller.go:221** by [@gabesaba](https://github.com/gabesaba) — 2025-05-20T08:30:07Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
+		if currentAnnotations == nil {
+			currentAnnotations = make(map[string]string)
+		}
+		existingFailedNode, annotationPresent := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
+		if annotationPresent && existingFailedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+		}
+		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			switch {
+			case annotationPresent && (workload.IsEvicted(&wl) || existingFailedNode != nodeName):
+				delete(currentAnnotations, kueuealpha.NodeToReplaceAnnotation)
+				wl.SetAnnotations(currentAnnotations)
+				r.log.V(4).Info("Removing unavailable node from workload annotation", "workload", wlKey, "nodeName", existingFailedNode)
+			case annotationPresent && existingFailedNode == nodeName:
 				return false, nil
+			default:
+				currentAnnotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(currentAnnotations)
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", nodeName)
```

  Can the control flow be structured in a little more readable way? I find the control flow is a little confusing, as in some branches we are returning, and in others we rely on hitting `return true, nil`
  
  Perhaps: 
  ```
  if !annotationPresent {
    setAnnotation
    return true
  }
  if evicted || node1!=node2 {
      delete annotation
      return  true
  }
  return false
  
  ```

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-20T10:46:00Z

- **pkg/controller/tas/node_failure_controller_test.go:215** by [@pajakd](https://github.com/pajakd) — 2025-05-20T10:46:00Z
```diff
@@ -177,6 +204,19 @@ func TestNodeFailureReconciler(t *testing.T) {
 			if diff := cmp.Diff(tc.wantRequeue, result.RequeueAfter); diff != "" {
 				t.Errorf("Unexpected RequeueAfter (-want/+got):\n%s", diff)
 			}
+
+			if tc.wantEvicted {
+				foundEvicted := false
+				for _, cond := range wl.Status.Conditions {
+					if cond.Type == kueue.WorkloadEvicted && cond.Status == metav1.ConditionTrue && cond.Reason == kueue.WorkloadEvictedDueToTASNodeFailures {
+						foundEvicted = true
+						break
+					}
+				}
```

  Yes, thank you.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-20T11:56:55Z

- **pkg/controller/tas/node_failure_controller.go:194** by [@pajakd](https://github.com/pajakd) — 2025-05-20T11:56:55Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-20T11:57:36Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@pajakd](https://github.com/pajakd) — 2025-05-20T11:57:36Z
```diff
@@ -580,6 +580,10 @@ const (
 	// because the LocalQueue is Stopped.
 	WorkloadEvictedByLocalQueueStopped = "LocalQueueStopped"
 
+	// WorkloadEvictedDueToTASNodeFailures indicates that the workload was evicted
+	// due to non-recoverable node failures in TAS.
+	WorkloadEvictedDueToTASNodeFailures = "WorkloadEvictedDueToTASNodeFailures"
```

  TAS is not necessary here, removed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T14:28:22Z

- **pkg/controller/tas/node_failure_controller.go:212** by [@mimowo](https://github.com/mimowo) — 2025-05-20T14:28:22Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			}
+		}
+		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			switch {
+			case ok && (workload.IsEvicted(&wl) || failedNode != nodeName):
```

  I would prefer to avoid duplicating the entry condition. I would suggest to prepare the object to patch outside of calling the "Patch" function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T14:33:06Z

- **test/util/util.go:1070** by [@mimowo](https://github.com/mimowo) — 2025-05-20T14:33:05Z
```diff
@@ -1065,3 +1066,14 @@ func DeactivateWorkload(ctx context.Context, c client.Client, key client.ObjectK
 		g.Expect(c.Update(ctx, wl)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func SetNodeCondition(ctx context.Context, k8sClient client.Client, node *corev1.Node, conditionType corev1.NodeConditionType, conditionStatus corev1.ConditionStatus, time time.Time) {
```

  nit: I think for re-usability it is better to accept the entire NodeCondition here. 
  
  Also. this function will break if the condition if the condition does not exist. I know this is just for tests, but it is likely to surprise someone. So, in the node util package we could have `SetNodeCondition` similar to (don't need to cover all cases for now): https://github.com/kubernetes/apimachinery/blob/da3bba90543ccf634efc0e754bfbebe2df700ac2/pkg/api/meta/conditions.go#L31
  
  Having said that, I consider this non-blocking nit which can be improved in follow up issues.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-20T17:49:06Z

- **pkg/controller/tas/node_failure_controller.go:221** by [@pajakd](https://github.com/pajakd) — 2025-05-20T17:49:06Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
+		if currentAnnotations == nil {
+			currentAnnotations = make(map[string]string)
+		}
+		existingFailedNode, annotationPresent := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
+		if annotationPresent && existingFailedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+		}
+		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			switch {
+			case annotationPresent && (workload.IsEvicted(&wl) || existingFailedNode != nodeName):
+				delete(currentAnnotations, kueuealpha.NodeToReplaceAnnotation)
+				wl.SetAnnotations(currentAnnotations)
+				r.log.V(4).Info("Removing unavailable node from workload annotation", "workload", wlKey, "nodeName", existingFailedNode)
+			case annotationPresent && existingFailedNode == nodeName:
 				return false, nil
+			default:
+				currentAnnotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(currentAnnotations)
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", nodeName)
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-20T17:49:09Z

- **pkg/controller/tas/node_failure_controller.go:209** by [@pajakd](https://github.com/pajakd) — 2025-05-20T17:49:09Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
+		if currentAnnotations == nil {
+			currentAnnotations = make(map[string]string)
+		}
+		existingFailedNode, annotationPresent := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
+		if annotationPresent && existingFailedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+		}
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-20T17:49:12Z

- **pkg/controller/tas/node_failure_controller.go:195** by [@pajakd](https://github.com/pajakd) — 2025-05-20T17:49:12Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-20T18:00:09Z

- **pkg/controller/tas/node_failure_controller.go:221** by [@pajakd](https://github.com/pajakd) — 2025-05-20T18:00:08Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
+		if currentAnnotations == nil {
+			currentAnnotations = make(map[string]string)
+		}
+		existingFailedNode, annotationPresent := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
+		if annotationPresent && existingFailedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+		}
+		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			switch {
+			case annotationPresent && (workload.IsEvicted(&wl) || existingFailedNode != nodeName):
+				delete(currentAnnotations, kueuealpha.NodeToReplaceAnnotation)
+				wl.SetAnnotations(currentAnnotations)
+				r.log.V(4).Info("Removing unavailable node from workload annotation", "workload", wlKey, "nodeName", existingFailedNode)
+			case annotationPresent && existingFailedNode == nodeName:
 				return false, nil
+			default:
+				currentAnnotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(currentAnnotations)
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", nodeName)
```

  Ok, I think it looks good now -- but does it still make sense to have it as a separate function (see the previous comment)?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-20T18:09:38Z

- **pkg/controller/tas/node_failure_controller.go:210** by [@pajakd](https://github.com/pajakd) — 2025-05-20T18:09:38Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		currentAnnotations := wl.GetAnnotations()
+		if currentAnnotations == nil {
+			currentAnnotations = make(map[string]string)
+		}
+		existingFailedNode, annotationPresent := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
+		if annotationPresent && existingFailedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+		}
+		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
```

  I tried and it did not work for me. I want to use the workload.ApplyAdmissionStatus function for the eviction because its doing a quite complex work that I don't want to duplicate. And it turns out (unless I'm doing something wrong) that this function cannot update the Annotations.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T10:44:26Z

- **test/util/util.go:1070** by [@pajakd](https://github.com/pajakd) — 2025-05-21T10:44:26Z
```diff
@@ -1065,3 +1066,14 @@ func DeactivateWorkload(ctx context.Context, c client.Client, key client.ObjectK
 		g.Expect(c.Update(ctx, wl)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func SetNodeCondition(ctx context.Context, k8sClient client.Client, node *corev1.Node, conditionType corev1.NodeConditionType, conditionStatus corev1.ConditionStatus, time time.Time) {
```

  Makes sense, updated the function as we might need the additional flexibility in the future work.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T10:48:52Z

- **pkg/controller/tas/node_failure_controller_test.go:184** by [@pajakd](https://github.com/pajakd) — 2025-05-21T10:48:52Z
```diff
@@ -79,90 +79,117 @@ func TestNodeFailureReconciler(t *testing.T) {
 		Admitted(true).
 		Obj()
 
+	workloadWithTwoNodes := utiltesting.MakeWorkload(wlName, nsName).
+		Finalizers(kueue.ResourceInUseFinalizerName).
+		PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 2).Request(corev1.ResourceCPU, "1").Obj()).
+		ReserveQuota(
+			utiltesting.MakeAdmission("cq").
+				Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+				AssignmentPodCount(2).
+				TopologyAssignment(&kueue.TopologyAssignment{
+					Levels: []string{corev1.LabelHostname},
+					Domains: []kueue.TopologyDomainAssignment{
+						{Count: 1, Values: []string{nodeName}},
+						{Count: 1, Values: []string{nodeName2}},
+					},
+				}).
+				Obj(),
+		).
+		Admitted(true).
+		Obj()
+
 	basePod := testingpod.MakePod("test-pod", nsName).
 		Annotation(kueuealpha.WorkloadAnnotation, wlName).
 		Label(kueuealpha.TASLabel, "true").
 		NodeName(nodeName).
 		Obj()
 
 	tests := map[string]struct {
-		initObjs       []client.Object
-		req            reconcile.Request
-		advanceClock   time.Duration
-		wantFailedNode string
-		wantRequeue    time.Duration
+		initObjs          []client.Object
+		reconcileRequests []reconcile.Request
+		advanceClock      time.Duration
+		wantFailedNode    string
+		wantRequeue       time.Duration
+		wantEvicted       bool
 	}{
+		"Node Found and Healthy - not marked as unavailable": {
+			initObjs: []client.Object{
+				newNodeTest(nodeName, corev1.ConditionTrue, fakeClock, time.Duration(0)),
+				baseWorkload.DeepCopy(),
+				basePod.DeepCopy(),
+			},
+			reconcileRequests: []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantFailedNode:    "",
+		},
 		"Node Found and Unhealthy (NotReady), delay not passed - not marked as unavailable": {
 			initObjs: []client.Object{
 				newNodeTest(nodeName, corev1.ConditionFalse, fakeClock, time.Duration(0)),
 				baseWorkload.DeepCopy(),
 				basePod.DeepCopy(),
 			},
-			req:            reconcile.Request{NamespacedName: types.NamespacedName{Name: nodeName}},
-			wantFailedNode: "",
-			wantRequeue:    NodeFailureDelay,
+			reconcileRequests: []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantFailedNode:    "",
+			wantRequeue:       NodeFailureDelay,
 		},
 		"Node Found and Unhealthy (NotReady), delay passed - marked as unavailable": {
 			initObjs: []client.Object{
 				newNodeTest(nodeName, corev1.ConditionFalse, fakeClock, NodeFailureDelay),
 				baseWorkload.DeepCopy(),
 				basePod.DeepCopy(),
 			},
-			req:            reconcile.Request{NamespacedName: types.NamespacedName{Name: nodeName}},
-			wantFailedNode: nodeName,
+			reconcileRequests: []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantFailedNode:    nodeName,
 		},
-		"Node Found and Healthy - not marked as unavailable": {
+		"Node Deleted - marked as unavailable": {
 			initObjs: []client.Object{
-				newNodeTest(nodeName, corev1.ConditionTrue, fakeClock, time.Duration(0)),
-				utiltesting.MakeWorkload(wlName, nsName).
-					Finalizers(kueue.ResourceInUseFinalizerName).
-					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(1).Obj()).
-					Admitted(true).
-					Obj(),
+				baseWorkload.DeepCopy(),
 				basePod.DeepCopy(),
 			},
-			req:            reconcile.Request{NamespacedName: types.NamespacedName{Name: nodeName}},
-			wantFailedNode: "",
+			reconcileRequests: []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantFailedNode:    nodeName,
 		},
-		"Node Deleted - marked as unavailable": {
+		"Two Nodes Unhealthy (NotReady), delay passed - workload evicted": {
 			initObjs: []client.Object{
-				baseWorkload.DeepCopy(),
-				basePod.DeepCopy(),
+				newNodeTest(nodeName, corev1.ConditionFalse, fakeClock, NodeFailureDelay),
+				newNodeTest(nodeName2, corev1.ConditionFalse, fakeClock, NodeFailureDelay),
+				workloadWithTwoNodes.DeepCopy(),
+				testingpod.MakePod("pod1", nsName).Annotation(kueuealpha.WorkloadAnnotation, wlName).Label(kueuealpha.TASLabel, "true").NodeName(nodeName).Obj(),
+				testingpod.MakePod("pod2", nsName).Annotation(kueuealpha.WorkloadAnnotation, wlName).Label(kueuealpha.TASLabel, "true").NodeName(nodeName2).Obj(),
+			},
+			reconcileRequests: []reconcile.Request{
+				{NamespacedName: types.NamespacedName{Name: nodeName}},
+				{NamespacedName: types.NamespacedName{Name: nodeName2}},
 			},
-			req:            reconcile.Request{NamespacedName: types.NamespacedName{Name: nodeName}},
-			wantFailedNode: nodeName,
+			wantFailedNode: "",
+			wantEvicted:    true,
 		},
 	}
-
 	for name, tc := range tests {
 		t.Run(name, func(t *testing.T) {
 			features.SetFeatureGateDuringTest(t, features.TASFailedNodeReplacement, true)
 			ctx := t.Context()
 			fakeClock.SetTime(testStartTime)
-			s := scheme.Scheme
-			if err := kueue.AddToScheme(s); err != nil {
-				t.Fatalf("Failed to add kueue scheme: %v", err)
-			}
-			if err := corev1.AddToScheme(s); err != nil {
-				t.Fatalf("Failed to add kueue scheme: %v", err)
-			}
 
-			clientBuilder := fake.NewClientBuilder().
-				WithScheme(s).
+			clientBuilder := utiltesting.NewClientBuilder().
 				WithObjects(tc.initObjs...).
-				WithStatusSubresource(tc.initObjs...)
+				WithStatusSubresource(tc.initObjs...).
+				WithInterceptorFuncs(interceptor.Funcs{SubResourcePatch: utiltesting.TreatSSAAsStrategicMerge})
```

  Without this I'm getting an error "apply patches are not supported in the fake client".

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T10:51:52Z

- **pkg/controller/tas/node_failure_controller.go:212** by [@pajakd](https://github.com/pajakd) — 2025-05-21T10:51:52Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			}
+		}
+		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			switch {
+			case ok && (workload.IsEvicted(&wl) || failedNode != nodeName):
```

  Updated this part according to Gabes suggestion. Is it acceptable now? I'm not sure what you mean by "avoid duplicating the entry condition" here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T12:21:34Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T12:21:26Z
```diff
@@ -580,6 +580,10 @@ const (
 	// because the LocalQueue is Stopped.
 	WorkloadEvictedByLocalQueueStopped = "LocalQueueStopped"
 
+	// WorkloadEvictedDueToNodeFailures indicates that the workload was evicted
+	// due to non-recoverable node failures.
+	WorkloadEvictedDueToNodeFailures = "WorkloadEvictedDueToNodeFailures"
```

  Why not just use `NodeFaulures`? IIUC, isn't this for the Eviction condition type reason?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T12:23:34Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@mimowo](https://github.com/mimowo) — 2025-05-21T12:23:34Z
```diff
@@ -580,6 +580,10 @@ const (
 	// because the LocalQueue is Stopped.
 	WorkloadEvictedByLocalQueueStopped = "LocalQueueStopped"
 
+	// WorkloadEvictedDueToNodeFailures indicates that the workload was evicted
+	// due to non-recoverable node failures.
+	WorkloadEvictedDueToNodeFailures = "WorkloadEvictedDueToNodeFailures"
```

  Yes, the condition type already sets the context

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T12:25:12Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T12:25:12Z
```diff
@@ -580,6 +580,10 @@ const (
 	// because the LocalQueue is Stopped.
 	WorkloadEvictedByLocalQueueStopped = "LocalQueueStopped"
 
+	// WorkloadEvictedDueToNodeFailures indicates that the workload was evicted
+	// due to non-recoverable node failures.
+	WorkloadEvictedDueToNodeFailures = "WorkloadEvictedDueToNodeFailures"
```

  A built condition will be the following. The condition has `Evict` term in 2 places. It looks like too redundant.
  
  ```yaml
  status:
    conditions:
    - type: Evicted
      reason: WorkloadEvictedDueToNodeFailures
      status: True
      message: xyz
  ```

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T12:26:54Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@pajakd](https://github.com/pajakd) — 2025-05-21T12:26:53Z
```diff
@@ -580,6 +580,10 @@ const (
 	// because the LocalQueue is Stopped.
 	WorkloadEvictedByLocalQueueStopped = "LocalQueueStopped"
 
+	// WorkloadEvictedDueToNodeFailures indicates that the workload was evicted
+	// due to non-recoverable node failures.
+	WorkloadEvictedDueToNodeFailures = "WorkloadEvictedDueToNodeFailures"
```

  Yes, you are right, sorry, removed the redundant prefix.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T13:08:06Z

- **apis/kueue/v1beta1/workload_types.go:585** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T13:08:06Z
```diff
@@ -580,6 +580,10 @@ const (
 	// because the LocalQueue is Stopped.
 	WorkloadEvictedByLocalQueueStopped = "LocalQueueStopped"
 
+	// WorkloadEvictedDueToNodeFailures indicates that the workload was evicted
+	// due to non-recoverable node failures.
+	WorkloadEvictedDueToNodeFailures = "WorkloadEvictedDueToNodeFailures"
```

  SGTM, thanks

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-21T13:43:54Z

- **pkg/controller/tas/node_failure_controller.go:199** by [@gabesaba](https://github.com/gabesaba) — 2025-05-21T13:40:50Z
```diff
@@ -187,20 +191,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
```

  please reduce scope of these variables, and fetch again below (inside the function provided to patch). This is especially important for readability of !ok below.
  
  Or other option is to rename the ok back to something more verbose. I prefer more local variable option, however - it makes the 2nd block easier to read as you don't need to scroll up to see what the variables are.
  ```suggestion
  		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
  ```

- **pkg/controller/tas/node_failure_controller.go:219** by [@gabesaba](https://github.com/gabesaba) — 2025-05-21T13:42:20Z
```diff
@@ -187,20 +191,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			if !ok {
```

  complement of above
  ```suggestion
                          failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
  			if !ok {
  ```

- **pkg/controller/tas/node_failure_controller.go:244** by [@gabesaba](https://github.com/gabesaba) — 2025-05-21T13:43:01Z
```diff
@@ -214,3 +226,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 	}
 	return nil
 }
+
+func (r *nodeFailureReconciler) evictWorkloadWhenMultipleFailures(ctx context.Context, wl *kueue.Workload, wlKey types.NamespacedName, workloadProcessingErrors []error) (kueue.Workload, []error) {
+	r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+	if evictionErr := r.startEviction(ctx, wl); evictionErr != nil {
+		r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+	}
+	if err := r.client.Get(ctx, wlKey, wl); err != nil {
+		r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, err)
+	}
+	return *wl, workloadProcessingErrors
+}
+
+func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload) error {
+	message := "Workload eviction triggered due to multiple TAS assigned node failures"
```

  Should this be a file constant?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-21T13:52:43Z

- **pkg/controller/tas/node_failure_controller.go:206** by [@gabesaba](https://github.com/gabesaba) — 2025-05-21T13:49:47Z
```diff
@@ -187,20 +191,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			if !ok {
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
```

  nit: move log to start of block

- **pkg/controller/tas/node_failure_controller.go:212** by [@gabesaba](https://github.com/gabesaba) — 2025-05-21T13:49:57Z
```diff
@@ -187,20 +191,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			if !ok {
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) || failedNode != nodeName {
+				delete(annotations, kueuealpha.NodeToReplaceAnnotation)
+				wl.SetAnnotations(annotations)
+				r.log.V(4).Info("Removing unavailable node from workload annotation", "workload", wlKey, "nodeName", failedNode)
```

  nit: same as above, move to start of block

- **pkg/controller/tas/node_failure_controller.go:251** by [@gabesaba](https://github.com/gabesaba) — 2025-05-21T13:50:56Z
```diff
@@ -214,3 +226,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 	}
 	return nil
 }
+
+func (r *nodeFailureReconciler) evictWorkloadWhenMultipleFailures(ctx context.Context, wl *kueue.Workload, wlKey types.NamespacedName, workloadProcessingErrors []error) (kueue.Workload, []error) {
+	r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+	if evictionErr := r.startEviction(ctx, wl); evictionErr != nil {
+		r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+	}
+	if err := r.client.Get(ctx, wlKey, wl); err != nil {
+		r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, err)
+	}
+	return *wl, workloadProcessingErrors
+}
+
+func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload) error {
+	message := "Workload eviction triggered due to multiple TAS assigned node failures"
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedDueToNodeFailures, message)
+	workload.ResetChecksOnEviction(wl, r.clock.Now())
+	if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock); err != nil {
+		return err
+	}
+	cqName := wl.Status.Admission.ClusterQueue
+	workload.ReportEvictedWorkload(r.recorder, wl, cqName, kueue.WorkloadEvictedDueToNodeFailures, message)
```

  Just inline CQ?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T14:07:03Z

- **pkg/controller/tas/node_failure_controller.go:199** by [@pajakd](https://github.com/pajakd) — 2025-05-21T14:07:03Z
```diff
@@ -187,20 +191,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
```

  Done (first option).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T14:07:20Z

- **pkg/controller/tas/node_failure_controller.go:219** by [@pajakd](https://github.com/pajakd) — 2025-05-21T14:07:20Z
```diff
@@ -187,20 +191,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			if !ok {
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T14:07:24Z

- **pkg/controller/tas/node_failure_controller.go:244** by [@pajakd](https://github.com/pajakd) — 2025-05-21T14:07:23Z
```diff
@@ -214,3 +226,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 	}
 	return nil
 }
+
+func (r *nodeFailureReconciler) evictWorkloadWhenMultipleFailures(ctx context.Context, wl *kueue.Workload, wlKey types.NamespacedName, workloadProcessingErrors []error) (kueue.Workload, []error) {
+	r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+	if evictionErr := r.startEviction(ctx, wl); evictionErr != nil {
+		r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+	}
+	if err := r.client.Get(ctx, wlKey, wl); err != nil {
+		r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, err)
+	}
+	return *wl, workloadProcessingErrors
+}
+
+func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload) error {
+	message := "Workload eviction triggered due to multiple TAS assigned node failures"
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T14:07:27Z

- **pkg/controller/tas/node_failure_controller.go:206** by [@pajakd](https://github.com/pajakd) — 2025-05-21T14:07:26Z
```diff
@@ -187,20 +191,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			if !ok {
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T14:07:30Z

- **pkg/controller/tas/node_failure_controller.go:212** by [@pajakd](https://github.com/pajakd) — 2025-05-21T14:07:29Z
```diff
@@ -187,20 +191,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			if !ok {
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) || failedNode != nodeName {
+				delete(annotations, kueuealpha.NodeToReplaceAnnotation)
+				wl.SetAnnotations(annotations)
+				r.log.V(4).Info("Removing unavailable node from workload annotation", "workload", wlKey, "nodeName", failedNode)
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-21T14:07:33Z

- **pkg/controller/tas/node_failure_controller.go:251** by [@pajakd](https://github.com/pajakd) — 2025-05-21T14:07:32Z
```diff
@@ -214,3 +226,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 	}
 	return nil
 }
+
+func (r *nodeFailureReconciler) evictWorkloadWhenMultipleFailures(ctx context.Context, wl *kueue.Workload, wlKey types.NamespacedName, workloadProcessingErrors []error) (kueue.Workload, []error) {
+	r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+	if evictionErr := r.startEviction(ctx, wl); evictionErr != nil {
+		r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+	}
+	if err := r.client.Get(ctx, wlKey, wl); err != nil {
+		r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, err)
+	}
+	return *wl, workloadProcessingErrors
+}
+
+func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload) error {
+	message := "Workload eviction triggered due to multiple TAS assigned node failures"
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedDueToNodeFailures, message)
+	workload.ResetChecksOnEviction(wl, r.clock.Now())
+	if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock); err != nil {
+		return err
+	}
+	cqName := wl.Status.Admission.ClusterQueue
+	workload.ReportEvictedWorkload(r.recorder, wl, cqName, kueue.WorkloadEvictedDueToNodeFailures, message)
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T15:42:02Z

- **pkg/controller/tas/node_failure_controller.go:203** by [@mimowo](https://github.com/mimowo) — 2025-05-21T15:42:02Z
```diff
@@ -187,20 +195,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
```

  I would suggest to unfold this function - it is called from only one place, and actually it hides the fact the workload is Get again. 
  
   I think there is a subtle bug here- we memorize the `annotations := wl.GetAnnotations()`. Now, we Get the workload, but if the workload annotations where changed by the user, then we will override the change, by using the memorized annotations. I think we should read the annotations again after the Get, but the get is not visible here, so easy to miss.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T15:49:03Z

- **pkg/controller/tas/node_failure_controller.go:248** by [@mimowo](https://github.com/mimowo) — 2025-05-21T15:49:03Z
```diff
@@ -214,3 +230,26 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 	}
 	return nil
 }
+
+func (r *nodeFailureReconciler) evictWorkloadWhenMultipleFailures(ctx context.Context, wl *kueue.Workload, wlKey types.NamespacedName, workloadProcessingErrors []error) (kueue.Workload, []error) {
+	r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+	if evictionErr := r.startEviction(ctx, wl); evictionErr != nil {
+		r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+	}
+	if err := r.client.Get(ctx, wlKey, wl); err != nil {
+		r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, err)
+	}
+	return *wl, workloadProcessingErrors
+}
+
+func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload) error {
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedDueToNodeFailures, NodeMultipleFailuresEvictionMessage)
```

  I think it will be very useful to include the nodeNames in the message for debuggability. wdyt? I'm ok with a follow up (let me know).
  
  Maybe we can say "Workload eviction triggered due to multiple TAS assigned node failures, including <nodename1>, <nodeName2>".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T15:53:50Z

- **pkg/controller/tas/node_failure_controller.go:212** by [@mimowo](https://github.com/mimowo) — 2025-05-21T15:53:50Z
```diff
@@ -187,19 +191,35 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+		if ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			if evictionErr := r.startEviction(ctx, &wl); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			}
+		}
+		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			switch {
+			case ok && (workload.IsEvicted(&wl) || failedNode != nodeName):
```

  I'm still confused about the flow, but I will ask in new comments since the code has changed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T16:02:51Z

- **pkg/controller/tas/node_failure_controller.go:208** by [@mimowo](https://github.com/mimowo) — 2025-05-21T16:02:51Z
```diff
@@ -187,20 +195,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
```

  nit: Let's use consistent vocabulary. In some places we have "unavailable" in some "failed" / failure, and the annotation itself is "nodeToReplace". 
  
  I would probably say "nodeToReplace" everywhere, for example, here: "Adding node to the %s annotation". where %s just unfolds to full name of the nodeReplace annotation.
  
  However,  if you like "failed" or "unavailable" for brevity, then stick to one of them.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T16:10:08Z

- **pkg/controller/tas/node_failure_controller.go:213** by [@mimowo](https://github.com/mimowo) — 2025-05-21T16:10:08Z
```diff
@@ -187,20 +195,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) || failedNode != nodeName {
```

  Why do we need this check ` || failedNode != nodeName`? 
  
  I think there is a subtle bug here. Suppose the eviction request failed (for example API server was overloaded, Get succeeded), so the workload is not evicted, and we will delete the annotation. So it will not be retried (the node replacement)?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T05:51:24Z

- **pkg/controller/tas/node_failure_controller.go:248** by [@pajakd](https://github.com/pajakd) — 2025-05-22T05:51:24Z
```diff
@@ -214,3 +230,26 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 	}
 	return nil
 }
+
+func (r *nodeFailureReconciler) evictWorkloadWhenMultipleFailures(ctx context.Context, wl *kueue.Workload, wlKey types.NamespacedName, workloadProcessingErrors []error) (kueue.Workload, []error) {
+	r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+	if evictionErr := r.startEviction(ctx, wl); evictionErr != nil {
+		r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+	}
+	if err := r.client.Get(ctx, wlKey, wl); err != nil {
+		r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+		workloadProcessingErrors = append(workloadProcessingErrors, err)
+	}
+	return *wl, workloadProcessingErrors
+}
+
+func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload) error {
+	workload.SetEvictedCondition(wl, kueue.WorkloadEvictedDueToNodeFailures, NodeMultipleFailuresEvictionMessage)
```

  Done (updated the message to contain the node names).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T05:51:29Z

- **pkg/controller/tas/node_failure_controller.go:203** by [@pajakd](https://github.com/pajakd) — 2025-05-22T05:51:29Z
```diff
@@ -187,20 +195,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
```

  Thank you for spotting this. Removed the function and updated the annotations after the Get.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T05:51:36Z

- **pkg/controller/tas/node_failure_controller.go:208** by [@pajakd](https://github.com/pajakd) — 2025-05-22T05:51:36Z
```diff
@@ -187,20 +195,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
```

  Removed the "unavailable" to reduce confusion. But I think the phrases like "failed"/"node failure" have to stay -- the whole process starts with node failure(s). We react to failures in our controller, and later look for replacements of the failed nodes. I hope the vocabulary with both "failure" (as the cause) and "nodeToReplace" (as our reaction) will not be too complicated.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T05:51:44Z

- **pkg/controller/tas/node_failure_controller.go:213** by [@pajakd](https://github.com/pajakd) — 2025-05-22T05:51:44Z
```diff
@@ -187,20 +195,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) || failedNode != nodeName {
```

  Oh good point, did not think about this. Fixed by removing `|| failedNode != nodeName `.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T07:11:57Z

- **pkg/controller/tas/node_failure_controller.go:203** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T07:11:57Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
```

  Can we add some log verbosity?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T07:13:07Z

- **pkg/controller/tas/node_failure_controller.go:211** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T07:13:07Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			} else {
+				annotations = getAnnotations(&wl)
+			}
```

  Why do we need to assign getAnnotations once again?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T07:17:11Z

- **pkg/controller/tas/node_failure_controller_test.go:224** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T07:17:11Z
```diff
@@ -177,6 +208,20 @@ func TestNodeFailureReconciler(t *testing.T) {
 			if diff := cmp.Diff(tc.wantRequeue, result.RequeueAfter); diff != "" {
 				t.Errorf("Unexpected RequeueAfter (-want/+got):\n%s", diff)
 			}
+
+			isEvicted := apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted)
+			if diff := cmp.Diff(tc.wantEvicted, isEvicted); diff != "" {
+				t.Errorf("Unexpected eviction status (-want/+got):\n%s", diff)
+			}
+
+			if tc.wantEvicted {
+				evictedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted)
+				if evictedCond == nil {
+					t.Errorf("Expected WorkloadEvicted condition to be true, but it was not found")
+				} else if diff := cmp.Diff(tc.wantEvictionMsg, evictedCond.Message); diff != "" {
+					t.Errorf("Unexpected eviction message (-want/+got):\n%s", diff)
+				}
+			}
```

  Maybe we could have the whole condition as `tc.evictedCond` and simply compare it to the one returned from `apimeta.FindStatusCondition`?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T07:17:41Z

- **pkg/controller/tas/node_failure_controller.go:211** by [@pajakd](https://github.com/pajakd) — 2025-05-22T07:17:41Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			} else {
+				annotations = getAnnotations(&wl)
+			}
```

  This is answering this comment https://github.com/kubernetes-sigs/kueue/pull/5272#discussion_r2100617772

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-22T07:24:24Z

- **pkg/controller/tas/node_failure_controller.go:199** by [@mimowo](https://github.com/mimowo) — 2025-05-22T07:24:24Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
```

  ```suggestion
  		if failedNode, ok := wl.GetAnnotations()[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
  ```
  just a nit suggestion to avoid having the "global" annotations. Then, we would call ` getAnnotations(&wl)` in the scope of the Patch, as the first line,  wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-22T07:25:03Z

- **pkg/controller/tas/node_failure_controller.go:208** by [@mimowo](https://github.com/mimowo) — 2025-05-22T07:25:03Z
```diff
@@ -187,20 +195,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
```

  sgtm, thanks, at least one synonym less :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-22T07:25:11Z

- **pkg/controller/tas/node_failure_controller.go:213** by [@mimowo](https://github.com/mimowo) — 2025-05-22T07:25:11Z
```diff
@@ -187,20 +195,28 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := wl.GetAnnotations()
+		if annotations == nil {
+			annotations = make(map[string]string)
+		}
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			wl, workloadProcessingErrors = r.evictWorkloadWhenMultipleFailures(ctx, &wl, wlKey, workloadProcessingErrors)
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", failedNode)
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) || failedNode != nodeName {
```

  cool, thanks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T07:26:04Z

- **pkg/controller/tas/node_failure_controller.go:211** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T07:26:03Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			} else {
+				annotations = getAnnotations(&wl)
+			}
```

  Got it. Still I think there might be unwanted behavior here:
  What if the second GET returns an error? We will end up with stale annotations and send the second PATCH based on it. I think we should skip the workload if GET returns an error. Something along the lines
  ```suggestion
  			        continue
  			}
  			annotations = getAnnotations(&wl)
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T07:27:27Z

- **pkg/controller/tas/node_failure_controller.go:205** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T07:27:27Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
```

  Similarly here, should we skip this workload if eviction return an error or proceed with it?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T07:29:16Z

- **pkg/controller/tas/node_failure_controller.go:211** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T07:29:16Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			} else {
+				annotations = getAnnotations(&wl)
+			}
```

  Or maybe even better, maybe we could move this getAnnotations call inside `clientutil.Patch(` in L213

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T07:30:44Z

- **pkg/controller/tas/node_failure_controller.go:234** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T07:30:43Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			} else {
+				annotations = getAnnotations(&wl)
+			}
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) {
+				r.log.V(4).Info(fmt.Sprintf("Removing node from %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
+				delete(annotations, kueuealpha.NodeToReplaceAnnotation)
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			currentAnnotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
-			wl.SetAnnotations(currentAnnotations)
-			r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", nodeName)
-			return true, nil
+			return false, nil
 		})
 		if err != nil {
-			r.log.V(2).Error(err, "Failed to patch workload with annotation", "workload", wlKey)
+			r.log.V(2).Error(err, "Failed to patch workload annotation", "workload", wlKey)
 			workloadProcessingErrors = append(workloadProcessingErrors, err)
 		} else {
-			r.log.V(3).Info("Successfully patched workload with annotation", "workload", wlKey, "unavailableNodesAnnotation", wl.GetAnnotations()[kueuealpha.NodeToReplaceAnnotation])
+			r.log.V(3).Info("Successfully patched workload annotation", "workload", wlKey, "nodesToReplaceAnnotation", wl.GetAnnotations()[kueuealpha.NodeToReplaceAnnotation])
 		}
```

  Similarly here, maybe we could `continue` in case of error and get rid of `else` statement

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-22T07:31:42Z

- **pkg/controller/tas/node_failure_controller.go:205** by [@mimowo](https://github.com/mimowo) — 2025-05-22T07:31:42Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
```

  I think both approaches are valid if done correctly. 
  
  Returning error may actually simplify the flow, as attempting to process the annotations after the attempt to evict was the source of the subtle bug I mentioned in https://github.com/kubernetes-sigs/kueue/pull/5272#discussion_r2100682528 .

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T07:33:47Z

- **pkg/controller/tas/node_failure_controller.go:203** by [@pajakd](https://github.com/pajakd) — 2025-05-22T07:33:47Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T07:33:54Z

- **pkg/controller/tas/node_failure_controller_test.go:224** by [@pajakd](https://github.com/pajakd) — 2025-05-22T07:33:53Z
```diff
@@ -177,6 +208,20 @@ func TestNodeFailureReconciler(t *testing.T) {
 			if diff := cmp.Diff(tc.wantRequeue, result.RequeueAfter); diff != "" {
 				t.Errorf("Unexpected RequeueAfter (-want/+got):\n%s", diff)
 			}
+
+			isEvicted := apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted)
+			if diff := cmp.Diff(tc.wantEvicted, isEvicted); diff != "" {
+				t.Errorf("Unexpected eviction status (-want/+got):\n%s", diff)
+			}
+
+			if tc.wantEvicted {
+				evictedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted)
+				if evictedCond == nil {
+					t.Errorf("Expected WorkloadEvicted condition to be true, but it was not found")
+				} else if diff := cmp.Diff(tc.wantEvictionMsg, evictedCond.Message); diff != "" {
+					t.Errorf("Unexpected eviction message (-want/+got):\n%s", diff)
+				}
+			}
```

  Good point, done.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T07:46:31Z

- **pkg/controller/tas/node_failure_controller.go:205** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T07:46:31Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
```

  I meant add the error to the list and then skip

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-22T08:07:09Z

- **pkg/controller/tas/node_failure_controller.go:205** by [@mimowo](https://github.com/mimowo) — 2025-05-22T08:07:09Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
```

  I believe there are multiple ways to implement this correctly, so not sure how to answer, I will leave it to @pajakd . 
  
  It may help to clarify if your question / suggestion is motivated by a suspected bug / issue, or it aims to cleanup / simplify the code.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T08:10:50Z

- **pkg/controller/tas/node_failure_controller.go:205** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T08:10:50Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
```

  Well to clarify, I think we shouldn't proceed with the logic, if eviction failed

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T08:33:49Z

- **pkg/controller/tas/node_failure_controller.go:234** by [@pajakd](https://github.com/pajakd) — 2025-05-22T08:33:49Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			} else {
+				annotations = getAnnotations(&wl)
+			}
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) {
+				r.log.V(4).Info(fmt.Sprintf("Removing node from %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
+				delete(annotations, kueuealpha.NodeToReplaceAnnotation)
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			currentAnnotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
-			wl.SetAnnotations(currentAnnotations)
-			r.log.V(4).Info("Adding unavailable node to workload annotation", "workload", wlKey, "nodeName", nodeName)
-			return true, nil
+			return false, nil
 		})
 		if err != nil {
-			r.log.V(2).Error(err, "Failed to patch workload with annotation", "workload", wlKey)
+			r.log.V(2).Error(err, "Failed to patch workload annotation", "workload", wlKey)
 			workloadProcessingErrors = append(workloadProcessingErrors, err)
 		} else {
-			r.log.V(3).Info("Successfully patched workload with annotation", "workload", wlKey, "unavailableNodesAnnotation", wl.GetAnnotations()[kueuealpha.NodeToReplaceAnnotation])
+			r.log.V(3).Info("Successfully patched workload annotation", "workload", wlKey, "nodesToReplaceAnnotation", wl.GetAnnotations()[kueuealpha.NodeToReplaceAnnotation])
 		}
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T08:33:54Z

- **pkg/controller/tas/node_failure_controller.go:205** by [@pajakd](https://github.com/pajakd) — 2025-05-22T08:33:54Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
```

  Right, we shouldn't clear the annotation if either eviction or re-fetch failed. Added `continue` statements in both places.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T08:34:06Z

- **pkg/controller/tas/node_failure_controller.go:211** by [@pajakd](https://github.com/pajakd) — 2025-05-22T08:34:06Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+			} else {
+				annotations = getAnnotations(&wl)
+			}
```

  Makes sense, updated as suggested.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T08:39:22Z

- **pkg/controller/tas/node_failure_controller.go:199** by [@pajakd](https://github.com/pajakd) — 2025-05-22T08:39:22Z
```diff
@@ -187,30 +195,64 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
```

  Done.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T08:58:42Z

- **pkg/controller/tas/node_failure_controller.go:225** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T08:58:42Z
```diff
@@ -187,30 +195,68 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		evictedNow := false
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+				continue
+			} else {
+				evictedNow = true
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+				continue
+			}
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			annotations = getAnnotations(&wl)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) || evictedNow {
```

  Do we need to check for this alternative `evictedNow`?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-22T10:02:46Z

- **pkg/controller/tas/node_failure_controller.go:225** by [@pajakd](https://github.com/pajakd) — 2025-05-22T10:02:45Z
```diff
@@ -187,30 +195,68 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		evictedNow := false
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+				continue
+			} else {
+				evictedNow = true
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+				continue
+			}
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			annotations = getAnnotations(&wl)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) || evictedNow {
```

  Here is what happens in the tests:
  1. Evict the workload using workload.ApplyAdmissionStatus (successful)
  2. Re-fetch the workload (also successful)
  3. Believe it or not, but the re-fetched workload is not evicted any more (workload.IsEvicted(&wl) is false)
  
  Perhaps the scheduling is so fast, that it immediately reschedules the workload. Or the re-fetch Get gives me workload before the eviction Patch was applied. 
  
  So (regardless of the reason), I'm using the flag to remember that we did evict it and should clear the annotation.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-05-22T10:42:47Z

- **pkg/controller/tas/node_failure_controller.go:225** by [@PBundyra](https://github.com/PBundyra) — 2025-05-22T10:42:47Z
```diff
@@ -187,30 +195,68 @@ func (r *nodeFailureReconciler) patchWorkloadsForUnavailableNode(ctx context.Con
 			}
 			continue
 		}
-
+		annotations := getAnnotations(&wl)
+		evictedNow := false
+		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
+			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
+			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
+			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
+				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
+				continue
+			} else {
+				evictedNow = true
+			}
+			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
+				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				workloadProcessingErrors = append(workloadProcessingErrors, err)
+				continue
+			}
+		}
 		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			currentAnnotations := wl.GetAnnotations()
-			if currentAnnotations == nil {
-				currentAnnotations = make(map[string]string)
+			annotations = getAnnotations(&wl)
+			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
+			if !ok {
+				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
+				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
+				wl.SetAnnotations(annotations)
+				return true, nil
 			}
-			existingFailedNode, ok := currentAnnotations[kueuealpha.NodeToReplaceAnnotation]
-			if ok && existingFailedNode == nodeName {
-				return false, nil
+			if workload.IsEvicted(&wl) || evictedNow {
```

  Got it

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-22T10:52:20Z

- **test/util/util.go:1087** by [@mimowo](https://github.com/mimowo) — 2025-05-22T10:52:20Z
```diff
@@ -1065,3 +1066,24 @@ func DeactivateWorkload(ctx context.Context, c client.Client, key client.ObjectK
 		g.Expect(c.Update(ctx, wl)).To(gomega.Succeed())
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+// conditionType corev1.NodeConditionType, conditionStatus corev1.ConditionStatus, time time.Time
+func SetNodeCondition(ctx context.Context, k8sClient client.Client, node *corev1.Node, newCondition *corev1.NodeCondition) {
+	gomega.EventuallyWithOffset(1, func(g gomega.Gomega) {
+		var updatedNode corev1.Node
+		g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(node), &updatedNode)).To(gomega.Succeed())
+		condition := utilnode.GetNodeCondition(&updatedNode, newCondition.Type)
+		changed := false
+		if condition == nil {
+			updatedNode.Status.Conditions = append(updatedNode.Status.Conditions, *newCondition)
+			changed = true
+		} else if condition.Status != newCondition.Status {
+			condition.Status = newCondition.Status
+			condition.LastTransitionTime = newCondition.LastTransitionTime
+			changed = true
+		}
+		if changed {
+			g.Expect(k8sClient.Status().Update(ctx, &updatedNode)).To(gomega.Succeed())
+		}
```

  nit, I would suggest to move the logic into utilnode.SetNodeCondition. This will allow us to unit test, and also it will be aligned with the approach for other conditions.
