# PR #5278: Fix restoring of the image in artifacts goal

**Summary**: Fix restoring of the image in artifacts goal

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5278

**Last updated**: 2025-07-10T12:06:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-restoring-after-artifacts` → `main`
- **Created**: 2025-05-19T07:40:21Z
- **Updated**: 2025-07-10T12:06:14Z
- **Merged**: 2025-05-28T13:00:18Z
- **Closed**: 2025-05-28T13:00:18Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `size/XS`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 1   **+1 / -1**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

Fix restoring of the image after the "artifacts" goal with custom `IMAGE_REGISTRY` used (as during the release process)

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-19T07:40:29Z

/assign @tenzen-y

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-19T07:40:36Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 652812c1b67023a6138523de86d5b2fbf153b2a5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/682b2f3b96319b00083e6da9 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-5278--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-5278--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTUyNzgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.leQIZZin3O4LxczqrSHGsci4NKQqNt5impJfAWvq_lA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-5278--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-28T12:24:14Z

LGTM label has been added.  <details>Git tree hash: 2a648adb39fe1ae6b46da881fa36e113c74ba1b4</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-28T12:24:15Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5278#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5278#discussion_r2111716892" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-28T13:01:17Z

/cherry-pick release-0.12 release-0.11

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-05-28T13:01:54Z

@tenzen-y: new pull request created: #5402

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5278#issuecomment-2916234778):

>/cherry-pick release-0.12 release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-28T13:55:06Z

/cherry-pick release-0.11

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-05-28T13:55:42Z

@tenzen-y: #5278 failed to apply on top of branch "release-0.11":
```
Applying: Fix restoring of the image in artifacts goal
Using index info to reconstruct a base tree...
M	Makefile
Falling back to patching base and 3-way merge...
Auto-merging Makefile
CONFLICT (content): Merge conflict in Makefile
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Fix restoring of the image in artifacts goal

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5278#issuecomment-2916453945):

>/cherry-pick release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-28T13:56:56Z

@mimowo Could you check this one?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-28T14:00:33Z

Yes I will do it

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-19T10:06:03Z

- **Makefile:327** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-19T10:05:58Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  Why do we need this change? IIUC, if we specify `STAGING_IMAGE_REGISTRY=foo/bar make artifacts` will apply the custom registry here, isn't it?
  
  https://github.com/kubernetes-sigs/kueue/blob/292054c57c4139c400064e05e5dc8f439cdae4bd/Makefile#L37-L38

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T10:09:06Z

- **Makefile:327** by [@mimowo](https://github.com/mimowo) — 2025-05-19T10:09:06Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  > Why do we need this change? IIUC, if we specify `STAGING_IMAGE_REGISTRY=foo/bar make artifacts` will apply the custom registry here, isn't it?
  
  Our release procedure step says 
  
  https://github.com/kubernetes-sigs/kueue/blob/1c9517d9975edf354a92a9c66054403b4960ebdf/.github/ISSUE_TEMPLATE/NEW_RELEASE.md?plain=1#L39
  
  and this does not get reverted currently.
  
  I hit this issue once and committed without reverting manually by mistake, so this is annoying bug.
  
  We never say in the release steps to override `STAGING_IMAGE_REGISTRY`, and  I'm not sure you can override ` STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images ` like that. If you prefer, we can just hardcode reverting to `us-central1-docker.pkg.dev/k8s-staging-images `

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-19T13:17:08Z

- **Makefile:327** by [@mimowo](https://github.com/mimowo) — 2025-05-19T13:17:08Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  Rebased, ptal

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-26T05:52:33Z

- **Makefile:327** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-26T05:52:33Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  Thank you for the explanations.
  Do you indicate the situation where we release RC version? I guess that this will not be problem on stable release.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-26T06:00:48Z

- **Makefile:327** by [@mimowo](https://github.com/mimowo) — 2025-05-26T06:00:48Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  actually this happens only for full releases, when calling make artifacts with Image registry parameter as for the release version.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-26T10:21:05Z

- **Makefile:327** by [@mimowo](https://github.com/mimowo) — 2025-05-26T10:21:05Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  basically this command `make artifacts IMAGE_REGISTRY=registry.k8s.io/kueue GIT_TAG=$VERSION`, recommended in [our release steps](https://github.com/kubernetes-sigs/kueue/blob/main/.github/ISSUE_TEMPLATE/NEW_RELEASE.md?plain=1#L39), leaves the git status dirty.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-28T12:24:07Z

- **Makefile:327** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-28T12:24:07Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  > basically this command make artifacts IMAGE_REGISTRY=registry.k8s.io/kueue GIT_TAG=$VERSION, recommended in [our release steps](https://github.com/kubernetes-sigs/kueue/blob/main/.github/ISSUE_TEMPLATE/NEW_RELEASE.md?plain=1#L39), leaves the git status dirty.
  
  Yes, I also always the working branch with `git reset --hard`. 
  
  Thank you for describing that. I might misunderstand the objective for this change. 
  
  If this PR aims to automatically revert the image change during the artifact preparation step, I'm fine with this PR. If the answer is yes, feel free to unhold PR.
  
  /lgtm
  /approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-28T12:24:34Z

- **Makefile:327** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-28T12:24:34Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  /hold

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-28T12:34:27Z

- **Makefile:327** by [@mimowo](https://github.com/mimowo) — 2025-05-28T12:34:27Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  > If this PR aims to automatically revert the image change during the artifact preparation step, I'm fine with this PR. If the answer is yes, feel free to unhold PR.
  
  Yes, this is exactly the point, to avoid the need to call `git reset --hard` every time during the process.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-28T12:34:33Z

- **Makefile:327** by [@mimowo](https://github.com/mimowo) — 2025-05-28T12:34:33Z
```diff
@@ -306,7 +306,7 @@ artifacts: kustomize yq helm ## Generate release artifacts.
 	$(HELM) package --version $(GIT_TAG) --app-version $(GIT_TAG) charts/kueue -d artifacts/
 	mv artifacts/kueue-$(GIT_TAG).tgz artifacts/kueue-chart-$(GIT_TAG).tgz
 	# Revert the image changes
-	$(YQ)  e  '.controllerManager.manager.image.repository = "$(IMAGE_REGISTRY)/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
+	$(YQ)  e  '.controllerManager.manager.image.repository = "$(STAGING_IMAGE_REGISTRY)/kueue/$(IMAGE_NAME)" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = "Always"' -i charts/kueue/values.yaml
```

  /unhold
