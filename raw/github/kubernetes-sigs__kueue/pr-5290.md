# PR #5290: [Feature] Allow to merge equivalent/identical PodSets

**Summary**: [Feature] Allow to merge equivalent/identical PodSets

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5290

**Last updated**: 2025-05-22T07:07:17Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feature/5266-allow-to-merge-equivalent-podsets` → `main`
- **Created**: 2025-05-20T09:39:40Z
- **Updated**: 2025-05-22T07:07:17Z
- **Merged**: 2025-05-22T07:06:39Z
- **Closed**: 2025-05-22T07:06:39Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 11   **+758 / -17**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Enables workloads with multiple PodSets to function on cloud providers with PodTemplate limitations.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5266

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Introduce the ProvisioningRequestConfig API which allows to instruct Kueue to create a ProvisioningRequest with a single PodSet with aggregated resources from multiple PodSets in the Workload (eg. PyTorchJob) created by the user.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-20T09:39:42Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-20T09:39:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6d8b318101113c1388955b088e9929065f841c0a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/682e1b43e6a1d90008d1dd94 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-20T14:49:40Z

/test all

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T12:31:23Z

ptal @mimowo @mwielgus @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-21T12:45:55Z

Please add  a release note. Maybe something like:

Introduce the ProvisioningRequestConfig API which allows to instruct Kueue to create a ProvisioningRequest with a single PodSet with aggregated resources from multiple PodSets in the Workload (eg. PyTorchJob) created by the user.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-21T12:57:43Z

/retest
https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/5290/pull-kueue-test-e2e-customconfigs-main/1925169619060920320
is a known flake. cc @mykysha

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-21T13:15:37Z

/test pull-kueue-test-e2e-customconfigs-main

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T13:55:25Z

/test pull-kueue-test-integration-baseline-main
/test pull-kueue-test-integration-extended-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-21T14:33:05Z

LGTM overall, just left small-ish comments to cleanup and reduce the testing time. Most of the test cases can be covered in unit tests IMO.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T15:42:12Z

> LGTM overall, just left small-ish comments to cleanup and reduce the testing time. Most of the test cases can be covered in unit tests IMO.

Done. I think I have covered most of them in the unit tests already.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T19:24:46Z

/test pull-kueue-test-e2e-customconfigs-main

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-05-22T05:43:07Z

/test pull-kueue-test-e2e-customconfigs-main

not related flake

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-22T07:04:36Z

/lgtm
/approve
cc @tenzen-y @mwielgus I'm merging the PR as I believe we have alignment on the API, and I already reviewed the implementation. I want to prepare release candidate today so that some users may already start testing the feature, and maybe we can get early feedback.

Having said that it is early merge, so if you have some extra comments, then don't hesitate to post them, and we will address post-merge.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-22T07:04:42Z

LGTM label has been added.  <details>Git tree hash: 619fed2602cc663d5c7ff5d4dc7264ae7789b0cb</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-22T07:04:44Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5290#issuecomment-2900144222" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5290#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T09:48:30Z

- **pkg/controller/admissionchecks/provisioning/controller.go:848** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T09:48:30Z
```diff
@@ -833,3 +829,120 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode kueue.MergePodSetsMode,
+) []MergedPodSet {
+	merged := []MergedPodSet{}
+
+	if mergeMode == "disabled" {
```

  to enum

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T09:48:52Z

- **pkg/controller/admissionchecks/provisioning/controller.go:886** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T09:48:52Z
```diff
@@ -833,3 +829,120 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode kueue.MergePodSetsMode,
+) []MergedPodSet {
+	merged := []MergedPodSet{}
+
+	if mergeMode == "disabled" {
+		for _, psName := range expectedPodSets {
+			ps := podSetMap[psName]
+			psa := psaMap[psName]
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged
+	}
+
+	for _, psName := range expectedPodSets {
+		ps := podSetMap[psName]
+		psa := psaMap[psName]
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode kueue.MergePodSetsMode) bool {
+	switch mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
```

  whatever we decide means identical

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-20T09:49:40Z

- **pkg/controller/admissionchecks/provisioning/controller.go:906** by [@mszadkow](https://github.com/mszadkow) — 2025-05-20T09:49:40Z
```diff
@@ -833,3 +829,120 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode kueue.MergePodSetsMode,
+) []MergedPodSet {
+	merged := []MergedPodSet{}
+
+	if mergeMode == "disabled" {
+		for _, psName := range expectedPodSets {
+			ps := podSetMap[psName]
+			psa := psaMap[psName]
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged
+	}
+
+	for _, psName := range expectedPodSets {
+		ps := podSetMap[psName]
+		psa := psaMap[psName]
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode kueue.MergePodSetsMode) bool {
+	switch mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return arePodSetsIdentical(ps1, ps2)
+	case kueue.Equivalent:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsEquivalent(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsIdentical(ps1, ps2 *kueue.PodSet) bool {
+	if !areResourceListsEqual(ps1.Template.Spec.Resources.Requests, ps2.Template.Spec.Resources.Requests) {
+		return false
+	}
+	if !areMapsEqual(ps1.Template.Labels, ps2.Template.Labels) {
```

  I already know labels and annotations might not be equal but I will double check with PyTorchJobs

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T10:12:44Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-20T10:12:43Z
```diff
@@ -21,10 +21,16 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Disabled   MergePodSetsMode = "Disabled"
```

  ```suggestion
  	Never   MergePodSetsMode = "Never"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T10:14:29Z

- **pkg/controller/admissionchecks/provisioning/controller.go:903** by [@mimowo](https://github.com/mimowo) — 2025-05-20T10:14:28Z
```diff
@@ -833,3 +829,120 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode kueue.MergePodSetsMode,
+) []MergedPodSet {
+	merged := []MergedPodSet{}
+
+	if mergeMode == "disabled" {
+		for _, psName := range expectedPodSets {
+			ps := podSetMap[psName]
+			psa := psaMap[psName]
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged
+	}
+
+	for _, psName := range expectedPodSets {
+		ps := podSetMap[psName]
+		psa := psaMap[psName]
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode kueue.MergePodSetsMode) bool {
+	switch mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return arePodSetsIdentical(ps1, ps2)
+	case kueue.Equivalent:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsEquivalent(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsIdentical(ps1, ps2 *kueue.PodSet) bool {
+	if !areResourceListsEqual(ps1.Template.Spec.Resources.Requests, ps2.Template.Spec.Resources.Requests) {
```

  I think identical should mean `ps1.Template` equal to `ps2.Template`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T10:18:00Z

- **pkg/controller/admissionchecks/provisioning/controller.go:916** by [@mimowo](https://github.com/mimowo) — 2025-05-20T10:18:00Z
```diff
@@ -833,3 +829,120 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode kueue.MergePodSetsMode,
+) []MergedPodSet {
+	merged := []MergedPodSet{}
+
+	if mergeMode == "disabled" {
+		for _, psName := range expectedPodSets {
+			ps := podSetMap[psName]
+			psa := psaMap[psName]
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged
+	}
+
+	for _, psName := range expectedPodSets {
+		ps := podSetMap[psName]
+		psa := psaMap[psName]
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode kueue.MergePodSetsMode) bool {
+	switch mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return arePodSetsIdentical(ps1, ps2)
+	case kueue.Equivalent:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsEquivalent(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsIdentical(ps1, ps2 *kueue.PodSet) bool {
+	if !areResourceListsEqual(ps1.Template.Spec.Resources.Requests, ps2.Template.Spec.Resources.Requests) {
+		return false
+	}
+	if !areMapsEqual(ps1.Template.Labels, ps2.Template.Labels) {
+		return false
+	}
+	if !areMapsEqual(ps1.Template.Annotations, ps2.Template.Annotations) {
+		return false
+	}
+	// What else should we compare
+	return true
+}
+
+func arePodSetsEquivalent(ps1, ps2 *kueue.PodSet) bool {
```

  For "Equivalent" I would say idendical: "ps2.Template.Spec.Resources", , "ps2.Template.Spec.Containers[].Resources", "ps2.Template.Spec.InitContainers[].Resources","ps2.Template.Spec.NodeSelector", "ps2.Template.Spec.Tolerations", "ps2.Template.Spec.Affinity".
  
  cc @mwielgus

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-20T10:39:00Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:92** by [@mwielgus](https://github.com/mwielgus) — 2025-05-20T10:39:00Z
```diff
@@ -76,6 +82,14 @@ type ProvisioningRequestConfigSpec struct {
 	//
 	// +optional
 	PodSetUpdates *ProvisioningRequestPodSetUpdates `json:"podSetUpdates,omitempty"`
+
+	// mergePodSets specifies the mode for merging PodSets before being passed
+	// to the cluster autoscaler.
+	//
+	// +optional
+	// +kubebuilder:validation:Enum=Disabled;Identical;Equivalent
+	// +kubebuilder:default=disabled
+	MergePodSets MergePodSetsMode `json:"mergePodSets,omitempty"`
```

  Why not *string and treat nil as Never?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T10:42:28Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:92** by [@mimowo](https://github.com/mimowo) — 2025-05-20T10:42:28Z
```diff
@@ -76,6 +82,14 @@ type ProvisioningRequestConfigSpec struct {
 	//
 	// +optional
 	PodSetUpdates *ProvisioningRequestPodSetUpdates `json:"podSetUpdates,omitempty"`
+
+	// mergePodSets specifies the mode for merging PodSets before being passed
+	// to the cluster autoscaler.
+	//
+	// +optional
+	// +kubebuilder:validation:Enum=Disabled;Identical;Equivalent
+	// +kubebuilder:default=disabled
+	MergePodSets MergePodSetsMode `json:"mergePodSets,omitempty"`
```

  It should definitely be ptr as a new field in the schema. 
  
  Should we have "Never" explicit?  There are two schools, I'm ok without it.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-20T10:43:09Z

- **pkg/controller/admissionchecks/provisioning/controller.go:903** by [@mwielgus](https://github.com/mwielgus) — 2025-05-20T10:43:09Z
```diff
@@ -833,3 +829,120 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode kueue.MergePodSetsMode,
+) []MergedPodSet {
+	merged := []MergedPodSet{}
+
+	if mergeMode == "disabled" {
+		for _, psName := range expectedPodSets {
+			ps := podSetMap[psName]
+			psa := psaMap[psName]
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged
+	}
+
+	for _, psName := range expectedPodSets {
+		ps := podSetMap[psName]
+		psa := psaMap[psName]
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode kueue.MergePodSetsMode) bool {
+	switch mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return arePodSetsIdentical(ps1, ps2)
+	case kueue.Equivalent:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsEquivalent(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsIdentical(ps1, ps2 *kueue.PodSet) bool {
+	if !areResourceListsEqual(ps1.Template.Spec.Resources.Requests, ps2.Template.Spec.Resources.Requests) {
```

  Or at least cover much more fields.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-20T10:45:28Z

- **pkg/controller/admissionchecks/provisioning/controller.go:916** by [@mwielgus](https://github.com/mwielgus) — 2025-05-20T10:45:28Z
```diff
@@ -833,3 +829,120 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode kueue.MergePodSetsMode,
+) []MergedPodSet {
+	merged := []MergedPodSet{}
+
+	if mergeMode == "disabled" {
+		for _, psName := range expectedPodSets {
+			ps := podSetMap[psName]
+			psa := psaMap[psName]
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged
+	}
+
+	for _, psName := range expectedPodSets {
+		ps := podSetMap[psName]
+		psa := psaMap[psName]
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode kueue.MergePodSetsMode) bool {
+	switch mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return arePodSetsIdentical(ps1, ps2)
+	case kueue.Equivalent:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsEquivalent(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsIdentical(ps1, ps2 *kueue.PodSet) bool {
+	if !areResourceListsEqual(ps1.Template.Spec.Resources.Requests, ps2.Template.Spec.Resources.Requests) {
+		return false
+	}
+	if !areMapsEqual(ps1.Template.Labels, ps2.Template.Labels) {
+		return false
+	}
+	if !areMapsEqual(ps1.Template.Annotations, ps2.Template.Annotations) {
+		return false
+	}
+	// What else should we compare
+	return true
+}
+
+func arePodSetsEquivalent(ps1, ps2 *kueue.PodSet) bool {
```

  + possibly ResouceClaims.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T10:51:11Z

- **pkg/controller/admissionchecks/provisioning/controller.go:903** by [@mimowo](https://github.com/mimowo) — 2025-05-20T10:51:10Z
```diff
@@ -833,3 +829,120 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode kueue.MergePodSetsMode,
+) []MergedPodSet {
+	merged := []MergedPodSet{}
+
+	if mergeMode == "disabled" {
+		for _, psName := range expectedPodSets {
+			ps := podSetMap[psName]
+			psa := psaMap[psName]
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged
+	}
+
+	for _, psName := range expectedPodSets {
+		ps := podSetMap[psName]
+		psa := psaMap[psName]
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode kueue.MergePodSetsMode) bool {
+	switch mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return arePodSetsIdentical(ps1, ps2)
+	case kueue.Equivalent:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsEquivalent(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsIdentical(ps1, ps2 *kueue.PodSet) bool {
+	if !areResourceListsEqual(ps1.Template.Spec.Resources.Requests, ps2.Template.Spec.Resources.Requests) {
```

  For comparisons prefer to use `equality.Semantic.DeepEqual`, rather than custom functions. If for whatever reason the function does not work, then add a comment why.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T15:30:00Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-20T15:30:00Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  Maybe we would prefer to call it "Similar" so that it is less formal. Maybe in the future we have some knobs to define what "Similar" means. wdyt @tenzen-y @mwielgus ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T15:30:35Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:90** by [@mimowo](https://github.com/mimowo) — 2025-05-20T15:30:35Z
```diff
@@ -76,6 +81,13 @@ type ProvisioningRequestConfigSpec struct {
 	//
 	// +optional
 	PodSetUpdates *ProvisioningRequestPodSetUpdates `json:"podSetUpdates,omitempty"`
+
+	// mergePodSets specifies the mode for merging PodSets before being passed
+	// to the cluster autoscaler.
+	//
+	// +optional
+	// +kubebuilder:validation:Enum=Identical;Equivalent
+	MergePodSets *MergePodSetsMode `json:"mergePodSets,omitempty"`
```

  ```suggestion
  	MergePodSetsMode *MergePodSetsMode `json:"mergePodSetsMode,omitempty"`
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T15:36:17Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T15:36:16Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  Do you have use case to specify the identical keys. Currently, keys are hardcoded as 
  
  ```
  	return areContainersEqual &&
  		areInitContainersEqual &&
  		equality.Semantic.DeepEqual(ps1.Template.Spec.Resources, ps2.Template.Spec.Resources) &&
  		equality.Semantic.DeepEqual(ps1.Template.Spec.NodeSelector, ps2.Template.Spec.NodeSelector) &&
  		equality.Semantic.DeepEqual(ps1.Template.Spec.Tolerations, ps2.Template.Spec.Tolerations) &&
  		equality.Semantic.DeepEqual(ps1.Template.Spec.Affinity, ps2.Template.Spec.Affinity) &&
  		equality.Semantic.DeepEqual(ps1.Template.Spec.ResourceClaims, ps2.Template.Spec.ResourceClaims)
  
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T15:39:35Z

- **pkg/controller/admissionchecks/provisioning/controller.go:898** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T15:39:35Z
```diff
@@ -833,3 +833,116 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.MergePodSetsMode,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
+
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode *kueue.MergePodSetsMode) bool {
+	switch *mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return arePodSetsIdentical(ps1, ps2)
+	case kueue.Equivalent:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsEquivalent(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsIdentical(ps1, ps2 *kueue.PodSet) bool {
+	return equality.Semantic.DeepEqual(ps1.Template, ps2.Template)
+}
+
+func arePodSetsEquivalent(ps1, ps2 *kueue.PodSet) bool {
+	ps1ContainersRes := aggregateTotalResources(ps1.Template.Spec.Containers)
+	ps2ContainersRes := aggregateTotalResources(ps2.Template.Spec.Containers)
+	areContainersEqual := equality.Semantic.DeepEqual(ps1ContainersRes, ps2ContainersRes)
+
+	ps1InitContainersRes := aggregateTotalResources(ps1.Template.Spec.InitContainers)
+	ps2InitContainersRes := aggregateTotalResources(ps2.Template.Spec.InitContainers)
+	areInitContainersEqual := equality.Semantic.DeepEqual(ps1InitContainersRes, ps2InitContainersRes)
+
+	return areContainersEqual &&
+		areInitContainersEqual &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Resources, ps2.Template.Spec.Resources) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.NodeSelector, ps2.Template.Spec.NodeSelector) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Tolerations, ps2.Template.Spec.Tolerations) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Affinity, ps2.Template.Spec.Affinity) &&
```

  Do you really want to contain the `affinity` in identical keys? The affinity is a bit of a flexible field. So, if we contain this in here, I can imagine this identical check is much stricter. In other words, this affinity inclusion blocks PodSet merges in a lot of cases.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T15:41:57Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:24** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T15:41:56Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
```

  ```suggestion
  type ProvisioningRequestConfigMergePodSetsMode string
  ```
  
  Could you add a prefix to avoid the conflicts between the API directory?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T16:12:56Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-20T16:12:56Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  > Do you have use case to specify the identical keys. 
  
  Do you ask if we have a use-case for supporting "Identical"? 
  
  I think in most cases "Similar/Equivalent" will be enough. 
  
  Still, I think maybe some users may prefer to be conservative. For example, there might be some custom provisioningRequestClass which discriminates by annotation - for example it could have different behavior depending on "kueue.k8s-x.io/podset-required-topology: rack`  . So, maybe some users don't want to merge PodSets with different annotations.
  
  I think "Identical" is expected to be enough in the popular case where using PyTorchJob. In this case the user specifies the same template for master & worker. IIUC the differentiating env. variables (such as RANK) are only added to the created pods by the potorchjob_controller.go.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T16:14:39Z

- **pkg/controller/admissionchecks/provisioning/controller.go:912** by [@mimowo](https://github.com/mimowo) — 2025-05-20T16:14:39Z
```diff
@@ -833,3 +833,116 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.MergePodSetsMode,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
+
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode *kueue.MergePodSetsMode) bool {
+	switch *mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return arePodSetsIdentical(ps1, ps2)
+	case kueue.Equivalent:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsEquivalent(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsIdentical(ps1, ps2 *kueue.PodSet) bool {
```

  no need for the function IMO, it is called from one place

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T16:18:44Z

- **pkg/controller/admissionchecks/provisioning/controller.go:898** by [@mimowo](https://github.com/mimowo) — 2025-05-20T16:18:44Z
```diff
@@ -833,3 +833,116 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.MergePodSetsMode,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
+
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode *kueue.MergePodSetsMode) bool {
+	switch *mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return arePodSetsIdentical(ps1, ps2)
+	case kueue.Equivalent:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsEquivalent(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsIdentical(ps1, ps2 *kueue.PodSet) bool {
+	return equality.Semantic.DeepEqual(ps1.Template, ps2.Template)
+}
+
+func arePodSetsEquivalent(ps1, ps2 *kueue.PodSet) bool {
+	ps1ContainersRes := aggregateTotalResources(ps1.Template.Spec.Containers)
+	ps2ContainersRes := aggregateTotalResources(ps2.Template.Spec.Containers)
+	areContainersEqual := equality.Semantic.DeepEqual(ps1ContainersRes, ps2ContainersRes)
+
+	ps1InitContainersRes := aggregateTotalResources(ps1.Template.Spec.InitContainers)
+	ps2InitContainersRes := aggregateTotalResources(ps2.Template.Spec.InitContainers)
+	areInitContainersEqual := equality.Semantic.DeepEqual(ps1InitContainersRes, ps2InitContainersRes)
+
+	return areContainersEqual &&
+		areInitContainersEqual &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Resources, ps2.Template.Spec.Resources) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.NodeSelector, ps2.Template.Spec.NodeSelector) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Tolerations, ps2.Template.Spec.Tolerations) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Affinity, ps2.Template.Spec.Affinity) &&
```

  I think it is possible that ClusterAutoscaler is using Affinities for deciding on the structure of the newly provisioned nodes. So, some users may not want to have the same "merged" provisioning semantics. For example, if Pods from one Master have "anti-affinity" to pods in Workers. 
  
  AFAIK we don't have a use-case currently that would discriminate the semantics. Maybe in the future we need more knobs to decide what "Equivalent/Similar" means.
   
   wdyt @mwielgus ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T16:23:16Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T16:23:16Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  > Do you ask if we have a use-case for supporting "Identical"?
  
  I meant do you have cases where you want to customize the keys to identify the equalities between multiple PodSets. Let's say in some cases, you might want to include the podAffinity for identification, otherwise, you might want to exclude the podAffinity for identification.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T16:29:08Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-20T16:29:08Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  Not yet. For now the currently requested use assume actually identical podsets. The introduction of Similar is to anticipate the potential future use case, which is likely, so we are making assumptions.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T16:30:07Z

- **pkg/controller/admissionchecks/provisioning/controller.go:281** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T16:30:07Z
```diff
@@ -277,14 +278,13 @@ func (c *Controller) syncOwnedProvisionRequest(
 			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
 			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
 			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetReference { return ps.Name })
-			for _, psName := range expectedPodSets {
-				ps, psFound := podSetMap[psName]
-				psa, psaFound := psaMap[psName]
-				if !psFound || !psaFound {
-					return errInconsistentPodSetAssignments
-				}
+			mergedPodSets, err := mergePodSets(expectedPodSets, podSetMap, psaMap, prc.Spec.MergePodSets)
```

  What if the identity keys are chenged during the AdmissionCheck `Retry` something waiting phase?
  Let's say the PodSet has gpu tolerations on creation, after that the PodSet tolerations are removed during AC `Retry` phase.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T16:34:22Z

- **pkg/controller/admissionchecks/provisioning/controller.go:281** by [@mimowo](https://github.com/mimowo) — 2025-05-20T16:34:22Z
```diff
@@ -277,14 +278,13 @@ func (c *Controller) syncOwnedProvisionRequest(
 			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
 			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
 			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetReference { return ps.Name })
-			for _, psName := range expectedPodSets {
-				ps, psFound := podSetMap[psName]
-				psa, psaFound := psaMap[psName]
-				if !psFound || !psaFound {
-					return errInconsistentPodSetAssignments
-				}
+			mergedPodSets, err := mergePodSets(expectedPodSets, podSetMap, psaMap, prc.Spec.MergePodSets)
```

  ACs should not be modifying the PodSet template directly. This is what the user supplied. If some AC want to make adjustments then the PodSetUpdates in the workload.status admissionChecks.podSetUpdates should be used. I think the identity check should only care about equivalence of what is supplied to the ClusterAutoscaler, so what is in PosSet PodTemplate directly. ignoring podSetUpdates.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T16:35:09Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T16:35:09Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  Thank you for letting me know. In that case, what about `ProvisioningDirectives`? And we can describe which fields are considered ProvisioningDirectives in the documentation.
  
  After we find additional provisioning directives or want to give customizability for that. We can easily expand the definition.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T16:39:05Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-20T16:39:05Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  by ProvisioningDirectives you mean an enum value or a whole configuration struct?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T16:39:06Z

- **pkg/controller/admissionchecks/provisioning/controller.go:281** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T16:39:06Z
```diff
@@ -277,14 +278,13 @@ func (c *Controller) syncOwnedProvisionRequest(
 			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
 			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
 			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetReference { return ps.Name })
-			for _, psName := range expectedPodSets {
-				ps, psFound := podSetMap[psName]
-				psa, psaFound := psaMap[psName]
-				if !psFound || !psaFound {
-					return errInconsistentPodSetAssignments
-				}
+			mergedPodSets, err := mergePodSets(expectedPodSets, podSetMap, psaMap, prc.Spec.MergePodSets)
```

  Yes, updating the Workload is what I meant. I don't point to manually updated ProvisioningRequest.
  I wanted to check if we can handle those appropriately, even if users update Workload with a container spec, something.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T16:39:47Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T16:39:46Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  I meant the `ProvisioningDirectives` is an alternative for `Equivalent` mode.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T16:40:06Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T16:40:05Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  So, it is an enum.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T16:46:17Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-20T16:46:17Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  fyi the most likely use case for different podtemplates we anticipate is to have a slightly different container command or args, or env vars, between leader and worker 
  
  
  The proposed enum would work in this case, so sgtm. In a way Equivalent means equivalent from the perspective of scheduling directives which can be considered by ClusterAutoscaler

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-20T16:50:16Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-20T16:50:16Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  I intentionally avoided using ‘scheduling directives’ and proposed ‘provisioning directives' since it will conflict with Pod Scheduling Directives.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T16:52:09Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-20T16:52:09Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  ack, I'm ok with ProvisioningDirectives . wdyt @mwielgus?
  
  @mszadkow feel free to change it like that tentatively

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T16:57:30Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-20T16:57:30Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type MergePodSetsMode string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	Equivalent MergePodSetsMode = "Equivalent"
```

  Or maybe we have MergePodSetPolicy: Identical / IdenticalProvisioningDirectives

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-20T19:28:22Z

- **pkg/controller/admissionchecks/provisioning/controller.go:931** by [@mimowo](https://github.com/mimowo) — 2025-05-20T19:28:22Z
```diff
@@ -833,3 +833,112 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
+
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy) bool {
+	switch *mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return equality.Semantic.DeepEqual(ps1.Template, ps2.Template)
+	case kueue.ProvisioningDirectives:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsSimilar(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsSimilar(ps1, ps2 *kueue.PodSet) bool {
+	ps1ContainersRes := aggregateTotalResources(ps1.Template.Spec.Containers)
+	ps2ContainersRes := aggregateTotalResources(ps2.Template.Spec.Containers)
+	areContainersEqual := equality.Semantic.DeepEqual(ps1ContainersRes, ps2ContainersRes)
+
+	ps1InitContainersRes := aggregateTotalResources(ps1.Template.Spec.InitContainers)
+	ps2InitContainersRes := aggregateTotalResources(ps2.Template.Spec.InitContainers)
+	areInitContainersEqual := equality.Semantic.DeepEqual(ps1InitContainersRes, ps2InitContainersRes)
+
+	return areContainersEqual &&
+		areInitContainersEqual &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Resources, ps2.Template.Spec.Resources) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.NodeSelector, ps2.Template.Spec.NodeSelector) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Tolerations, ps2.Template.Spec.Tolerations) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Affinity, ps2.Template.Spec.Affinity) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.ResourceClaims, ps2.Template.Spec.ResourceClaims)
+}
+
+// aggregateTotalResources aggregates the total resources across all containers in a PodSet.
+func aggregateTotalResources(containers []corev1.Container) corev1.ResourceList {
```

  Why do you aggregate the containers instead of checking directly equality with `equality.Semantic.DeepEqual`? I think in some extreme cases this will not work properly. For example
  two containes:
  [cpu: 3, mem: 3], will aggregate to the same as [cpu: 2, mem: 1], and [cpu: 1, mem: 2]?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-21T06:51:36Z

- **pkg/controller/admissionchecks/provisioning/controller.go:931** by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T06:51:36Z
```diff
@@ -833,3 +833,112 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
+
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy) bool {
+	switch *mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return equality.Semantic.DeepEqual(ps1.Template, ps2.Template)
+	case kueue.ProvisioningDirectives:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsSimilar(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsSimilar(ps1, ps2 *kueue.PodSet) bool {
+	ps1ContainersRes := aggregateTotalResources(ps1.Template.Spec.Containers)
+	ps2ContainersRes := aggregateTotalResources(ps2.Template.Spec.Containers)
+	areContainersEqual := equality.Semantic.DeepEqual(ps1ContainersRes, ps2ContainersRes)
+
+	ps1InitContainersRes := aggregateTotalResources(ps1.Template.Spec.InitContainers)
+	ps2InitContainersRes := aggregateTotalResources(ps2.Template.Spec.InitContainers)
+	areInitContainersEqual := equality.Semantic.DeepEqual(ps1InitContainersRes, ps2InitContainersRes)
+
+	return areContainersEqual &&
+		areInitContainersEqual &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Resources, ps2.Template.Spec.Resources) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.NodeSelector, ps2.Template.Spec.NodeSelector) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Tolerations, ps2.Template.Spec.Tolerations) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Affinity, ps2.Template.Spec.Affinity) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.ResourceClaims, ps2.Template.Spec.ResourceClaims)
+}
+
+// aggregateTotalResources aggregates the total resources across all containers in a PodSet.
+func aggregateTotalResources(containers []corev1.Container) corev1.ResourceList {
```

  right, I see the problem
  My idea was to aggregate to avoid situation when I compare containers that are not in order.
  But yeah it's 99% of time single container, so shouldn't be a problem

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T06:54:09Z

- **pkg/controller/admissionchecks/provisioning/controller.go:931** by [@mimowo](https://github.com/mimowo) — 2025-05-21T06:54:09Z
```diff
@@ -833,3 +833,112 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
+
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy) bool {
+	switch *mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return equality.Semantic.DeepEqual(ps1.Template, ps2.Template)
+	case kueue.ProvisioningDirectives:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsSimilar(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsSimilar(ps1, ps2 *kueue.PodSet) bool {
+	ps1ContainersRes := aggregateTotalResources(ps1.Template.Spec.Containers)
+	ps2ContainersRes := aggregateTotalResources(ps2.Template.Spec.Containers)
+	areContainersEqual := equality.Semantic.DeepEqual(ps1ContainersRes, ps2ContainersRes)
+
+	ps1InitContainersRes := aggregateTotalResources(ps1.Template.Spec.InitContainers)
+	ps2InitContainersRes := aggregateTotalResources(ps2.Template.Spec.InitContainers)
+	areInitContainersEqual := equality.Semantic.DeepEqual(ps1InitContainersRes, ps2InitContainersRes)
+
+	return areContainersEqual &&
+		areInitContainersEqual &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Resources, ps2.Template.Spec.Resources) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.NodeSelector, ps2.Template.Spec.NodeSelector) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Tolerations, ps2.Template.Spec.Tolerations) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Affinity, ps2.Template.Spec.Affinity) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.ResourceClaims, ps2.Template.Spec.ResourceClaims)
+}
+
+// aggregateTotalResources aggregates the total resources across all containers in a PodSet.
+func aggregateTotalResources(containers []corev1.Container) corev1.ResourceList {
```

  we can assume the user puts the containers in the same order

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-21T06:54:12Z

- **pkg/controller/admissionchecks/provisioning/controller.go:931** by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T06:54:12Z
```diff
@@ -833,3 +833,112 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
+
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy) bool {
+	switch *mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return equality.Semantic.DeepEqual(ps1.Template, ps2.Template)
+	case kueue.ProvisioningDirectives:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsSimilar(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsSimilar(ps1, ps2 *kueue.PodSet) bool {
+	ps1ContainersRes := aggregateTotalResources(ps1.Template.Spec.Containers)
+	ps2ContainersRes := aggregateTotalResources(ps2.Template.Spec.Containers)
+	areContainersEqual := equality.Semantic.DeepEqual(ps1ContainersRes, ps2ContainersRes)
+
+	ps1InitContainersRes := aggregateTotalResources(ps1.Template.Spec.InitContainers)
+	ps2InitContainersRes := aggregateTotalResources(ps2.Template.Spec.InitContainers)
+	areInitContainersEqual := equality.Semantic.DeepEqual(ps1InitContainersRes, ps2InitContainersRes)
+
+	return areContainersEqual &&
+		areInitContainersEqual &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Resources, ps2.Template.Spec.Resources) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.NodeSelector, ps2.Template.Spec.NodeSelector) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Tolerations, ps2.Template.Spec.Tolerations) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Affinity, ps2.Template.Spec.Affinity) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.ResourceClaims, ps2.Template.Spec.ResourceClaims)
+}
+
+// aggregateTotalResources aggregates the total resources across all containers in a PodSet.
+func aggregateTotalResources(containers []corev1.Container) corev1.ResourceList {
```

  this way we also discard situation when they have similar resources but different amount of containers, right?
  if e.g.
  leader pod has 1 container, but worker container has 2 containers for some reason - they are not similar (and that's true indeed)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T06:55:51Z

- **pkg/controller/admissionchecks/provisioning/controller.go:931** by [@mimowo](https://github.com/mimowo) — 2025-05-21T06:55:51Z
```diff
@@ -833,3 +833,112 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
+
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		mergable := false
+		for i, mps := range merged {
+			if canMergePodSets(mps.PodSet, ps, mergeMode) {
+				merged[i].Count += ptr.Deref(psa.Count, ps.Count)
+				mergable = true
+				break
+			}
+		}
+
+		if !mergable {
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return merged, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergeMode *kueue.ProvisioningRequestConfigMergePodSetPolicy) bool {
+	switch *mergeMode {
+	case kueue.Identical:
+		// Strict comparison: all fields must match
+		return equality.Semantic.DeepEqual(ps1.Template, ps2.Template)
+	case kueue.ProvisioningDirectives:
+		// Relaxed comparison: only resource requests must match
+		return arePodSetsSimilar(ps1, ps2)
+	default:
+		// Unsupported merge mode
+		return false
+	}
+}
+
+func arePodSetsSimilar(ps1, ps2 *kueue.PodSet) bool {
+	ps1ContainersRes := aggregateTotalResources(ps1.Template.Spec.Containers)
+	ps2ContainersRes := aggregateTotalResources(ps2.Template.Spec.Containers)
+	areContainersEqual := equality.Semantic.DeepEqual(ps1ContainersRes, ps2ContainersRes)
+
+	ps1InitContainersRes := aggregateTotalResources(ps1.Template.Spec.InitContainers)
+	ps2InitContainersRes := aggregateTotalResources(ps2.Template.Spec.InitContainers)
+	areInitContainersEqual := equality.Semantic.DeepEqual(ps1InitContainersRes, ps2InitContainersRes)
+
+	return areContainersEqual &&
+		areInitContainersEqual &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Resources, ps2.Template.Spec.Resources) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.NodeSelector, ps2.Template.Spec.NodeSelector) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Tolerations, ps2.Template.Spec.Tolerations) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.Affinity, ps2.Template.Spec.Affinity) &&
+		equality.Semantic.DeepEqual(ps1.Template.Spec.ResourceClaims, ps2.Template.Spec.ResourceClaims)
+}
+
+// aggregateTotalResources aggregates the total resources across all containers in a PodSet.
+func aggregateTotalResources(containers []corev1.Container) corev1.ResourceList {
```

  yes, let's discard similar containers for now. we may or may not need to support in the future. If we ever need it it would be under dedicated enum value or config

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-21T09:14:04Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:90** by [@mwielgus](https://github.com/mwielgus) — 2025-05-21T09:14:03Z
```diff
@@ -76,6 +81,13 @@ type ProvisioningRequestConfigSpec struct {
 	//
 	// +optional
 	PodSetUpdates *ProvisioningRequestPodSetUpdates `json:"podSetUpdates,omitempty"`
+
+	// mergePodSetPolicy specifies the policy for merging PodSets before being passed
+	// to the cluster autoscaler.
+	//
+	// +optional
+	// +kubebuilder:validation:Enum=Identical;ProvisioningDirectives
+	MergePodSetsPolicy *ProvisioningRequestConfigMergePodSetPolicy `json:"mergePodSetPolicy,omitempty"`
```

  Maybe PodSetMergePolicy?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-21T09:20:08Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:90** by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T09:20:08Z
```diff
@@ -76,6 +81,13 @@ type ProvisioningRequestConfigSpec struct {
 	//
 	// +optional
 	PodSetUpdates *ProvisioningRequestPodSetUpdates `json:"podSetUpdates,omitempty"`
+
+	// mergePodSetPolicy specifies the policy for merging PodSets before being passed
+	// to the cluster autoscaler.
+	//
+	// +optional
+	// +kubebuilder:validation:Enum=Identical;ProvisioningDirectives
+	MergePodSetsPolicy *ProvisioningRequestConfigMergePodSetPolicy `json:"mergePodSetPolicy,omitempty"`
```

  sounds better, thanks

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-21T09:25:58Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mwielgus](https://github.com/mwielgus) — 2025-05-21T09:25:58Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
```

  The merge is done based on the workload definition. In the definition there is usually nothing about provisioning. There are scheduling requirements (node selectors, affinity, tolerations etc) which may result in provisioning some nodes. However provisioning itself is usually not the intent of the user. They just provide constraints. Thus I have mixed feelings about this name. What about "IdenticalSchedulingRequirements" ?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-21T09:26:38Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:32** by [@mwielgus](https://github.com/mwielgus) — 2025-05-21T09:26:38Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
+	Identical              ProvisioningRequestConfigMergePodSetPolicy = "Identical"
```

  Identical, but what? Maybe "IdenticalPodTemplates" or something along these lines?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T09:43:28Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T09:43:27Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
+	Identical              ProvisioningRequestConfigMergePodSetPolicy = "Identical"
```

  +1 on `IdenticalPodTemplates`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T09:51:31Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T09:51:31Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
```

  IIUC, we use the containers as identification when merging multiple PodSets. It indicates env, command, and others will be used. And those are not related to scheduling. So, I intentionally avoid the "scheduling" word.
  
  >  However provisioning itself is usually not the intent of the user. 
  
  Could you elaborate this one? I thought we can say `IdenticalProvisioningDirectives` to represent "The workloads with the same Provisioning Directives are considered to be able to request additional Nodes at once"

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T09:56:33Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:90** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T09:56:31Z
```diff
@@ -76,6 +81,13 @@ type ProvisioningRequestConfigSpec struct {
 	//
 	// +optional
 	PodSetUpdates *ProvisioningRequestPodSetUpdates `json:"podSetUpdates,omitempty"`
+
+	// podSetMergePolicy specifies the policy for merging PodSets before being passed
+	// to the cluster autoscaler.
+	//
+	// +optional
+	// +kubebuilder:validation:Enum=Identical;ProvisioningDirectives
+	PodSetMergePolicy *ProvisioningRequestConfigPodSetMergePolicy `json:"podSetMergePolicy,omitempty"`
```

  @mwielgus @mimowo Do you assume any usecase to addadditional parameters in the future? If yes, we can make a space for now in the following: 
  This is the similar decision as admissionChecks and admissionCheckStrategies in ClusterQueue.
  
  ```yaml
  porSetMergePolicy:
    mode: xxx
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T10:15:45Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:90** by [@mimowo](https://github.com/mimowo) — 2025-05-21T10:15:45Z
```diff
@@ -76,6 +81,13 @@ type ProvisioningRequestConfigSpec struct {
 	//
 	// +optional
 	PodSetUpdates *ProvisioningRequestPodSetUpdates `json:"podSetUpdates,omitempty"`
+
+	// podSetMergePolicy specifies the policy for merging PodSets before being passed
+	// to the cluster autoscaler.
+	//
+	// +optional
+	// +kubebuilder:validation:Enum=Identical;ProvisioningDirectives
+	PodSetMergePolicy *ProvisioningRequestConfigPodSetMergePolicy `json:"podSetMergePolicy,omitempty"`
```

  We don't have such use case for now. When they come we can introduce the podSetMergeConfig or podSetMergeStrategy with more details.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T10:15:58Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:32** by [@mimowo](https://github.com/mimowo) — 2025-05-21T10:15:57Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
+	Identical              ProvisioningRequestConfigMergePodSetPolicy = "Identical"
```

  +1

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-21T10:19:36Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mwielgus](https://github.com/mwielgus) — 2025-05-21T10:19:36Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
```

  The problem that I have with this is that there are no user-specified provisioning directives at the podtemplate/workload level. There is affinity, node selector, resource request and these things are being checked in this mode. And in my opinion these fields are "scheduling" requirements.  
  
  As provisioning directive I understand a specific type of provisioning request/class that is being used to admit the workload. And this stuff is the same for all PodSets that are being used there.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T10:21:26Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-21T10:21:26Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
```

  Maybe @mwielgus can elaborate more, but I synced with Marcin, and there are two main points:
  1. ProvisioningDirectives is not a known term current, also SchedulingDirectives are not widely known to users, and this is user / admin-facing API
  2. creating PodTemplates users don't think about "Provisioning", it is job of the admin to define the ResourceFlavors. Depending on the selected resource flavor the Pod might be provisioning or not (may land on Reservations or Provisioning RF). So, the fields that user specifies in the template are the user's "scheduling requirements", not "provisioning requirements".
  
  I think these are valid points. Having said that I'm personally ok with either `IdenticalProvisioningDirectives` or `IdenticalSchedulingRequirements`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T10:26:52Z

- **pkg/controller/admissionchecks/provisioning/controller.go:281** by [@mimowo](https://github.com/mimowo) — 2025-05-21T10:26:51Z
```diff
@@ -277,14 +278,13 @@ func (c *Controller) syncOwnedProvisionRequest(
 			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
 			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
 			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetReference { return ps.Name })
-			for _, psName := range expectedPodSets {
-				ps, psFound := podSetMap[psName]
-				psa, psaFound := psaMap[psName]
-				if !psFound || !psaFound {
-					return errInconsistentPodSetAssignments
-				}
+			mergedPodSets, err := mergePodSets(expectedPodSets, podSetMap, psaMap, prc.Spec.MergePodSets)
```

  We don't support in Kueue users updating the container specs inside the workload.status.podSets. 
  
  If users update workload.status.podSets they may run into issues (like immediate Workload eviction),  but I don't think there is any new class of issues they may run into by merging of PodSets.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T10:42:46Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:90** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T10:42:45Z
```diff
@@ -76,6 +81,13 @@ type ProvisioningRequestConfigSpec struct {
 	//
 	// +optional
 	PodSetUpdates *ProvisioningRequestPodSetUpdates `json:"podSetUpdates,omitempty"`
+
+	// podSetMergePolicy specifies the policy for merging PodSets before being passed
+	// to the cluster autoscaler.
+	//
+	// +optional
+	// +kubebuilder:validation:Enum=Identical;ProvisioningDirectives
+	PodSetMergePolicy *ProvisioningRequestConfigPodSetMergePolicy `json:"podSetMergePolicy,omitempty"`
```

  > We don't have such use case for now. When they come we can introduce the podSetMergeConfig or podSetMergeStrategy with more details.
  
  SGTM, thanks

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T12:17:48Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T12:17:47Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
```

  > There is affinity, node selector, resource request and these things are being checked in this mode. And in my opinion these fields are "scheduling" requirements.
  
  Yes, you're right. My point is this identical mode contain non-scheduling semantical keys like containers and initContainers: https://github.com/kubernetes-sigs/kueue/blob/aa5378522c95c184114bcff2ab8145df2f52fccc/pkg/controller/admissionchecks/provisioning/controller.go#L913-L914
  
  > As provisioning directive I understand a specific type of provisioning request/class that is being used to admit the workload. And this stuff is the same for all PodSets that are being used there.
  
  I see. However, actually, this mode uses non scheduling related parameters as well. I think `SchedulingDirectives` are widely known term since it is used in k/k feature gate like MutableSchedulingDirectivesForJobs. So, what about `WorkloadSchedulingRequirements` or something?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T12:18:58Z

- **pkg/controller/admissionchecks/provisioning/controller.go:281** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T12:18:57Z
```diff
@@ -277,14 +278,13 @@ func (c *Controller) syncOwnedProvisionRequest(
 			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
 			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
 			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetReference { return ps.Name })
-			for _, psName := range expectedPodSets {
-				ps, psFound := podSetMap[psName]
-				psa, psaFound := psaMap[psName]
-				if !psFound || !psaFound {
-					return errInconsistentPodSetAssignments
-				}
+			mergedPodSets, err := mergePodSets(expectedPodSets, podSetMap, psaMap, prc.Spec.MergePodSets)
```

  Thank you for describing that. If there are any conflicts when recomputing the merged PodSet, I'm ok with that.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-05-21T12:51:18Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mwielgus](https://github.com/mwielgus) — 2025-05-21T12:51:18Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
```

  I'm OK with IndenticalWorkloadSchedulingRequirements

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-05-21T12:56:44Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@tenzen-y](https://github.com/tenzen-y) — 2025-05-21T12:56:44Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigMergePodSetPolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	ProvisioningDirectives ProvisioningRequestConfigMergePodSetPolicy = "ProvisioningDirectives"
```

  > I'm OK with IndenticalWorkloadSchedulingRequirements
  
  LGTM, thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T13:56:58Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:31** by [@mimowo](https://github.com/mimowo) — 2025-05-21T13:56:57Z
```diff
@@ -21,10 +21,15 @@ import (
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 )
 
+type ProvisioningRequestConfigPodSetMergePolicy string
+
 const (
 	// ProvisioningRequestControllerName is the name used by the Provisioning
 	// Request admission check controller.
 	ProvisioningRequestControllerName = "kueue.x-k8s.io/provisioning-request"
+
+	IdenticalSchedulingRequirements ProvisioningRequestConfigPodSetMergePolicy = "IdenticalSchedulingRequirements"
```

  rename to IdenticalWorkloadSchedulingRequirements as per:
  https://github.com/kubernetes-sigs/kueue/pull/5290#discussion_r2100227988

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T14:17:53Z

- **pkg/controller/admissionchecks/provisioning/controller.go:280** by [@mimowo](https://github.com/mimowo) — 2025-05-21T14:17:53Z
```diff
@@ -277,14 +278,13 @@ func (c *Controller) syncOwnedProvisionRequest(
 			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
 			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
 			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetReference { return ps.Name })
```

  Don't build the map here,because it is only used inside `mergePodSets`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T14:18:22Z

- **pkg/controller/admissionchecks/provisioning/controller.go:279** by [@mimowo](https://github.com/mimowo) — 2025-05-21T14:18:22Z
```diff
@@ -277,14 +278,13 @@ func (c *Controller) syncOwnedProvisionRequest(
 			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
 			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
```

  Also, if this helper structure is needed, then build it inside `mergePodSets`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T14:21:08Z

- **pkg/controller/admissionchecks/provisioning/controller.go:867** by [@mimowo](https://github.com/mimowo) — 2025-05-21T14:21:08Z
```diff
@@ -833,3 +833,101 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.ProvisioningRequestConfigPodSetMergePolicy,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
```

  I would prefer to avoid this duplication. We can just respond false from `canMergePodSets`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T14:23:38Z

- **pkg/controller/admissionchecks/provisioning/controller.go:876** by [@mimowo](https://github.com/mimowo) — 2025-05-21T14:23:38Z
```diff
@@ -833,3 +833,101 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	expectedPodSets []kueue.PodSetReference,
+	podSetMap map[kueue.PodSetReference]*kueue.PodSet,
+	psaMap map[kueue.PodSetReference]*kueue.PodSetAssignment,
+	mergeMode *kueue.ProvisioningRequestConfigPodSetMergePolicy,
+) ([]MergedPodSet, error) {
+	merged := []MergedPodSet{}
+
+	if mergeMode == nil {
+		for _, psName := range expectedPodSets {
+			ps, psFound := podSetMap[psName]
+			psa, psaFound := psaMap[psName]
+			if !psFound || !psaFound {
+				return nil, errInconsistentPodSetAssignments
+			}
+			merged = append(merged, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+		return merged, nil
+	}
+
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		mergable := false
```

  ```suggestion
  		merged := false
  ```
  nit, but it will read better I think

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T14:28:50Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1404** by [@mimowo](https://github.com/mimowo) — 2025-05-21T14:28:50Z
```diff
@@ -1256,4 +1256,275 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 	})
+
+	ginkgo.When("A workload is using a provision request with enabled PodSetMergePolicy", func() {
+		var (
+			ns             *corev1.Namespace
+			wlKey          types.NamespacedName
+			provReqKey     types.NamespacedName
+			ac             *kueue.AdmissionCheck
+			prc            *kueue.ProvisioningRequestConfig
+			rf             *kueue.ResourceFlavor
+			cq             *kueue.ClusterQueue
+			lq             *kueue.LocalQueue
+			admission      *kueue.Admission
+			createdRequest autoscaling.ProvisioningRequest
+			updatedWl      kueue.Workload
+		)
+
+		ginkgo.JustBeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "provisioning-")
+
+			prc = baseConfig.Clone().
+				RetryLimit(0).
+				PodSetMergePolicy(kueue.IdenticalSchedulingRequirements).
+				Obj()
+			util.MustCreate(ctx, k8sClient, prc)
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(kueue.ProvisioningRequestControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "prov-config").
+				Obj()
+			util.MustCreate(ctx, k8sClient, ac)
+
+			rf = testing.MakeResourceFlavor(flavorOnDemand).NodeLabel("ns1", "ns1v").Obj()
+			util.MustCreate(ctx, k8sClient, rf)
+
+			cq = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				AdmissionChecks(kueue.AdmissionCheckReference(ac.Name)).
+				Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+
+			lq = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lq), lq)).Should(gomega.Succeed())
+				g.Expect(lq.Status.Conditions).Should(testing.HaveConditionStatusTrue(kueue.LocalQueueActive))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue(kueue.LocalQueueName(lq.Name)).
+				PodSets(
+					*testing.MakePodSet("master", 1).
+						Request(corev1.ResourceCPU, "1").
+						Request(corev1.ResourceMemory, "2Gi").
+						Image("image").
+						Obj(),
+					*testing.MakePodSet("worker1", 2).
+						Request(corev1.ResourceCPU, "1").
+						Request(corev1.ResourceMemory, "2Gi").
+						Image("image").
+						Obj(),
+					// this PodSets has different RestartPolicy, this often differs e.b. in PyTorchJob
+					*testing.MakePodSet("worker2", 2).
+						Request(corev1.ResourceCPU, "1").
+						Request(corev1.ResourceMemory, "2Gi").
+						Image("image").
+						RestartPolicy(corev1.RestartPolicyOnFailure).
+						Obj(),
+					// this PodSets has different resource requests
+					*testing.MakePodSet("worker3", 2).
+						Request(corev1.ResourceCPU, "2").
+						Request(corev1.ResourceMemory, "4Gi").
+						Image("image").
+						RestartPolicy(corev1.RestartPolicyOnFailure).
+						Obj(),
+				).
+				Obj()
+			util.MustCreate(ctx, k8sClient, wl)
+
+			wlKey = client.ObjectKeyFromObject(wl)
+			provReqKey = types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+			}
+
+			admission = testing.MakeAdmission(cq.Name).
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "master",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("2Gi"),
+						},
+						Count: ptr.To[int32](1),
+					},
+					kueue.PodSetAssignment{
+						Name: "worker1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("2Gi"),
+						},
+						Count: ptr.To[int32](1),
+					},
+					kueue.PodSetAssignment{
+						Name: "worker2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("2Gi"),
+						},
+						Count: ptr.To[int32](1),
+					},
+					kueue.PodSetAssignment{
+						Name: "worker3",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU:    resource.MustParse("2"),
+							corev1.ResourceMemory: resource.MustParse("4Gi"),
+						},
+						Count: ptr.To[int32](1),
+					},
+				).
+				Obj()
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, prc, true)
+		})
+
+		// test multi PodSets workloads with different merge modes
```

  To be fair I don't see much value in multiple integration tests for all options. One integration test already proves that the mechanism kicks-in. The other test case scenarios can be covered in unit tests.
  
  hint: take a backup of the tests in case some reviewer disagrees, but I think we should be conscious about the "testing pyramid" and avoid testing corner cases in integration tests which take long already.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T14:30:58Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1321** by [@mimowo](https://github.com/mimowo) — 2025-05-21T14:30:58Z
```diff
@@ -1256,4 +1256,275 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 	})
+
+	ginkgo.When("A workload is using a provision request with enabled PodSetMergePolicy", func() {
+		var (
+			ns             *corev1.Namespace
+			wlKey          types.NamespacedName
+			provReqKey     types.NamespacedName
+			ac             *kueue.AdmissionCheck
+			prc            *kueue.ProvisioningRequestConfig
+			rf             *kueue.ResourceFlavor
+			cq             *kueue.ClusterQueue
+			lq             *kueue.LocalQueue
+			admission      *kueue.Admission
+			createdRequest autoscaling.ProvisioningRequest
+			updatedWl      kueue.Workload
+		)
+
+		ginkgo.JustBeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "provisioning-")
+
+			prc = baseConfig.Clone().
+				RetryLimit(0).
+				PodSetMergePolicy(kueue.IdenticalSchedulingRequirements).
+				Obj()
+			util.MustCreate(ctx, k8sClient, prc)
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(kueue.ProvisioningRequestControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "prov-config").
+				Obj()
+			util.MustCreate(ctx, k8sClient, ac)
+
+			rf = testing.MakeResourceFlavor(flavorOnDemand).NodeLabel("ns1", "ns1v").Obj()
+			util.MustCreate(ctx, k8sClient, rf)
+
+			cq = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				AdmissionChecks(kueue.AdmissionCheckReference(ac.Name)).
+				Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+
+			lq = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lq), lq)).Should(gomega.Succeed())
+				g.Expect(lq.Status.Conditions).Should(testing.HaveConditionStatusTrue(kueue.LocalQueueActive))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue(kueue.LocalQueueName(lq.Name)).
+				PodSets(
+					*testing.MakePodSet("master", 1).
+						Request(corev1.ResourceCPU, "1").
+						Request(corev1.ResourceMemory, "2Gi").
+						Image("image").
+						Obj(),
+					*testing.MakePodSet("worker1", 2).
+						Request(corev1.ResourceCPU, "1").
+						Request(corev1.ResourceMemory, "2Gi").
+						Image("image").
+						Obj(),
```

  Let's just have master and worker templates. To make them slightly different use different env vars.
  
  This way the integration test case will be closer to the actual use case.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-21T15:26:02Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1321** by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T15:26:02Z
```diff
@@ -1256,4 +1256,275 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 	})
+
+	ginkgo.When("A workload is using a provision request with enabled PodSetMergePolicy", func() {
+		var (
+			ns             *corev1.Namespace
+			wlKey          types.NamespacedName
+			provReqKey     types.NamespacedName
+			ac             *kueue.AdmissionCheck
+			prc            *kueue.ProvisioningRequestConfig
+			rf             *kueue.ResourceFlavor
+			cq             *kueue.ClusterQueue
+			lq             *kueue.LocalQueue
+			admission      *kueue.Admission
+			createdRequest autoscaling.ProvisioningRequest
+			updatedWl      kueue.Workload
+		)
+
+		ginkgo.JustBeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "provisioning-")
+
+			prc = baseConfig.Clone().
+				RetryLimit(0).
+				PodSetMergePolicy(kueue.IdenticalSchedulingRequirements).
+				Obj()
+			util.MustCreate(ctx, k8sClient, prc)
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(kueue.ProvisioningRequestControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "prov-config").
+				Obj()
+			util.MustCreate(ctx, k8sClient, ac)
+
+			rf = testing.MakeResourceFlavor(flavorOnDemand).NodeLabel("ns1", "ns1v").Obj()
+			util.MustCreate(ctx, k8sClient, rf)
+
+			cq = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				AdmissionChecks(kueue.AdmissionCheckReference(ac.Name)).
+				Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+
+			lq = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lq), lq)).Should(gomega.Succeed())
+				g.Expect(lq.Status.Conditions).Should(testing.HaveConditionStatusTrue(kueue.LocalQueueActive))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue(kueue.LocalQueueName(lq.Name)).
+				PodSets(
+					*testing.MakePodSet("master", 1).
+						Request(corev1.ResourceCPU, "1").
+						Request(corev1.ResourceMemory, "2Gi").
+						Image("image").
+						Obj(),
+					*testing.MakePodSet("worker1", 2).
+						Request(corev1.ResourceCPU, "1").
+						Request(corev1.ResourceMemory, "2Gi").
+						Image("image").
+						Obj(),
```

  I have added labels, they differ too

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T17:34:32Z

- **pkg/controller/admissionchecks/provisioning/controller.go:869** by [@mimowo](https://github.com/mimowo) — 2025-05-21T17:34:32Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	wl *kueue.Workload,
+	prcSpec *kueue.ProvisioningRequestConfigSpec,
+) ([]MergedPodSet, error) {
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prcSpec.ManagedResources)
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetReference { return ps.Name })
+
+	mergePolicy := prcSpec.PodSetMergePolicy
+	mergedPodSets := []MergedPodSet{}
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		// Try to merge with an existing pod set
+		merged := false
+		if mergePolicy != nil {
+			for i, mps := range mergedPodSets {
+				if merged = canMergePodSets(mps.PodSet, ps, mergePolicy); merged {
+					mergedPodSets[i].Count += ptr.Deref(psa.Count, ps.Count)
+					break
+				}
+			}
+		}
+
+		// If not merged (or if mergePolicy is nil), add as a new pod set
```

  Drop this comment, it seems not matching the impl, and it is also self-explanatory

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T17:34:58Z

- **pkg/controller/admissionchecks/provisioning/controller.go:889** by [@mimowo](https://github.com/mimowo) — 2025-05-21T17:34:58Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	wl *kueue.Workload,
+	prcSpec *kueue.ProvisioningRequestConfigSpec,
+) ([]MergedPodSet, error) {
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prcSpec.ManagedResources)
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetReference { return ps.Name })
+
+	mergePolicy := prcSpec.PodSetMergePolicy
+	mergedPodSets := []MergedPodSet{}
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		// Try to merge with an existing pod set
+		merged := false
+		if mergePolicy != nil {
+			for i, mps := range mergedPodSets {
+				if merged = canMergePodSets(mps.PodSet, ps, mergePolicy); merged {
+					mergedPodSets[i].Count += ptr.Deref(psa.Count, ps.Count)
+					break
+				}
+			}
+		}
+
+		// If not merged (or if mergePolicy is nil), add as a new pod set
+		if !merged {
+			mergedPodSets = append(mergedPodSets, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return mergedPodSets, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergePolicy *kueue.ProvisioningRequestConfigPodSetMergePolicy) bool {
+	switch *mergePolicy {
+	case kueue.IdenticalPodTemplates:
+		// Strict comparison: all fields must match
+		return equality.Semantic.DeepEqual(ps1.Template, ps2.Template)
+	case kueue.IdenticalWorkloadSchedulingRequirements:
+		// Relaxed comparison: only resource requests must match
```

  Drop the comment or make it accurate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T17:36:30Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1415** by [@mimowo](https://github.com/mimowo) — 2025-05-21T17:36:30Z
```diff
@@ -1256,4 +1256,167 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			})
 		})
 	})
+
+	ginkgo.When("A workload is using a provision request with enabled PodSetMergePolicy", func() {
+		var (
+			ns             *corev1.Namespace
+			wlKey          types.NamespacedName
+			provReqKey     types.NamespacedName
+			ac             *kueue.AdmissionCheck
+			prc            *kueue.ProvisioningRequestConfig
+			rf             *kueue.ResourceFlavor
+			cq             *kueue.ClusterQueue
+			lq             *kueue.LocalQueue
+			admission      *kueue.Admission
+			createdRequest autoscaling.ProvisioningRequest
+			updatedWl      kueue.Workload
+		)
+
+		ginkgo.JustBeforeEach(func() {
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "provisioning-")
+
+			prc = baseConfig.Clone().
+				RetryLimit(0).
+				PodSetMergePolicy(kueue.IdenticalWorkloadSchedulingRequirements).
+				Obj()
+			util.MustCreate(ctx, k8sClient, prc)
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(kueue.ProvisioningRequestControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", "prov-config").
+				Obj()
+			util.MustCreate(ctx, k8sClient, ac)
+
+			rf = testing.MakeResourceFlavor(flavorOnDemand).NodeLabel("ns1", "ns1v").Obj()
+			util.MustCreate(ctx, k8sClient, rf)
+
+			cq = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(resourceGPU, "5", "5").Obj()).
+				Cohort("cohort").
+				AdmissionChecks(kueue.AdmissionCheckReference(ac.Name)).
+				Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+
+			lq = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lq), lq)).Should(gomega.Succeed())
+				g.Expect(lq.Status.Conditions).Should(testing.HaveConditionStatusTrue(kueue.LocalQueueActive))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				Queue(kueue.LocalQueueName(lq.Name)).
+				PodSets(
+					*testing.MakePodSet("master", 1).
+						Request(corev1.ResourceCPU, "1").
+						Request(corev1.ResourceMemory, "2Gi").
+						Image("image").
+						Labels(map[string]string{"role": "master"}).
+						Obj(),
+					*testing.MakePodSet("worker", 4).
+						Request(corev1.ResourceCPU, "1").
+						Request(corev1.ResourceMemory, "2Gi").
+						Image("image").
+						Labels(map[string]string{"role": "worker"}).
+						Obj(),
+				).
+				Obj()
+			util.MustCreate(ctx, k8sClient, wl)
+
+			wlKey = client.ObjectKeyFromObject(wl)
+			provReqKey = types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+			}
+
+			admission = testing.MakeAdmission(cq.Name).
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "master",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("2Gi"),
+						},
+						Count: ptr.To[int32](1),
+					},
+					kueue.PodSetAssignment{
+						Name: "worker",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("2Gi"),
+						},
+						Count: ptr.To[int32](2),
+					},
+				).
+				Obj()
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, prc, true)
+		})
+
+		ginkgo.It("Should merge similar PodSets into one PodTemplate, PodSetMergePolicy is IdenticalWorkloadSchedulingRequirements", func() {
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the provision request is created", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(1))
+				gomega.Expect(createdRequest.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+
+				mergedPodSet := createdRequest.Spec.PodSets[0]
+				gomega.Expect(mergedPodSet.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(mergedPodSet.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the merged pod template
+				mergedTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      mergedPodSet.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, templateKey, mergedTemplate)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(mergedTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(mergedTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+				gomega.Expect(mergedTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, nil)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the provision request is preserved", func() {
```

  I'm not sure exactly why we are checking this, do we suspect it might have been deleted? I would prefer to do not waste time on checks which aren't directly related to the new code. Also the intention of the By "Removing the quota reservation from the workload" is not clear to me.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T17:37:59Z

- **pkg/controller/admissionchecks/provisioning/controller.go:841** by [@mimowo](https://github.com/mimowo) — 2025-05-21T17:37:59Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
```

  Is this fully covered by the unit tests? If not, what are the missing branches?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T17:38:51Z

- **pkg/controller/admissionchecks/provisioning/controller.go:886** by [@mimowo](https://github.com/mimowo) — 2025-05-21T17:38:50Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
+	wl *kueue.Workload,
+	prcSpec *kueue.ProvisioningRequestConfigSpec,
+) ([]MergedPodSet, error) {
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prcSpec.ManagedResources)
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) kueue.PodSetReference { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetReference { return ps.Name })
+
+	mergePolicy := prcSpec.PodSetMergePolicy
+	mergedPodSets := []MergedPodSet{}
+	for _, psName := range expectedPodSets {
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+		if !psFound || !psaFound {
+			return nil, errInconsistentPodSetAssignments
+		}
+
+		// Try to merge with an existing pod set
+		merged := false
+		if mergePolicy != nil {
+			for i, mps := range mergedPodSets {
+				if merged = canMergePodSets(mps.PodSet, ps, mergePolicy); merged {
+					mergedPodSets[i].Count += ptr.Deref(psa.Count, ps.Count)
+					break
+				}
+			}
+		}
+
+		// If not merged (or if mergePolicy is nil), add as a new pod set
+		if !merged {
+			mergedPodSets = append(mergedPodSets, MergedPodSet{
+				Name:             psName,
+				PodSet:           ps,
+				PodSetAssignment: psa,
+				Count:            ptr.Deref(psa.Count, ps.Count),
+			})
+		}
+	}
+
+	return mergedPodSets, nil
+}
+
+func canMergePodSets(ps1, ps2 *kueue.PodSet, mergePolicy *kueue.ProvisioningRequestConfigPodSetMergePolicy) bool {
+	switch *mergePolicy {
+	case kueue.IdenticalPodTemplates:
+		// Strict comparison: all fields must match
```

  I think this comment isn't necessary. This part is self explanatory. Also, it may be misleading as not just fields but also metadata.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-21T18:08:44Z

- **pkg/controller/admissionchecks/provisioning/controller.go:841** by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T18:08:44Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
```

  According to coverage tool it 95.2% coverage.
  I have covered both new policies and I showed when podSets can be and won't be merged in both of those scenarios.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T18:11:50Z

- **pkg/controller/admissionchecks/provisioning/controller.go:841** by [@mimowo](https://github.com/mimowo) — 2025-05-21T18:11:50Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
```

  cool, what about the missing 5 percent, could this be covered too?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-21T18:15:35Z

- **pkg/controller/admissionchecks/provisioning/controller.go:841** by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T18:15:35Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
```

  I think it's only this part not covered: 	
  ```	
  if !psFound || !psaFound {
     return nil, errInconsistentPodSetAssignments
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-21T18:17:39Z

- **pkg/controller/admissionchecks/provisioning/controller.go:841** by [@mimowo](https://github.com/mimowo) — 2025-05-21T18:17:39Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
```

  Cool, this wasn't covered before either, so fine with me.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-21T18:27:15Z

- **pkg/controller/admissionchecks/provisioning/controller.go:841** by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T18:27:15Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
```

  yes, it's possible, I will push 100% covered after I pass verify

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-05-21T18:30:09Z

- **pkg/controller/admissionchecks/provisioning/controller.go:841** by [@mszadkow](https://github.com/mszadkow) — 2025-05-21T18:30:09Z
```diff
@@ -833,3 +830,88 @@ func limitObjectName(fullName string) string {
 	hashBytes := hex.EncodeToString(h.Sum(nil))
 	return fmt.Sprintf("%s-%s", fullName[:objNameMaxPrefixLength], hashBytes[:objNameHashLength])
 }
+
+type MergedPodSet struct {
+	Name             kueue.PodSetReference
+	PodSet           *kueue.PodSet
+	PodSetAssignment *kueue.PodSetAssignment
+	Count            int32
+}
+
+func mergePodSets(
```

  done
