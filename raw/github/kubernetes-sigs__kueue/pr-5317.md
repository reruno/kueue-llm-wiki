# PR #5317: Extract EvictWorkload to Function

**Summary**: Extract EvictWorkload to Function

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5317

**Last updated**: 2025-05-23T14:36:34Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `evict_workload` → `main`
- **Created**: 2025-05-23T09:17:39Z
- **Updated**: 2025-05-23T14:36:34Z
- **Merged**: 2025-05-23T12:02:36Z
- **Closed**: 2025-05-23T12:02:36Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+42 / -24**

## Description

#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:
Small follow-up to #5272

Extract EvictWorkload to function, and reduce scope of several variables. 

#### Which issue(s) this PR fixes:
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-23T09:17:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2fcc0debe69f515ae0bd5f954bf23a4dc78b8445 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68304c21c99094000830653d |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-23T10:07:01Z

As we are on cleaning the controller, could you also make NodeMultipleFailuresEvictionMessageFormat not exported?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-23T10:23:06Z

This is the exact issue I've created a few days ago :)
https://github.com/kubernetes-sigs/kueue/issues/5294

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-23T10:25:39Z

> This is the exact issue I've created a few days ago :)
> #5294

Does this PR address your issue? Maybe the comment was intended for https://github.com/kubernetes-sigs/kueue/pull/5318? PTAL

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-23T10:26:34Z

/lgtm
/approve
thanks 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-23T10:26:41Z

LGTM label has been added.  <details>Git tree hash: 0ec51c88e5522e5b4cbbae3106c7f1a9aefb6b87</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-05-23T10:26:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5317#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5317#issuecomment-2903989610" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-23T10:40:47Z

/hold

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-23T10:47:48Z

Can we also add `workload.WorkloadEvictionStateInc` to the `startEviction` func?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-23T10:52:32Z

> > This is the exact issue I've created a few days ago :)
> > #5294
> 
> Does this PR address your issue? Maybe the comment was intended for #5318? PTAL

Sorry, this is similar but not exactly that, my concerns are addressed by #5318

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-05-23T10:53:31Z

> Can we also add `workload.WorkloadEvictionStateInc` to the `startEviction` func?

If you think it's out of scope for this PR, I'm good with a small follow-up but I thought we can do it here as well. Feel free to unhold if ready/will do in a follow-up

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-05-23T11:37:01Z

> > Can we also add `workload.WorkloadEvictionStateInc` to the `startEviction` func?
> 
> If you think it's out of scope for this PR, I'm good with a small follow-up but I thought we can do it here as well. Feel free to unhold if ready/will do in a follow-up

I think that this is out of scope for this PR.

/unhold

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-23T09:27:46Z

- **pkg/controller/tas/node_failure_controller.go:183** by [@mimowo](https://github.com/mimowo) — 2025-05-23T09:27:46Z
```diff
@@ -176,6 +176,22 @@ func (r *nodeFailureReconciler) getWorkloadsOnNode(ctx context.Context, nodeName
 	return workloadsToProcess, nil
 }
 
+// evictWorkload idempotently evicts the workload when the node has failed.
+// It returns whether the node was evicted, and whether an error was encountered.
+func (r *nodeFailureReconciler) evictWorkload(ctx context.Context, wl *kueue.Workload, nodeName string, wlKey types.NamespacedName) (bool, error) {
+	if failedNode, ok := getAnnotations(wl)[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(wl) {
+		r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
```

  As we are on that, can we log both the names for debuggability? ("Evicting workload due to multiple node failures, including %s, %s"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-23T09:29:58Z

- **pkg/controller/tas/node_failure_controller.go:242** by [@mimowo](https://github.com/mimowo) — 2025-05-23T09:29:58Z
```diff
@@ -195,34 +212,34 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 			}
 			continue
 		}
-		annotations := getAnnotations(&wl)
-		evictedNow := false
-		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
-			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
-			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
-			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
-				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
-				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
-				continue
-			} else {
-				evictedNow = true
-			}
+
+		// evict workload when annotation present.
+		evicted, err := r.evictWorkload(ctx, &wl, nodeName, wlKey)
+		if err != nil {
+			workloadProcessingErrors = append(workloadProcessingErrors, err)
+			continue
+		}
+
+		// re-fetch workload if we evicted it.
+		if evicted {
 			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
 				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
 				workloadProcessingErrors = append(workloadProcessingErrors, err)
 				continue
 			}
 		}
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			annotations = getAnnotations(&wl)
+
+		// update annotations.
+		err = clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			annotations := getAnnotations(&wl)
 			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
 			if !ok {
 				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
 				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
 				wl.SetAnnotations(annotations)
 				return true, nil
 			}
-			if workload.IsEvicted(&wl) || evictedNow {
+			if workload.IsEvicted(&wl) {
```

  This change seems to go beyond just extracting the function, with user-facing potential. What is the motivation?

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-23T09:30:40Z

- **pkg/controller/tas/node_failure_controller.go:242** by [@gabesaba](https://github.com/gabesaba) — 2025-05-23T09:30:40Z
```diff
@@ -195,34 +212,34 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 			}
 			continue
 		}
-		annotations := getAnnotations(&wl)
-		evictedNow := false
-		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
-			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
-			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
-			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
-				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
-				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
-				continue
-			} else {
-				evictedNow = true
-			}
+
+		// evict workload when annotation present.
+		evicted, err := r.evictWorkload(ctx, &wl, nodeName, wlKey)
+		if err != nil {
+			workloadProcessingErrors = append(workloadProcessingErrors, err)
+			continue
+		}
+
+		// re-fetch workload if we evicted it.
+		if evicted {
 			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
 				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
 				workloadProcessingErrors = append(workloadProcessingErrors, err)
 				continue
 			}
 		}
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			annotations = getAnnotations(&wl)
+
+		// update annotations.
+		err = clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			annotations := getAnnotations(&wl)
 			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
 			if !ok {
 				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
 				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
 				wl.SetAnnotations(annotations)
 				return true, nil
 			}
-			if workload.IsEvicted(&wl) || evictedNow {
+			if workload.IsEvicted(&wl) {
```

  Since we refetch the workload after eviction, we can read whether it is evicted from the ground truth - the workload itself

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-23T09:32:35Z

- **pkg/controller/tas/node_failure_controller.go:242** by [@pajakd](https://github.com/pajakd) — 2025-05-23T09:32:34Z
```diff
@@ -195,34 +212,34 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 			}
 			continue
 		}
-		annotations := getAnnotations(&wl)
-		evictedNow := false
-		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
-			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
-			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
-			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
-				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
-				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
-				continue
-			} else {
-				evictedNow = true
-			}
+
+		// evict workload when annotation present.
+		evicted, err := r.evictWorkload(ctx, &wl, nodeName, wlKey)
+		if err != nil {
+			workloadProcessingErrors = append(workloadProcessingErrors, err)
+			continue
+		}
+
+		// re-fetch workload if we evicted it.
+		if evicted {
 			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
 				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
 				workloadProcessingErrors = append(workloadProcessingErrors, err)
 				continue
 			}
 		}
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			annotations = getAnnotations(&wl)
+
+		// update annotations.
+		err = clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			annotations := getAnnotations(&wl)
 			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
 			if !ok {
 				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
 				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
 				wl.SetAnnotations(annotations)
 				return true, nil
 			}
-			if workload.IsEvicted(&wl) || evictedNow {
+			if workload.IsEvicted(&wl) {
```

  So, the reason this flag is there in the first place is this https://github.com/kubernetes-sigs/kueue/pull/5272#discussion_r2102164883

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-05-23T09:33:01Z

- **pkg/controller/tas/node_failure_controller.go:242** by [@pajakd](https://github.com/pajakd) — 2025-05-23T09:33:01Z
```diff
@@ -195,34 +212,34 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 			}
 			continue
 		}
-		annotations := getAnnotations(&wl)
-		evictedNow := false
-		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
-			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
-			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
-			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
-				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
-				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
-				continue
-			} else {
-				evictedNow = true
-			}
+
+		// evict workload when annotation present.
+		evicted, err := r.evictWorkload(ctx, &wl, nodeName, wlKey)
+		if err != nil {
+			workloadProcessingErrors = append(workloadProcessingErrors, err)
+			continue
+		}
+
+		// re-fetch workload if we evicted it.
+		if evicted {
 			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
 				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
 				workloadProcessingErrors = append(workloadProcessingErrors, err)
 				continue
 			}
 		}
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			annotations = getAnnotations(&wl)
+
+		// update annotations.
+		err = clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			annotations := getAnnotations(&wl)
 			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
 			if !ok {
 				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
 				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
 				wl.SetAnnotations(annotations)
 				return true, nil
 			}
-			if workload.IsEvicted(&wl) || evictedNow {
+			if workload.IsEvicted(&wl) {
```

  And this is why the integration test is failing

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-23T09:34:55Z

- **pkg/controller/tas/node_failure_controller.go:242** by [@gabesaba](https://github.com/gabesaba) — 2025-05-23T09:34:55Z
```diff
@@ -195,34 +212,34 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 			}
 			continue
 		}
-		annotations := getAnnotations(&wl)
-		evictedNow := false
-		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
-			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
-			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
-			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
-				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
-				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
-				continue
-			} else {
-				evictedNow = true
-			}
+
+		// evict workload when annotation present.
+		evicted, err := r.evictWorkload(ctx, &wl, nodeName, wlKey)
+		if err != nil {
+			workloadProcessingErrors = append(workloadProcessingErrors, err)
+			continue
+		}
+
+		// re-fetch workload if we evicted it.
+		if evicted {
 			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
 				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
 				workloadProcessingErrors = append(workloadProcessingErrors, err)
 				continue
 			}
 		}
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			annotations = getAnnotations(&wl)
+
+		// update annotations.
+		err = clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			annotations := getAnnotations(&wl)
 			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
 			if !ok {
 				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
 				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
 				wl.SetAnnotations(annotations)
 				return true, nil
 			}
-			if workload.IsEvicted(&wl) || evictedNow {
+			if workload.IsEvicted(&wl) {
```

  Perhaps my model is wrong - as now this integration test is failing

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-23T09:35:17Z

- **pkg/controller/tas/node_failure_controller.go:183** by [@gabesaba](https://github.com/gabesaba) — 2025-05-23T09:35:16Z
```diff
@@ -176,6 +176,22 @@ func (r *nodeFailureReconciler) getWorkloadsOnNode(ctx context.Context, nodeName
 	return workloadsToProcess, nil
 }
 
+// evictWorkload idempotently evicts the workload when the node has failed.
+// It returns whether the node was evicted, and whether an error was encountered.
+func (r *nodeFailureReconciler) evictWorkload(ctx context.Context, wl *kueue.Workload, nodeName string, wlKey types.NamespacedName) (bool, error) {
+	if failedNode, ok := getAnnotations(wl)[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(wl) {
+		r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
```

  Is a new key "nodeName" in the log fine?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-23T09:39:47Z

- **pkg/controller/tas/node_failure_controller.go:183** by [@mimowo](https://github.com/mimowo) — 2025-05-23T09:39:47Z
```diff
@@ -176,6 +176,22 @@ func (r *nodeFailureReconciler) getWorkloadsOnNode(ctx context.Context, nodeName
 	return workloadsToProcess, nil
 }
 
+// evictWorkload idempotently evicts the workload when the node has failed.
+// It returns whether the node was evicted, and whether an error was encountered.
+func (r *nodeFailureReconciler) evictWorkload(ctx context.Context, wl *kueue.Workload, nodeName string, wlKey types.NamespacedName) (bool, error) {
+	if failedNode, ok := getAnnotations(wl)[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(wl) {
+		r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
```

  Maybe "nodeName", and "failedNode" to include both?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-23T09:42:06Z

- **pkg/controller/tas/node_failure_controller.go:242** by [@mimowo](https://github.com/mimowo) — 2025-05-23T09:42:06Z
```diff
@@ -195,34 +212,34 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 			}
 			continue
 		}
-		annotations := getAnnotations(&wl)
-		evictedNow := false
-		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
-			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
-			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
-			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
-				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
-				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
-				continue
-			} else {
-				evictedNow = true
-			}
+
+		// evict workload when annotation present.
+		evicted, err := r.evictWorkload(ctx, &wl, nodeName, wlKey)
+		if err != nil {
+			workloadProcessingErrors = append(workloadProcessingErrors, err)
+			continue
+		}
+
+		// re-fetch workload if we evicted it.
+		if evicted {
 			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
 				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
 				workloadProcessingErrors = append(workloadProcessingErrors, err)
 				continue
 			}
 		}
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			annotations = getAnnotations(&wl)
+
+		// update annotations.
+		err = clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			annotations := getAnnotations(&wl)
 			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
 			if !ok {
 				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
 				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
 				wl.SetAnnotations(annotations)
 				return true, nil
 			}
-			if workload.IsEvicted(&wl) || evictedNow {
+			if workload.IsEvicted(&wl) {
```

  > we can read whether it is evicted from the ground truth - the workload itself
  
  Due to k8s model of event propagation the workload may already be evicted (the request succeeded), but the new state of the workload was not yet handled by the "Update" event handler, and so Get returns the old version :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-23T09:45:58Z

- **pkg/controller/tas/node_failure_controller.go:242** by [@mimowo](https://github.com/mimowo) — 2025-05-23T09:45:58Z
```diff
@@ -195,34 +212,34 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 			}
 			continue
 		}
-		annotations := getAnnotations(&wl)
-		evictedNow := false
-		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
-			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
-			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
-			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
-				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
-				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
-				continue
-			} else {
-				evictedNow = true
-			}
+
+		// evict workload when annotation present.
+		evicted, err := r.evictWorkload(ctx, &wl, nodeName, wlKey)
+		if err != nil {
+			workloadProcessingErrors = append(workloadProcessingErrors, err)
+			continue
+		}
+
+		// re-fetch workload if we evicted it.
+		if evicted {
 			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
 				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
 				workloadProcessingErrors = append(workloadProcessingErrors, err)
 				continue
 			}
 		}
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			annotations = getAnnotations(&wl)
+
+		// update annotations.
+		err = clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			annotations := getAnnotations(&wl)
 			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
 			if !ok {
 				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
 				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
 				wl.SetAnnotations(annotations)
 				return true, nil
 			}
-			if workload.IsEvicted(&wl) || evictedNow {
+			if workload.IsEvicted(&wl) {
```

  btw, we would not need to have any of the considerations if "nodeToReplace" was a proper field in workload.status. Then, eviction and setting the field would be atomic, I would really love to prioritize making it  a status field for the future releases.
  
  The non-atomicity of the eviction & the annotation also affects the new PR: https://github.com/kubernetes-sigs/kueue/pull/5287 making this extra code necessary: https://github.com/kubernetes-sigs/kueue/pull/5287/files#diff-60dd240c20adbd6a189d018d1c216c2d296730f446c341d8bf449fa6657964ffR197-R209, and adding non-trivial races to consider.
  
  cc @PBundyra

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-05-23T09:48:46Z

- **pkg/controller/tas/node_failure_controller.go:183** by [@gabesaba](https://github.com/gabesaba) — 2025-05-23T09:48:46Z
```diff
@@ -176,6 +176,22 @@ func (r *nodeFailureReconciler) getWorkloadsOnNode(ctx context.Context, nodeName
 	return workloadsToProcess, nil
 }
 
+// evictWorkload idempotently evicts the workload when the node has failed.
+// It returns whether the node was evicted, and whether an error was encountered.
+func (r *nodeFailureReconciler) evictWorkload(ctx context.Context, wl *kueue.Workload, nodeName string, wlKey types.NamespacedName) (bool, error) {
+	if failedNode, ok := getAnnotations(wl)[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(wl) {
+		r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
```

  Will these ever be different?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-23T10:05:09Z

- **pkg/controller/tas/node_failure_controller.go:183** by [@mimowo](https://github.com/mimowo) — 2025-05-23T10:05:09Z
```diff
@@ -176,6 +176,22 @@ func (r *nodeFailureReconciler) getWorkloadsOnNode(ctx context.Context, nodeName
 	return workloadsToProcess, nil
 }
 
+// evictWorkload idempotently evicts the workload when the node has failed.
+// It returns whether the node was evicted, and whether an error was encountered.
+func (r *nodeFailureReconciler) evictWorkload(ctx context.Context, wl *kueue.Workload, nodeName string, wlKey types.NamespacedName) (bool, error) {
+	if failedNode, ok := getAnnotations(wl)[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(wl) {
+		r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
```

  They will always be different since the log is guarded by the check `failedNode != nodeName `. We actually already build the message in `evictionMsg`. Maybe just log it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-05-23T10:12:02Z

- **pkg/controller/tas/node_failure_controller.go:242** by [@mimowo](https://github.com/mimowo) — 2025-05-23T10:12:01Z
```diff
@@ -185,57 +201,58 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 	}
 	var workloadProcessingErrors []error
 	for wlKey := range workloadsToProcess {
+		log := r.log.WithValues("workload", wlKey, "nodeName", nodeName)
+		// fetch workload.
 		var wl kueue.Workload
 		if err := r.client.Get(ctx, wlKey, &wl); err != nil {
 			if apierrors.IsNotFound(err) {
-				r.log.V(4).Info("Workload not found, skipping", "workload", wlKey)
+				log.V(4).Info("Workload not found, skipping")
 			} else {
-				r.log.V(2).Error(err, "Failed to get workload", "workload", wlKey)
+				log.V(2).Error(err, "Failed to get workload")
 				workloadProcessingErrors = append(workloadProcessingErrors, err)
 			}
 			continue
 		}
-		annotations := getAnnotations(&wl)
-		evictedNow := false
-		if failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]; ok && failedNode != nodeName && !workload.IsEvicted(&wl) {
-			r.log.V(3).Info("Evicting workload due to multiple node failures", "workload", wlKey)
-			evictionMsg := fmt.Sprintf(NodeMultipleFailuresEvictionMessageFormat, failedNode, nodeName)
-			if evictionErr := r.startEviction(ctx, &wl, evictionMsg); evictionErr != nil {
-				r.log.V(2).Error(evictionErr, "Failed to complete eviction process", "workload", wlKey)
-				workloadProcessingErrors = append(workloadProcessingErrors, evictionErr)
-				continue
-			} else {
-				evictedNow = true
-			}
+
+		// evict workload when annotation present.
+		evicted, err := r.evictWorkload(ctx, log, &wl, wlKey, nodeName)
+		if err != nil {
+			workloadProcessingErrors = append(workloadProcessingErrors, err)
+			continue
+		}
+		// re-fetch workload if we evicted it.
+		if evicted {
 			if err := r.client.Get(ctx, wlKey, &wl); err != nil {
-				r.log.V(2).Error(err, "Failed to re-fetch workload after eviction", "workload", wlKey)
+				log.V(2).Error(err, "Failed to re-fetch workload after eviction")
 				workloadProcessingErrors = append(workloadProcessingErrors, err)
 				continue
 			}
 		}
-		err := clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
-			annotations = getAnnotations(&wl)
+
+		// update annotations.
+		err = clientutil.Patch(ctx, r.client, &wl, true, func() (bool, error) {
+			annotations := getAnnotations(&wl)
 			failedNode, ok := annotations[kueuealpha.NodeToReplaceAnnotation]
 			if !ok {
-				r.log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
+				log.V(4).Info(fmt.Sprintf("Adding node to %s annotation", kueuealpha.NodeToReplaceAnnotation))
 				annotations[kueuealpha.NodeToReplaceAnnotation] = nodeName
 				wl.SetAnnotations(annotations)
 				return true, nil
 			}
-			if workload.IsEvicted(&wl) || evictedNow {
-				r.log.V(4).Info(fmt.Sprintf("Removing node from %s annotation", kueuealpha.NodeToReplaceAnnotation), "workload", wlKey, "nodeName", failedNode)
+			if workload.IsEvicted(&wl) || evicted {
```

  feel free to keep as is if you disagree, but I liked the name evictedNow more.
  
  The reason is that it is possible that "evicted=false", but the workload is actually evicted, by another controller. Thus we have the check ` workload.IsEvicted(&wl) `.
