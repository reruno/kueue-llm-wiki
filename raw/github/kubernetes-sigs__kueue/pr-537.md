# PR #537: [KEP]Add job interface for integration

**Summary**: [KEP]Add job interface for integration

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/537

**Last updated**: 2023-02-17T07:39:08Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `design/add-interface-to-kueue` → `main`
- **Created**: 2023-01-30T10:27:32Z
- **Updated**: 2023-02-17T07:39:08Z
- **Merged**: 2023-02-17T07:21:55Z
- **Closed**: 2023-02-17T07:21:55Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 2   **+437 / -0**

## Description

Signed-off-by: Kante Yin <kerthcet@gmail.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of https://github.com/kubernetes-sigs/kueue/issues/369

#### Special notes for your reviewer:

## Discussion

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-01-31T08:00:01Z

/cc

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-02-02T10:44:27Z

New commit: 024aee2b31f89a244f2375262cbc48b8df4183ef

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-02-02T10:58:56Z

POC  ebf2b812705d0b82ce00fe3dbb39fc008d5a0d77

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-02-15T09:23:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 93c254963088cc22decccadc150e224eed9c7313 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/63ef2869c2970800074b1772 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-537--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-537--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTUzNy0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.2UQlGuP5TR4-tAs1C1nn1Wyx4o-95SLoYqQy9XrjAvM)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site settings](https://app.netlify.com/sites/kubernetes-sigs-kueue/settings/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-02-16T19:34:03Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/537#pullrequestreview-1302152059" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/537#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-02-17T03:13:36Z

> I'm generally happy with this. This is certainly not set in stone and likely to change in the future. But that's life :)

Yes, we will need the feedbacks from MPIJob.

### Comment by [@mimowo](https://github.com/mimowo) — 2023-02-17T07:01:33Z

/lgtm
The interface is a good starting point. It is likely to evolve as we go along.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-02-17T07:08:49Z

/hold cancel
Let's go!

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-02-17T07:11:02Z

squashed, would you mind to add a LGTM again @mimowo ? Thanks.

### Comment by [@mimowo](https://github.com/mimowo) — 2023-02-17T07:13:16Z

/lgtm
Let's go :)

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-30T17:04:12Z

- **keps/369-job-interface/README.md:61** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T16:51:41Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
```

  And a library with helpers? I feel like a lot of code can be shared using `runtime.Object`, generics and/or the interface you are proposing.

- **keps/369-job-interface/README.md:171** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T16:57:37Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
```

  Options to disambiguate:
  
  - GenericJob
  - QueueableJob (although hard to type and pronounce)

- **keps/369-job-interface/README.md:206** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T16:58:15Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  How is this used?

- **keps/369-job-interface/README.md:175** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T16:58:49Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
```

  why do we need the Workload?

- **keps/369-job-interface/README.md:179** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T16:59:12Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
```

  Do we need to pass the ResourceFlavor too? Or directly the labels?

- **keps/369-job-interface/README.md:182** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T16:59:44Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
```

  ```suggestion
    // Finished means whether the job is completed/failed or not.
  ```

- **keps/369-job-interface/README.md:185** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T17:00:24Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
```

  Why does the Job need to implement this? This should be part of the helper code.

- **keps/369-job-interface/README.md:199** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T17:01:06Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
```

  Can you remind me where we use this?

- **keps/369-job-interface/README.md:189** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T17:02:08Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
```

  Maybe this should be something like `PodSets` that will return `[]kueue.PodSet`. Then from there we can provide code to create the Workload object.

- **keps/369-job-interface/README.md:191** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-30T17:03:21Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
```

  `PriorityClass`? And then we can have code to get the priority from the client. Although maybe some custom jobs might choose to give you a number already.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-01-30T22:04:41Z

- **keps/369-job-interface/README.md:166** by [@mwielgus](https://github.com/mwielgus) — 2023-01-30T22:04:41Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
```

  What is missing (or at least I'm missing) is the overall mechanic. Who is calling what and when? What is the flow?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-01-30T22:07:24Z

- **keps/369-job-interface/README.md:139** by [@mwielgus](https://github.com/mwielgus) — 2023-01-30T22:07:23Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
```

  To be honest I'm lost here. Is it a template, a hint, a set of best practices or a library?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-01-31T10:36:55Z

- **keps/369-job-interface/README.md:171** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T06:19:32Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
```

  Update with GenericJob for unambiguous.

- **keps/369-job-interface/README.md:175** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T06:30:05Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
```

  Start is more than unsuspending job, like injecting the nodeAffinity, which will need the workload. I'll polish the comment.

- **keps/369-job-interface/README.md:179** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T06:53:05Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
```

  For kubernetes job, we only need one podSet, but for other custom Jobs, this may not true. Then we may need to pass-in an array.  Change to []labels to make the parameters as minimally and precisely as possible.

- **keps/369-job-interface/README.md:185** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T07:14:12Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
```

  Removed.

- **keps/369-job-interface/README.md:199** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T07:28:52Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
```

  This is based on the current implementation, when we verify whether this is only one matched workload, we'll try to  compare the workload with the Job. https://github.com/kubernetes-sigs/kueue/blob/160021fcd342f0b010e4d0d5e64ea634dba54df2/pkg/controller/workload/job/job_controller.go#L524-L540
  
  This can be removed from the interface at the moment, I just list it out for discussion.

- **keps/369-job-interface/README.md:189** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T07:38:38Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
```

  Or ConstructWorkload(), besides podSets, we still want to queueName, namespace, name and so on.

- **keps/369-job-interface/README.md:191** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T08:13:53Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
```

  Let's use priorityClass for now.

- **keps/369-job-interface/README.md:206** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T08:16:59Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  This is the current `waitForPodsReady`, I think not every job needs this and it can be a separate interface with advanced features.

- **keps/369-job-interface/README.md:206** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T08:21:00Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  GangSchedulingSucceeded() is equal to `podsReady` today.

- **keps/369-job-interface/README.md:61** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T08:37:30Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
```

  Should this be part of the KEP？Sounds like implementation details.

- **keps/369-job-interface/README.md:139** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T09:11:35Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
```

  🙃 Didn't find a proper word here. Updated:
  ```
  Job interface defined here is a hint for developers to build their own controllers, 
  it's not a hard constraint(to implement the interface) but a good reference.
  ```

- **keps/369-job-interface/README.md:166** by [@kerthcet](https://github.com/kerthcet) — 2023-01-31T09:44:24Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
```

  Yes, I thought about writing a flow with pseudo-code, but thinking that we'll cover them in job controller so... Thanks for reminding, I'll add one as a reference.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-01-31T12:04:58Z

- **keps/369-job-interface/README.md:206** by [@tenzen-y](https://github.com/tenzen-y) — 2023-01-31T12:04:58Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  `GangSchedulingJob` doesn't sound appropriate to me. Since we don't use the expression `GangSchedulling` in the document and kueue doesn't guarantee gang-schedule all Pods to Nodes.
  
  https://github.com/kubernetes-sigs/kueue/blob/160021fcd342f0b010e4d0d5e64ea634dba54df2/docs/tasks/setup_sequential_admission.md
  
  However, I'm not sure what name is appropriate.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-01-31T12:09:57Z

- **keps/369-job-interface/README.md:167** by [@tenzen-y](https://github.com/tenzen-y) — 2023-01-31T12:09:57Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
```

  Can we also provide interfaces for webhook like https://github.com/kubernetes-sigs/kueue/blob/160021fcd342f0b010e4d0d5e64ea634dba54df2/pkg/controller/workload/job/job_webhook.go?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-01-31T18:44:20Z

- **keps/369-job-interface/README.md:61** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-31T18:29:32Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
```

  The library itself will be part of the PR review, but it should be part of the goals of the KEP to have a library or a controller implementation that can accept the proposed interface in the controller constructor.

- **keps/369-job-interface/README.md:206** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-31T18:34:02Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  waitForPodsReady is currently a Kueue level configuration, so it doesn't make sense to have it as part of the GenericJobInterface for now.

- **keps/369-job-interface/README.md:175** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-31T18:35:09Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
```

  I was thinking that maybe it was about nodeAffinity, but you have separate methods for that.

- **keps/369-job-interface/README.md:199** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-31T18:37:55Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
```

  I guess it makes sense to keep it for now. I think currently if you change the parallelism of a running job, we would delete the Workload object to cause it to be requeued.

- **keps/369-job-interface/README.md:175** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-31T18:39:24Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start will arm the job ready to start, like unsuspending the job and injecting nodeAffinity.
+  Start(ctx context.Context, wl *kueue.Workload) error
```

  why pass a context? If there is no kubeclient, context is probably not necessary either?

- **keps/369-job-interface/README.md:167** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-31T18:40:16Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
```

  Maybe we can focus in the controller for now to make progress.

- **keps/369-job-interface/README.md:193** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-31T18:41:24Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start will arm the job ready to start, like unsuspending the job and injecting nodeAffinity.
+  Start(ctx context.Context, wl *kueue.Workload) error
+  // Stop will disarm the job ready to stop, like suspending the job and restoring nodeAffinity.
+  Stop(ctx context.Context, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not.
+  Finished() bool
+  // ConstructWorkload will build a workload corresponding to the job.
+  ConstructWorkload() (*kueue.Workload, error)
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
```

  Should this return an array? Why do we need this in addition to ConstructWorkload?

- **keps/369-job-interface/README.md:189** by [@alculquicondor](https://github.com/alculquicondor) — 2023-01-31T18:44:10Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
```

  We can for sure obtain the namespace from the ObjectMeta. I wonder if there are any APIs that already include a queue name in a field. Otherwise we can also obtain the queue name from the (standard) annotation in the ObjectMeta.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-01-31T19:19:09Z

- **keps/369-job-interface/README.md:206** by [@tenzen-y](https://github.com/tenzen-y) — 2023-01-31T19:19:08Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  > waitForPodsReady is currently a Kueue level configuration, so it doesn't make sense to have it as part of the GenericJobInterface for now.
  
  I agree. I wanted to ask whether `Gang` is a general term or not.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-01-31T19:20:56Z

- **keps/369-job-interface/README.md:167** by [@tenzen-y](https://github.com/tenzen-y) — 2023-01-31T19:20:56Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
```

  It makes sense.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-01T09:08:23Z

- **keps/369-job-interface/README.md:206** by [@kerthcet](https://github.com/kerthcet) — 2023-02-01T09:08:23Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  My original thought is this may also needed by other jobs, by implementing like https://github.com/kubernetes-sigs/kueue/blob/3268949a656a18b779c6d5369de8a1a92034cef4/pkg/controller/workload/job/job_controller.go#L255-L258
  
  But we can extend the interface if needed, removed then.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-01T09:35:43Z

- **keps/369-job-interface/README.md:199** by [@kerthcet](https://github.com/kerthcet) — 2023-02-01T09:35:43Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
```

  Yes, this is needed when job can be modified in runtime.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-01T09:46:17Z

- **keps/369-job-interface/README.md:189** by [@kerthcet](https://github.com/kerthcet) — 2023-02-01T09:46:16Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
```

  Fine, then we'll have a help function named like `createWorkload()` with client passed-in.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-01T09:51:18Z

- **keps/369-job-interface/README.md:175** by [@kerthcet](https://github.com/kerthcet) — 2023-02-01T09:51:18Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start will arm the job ready to start, like unsuspending the job and injecting nodeAffinity.
+  Start(ctx context.Context, wl *kueue.Workload) error
```

  Yes, forgot to remove, same to other methods.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-01T09:52:20Z

- **keps/369-job-interface/README.md:193** by [@kerthcet](https://github.com/kerthcet) — 2023-02-01T09:52:20Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start will arm the job ready to start, like unsuspending the job and injecting nodeAffinity.
+  Start(ctx context.Context, wl *kueue.Workload) error
+  // Stop will disarm the job ready to stop, like suspending the job and restoring nodeAffinity.
+  Stop(ctx context.Context, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not.
+  Finished() bool
+  // ConstructWorkload will build a workload corresponding to the job.
+  ConstructWorkload() (*kueue.Workload, error)
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
```

  Both ConstructWorkload and PodsCount are removed now.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-01T10:08:38Z

- **keps/369-job-interface/README.md:175** by [@kerthcet](https://github.com/kerthcet) — 2023-02-01T10:08:37Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
```

  Start is not good designed I think, changed to:
  ```
  IsSuspend() bool        --> former is Suspend() bool
  Suspend() error         --> former is Start() error
  Unsuspend() error       --> former is Stop() error
  ```
  
  We'll have a `Start()` helper function to call `Unsuspend()` and `InjectNodeAffinity` separately.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-01T16:29:58Z

- **keps/369-job-interface/README.md:61** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-01T16:23:14Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
```

  Can you add this?

- **keps/369-job-interface/README.md:81** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-01T16:23:54Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
```

  ```suggestion
  But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
  ```

- **keps/369-job-interface/README.md:193** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-01T16:29:01Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start will arm the job ready to start, like unsuspending the job and injecting nodeAffinity.
+  Start(ctx context.Context, wl *kueue.Workload) error
+  // Stop will disarm the job ready to stop, like suspending the job and restoring nodeAffinity.
+  Stop(ctx context.Context, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not.
+  Finished() bool
+  // ConstructWorkload will build a workload corresponding to the job.
+  ConstructWorkload() (*kueue.Workload, error)
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
```

  did you forget to commit/push?

- **keps/369-job-interface/README.md:206** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-01T16:29:49Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  ah, I see what you mean now. Let's better be more explicit and make it `PodsReady() bool`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-02-01T17:46:39Z

- **keps/369-job-interface/README.md:206** by [@tenzen-y](https://github.com/tenzen-y) — 2023-02-01T17:46:39Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  I see too. @alculquicondor's idea sounds good to me.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-02T08:08:19Z

- **keps/369-job-interface/README.md:61** by [@kerthcet](https://github.com/kerthcet) — 2023-02-02T08:08:18Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
```

  Added

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-02T08:09:07Z

- **keps/369-job-interface/README.md:206** by [@kerthcet](https://github.com/kerthcet) — 2023-02-02T08:09:06Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  Add a new interface SequenceJob to containing `PodsReady()`

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-02T08:09:49Z

- **keps/369-job-interface/README.md:193** by [@kerthcet](https://github.com/kerthcet) — 2023-02-02T08:09:49Z
```diff
@@ -0,0 +1,381 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start will arm the job ready to start, like unsuspending the job and injecting nodeAffinity.
+  Start(ctx context.Context, wl *kueue.Workload) error
+  // Stop will disarm the job ready to stop, like suspending the job and restoring nodeAffinity.
+  Stop(ctx context.Context, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not.
+  Finished() bool
+  // ConstructWorkload will build a workload corresponding to the job.
+  ConstructWorkload() (*kueue.Workload, error)
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
```

  Yes, pushed.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-02T10:55:33Z

- **keps/369-job-interface/README.md:193** by [@kerthcet](https://github.com/kerthcet) — 2023-02-02T10:53:14Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
```

  Doubt about the return values of `condition`, but without it, we have to iterate the `j.Status.Conditions` again to get the condition. Or return the conditionType instead, similar to
  https://github.com/kubernetes-sigs/kueue/blob/b6bdec3bfd7427bf69af74398ddcbae4acf35d76/pkg/controller/workload/job/job_controller.go#L511-L518

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-02T11:01:04Z

- **keps/369-job-interface/README.md:193** by [@kerthcet](https://github.com/kerthcet) — 2023-02-02T11:01:04Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
```

  This is more clear, implementation based on the design. https://github.com/kubernetes-sigs/kueue/blob/ebf2b812705d0b82ce00fe3dbb39fc008d5a0d77/pkg/controller/workload/job/job_controller.go#L310-L320

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-02T13:31:29Z

- **keps/369-job-interface/README.md:206** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T13:31:29Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  Does it need to be a separate interface?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-02T16:00:12Z

- **keps/369-job-interface/README.md:206** by [@kerthcet](https://github.com/kerthcet) — 2023-02-02T16:00:11Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  Not every job needs this, I treat it as an advanced feature, so maybe it's good to keep the interface as small as possible.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-02T16:10:23Z

- **keps/369-job-interface/README.md:206** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T16:10:23Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named Job, this should be implemented by custom job-like applications:
+
+```golang
+type Job interface {
+  // Suspend instructs whether the job is suspended or not.
+  Suspend() bool
+  // Start means starting the job, like unsuspending the job.
+  Start(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Stop means stopping the job, like suspending the job.
+  Stop(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // Finished instructs whether the job is completed/failed or not.
+  Finished() bool
+  // Finish will update the workload to finished.
+  Finish(ctx context.Context, client client.Client, wl *kueue.Workload) error
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // CreateWorkload will create a workload constructing by the job.
+  CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme) (*kueue.Workload, error)
+  // Priority returns the job's priority
+  Priority(ctx context.Context, client client.Client) (string, int32, error)
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+  // PodsCount returns the pods number job requires.
+  PodsCount() int32
+}
+```
+
+Also defined another interface for all-or-nothing scheduling required job-like applications:
+
+```golang
+type GangSchedulingJob interface {
+  // GangSchedulingSucceeded instructs whether job is scheduled successfully in gang-scheduling.
+  GangSchedulingSucceeded() bool
```

  What would we do with a job that doesn't implement the interface if the kueue is configured with `waitForReadyPods: true`?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-02T18:19:22Z

This is looking great!

- **keps/369-job-interface/README.md:98** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:00:32Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
```

  ```suggestion
  - Provide a library to build integration controllers for job APIs that implement the interface
  ```

- **keps/369-job-interface/README.md:107** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:00:46Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
```

  ```suggestion
  - Integrate any job-like applications
  ```

- **keps/369-job-interface/README.md:131** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:01:10Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
```

  ```suggestion
  We collected feedback from the community about how to fully integrate Kueue with MPIJob,
  ```

- **keps/369-job-interface/README.md:144** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:01:47Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
```

  It's a hard constrain if they wish to use the library, but they can always write the controllers from scratch

- **keps/369-job-interface/README.md:166** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:02:21Z
```diff
@@ -0,0 +1,329 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to help other job-like applications to smoothly integrate with Kueue.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lies in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface here is more like a template and tell developers what to do, but even without
+implementing the interface people can also build their controllers.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
```

  ```suggestion
  ### Job Interface
  ```

- **keps/369-job-interface/README.md:179** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:02:47Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
```

  ```suggestion
    IsSuspended() bool
  ```

- **keps/369-job-interface/README.md:186** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:03:37Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
```

  What is this for?

- **keps/369-job-interface/README.md:188** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:04:24Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
```

  ```suggestion
    InjectNodeSelectors(nodeSelectors []map[string]string) error
  ```

- **keps/369-job-interface/README.md:190** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:04:44Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
```

  ```suggestion
    RestoreNodeSelectors(nodeSelectors []map[string]string) error
  ```

- **keps/369-job-interface/README.md:193** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:07:09Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
```

  Do we need the job condition or do we just need to know whether it finished?
  
  Also, some APIs might not use metav1.Condition (kubeflow jobs don't).

- **keps/369-job-interface/README.md:194** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:07:37Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
```

  ```suggestion
    // PodSets the podSets corresponding to the job.
  ```

- **keps/369-job-interface/README.md:203** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:08:59Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
```

  Maybe:
  
  ```suggestion
    IgnoreFromQueueing() bool
  ```

- **keps/369-job-interface/README.md:212** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:10:04Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
```

  I feel like it should just be part of the main interface, and if they don't have any semantics for it, they should return true.

- **keps/369-job-interface/README.md:216** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:10:19Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
```

  ```suggestion
  ### Kubernetes Job implementation details
  ```

- **keps/369-job-interface/README.md:217** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:11:26Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
```

  ```suggestion
  type BatchJob  batchv1.Job
  ```

- **keps/369-job-interface/README.md:261** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:15:41Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
```

  I think this should be part of the library. Something along these lines:
  
  ```golang
  type Controller[J GenericJob] struct {}
  
  func NewController[J GenericJob]() Controller[J] {
    return Controller[J]{}
  }
  ```

- **keps/369-job-interface/README.md:231** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:16:43Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
```

  I think we can leave this to the code review. The pseudocode below should be enough

- **keps/369-job-interface/README.md:413** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:17:58Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
+    // Ignore unmanaged jobs, like lacking queueName.
+    if job.Ignored():
+        return
+
+    // Ensure there's only one corresponding workload and
+    // return the matched workload, it could be nil.
+    workload = EnsureOneWorkload()
+
+    // Handing job is finished.
+    if job.Finished():
+        // Processing marking workload finished if not.
+        SetWorkloadCondition()
+        return
+
+    // Handing workload is nil.
+    if workload == nil:
+        // If workload is nil, the job should be unsuspend.
+        if !job.IsSuspend():
+            // When stopping the job, we'll call Suspend(), RestoreNodeAffinity() etc.,
+            // and update the job with client.
+            StopJob()
+
+        // When creating workload, we'll call PodSets(), QueueName(), PodsCount() etc.
+        // to fill up the workload.
+        workload = CreateWorkload()
+        // creating the constructed workload with client
+        // ...
+
+    // Handing job is suspend.
+    if job.IsSuspend():
+        // If job is suspend but workload is admitted,
+        // we should start the job.
+        if workload.Spec.Admission != nil:
+            // When starting the job, we'll call Unsuspend(), InjectNodeAffinity() etc..
+            StartJob()
+            return
+
+        // If job is suspend but we changed its queueName,
+        // we should update the workload's queueName.
+        // ...
+
+    // Handing job is unsuspend.
+
+    // If job is unsuspend but workload is unadmitted,
+    // we should suspend the job.
+    if workload.Spec.Admission == nil:
+        StopJob()
+        return
+
+    // Processing other logics like all-or-nothing scheduling
+    // ...
+```
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+No.
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+- `pkg/controller/workload/job`: `2023.01.30` - `5.5%` (This is output via the go tool)
+
+#### Integration tests
+
+<!--
+Describe what tests will be added to ensure proper quality of the enhancement.
+
+After the implementation PR is merged, add the names of the tests here.
+-->
+
+This is more like a refactor of the current implementation, theoretically no need to add more
+integration tests.
+
+### Graduation Criteria
+
+<!--
+
+Clearly define what it means for the feature to be implemented and
+considered stable.
+
+If the feature you are introducing has high complexity, consider adding graduation
+milestones with these graduation criteria:
+- [Maturity levels (`alpha`, `beta`, `stable`)][maturity-levels]
+- [Feature gate][feature gate] lifecycle
+- [Deprecation policy][deprecation-policy]
+
+[feature gate]: https://git.k8s.io/community/contributors/devel/sig-architecture/feature-gates.md
+[maturity-levels]: https://git.k8s.io/community/contributors/devel/sig-architecture/api_changes.md#alpha-beta-and-stable-versions
+[deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
+-->
+
+## Implementation History
+
+<!--
+Major milestones in the lifecycle of a KEP should be tracked in this section.
+Major milestones might include:
+- the `Summary` and `Motivation` sections being merged, signaling SIG acceptance
+- the `Proposal` section being merged, signaling agreement on a proposed design
+- the date implementation started
+- the first Kubernetes release where an initial version of the KEP was available
+- the version of Kubernetes where the KEP graduated to general availability
+- when the KEP was retired or superseded
+-->
+
+- 2023.01.09: KEP proposed for review, including motivation, proposal, risks,
+test plan.
+
+## Drawbacks
+
+<!--
+Why should this KEP _not_ be implemented?
+-->
+
+- Developers who want to implement their own controllers will have to read more source code for
```

  They will have to read less code than today, so it's not a drawback :)

- **keps/369-job-interface/README.md:415** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:18:51Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
+    // Ignore unmanaged jobs, like lacking queueName.
+    if job.Ignored():
+        return
+
+    // Ensure there's only one corresponding workload and
+    // return the matched workload, it could be nil.
+    workload = EnsureOneWorkload()
+
+    // Handing job is finished.
+    if job.Finished():
+        // Processing marking workload finished if not.
+        SetWorkloadCondition()
+        return
+
+    // Handing workload is nil.
+    if workload == nil:
+        // If workload is nil, the job should be unsuspend.
+        if !job.IsSuspend():
+            // When stopping the job, we'll call Suspend(), RestoreNodeAffinity() etc.,
+            // and update the job with client.
+            StopJob()
+
+        // When creating workload, we'll call PodSets(), QueueName(), PodsCount() etc.
+        // to fill up the workload.
+        workload = CreateWorkload()
+        // creating the constructed workload with client
+        // ...
+
+    // Handing job is suspend.
+    if job.IsSuspend():
+        // If job is suspend but workload is admitted,
+        // we should start the job.
+        if workload.Spec.Admission != nil:
+            // When starting the job, we'll call Unsuspend(), InjectNodeAffinity() etc..
+            StartJob()
+            return
+
+        // If job is suspend but we changed its queueName,
+        // we should update the workload's queueName.
+        // ...
+
+    // Handing job is unsuspend.
+
+    // If job is unsuspend but workload is unadmitted,
+    // we should suspend the job.
+    if workload.Spec.Admission == nil:
+        StopJob()
+        return
+
+    // Processing other logics like all-or-nothing scheduling
+    // ...
+```
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+No.
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+- `pkg/controller/workload/job`: `2023.01.30` - `5.5%` (This is output via the go tool)
+
+#### Integration tests
+
+<!--
+Describe what tests will be added to ensure proper quality of the enhancement.
+
+After the implementation PR is merged, add the names of the tests here.
+-->
+
+This is more like a refactor of the current implementation, theoretically no need to add more
+integration tests.
+
+### Graduation Criteria
+
+<!--
+
+Clearly define what it means for the feature to be implemented and
+considered stable.
+
+If the feature you are introducing has high complexity, consider adding graduation
+milestones with these graduation criteria:
+- [Maturity levels (`alpha`, `beta`, `stable`)][maturity-levels]
+- [Feature gate][feature gate] lifecycle
+- [Deprecation policy][deprecation-policy]
+
+[feature gate]: https://git.k8s.io/community/contributors/devel/sig-architecture/feature-gates.md
+[maturity-levels]: https://git.k8s.io/community/contributors/devel/sig-architecture/api_changes.md#alpha-beta-and-stable-versions
+[deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
+-->
+
+## Implementation History
+
+<!--
+Major milestones in the lifecycle of a KEP should be tracked in this section.
+Major milestones might include:
+- the `Summary` and `Motivation` sections being merged, signaling SIG acceptance
+- the `Proposal` section being merged, signaling agreement on a proposed design
+- the date implementation started
+- the first Kubernetes release where an initial version of the KEP was available
+- the version of Kubernetes where the KEP graduated to general availability
+- when the KEP was retired or superseded
+-->
+
+- 2023.01.09: KEP proposed for review, including motivation, proposal, risks,
+test plan.
+
+## Drawbacks
+
+<!--
+Why should this KEP _not_ be implemented?
+-->
+
+- Developers who want to implement their own controllers will have to read more source code for
+better understandings.
+- Bad for the project long-term maintenance.
```

  I wouldn't say bad, but it does add complexity: we should try to maintain the number of changes to the interface to a minimum

- **keps/369-job-interface/README.md:410** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-02T18:19:12Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
+    // Ignore unmanaged jobs, like lacking queueName.
+    if job.Ignored():
+        return
+
+    // Ensure there's only one corresponding workload and
+    // return the matched workload, it could be nil.
+    workload = EnsureOneWorkload()
+
+    // Handing job is finished.
+    if job.Finished():
+        // Processing marking workload finished if not.
+        SetWorkloadCondition()
+        return
+
+    // Handing workload is nil.
+    if workload == nil:
+        // If workload is nil, the job should be unsuspend.
+        if !job.IsSuspend():
+            // When stopping the job, we'll call Suspend(), RestoreNodeAffinity() etc.,
+            // and update the job with client.
+            StopJob()
+
+        // When creating workload, we'll call PodSets(), QueueName(), PodsCount() etc.
+        // to fill up the workload.
+        workload = CreateWorkload()
+        // creating the constructed workload with client
+        // ...
+
+    // Handing job is suspend.
+    if job.IsSuspend():
+        // If job is suspend but workload is admitted,
+        // we should start the job.
+        if workload.Spec.Admission != nil:
+            // When starting the job, we'll call Unsuspend(), InjectNodeAffinity() etc..
+            StartJob()
+            return
+
+        // If job is suspend but we changed its queueName,
+        // we should update the workload's queueName.
+        // ...
+
+    // Handing job is unsuspend.
+
+    // If job is unsuspend but workload is unadmitted,
+    // we should suspend the job.
+    if workload.Spec.Admission == nil:
+        StopJob()
+        return
+
+    // Processing other logics like all-or-nothing scheduling
+    // ...
+```
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+No.
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+- `pkg/controller/workload/job`: `2023.01.30` - `5.5%` (This is output via the go tool)
+
+#### Integration tests
+
+<!--
+Describe what tests will be added to ensure proper quality of the enhancement.
+
+After the implementation PR is merged, add the names of the tests here.
+-->
+
+This is more like a refactor of the current implementation, theoretically no need to add more
+integration tests.
+
+### Graduation Criteria
+
+<!--
+
+Clearly define what it means for the feature to be implemented and
+considered stable.
+
+If the feature you are introducing has high complexity, consider adding graduation
+milestones with these graduation criteria:
+- [Maturity levels (`alpha`, `beta`, `stable`)][maturity-levels]
+- [Feature gate][feature gate] lifecycle
+- [Deprecation policy][deprecation-policy]
+
+[feature gate]: https://git.k8s.io/community/contributors/devel/sig-architecture/feature-gates.md
+[maturity-levels]: https://git.k8s.io/community/contributors/devel/sig-architecture/api_changes.md#alpha-beta-and-stable-versions
+[deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
+-->
+
+## Implementation History
+
+<!--
+Major milestones in the lifecycle of a KEP should be tracked in this section.
+Major milestones might include:
+- the `Summary` and `Motivation` sections being merged, signaling SIG acceptance
+- the `Proposal` section being merged, signaling agreement on a proposed design
+- the date implementation started
+- the first Kubernetes release where an initial version of the KEP was available
+- the version of Kubernetes where the KEP graduated to general availability
+- when the KEP was retired or superseded
+-->
+
+- 2023.01.09: KEP proposed for review, including motivation, proposal, risks,
+test plan.
+
+## Drawbacks
+
+<!--
+Why should this KEP _not_ be implemented?
+-->
+
+- Developers who want to implement their own controllers will have to read more source code for
+better understandings.
+- Bad for the project long-term maintenance.
+
+## Alternatives
+
+<!--
+What other approaches did you consider, and why did you rule them out? These do
+not need to be as detailed as the proposal, but should include enough
+information to express the idea and why it was not acceptable.
+-->
```

  Each job implements their controller from scratch

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-02-03T21:41:43Z

- **keps/369-job-interface/README.md:189** by [@mwielgus](https://github.com/mwielgus) — 2023-02-03T21:41:43Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
```

  How will the library/controller know which job(and of what kind) to unsuspend because a particular workload got admitted? There is no key/identrifier returned by this Interface.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-02-03T21:44:36Z

- **keps/369-job-interface/README.md:199** by [@mwielgus](https://github.com/mwielgus) — 2023-02-03T21:44:36Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
```

  We probably need 2 methods instead of one:
  
  * To tell what workload name matches to that particular GenericJob.
  * To tell that workload and generic job got out of sync.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-02-03T22:13:19Z

- **keps/369-job-interface/README.md:190** by [@mwielgus](https://github.com/mwielgus) — 2023-02-03T22:13:19Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
```

  Why is this needed?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-02-03T22:15:25Z

- **keps/369-job-interface/README.md:187** by [@mwielgus](https://github.com/mwielgus) — 2023-02-03T22:15:25Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
```

  Under what conditions the job will be suspended again? What are assumption regarding the underlying behaviour?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-02-03T22:17:56Z

- **keps/369-job-interface/README.md:415** by [@mwielgus](https://github.com/mwielgus) — 2023-02-03T22:17:55Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
+    // Ignore unmanaged jobs, like lacking queueName.
+    if job.Ignored():
+        return
+
+    // Ensure there's only one corresponding workload and
+    // return the matched workload, it could be nil.
+    workload = EnsureOneWorkload()
+
+    // Handing job is finished.
+    if job.Finished():
+        // Processing marking workload finished if not.
+        SetWorkloadCondition()
+        return
+
+    // Handing workload is nil.
+    if workload == nil:
+        // If workload is nil, the job should be unsuspend.
+        if !job.IsSuspend():
+            // When stopping the job, we'll call Suspend(), RestoreNodeAffinity() etc.,
+            // and update the job with client.
+            StopJob()
+
+        // When creating workload, we'll call PodSets(), QueueName(), PodsCount() etc.
+        // to fill up the workload.
+        workload = CreateWorkload()
+        // creating the constructed workload with client
+        // ...
+
+    // Handing job is suspend.
+    if job.IsSuspend():
+        // If job is suspend but workload is admitted,
+        // we should start the job.
+        if workload.Spec.Admission != nil:
+            // When starting the job, we'll call Unsuspend(), InjectNodeAffinity() etc..
+            StartJob()
+            return
+
+        // If job is suspend but we changed its queueName,
+        // we should update the workload's queueName.
+        // ...
+
+    // Handing job is unsuspend.
+
+    // If job is unsuspend but workload is unadmitted,
+    // we should suspend the job.
+    if workload.Spec.Admission == nil:
+        StopJob()
+        return
+
+    // Processing other logics like all-or-nothing scheduling
+    // ...
+```
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+No.
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+- `pkg/controller/workload/job`: `2023.01.30` - `5.5%` (This is output via the go tool)
+
+#### Integration tests
+
+<!--
+Describe what tests will be added to ensure proper quality of the enhancement.
+
+After the implementation PR is merged, add the names of the tests here.
+-->
+
+This is more like a refactor of the current implementation, theoretically no need to add more
+integration tests.
+
+### Graduation Criteria
+
+<!--
+
+Clearly define what it means for the feature to be implemented and
+considered stable.
+
+If the feature you are introducing has high complexity, consider adding graduation
+milestones with these graduation criteria:
+- [Maturity levels (`alpha`, `beta`, `stable`)][maturity-levels]
+- [Feature gate][feature gate] lifecycle
+- [Deprecation policy][deprecation-policy]
+
+[feature gate]: https://git.k8s.io/community/contributors/devel/sig-architecture/feature-gates.md
+[maturity-levels]: https://git.k8s.io/community/contributors/devel/sig-architecture/api_changes.md#alpha-beta-and-stable-versions
+[deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
+-->
+
+## Implementation History
+
+<!--
+Major milestones in the lifecycle of a KEP should be tracked in this section.
+Major milestones might include:
+- the `Summary` and `Motivation` sections being merged, signaling SIG acceptance
+- the `Proposal` section being merged, signaling agreement on a proposed design
+- the date implementation started
+- the first Kubernetes release where an initial version of the KEP was available
+- the version of Kubernetes where the KEP graduated to general availability
+- when the KEP was retired or superseded
+-->
+
+- 2023.01.09: KEP proposed for review, including motivation, proposal, risks,
+test plan.
+
+## Drawbacks
+
+<!--
+Why should this KEP _not_ be implemented?
+-->
+
+- Developers who want to implement their own controllers will have to read more source code for
+better understandings.
+- Bad for the project long-term maintenance.
```

  I don't agree. I would say that such interface (together with library/controller boilerplate) is critical for Kueue. Who and how otherwise will integrate with it?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-02-03T22:23:13Z

- **keps/369-job-interface/README.md:231** by [@mwielgus](https://github.com/mwielgus) — 2023-02-03T22:23:13Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
```

  What about providing the full controller instead of just helper functions? So that the user can just implement the interface and be done with it.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2023-02-08T23:26:33Z

- **keps/369-job-interface/README.md:187** by [@ahg-g](https://github.com/ahg-g) — 2023-02-08T23:26:33Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
```

  1) Preemption; 2) for some reason the job was unsuspended but was never admitted.
  
  The expected behavior is for the job controller to delete all active pods.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-15T09:23:35Z

Thanks all, updated as advised, PTAL.

- **keps/369-job-interface/README.md:413** by [@kerthcet](https://github.com/kerthcet) — 2023-02-14T09:17:15Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
+    // Ignore unmanaged jobs, like lacking queueName.
+    if job.Ignored():
+        return
+
+    // Ensure there's only one corresponding workload and
+    // return the matched workload, it could be nil.
+    workload = EnsureOneWorkload()
+
+    // Handing job is finished.
+    if job.Finished():
+        // Processing marking workload finished if not.
+        SetWorkloadCondition()
+        return
+
+    // Handing workload is nil.
+    if workload == nil:
+        // If workload is nil, the job should be unsuspend.
+        if !job.IsSuspend():
+            // When stopping the job, we'll call Suspend(), RestoreNodeAffinity() etc.,
+            // and update the job with client.
+            StopJob()
+
+        // When creating workload, we'll call PodSets(), QueueName(), PodsCount() etc.
+        // to fill up the workload.
+        workload = CreateWorkload()
+        // creating the constructed workload with client
+        // ...
+
+    // Handing job is suspend.
+    if job.IsSuspend():
+        // If job is suspend but workload is admitted,
+        // we should start the job.
+        if workload.Spec.Admission != nil:
+            // When starting the job, we'll call Unsuspend(), InjectNodeAffinity() etc..
+            StartJob()
+            return
+
+        // If job is suspend but we changed its queueName,
+        // we should update the workload's queueName.
+        // ...
+
+    // Handing job is unsuspend.
+
+    // If job is unsuspend but workload is unadmitted,
+    // we should suspend the job.
+    if workload.Spec.Admission == nil:
+        StopJob()
+        return
+
+    // Processing other logics like all-or-nothing scheduling
+    // ...
+```
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+No.
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+- `pkg/controller/workload/job`: `2023.01.30` - `5.5%` (This is output via the go tool)
+
+#### Integration tests
+
+<!--
+Describe what tests will be added to ensure proper quality of the enhancement.
+
+After the implementation PR is merged, add the names of the tests here.
+-->
+
+This is more like a refactor of the current implementation, theoretically no need to add more
+integration tests.
+
+### Graduation Criteria
+
+<!--
+
+Clearly define what it means for the feature to be implemented and
+considered stable.
+
+If the feature you are introducing has high complexity, consider adding graduation
+milestones with these graduation criteria:
+- [Maturity levels (`alpha`, `beta`, `stable`)][maturity-levels]
+- [Feature gate][feature gate] lifecycle
+- [Deprecation policy][deprecation-policy]
+
+[feature gate]: https://git.k8s.io/community/contributors/devel/sig-architecture/feature-gates.md
+[maturity-levels]: https://git.k8s.io/community/contributors/devel/sig-architecture/api_changes.md#alpha-beta-and-stable-versions
+[deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
+-->
+
+## Implementation History
+
+<!--
+Major milestones in the lifecycle of a KEP should be tracked in this section.
+Major milestones might include:
+- the `Summary` and `Motivation` sections being merged, signaling SIG acceptance
+- the `Proposal` section being merged, signaling agreement on a proposed design
+- the date implementation started
+- the first Kubernetes release where an initial version of the KEP was available
+- the version of Kubernetes where the KEP graduated to general availability
+- when the KEP was retired or superseded
+-->
+
+- 2023.01.09: KEP proposed for review, including motivation, proposal, risks,
+test plan.
+
+## Drawbacks
+
+<!--
+Why should this KEP _not_ be implemented?
+-->
+
+- Developers who want to implement their own controllers will have to read more source code for
```

  mixed with not adding the interface{}, updated.

- **keps/369-job-interface/README.md:415** by [@kerthcet](https://github.com/kerthcet) — 2023-02-14T09:29:50Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
+    // Ignore unmanaged jobs, like lacking queueName.
+    if job.Ignored():
+        return
+
+    // Ensure there's only one corresponding workload and
+    // return the matched workload, it could be nil.
+    workload = EnsureOneWorkload()
+
+    // Handing job is finished.
+    if job.Finished():
+        // Processing marking workload finished if not.
+        SetWorkloadCondition()
+        return
+
+    // Handing workload is nil.
+    if workload == nil:
+        // If workload is nil, the job should be unsuspend.
+        if !job.IsSuspend():
+            // When stopping the job, we'll call Suspend(), RestoreNodeAffinity() etc.,
+            // and update the job with client.
+            StopJob()
+
+        // When creating workload, we'll call PodSets(), QueueName(), PodsCount() etc.
+        // to fill up the workload.
+        workload = CreateWorkload()
+        // creating the constructed workload with client
+        // ...
+
+    // Handing job is suspend.
+    if job.IsSuspend():
+        // If job is suspend but workload is admitted,
+        // we should start the job.
+        if workload.Spec.Admission != nil:
+            // When starting the job, we'll call Unsuspend(), InjectNodeAffinity() etc..
+            StartJob()
+            return
+
+        // If job is suspend but we changed its queueName,
+        // we should update the workload's queueName.
+        // ...
+
+    // Handing job is unsuspend.
+
+    // If job is unsuspend but workload is unadmitted,
+    // we should suspend the job.
+    if workload.Spec.Admission == nil:
+        StopJob()
+        return
+
+    // Processing other logics like all-or-nothing scheduling
+    // ...
+```
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+No.
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+- `pkg/controller/workload/job`: `2023.01.30` - `5.5%` (This is output via the go tool)
+
+#### Integration tests
+
+<!--
+Describe what tests will be added to ensure proper quality of the enhancement.
+
+After the implementation PR is merged, add the names of the tests here.
+-->
+
+This is more like a refactor of the current implementation, theoretically no need to add more
+integration tests.
+
+### Graduation Criteria
+
+<!--
+
+Clearly define what it means for the feature to be implemented and
+considered stable.
+
+If the feature you are introducing has high complexity, consider adding graduation
+milestones with these graduation criteria:
+- [Maturity levels (`alpha`, `beta`, `stable`)][maturity-levels]
+- [Feature gate][feature gate] lifecycle
+- [Deprecation policy][deprecation-policy]
+
+[feature gate]: https://git.k8s.io/community/contributors/devel/sig-architecture/feature-gates.md
+[maturity-levels]: https://git.k8s.io/community/contributors/devel/sig-architecture/api_changes.md#alpha-beta-and-stable-versions
+[deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
+-->
+
+## Implementation History
+
+<!--
+Major milestones in the lifecycle of a KEP should be tracked in this section.
+Major milestones might include:
+- the `Summary` and `Motivation` sections being merged, signaling SIG acceptance
+- the `Proposal` section being merged, signaling agreement on a proposed design
+- the date implementation started
+- the first Kubernetes release where an initial version of the KEP was available
+- the version of Kubernetes where the KEP graduated to general availability
+- when the KEP was retired or superseded
+-->
+
+- 2023.01.09: KEP proposed for review, including motivation, proposal, risks,
+test plan.
+
+## Drawbacks
+
+<!--
+Why should this KEP _not_ be implemented?
+-->
+
+- Developers who want to implement their own controllers will have to read more source code for
+better understandings.
+- Bad for the project long-term maintenance.
```

  mixed with not adding the interface{}, updated.

- **keps/369-job-interface/README.md:415** by [@kerthcet](https://github.com/kerthcet) — 2023-02-14T09:31:22Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
+    // Ignore unmanaged jobs, like lacking queueName.
+    if job.Ignored():
+        return
+
+    // Ensure there's only one corresponding workload and
+    // return the matched workload, it could be nil.
+    workload = EnsureOneWorkload()
+
+    // Handing job is finished.
+    if job.Finished():
+        // Processing marking workload finished if not.
+        SetWorkloadCondition()
+        return
+
+    // Handing workload is nil.
+    if workload == nil:
+        // If workload is nil, the job should be unsuspend.
+        if !job.IsSuspend():
+            // When stopping the job, we'll call Suspend(), RestoreNodeAffinity() etc.,
+            // and update the job with client.
+            StopJob()
+
+        // When creating workload, we'll call PodSets(), QueueName(), PodsCount() etc.
+        // to fill up the workload.
+        workload = CreateWorkload()
+        // creating the constructed workload with client
+        // ...
+
+    // Handing job is suspend.
+    if job.IsSuspend():
+        // If job is suspend but workload is admitted,
+        // we should start the job.
+        if workload.Spec.Admission != nil:
+            // When starting the job, we'll call Unsuspend(), InjectNodeAffinity() etc..
+            StartJob()
+            return
+
+        // If job is suspend but we changed its queueName,
+        // we should update the workload's queueName.
+        // ...
+
+    // Handing job is unsuspend.
+
+    // If job is unsuspend but workload is unadmitted,
+    // we should suspend the job.
+    if workload.Spec.Admission == nil:
+        StopJob()
+        return
+
+    // Processing other logics like all-or-nothing scheduling
+    // ...
+```
+
+### Test Plan
+
+<!--
+**Note:** *Not required until targeted at a release.*
+The goal is to ensure that we don't accept enhancements with inadequate testing.
+
+All code is expected to have adequate tests (eventually with coverage
+expectations). Please adhere to the [Kubernetes testing guidelines][testing-guidelines]
+when drafting this test plan.
+
+[testing-guidelines]: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
+-->
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+##### Prerequisite testing updates
+
+<!--
+Based on reviewers feedback describe what additional tests need to be added prior
+implementing this enhancement to ensure the enhancements have also solid foundations.
+-->
+
+No.
+
+#### Unit Tests
+
+<!--
+In principle every added code should have complete unit test coverage, so providing
+the exact set of tests will not bring additional value.
+However, if complete unit test coverage is not possible, explain the reason of it
+together with explanation why this is acceptable.
+-->
+
+<!--
+Additionally, try to enumerate the core package you will be touching
+to implement this enhancement and provide the current unit coverage for those
+in the form of:
+- <package>: <date> - <current test coverage>
+
+This can inform certain test coverage improvements that we want to do before
+extending the production code to implement this enhancement.
+-->
+
+- `pkg/controller/workload/job`: `2023.01.30` - `5.5%` (This is output via the go tool)
+
+#### Integration tests
+
+<!--
+Describe what tests will be added to ensure proper quality of the enhancement.
+
+After the implementation PR is merged, add the names of the tests here.
+-->
+
+This is more like a refactor of the current implementation, theoretically no need to add more
+integration tests.
+
+### Graduation Criteria
+
+<!--
+
+Clearly define what it means for the feature to be implemented and
+considered stable.
+
+If the feature you are introducing has high complexity, consider adding graduation
+milestones with these graduation criteria:
+- [Maturity levels (`alpha`, `beta`, `stable`)][maturity-levels]
+- [Feature gate][feature gate] lifecycle
+- [Deprecation policy][deprecation-policy]
+
+[feature gate]: https://git.k8s.io/community/contributors/devel/sig-architecture/feature-gates.md
+[maturity-levels]: https://git.k8s.io/community/contributors/devel/sig-architecture/api_changes.md#alpha-beta-and-stable-versions
+[deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
+-->
+
+## Implementation History
+
+<!--
+Major milestones in the lifecycle of a KEP should be tracked in this section.
+Major milestones might include:
+- the `Summary` and `Motivation` sections being merged, signaling SIG acceptance
+- the `Proposal` section being merged, signaling agreement on a proposed design
+- the date implementation started
+- the first Kubernetes release where an initial version of the KEP was available
+- the version of Kubernetes where the KEP graduated to general availability
+- when the KEP was retired or superseded
+-->
+
+- 2023.01.09: KEP proposed for review, including motivation, proposal, risks,
+test plan.
+
+## Drawbacks
+
+<!--
+Why should this KEP _not_ be implemented?
+-->
+
+- Developers who want to implement their own controllers will have to read more source code for
+better understandings.
+- Bad for the project long-term maintenance.
```

  Mixed with not having the interface.

- **keps/369-job-interface/README.md:193** by [@kerthcet](https://github.com/kerthcet) — 2023-02-14T09:35:21Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
```

  We need to know whether the job is finished or not, then we know what to do next. Removed the condition, leave it to pr review then.

- **keps/369-job-interface/README.md:189** by [@kerthcet](https://github.com/kerthcet) — 2023-02-14T09:41:43Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
```

  This is called by job itself.

- **keps/369-job-interface/README.md:190** by [@kerthcet](https://github.com/kerthcet) — 2023-02-14T09:42:51Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
```

  When job suspend again from unsuspend, we'll call this, for that moment, we can update the job node selectors.

- **keps/369-job-interface/README.md:231** by [@kerthcet](https://github.com/kerthcet) — 2023-02-14T09:45:10Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
```

  This is a good suggestion, and in theory, this should work, but there're so many trivial things around and job differs with job, let me try.

- **keps/369-job-interface/README.md:199** by [@kerthcet](https://github.com/kerthcet) — 2023-02-14T10:44:06Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
```

  maybe you mean `job.WorkloadName() string`, no strong option to add it or not, how about leave this to PR review then? Hope to make the interface{} small for the first version.

- **keps/369-job-interface/README.md:217** by [@kerthcet](https://github.com/kerthcet) — 2023-02-15T07:35:09Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
```

  Then BatchJob should be a Kubernetes Object? Seems weird and have no idea how to make it a kubernetes object in easy ways.

- **keps/369-job-interface/README.md:261** by [@kerthcet](https://github.com/kerthcet) — 2023-02-15T08:51:41Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
```

  I'd like to provide a common controller, looks like:
  ```
  func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
  	var batchJob BatchJob
  	return GenericReconcile(ctx, req, &batchJob, reconcileOptions)
  }
  func GenericReconcile(ctx context.Context, req ctrl.Request, job GenericJob, reconcileOptions) (ctrl.Result, error) {
      // generic logics here
  }
  ```
  
  The `GenericReconcile` will handle all the logic. But that means we may have to add more methods to the interface, I have no idea at the moment, will fill this up until we implemented with batch job. 
  
  Then for developers, what they should do is only implement the Job Interface. WDYT?

- **keps/369-job-interface/README.md:261** by [@kerthcet](https://github.com/kerthcet) — 2023-02-15T09:11:57Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
```

  Some hooks maybe needed, but we can add them later.

- **keps/369-job-interface/README.md:261** by [@kerthcet](https://github.com/kerthcet) — 2023-02-15T09:14:58Z
```diff
@@ -0,0 +1,423 @@
+# KEP-369: Add job interface to help integrating Kueue with other job-like applications
+
+<!--
+This is the title of your KEP. Keep it short, simple, and descriptive. A good
+title can help communicate what the KEP is and should be considered as part of
+any review.
+-->
+
+<!--
+A table of contents is helpful for quickly jumping to sections of a KEP and for
+highlighting any additional information provided beyond the standard KEP
+template.
+
+Ensure the TOC is wrapped with
+  <code>&lt;!-- toc --&rt;&lt;!-- /toc --&rt;</code>
+tags, and then generate with `hack/update-toc.sh`.
+-->
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+<!--
+This section is incredibly important for producing high-quality, user-focused
+documentation such as release notes or a development roadmap. It should be
+possible to collect this information before implementation begins, in order to
+avoid requiring implementors to split their attention between writing release
+notes and implementing the feature itself. KEP editors and SIG Docs
+should help to ensure that the tone and content of the `Summary` section is
+useful for a wide audience.
+
+A good summary is probably at least a paragraph in length.
+
+Both in this section and below, follow the guidelines of the [documentation
+style guide]. In particular, wrap lines to a reasonable length, to make it
+easier for reviewers to cite specific portions, and to minimize diff churn on
+updates.
+
+[documentation style guide]: https://github.com/kubernetes/community/blob/master/contributors/guide/style-guide.md
+-->
+
+Define a set of methods(known as golang interface) to shape the default behaviors of job.
+In addition, offer a library and controller implementation example(based on Kubernetes Job) for developers
+to follow when building their own controllers.
+This will help the community to integrate with Kueue more easily.
+
+## Motivation
+
+<!--
+This section is for explicitly listing the motivation, goals, and non-goals of
+this KEP.  Describe why the change is important and the benefits to users. The
+motivation section can optionally provide links to [experience reports] to
+demonstrate the interest in a KEP within the wider Kubernetes community.
+
+[experience reports]: https://github.com/golang/go/wiki/ExperienceReports
+-->
+
+From day 0 in Kueue, we natively support Kubernetes Job by leveraging the capacity of [_suspend_](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job),
+this helps us to build a multi-tenant job queueing system in Kubernetes, this is attractive to other job-like applications
+like MPIJob, who lacks the capacity of queueing.
+
+The good news is Kueue is extensible and simple to integrate with through the intermediate medium, we named _Workload_ in Kueue,
+what we need to do is to build a controller to reconcile the workload and the job itself.
+
+But the complexity lays in developers who are familiar with job-like applications may have little knowledge of the
+implementation details of Kueue and they have no idea where to start to build the controller. In this case, if we can provide an
+interface which defines the default behaviors of the Job, and serve the Kubernetes Job as a standard template, it will do them
+a great favor.
+
+### Goals
+
+<!--
+List the specific goals of the KEP. What is it trying to achieve? How will we
+know that this has succeeded?
+-->
+
+- Define an interface which shapes the default behaviors of Job
+- Make Kubernetes Job an implementation template of the interface
+- Provide a library to organize helper functions which is used in constructing controllers
+
+### Non-Goals
+
+<!--
+What is out of scope for this KEP? Listing non-goals helps to focus discussion
+and make progress.
+-->
+
+- Integrate with other job-like applications
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+<!--
+Detail the things that people will be able to do if this KEP is implemented.
+Include as much detail as possible so that people can understand the "how" of
+the system. The goal here is to make this feel real for users without getting
+bogged down.
+-->
+
+#### Story 1
+
+We collected feedbacks from the community about how to fully integrate Kueue with MPIJob,
+see [#499](https://github.com/kubernetes-sigs/kueue/issues/499).
+
+### Notes/Constraints/Caveats (Optional)
+
+<!--
+What are the caveats to the proposal?
+What are some important details that didn't come across above?
+Go in to as much detail as necessary here.
+This might be a good place to talk about core concepts and how they relate.
+-->
+
+- Job interface defined here is a hint for developers to build their own controllers,
+it's not a hard constraint(to implement the interface) but a good reference.
+- This will increase the code complexity by wrapping the original Jobs.
+
+### Risks and Mitigations
+
+<!--
+What are the risks of this proposal, and how do we mitigate? Think broadly.
+For example, consider both security and how this will impact the larger
+Kubernetes ecosystem.
+
+How will security be reviewed, and by whom?
+
+How will UX be reviewed, and by whom?
+
+Consider including folks who also work outside the SIG or subproject.
+-->
+
+## Design Details
+
+<!--
+This section should contain enough information that the specifics of your
+change are understandable. This may include API specs (though not always
+required) or even code snippets. If there's any ambiguity about HOW your
+proposal will be implemented, this is the place to discuss them.
+-->
+
+Part I: Job Interface
+
+We will define a new interface named GenericJob, this should be implemented by custom job-like applications:
+
+```golang
+type GenericJob interface {
+  // Object returns the job instance.
+  Object() client.Object
+  // IsSuspend returns whether the job is suspend or not.
+  IsSuspend() bool
+  // Suspend will suspend the job.
+  Suspend() error
+  // UnSuspend will unsuspend the job.
+  UnSuspend() error
+  // ResetStatus will reset the job status to the original state.
+  // If true, status is modified, if not, status is as it was.
+  ResetStatus() bool
+  // InjectNodeAffinity will inject the node affinity extracting from workload to job.
+  InjectNodeAffinity(nodeSelectors []map[string]string) error
+  // RestoreNodeAffinity will restore the original node affinity of job.
+  RestoreNodeAffinity(nodeSelectors []map[string]string) error
+  // Finished means whether the job is completed/failed or not,
+  // condition represents the workload finished condition.
+  Finished() (condition metav1.Condition, finished bool)
+  // PodSets will build workload podSets corresponding to the job.
+  PodSets() []kueue.PodSet
+  // EquivalentToWorkload validates whether the workload is semantically equal to the job.
+  EquivalentToWorkload(wl kueue.Workload) bool
+  // PriorityClass returns the job's priority class name.
+  PriorityClass() string
+  // QueueName returns the queue name the job enqueued.
+  QueueName() string
+  // Ignored instructs whether this job should be ignored in reconciling, e.g. lacking the queueName.
+  Ignored() bool
+}
+```
+
+Also defined another interface `SequenceJob` for scheduling in sequence requirements, see design details here [All-or-nothing semantics for job resource assignment](https://github.com/kubernetes-sigs/kueue/tree/main/keps/349-all-or-nothing#kep-349-all-or-nothing-semantics-for-job-resource-assignment).
+
+```golang
+type SequenceJob interface {
+    // PodsReady instructs whether job derived pods are all ready now.
+    PodsReady() bool
+}
+```
+
+Part II: Kubernetes Job implementation details
+
+We'll wrap the batchv1.Job to `BatchJob` who implements the GenericJob interface, as well as SequenceJob interface.
+
+```golang
+type BatchJob struct {
+  batchv1.Job
+}
+
+var _ GenericJob = &BatchJob{}
+var _ SequenceJob = &BatchJob{}
+```
+
+Part III: Library
+
+We'll have several helper functions in constructing controllers, they can be imported directly in the future:
+
+```golang
+// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// If there're more than one workload, we should delete the excess ones.
+// The returned workload could be nil.
+EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request, record record.EventRecorder, job GenericJob) (*kueue.Workload, error)
+
+// StartJob will unsuspend the job, and also inject the node affinity.
+StartJob(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+
+// StopJob will suspend the job, and also restore node affinity, reset job status if needed.
+ StopJob(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // CreateWorkload will derive and create a workload from the corresponding job.
+ CreateWorkload(ctx context.Context, client client.Client, scheme *runtime.Scheme, job GenericJob) (*kueue.Workload, error)
+
+ // GetNodeSelectors will extract node selectors from admitted workloads.
+ GetNodeSelectors(ctx context.Context, client client.Client, w *kueue.Workload) ([]map[string]string, error)
+
+ // UpdateQueueNameIfChanged will update workload queue name if changed.
+ UpdateQueueNameIfChanged(ctx context.Context, client client.Client, job GenericJob, wl *kueue.Workload) error
+
+ // SetWorkloadCondition will update the workload condition by the provide one.
+ SetWorkloadCondition(ctx context.Context, client client.Client, wl *kueue.Workload, condition metav1.Condition) error
+```
+
+Part IV: Working Flow in pseudo-code
+
+```golang
+Reconcile:
```

  The disadvantage is the interface may be changed more frequently. We can make the interface as small as possible to mitigate this.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-16T19:33:52Z

I'm generally happy with this. This is certainly not set in stone and likely to change in the future. But that's life :)

/approve
/hold
I'll leave the LGTM to @mimowo.
