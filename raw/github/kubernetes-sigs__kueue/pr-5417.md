# PR #5417: Update documentation on TAS+ProvReq

**Summary**: Update documentation on TAS+ProvReq

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5417

**Last updated**: 2025-07-10T12:05:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `update-provreq-tas` → `main`
- **Created**: 2025-05-30T06:04:41Z
- **Updated**: 2025-07-10T12:05:31Z
- **Merged**: 2025-06-04T16:04:39Z
- **Closed**: 2025-06-04T16:04:39Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 2   **+57 / -6**

## Description


#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5267

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-30T06:04:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9f461861b76b8ba5577925449f19cf041ab674a1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/684060dafb560d0008684d63 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-5417--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-5417--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTU0MTctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.Z7dRUGSzh3sa1yPNNK-uHhRwUIIaFfAqth8iXYD6EGE)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-5417--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-30T06:05:05Z

cc @PBundyra @tenzen-y @mwysokin PTAL

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-04T14:10:24Z

LGTM overall, I'm leaving marking it as good to @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-04T16:03:07Z

LGTM label has been added.  <details>Git tree hash: 42a11b3601952d796acc42fb36f8acf522e84e0c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-04T16:03:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5417#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5417#pullrequestreview-2897320909" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T16:03:42Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-04T16:03:45Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5417#issuecomment-2940589599):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-04T16:05:18Z

@tenzen-y: new pull request created: #5503

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5417#issuecomment-2940589599):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-02T08:33:50Z

- **site/content/en/docs/admission-check-controllers/provisioning.md:70** by [@PBundyra](https://github.com/PBundyra) — 2025-06-02T08:33:50Z
```diff
@@ -56,21 +56,27 @@ Where:
 - **retryStrategy.backoffLimitCount** - indicates how many times ProvisioningRequest should be retried in case of failure. Defaults to 3.
 - **retryStrategy.backoffBaseSeconds** - provides the base for calculating backoff time that ProvisioningRequest waits before being retried. Defaults to 60.
 - **retryStrategy.backoffMaxSeconds** - indicates the maximum backoff time (in seconds) before retrying a ProvisioningRequest. Defaults to 1800.
-- **podSetMergePolicy** - allows to merge similar PodSets into a single PodTemplate used by the ProvisioningRequest. 
+- **podSetMergePolicy** - allows to merge similar PodSets into a single PodTemplate used by the ProvisioningRequest.
+- **podSetUpdates** - allows to update the Workload's PodSets with nodeSelectors based on the successful ProvisioningRequest.
+  This allows to restrict scheduling of the PodSets' pods to the newly provisioned nodes.
+
+#### PodSet merge policy
 
 {{% alert title="Note" color="primary" %}}
 `podSetMergePolicy` feature is available in Kueue v0.12.0 version or newer.
 
-It offers two options: 
-- `IdenticalPodTemplates` - merges only identical PodTemplates 
+It offers two options:
+- `IdenticalPodTemplates` - merges only identical PodTemplates
 - `IdenticalWorkloadSchedulingRequirements`  - merges PodTemplates which have identical fields which are considered for defining the workload scheduling requirements.
```

  Which fields are exactly considered for defining requirements? Can we have an example of that

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T11:08:22Z

- **site/content/en/docs/admission-check-controllers/provisioning.md:70** by [@mimowo](https://github.com/mimowo) — 2025-06-02T11:08:22Z
```diff
@@ -56,21 +56,27 @@ Where:
 - **retryStrategy.backoffLimitCount** - indicates how many times ProvisioningRequest should be retried in case of failure. Defaults to 3.
 - **retryStrategy.backoffBaseSeconds** - provides the base for calculating backoff time that ProvisioningRequest waits before being retried. Defaults to 60.
 - **retryStrategy.backoffMaxSeconds** - indicates the maximum backoff time (in seconds) before retrying a ProvisioningRequest. Defaults to 1800.
-- **podSetMergePolicy** - allows to merge similar PodSets into a single PodTemplate used by the ProvisioningRequest. 
+- **podSetMergePolicy** - allows to merge similar PodSets into a single PodTemplate used by the ProvisioningRequest.
+- **podSetUpdates** - allows to update the Workload's PodSets with nodeSelectors based on the successful ProvisioningRequest.
+  This allows to restrict scheduling of the PodSets' pods to the newly provisioned nodes.
+
+#### PodSet merge policy
 
 {{% alert title="Note" color="primary" %}}
 `podSetMergePolicy` feature is available in Kueue v0.12.0 version or newer.
 
-It offers two options: 
-- `IdenticalPodTemplates` - merges only identical PodTemplates 
+It offers two options:
+- `IdenticalPodTemplates` - merges only identical PodTemplates
 - `IdenticalWorkloadSchedulingRequirements`  - merges PodTemplates which have identical fields which are considered for defining the workload scheduling requirements.
```

  The list of fields used is here https://github.com/kubernetes-sigs/kueue/blob/3396d6f7fa50be4599ed0a4a6e40bf9c4dec0b88/pkg/controller/admissionchecks/provisioning/controller.go#L892-L900, 
  
  We could document the complete list, but I vote this is a separate PR. In this PR the section has only got re-formatted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T11:09:17Z

- **site/content/en/docs/admission-check-controllers/provisioning.md:70** by [@mimowo](https://github.com/mimowo) — 2025-06-02T11:09:17Z
```diff
@@ -56,21 +56,27 @@ Where:
 - **retryStrategy.backoffLimitCount** - indicates how many times ProvisioningRequest should be retried in case of failure. Defaults to 3.
 - **retryStrategy.backoffBaseSeconds** - provides the base for calculating backoff time that ProvisioningRequest waits before being retried. Defaults to 60.
 - **retryStrategy.backoffMaxSeconds** - indicates the maximum backoff time (in seconds) before retrying a ProvisioningRequest. Defaults to 1800.
-- **podSetMergePolicy** - allows to merge similar PodSets into a single PodTemplate used by the ProvisioningRequest. 
+- **podSetMergePolicy** - allows to merge similar PodSets into a single PodTemplate used by the ProvisioningRequest.
+- **podSetUpdates** - allows to update the Workload's PodSets with nodeSelectors based on the successful ProvisioningRequest.
+  This allows to restrict scheduling of the PodSets' pods to the newly provisioned nodes.
+
+#### PodSet merge policy
 
 {{% alert title="Note" color="primary" %}}
 `podSetMergePolicy` feature is available in Kueue v0.12.0 version or newer.
 
-It offers two options: 
-- `IdenticalPodTemplates` - merges only identical PodTemplates 
+It offers two options:
+- `IdenticalPodTemplates` - merges only identical PodTemplates
 - `IdenticalWorkloadSchedulingRequirements`  - merges PodTemplates which have identical fields which are considered for defining the workload scheduling requirements.
```

  Let me revert the formatting changes, and I will open a follow up cleanup PR, becasue this is not directly related to this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T11:16:18Z

- **site/content/en/docs/admission-check-controllers/provisioning.md:70** by [@mimowo](https://github.com/mimowo) — 2025-06-02T11:16:17Z
```diff
@@ -56,21 +56,27 @@ Where:
 - **retryStrategy.backoffLimitCount** - indicates how many times ProvisioningRequest should be retried in case of failure. Defaults to 3.
 - **retryStrategy.backoffBaseSeconds** - provides the base for calculating backoff time that ProvisioningRequest waits before being retried. Defaults to 60.
 - **retryStrategy.backoffMaxSeconds** - indicates the maximum backoff time (in seconds) before retrying a ProvisioningRequest. Defaults to 1800.
-- **podSetMergePolicy** - allows to merge similar PodSets into a single PodTemplate used by the ProvisioningRequest. 
+- **podSetMergePolicy** - allows to merge similar PodSets into a single PodTemplate used by the ProvisioningRequest.
+- **podSetUpdates** - allows to update the Workload's PodSets with nodeSelectors based on the successful ProvisioningRequest.
+  This allows to restrict scheduling of the PodSets' pods to the newly provisioned nodes.
+
+#### PodSet merge policy
 
 {{% alert title="Note" color="primary" %}}
 `podSetMergePolicy` feature is available in Kueue v0.12.0 version or newer.
 
-It offers two options: 
-- `IdenticalPodTemplates` - merges only identical PodTemplates 
+It offers two options:
+- `IdenticalPodTemplates` - merges only identical PodTemplates
 - `IdenticalWorkloadSchedulingRequirements`  - merges PodTemplates which have identical fields which are considered for defining the workload scheduling requirements.
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-04T08:46:56Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:106** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T08:27:27Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
```

  ```suggestion
  When a workload is assigned to the TAS ResourceFlavor with Provisioning
  ```
  
  The `ResourceFlavor` is object name, I think.

- **site/content/en/docs/concepts/topology_aware_scheduling.md:108** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T08:29:42Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
```

  ```suggestion
  1. **Quota reservation**: quota is reserved, and the Workload obtains the
  ```
  
  In this description, it looks like `workload` indicates `Workload` object instead of generic term.

- **site/content/en/docs/concepts/topology_aware_scheduling.md:110** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T08:30:39Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
```

  ```suggestion
  2. **Admission checks**: Kueue waits for all AdmissionChecks, including the
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:111** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T08:36:35Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
```

  ```suggestion
    Provisioning one. Once all AdmissionChecks are ready, the Workload gets `Ready` admissionCheck state.
  ```
  
  Does this `Ready` indicate for `AdmissionCheck` or `Workload`?

- **site/content/en/docs/concepts/topology_aware_scheduling.md:115** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T08:40:28Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
+3. **Topology assignment**: TAS, in the second pass of the Kueue scheduler,
+  calculates available capacity, including any newly provisioned nodes, and
+  updates the assignment with the assignment of topology domains. Once done the
```

  ```suggestion
  3. **Topology assignment**: Once TAS updates the topology assignments, the
  ```
  
  Shouldn't we avoid TAS description here?

- **site/content/en/docs/concepts/topology_aware_scheduling.md:128** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T08:41:14Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
+3. **Topology assignment**: TAS, in the second pass of the Kueue scheduler,
+  calculates available capacity, including any newly provisioned nodes, and
+  updates the assignment with the assignment of topology domains. Once done the
+  `Admitted` condition is set on the Workload.
+
+Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/admission-check-controllers/provisioning.md)
+to see how you can configure Kueue if you want to restrict scheduling to the
+newly provisioned nodes (assuming the provisioning class supports it).
+
 ### Limitations
 
 Currently, there are limitations for the compatibility of TAS with other
-features. In particular, a ClusterQueue referencing a TAS Resource
-Flavor (with the `.spec.topologyName` field) is marked as inactive if also
-using [MultiKueue](multikueue.md) or
-[ProvisioningRequest](/docs/admission-check-controllers/provisioning/) admission
-checks.
+features, including:
+- some scheduling directives (e.g. pod affinities and anti-affinities) are ignored,
+- the "podset-required-topology" annotation may fail if the underlying
+  ClusterAutoscaler cannot provision nodes that satisfy the domain constraint,
+- a ClusterQueue referencing a TAS Resource Flavor (with the `.spec.topologyName`
```

  ```suggestion
  - a ClusterQueue references ResourceFlavors with Topology name (`.spec.topologyName`)
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:129** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T08:46:54Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
+3. **Topology assignment**: TAS, in the second pass of the Kueue scheduler,
+  calculates available capacity, including any newly provisioned nodes, and
+  updates the assignment with the assignment of topology domains. Once done the
+  `Admitted` condition is set on the Workload.
+
+Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/admission-check-controllers/provisioning.md)
+to see how you can configure Kueue if you want to restrict scheduling to the
+newly provisioned nodes (assuming the provisioning class supports it).
+
 ### Limitations
 
 Currently, there are limitations for the compatibility of TAS with other
-features. In particular, a ClusterQueue referencing a TAS Resource
-Flavor (with the `.spec.topologyName` field) is marked as inactive if also
-using [MultiKueue](multikueue.md) or
-[ProvisioningRequest](/docs/admission-check-controllers/provisioning/) admission
-checks.
+features, including:
+- some scheduling directives (e.g. pod affinities and anti-affinities) are ignored,
+- the "podset-required-topology" annotation may fail if the underlying
+  ClusterAutoscaler cannot provision nodes that satisfy the domain constraint,
+- a ClusterQueue referencing a TAS Resource Flavor (with the `.spec.topologyName`
+field) is marked as inactive if also using [MultiKueue](multikueue.md).
```

  ```suggestion
  - a ClusterQueue for [MultiKueue](multikueue.md) references ResourceFlavors with Topology name (`.spec.topologyName`) is marked as inactive.
  ```
  
  Does this mean that CQ for MultiKueue does not support TAS, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-04T08:58:52Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:111** by [@mimowo](https://github.com/mimowo) — 2025-06-04T08:58:52Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
```

  it is under `workload.status.admissionChecks[].state`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-04T09:01:31Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:129** by [@mimowo](https://github.com/mimowo) — 2025-06-04T09:01:31Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
+3. **Topology assignment**: TAS, in the second pass of the Kueue scheduler,
+  calculates available capacity, including any newly provisioned nodes, and
+  updates the assignment with the assignment of topology domains. Once done the
+  `Admitted` condition is set on the Workload.
+
+Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/admission-check-controllers/provisioning.md)
+to see how you can configure Kueue if you want to restrict scheduling to the
+newly provisioned nodes (assuming the provisioning class supports it).
+
 ### Limitations
 
 Currently, there are limitations for the compatibility of TAS with other
-features. In particular, a ClusterQueue referencing a TAS Resource
-Flavor (with the `.spec.topologyName` field) is marked as inactive if also
-using [MultiKueue](multikueue.md) or
-[ProvisioningRequest](/docs/admission-check-controllers/provisioning/) admission
-checks.
+features, including:
+- some scheduling directives (e.g. pod affinities and anti-affinities) are ignored,
+- the "podset-required-topology" annotation may fail if the underlying
+  ClusterAutoscaler cannot provision nodes that satisfy the domain constraint,
+- a ClusterQueue referencing a TAS Resource Flavor (with the `.spec.topologyName`
+field) is marked as inactive if also using [MultiKueue](multikueue.md).
```

  Correct, currently no. We have an issue to support it: https://github.com/kubernetes-sigs/kueue/issues/5313

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-04T09:35:13Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:129** by [@mimowo](https://github.com/mimowo) — 2025-06-04T09:35:13Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
+3. **Topology assignment**: TAS, in the second pass of the Kueue scheduler,
+  calculates available capacity, including any newly provisioned nodes, and
+  updates the assignment with the assignment of topology domains. Once done the
+  `Admitted` condition is set on the Workload.
+
+Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/admission-check-controllers/provisioning.md)
+to see how you can configure Kueue if you want to restrict scheduling to the
+newly provisioned nodes (assuming the provisioning class supports it).
+
 ### Limitations
 
 Currently, there are limitations for the compatibility of TAS with other
-features. In particular, a ClusterQueue referencing a TAS Resource
-Flavor (with the `.spec.topologyName` field) is marked as inactive if also
-using [MultiKueue](multikueue.md) or
-[ProvisioningRequest](/docs/admission-check-controllers/provisioning/) admission
-checks.
+features, including:
+- some scheduling directives (e.g. pod affinities and anti-affinities) are ignored,
+- the "podset-required-topology" annotation may fail if the underlying
+  ClusterAutoscaler cannot provision nodes that satisfy the domain constraint,
+- a ClusterQueue referencing a TAS Resource Flavor (with the `.spec.topologyName`
+field) is marked as inactive if also using [MultiKueue](multikueue.md).
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-04T09:36:36Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:111** by [@mimowo](https://github.com/mimowo) — 2025-06-04T09:36:35Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
```

  >  Once all AdmissionChecks are ready, the Workload gets `Ready` admissionCheck state.
  
  This is not exactly accurate I think, because the fields `workload.status.admissionChecks[].state` are updated by AC controllers one by one

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-04T09:38:01Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:111** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T09:38:01Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
```

  ```
  2. **Admission checks**: The Workloads wait for all AdmissionChecks Ready, including the
    Provisioning one.
  ```
  
  In that case, how about this one?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-04T15:01:04Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:111** by [@mimowo](https://github.com/mimowo) — 2025-06-04T15:01:03Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
```

  I have rephrased. PTAL

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-04T15:06:33Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:115** by [@mimowo](https://github.com/mimowo) — 2025-06-04T15:06:33Z
```diff
@@ -98,14 +98,35 @@ to a TAS ResourceFlavor.
 
 {{< include "examples/tas/sample-job-preferred.yaml" "yaml" >}}
 
+### ClusterAutoscaler support
+
+TAS integrates with the [Kubernetes ClusterAutoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+through the [Provisioning AdmissionCheck](/docs/admission-check-controllers/provisioning/).
+
+When a workload is assigned to the TAS Resource Flavor with Provisioning
+AdmissionCheck, then its admission flow has the following stages:
+1. **Quota reservation**: quota is reserved, and the workload obtains the
+  `QuotaReserved` condtion. Preemptions are evaluated if configured.
+2. **Admission checks**: waiting for all AdmissionChecks, including the
+  Provisioning one, to report `Ready`.
+3. **Topology assignment**: TAS, in the second pass of the Kueue scheduler,
+  calculates available capacity, including any newly provisioned nodes, and
+  updates the assignment with the assignment of topology domains. Once done the
```

  Yes, I rephrased the section to make it more user-oriented. Users don't need to know about "Second pass".

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-04T16:03:00Z

Thank you!
/lgtm
/approve
