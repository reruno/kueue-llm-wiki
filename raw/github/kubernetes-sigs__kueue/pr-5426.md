# PR #5426: TAS: Fix using reservation is the second flavor is Provisioning

**Summary**: TAS: Fix using reservation is the second flavor is Provisioning

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5426

**Last updated**: 2025-07-10T12:05:34Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-provreq-tas-fallback` → `main`
- **Created**: 2025-05-30T15:14:28Z
- **Updated**: 2025-07-10T12:05:34Z
- **Merged**: 2025-06-03T09:42:39Z
- **Closed**: 2025-06-03T09:42:39Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 5   **+104 / -11**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:

It fixes the scenario when the first resource flavor is using reservation, and the second is using provisioning. 

In that scenario, a workload assigned to the reservation flavor is pending indefinitely as they obtains `delayedTopologyRequest: Pending` which prevents admission. At the same time it will never be set to Ready, because this flavor is not handled by second pass scheduling.

#### Special notes for your reviewer:

I'm yet investigating if there is a downstream issue if we evict from Provisioning flavor, and attempt to re-admit into reservation. I suspect there is, but I believe it can be handled in a follow up when confirmed. 

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Fix bug which prevented admitting any workloads if the first resource flavor is reservation, and the fallback is using ProvisioningRequest.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-05-30T15:14:34Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6483ee4370857ae78adec43d72d57df55ecf3445 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/683ebd2fb467b00008c30b1b |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-05-30T15:22:24Z

cc @PBundyra @mwysokin

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-02T13:07:30Z

I believe this is ready for review @PBundyra @gabesaba

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-03T08:31:37Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-03T08:31:44Z

LGTM label has been added.  <details>Git tree hash: 1b71873b4411924511eaf95ec292671d6c5aa9a2</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-03T08:31:46Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5426#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5426#issuecomment-2934124176" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/cache/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/OWNERS)~~ [mimowo,tenzen-y]
- ~~[pkg/scheduler/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-03T09:14:15Z

I need to adjust the code after the WorkloadReference is merged. On it.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-03T09:16:21Z

@tenzen-y updated and rebased. The only place which required update was this line: https://github.com/kubernetes-sigs/kueue/pull/5426/files#diff-130e36b28b6a84d355879caa6b7927c98ec18c63adad6ab4fc486458763080bfR4691

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-03T09:41:02Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-03T09:41:09Z

LGTM label has been added.  <details>Git tree hash: e29116047b7f2b35d7ccd6b9893b817ed29af181</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-03T09:43:57Z

I will prepare cherry-pick manually

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-02T15:40:11Z

- **pkg/cache/clusterqueue.go:652** by [@PBundyra](https://github.com/PBundyra) — 2025-06-02T15:40:11Z
```diff
@@ -636,6 +636,24 @@ func (c *clusterQueue) isTASOnly() bool {
 	return true
 }
 
-func (c *clusterQueue) hasProvRequestAdmissionCheck() bool {
-	return len(c.provisioningAdmissionChecks) > 0
+func (c *clusterQueue) flavorsWithProvReqAdmissionCheck() sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	for _, ac := range c.provisioningAdmissionChecks {
+		result.Insert(c.flavorsForAdmissionCheck(ac).UnsortedList()...)
+	}
+	return result
+}
+
+func (c *clusterQueue) flavorsForAdmissionCheck(ac kueue.AdmissionCheckReference) sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	if len(c.AdmissionChecks[ac]) == 0 {
+		for _, rg := range c.ResourceGroups {
+			result.Insert(rg.Flavors...)
+		}
```

  Why do we iterate over resourceGroups if there is no flavor for admissionCheck. Is there an implicit assumption that c.AdmissionChecks[ac] returns only flavors that are indicated by AdmissionCheckStrategy?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-02T15:48:14Z

- **pkg/cache/clusterqueue.go:640** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-02T14:55:53Z
```diff
@@ -636,6 +636,24 @@ func (c *clusterQueue) isTASOnly() bool {
 	return true
 }
 
-func (c *clusterQueue) hasProvRequestAdmissionCheck() bool {
-	return len(c.provisioningAdmissionChecks) > 0
+func (c *clusterQueue) flavorsWithProvReqAdmissionCheck() sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
```

  ```suggestion
  	rfs := sets.New[kueue.ResourceFlavorReference]()
  ```
  
  Could you avoid magic variable name?

- **pkg/cache/clusterqueue.go:648** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-02T14:56:40Z
```diff
@@ -636,6 +636,24 @@ func (c *clusterQueue) isTASOnly() bool {
 	return true
 }
 
-func (c *clusterQueue) hasProvRequestAdmissionCheck() bool {
-	return len(c.provisioningAdmissionChecks) > 0
+func (c *clusterQueue) flavorsWithProvReqAdmissionCheck() sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	for _, ac := range c.provisioningAdmissionChecks {
+		result.Insert(c.flavorsForAdmissionCheck(ac).UnsortedList()...)
+	}
+	return result
+}
+
+func (c *clusterQueue) flavorsForAdmissionCheck(ac kueue.AdmissionCheckReference) sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
```

  ```suggestion
  	flvs := sets.New[kueue.ResourceFlavorReference]()
  ```
  
  ditto

- **pkg/cache/clusterqueue.go:654** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-02T14:59:36Z
```diff
@@ -636,6 +636,24 @@ func (c *clusterQueue) isTASOnly() bool {
 	return true
 }
 
-func (c *clusterQueue) hasProvRequestAdmissionCheck() bool {
-	return len(c.provisioningAdmissionChecks) > 0
+func (c *clusterQueue) flavorsWithProvReqAdmissionCheck() sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	for _, ac := range c.provisioningAdmissionChecks {
+		result.Insert(c.flavorsForAdmissionCheck(ac).UnsortedList()...)
+	}
+	return result
+}
+
+func (c *clusterQueue) flavorsForAdmissionCheck(ac kueue.AdmissionCheckReference) sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	if len(c.AdmissionChecks[ac]) == 0 {
+		for _, rg := range c.ResourceGroups {
+			result.Insert(rg.Flavors...)
+		}
+	} else {
+		for _, ac := range c.AdmissionChecks {
```

  ```suggestion
  		for _, flv := range c.AdmissionChecks {
  ```
  
  Actually, values are ResourceFlavors.

- **pkg/cache/clusterqueue_snapshot.go:61** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-02T15:33:04Z
```diff
@@ -58,7 +58,7 @@ type ClusterQueueSnapshot struct {
 	TASFlavors map[kueue.ResourceFlavorReference]*TASFlavorSnapshot
 	tasOnly    bool
 
-	hasProvRequestAdmissionCheck bool
+	flavorsWithProvReqACs sets.Set[kueue.ResourceFlavorReference]
```

  ```suggestion
  	flavorsForProvReqACs sets.Set[kueue.ResourceFlavorReference]
  ```
  
  Actually, this will return only flavors. This does not return Flavor with ProvReqs.

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:53** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-02T15:42:22Z
```diff
@@ -44,15 +44,11 @@ func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info, cq *cache.Clus
 				// we add it to the list of TASRequests
 				continue
 			}
-			if !workload.HasQuotaReservation(wl.Obj) && cq.HasProvRequestAdmissionCheck() {
-				psAssignment.DelayedTopologyRequest = ptr.To(kueue.DelayedTopologyRequestStatePending)
-				continue
-			}
 			isTASImplied := isTASImplied(&podSet, cq)
 			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, isTASImplied, i)
 			if err != nil {
 				psAssignment.error(err)
-			} else {
+			} else if psTASRequest != nil {
 				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
```

  What if psTASReqeust is nil?
  Before this PR, we updated `tasRequests`.

- **pkg/scheduler/scheduler_test.go:4646** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-02T15:48:11Z
```diff
@@ -4643,6 +4643,83 @@ func TestScheduleForTAS(t *testing.T) {
 				},
 			},
 		},
+		"workload in CQ with reservation and provisioning flavors; fits into reservation": {
```

  Could you generalize the name of this situation? Does this mean that a Workload gets 2 flavors, (1 is backed by a ready node, and (2 is backed by under-provisioning node?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T16:02:35Z

- **pkg/cache/clusterqueue.go:652** by [@mimowo](https://github.com/mimowo) — 2025-06-02T16:02:35Z
```diff
@@ -636,6 +636,24 @@ func (c *clusterQueue) isTASOnly() bool {
 	return true
 }
 
-func (c *clusterQueue) hasProvRequestAdmissionCheck() bool {
-	return len(c.provisioningAdmissionChecks) > 0
+func (c *clusterQueue) flavorsWithProvReqAdmissionCheck() sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	for _, ac := range c.provisioningAdmissionChecks {
+		result.Insert(c.flavorsForAdmissionCheck(ac).UnsortedList()...)
+	}
+	return result
+}
+
+func (c *clusterQueue) flavorsForAdmissionCheck(ac kueue.AdmissionCheckReference) sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	if len(c.AdmissionChecks[ac]) == 0 {
+		for _, rg := range c.ResourceGroups {
+			result.Insert(rg.Flavors...)
+		}
```

  Yes. this is because the AdmissionChecks[ac] does not contain the flavors if AdmissionCheckStrategy is used: https://github.com/kubernetes-sigs/kueue/blob/e3bbc66fa7c95df49668a33934e9ae9c97d9b65a/pkg/cache/clusterqueue.go#L62-L64
  
  I'm not a big fun of that and would consider the map should always contain the flavors, but it seems more work.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T16:11:57Z

- **pkg/cache/clusterqueue.go:640** by [@mimowo](https://github.com/mimowo) — 2025-06-02T16:11:56Z
```diff
@@ -636,6 +636,24 @@ func (c *clusterQueue) isTASOnly() bool {
 	return true
 }
 
-func (c *clusterQueue) hasProvRequestAdmissionCheck() bool {
-	return len(c.provisioningAdmissionChecks) > 0
+func (c *clusterQueue) flavorsWithProvReqAdmissionCheck() sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
```

  Done as `flvs` (for consistency with the other function)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T16:12:04Z

- **pkg/cache/clusterqueue.go:648** by [@mimowo](https://github.com/mimowo) — 2025-06-02T16:12:04Z
```diff
@@ -636,6 +636,24 @@ func (c *clusterQueue) isTASOnly() bool {
 	return true
 }
 
-func (c *clusterQueue) hasProvRequestAdmissionCheck() bool {
-	return len(c.provisioningAdmissionChecks) > 0
+func (c *clusterQueue) flavorsWithProvReqAdmissionCheck() sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	for _, ac := range c.provisioningAdmissionChecks {
+		result.Insert(c.flavorsForAdmissionCheck(ac).UnsortedList()...)
+	}
+	return result
+}
+
+func (c *clusterQueue) flavorsForAdmissionCheck(ac kueue.AdmissionCheckReference) sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T16:13:03Z

- **pkg/cache/clusterqueue.go:654** by [@mimowo](https://github.com/mimowo) — 2025-06-02T16:13:03Z
```diff
@@ -636,6 +636,24 @@ func (c *clusterQueue) isTASOnly() bool {
 	return true
 }
 
-func (c *clusterQueue) hasProvRequestAdmissionCheck() bool {
-	return len(c.provisioningAdmissionChecks) > 0
+func (c *clusterQueue) flavorsWithProvReqAdmissionCheck() sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	for _, ac := range c.provisioningAdmissionChecks {
+		result.Insert(c.flavorsForAdmissionCheck(ac).UnsortedList()...)
+	}
+	return result
+}
+
+func (c *clusterQueue) flavorsForAdmissionCheck(ac kueue.AdmissionCheckReference) sets.Set[kueue.ResourceFlavorReference] {
+	result := sets.New[kueue.ResourceFlavorReference]()
+	if len(c.AdmissionChecks[ac]) == 0 {
+		for _, rg := range c.ResourceGroups {
+			result.Insert(rg.Flavors...)
+		}
+	} else {
+		for _, ac := range c.AdmissionChecks {
```

  Actually, it should have been using `c.AdmissionChecks[ac]`. I fixed that and simplified the code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T16:13:11Z

- **pkg/cache/clusterqueue_snapshot.go:61** by [@mimowo](https://github.com/mimowo) — 2025-06-02T16:13:11Z
```diff
@@ -58,7 +58,7 @@ type ClusterQueueSnapshot struct {
 	TASFlavors map[kueue.ResourceFlavorReference]*TASFlavorSnapshot
 	tasOnly    bool
 
-	hasProvRequestAdmissionCheck bool
+	flavorsWithProvReqACs sets.Set[kueue.ResourceFlavorReference]
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T16:13:18Z

- **pkg/scheduler/scheduler_test.go:4646** by [@mimowo](https://github.com/mimowo) — 2025-06-02T16:13:18Z
```diff
@@ -4643,6 +4643,83 @@ func TestScheduleForTAS(t *testing.T) {
 				},
 			},
 		},
+		"workload in CQ with reservation and provisioning flavors; fits into reservation": {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-02T16:15:06Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:53** by [@mimowo](https://github.com/mimowo) — 2025-06-02T16:15:06Z
```diff
@@ -44,15 +44,11 @@ func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info, cq *cache.Clus
 				// we add it to the list of TASRequests
 				continue
 			}
-			if !workload.HasQuotaReservation(wl.Obj) && cq.HasProvRequestAdmissionCheck() {
-				psAssignment.DelayedTopologyRequest = ptr.To(kueue.DelayedTopologyRequestStatePending)
-				continue
-			}
 			isTASImplied := isTASImplied(&podSet, cq)
 			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, isTASImplied, i)
 			if err != nil {
 				psAssignment.error(err)
-			} else {
+			} else if psTASRequest != nil {
 				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
```

  previously psTASReqeust was never nil if no error. Now psTASReqeust=nil, and err=nil is possible. This only happens if we have PodSet using flavor which has the ProvisinoningRequest. In this case we just set delayedTopologyRequest=Pending, and return. This is not an error.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-03T08:31:30Z

- **pkg/scheduler/flavorassigner/tas_flavorassigner.go:53** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-03T08:31:30Z
```diff
@@ -44,15 +44,11 @@ func (a *Assignment) WorkloadsTopologyRequests(wl *workload.Info, cq *cache.Clus
 				// we add it to the list of TASRequests
 				continue
 			}
-			if !workload.HasQuotaReservation(wl.Obj) && cq.HasProvRequestAdmissionCheck() {
-				psAssignment.DelayedTopologyRequest = ptr.To(kueue.DelayedTopologyRequestStatePending)
-				continue
-			}
 			isTASImplied := isTASImplied(&podSet, cq)
 			psTASRequest, err := podSetTopologyRequest(psAssignment, wl, cq, isTASImplied, i)
 			if err != nil {
 				psAssignment.error(err)
-			} else {
+			} else if psTASRequest != nil {
 				tasRequests[psTASRequest.Flavor] = append(tasRequests[psTASRequest.Flavor], *psTASRequest)
 			}
```

  That makes sense. Thank you.
