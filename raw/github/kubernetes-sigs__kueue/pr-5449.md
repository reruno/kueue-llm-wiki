# PR #5449: [KEP-2724] Introduce two-level scheduling

**Summary**: [KEP-2724] Introduce two-level scheduling

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5449

**Last updated**: 2025-06-06T15:04:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@lchrzaszcz](https://github.com/lchrzaszcz)
- **Branch**: `update-tas-kep-with-two-level-scheduling` → `main`
- **Created**: 2025-06-02T14:22:38Z
- **Updated**: 2025-06-06T15:04:43Z
- **Merged**: 2025-06-06T15:04:41Z
- **Closed**: 2025-06-06T15:04:41Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+225 / -69**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

This PR updates Topology-Aware Scheduling KEP to introduce Two-Level Scheduling and PodSet Chunk topology feature.

KEP for #5439

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-02T14:22:44Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e808294c2a094bb3acab862bd562b3c32893580f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6842fbc0ef8b3f00089ac1f5 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-02T14:22:48Z

Hi @lchrzaszcz. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-02T14:25:40Z

cc @gabesaba @PBundyra

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-02T14:25:50Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-02T14:51:17Z

> Fixes #5439

change to Part of, or KEP for.

we have a robot which will close the issue once PR with Fixes before issue number is merged.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T14:15:24Z

Thank you 👍 This is a great extension to TAS capabilities. I'm pretty sure there will pop up some questions as we go (as always 😄 ).

/lgtm
leaving the final approval to @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-05T14:15:32Z

LGTM label has been added.  <details>Git tree hash: 50634042ff1e05fcee89777c523205bcc9710f04</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-06T12:25:40Z

LGTM label has been added.  <details>Git tree hash: e45813b9fb7e9fddede8c2717c7c4b82d1321e0c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-06T12:25:44Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5449#" title="Author self-approved">lchrzaszcz</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5449#pullrequestreview-2904767396" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-06T12:42:21Z

@lchrzaszcz could you fix CI with `make toc-update`?

### Comment by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-06T12:55:09Z

@tenzen-y Sure! I'll make sure it is fixed in follow-up PR here:https://github.com/kubernetes-sigs/kueue/pull/5538

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-06T12:57:11Z

> @tenzen-y Sure! I'll make sure it is fixed in follow-up PR here:#5538

Sorry? I meant to fix this PR CI.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-06T12:58:45Z

Once you perform `make toc-update` in your local and push it to here, the CI errors will be gone away.

### Comment by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-06T14:12:10Z

Oh, right, sorry, I thought it got merged already and it failed on main branch. I've just updated TOC but it resulted in no changes. I'll just try to rerun that CI. If it fails again it might be the case that merge with current main makes it fail (if CI merges with main before tests), I'll rebase it then.
/test pull-kueue-verify-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-06T14:38:50Z

LGTM label has been added.  <details>Git tree hash: d6feb784e214e7277430da87b398c01985c63e09</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-06-02T15:01:23Z

- **keps/2724-topology-aware-scheduling/README.md:711** by [@kannon92](https://github.com/kannon92) — 2025-06-02T15:01:23Z
```diff
@@ -539,6 +699,17 @@ type PodSetTopologyRequest struct {
   // SubGroupIndexLabel indicates the count of replicated Jobs (groups) within a PodSet.
   // For example, in the context of JobSet this value is read from jobset.sigs.k8s.io/replicatedjob-replicas.
   SubGroupCount *int32
+
+  // PodSetChunkRequiredTopology indicates the topology level required by the PodSet chunk, as
+  // indicated by the `kueue.x-k8s.io/podset-chunk-required-topology` annotation.
+  //
+  // +optional
+  PodSetChunkRequiredTopology *string `json:"podSetChunkRequiredTopology,omitempty"`
+
+  // PodSetChunkSize indicates the size of a subgroup of pods in a PodSet for which
+  // Kueue finds a requested topology domain on a level defined
+  // in `kueue.x-k8s.io/podset-chunk-required-topology` annotation.
```

  ```suggestion
    // in `kueue.x-k8s.io/podset-chunk-required-topology` annotation.
    // +optional
  ```

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-06-02T15:03:13Z

- **keps/2724-topology-aware-scheduling/README.md:270** by [@kannon92](https://github.com/kannon92) — 2025-06-02T15:03:12Z
```diff
@@ -257,6 +265,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Chunks, which are
```

  Chunk seems really unformal..
  
  Can we call this a PodSet Group? 
  
  Or at least suggest a few names?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-06-02T19:15:10Z

- **keps/2724-topology-aware-scheduling/README.md:170** by [@kannon92](https://github.com/kannon92) — 2025-06-02T19:15:10Z
```diff
@@ -163,6 +165,11 @@ consecutive ranks to be placed within the same topology domain (if possible).
 
 Extension: support for indicating the order of pods for external Jobs.
 
+#### Story 5
```

  @mimowo @tenzen-y 
  
  Should we include LWS for "two-level" scheduling?
  
  I think there will similar work for JobSet/LWS.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-03T07:49:44Z

- **keps/2724-topology-aware-scheduling/README.md:270** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-03T07:49:44Z
```diff
@@ -257,6 +265,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Chunks, which are
```

  I have some other ideas:
  - PodSet Slice
  - PodSet Partition
  - PodSet Subgroup
  - PodSet Subset
  
  I think that "PodSet Group" might suggest that it groups several PodSets together, which is not the case. I would like to emphasize on "chunk" being a subset of the PodSet.
  
  @kannon92 @mimowo @tenzen-y What do you think would be a good name? Here are the candidates we have so far:
  - PodSet Chunk
  - PodSet Group
  - PodSet Slice
  - PodSet Partition
  - PodSet Subgroup
  - PodSet Subset

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-03T08:02:29Z

- **keps/2724-topology-aware-scheduling/README.md:270** by [@mimowo](https://github.com/mimowo) — 2025-06-03T08:02:29Z
```diff
@@ -257,6 +265,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Chunks, which are
```

  Thank you for the call @kannon92 . I like chunks as it captures the idea of fragmenting data into same size fragments. 
  
  I think the term is used in computer science contexts formally, for example in HTTP transfer encoding `Transfer-Encoding: chunked`. 
  
  It is also commonly used referring to "chunking" arrays of lists into fragments of the same size, in my perception the other terms don't capture "same size" that well.
  
  Having said that I'm open to other options, this is my ordered preference list:
  1. Chunk
  2. Slice
  3. Fragment
  
  I agree "Group" could be misleading suggesting grouping rather than fragmenting.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-03T08:04:44Z

- **keps/2724-topology-aware-scheduling/README.md:170** by [@mimowo](https://github.com/mimowo) — 2025-06-03T08:04:44Z
```diff
@@ -163,6 +165,11 @@ consecutive ranks to be placed within the same topology domain (if possible).
 
 Extension: support for indicating the order of pods for external Jobs.
 
+#### Story 5
```

  yes, extending to "LWS" is definetly on our radar. However, for LWS we need to co-schedule PodSets rather than "chunk" them. So, we consider it a follow up.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-03T09:35:55Z

- **keps/2724-topology-aware-scheduling/README.md:711** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-03T09:35:55Z
```diff
@@ -539,6 +699,17 @@ type PodSetTopologyRequest struct {
   // SubGroupIndexLabel indicates the count of replicated Jobs (groups) within a PodSet.
   // For example, in the context of JobSet this value is read from jobset.sigs.k8s.io/replicatedjob-replicas.
   SubGroupCount *int32
+
+  // PodSetChunkRequiredTopology indicates the topology level required by the PodSet chunk, as
+  // indicated by the `kueue.x-k8s.io/podset-chunk-required-topology` annotation.
+  //
+  // +optional
+  PodSetChunkRequiredTopology *string `json:"podSetChunkRequiredTopology,omitempty"`
+
+  // PodSetChunkSize indicates the size of a subgroup of pods in a PodSet for which
+  // Kueue finds a requested topology domain on a level defined
+  // in `kueue.x-k8s.io/podset-chunk-required-topology` annotation.
```

  Thanks for suggestion! I've applied the change.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-06-03T11:57:41Z

- **keps/2724-topology-aware-scheduling/README.md:270** by [@kannon92](https://github.com/kannon92) — 2025-06-03T11:57:41Z
```diff
@@ -257,6 +265,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Chunks, which are
```

  Thanks for clearing that up. If you and @tenzen-y are happy with chunk than that is fine to me.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-03T14:06:41Z

- **keps/2724-topology-aware-scheduling/README.md:956** by [@PBundyra](https://github.com/PBundyra) — 2025-06-03T14:06:41Z
```diff
@@ -762,6 +929,39 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Chunk, which is a subset of PodSet. All chunks
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for chunk. The requested topology 
+level for chunks has to be on the same or below level of the main topology.
+
+Requesting chunk topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+chunks it considers the amount of chunks that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for chunk is "host" and the podset chunk size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
+| -----------------------| ----------- | - | - | - | - | - |
+| BestFit                | 12          | 6 | . | 4 | . | 2 |
```

  I think it's worth noting that even with the `BestFit` algorithm there is no guarantee that there won't be any leftover capacity in the selected nodes, as there is already some rounding in the assumption nodes can accomodate an integer number of pods. The `BestFit` algorithm excellently mitigates resource fragmentation, and it's no doubt it's the way to go, but let's just keep in mind even that has some limits, and we won't mitigate fragmentation completely.  Of course this reasoning applies to both single-level and two-level scheduling

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-03T15:24:01Z

- **keps/2724-topology-aware-scheduling/README.md:270** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-03T15:24:00Z
```diff
@@ -257,6 +265,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Chunks, which are
```

  @mimowo I think `PodSet Slice` might be better since `slice` is a kubernetes term and an infrastructure term like `ResourceSlice` for DRA and Google Cloud TPU Slice. Do you have a good example if `Chunk` would align with Kubernetes term?
  
  So, I believe that the `Slice` is more popular in the Kubernetes and Platform Engineering world.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-04T08:53:30Z

- **keps/2724-topology-aware-scheduling/README.md:270** by [@mimowo](https://github.com/mimowo) — 2025-06-04T08:53:30Z
```diff
@@ -257,6 +265,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Chunks, which are
```

  Indeed "slice" is much more common in k8s world.
  
  The reasoning for using chunk was that the term "chunk" often indicates fixed size, while "slice" is more flexible. However, indeed I think slice is used in K8s for EndpointSlice when the Endpoints are split into same size chunks.
  
  Let's go with PodSetSlice then.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-04T09:12:20Z

- **keps/2724-topology-aware-scheduling/README.md:270** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-04T09:12:20Z
```diff
@@ -257,6 +265,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Chunks, which are
```

  SGTM, thanks.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-04T09:56:50Z

- **keps/2724-topology-aware-scheduling/README.md:270** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-04T09:56:50Z
```diff
@@ -257,6 +265,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Chunks, which are
```

  I've replaced all references to "chunk" with "slice". Thanks for the suggestions.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-04T09:57:19Z

- **keps/2724-topology-aware-scheduling/README.md:956** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-04T09:57:19Z
```diff
@@ -762,6 +929,39 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Chunk, which is a subset of PodSet. All chunks
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for chunk. The requested topology 
+level for chunks has to be on the same or below level of the main topology.
+
+Requesting chunk topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+chunks it considers the amount of chunks that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for chunk is "host" and the podset chunk size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
+| -----------------------| ----------- | - | - | - | - | - |
+| BestFit                | 12          | 6 | . | 4 | . | 2 |
```

  I've added a one-sentence disclaimer that the "tight fit" I'm referring to does not guarantee no dangling resources.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T09:30:42Z

- **keps/2724-topology-aware-scheduling/README.md:317** by [@mimowo](https://github.com/mimowo) — 2025-06-05T09:30:42Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
```

  What if this annotation `kueue.x-k8s.io/podset-slice-size` is not provided by user?
  
  Do we;
  1. return error from validation webhook
  2. default it implicitly?
  3. default it explicitly in our Jobset webhook?
  
  It seems setting it to anything different than "parallelism" in case of JobSet will be very exceptional. So, I would be leaning to default the annotation to parallelism when `kueue.x-k8s.io/podset-slice-required-topology`. Leaving this to be set by user every time by the user makes room for error.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T09:45:21Z

- **keps/2724-topology-aware-scheduling/README.md:381** by [@mimowo](https://github.com/mimowo) — 2025-06-05T09:45:21Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
+PodSet into slices with 4 pods each (so 2 slices in total) and
+each slice requires "host" topology domain.
+
+Since slice size is equal to "completions" for the Job, the end result
+is that each Job will be placed within a "host".
+
+For now PodSet Slice topology has to be required (cannot be "preferred").
+
+###### JobSet with two-level scheduling
+
+According to [Story 5](#story-5) we noticed that some users would like to
+co-locate all Jobs resulting from ReplicatedJob in JobSet in some 
+higher-level domain (like "block"), but also  co-locate each Job within
+lower-level domain like "host".
+
+To achieve that we are allowing to defined both main and slice topologies.
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-preferred-topology: cloud.provider.com/topology-block
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
```

  Same question about handling JobSets without this annotation, but with `kueue.x-k8s.io/podset-slice-required-topology`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T09:50:18Z

LGTM overall. I just have a question to clarify about the handling the case without "kueue.x-k8s.io/podset-slice-size".

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T09:51:39Z

- **keps/2724-topology-aware-scheduling/README.md:317** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T09:51:39Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
```

  I think it's a good idea to provide the default for a JobSet. Right now in this PR: https://github.com/kubernetes-sigs/kueue/pull/5353 I'm assuming that the default is 1 pod (because technically TAS scheduling without slice topology is TAS scheduling with slice topology of the lowest topology level with size of 1 pod).
  
  From the validation point of view I would say we should assume a default value of parallelism if it's JobSet, but return an error if it's something else (of course assuming the slice topology is requested).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T10:12:11Z

- **keps/2724-topology-aware-scheduling/README.md:317** by [@mimowo](https://github.com/mimowo) — 2025-06-05T10:12:10Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
```

  So, IIUC we throw an error for all CRDs requiring to set `kueue.x-k8s.io/podset-slice-size` explicitly, except for JobSet. For JobSet we have the sensible default: `parallelism`.
  
  We will decide during implementation if we default it implicitly or explicitly.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-05T10:38:47Z

Could you update the story 2 Notes since we are working on this as part of alpha stage?

```
Note: not planned for [Alpha](https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-scheduling#alpha), to be evaluated for [Beta](https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-scheduling#beta).`
```
https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-scheduling#story-2

- **keps/2724-topology-aware-scheduling/README.md:324** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T10:20:27Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
```

  What if completions and parallelism are different values?
  Do you want to say `2 ReplicatedJob instances with 4 "parallelism" each`?

- **keps/2724-topology-aware-scheduling/README.md:331** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T10:21:39Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
+PodSet into slices with 4 pods each (so 2 slices in total) and
+each slice requires "host" topology domain.
+
+Since slice size is equal to "completions" for the Job, the end result
+is that each Job will be placed within a "host".
+
+For now PodSet Slice topology has to be required (cannot be "preferred").
```

  What do you mean `for now`? Does this mean `preferred` will be supported in Beta stage?

- **keps/2724-topology-aware-scheduling/README.md:729** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T10:29:25Z
```diff
@@ -539,6 +698,19 @@ type PodSetTopologyRequest struct {
   // SubGroupIndexLabel indicates the count of replicated Jobs (groups) within a PodSet.
   // For example, in the context of JobSet this value is read from jobset.sigs.k8s.io/replicatedjob-replicas.
   SubGroupCount *int32
+
+  // PodSetSliceRequiredTopology indicates the topology level required by the PodSet slice, as
+  // indicated by the `kueue.x-k8s.io/podset-slice-required-topology` annotation.
+  //
+  // +optional
+  PodSetSliceRequiredTopology *string `json:"podSetSliceRequiredTopology,omitempty"`
+
+  // PodSetSliceSize indicates the size of a subgroup of pods in a PodSet for which
+  // Kueue finds a requested topology domain on a level defined
+  // in `kueue.x-k8s.io/podset-slice-required-topology` annotation.
+  //
+  // +optional
+  PodSetSliceSize *int32 `json:"podSetSliceSize,omitempty"`
```

  ```yaml
  topologyRequest:
    podSetSlice:
      requiredTopology: xxx
      size: yyy
      preferred: zzz // This is not included in this proposal. for future expanding.
  ```
  
  Do we need to consider future API expansion?
  @mimowo Do you have any plans to expand the PodSet Slice capability in TAS?

- **keps/2724-topology-aware-scheduling/README.md:932** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T10:31:09Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
```

  ```suggestion
  ### Two-level Topology Aware scheduling
  ```
  
  Does this indicate for TAS? Because TAS is already a second-level scheduler and an entire Kueue PoV.

- **keps/2724-topology-aware-scheduling/README.md:954** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T10:33:15Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  What does `PodSet size` mean? PodSet count? PodSet Slice size?

- **keps/2724-topology-aware-scheduling/README.md:962** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T10:38:29Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
+| -----------------------| ----------- | - | - | - | - | - |
+| BestFit                | 12          | 6 | . | 4 | . | 2 |
+| MostFreeCapacity       | 12          | 6 | 2 | 4 | . | . |
+| LeastFreeCapacity      | 10          | . | 2 | 4 | 2 | 2 |
+
+Explanation:
+- `BestFit` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices. The last domain has been "optimized" to find the tight fit. 
+- `MostFreeCapacity` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices, so we assigned as much pods as possible there and filled it entirely.
```

  Uhm, it sounds weird. It looks like this violates `MostFree` strategy.
  Why do we need to violate it only when `MostFreeCapacity`?

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T10:44:09Z

- **keps/2724-topology-aware-scheduling/README.md:331** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T10:44:09Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
+PodSet into slices with 4 pods each (so 2 slices in total) and
+each slice requires "host" topology domain.
+
+Since slice size is equal to "completions" for the Job, the end result
+is that each Job will be placed within a "host".
+
+For now PodSet Slice topology has to be required (cannot be "preferred").
```

  That's not something we've planned, so I think it's just a poor choice of word that suggest it is planned. I'll change it to just state that without any assumption.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T10:45:35Z

- **keps/2724-topology-aware-scheduling/README.md:729** by [@mimowo](https://github.com/mimowo) — 2025-06-05T10:45:35Z
```diff
@@ -539,6 +698,19 @@ type PodSetTopologyRequest struct {
   // SubGroupIndexLabel indicates the count of replicated Jobs (groups) within a PodSet.
   // For example, in the context of JobSet this value is read from jobset.sigs.k8s.io/replicatedjob-replicas.
   SubGroupCount *int32
+
+  // PodSetSliceRequiredTopology indicates the topology level required by the PodSet slice, as
+  // indicated by the `kueue.x-k8s.io/podset-slice-required-topology` annotation.
+  //
+  // +optional
+  PodSetSliceRequiredTopology *string `json:"podSetSliceRequiredTopology,omitempty"`
+
+  // PodSetSliceSize indicates the size of a subgroup of pods in a PodSet for which
+  // Kueue finds a requested topology domain on a level defined
+  // in `kueue.x-k8s.io/podset-slice-required-topology` annotation.
+  //
+  // +optional
+  PodSetSliceSize *int32 `json:"podSetSliceSize,omitempty"`
```

  > @mimowo Do you have any plans to expand the PodSet Slice capability in TAS?
  
  No,  the current API proposal is already covering all use-cases we currently have regarding PodSet slices.
  
  There might be future extensions which are hard to predict as typically with future :).
  
  Personally, I think flat structure as in the current KEP is ok for simplicity.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T10:46:53Z

- **keps/2724-topology-aware-scheduling/README.md:331** by [@mimowo](https://github.com/mimowo) — 2025-06-05T10:46:53Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
+PodSet into slices with 4 pods each (so 2 slices in total) and
+each slice requires "host" topology domain.
+
+Since slice size is equal to "completions" for the Job, the end result
+is that each Job will be placed within a "host".
+
+For now PodSet Slice topology has to be required (cannot be "preferred").
```

  We don't have currently any use-cases going beyond "required". I would suggest to indeed drop "For now", because it warrants questions. When we have such use cases we will need to update the KEP anyway.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T10:48:36Z

- **keps/2724-topology-aware-scheduling/README.md:954** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T10:48:36Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  It's a number of pods in PodSet. To make it more readable I have a few ideas how to rephrase it:
  - PodSet count - consistent with the code I guess
  - Pods count
  - Pods count in PodSet
  - Number of pods in PodSet
  - What do you think suits the most?

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T10:56:30Z

- **keps/2724-topology-aware-scheduling/README.md:962** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T10:56:30Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
+| -----------------------| ----------- | - | - | - | - | - |
+| BestFit                | 12          | 6 | . | 4 | . | 2 |
+| MostFreeCapacity       | 12          | 6 | 2 | 4 | . | . |
+| LeastFreeCapacity      | 10          | . | 2 | 4 | 2 | 2 |
+
+Explanation:
+- `BestFit` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices. The last domain has been "optimized" to find the tight fit. 
+- `MostFreeCapacity` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices, so we assigned as much pods as possible there and filled it entirely.
```

  That's a great question. I was thinking about sorting domains based on `state` as that feels intuitive for "MostFree" strategy. However we've discussed that with @mimowo that this mode is deprecated and do not really want to invest time in that mode, so this is the result of just using "BestFit" logic without optimizing the last domain.
  
  Do you think we should just apply sorting by "state" to MostFree mode?

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T11:17:38Z

- **keps/2724-topology-aware-scheduling/README.md:324** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T11:17:38Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
```

  Yes, thanks for spotting that!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-05T11:29:45Z

- **keps/2724-topology-aware-scheduling/README.md:729** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T11:29:45Z
```diff
@@ -539,6 +698,19 @@ type PodSetTopologyRequest struct {
   // SubGroupIndexLabel indicates the count of replicated Jobs (groups) within a PodSet.
   // For example, in the context of JobSet this value is read from jobset.sigs.k8s.io/replicatedjob-replicas.
   SubGroupCount *int32
+
+  // PodSetSliceRequiredTopology indicates the topology level required by the PodSet slice, as
+  // indicated by the `kueue.x-k8s.io/podset-slice-required-topology` annotation.
+  //
+  // +optional
+  PodSetSliceRequiredTopology *string `json:"podSetSliceRequiredTopology,omitempty"`
+
+  // PodSetSliceSize indicates the size of a subgroup of pods in a PodSet for which
+  // Kueue finds a requested topology domain on a level defined
+  // in `kueue.x-k8s.io/podset-slice-required-topology` annotation.
+  //
+  // +optional
+  PodSetSliceSize *int32 `json:"podSetSliceSize,omitempty"`
```

  Alright. In that case, let's keep the current form.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T11:30:45Z

- **keps/2724-topology-aware-scheduling/README.md:381** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T11:30:45Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
+PodSet into slices with 4 pods each (so 2 slices in total) and
+each slice requires "host" topology domain.
+
+Since slice size is equal to "completions" for the Job, the end result
+is that each Job will be placed within a "host".
+
+For now PodSet Slice topology has to be required (cannot be "preferred").
+
+###### JobSet with two-level scheduling
+
+According to [Story 5](#story-5) we noticed that some users would like to
+co-locate all Jobs resulting from ReplicatedJob in JobSet in some 
+higher-level domain (like "block"), but also  co-locate each Job within
+lower-level domain like "host".
+
+To achieve that we are allowing to defined both main and slice topologies.
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-preferred-topology: cloud.provider.com/topology-block
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
```

  Added a sentence to clarify that for JobSet we will introduce a default of `parallelism`. Added also a separate section about validation of podset slice size.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T11:31:00Z

- **keps/2724-topology-aware-scheduling/README.md:317** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T11:31:00Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
```

  Added a sentence to clarify that for JobSet we will introduce a default of `parallelism`. Added also a separate section about validation of podset slice size.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-05T11:31:28Z

- **keps/2724-topology-aware-scheduling/README.md:331** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T11:31:28Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
+PodSet into slices with 4 pods each (so 2 slices in total) and
+each slice requires "host" topology domain.
+
+Since slice size is equal to "completions" for the Job, the end result
+is that each Job will be placed within a "host".
+
+For now PodSet Slice topology has to be required (cannot be "preferred").
```

  Thank you for an explanation. Could you add it to Non Goal section?
  Once we decide to support it, we can remove it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T11:32:07Z

- **keps/2724-topology-aware-scheduling/README.md:962** by [@mimowo](https://github.com/mimowo) — 2025-06-05T11:32:07Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
+| -----------------------| ----------- | - | - | - | - | - |
+| BestFit                | 12          | 6 | . | 4 | . | 2 |
+| MostFreeCapacity       | 12          | 6 | 2 | 4 | . | . |
+| LeastFreeCapacity      | 10          | . | 2 | 4 | 2 | 2 |
+
+Explanation:
+- `BestFit` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices. The last domain has been "optimized" to find the tight fit. 
+- `MostFreeCapacity` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices, so we assigned as much pods as possible there and filled it entirely.
```

  Yes, and in fact I would like to drop MostFreeCapacity in 0.13. It was implemented first, but we quickly realized BestFit is better in all known cases, we introduced the MostFreeCapacity feature gate just as a bailout option in case there is a bug in BestFit. It is deprecated since 0.11. It makes like harder I would suggest sending a preparatory or follow up PR to drop it. 
  
  wdyt @tenzen-y ?
  
  btw. for LeastFreeCapacity we would like to still maintain for a bit, because there might be use-cases for reducing fragmentation.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T11:34:05Z

- **keps/2724-topology-aware-scheduling/README.md:331** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T11:34:05Z
```diff
@@ -257,6 +264,137 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
+PodSet into slices with 4 pods each (so 2 slices in total) and
+each slice requires "host" topology domain.
+
+Since slice size is equal to "completions" for the Job, the end result
+is that each Job will be placed within a "host".
+
+For now PodSet Slice topology has to be required (cannot be "preferred").
```

  Sure. I've added it there.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-05T14:44:03Z

- **keps/2724-topology-aware-scheduling/README.md:324** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T14:26:26Z
```diff
@@ -257,6 +264,139 @@ the "rack" topology domain (there is only one such Pod, so the requirement
 is satisfied trivially). The PodSet corresponding to the worker only
 prefers the "rack" topology domain, so it may fallback to "block".
 
+###### JobSet with ReplicatedJob instance topology
+
+To address [Story 2](#story-2) we are introducing PodSet Slices, which are
+subsets of the whole PodSet that are target to their own topology requirements.
+
+To keep solution generic (not only for JobSet) and allow to split PodSet into
+groups for other workload types, we decided to go with low-level API to allow
+user to directly specify PodSet Slice size.
+
+To achieve a result of each ReplicatedJob instance having its own topology
+domain user should align the slice size to "completions".
+
+**Example**:
+
+```yaml
+apiVersion: jobset.x-k8s.io/v1alpha2
+kind: JobSet
+metadata:
+  name: tas-example-jobset
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  replicatedJobs:
+  - name: leader
+    replicas: 1
+    template:
+      spec:
+        completions: 1
+        parallelism: 1
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-required-topology: cloud.provider.com/topology-block
+          spec:
+            containers:
+            - name: leader
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+  - name: workers
+    replicas: 2
+    template:
+      spec:
+        completions: 4
+        parallelism: 4
+        template:
+          metadata:
+            annotations:
+              kueue.x-k8s.io/podset-slice-required-topology: cloud.provider.com/topology-host
+              kueue.x-k8s.io/podset-slice-size: 4
+          spec:
+            containers:
+            - name: worker
+              image: registry.k8s.io/e2e-test-images/agnhost:2.53
+              args: ["pause"]
+```
+
+In this example there will be 8 (2 ReplicatedJob instances with 4 
+"completions" each) worker pods and we say that we want to split
```

  ```suggestion
  "parallelism" each) worker pods and we say that we want to split
  ```
  
  Isn't this parallelism instead of completions?

- **keps/2724-topology-aware-scheduling/README.md:954** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T14:31:41Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  > It's a number of pods in PodSet. To make it more readable I have a few ideas how to rephrase it:
  > 
  > * PodSet count - consistent with the code I guess
  > * Pods count
  > * Pods count in PodSet
  > * Number of pods in PodSet
  > * What do you think suits the most?
  
  IIUC, is it calculated by `parallelism (Job) × count (PodSet)`? If yes, let's say `Pods count`.
  The PodSet count indicates `.spec.podSet[*].count`, I think.

- **keps/2724-topology-aware-scheduling/README.md:962** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T14:38:14Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
+| -----------------------| ----------- | - | - | - | - | - |
+| BestFit                | 12          | 6 | . | 4 | . | 2 |
+| MostFreeCapacity       | 12          | 6 | 2 | 4 | . | . |
+| LeastFreeCapacity      | 10          | . | 2 | 4 | 2 | 2 |
+
+Explanation:
+- `BestFit` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices. The last domain has been "optimized" to find the tight fit. 
+- `MostFreeCapacity` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices, so we assigned as much pods as possible there and filled it entirely.
```

  @lchrzaszcz I think the current algorithm looks like a mixed strategy between MostFree and LeastFree as we can see in your example: `| MostFreeCapacity       | 12          | 6 | 2 | 4 | . | . |`
  
  If we want to keep maintaining `MostFreeCapacity` strategy, we should not violate the strategy in case of Two-Level scheduling as well. However, as @mimowo mentioned in the above, he want to drop supporting the MostFreeCapacity strategy. So, Let's delete the strategy first before we implement the Two-Level scheduling feature.
  
  > Yes, and in fact I would like to drop MostFreeCapacity in 0.13. It was implemented first, but we quickly realized BestFit is better in all known cases, we introduced the MostFreeCapacity feature gate just as a bailout option in case there is a bug in BestFit. It is deprecated since 0.11. It makes like harder I would suggest sending a preparatory or follow up PR to drop it.
  > 
  > wdyt @tenzen-y ?
  > 
  > btw. for LeastFreeCapacity we would like to still maintain for a bit, because there might be use-cases for reducing fragmentation.
  
  @mimowo Yes, that makes sense. As we discussed in TAS profiles KEP, we should drop those step by step.
  Surely, we should remove the `MostFreeCapacity` TAS profile first.
  For `LeastFreeCapacity`, let us consider how to grow the maturity and support TAS profiles in the future issue and KEP.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T14:56:12Z

- **keps/2724-topology-aware-scheduling/README.md:954** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T14:56:12Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  If I'm not mistaken, PodSet count is `parallelism x replicas (ReplicatedJob)`, at least that's the whole PodSet.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-05T15:04:00Z

- **keps/2724-topology-aware-scheduling/README.md:954** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T15:04:00Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  In the case of JobSet, replicated Job replicas corresponds to the PodSet count.
  So, in case of JobSet, PodSet count (`.spec.podSet[*].count`) is replicatedJob replicas (`.spec.template.spec.replicatedJobs[*].replicas`).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T15:14:11Z

- **keps/2724-topology-aware-scheduling/README.md:954** by [@mimowo](https://github.com/mimowo) — 2025-06-05T15:14:11Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  I think @tenzen-t it is replicas count parallelism of a single Job

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T15:18:07Z

- **keps/2724-topology-aware-scheduling/README.md:954** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T15:18:06Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  I'm a new contributor, so I might be mistaken, but looking here https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobs/jobset/jobset_controller.go#L232, we multiply parallelism/completions and replicas

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-05T15:19:57Z

- **keps/2724-topology-aware-scheduling/README.md:954** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-05T15:19:57Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  Nevertheless, I suggest that I'll just rename that column to "Pods count", so it's more obvious. WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-05T15:24:36Z

- **keps/2724-topology-aware-scheduling/README.md:954** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T15:24:36Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  > I'm a new contributor, so I might be mistaken, but looking here https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobs/jobset/jobset_controller.go#L232, we multiply parallelism/completions and replicas
  
  yes, that's right. The above yaml place was not correct. Let me show those more accurately.
  I wanted to show in the following:
  
  - PodSet Count: `.spec.podSet[*].count` (Workload)
  - Pod Count: `.spec.replicatedJobs[*].replicas` (JobSet) × `.spec.replicatedJobs[*].template.spec.parallelism`
  - Pod Count for more generic: `.spec.podSet[*].count` (Workload) × `.spec.replicatedJobs[*].template.spec.parallelism`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-05T15:28:09Z

- **keps/2724-topology-aware-scheduling/README.md:954** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T15:28:08Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  > Nevertheless, I suggest that I'll just rename that column to "Pods count", so it's more obvious. WDYT?
  
  If this indicates my above definition, "Pods count" would be better.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-05T15:31:10Z

- **keps/2724-topology-aware-scheduling/README.md:962** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-05T15:31:10Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
+| -----------------------| ----------- | - | - | - | - | - |
+| BestFit                | 12          | 6 | . | 4 | . | 2 |
+| MostFreeCapacity       | 12          | 6 | 2 | 4 | . | . |
+| LeastFreeCapacity      | 10          | . | 2 | 4 | 2 | 2 |
+
+Explanation:
+- `BestFit` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices. The last domain has been "optimized" to find the tight fit. 
+- `MostFreeCapacity` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices, so we assigned as much pods as possible there and filled it entirely.
```

  @lchrzaszcz @mimowo I'm ok with refining `MostFreeCapacity` description when we delete the TAS profile.
  In other words, we can move all `MostFreeCapacity` descriptions to the Alternative section.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-06T12:10:21Z

- **keps/2724-topology-aware-scheduling/README.md:954** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-06T12:10:21Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
```

  Yup. Replaced it with `Pods count`

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-06-06T12:15:25Z

- **keps/2724-topology-aware-scheduling/README.md:962** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-06-06T12:15:25Z
```diff
@@ -762,6 +929,41 @@ We recommend the BestFit algorithm for most of use cases, however we give more f
 Based on the collected feedback we will introduce TAS configuration that would allow user to select the desired algorithm.
 Eventually we'll remove the feature as they will be no longer need when we implement API for TAS configuration.
 
+### Two-level scheduling
+In consideration of a [Story 5](#story-5) a two-level scheduling is introduced. 
+We introduce a notion of PodSet Slice, which is a subset of PodSet. All slices
+have the same size and evenly divide the PodSet. To simplify implementation we 
+are allowing only for a "required" topology for slice. The requested topology 
+level for slices has to be on the same or below level of the main topology.
+
+Requesting slice topology changes the way algorithm chooses domains. In all 
+modes in [Computing the assignment](#computing-the-assignment) algorithm greedily
+selects domains based on the free capacity (either most or least). However, with
+slices it considers the amount of slices that can fit into a domain as free 
+capacity, but if that number is equal then it prioritizes domains with fewer 
+remaining resources.
+
+#### Example
+Consider a rack with nodes that can accommodate 6, 5, 4, 3, and 2 pods respectively. 
+The required topology for slice is "host" and the podset slice size is 2.
+
+As an example let's analyze the assignment based on the size of a podset and selected 
+mode. See the table below where the numbers are pods assigned to a particular node.
+The header for columns dedicated to nodes correspond node's initial capacity.
+
+| mode                   | PodSet size | 6 | 5 | 4 | 3 | 2 |
+| -----------------------| ----------- | - | - | - | - | - |
+| BestFit                | 12          | 6 | . | 4 | . | 2 |
+| MostFreeCapacity       | 12          | 6 | 2 | 4 | . | . |
+| LeastFreeCapacity      | 10          | . | 2 | 4 | 2 | 2 |
+
+Explanation:
+- `BestFit` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices. The last domain has been "optimized" to find the tight fit. 
+- `MostFreeCapacity` - We prioritized 3rd node over 2nd node because 3rd node was a tight fit among all domains that could fit 2 slices, so we assigned as much pods as possible there and filled it entirely.
```

  I've removed `MostFreeCapacity` from two-level scheduling descriptions (this PR). In the other PR (https://github.com/kubernetes-sigs/kueue/pull/5536) I'm moving `MostFreeCapacity` documentation to `Alternatives` section.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-06T12:25:33Z

Awesome!
Thank you!
/lgtm
/approve

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T14:38:44Z

/lgtm
