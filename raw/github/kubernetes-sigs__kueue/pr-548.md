# PR #548: First pass - Documentation improvement

**Summary**: First pass - Documentation improvement

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/548

**Last updated**: 2023-02-13T17:05:30Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@rbarberop](https://github.com/rbarberop)
- **Branch**: `main` → `main`
- **Created**: 2023-02-03T18:14:57Z
- **Updated**: 2023-02-13T17:05:30Z
- **Merged**: 2023-02-13T17:05:30Z
- **Closed**: 2023-02-13T17:05:30Z
- **Labels**: `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 12   **+214 / -191**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Documentation changes to address the insights from UX study sessions: 
https://docs.google.com/document/d/1xbN46OLuhsXXHeqZrKrl9I57kpFQ2yqiYdOx0sHZC4Q/edit

#### High level changes:
- Changed order in 'Concepts' main page: ResourceFlavor first, to follow logical order of creation
- Separated ResourceFlavor section into it's own page, so it's more clean
- Added yaml example to LocalQueue page
- Used more descriptive names for Kueue objects in yaml and made them uniform between examples / pages

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-02-03T18:15:07Z

Hi @rbarberop. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T18:56:56Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:00:55Z

FYI @cortespao

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-02-07T19:37:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | caaff2de805ab751e1b5d104517c2ec2f645e955 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/63ea6bc04061e10008075c22 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-10T15:26:40Z

/approve
/label tide/merge-method-squash


Leaving the LGTM to @cortespao

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-02-13T16:23:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/548#issuecomment-1425966635" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/548#pullrequestreview-1295998973" title="Approved">cortespao</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/548#" title="Author self-approved">rbarberop</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-13T17:00:08Z

/lgtm
Thanks!

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-03T19:28:13Z

- **docs/concepts/README.md:10** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T18:58:22Z
```diff
@@ -5,6 +5,12 @@ abstractions that Kueue uses to represent your cluster and workloads.
 
 ## APIs
 
+### [Resource Flavor](resource_flavor.md)
+
+A kind or type of resource in a cluster. It could distinguish among different
```

  A kind or type of resource in a cluster, typically associated with the characteristics of a group of Nodes

- **docs/concepts/cluster_queue.md:5** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T18:59:54Z
```diff
@@ -2,7 +2,7 @@
 
 A ClusterQueue is a cluster-scoped object that governs a pool of resources
 such as CPU, memory, and hardware accelerators. A ClusterQueue defines:
-- The [resource _flavors_](#resourceflavor-object) that the ClusterQueue manages,
+- The [resource _flavors_](/docs/concepts/resource_flavor.md) that the ClusterQueue manages,
```

  ```suggestion
  - The quotas for the [resource _flavors_](/docs/concepts/resource_flavor.md) that the ClusterQueue manages,
  ```

- **docs/concepts/cluster_queue.md:7** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:00:27Z
```diff
@@ -2,7 +2,7 @@
 
 A ClusterQueue is a cluster-scoped object that governs a pool of resources
 such as CPU, memory, and hardware accelerators. A ClusterQueue defines:
-- The [resource _flavors_](#resourceflavor-object) that the ClusterQueue manages,
+- The [resource _flavors_](/docs/concepts/resource_flavor.md) that the ClusterQueue manages,
   with usage limits and order of consumption.
 - Fair sharing rules across the tenants of the cluster.
```

  across the multiple ClusterQueues in the cluster.

- **docs/concepts/cluster_queue.md:72** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:04:23Z
```diff
@@ -69,7 +69,7 @@ An example of a ClusterQueue with codependent resources looks like the following
 apiVersion: kueue.x-k8s.io/v1alpha2
 kind: ClusterQueue
 metadata:
-  name: cluster-total
+  name: cluster-queue
```

  I think we can keep this one. The name attempts to reflect that there is one just one cluster queue in this cluster. Otherwise, change the name in line 17, to keep names consistent.

- **docs/concepts/cluster_queue.md:253** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:05:36Z
```diff
@@ -334,4 +250,5 @@ borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
 ## What's next?
 
+- Create [local queues](/docs/concepts/local_queue.md)
```

  I would also add a link to Create resource flavors.
  Admins might choose to create flavors and cluster queues in any order. Better cross link.

- **docs/concepts/local_queue.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:06:55Z
```diff
@@ -4,14 +4,24 @@ A `LocalQueue` is a namespaced object that groups closely related workloads
 belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
 from which resources are allocated to run its workloads.
 
```

  A LocalQueue definition looks like the following:

- **docs/concepts/local_queue.md:13** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:08:52Z
```diff
@@ -4,14 +4,24 @@ A `LocalQueue` is a namespaced object that groups closely related workloads
 belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
 from which resources are allocated to run its workloads.
 
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: LocalQueue
+metadata:
+  namespace: team-a 
+  name: team-a-lq
+spec:
+  clusterQueue: cluster-queue 
+```
+
 Users submit jobs to a `LocalQueue`, instead of to a `ClusterQueue` directly.
 Tenants can discover which queues they can submit jobs to by listing the
 local queues in their namespace. The command is similar to the following:
 
 ```sh
-kubectl get -n my-namespace localqueues
-# Alternatively, use the alias `queue` or `queues`
```

  why not leave the aliases?

- **docs/concepts/local_queue.md:4** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:10:29Z
```diff
@@ -4,14 +4,24 @@ A `LocalQueue` is a namespaced object that groups closely related workloads
 belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
```

  ```suggestion
  belonging to a single namespace. A namespace is typically assigned to a tenant (team or user) of the organization. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
  ```

- **docs/concepts/local_queue.md:27** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:12:18Z
```diff
@@ -4,14 +4,24 @@ A `LocalQueue` is a namespaced object that groups closely related workloads
 belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
 from which resources are allocated to run its workloads.
 
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: LocalQueue
+metadata:
+  namespace: team-a 
+  name: team-a-lq
+spec:
+  clusterQueue: cluster-queue 
+```
+
 Users submit jobs to a `LocalQueue`, instead of to a `ClusterQueue` directly.
 Tenants can discover which queues they can submit jobs to by listing the
 local queues in their namespace. The command is similar to the following:
 
 ```sh
-kubectl get -n my-namespace localqueues
-# Alternatively, use the alias `queue` or `queues`
-kubectl get -n my-namespace queues
+kubectl get -n team-a localqueues
 ```
 
-`queue` and `queues` are aliases for `localqueue`.
+## What's next?
+
+- Launch a [workload](/docs/concepts/workload.md) against a local queue
```

  ```suggestion
  - Launch a [workload](/docs/concepts/workload.md) through a local queue
  ```
  ?

- **docs/concepts/resource_flavor.md:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:13:34Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
```

  ```suggestion
  # Resource Flavor
  ```
  
  for consistency with the other pages. Or maybe they should be titled `ClusterQueue` and `LocalQueue` instead? like  https://kubernetes.io/docs/concepts/workloads/controllers/statefulset/

- **docs/concepts/resource_flavor.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:14:45Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
```

  ```suggestion
  allows you to associate them with cluster Nodes through labels and taints.
  ```

- **docs/concepts/resource_flavor.md:30** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:15:45Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
```

  link to ClusterQueue

- **docs/concepts/resource_flavor.md:50** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:18:02Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
```

  Add:
  
  ResourceFlavors that don't match the node affinity of the Workload cannot be assigned to a Workload's podSet.
  
  We should probably add examples about this, but it could be a follow up.

- **docs/concepts/resource_flavor.md:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:19:45Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
+
+## ResourceFlavor taints
+
+To restrict the usage of a ResourceFlavor, you can configure the `.taints` field
+with taints.
```

  These taints should typically match the taints of the Nodes associated with this ResourceFlavor.

- **docs/concepts/resource_flavor.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:20:45Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
+
+## ResourceFlavor taints
+
+To restrict the usage of a ResourceFlavor, you can configure the `.taints` field
+with taints.
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
+workload should have a toleration for it. As opposed to the behavior for
+[ResourceFlavor labels](#resourceflavor-labels), Kueue does not add tolerations
+for the flavor taints.
+
+## Empty ResourceFlavor
+
+If your cluster has homogeneous resources, or if you don't need to manage
+quotas for the different flavors of a resource separately, you can create a
+ResourceFlavor without any labels or taints. Such ResourceFlavor is called an
+empty ResourceFlavor and its sample looks like the following:
```

  ```suggestion
  empty ResourceFlavor and its definition looks like the following:
  ```

- **docs/concepts/workload.md:26** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:22:44Z
```diff
@@ -21,9 +21,9 @@ apiVersion: kueue.x-k8s.io/v1alpha2
 kind: Workload
 metadata:
   name: sample-job
-  namespace: default
+  namespace: team-a
 spec:
-  queueName: user-queue
+  queueName: team-a-lq
```

  ```suggestion
    queueName: team-a-queue
  ```

- **docs/concepts/local_queue.md:12** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:23:02Z
```diff
@@ -4,14 +4,24 @@ A `LocalQueue` is a namespaced object that groups closely related workloads
 belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
 from which resources are allocated to run its workloads.
 
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: LocalQueue
+metadata:
+  namespace: team-a 
+  name: team-a-lq
```

  ```suggestion
    name: team-a-queue
  ```

- **docs/tasks/administer_cluster_quotas.md:36** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:23:35Z
```diff
@@ -33,30 +33,30 @@ cluster.
 Write the manifest for the ClusterQueue. It should look similar to the following:
 
 ```yaml
-# cluster-total.yaml
+# cluster-queue.yaml
```

  I think cluster-total made the most sense in this case (Single ClusterQueue and single ResourceFlavor setup)

- **docs/tasks/administer_cluster_quotas.md:84** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:24:06Z
```diff
@@ -81,7 +81,7 @@ Write the manifest for the ResourceFlavor. It should look similar to the followi
 apiVersion: kueue.x-k8s.io/v1alpha2
 kind: ResourceFlavor
 metadata:
-  name: default
+  name: default-flavor
```

  Update line 70

- **docs/tasks/administer_cluster_quotas.md:145** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:24:49Z
```diff
@@ -142,7 +142,7 @@ following:
 apiVersion: kueue.x-k8s.io/v1alpha2
 kind: ResourceFlavor
 metadata:
-  name: x86
+  name: x86-flavor
```

  if we add `-flavor` to everything, users might get the impression that it's necessary?
  
  I think we could leave it out for simplicity.

- **docs/tasks/administer_cluster_quotas.md:203** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:26:24Z
```diff
@@ -174,40 +174,40 @@ Write the manifest for the ClusterQueue that references the flavors. It should
 look similar to the following:
 
 ```yaml
-# cluster-total.yaml
+# cluster-queue.yaml
 apiVersion: kueue.x-k8s.io/v1alpha2
 kind: ClusterQueue
 metadata:
-  name: cluster-total
+  name: cluster-queue
 spec:
   namespaceSelector: {}
   resources:
   - name: "cpu"
     flavors:
-    - name: x86
+    - name: x86-flavor
       quota:
         min: 9
-    - name: arm
+    - name: arm-flavor
       quota:
         min: 12
   - name: "memory"
     flavors:
-    - name: default
+    - name: default-flavor
       quota:
         min: 84Gi
 ```
 
 The flavor names in the fields `.spec.resources[*].flavors[*].resourceFlavor`
 should match the names of the ResourceFlavors created earlier.
 
-Note that `memory` is referencing the `default` flavor created in the [single flavor setup.](#single-clusterqueue-and-single-resourceflavor-setup)
-This means that you don't want to distinguish if the memory is given from `x86`
-or `arm` nodes.
+Note that `memory` is referencing the `default-flavor` flavor created in the [single flavor setup.](#single-clusterqueue-and-single-resourceflavor-setup)
```

  Actually, we probably want to change this.
  
  I think I wrote it before we supported codependent resources https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/cluster_queue.md#codependent-resources
  
  typically cpu and memory are tied to the same flavors.

- **docs/tasks/run_jobs.md:23** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:27:06Z
```diff
@@ -19,15 +19,13 @@ Run the following command to list the Queues available in your namespace.
 
 ```shell
 kubectl -n default get localqueues
-# Or use the 'queues' alias.
-kubectl -n default get queues
```

  I think we can keep this. It's neat from an end-user perspective

- **docs/tasks/run_jobs.md:28** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T19:27:45Z
```diff
@@ -19,15 +19,13 @@ Run the following command to list the Queues available in your namespace.
 
 ```shell
 kubectl -n default get localqueues
-# Or use the 'queues' alias.
-kubectl -n default get queues
 ```
 
 The output is similar to the following:
 
 ```
 NAME   CLUSTERQUEUE    PENDING WORKLOADS
-main   cluster-total   3
+main   cluster-queue   3
```

  ```suggestion
  user-queue   cluster-queue   3
  ```
  
  ?

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2023-02-03T20:54:38Z

Leaving some suggestions 😃

- **docs/concepts/README.md:10** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:24:42Z
```diff
@@ -5,6 +5,12 @@ abstractions that Kueue uses to represent your cluster and workloads.
 
 ## APIs
 
+### [Resource Flavor](resource_flavor.md)
+
+A kind or type of resource in a cluster. It could distinguish among different
```

  Is this something the user can define?
  'A kind or type of resource that you can define in a cluster. Typically, this resource is associated with the characteristics of a group of Nodes'

- **docs/concepts/cluster_queue.md:46** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:25:31Z
```diff
@@ -43,7 +43,7 @@ In a ClusterQueue, you can define quotas for multiple [compute resources](https:
 
 For each resource, you can define quotas for multiple _flavors_.
 Flavors represent different variations of a resource (for example, different GPU
-models). A flavor is defined using a [ResourceFlavor object](#resourceflavor-object).
+models). A flavor is defined using a [ResourceFlavor object](/docs/concepts/resource_flavor.md).
```

  ```suggestion
  models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/resource_flavor.md).
  ```

- **docs/concepts/local_queue.md:4** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:31:34Z
```diff
@@ -4,14 +4,24 @@ A `LocalQueue` is a namespaced object that groups closely related workloads
 belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
```

  ```suggestion
  that belong to a single namespace (...)
  ```

- **docs/concepts/resource_flavor.md:4** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:32:33Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
```

  ```suggestion
  - pricing and availability (for example, spot versus on-demand VMs)
  ```

- **docs/concepts/resource_flavor.md:5** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:32:53Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
```

  ```suggestion
  - architecture (for example, x86 versus ARM CPUs)
  ```

- **docs/concepts/resource_flavor.md:6** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:33:06Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
```

  ```suggestion
  - brands and models (for example, Radeon 7000 versus Nvidia A100 vs T4 GPUs)
  ```

- **docs/concepts/resource_flavor.md:11** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:36:12Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
```

  ```suggestion following the initial paragraph terminology
  **Note**: If the resources in your cluster are homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
  ```

- **docs/concepts/resource_flavor.md:29** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:38:39Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
```

  ```suggestion
  You can use the `.metadata.name` field to reference a ResourceFlavor from a
  ```

- **docs/concepts/resource_flavor.md:39** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:41:02Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
```

  ```suggestion
  (or equivalent controllers), make sure the controller is configured to add those labels when
  ```
  
  I'm just assuming "it" refers to "the controller". I highly recommend you change "it" for the subject name to avoid confusion.

- **docs/concepts/resource_flavor.md:44** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:42:40Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
```

  ```suggestion
  To guarantee that the workload Pods run on the nodes associated to the flavor
  that Kueue selected, Kueue performs the following
  steps:
  ```

- **docs/concepts/resource_flavor.md:44** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:43:05Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
```

  This is just to make the sentence more concise

- **docs/concepts/resource_flavor.md:53** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:44:08Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
```

  ```suggestion
    `.nodeSelector` of the underlying workload Pod templates. This occurs if the workload
     didn't specify the ResourceFlavor labels already.
  ```

- **docs/concepts/resource_flavor.md:57** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:44:35Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
```

  ```suggestion
     guarantees that the workload's Pods can only be scheduled on the nodes
  ```

- **docs/concepts/resource_flavor.md:61** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:44:47Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
```

  ```suggestion
     targeted by the flavor that Kueue assigned to the workload.
  ```

- **docs/concepts/resource_flavor.md:66** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:48:32Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
+
+## ResourceFlavor taints
+
+To restrict the usage of a ResourceFlavor, you can configure the `.taints` field
+with taints.
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
```

  Maybe "allow" is better than "admit" in this context?

- **docs/concepts/resource_flavor.md:69** by [@cortespao](https://github.com/cortespao) — 2023-02-03T20:52:39Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
+
+## ResourceFlavor taints
+
+To restrict the usage of a ResourceFlavor, you can configure the `.taints` field
+with taints.
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
+workload should have a toleration for it. As opposed to the behavior for
+[ResourceFlavor labels](#resourceflavor-labels), Kueue does not add tolerations
+for the flavor taints.
```

  ```suggestion
  workload should have a toleration for it. In contrast, Kueue does not add tolerations
  for the flavor taints with [ResourceFlavor labels](#resourceflavor-labels).
  ```
  
  Please, disregard if my suggestion affects the accuracy.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-03T21:07:30Z

- **docs/concepts/resource_flavor.md:61** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T21:07:30Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
```

  We are using Camelcase for API objects

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-03T21:08:27Z

- **docs/concepts/resource_flavor.md:66** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T21:08:26Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
+
+## ResourceFlavor taints
+
+To restrict the usage of a ResourceFlavor, you can configure the `.taints` field
+with taints.
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
```

  There are two related words:
  Admission and assignment. During admission, a workload is assigned flavors.
  Not sure how to rephrase the sentence, though.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-03T21:09:32Z

- **docs/concepts/resource_flavor.md:69** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T21:09:32Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
+
+## ResourceFlavor taints
+
+To restrict the usage of a ResourceFlavor, you can configure the `.taints` field
+with taints.
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
+workload should have a toleration for it. As opposed to the behavior for
+[ResourceFlavor labels](#resourceflavor-labels), Kueue does not add tolerations
+for the flavor taints.
```

  Maybe we can remove the mention of labels here.

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-07T18:40:36Z

- **docs/concepts/cluster_queue.md:72** by [@rbarberop](https://github.com/rbarberop) — 2023-02-07T18:40:35Z
```diff
@@ -69,7 +69,7 @@ An example of a ClusterQueue with codependent resources looks like the following
 apiVersion: kueue.x-k8s.io/v1alpha2
 kind: ClusterQueue
 metadata:
-  name: cluster-total
+  name: cluster-queue
```

  I'll change the name in line 17... I prefer the more descriptive name.

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-07T19:01:50Z

- **docs/concepts/local_queue.md:13** by [@rbarberop](https://github.com/rbarberop) — 2023-02-07T19:01:49Z
```diff
@@ -4,14 +4,24 @@ A `LocalQueue` is a namespaced object that groups closely related workloads
 belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
 from which resources are allocated to run its workloads.
 
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: LocalQueue
+metadata:
+  namespace: team-a 
+  name: team-a-lq
+spec:
+  clusterQueue: cluster-queue 
+```
+
 Users submit jobs to a `LocalQueue`, instead of to a `ClusterQueue` directly.
 Tenants can discover which queues they can submit jobs to by listing the
 local queues in their namespace. The command is similar to the following:
 
 ```sh
-kubectl get -n my-namespace localqueues
-# Alternatively, use the alias `queue` or `queues`
```

  I read this in the UX document:
  
  > Limited information or stale names (the old Queue name is still present in some instances).
  
  And I thought it referred to this... Should I keep it then?

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-07T19:30:24Z

- **docs/tasks/administer_cluster_quotas.md:36** by [@rbarberop](https://github.com/rbarberop) — 2023-02-07T19:30:23Z
```diff
@@ -33,30 +33,30 @@ cluster.
 Write the manifest for the ClusterQueue. It should look similar to the following:
 
 ```yaml
-# cluster-total.yaml
+# cluster-queue.yaml
```

  I'd like to use the same names as much as possible in the documentation, so someone following it and copy-pasting the examples doesn't need to change much when jumping from one page to another

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-07T19:33:33Z

- **docs/tasks/administer_cluster_quotas.md:145** by [@rbarberop](https://github.com/rbarberop) — 2023-02-07T19:33:33Z
```diff
@@ -142,7 +142,7 @@ following:
 apiVersion: kueue.x-k8s.io/v1alpha2
 kind: ResourceFlavor
 metadata:
-  name: x86
+  name: x86-flavor
```

  You're right... and in this case it's clear the difference... I'd still keep default-flavor so it matches the rest of the pages.

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-07T19:43:34Z

- **docs/tasks/run_jobs.md:23** by [@rbarberop](https://github.com/rbarberop) — 2023-02-07T19:43:33Z
```diff
@@ -19,15 +19,13 @@ Run the following command to list the Queues available in your namespace.
 
 ```shell
 kubectl -n default get localqueues
-# Or use the 'queues' alias.
-kubectl -n default get queues
```

  Same case as in here
  https://github.com/kubernetes-sigs/kueue/pull/548#discussion_r1099080107

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-07T19:45:44Z

- **docs/concepts/resource_flavor.md:61** by [@rbarberop](https://github.com/rbarberop) — 2023-02-07T19:45:44Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
```

  Ops... I might have made lowercase all 'workloads'... let me know if I should recapitalize

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-07T19:55:20Z

- **docs/concepts/resource_flavor.md:39** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-03T21:10:40Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
```

  "cluster autoscaler" again?

- **docs/concepts/local_queue.md:13** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-07T19:54:59Z
```diff
@@ -4,14 +4,24 @@ A `LocalQueue` is a namespaced object that groups closely related workloads
 belonging to a single tenant. A `LocalQueue` points to one [`ClusterQueue`](cluster_queue.md)
 from which resources are allocated to run its workloads.
 
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: LocalQueue
+metadata:
+  namespace: team-a 
+  name: team-a-lq
+spec:
+  clusterQueue: cluster-queue 
+```
+
 Users submit jobs to a `LocalQueue`, instead of to a `ClusterQueue` directly.
 Tenants can discover which queues they can submit jobs to by listing the
 local queues in their namespace. The command is similar to the following:
 
 ```sh
-kubectl get -n my-namespace localqueues
-# Alternatively, use the alias `queue` or `queues`
```

  no, it doesn't refer to this command.
  
  It might refer to mentions of `Queue` (with capital letter) instead of `LocalQueue`, like this one:
  
  https://github.com/kubernetes-sigs/kueue/blob/main/docs/tasks/run_jobs.md#0-identify-the-queues-available-in-your-namespace

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-07T19:56:44Z

- **docs/concepts/resource_flavor.md:61** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-07T19:56:43Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
```

  When referring to API objects, the kubernetes documentation uses CamelCase.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-08T09:23:22Z

- **docs/concepts/README.md:13** by [@kerthcet](https://github.com/kerthcet) — 2023-02-08T09:18:19Z
```diff
@@ -5,6 +5,13 @@ abstractions that Kueue uses to represent your cluster and workloads.
 
 ## APIs
 
+### [Resource Flavor](resource_flavor.md)
+
+A kind or type of resource that you can define in a cluster. Typically,
+this resource is associated with the characteristics of a group of Nodes. 
+It could distinguish among different characteristics of resources such as 
+availability, pricing, architecture, models, etc.
```

  If we can have examples, it's better, like pricing (on-demand vs spot)

- **docs/concepts/resource_flavor.md:42** by [@kerthcet](https://github.com/kerthcet) — 2023-02-08T09:22:08Z
```diff
@@ -0,0 +1,92 @@
+# ResourceFlavor
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (for example, spot verus on-demand VMs)
+- architecture (for example, x86 versus ARM CPUs)
+- brands and models (for example, Radeon 7000 versus Nvidia A100 versus T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with cluster nodes through labels and taints.
+
+**Note**: If your the resources in your cluster are homogeneous, you can use 
+an [empty ResourceFlavor](#empty-resourceflavor) instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` field to reference a ResourceFlavor from a
+[ClusterQueue](/docs/concepts/cluster_queue.md) in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure that the controller is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
```

  Maybe we can uppercase workload to `Workload` across the document to mark it a special Object, just like ResourceFlavor and ClusterQueue.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-08T09:24:35Z

- **docs/concepts/README.md:13** by [@kerthcet](https://github.com/kerthcet) — 2023-02-08T09:24:31Z
```diff
@@ -5,6 +5,13 @@ abstractions that Kueue uses to represent your cluster and workloads.
 
 ## APIs
 
+### [Resource Flavor](resource_flavor.md)
+
+A kind or type of resource that you can define in a cluster. Typically,
+this resource is associated with the characteristics of a group of Nodes. 
+It could distinguish among different characteristics of resources such as 
+availability, pricing, architecture, models, etc.
```

  Or you list them in the subpage, forget it. Sorry.

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-08T21:35:05Z

- **docs/concepts/resource_flavor.md:42** by [@rbarberop](https://github.com/rbarberop) — 2023-02-08T21:35:04Z
```diff
@@ -0,0 +1,92 @@
+# ResourceFlavor
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (for example, spot verus on-demand VMs)
+- architecture (for example, x86 versus ARM CPUs)
+- brands and models (for example, Radeon 7000 versus Nvidia A100 versus T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with cluster nodes through labels and taints.
+
+**Note**: If your the resources in your cluster are homogeneous, you can use 
+an [empty ResourceFlavor](#empty-resourceflavor) instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` field to reference a ResourceFlavor from a
+[ClusterQueue](/docs/concepts/cluster_queue.md) in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure that the controller is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
```

  Done

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-08T21:35:19Z

- **docs/concepts/resource_flavor.md:61** by [@rbarberop](https://github.com/rbarberop) — 2023-02-08T21:35:19Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
```

  Done... but might need some review... I have decided that most of the times we are talking about the API object, even when talking in plural... but I left a few exceptions, where I considered it was talking about 'workload' as a generic term.

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-08T21:37:02Z

- **docs/tasks/administer_cluster_quotas.md:203** by [@rbarberop](https://github.com/rbarberop) — 2023-02-08T21:37:02Z
```diff
@@ -174,40 +174,40 @@ Write the manifest for the ClusterQueue that references the flavors. It should
 look similar to the following:
 
 ```yaml
-# cluster-total.yaml
+# cluster-queue.yaml
 apiVersion: kueue.x-k8s.io/v1alpha2
 kind: ClusterQueue
 metadata:
-  name: cluster-total
+  name: cluster-queue
 spec:
   namespaceSelector: {}
   resources:
   - name: "cpu"
     flavors:
-    - name: x86
+    - name: x86-flavor
       quota:
         min: 9
-    - name: arm
+    - name: arm-flavor
       quota:
         min: 12
   - name: "memory"
     flavors:
-    - name: default
+    - name: default-flavor
       quota:
         min: 84Gi
 ```
 
 The flavor names in the fields `.spec.resources[*].flavors[*].resourceFlavor`
 should match the names of the ResourceFlavors created earlier.
 
-Note that `memory` is referencing the `default` flavor created in the [single flavor setup.](#single-clusterqueue-and-single-resourceflavor-setup)
-This means that you don't want to distinguish if the memory is given from `x86`
-or `arm` nodes.
+Note that `memory` is referencing the `default-flavor` flavor created in the [single flavor setup.](#single-clusterqueue-and-single-resourceflavor-setup)
```

  @alculquicondor can you clarify this, please?

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-09T17:35:34Z

- **docs/concepts/resource_flavor.md:61** by [@rbarberop](https://github.com/rbarberop) — 2023-02-09T17:35:34Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
```

  @cortespao could you do a quick pass? I just reviewed and tried to differentiate between Workload and workload... It's in the last commit.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-09T21:25:44Z

- **docs/tasks/administer_cluster_quotas.md:203** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-09T21:25:43Z
```diff
@@ -174,40 +174,40 @@ Write the manifest for the ClusterQueue that references the flavors. It should
 look similar to the following:
 
 ```yaml
-# cluster-total.yaml
+# cluster-queue.yaml
 apiVersion: kueue.x-k8s.io/v1alpha2
 kind: ClusterQueue
 metadata:
-  name: cluster-total
+  name: cluster-queue
 spec:
   namespaceSelector: {}
   resources:
   - name: "cpu"
     flavors:
-    - name: x86
+    - name: x86-flavor
       quota:
         min: 9
-    - name: arm
+    - name: arm-flavor
       quota:
         min: 12
   - name: "memory"
     flavors:
-    - name: default
+    - name: default-flavor
       quota:
         min: 84Gi
 ```
 
 The flavor names in the fields `.spec.resources[*].flavors[*].resourceFlavor`
 should match the names of the ResourceFlavors created earlier.
 
-Note that `memory` is referencing the `default` flavor created in the [single flavor setup.](#single-clusterqueue-and-single-resourceflavor-setup)
-This means that you don't want to distinguish if the memory is given from `x86`
-or `arm` nodes.
+Note that `memory` is referencing the `default-flavor` flavor created in the [single flavor setup.](#single-clusterqueue-and-single-resourceflavor-setup)
```

  So before codependent resources were supported, the example that I put avoided having flavors for memory.
  
  Now that they are supported, it's better to just put the same flavors for memory as cpu, because in real-world clusters, CPU and memory are usually resources tied to machine types or spot/on-demand.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-09T21:41:16Z

- **docs/concepts/README.md:10** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-09T21:34:10Z
```diff
@@ -7,9 +7,10 @@ abstractions that Kueue uses to represent your cluster and workloads.
 
 ### [Resource Flavor](resource_flavor.md)
 
-A kind or type of resource in a cluster. It could distinguish among different
-characteristics of resources such as availability, pricing, architecture,
-models, etc.
+A kind or type of resource that you can define in a cluster. Typically,
```

  `that is available in a cluster`
  
  Or `that you can define to describe what is available in the cluster`?

- **docs/concepts/resource_flavor.md:52** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-09T21:37:14Z
```diff
@@ -36,31 +36,36 @@ ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
 To associate a ResourceFlavor with a subset of nodes of your cluster, you can
 configure the `.nodeSelector` field with matching node labels that uniquely identify
 the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
-(or equivalent controllers), make sure it is configured to add those labels when
+(or equivalent controllers), make sure that the controller is configured to add those labels when
 adding new nodes.
 
 To guarantee that the workload Pods run on the nodes associated to the flavor
-that Kueue decided that the workload should use, Kueue performs the following
+that Kueue selected, Kueue performs the following
 steps:
 
 1. When admitting a workload, Kueue evaluates the
    [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
    and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
    fields in the PodSpecs of your [Workload](workload.md) against the
    ResourceFlavor labels.
+
```

  same paragraph should be good, so remove this empty line.

- **docs/concepts/resource_flavor.md:58** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-09T21:37:44Z
```diff
@@ -36,31 +36,36 @@ ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
 To associate a ResourceFlavor with a subset of nodes of your cluster, you can
 configure the `.nodeSelector` field with matching node labels that uniquely identify
 the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
-(or equivalent controllers), make sure it is configured to add those labels when
+(or equivalent controllers), make sure that the controller is configured to add those labels when
 adding new nodes.
 
 To guarantee that the workload Pods run on the nodes associated to the flavor
-that Kueue decided that the workload should use, Kueue performs the following
+that Kueue selected, Kueue performs the following
 steps:
 
 1. When admitting a workload, Kueue evaluates the
    [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
    and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
    fields in the PodSpecs of your [Workload](workload.md) against the
    ResourceFlavor labels.
+
+   `ResourceFlavors` that don't match the node affinity of the workload
+   cannot be assigned to a workload's podSet.
+
+
 2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
-  `.nodeSelector` of the underlying workload Pod templates, if the workload
-   didn't specify them already.
+  `.nodeSelector` of the underlying workload Pod templates. This occurs if the workload
+   didn't specify the `ResourceFlavor` labels already.
```

  as part of its nodeSelector.

- **docs/concepts/resource_flavor.md:68** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-09T21:38:21Z
```diff
@@ -36,31 +36,36 @@ ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
 To associate a ResourceFlavor with a subset of nodes of your cluster, you can
 configure the `.nodeSelector` field with matching node labels that uniquely identify
 the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
-(or equivalent controllers), make sure it is configured to add those labels when
+(or equivalent controllers), make sure that the controller is configured to add those labels when
 adding new nodes.
 
 To guarantee that the workload Pods run on the nodes associated to the flavor
-that Kueue decided that the workload should use, Kueue performs the following
+that Kueue selected, Kueue performs the following
 steps:
 
 1. When admitting a workload, Kueue evaluates the
    [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
    and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
    fields in the PodSpecs of your [Workload](workload.md) against the
    ResourceFlavor labels.
+
+   `ResourceFlavors` that don't match the node affinity of the workload
+   cannot be assigned to a workload's podSet.
+
+
 2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
-  `.nodeSelector` of the underlying workload Pod templates, if the workload
-   didn't specify them already.
+  `.nodeSelector` of the underlying workload Pod templates. This occurs if the workload
+   didn't specify the `ResourceFlavor` labels already.
 
    For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
    Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
-   guarantees that the Workload's Pods can only be scheduled on the nodes
-   targeted by the flavor that Kueue assigned to the Workload.
+   guarantees that the workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the workload.
 
 ## ResourceFlavor taints
 
 To restrict the usage of a ResourceFlavor, you can configure the `.taints` field
-with taints.
+with taints. These taints should typically match the taints of the Nodes associated with ResourceFlavor.
```

  the* ResourceFlavor

- **docs/concepts/cluster_queue.md:49** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-09T21:40:20Z
```diff
@@ -46,7 +46,7 @@ Flavors represent different variations of a resource (for example, different GPU
 models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/resource_flavor.md).
 
 In a process called [admission](README.md#admission), Kueue assigns to the
-[Workload pod sets](workload.md#pod-sets) a flavor for each resource the pod set
+[Workload Pod Sets](workload.md#pod-sets) a flavor for each resource the pod set
```

  PodSet is not a top level object, so you can say pod sets or `podSets`

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-10T15:18:43Z

- **docs/concepts/README.md:10** by [@rbarberop](https://github.com/rbarberop) — 2023-02-10T15:18:43Z
```diff
@@ -7,9 +7,10 @@ abstractions that Kueue uses to represent your cluster and workloads.
 
 ### [Resource Flavor](resource_flavor.md)
 
-A kind or type of resource in a cluster. It could distinguish among different
-characteristics of resources such as availability, pricing, architecture,
-models, etc.
+A kind or type of resource that you can define in a cluster. Typically,
```

  Changed the wording a bit, following also the definition under resourceflavor.md 
  
  Let me know what you think.

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-13T16:10:17Z

- **docs/concepts/resource_flavor.md:66** by [@rbarberop](https://github.com/rbarberop) — 2023-02-13T16:10:17Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
+
+## ResourceFlavor taints
+
+To restrict the usage of a ResourceFlavor, you can configure the `.taints` field
+with taints.
+
+Taints on the ResourceFlavor work similarly to [node taints](https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/).
+For Kueue to admit a workload to use the ResourceFlavor, the PodSpecs in the
```

  I have added a link from 'admit' to the definition of 'Admission' in the main README. 
  Hope that makes things more clear.

### Review by [@cortespao](https://github.com/cortespao) (approved) — 2023-02-13T16:23:10Z

LGTM. Thanks!

- **docs/concepts/resource_flavor.md:42** by [@cortespao](https://github.com/cortespao) — 2023-02-13T16:20:28Z
```diff
@@ -0,0 +1,91 @@
+# ResourceFlavor
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (for example, spot verus on-demand VMs)
+- architecture (for example, x86 versus ARM CPUs)
+- brands and models (for example, Radeon 7000 versus Nvidia A100 versus T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with cluster nodes through labels and taints.
+
+**Note**: If the resources in your cluster are homogeneous, you can use 
+an [empty ResourceFlavor](#empty-resourceflavor) instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` field to reference a ResourceFlavor from a
+[ClusterQueue](/docs/concepts/cluster_queue.md) in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure that the controller is configured to add those labels when
+adding new nodes.
+
+To guarantee that the Workload's Pods run on the nodes associated to the flavor
```

  Maybe 'Workload's Pods' is hard to understand. Does this mean "Pods assigned to the Workload" or "Pods in the Workload"?
  Maybe it's worth expanding the terms a bit here.

- **docs/concepts/resource_flavor.md:61** by [@cortespao](https://github.com/cortespao) — 2023-02-13T16:21:15Z
```diff
@@ -0,0 +1,87 @@
+# ResourceFlavor object
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (ex: spot vs on-demand VMs)
+- architecture (ex: x86 vs ARM CPUs)
+- brands and models (ex: Radeon 7000 vs Nvidia A100 vs T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with node labels and taints.
+
+**Note**: If your cluster is homogeneous, you can use an [empty ResourceFlavor](#empty-resourceflavor)
+instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` to reference a ResourceFlavor from a
+ClusterQueue in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure it is configured to add those labels when
+adding new nodes.
+
+To guarantee that the workload Pods run on the nodes associated to the flavor
+that Kueue decided that the workload should use, Kueue performs the following
+steps:
+
+1. When admitting a workload, Kueue evaluates the
+   [`.nodeSelector`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector)
+   and [`.affinity.nodeAffinity.requiredDuringSchedulingIgnoredDuringExecution`](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#node-affinity)
+   fields in the PodSpecs of your [Workload](workload.md) against the
+   ResourceFlavor labels.
+2. Once the workload is admitted, Kueue adds the ResourceFlavor labels to the
+  `.nodeSelector` of the underlying workload Pod templates, if the workload
+   didn't specify them already.
+
+   For example, for a [batch/v1.Job](https://kubernetes.io/docs/concepts/workloads/controllers/job/),
+   Kueue adds the labels to the `.spec.template.spec.nodeSelector` field. This
+   guarantees that the Workload's Pods can only be scheduled on the nodes
+   targeted by the flavor that Kueue assigned to the Workload.
```

  I did a quick pass and all seems right. Thanks, Roberto 😄

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-13T16:52:07Z

- **docs/concepts/resource_flavor.md:42** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-13T16:52:07Z
```diff
@@ -0,0 +1,91 @@
+# ResourceFlavor
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (for example, spot verus on-demand VMs)
+- architecture (for example, x86 versus ARM CPUs)
+- brands and models (for example, Radeon 7000 versus Nvidia A100 versus T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with cluster nodes through labels and taints.
+
+**Note**: If the resources in your cluster are homogeneous, you can use 
+an [empty ResourceFlavor](#empty-resourceflavor) instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` field to reference a ResourceFlavor from a
+[ClusterQueue](/docs/concepts/cluster_queue.md) in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure that the controller is configured to add those labels when
+adding new nodes.
+
+To guarantee that the Workload's Pods run on the nodes associated to the flavor
```

  Pods in the workload. Maybe we can add a link to Workload.

### Review by [@rbarberop](https://github.com/rbarberop) (commented) — 2023-02-13T16:56:36Z

- **docs/concepts/resource_flavor.md:42** by [@rbarberop](https://github.com/rbarberop) — 2023-02-13T16:56:36Z
```diff
@@ -0,0 +1,91 @@
+# ResourceFlavor
+
+Resources in a cluster are typically not homogeneous. Resources could differ in:
+- pricing and availability (for example, spot verus on-demand VMs)
+- architecture (for example, x86 versus ARM CPUs)
+- brands and models (for example, Radeon 7000 versus Nvidia A100 versus T4 GPUs)
+
+A ResourceFlavor is an object that represents these resource variations and
+allows you to associate them with cluster nodes through labels and taints.
+
+**Note**: If the resources in your cluster are homogeneous, you can use 
+an [empty ResourceFlavor](#empty-resourceflavor) instead of adding labels to custom ResourceFlavors.
+
+A sample ResourceFlavor looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha2
+kind: ResourceFlavor
+metadata:
+  name: spot
+nodeSelector:
+  instance-type: spot
+taints:
+- effect: NoSchedule
+  key: spot
+  value: "true"
+```
+
+You can use the `.metadata.name` field to reference a ResourceFlavor from a
+[ClusterQueue](/docs/concepts/cluster_queue.md) in the `.spec.resources[*].flavors[*].name` field.
+
+## ResourceFlavor labels
+
+**Requires Kubernetes 1.23 or newer**
+
+To associate a ResourceFlavor with a subset of nodes of your cluster, you can
+configure the `.nodeSelector` field with matching node labels that uniquely identify
+the nodes. If you are using [cluster autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler)
+(or equivalent controllers), make sure that the controller is configured to add those labels when
+adding new nodes.
+
+To guarantee that the Workload's Pods run on the nodes associated to the flavor
```

  Done
