# PR #5510: Support Elastic Jobs via WorkloadSlices

**Summary**: Support Elastic Jobs via WorkloadSlices

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5510

**Last updated**: 2025-07-23T15:09:04Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ichekrygin](https://github.com/ichekrygin)
- **Branch**: `wl-slices` → `main`
- **Created**: 2025-06-05T05:33:41Z
- **Updated**: 2025-07-23T15:09:04Z
- **Merged**: 2025-07-23T07:16:29Z
- **Closed**: 2025-07-23T07:16:29Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 22   **+3028 / -50**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

This PR introduces the foundational implementation of WorkloadSlices in Kueue, as proposed in [KEP-77](https://github.com/kubernetes/enhancements/pull/XYZ). WorkloadSlices enable controlled scaling of admitted workloads (e.g., scale-up) while preserving Kueue's scheduling guarantees and resource tracking semantics.

📌 Summary
- Introduces WorkloadSlice concept as a transient workload object representing a logical scale-up request.
- Enables mutable workload behavior using a dual-Workload model:
  - Original admitted workload.
  - A new WorkloadSlice with additional requested capacity (1:2 state during transition).
- Admission of the new WorkloadSlice triggers preemption of the original Workload, even if additional capacity is available, to enforce consistent admission-state transitions.
- Uses Pod scheduling gates (instead of spec.suspend) for gating new pods until the slice is admitted.
- Defaulting logic enables the feature automatically for supported jobs (e.g., batchv1.Job, RayJob).
- Ensures all new pods created during the transition are gated until the corresponding Workload is admitted.
- Aggregates admission state and lifecycle management into the core Workload controller flow.

📎 Additional Notes
- Fully backward-compatible with existing single-Workload flow.
- Includes tests for:
  - Slice creation logic.
  - Admission/preemption interaction.
  - Scheduling gate behavior.
- Documentation and KEP link updates to follow in separate PR.

⚠️ Known Limitations
- Multi-cluster support for WorkloadSlices is still a work in progress and will be addressed in this or follow-up PRs.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5528

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Support for Elastic (Dynamically Sized Jobs) in Alpha as designed in [KEP-77](https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs). 
The implementation supports resizing (scale up and down) of batch/v1.Job and is behind the Alpha 
`ElasticJobsViaWorkloadSlices` feature gate. Jobs which are subject to resizing need to have the
`kueue.x-k8s.io/elastic-job` annotation added at creation time.
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2025-06-05T05:33:44Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: ichekrygin / name: Illya Chekrygin  (4e08884aeb7b41570a86721f3fd77ed515564148, 143d5c502a1ae33f028bd4ab0abc13b74fa96bcf, f63aa55be1867809eb9c78485f0e151a9f40d7a6, fdf77174c843be8816874d5fa9de0b2cf80d1e2d, d9255a2c3e4184995becf1e59dbb01876691aaed, fc49d7acaef9fbd511625e2279177c8d968b14b9, dd4ce880e547e76688a279af96d3df1829842551, 99e54230a14d479ac3ee4655062b7c08563e6e4a, f2869e420a026d1cbddaba082e8a25c9a9dc0583, d8e115fde460f8c5bbc2c0831502e0fa8d37f08f, 9bbccd3addf850cf6f1358565f1e12f36316c514, 0313da532bedfc9a45c2f0480d1d79e9308fd2e3, cabcd8e169535f6fafec72a79e778cff86088f3d, dafd141470cecab6904e08bb9a833cb15e182dcc, 4494d7853f9988640fa3a8d85c6eea1a5bbfa173, fe51aed7019d7c769c96070cf6fe3e1c1ea1981a, df97d77d3bb435207c320c9fa59cb71f6adf791a, de5aaa5cc7d7b94544d9e3cbeb421c6eb2d221e7, ebb7b69164b1b134254aa15bce08f3b3c8535404, c0e72efa2e49052510ad745da425bfcb6cec06bb, c60cfdb8fdd99062ca7d3b2992d3bceb6deb3cfb, af2da4d458e3efa1ec2ac114b2ecd20c190b3bf3, 870705898e4e996891afc82aa300379f8260c150, 11847419c0a9fe2ad73105bac65f6077c4e49808, 75c4f507ebfdc4b8bdc0a23fb0eaacf535b39c68, 0241eb73cf0d63292b8cec0e7507117adf7398ad, 7bf19d61c7cb14646ab8760f5146496a369e6805, 9f1096a0191424824a0134c8eccfff46c1271c8b, 76e5216474fee3a93d5dca27fee0cd29e61b9f56, f1d44095fcd85f0c1c394c6a32faf5cdbeccab30, 0509da1a08f9d406b5a8f79bc59bdb2f4027795a, 5ecbc024a32fdf63ee985bd88b729a75572d31e6, d8164cc56a2e17653d3e1139a9b55723cacf5225, 29e607c8685640418789aa79b2b2ba4bec953e2e, 922a8a03641d71228a5a2cd54029a2c69c57ab0a, 3afbbf4aebb38f547a15062b0f9dcec4b6a9f5a8, 083e71ca42c511d04d90f3d35c840237d0a52795, dcf49353764c4dba5a8923838d188055ddefc32a, a29432a3c7d34fd5282f74bf7f6a8893a9f06f9f, af552b375b29c30df29c7d6b63cf1f3786d1de89, aa2aefd5a4ebe29572b3957a596ab0ede8c8f752)</li></ul><!-- Date Modified: 2025-07-23 06:09:27.411554 -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-05T05:33:50Z

Hi @ichekrygin. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-05T05:34:13Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f1d44095fcd85f0c1c394c6a32faf5cdbeccab30 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68807c94966dc0000839ef27 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-27T18:29:33Z

/ok-to-test

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-07T19:03:23Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-08T17:53:42Z

/retitle Support Elastic Jobs via WorkloadSlices
Just to align with the recent naming in the KEP.

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T22:55:39Z

/retest

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-10T04:46:33Z

@mimowo, thank you for the detailed and insightful feedback. I reviewed, remedied, and/or replies to all your comments. PTAL when you get a moment.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-11T06:08:10Z

Thank you @ichekrygin Im pretty confident about the PR as for alpha. Going forward I have a couple of thoughts we may tackle in future iterations, or even the first if you have time:
1. what happens if the old workload is preempted by another workload. Then we will end up with two Pending workloads. I'm wondering if it would be better to mark the old as Finished in this case.
2. Would the  Scale Down support work day one for all other Job types like RayClauser or JobSet?
3. I would like to reverse control of unsuspending the pods by a dedicated controller. It would observe Workloads and then for an admitted workload find all the associated pods and ungate a given number of them. This could work for arbitrary Job types. Otherwise we need a lot of integration specific code. A similar problem is solved in TopologyUngater. I'm happy to drive the work on commonizing the approaches.

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-12T19:31:32Z

> What happens if the old workload is preempted by another workload? Then we’ll end up with two Pending workloads. I’m wondering if it would be better to mark the old one as Finished in this case.

Good question. The old workload slice shows up as a preemption target because of how flavor assignment works. And yeah, it’s possible that multiple workloads being scheduled could try to preempt the same old slice.

That said, [Kueue already handles overlapping preemption targets](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/scheduler.go#L259). If a workload’s preemption target was already finalized earlier in the same scheduling cycle, that workload just gets skipped, whether it’s a new slice or something else.

So if the old slice gets evicted by some workload *other than* the new one, the new slice will get skipped too, leaving it pending with the new (scaled-up) definition but not admitted. And the reverse is also true, if the new slice evicts the old one, any other workloads that were planning to preempt the old slice will get skipped.

This keeps us from ending up with multiple workloads depending on the same preemption target, and avoids running into conflicting Pending states.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-18T10:37:38Z

I looked at https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/5510/pull-kueue-test-integration-baseline-main/1946075894124646400 at it seems like an urelated flake. Note that the integration test does not enable the new feature gate. 

Let me retry, if confirmed, I will open an issue
/test pull-kueue-test-integration-baseline-main

Opened: https://github.com/kubernetes-sigs/kueue/issues/6018

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:59:06Z

@ichekrygin I think this is very close to be mergable. I left a bunch of comments, mostly renames to use "replacing" terminology consistently, rather than preemptions, because the mechanism only marginally relies on preemptions.

it would also be great to add integration tests for the happy path. The release is on Friday, so we still have a bit of time to address the comments I think.

Feel free to also squash the commits. There are 33 of them, I highly doubt anyone would like to be traversing them :)

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-22T07:22:09Z

LGTM, but please address the remaining comments

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-23T07:11:13Z

Let's make the note a bit more user-oriented, I think the workload-slices replacement is more of a technical detail. Putting a link to KEP77 is probably enough for interested readers.
/release-note-edit
```release-note
Support for Elastic (Dynamically Sized Jobs) in Alpha as designed in [KEP-77](https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs). 
The implementation supports resizing (scale up and down) of batch/v1.Job and is behind the Alpha 
`ElasticJobsViaWorkloadSlices` feature gate. Jobs which are subject to resizing need to have the
`kueue.x-k8s.io/elastic-job` annotation added at creation time.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-23T07:15:46Z

/lgtm
/approev
Thank you for your relentless work on [KEP-77](https://github.com/kubernetes-sigs/kueue/issues/77) and this implementation PR. This is one of the oldest and most anticipated KEPs in Kueue. While we still have a long way to go (e.g., support for other Job CRDs, MultiKueue, TAS), this is a huge milestone, and I'm very happy to get this in.

FYI @tenzen-y: Since the release is approaching and all of my comments have been addressed, I am merging this now to avoid potential conflicts with other PRs. I've taken extra care to ensure all new code is behind the alpha feature gate. Please feel free to add any further comments or open a new issue for follow-up items. I'm confident we can address them.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-23T07:15:53Z

LGTM label has been added.  <details>Git tree hash: b632836c18466b71df5bac3e1328c769844678ef</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-23T07:15:56Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-23T07:16:10Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5510#" title="Author self-approved">ichekrygin</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3106190245" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-11T04:58:45Z

- **pkg/controller/jobframework/workload_names.go:36** by [@mimowo](https://github.com/mimowo) — 2025-06-11T04:58:45Z
```diff
@@ -20,38 +20,93 @@ import (
 	"crypto/sha1"
 	"encoding/hex"
 	"fmt"
+	"strconv"
 	"strings"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/rand"
+	"k8s.io/apimachinery/pkg/util/validation"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
 const (
+	// nameDelimiter is the delimiter character used for joining name value tokens.
```

  @ichekrygin This PR is mixing some useful cleanups with the new functionality, which makes it harder to analyze (as it is not always immediately clear what is relevant). Would you like to submit a cleanup PR?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-06-13T16:01:28Z

- **pkg/controller/jobframework/workload_names.go:36** by [@ichekrygin](https://github.com/ichekrygin) — 2025-06-13T16:01:28Z
```diff
@@ -20,38 +20,93 @@ import (
 	"crypto/sha1"
 	"encoding/hex"
 	"fmt"
+	"strconv"
 	"strings"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/rand"
+	"k8s.io/apimachinery/pkg/util/validation"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
 const (
+	// nameDelimiter is the delimiter character used for joining name value tokens.
```

  Thank you for taking a look. I completely agree that PR changes should stay within the scope of the problem defined in the associated issues. While I try to avoid broadening the scope, in practice it is not always feasible, especially when introducing new functionality that naturally augments or relies on existing components.
  
  In this particular case, I am introducing a new workload naming convention as outlined in the updated KEP-77. As a result, I have made changes to existing logic to align with this new paradigm and to reduce code duplication. These adjustments would be difficult to justify or even implement in isolation, outside the context of this PR. Moving them to a separate, prerequisite PR would disconnect them from their rationale, and at the very least, would require a placeholder explanation such as: "This change is in preparation for an upcoming PR."
  
  That said, I have made sure that all such "small improvement" changes are isolated into separate commits that precede the main feature commit.
  
  Please let me know if keeping these related changes within the same PR, but clearly separated into individual commits, is an acceptable approach?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-20T20:58:31Z

- **pkg/controller/jobframework/workload_names.go:36** by [@mimowo](https://github.com/mimowo) — 2025-06-20T20:58:30Z
```diff
@@ -20,38 +20,93 @@ import (
 	"crypto/sha1"
 	"encoding/hex"
 	"fmt"
+	"strconv"
 	"strings"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/rand"
+	"k8s.io/apimachinery/pkg/util/validation"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
 const (
+	// nameDelimiter is the delimiter character used for joining name value tokens.
```

  More notably, the PR in the current form aims to implement an alpha feature but modifies some core logic, and it is very hard to ensure correctness. 
  
  I suggest avoiding making changes  to the core logic, and hide the logic behind a feature gate. In some cases changes to the core logic might be needed, but then make a preparatory PR to make it easier to assess correctness of the changes. We can always say "preparatory PR for " which will create the link.
  
  We squash all commits on merge, as in most cases this is more readable in the long run for readability of the history. So, I think maintaining the changes as separate commits puts a significant extra effort which is not worth it.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-06-28T00:06:27Z

- **pkg/controller/jobframework/workload_names.go:36** by [@ichekrygin](https://github.com/ichekrygin) — 2025-06-28T00:06:27Z
```diff
@@ -20,38 +20,93 @@ import (
 	"crypto/sha1"
 	"encoding/hex"
 	"fmt"
+	"strconv"
 	"strings"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/rand"
+	"k8s.io/apimachinery/pkg/util/validation"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
 const (
+	// nameDelimiter is the delimiter character used for joining name value tokens.
```

  This (and other similar improvements) should be reverted, PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T16:21:46Z

- **pkg/controller/jobframework/indexer.go:29** by [@mimowo](https://github.com/mimowo) — 2025-07-02T16:21:46Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  Is there any reason this is moved out of jobframework? I would suggest to minimize any refactoring changes which are not needed strictly, because the PR is already large. 
  
  So, any drive-by refactoring I would like goes to preparatory PRs / follow ups.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T16:24:36Z

- **pkg/controller/jobframework/reconciler_inpackage_test.go:2** by [@mimowo](https://github.com/mimowo) — 2025-07-02T16:24:36Z
```diff
@@ -0,0 +1,366 @@
+/*
+Copyright The Kubernetes Authors.
```

  This unit test using mock looks very useful per se. Would you like to submit separate PR, to offload the main one?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T16:26:36Z

- **test/integration/singlecluster/webhook/core/workload_test.go:972** by [@mimowo](https://github.com/mimowo) — 2025-07-02T16:26:36Z
```diff
@@ -966,5 +967,27 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 				g.Expect(k8sClient.Status().Update(ctx, wl)).Should(testing.BeForbiddenError())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should allow workload podSets count update when DynamicallySizedJob feature gate is enabled", func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicallySizedJob, true)
```

  I think we also need code to disable the gate.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T16:27:40Z

- **pkg/controller/core/indexer/indexer.go:54** by [@mimowo](https://github.com/mimowo) — 2025-07-02T16:27:39Z
```diff
@@ -38,6 +39,19 @@ const (
 	WorkloadQuotaReservedKey   = "status.quotaReserved"
 	WorkloadRuntimeClassKey    = "spec.runtimeClass"
 	OwnerReferenceUID          = "metadata.ownerReferences.uid"
+
+	// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
+	// for indexing or matching against a specific owner Group and Kind in a Kubernetes object's metadata.
+	//
+	// The format expects two placeholders: the owner's Group and Kind, and produces a path like:
+	// ".metadata.ownerReferences[<group>.<kind>]"
+	// which can be used as an index key in field selectors.
+	//
+	// Example:
+	//
+	//	fmt.Sprintf(OwnerReferenceGroupKindFmt, "batch", "Job")
+	//	=> ".metadata.ownerReferences[batch.Job]"
+	OwnerReferenceGroupKindFmt = ".metadata.ownerReferences[%s.%s]"
```

  It seems this could be moved back?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T16:30:16Z

Initial pass, overall I think it is looking good. My overall comment would be:
- try to minimize any drive by refactorings as the PR is already large (mostly left comments in this direction)
- make sure all the "main" code is protected by the feature gate (it seems so, but please double check)

I will do another more detailed pass later this week. Thank you for the work!

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-02T16:32:54Z

- **pkg/controller/jobframework/indexer.go:29** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-02T16:32:54Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  Thank you for taking a look, @mimowo.
  
  I had included the rationale in the commit message, but I now realize that commit messages are not always immediately visible during PR reviews. For visibility, here is the reasoning behind this change:
  
  ---
  
  **Commit message excerpt:**
  
  > Resolve a dependency cycle (`jobframework` → `workloadslices` → `jobframework`) by moving `OwnerReference*` index functionality into `core/indexer`.
  >
  > **Background:**
  > The `OwnerReference*` functionality was initially introduced in [[#5729](https://github.com/kubernetes-sigs/kueue/pull/5729)](https://github.com/kubernetes-sigs/kueue/pull/5729), where all workload indexing logic was consolidated under `core/index.go`, in part to avoid potential dependency cycles.
  >
  > However, during review, it was pointed out that `core/index.go` is targeted for an upcoming internalization effort. Based on that, the indexing logic was moved into `jobframework`, which unfortunately reintroduced the dependency cycle.
  >
  > **This change:**
  >
  > * Moves the remaining `OwnerReference*` workload index functionality from `jobframework` back to `core/indexer`, resolving the cycle.
  > * Leaves `SetupWorkloadOwnerIndex` in `jobframework` to maintain alignment with the anticipated internalization effort.
  
  ---
  
  In short, the new auxiliary functions must reside in `core/indexer` (or another independent package) to break the dependency cycle:
  `jobframework` → `workloadslicing` → `jobframework`.
  
  This approach seemed the most natural to me and was my original motivation for the refactoring in [#5729](https://github.com/kubernetes-sigs/kueue/pull/5729).
  
  As an alternative, we could consider splitting up the `workloadslicing` package to break the cycle, but that would risk introducing fragmentation and sprawl across workload slicing–related functionality.
  
  Let me know if you’d prefer that direction instead.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T16:40:56Z

- **pkg/controller/jobframework/indexer.go:29** by [@mimowo](https://github.com/mimowo) — 2025-07-02T16:40:56Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  Thank you for the summary.
  
  > As an alternative, we could consider splitting up the workloadslicing package to break the cycle, but that would risk introducing fragmentation and sprawl across workload slicing–related functionality.
  
  How would that look like? Generally it feels much better splitting new packages rather than modifying / moving pre-existing code. For example in case of the new TopologyAwareScheduling functionality we split out the "indexer" folder, see [here](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/tas). Maybe this would also work here?
  
  EDIT: for sure, I admit the pre-existing code is not ideal and could be restructured. However, I prefer this is done as a dedicated PR rather than as part of the alpha functionality PR. Finally, if there is truly no other way, I'm ok to accept refactoring as part of new feature, but let's explore other options first.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-02T16:50:43Z

- **pkg/controller/jobframework/reconciler_inpackage_test.go:2** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-02T16:50:43Z
```diff
@@ -0,0 +1,366 @@
+/*
+Copyright The Kubernetes Authors.
```

  Thanks! I assume you're referring to the `mockJob`, since the unit tests themselves are tied to the new `workloadslicing` functionality and can't be easily split out. If that assumption is correct, I'm happy to move the `mockJob` and related helpers into a follow-up PR to keep this one focused. 
  
  I wasn’t entirely sure what the maintainers’ stance is on the use of mock objects, as there seems to be some hesitance, or even a bit of a “one shall not mock” sentiment in parts of the community.
  
  Personally, I try to avoid rigid rules around mocking (or most code-related patterns) and fall more into the “it depends” camp. In this case, mocking the job object offers a straightforward and much less involved way to construct a job with the specific conditions needed to test how it's handled in relation to workloads, specifically, workload slices.
  
  Let me know if that aligns with what you had in mind.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-02T16:58:13Z

- **pkg/controller/jobframework/indexer.go:29** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-02T16:58:13Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  > How would that look like?
  
  One way to approach this would be to merge the contents of the `workloadslicing` package into `jobframework`. This would simplify the dependency structure but result in a larger delta within the `jobframework` package—something I’ve been trying to avoid.
  
  That said, I don’t have a strong stance on this and am open to refactoring if that’s the direction you’d prefer.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-02T16:59:05Z

- **pkg/controller/core/indexer/indexer.go:54** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-02T16:59:05Z
```diff
@@ -38,6 +39,19 @@ const (
 	WorkloadQuotaReservedKey   = "status.quotaReserved"
 	WorkloadRuntimeClassKey    = "spec.runtimeClass"
 	OwnerReferenceUID          = "metadata.ownerReferences.uid"
+
+	// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
+	// for indexing or matching against a specific owner Group and Kind in a Kubernetes object's metadata.
+	//
+	// The format expects two placeholders: the owner's Group and Kind, and produces a path like:
+	// ".metadata.ownerReferences[<group>.<kind>]"
+	// which can be used as an index key in field selectors.
+	//
+	// Example:
+	//
+	//	fmt.Sprintf(OwnerReferenceGroupKindFmt, "batch", "Job")
+	//	=> ".metadata.ownerReferences[batch.Job]"
+	OwnerReferenceGroupKindFmt = ".metadata.ownerReferences[%s.%s]"
```

  This is related to discussion in: https://github.com/kubernetes-sigs/kueue/pull/5510#pullrequestreview-2979834187

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T16:59:26Z

- **pkg/controller/jobframework/indexer.go:29** by [@mimowo](https://github.com/mimowo) — 2025-07-02T16:59:26Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  > For example in case of the new TopologyAwareScheduling functionality we split out the "indexer" folder, see [here](https://github.com/kubernetes-sigs/kueue/tree/main/pkg/controller/tas). Maybe this would also work here?
  
  would this be an option in case of workloadslice?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-03T03:27:52Z

- **pkg/controller/jobframework/indexer.go:29** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-03T03:27:52Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  I'm not entirely sure.
  
  On one hand, `workloadslices` doesn't introduce any *new* requirements for index functionality, it simply needs to use existing functionality, specifically `indexer.WorkloadOwnerIndexKey(jobObjectGVK)` and `indexer.OwnerReferenceUID`. Since this functionality didn’t exist prior to the recent refactoring, I don’t believe moving it into `core` introduces any backward compatibility concerns.
  
  On the other hand, I do want to promote code reuse and avoid duplication. 
  For example, I could have used raw string literals like `"metadata.ownerReferences.uid"` or `".metadata.ownerReferences[%s.%s]"` (as it was done pre-refactoring for latter), but that didn’t feel like the right long-term approach, which was another motivation for: [https://github.com/kubernetes-sigs/kueue/pull/5729](https://github.com/kubernetes-sigs/kueue/pull/5729).
  
  In summary, my main concern is avoiding sprawl in index functionality, particularly when it results in duplicating identical logic or definitions. Please let me know if this is a blocker.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-03T04:38:31Z

- **test/integration/singlecluster/webhook/core/workload_test.go:972** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-03T04:38:31Z
```diff
@@ -966,5 +967,27 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 				g.Expect(k8sClient.Status().Update(ctx, wl)).Should(testing.BeForbiddenError())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should allow workload podSets count update when DynamicallySizedJob feature gate is enabled", func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicallySizedJob, true)
```

  I was under the impression that it is covered here: https://github.com/kubernetes-sigs/kueue/blob/7d9368a72aae8bc3b6f950076edb0d676bb83fc7/test/integration/singlecluster/webhook/core/workload_test.go#L466
  I will add a dedicated test for podset.count specifically.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T04:57:27Z

- **test/integration/singlecluster/webhook/core/workload_test.go:972** by [@mimowo](https://github.com/mimowo) — 2025-07-03T04:57:27Z
```diff
@@ -966,5 +967,27 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 				g.Expect(k8sClient.Status().Update(ctx, wl)).Should(testing.BeForbiddenError())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should allow workload podSets count update when DynamicallySizedJob feature gate is enabled", func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicallySizedJob, true)
```

  What I mean is that we need to disable the feature gate at the end of the test so that the downstream tests are not affected by the enablement. Typically what we do is a separate Describe for a new feature where the feature gate is enabled in Before each and disabled in After each. Ideally I would follow the pattern here too

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T04:59:46Z

- **pkg/controller/jobframework/indexer.go:29** by [@mimowo](https://github.com/mimowo) — 2025-07-03T04:59:46Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  Hard to assess for me without hands on experience. Im going to checkout the branch this week and play with the code to see if I can somehow move the preexisting code back.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-03T05:14:18Z

- **test/integration/singlecluster/webhook/core/workload_test.go:972** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-03T05:14:18Z
```diff
@@ -966,5 +967,27 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 				g.Expect(k8sClient.Status().Update(ctx, wl)).Should(testing.BeForbiddenError())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should allow workload podSets count update when DynamicallySizedJob feature gate is enabled", func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicallySizedJob, true)
```

  Oh, I see, entirely misunderstood. 
  In such a case, IIUC, `SetFeatureGateDuringTest` sets the specified feature gate for the duration of a specific test, and automatically resets it to its original value during the test's [cleanup](https://github.com/kubernetes/component-base/blob/master/featuregate/testing/feature_gate.go#L75) phase.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T05:28:42Z

- **test/integration/singlecluster/webhook/core/workload_test.go:972** by [@mimowo](https://github.com/mimowo) — 2025-07-03T05:28:42Z
```diff
@@ -966,5 +967,27 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 				g.Expect(k8sClient.Status().Update(ctx, wl)).Should(testing.BeForbiddenError())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should allow workload podSets count update when DynamicallySizedJob feature gate is enabled", func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicallySizedJob, true)
```

  I think eventually we will need many more integration tests for the feature, and so I think it make sense to already start with a dedicated Describe section and cleanup the feature gate in After each. You may check some other feature gates

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T17:23:42Z

- **pkg/controller/jobframework/indexer.go:29** by [@mimowo](https://github.com/mimowo) — 2025-07-03T17:23:42Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  Ok, I had a chance to play locally more with the PR so that I have a better grasp on the required changes to pre-existing code. Indeed, I'm leaning to proceed with two changes:
  1. moving WorkloadOwnerIndexKey into core/indexer
  2. parametrizing GetWorkloadNameForOwnerWithGVK
  
  I think though both can be done in preparatory PRs. Let me try to propose some this week.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T17:47:20Z

- **test/integration/singlecluster/webhook/core/workload_test.go:972** by [@mimowo](https://github.com/mimowo) — 2025-07-03T17:47:20Z
```diff
@@ -966,5 +967,27 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 				g.Expect(k8sClient.Status().Update(ctx, wl)).Should(testing.BeForbiddenError())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should allow workload podSets count update when DynamicallySizedJob feature gate is enabled", func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicallySizedJob, true)
```

  Actually, my suggestion is not good, as we synced with @ichekrygin the feature gates are indeed cleaned up between "Its". In this case this looks great. Feel free to resolve.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T18:50:27Z

- **pkg/controller/jobframework/reconciler_inpackage_test.go:2** by [@mimowo](https://github.com/mimowo) — 2025-07-03T18:50:26Z
```diff
@@ -0,0 +1,366 @@
+/*
+Copyright The Kubernetes Authors.
```

  Yes, I like the mock, but now I realized we already have `testGenericJob` (pkg/controller/jobframework/validation_test.go) which plays the same role. Could we reuse it instead?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T19:06:20Z

- **pkg/controller/jobframework/indexer.go:29** by [@mimowo](https://github.com/mimowo) — 2025-07-03T19:06:20Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  Regarding (1.) I would like to extract the following bits from the PR (for the ease of reviewing):
  
  pkg/controller/core/indexer/indexer.go:
  - ownerReferenceGroupKindFmt(unexported)
  - WorkloadOwnerIndexKey
  - WorkloadOwnerIndex
  
  pkg/controller/jobframework/indexer.go:
  - SetupWorkloadOwnerIndex delegates to indexer.WorkloadOwnerIndex(gvk)
  - workloadOwnerReferenceIndexFieldMatcher (unexported)
  
  Feel free to submit the PR if this makes sense to you. All the comments are great.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-03T19:42:59Z

- **pkg/controller/jobframework/reconciler_inpackage_test.go:2** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-03T19:42:59Z
```diff
@@ -0,0 +1,366 @@
+/*
+Copyright The Kubernetes Authors.
```

  Thanks @mimowo, I did consider `testGenericJob` in `validation_test.go` as well as [almost] identical in `base_webhook_test.go`, and neither of those seems like a good fit for a couple of reasons:
  
  * **(Minor)** The one in `base_webhook_test.go` is defined under `package jobframework_test`, which makes it unavailable for use in tests within the `jobframework` package without exporting additional types or interfaces.
  
  * **(Major)** Both existing versions are tightly coupled to a `batchv1.Job` backing and offer only minimal support for actual mocking behavior:
  
    * The one in `validation_test.go` acts more like a placeholder and doesn’t provide any meaningful mocking, it lacks control over return values or behavioral overrides.
    * The one in `base_webhook_test.go` does mock two methods (`validateOnCreate` and `validateOnUpdate`), which happen to align with the current use case, but it’s still limited in flexibility and hard to generalize.
  
  While we’re on the topic,  have you (or the team) considered using something like [uber-go/mock](https://github.com/uber-go/mock) to streamline and formalize mock generation? It could help reduce boilerplate and make test behavior more explicit, especially as the codebase continues to grow in complexity.
  
  If there’s interest, I’d be happy to help set up and bootstrap `mockgen` for selected interfaces, starting with `GenericJob` in this case.
  
  We could wire this into the `Makefile` and output generated mocks under a dedicated directory like `./internal/mocks`, following the typical convention. This would make it easier to generate and maintain mocks consistently across the codebase, and could be extended incrementally as more interfaces benefit from mocking.
  
  Given that `GenericJob` is a common and widely used interface, I think there’s significant value in formalizing its mock as a standalone (possibly internal) package (with or without `go-mock`) with broader visibility.
  
  Placing it under something like `internal/mocks/genericjob` (or similar) with public accessibility would allow us to reuse the generated mock across all relevant packages in the Kueue codebase, improving consistency and reducing duplication in unit tests.
  
  Happy to help with structuring this if there's agreement on the direction.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-03T21:14:39Z

- **pkg/controller/jobframework/indexer.go:29** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-03T21:14:39Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  I decided to revert this change, as it is only tangentially related to the primary purpose of the PR. Additionally, I'm not entirely confident I understand the rationale behind the public/private exposure, both Kueue-wide in general and for workload index specifically. We can revisit this in a follow-up and clean up any resulting duplication as needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-04T07:14:11Z

- **pkg/controller/jobframework/indexer.go:29** by [@mimowo](https://github.com/mimowo) — 2025-07-04T07:14:10Z
```diff
@@ -18,48 +18,14 @@ package jobframework
 
 import (
 	"context"
-	"fmt"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
-// OwnerReferenceGroupKindFmt defines the format string used to construct a field path
```

  > I decided to revert this change, as it is only tangentially related to the primary purpose of the PR.
  
  Thank you, in that case feel free to resolve the comment and other (if any) related comments. 
  
  The point (2.) about GetWorkloadNameForOwnerWithGVK is less important, and I need more time to think how to address it, and it is also acceptable in the current form to me. If something occurs to me I will open dedicated comment with a concrete proposal.
  
  > I'm not entirely confident I understand the rationale behind the public/private exposure, both Kueue-wide in general and for workload index specifically. 
  
  Basically, I want to avoid exporting functions which are not used externally, to minimize the API surface. If we need the functions exported in the future we can always do it later.
  
  > We can revisit this in a follow-up and clean up any resulting duplication as needed.
  
  Sure, I'm open to feedback about Kueue code based more, and to evolve it in the future. Always feel free to open PR, issue, or reach out on slack.
  
  Still, I try to minimize drive-by cleanups in this PR to make it easier to review, and thus to ensure correctness.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-04T08:32:44Z

- **pkg/controller/jobframework/workload_names.go:36** by [@mimowo](https://github.com/mimowo) — 2025-07-04T08:32:43Z
```diff
@@ -20,38 +20,93 @@ import (
 	"crypto/sha1"
 	"encoding/hex"
 	"fmt"
+	"strconv"
 	"strings"
 
 	"k8s.io/apimachinery/pkg/runtime/schema"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/rand"
+	"k8s.io/apimachinery/pkg/util/validation"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
 )
 
 const (
+	// nameDelimiter is the delimiter character used for joining name value tokens.
```

  thanks, feel free to resolve

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T17:20:34Z

- **pkg/controller/jobframework/reconciler_inpackage_test.go:2** by [@mimowo](https://github.com/mimowo) — 2025-07-08T17:20:34Z
```diff
@@ -0,0 +1,366 @@
+/*
+Copyright The Kubernetes Authors.
```

  Thank you for the detailed summary. Indeed, the very fact that we already have two mocks (in `validation_test.go` and in `base_webhook_test.go`) is to me indication that:
  1. people find it useful (including me)
  2. it is tricky for reuse
  
  Since people find mocking useful, and in particular for the GenericJob interface, I'm happy to make it simpler. Indeed, the current approach is suboptimal as coupled to batch/Job and requiring to export for reuse.
  
  Given the above I'm totally +1 for using a library. We may just consider `uber-go/mock` seems like a reasonable choice. There is also  [testify](https://github.com/stretchr/testify) as alternative, but I don't have a preference.
  I will welcome contributions in this direction. Awesome if we could consolidate all the mocking.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T17:21:18Z

- **pkg/controller/jobframework/reconciler_inpackage_test.go:2** by [@mimowo](https://github.com/mimowo) — 2025-07-08T17:21:18Z
```diff
@@ -0,0 +1,366 @@
+/*
+Copyright The Kubernetes Authors.
```

  Regarding this particular PR. I'm happy with mockJob as a temporary measure until we have the library used.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T18:20:02Z

- **pkg/controller/jobframework/workload_names.go:41** by [@mimowo](https://github.com/mimowo) — 2025-07-08T18:20:02Z
```diff
@@ -36,10 +38,18 @@ func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerG
 	if len(prefixedName) > maxPrefixLength {
 		prefixedName = prefixedName[:maxPrefixLength]
 	}
-	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK)[:hashLength]
+	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK, nil)[:hashLength]
```

  Let's implement 
  ```
  func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerGVK schema.GroupVersionKind) string {
    return GetWorkloadNameForOwnerWithGVKAndGeneration(ownerName, ownerUID, ownerGVK, nil)
  }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T18:23:13Z

- **pkg/controller/jobframework/reconciler.go:778** by [@mimowo](https://github.com/mimowo) — 2025-07-08T18:23:13Z
```diff
@@ -754,6 +765,38 @@ func FindAncestorJobManagedByKueue(ctx context.Context, c client.Client, jobObj
 	}
 }
 
+// ensureWorkload ensures that a corresponding Kueue Workload exists for the given job.
+//
+// If workload slicing is enabled for the job, it attempts to manage the job using workload slices.
+// In this mode, only changes to PodSet.Count are considered compatible. Structural or semantic changes
+// to the pod sets result in an "incompatible" slice, and the function falls back to managing the job
+// using the standard "one workload per job" approach.
+//
+// Returns the associated workload (whether slice-based or not), or an error.
+func (r *JobReconciler) ensureWorkload(ctx context.Context, job GenericJob, jobObject client.Object) (*kueue.Workload, error) {
+	// If workload slicing is enabled for this job, use the slice-based processing path.
+	if WorkloadSliceEnabled(job) {
```

  I would prefer to move this `if` under `ensureOneWorkload`. I think conceptually it still ensures there is only one active workload. We may consider renaming to `ensureOneActiveWorkload`,  but this would be not necessary,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T18:27:05Z

- **pkg/scheduler/scheduler.go:500** by [@mimowo](https://github.com/mimowo) — 2025-07-08T18:27:05Z
```diff
@@ -462,20 +465,52 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// getInitialAssignments computes the initial resource flavor assignment and any required preemption targets
+// for a workload slice.
+//
+// The function attempts to assign resources to the provided workload slice using the current
+// snapshot of the scheduling state. It proceeds in the following steps:
+//
+//  1. It first checks for any preemptible workload slices that workload may replace, using an annotation-based lookup.
+//  2. It creates a flavor assigner to compute a full assignment scale-adjusted for preemptable workload slice targets
+//     based on either:
+//     - direct fit (no preemption needed), or
+//     - preemption (if needed and possible).
+//  3. If direct assignment isn't possible but preemption is enabled and viable, it includes any additional
+//     preemption targets obtained through the configured preemptor.
+//  4. If partial admission is enabled and the workload allows it, the function attempts to reduce pod counts
+//     across PodSets to find an assignable configuration—again checking for preemption if needed.
+//
+// Returns:
+//   - A flavorassigner.Assignment representing the selected (possibly reduced) flavor allocation.
+//   - A slice of preemption targets, which may include both explicitly annotated slices and those
+//     identified during scheduling.
+//
+// If no valid assignment can be made, returns the original full assignment with no preemption targets.
 func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, snap *cache.Snapshot) (flavorassigner.Assignment, []*preemption.Target) {
 	cq := snap.ClusterQueue(wl.ClusterQueue)
-	flvAssigner := flavorassigner.New(wl, cq, snap.ResourceFlavors, s.fairSharing.Enable, preemption.NewOracle(s.preemptor, snap))
+
+	var preemptionTargets []*preemption.Target
+	var preemptableWorkloadSlice *workload.Info
+
+	workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl)
+	if workloadSlicePreemptionTarget != nil {
+		preemptionTargets = []*preemption.Target{workloadSlicePreemptionTarget}
+		preemptableWorkloadSlice = workloadSlicePreemptionTarget.WorkloadInfo
+	}
```

  Let's guard it by the feature gate.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T22:46:53Z

- **pkg/controller/jobframework/reconciler_inpackage_test.go:2** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T22:46:53Z
```diff
@@ -0,0 +1,366 @@
+/*
+Copyright The Kubernetes Authors.
```

  Follow up in #5906

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T22:57:05Z

- **pkg/controller/jobframework/workload_names.go:41** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T22:57:05Z
```diff
@@ -36,10 +38,18 @@ func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerG
 	if len(prefixedName) > maxPrefixLength {
 		prefixedName = prefixedName[:maxPrefixLength]
 	}
-	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK)[:hashLength]
+	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK, nil)[:hashLength]
```

  I am assuming this suggestion should be accompanied by with the following change
  ```golang
  func GetWorkloadNameForOwnerWithGVKAndGeneration(ownerName string, ownerUID types.UID, ownerGVK schema.GroupVersionKind, generation *int64) string {...}
  ```
  Notable change, `generation int64 -> *int64`. 
  
  If my understanding of propose change is correct, I have the following concern.
  
  The name suggests that the generation is always required and will always be included in the logic, but since the generation parameter is a pointer and can be nil, that’s not quite true, it’s actually optional.
  
  That mismatch can be a little confusing for someone reading or using the function, they might assume generation is always used, when in reality it might be ignored if it's nil. It also makes it harder to reason about what the function is doing in different cases.
  
  Maybe a name like ...WithOptionalGeneration would better reflect what’s going on, or alternatively, it might make sense to treat the no-generation version as the default, and have a separate helper that adds generation when needed (as implemented in this PR).
  
  No strong objection here, just thinking about long-term readability and helping future readers understand the function’s intent at a glance.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T23:51:04Z

- **pkg/controller/jobframework/reconciler.go:778** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T23:51:04Z
```diff
@@ -754,6 +765,38 @@ func FindAncestorJobManagedByKueue(ctx context.Context, c client.Client, jobObj
 	}
 }
 
+// ensureWorkload ensures that a corresponding Kueue Workload exists for the given job.
+//
+// If workload slicing is enabled for the job, it attempts to manage the job using workload slices.
+// In this mode, only changes to PodSet.Count are considered compatible. Structural or semantic changes
+// to the pod sets result in an "incompatible" slice, and the function falls back to managing the job
+// using the standard "one workload per job" approach.
+//
+// Returns the associated workload (whether slice-based or not), or an error.
+func (r *JobReconciler) ensureWorkload(ctx context.Context, job GenericJob, jobObject client.Object) (*kueue.Workload, error) {
+	// If workload slicing is enabled for this job, use the slice-based processing path.
+	if WorkloadSliceEnabled(job) {
```

  If I understand your suggestion correctly, I have the following concern:
  
  The current implementation follows a top-level branching paradigm, for example:
  
  ```golang
  func (r *JobReconciler) ensureWorkload(...) {
    if condition {
      return workloadslices.EnsureWorkload(...)
    }
    return r.ensureOneWorkload(...)
  }
  ```
  
  This approach aligns with idiomatic Go, which favors clarity and explicit control flow. Top-level branching makes decisions immediately visible and cleanly separates responsibilities. It also makes the code easier to test and debug, as each branch can be understood and exercised in isolation.
  
  The tradeoff, of course, is the introduction of an additional function to host the top-level branching. If that’s the concern, it’s worth noting that both r.ensureWorkload and workloadslicing.EnsureWorkload already have thorough test coverage.
  
  The alternative you're suggesting moves toward a more nested or compositional style, such as:
  
  ```golang
  func (r *JobReconciler) ensureOneWorkload(...) {
    if condition {
      return workloadslices.EnsureWorkload(...)
    }
    // the rest of the current function implementation (100+ lines)
    return ...
  }
  ```
  
  I think my main reservation is that I’m a bit hesitant to modify the existing function without adding more unit tests, especially since `r.ensureOneWorkload` appears to have very limited unit-test coverage at the moment.
  
  That said, I might be misunderstanding your intent or missing some details, very possible. I'm happy to keep the conversation going if you see benefits I’m overlooking.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:32:04Z

- **pkg/controller/core/workload_controller.go:443** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:32:04Z
```diff
@@ -432,6 +438,32 @@ func (r *WorkloadReconciler) reconcileMaxExecutionTime(ctx context.Context, wl *
 	return 0, nil
 }
 
+// reconcileSliceBasedEviction completes aggregated workload slice life-cycle.
+// If workload slice is active - apply deactivation condition (if needed).
+func (r *WorkloadReconciler) reconcileSliceBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
```

  Guard the code with the new feature gate.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:34:10Z

- **pkg/controller/jobframework/workload_names.go:41** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:34:09Z
```diff
@@ -36,10 +38,18 @@ func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerG
 	if len(prefixedName) > maxPrefixLength {
 		prefixedName = prefixedName[:maxPrefixLength]
 	}
-	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK)[:hashLength]
+	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK, nil)[:hashLength]
```

  I see,, makes sense. I that case please introduce a new function 
  
  ```golang
  func generateWorkloadName(ownerName string, ownerUID types.UID, ownerGVK schema.GroupVersionKind, generation *int64) string
  ```
  Then call it from `GetWorkloadNameForOwnerWithGVK` and `GetWorkloadNameForOwnerWithGVKAndGeneration`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:35:00Z

- **pkg/controller/jobs/job/job_controller.go:379** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:35:00Z
```diff
@@ -370,6 +370,11 @@ func SetupIndexes(ctx context.Context, fieldIndexer client.FieldIndexer) error {
 	if err := fieldIndexer.IndexField(ctx, &batchv1.Job{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
 		return err
 	}
+	// Add pod index to be able to list pods for a given job.
+	if err := fieldIndexer.IndexField(ctx, &corev1.Pod{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
+		return err
+	}
```

  Only enable the index when the new feature gate is enabled.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:35:51Z

- **pkg/features/kube_features.go:162** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:35:50Z
```diff
@@ -170,6 +170,12 @@ const (
 	// Enable replacement of failed node in TAS.
 	TASFailedNodeReplacement featuregate.Feature = "TASFailedNodeReplacement"
 
+	// owner: @ichekrygin
+	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+	//
+	// ElasticJobsViaWorkloadSlices enables workload-slices support.
```

  ```suggestion
  	// ElasticJobsViaWorkloadSlices enables support for elastic Jobs using workload-slices.
  ```
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:36:30Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:647** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:36:30Z
```diff
@@ -612,6 +643,22 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 		// Calculate representativeMode for this assignment as the worst mode among all requests.
 		representativeMode := fit
 		for rName, val := range requests {
+			// Ensure the same resource flavor is used for the workload slice as in the original admitted slice.
+			if a.preemptWorkloadSlice != nil {
```

  please also guard with the new feature gate.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:36:41Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:647** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:36:41Z
```diff
@@ -585,6 +615,7 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
+
```

  nit revert formatting changes

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:41:31Z

- **pkg/scheduler/scheduler.go:279** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:41:30Z
```diff
@@ -273,7 +273,10 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		preemptedWorkloads.Insert(e.preemptionTargets)
 		cq.AddUsage(usage)
 
-		if e.assignment.RepresentativeMode() == flavorassigner.Preempt {
+		// When placing a workload slice it is possible for a new workload to "Fit" and
+		// still have a preempted workload (old slice).
+		if mode := e.assignment.RepresentativeMode(); mode == flavorassigner.Preempt ||
+			(mode == flavorassigner.Fit && len(preemptedWorkloads) > 0) {
```

  ```suggestion
  			(mode == flavorassigner.Fit && len(e.preemptionTargets) > 0) {
  ```
  Otherwise this could actually introduce a bug, when the previous entry introduced PreemptionTargets, and the new entry is using the code below, even though it is "Fit", and does not have any preemption targets.
  
  Since this actually would be a bug, I would like to either:
  1. modify the upstream code to say "Preempt" in that case (guarded by the new feature gate)
  2. extract the check to a helper function and guard it by the new feature gate
  
  My preference would be (1.) actually to make the mode simpler conceptually - there are targets so it is Preempt. However, I don't mind (2.) especially for Alpha.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:43:27Z

- **pkg/workload/workloadslicing/workloadslicing.go:18** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:43:27Z
```diff
@@ -0,0 +1,311 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
```

  I would like to move this package level up (not under workload). The reason is that many of the functions here actually are called from Jobframework packages, and are not directly related to workloads.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:47:25Z

- **pkg/workload/workload.go:1113** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:47:25Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
+	if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
```

  I would like to make it conditional also on the presence of the `kueue.x-k8s.io/workload-preemptible-slice` annotaiton. The reason being, that the feature gate (hopefully) will be beta at some point, then we only want to allow mutations for the workloads using the feature, not every workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T08:49:08Z

- **pkg/workload/workload.go:1112** by [@mimowo](https://github.com/mimowo) — 2025-07-09T08:49:07Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
```

  Please move the functions to `pkg/webhooks/workload_webhook.go`, and unexport. They are unlikely to be used externally. We can move them here when we have usecase for external use.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T15:14:22Z

- **pkg/controller/core/workload_controller.go:443** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T15:14:21Z
```diff
@@ -432,6 +438,32 @@ func (r *WorkloadReconciler) reconcileMaxExecutionTime(ctx context.Context, wl *
 	return 0, nil
 }
 
+// reconcileSliceBasedEviction completes aggregated workload slice life-cycle.
+// If workload slice is active - apply deactivation condition (if needed).
+func (r *WorkloadReconciler) reconcileSliceBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
```

  I intentionally did not add a feature gate check here. This functionality is responsible for finalizing already preempted workload slices. By definition, and under normal operation, this code is only reachable when the feature is enabled.
  
  However, in scenarios where the feature is later disabled, after some workload slices have already been preempted but not yet finalized, this code ensures that those slices are still properly finalized. This acts as a safeguard to maintain consistency in transitional states.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T15:17:16Z

- **pkg/controller/jobs/job/job_controller.go:379** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T15:17:16Z
```diff
@@ -370,6 +370,11 @@ func SetupIndexes(ctx context.Context, fieldIndexer client.FieldIndexer) error {
 	if err := fieldIndexer.IndexField(ctx, &batchv1.Job{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
 		return err
 	}
+	// Add pod index to be able to list pods for a given job.
+	if err := fieldIndexer.IndexField(ctx, &corev1.Pod{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
+		return err
+	}
```

  Similar to the rationale above, this functionality is required to clear scheduling gates for already admitted workload slices, even if the feature gate has since been disabled. It ensures consistent cleanup of slices that were admitted while the feature was active but have not yet completed their lifecycle.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T15:33:22Z

- **pkg/controller/core/workload_controller.go:443** by [@mimowo](https://github.com/mimowo) — 2025-07-09T15:33:22Z
```diff
@@ -432,6 +438,32 @@ func (r *WorkloadReconciler) reconcileMaxExecutionTime(ctx context.Context, wl *
 	return 0, nil
 }
 
+// reconcileSliceBasedEviction completes aggregated workload slice life-cycle.
+// If workload slice is active - apply deactivation condition (if needed).
+func (r *WorkloadReconciler) reconcileSliceBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
```

  TBH, I don't think we need to support disabling alpha feature gate. Non-gated code makes me anxious, but puts more effort on reviewing correctness. If you think this is really a use-case we should support in Alpha I can do that, but it is rather exception than regular procedure for alpha features. Same in core k8s AFAIK.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T15:57:28Z

- **pkg/controller/jobframework/reconciler.go:778** by [@mimowo](https://github.com/mimowo) — 2025-07-09T15:57:28Z
```diff
@@ -754,6 +765,38 @@ func FindAncestorJobManagedByKueue(ctx context.Context, c client.Client, jobObj
 	}
 }
 
+// ensureWorkload ensures that a corresponding Kueue Workload exists for the given job.
+//
+// If workload slicing is enabled for the job, it attempts to manage the job using workload slices.
+// In this mode, only changes to PodSet.Count are considered compatible. Structural or semantic changes
+// to the pod sets result in an "incompatible" slice, and the function falls back to managing the job
+// using the standard "one workload per job" approach.
+//
+// Returns the associated workload (whether slice-based or not), or an error.
+func (r *JobReconciler) ensureWorkload(ctx context.Context, job GenericJob, jobObject client.Object) (*kueue.Workload, error) {
+	// If workload slicing is enabled for this job, use the slice-based processing path.
+	if WorkloadSliceEnabled(job) {
```

  I think from the code structuring you summarized it well. I don't have much preference one way over another.
  
  One aspect skipped in the summary, and I think quite important, is making the diff easy to reason about by minimizing drive-by changes. 
  
  For example, by renaming the function from `ensureOneWorkload` to `ensureWorkload` we need to think about the new name. And, actually, I think the previous name reflects the intention better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T15:59:44Z

- **pkg/controller/core/workload_controller.go:443** by [@mimowo](https://github.com/mimowo) — 2025-07-09T15:59:43Z
```diff
@@ -432,6 +438,32 @@ func (r *WorkloadReconciler) reconcileMaxExecutionTime(ctx context.Context, wl *
 	return 0, nil
 }
 
+// reconcileSliceBasedEviction completes aggregated workload slice life-cycle.
+// If workload slice is active - apply deactivation condition (if needed).
+func (r *WorkloadReconciler) reconcileSliceBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
```

  Actually, we may not need the finalizing step at all, if we Deactivate or mark the workload as Finished in the same request as we make it Preempted. So, this would be my suggestion, and then we eliminate the issues altogether.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T16:28:51Z

- **pkg/controller/core/workload_controller.go:443** by [@mimowo](https://github.com/mimowo) — 2025-07-09T16:28:51Z
```diff
@@ -432,6 +438,32 @@ func (r *WorkloadReconciler) reconcileMaxExecutionTime(ctx context.Context, wl *
 	return 0, nil
 }
 
+// reconcileSliceBasedEviction completes aggregated workload slice life-cycle.
+// If workload slice is active - apply deactivation condition (if needed).
+func (r *WorkloadReconciler) reconcileSliceBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
```

  I also put similar suggestion [here](https://github.com/kubernetes-sigs/kueue/pull/5648#discussion_r2194267485). 
  
  I think the addition of the "Finished" condition could be done inside `applyPreemptionWithSSA`. The function currently sets "Evicted" and "Preempted". It could also set "Finished" in case of WorkloadSlice replacement.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T20:41:54Z

- **pkg/controller/jobframework/workload_names.go:41** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T20:41:54Z
```diff
@@ -36,10 +38,18 @@ func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerG
 	if len(prefixedName) > maxPrefixLength {
 		prefixedName = prefixedName[:maxPrefixLength]
 	}
-	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK)[:hashLength]
+	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK, nil)[:hashLength]
```

  SGTM. Worth noting this is almost exactly how it’s currently structured, just with the helper named `getHash(ownerName string, ownerUID types.UID, gvk schema.GroupVersionKind, generation *int64)`. I’ll go ahead and introduce the `generateWorkloadName(...)` function as suggested and wire it up through both callers.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T21:01:17Z

- **pkg/controller/jobframework/reconciler.go:778** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T21:01:17Z
```diff
@@ -754,6 +765,38 @@ func FindAncestorJobManagedByKueue(ctx context.Context, c client.Client, jobObj
 	}
 }
 
+// ensureWorkload ensures that a corresponding Kueue Workload exists for the given job.
+//
+// If workload slicing is enabled for the job, it attempts to manage the job using workload slices.
+// In this mode, only changes to PodSet.Count are considered compatible. Structural or semantic changes
+// to the pod sets result in an "incompatible" slice, and the function falls back to managing the job
+// using the standard "one workload per job" approach.
+//
+// Returns the associated workload (whether slice-based or not), or an error.
+func (r *JobReconciler) ensureWorkload(ctx context.Context, job GenericJob, jobObject client.Object) (*kueue.Workload, error) {
+	// If workload slicing is enabled for this job, use the slice-based processing path.
+	if WorkloadSliceEnabled(job) {
```

  Thanks for the thoughtful note, really appreciate it. I’d like to gently disagree on the naming point.
  
  The term `ensureOneWorkload` feels a bit redundant and slightly misleading:
  
  * The word “One” suggests a guaranteed singular return, but the function can return `nil` in valid cases, like when the workload is out-of-sync.
  * The function signature already makes it clear that the return value is either a `*Workload` or `nil`, so “One” doesn’t add much clarity.
  * Also, it doesn’t truly “ensure” a workload is returned—it just attempts to retrieve one under certain conditions.
  * With workload slices, there could technically be more than one “active” workload, even though at most one will be returned. The name “One” may understate that nuance.
  
  I totally agree we should minimize drive-by changes. And, naming things is [one of the two hard problems in computer science](https://martinfowler.com/bliki/TwoHardThings.html). That said, will you be open to something more general for the top-level function—maybe `jobWorkload` or similar?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T21:10:12Z

- **pkg/scheduler/scheduler.go:279** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T21:10:12Z
```diff
@@ -273,7 +273,10 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		preemptedWorkloads.Insert(e.preemptionTargets)
 		cq.AddUsage(usage)
 
-		if e.assignment.RepresentativeMode() == flavorassigner.Preempt {
+		// When placing a workload slice it is possible for a new workload to "Fit" and
+		// still have a preempted workload (old slice).
+		if mode := e.assignment.RepresentativeMode(); mode == flavorassigner.Preempt ||
+			(mode == flavorassigner.Fit && len(preemptedWorkloads) > 0) {
```

  Excellent catch, that nuance totally slipped past me, really glad you caught it, @mimowo!
  
  Fixed.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T22:36:46Z

- **pkg/workload/workload.go:1112** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T22:36:46Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
```

  Done.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T22:40:48Z

- **pkg/workload/workload.go:1113** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T22:40:47Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
+	if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
```

  Hm... good point. ~For that, I would need to create a different validation routine specifically for slice-enabled workloads (since the annotation is on the Workload object and not on the PodSet)~
  
  > I would like to make it conditional also on the presence of the `kueue.x-k8s.io/workload-preemptible-slice` annotation.
  
  Just to clarify, the annotation is not on the Workload object (nor on the PodSets) but rather on the actual Job object. It's certainly possible to validate, but it would require a slightly deeper validation routine, retrieving the original Job and checking its annotations. Let me know if that’s what you had in mind.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T22:42:44Z

- **pkg/workload/workloadslicing/workloadslicing.go:18** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T22:42:44Z
```diff
@@ -0,0 +1,311 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
```

  done

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T22:43:18Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:647** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T22:43:17Z
```diff
@@ -585,6 +615,7 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
+
```

  done.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T22:43:44Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:647** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T22:43:44Z
```diff
@@ -612,6 +643,22 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 		// Calculate representativeMode for this assignment as the worst mode among all requests.
 		representativeMode := fit
 		for rName, val := range requests {
+			// Ensure the same resource flavor is used for the workload slice as in the original admitted slice.
+			if a.preemptWorkloadSlice != nil {
```

  done

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-09T22:46:34Z

- **pkg/controller/jobframework/workload_names.go:41** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-09T22:46:34Z
```diff
@@ -36,10 +38,18 @@ func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerG
 	if len(prefixedName) > maxPrefixLength {
 		prefixedName = prefixedName[:maxPrefixLength]
 	}
-	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK)[:hashLength]
+	return prefixedName + "-" + getHash(ownerName, ownerUID, ownerGVK, nil)[:hashLength]
```

  updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-10T04:43:57Z

- **pkg/workload/workload.go:1113** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-10T04:43:57Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
+	if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
```

  Thinking about it a bit more, the intent behind the `workload-slice-enablement` annotation is to provide a convenient way to disable "elasticity" for a specific job, whether a particular framework or instance, without having to turn off the feature entirely across all types or workloads, while we are in "alpha" phase. 
  
  The idea is that once we’re confident in the functionality, this annotation should be removed and we should rely solely on the feature gate. Maybe that’s what we should have done from the start, but perhaps I’m just being overly cautious and wanted to include an additional “circuit breaker” for safety.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-10T05:37:09Z

- **pkg/controller/core/workload_controller.go:443** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-10T05:37:09Z
```diff
@@ -432,6 +438,32 @@ func (r *WorkloadReconciler) reconcileMaxExecutionTime(ctx context.Context, wl *
 	return 0, nil
 }
 
+// reconcileSliceBasedEviction completes aggregated workload slice life-cycle.
+// If workload slice is active - apply deactivation condition (if needed).
+func (r *WorkloadReconciler) reconcileSliceBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
```

  > Actually, we may not need the finalizing step at all, if we Deactivate or mark the workload as Finished in the same request as we make it Preempted. So, this would be my suggestion, and then we eliminate the issues altogether.
  
  This is a great suggestion, and it makes total sense. Let me take a look at how we can combine those operations.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T15:29:31Z

- **pkg/controller/jobframework/reconciler.go:778** by [@mimowo](https://github.com/mimowo) — 2025-07-10T15:29:31Z
```diff
@@ -754,6 +765,38 @@ func FindAncestorJobManagedByKueue(ctx context.Context, c client.Client, jobObj
 	}
 }
 
+// ensureWorkload ensures that a corresponding Kueue Workload exists for the given job.
+//
+// If workload slicing is enabled for the job, it attempts to manage the job using workload slices.
+// In this mode, only changes to PodSet.Count are considered compatible. Structural or semantic changes
+// to the pod sets result in an "incompatible" slice, and the function falls back to managing the job
+// using the standard "one workload per job" approach.
+//
+// Returns the associated workload (whether slice-based or not), or an error.
+func (r *JobReconciler) ensureWorkload(ctx context.Context, job GenericJob, jobObject client.Object) (*kueue.Workload, error) {
+	// If workload slicing is enabled for this job, use the slice-based processing path.
+	if WorkloadSliceEnabled(job) {
```

  The name might be already suboptimal for the points you mention, and this is precisely the reason I suggested to avoid making the change - in order not to get distracted  :) . If you want to make that change then I would suggest "syncWorkloads" - it could be a preparatory PR. wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T15:35:46Z

- **pkg/controller/jobframework/reconciler.go:1335** by [@mimowo](https://github.com/mimowo) — 2025-07-10T15:35:46Z
```diff
@@ -1240,8 +1330,9 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job Generic
 		}
 	}
 
-	// Wait until there are no active pods.
-	if job.IsActive() {
+	// Wait until there are no active pods, unless this is a workload-slice job.
+	// For workload-slice enabled job we allow for job to be "Active".
+	if job.IsActive() && !WorkloadSliceEnabled(job) {
```

  To be fair I don't understand the motivation for this change, it feels off like plumbing for some scenario, but I'm not sure what.
  
  Imagine a situation when the only workload is deleted. Then, regardless of if the Job is elastic or not I think it makes sense with re-creation of the Workload to avoid re-admission while the old Pods are still existing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T15:36:51Z

- **pkg/controller/jobs/job/job_controller.go:379** by [@mimowo](https://github.com/mimowo) — 2025-07-10T15:36:51Z
```diff
@@ -370,6 +370,11 @@ func SetupIndexes(ctx context.Context, fieldIndexer client.FieldIndexer) error {
 	if err := fieldIndexer.IndexField(ctx, &batchv1.Job{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
 		return err
 	}
+	// Add pod index to be able to list pods for a given job.
+	if err := fieldIndexer.IndexField(ctx, &corev1.Pod{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
+		return err
+	}
```

  Ok, hopefully it is no longer needed if the preemption request already finalizes the old workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T15:39:43Z

- **pkg/workload/workload.go:1112** by [@mimowo](https://github.com/mimowo) — 2025-07-10T15:39:42Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
```

  thx

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-10T15:48:51Z

- **pkg/controller/jobs/job/job_controller.go:379** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-10T15:48:51Z
```diff
@@ -370,6 +370,11 @@ func SetupIndexes(ctx context.Context, fieldIndexer client.FieldIndexer) error {
 	if err := fieldIndexer.IndexField(ctx, &batchv1.Job{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
 		return err
 	}
+	// Add pod index to be able to list pods for a given job.
+	if err := fieldIndexer.IndexField(ctx, &corev1.Pod{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
+		return err
+	}
```

  I think we still need this index to select pods for a given owner in order to clear scheduling gates for admitted workloads. In that sense, it’s part of the core workload-slice functionality. The real question is whether we should guard the index registration behind the feature gate.
  
  I will add it, to avoid confusion.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T15:51:37Z

- **pkg/workload/workload.go:1113** by [@mimowo](https://github.com/mimowo) — 2025-07-10T15:51:37Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
+	if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
```

  > Just to clarify, the annotation is not on the Workload object (nor on the PodSets) but rather on the actual Job object.
  
  please double check. I can see the `kueue.x-k8s.io/workload-preemptible-slice` annotation checked on Workload object [here](https://github.com/kubernetes-sigs/kueue/pull/5510/files#diff-4bf72bf71c4861d2acbd192aee4142392ff8cdbf7c68aa66e0f3fb8f11b38f2fR75-R81).
  
  I think this annotation in fact should be set on Workload when constructing a new Workload for an ElasticJob. Then, I meant to use this annotation for more specific validation inside `ValidateImmutablePodSet`.
  
  > The idea is that once we’re confident in the functionality, this annotation should be removed and we should rely solely on the feature gate. Maybe that’s what we should have done from the start, but perhaps I’m just being overly cautious and wanted to include an additional “circuit breaker” for safety.
  
  I think this refers more to the "kueue.x-k8s.io/elastic-job" annotation on Job. Indeed, I expect this annotation to be required even if the feature ElasticJobsViaWorkloadSlices is Beta (enabled by default). There are two reasons:
  - some users may prefer to re-queue the Job rather than resize, so that it has a chance to change ResourceFlavor. 
  - the mechanism has a signifficant performance cost of using SchedulingGates. This might be an unexpected regression for users who don't need ElasticJobs.
  
  That being said we can re-evaluate this when the feature gets to Beta and has some adoption, then we can react to user's feedback. Maybe we could have a global config change like "AllJobsAreElastic". I prefer to have the annotation for the foreseeable future.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T15:54:37Z

- **pkg/controller/jobframework/reconciler.go:1232** by [@mimowo](https://github.com/mimowo) — 2025-07-10T15:54:36Z
```diff
@@ -1151,6 +1199,46 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindActiveSlices(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
+	case 1:
+		// One active workload found for this job - typically, we are in a scale-up event, where previous
+		// workload is the "old" slice.
+		oldSlice := workloadSlices[0]
+		// Assert that we are processing a new workload-slice.
+		if wl.Name == oldSlice.Name {
+			// This should never happen.
+			// In the context of workload slices, prepareWorkload is only called during the
+			// "create new workload" flow. By definition, that means there should be no existing
+			// workload with the same name.
+			//
+			// If we reach this point—where we're preparing a new workload slice but there's already
+			// an existing workload with the same name—it suggests one of the following:
+			// - prepareWorkload is now being invoked in flows other than creation,
+			// - there is a bug or race condition, or
+			// - both.
+			return fmt.Errorf("unexpected workload-slice name collision: %s", wl.Name)
+		}
+		// Annotate new workload slice with the preemptible (old) workload slice.
+		metav1.SetMetaDataAnnotation(&wl.ObjectMeta, workloadslicing.WorkloadPreemptibleSliceNameKey, string(workload.Key(&oldSlice)))
```

  Instead here, I would prefer to set the annotation when constructing the new Workload from the Job. 
  
  This also eliminates the extra API request to the API server.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-10T16:11:09Z

- **pkg/workload/workload.go:1113** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-10T16:11:09Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
+	if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
```

  I think I see where the confusion is. To recap, we need to relax the immutability of `workload.podSets[].count` only for the **scale-down** event, to allow updating the pod count of an existing workload during downsizing. There's no workload slicing involved in the scale-down scenario.
  
  In contrast, during a **scale-up** event, we don’t modify the existing workload. Instead, we create a new workload slice with updated `podSets[].count` requirements. At that point, we annotate the new workload slice with the "preemptible slice" annotation.
  
  So to summarize:
  
  * `podSets.count` immutability relaxation is only needed for scale-down.
  * During scale-down, no new workload slice is created, so there’s no annotation on the workload itself.
  * Therefore, to determine whether a workload belongs to an elastic job in this context, we need to examine the Job object, which holds the `kueue.x-k8s.io/elastic-job` annotation.
  
  We could consider adding a specific annotation to signal that a workload belongs to an elastic job, allowing us to enforce stricter immutability validation. However, that feels like an unnecessary complication in my opinion.
  
  In particular, such validation could be easily circumvented, for example, by modifying a workload’s annotation even if it doesn’t actually belong to an elastic job. Workload-slice-related annotations are implementation details of the slicing mechanism, not part of the user-facing API, so relying on them for validation logic might be misleading or fragile.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T16:15:33Z

- **pkg/scheduler/scheduler.go:279** by [@mimowo](https://github.com/mimowo) — 2025-07-10T16:15:33Z
```diff
@@ -273,7 +273,10 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		preemptedWorkloads.Insert(e.preemptionTargets)
 		cq.AddUsage(usage)
 
-		if e.assignment.RepresentativeMode() == flavorassigner.Preempt {
+		// When placing a workload slice it is possible for a new workload to "Fit" and
+		// still have a preempted workload (old slice).
+		if mode := e.assignment.RepresentativeMode(); mode == flavorassigner.Preempt ||
+			(mode == flavorassigner.Fit && len(preemptedWorkloads) > 0) {
```

  I would still suggest  (1.), probably it is enough to update the `representativeMode` as [Preempt](https://github.com/kubernetes-sigs/kueue/pull/5510/files#diff-0870ec2c0ea330a7d087a6ef599905bf9b7164a0dd889d1b00ba17c31a1bfd05R646-R658) in this block. Any particular reason not to do it this way?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T16:15:45Z

- **pkg/workload/workloadslicing/workloadslicing.go:18** by [@mimowo](https://github.com/mimowo) — 2025-07-10T16:15:45Z
```diff
@@ -0,0 +1,311 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
```

  thx

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-10T16:20:57Z

- **pkg/controller/jobframework/reconciler.go:1232** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-10T16:20:57Z
```diff
@@ -1151,6 +1199,46 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindActiveSlices(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
+	case 1:
+		// One active workload found for this job - typically, we are in a scale-up event, where previous
+		// workload is the "old" slice.
+		oldSlice := workloadSlices[0]
+		// Assert that we are processing a new workload-slice.
+		if wl.Name == oldSlice.Name {
+			// This should never happen.
+			// In the context of workload slices, prepareWorkload is only called during the
+			// "create new workload" flow. By definition, that means there should be no existing
+			// workload with the same name.
+			//
+			// If we reach this point—where we're preparing a new workload slice but there's already
+			// an existing workload with the same name—it suggests one of the following:
+			// - prepareWorkload is now being invoked in flows other than creation,
+			// - there is a bug or race condition, or
+			// - both.
+			return fmt.Errorf("unexpected workload-slice name collision: %s", wl.Name)
+		}
+		// Annotate new workload slice with the preemptible (old) workload slice.
+		metav1.SetMetaDataAnnotation(&wl.ObjectMeta, workloadslicing.WorkloadPreemptibleSliceNameKey, string(workload.Key(&oldSlice)))
```

  I followed the existing paradigm where `constructWorkload` and `prepareWorkload` are distinct, though successive, steps.
  
  > This also eliminates the extra API request to the API server.
  
  I was initially confused by this as well. In this case, `constructWorkload` doesn’t actually create a Workload object in the Kubernetes API. It just builds an in-memory instance. The object is further parameterized in `prepareWorkload`, and only after that do we make the actual API call to create the Workload.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-10T16:22:22Z

- **pkg/scheduler/scheduler.go:279** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-10T16:22:22Z
```diff
@@ -273,7 +273,10 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		preemptedWorkloads.Insert(e.preemptionTargets)
 		cq.AddUsage(usage)
 
-		if e.assignment.RepresentativeMode() == flavorassigner.Preempt {
+		// When placing a workload slice it is possible for a new workload to "Fit" and
+		// still have a preempted workload (old slice).
+		if mode := e.assignment.RepresentativeMode(); mode == flavorassigner.Preempt ||
+			(mode == flavorassigner.Fit && len(preemptedWorkloads) > 0) {
```

  I am re-working this section to separate "preempted targets" from the "preempted workload slice target" - pls. stand by for the update.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T16:47:50Z

- **pkg/workload/workload.go:1113** by [@mimowo](https://github.com/mimowo) — 2025-07-10T16:47:49Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
+	if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
```

  > I think I see where the confusion is. To recap, we need to relax the immutability of `workload.podSets[].count` only for the **scale-down** event, to allow updating the pod count of an existing workload during downsizing. There's no workload slicing involved in the scale-down scenario.
  
  Got it, in that case should we say:
  
  ```golang
  func validateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
  	if features.Enabled(features.ElasticJobsViaWorkloadSlices) && new.Count < old.Count {
  	        // Allow Scale Downs
  		new.Count = old.Count
  	}
  	return apivalidation.ValidateImmutableField(new, old, path)
  }
  ```
  We can probably allow scale downs unconditionally of `ElasticJobsViaWorkloadSlices`, but it seems ok to also lock it under the gate for now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T16:54:26Z

- **pkg/controller/jobframework/reconciler.go:1232** by [@mimowo](https://github.com/mimowo) — 2025-07-10T16:54:26Z
```diff
@@ -1151,6 +1199,46 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindActiveSlices(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
+	case 1:
+		// One active workload found for this job - typically, we are in a scale-up event, where previous
+		// workload is the "old" slice.
+		oldSlice := workloadSlices[0]
+		// Assert that we are processing a new workload-slice.
+		if wl.Name == oldSlice.Name {
+			// This should never happen.
+			// In the context of workload slices, prepareWorkload is only called during the
+			// "create new workload" flow. By definition, that means there should be no existing
+			// workload with the same name.
+			//
+			// If we reach this point—where we're preparing a new workload slice but there's already
+			// an existing workload with the same name—it suggests one of the following:
+			// - prepareWorkload is now being invoked in flows other than creation,
+			// - there is a bug or race condition, or
+			// - both.
+			return fmt.Errorf("unexpected workload-slice name collision: %s", wl.Name)
+		}
+		// Annotate new workload slice with the preemptible (old) workload slice.
+		metav1.SetMetaDataAnnotation(&wl.ObjectMeta, workloadslicing.WorkloadPreemptibleSliceNameKey, string(workload.Key(&oldSlice)))
```

  I see what you are doing now. I must say I was confused by the meaning of the `WorkloadPreemptibleSliceNameKey` annotation. 
  
  I would prefer to drop it completely. Using the index we can quickly identify the old Workload slice without the annotation. Plus, the annotation may confuse Kueue in case the "old" workload is manually deleted by the user. 
  
  To be more precise I would like this function `PreemptibleWorkloadSliceTarget` to use `workloadslices.FindActiveSlices`. You just need to adjust the interface not to require the entire JobObject, but only the required information. You can retrieve the Namespace, Job UID and Job name from the Workload's ownerReferences. 
  
  I think this will simplify the overall design.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T17:14:19Z

- **pkg/controller/jobs/job/job_controller.go:379** by [@mimowo](https://github.com/mimowo) — 2025-07-10T17:14:19Z
```diff
@@ -370,6 +370,11 @@ func SetupIndexes(ctx context.Context, fieldIndexer client.FieldIndexer) error {
 	if err := fieldIndexer.IndexField(ctx, &batchv1.Job{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
 		return err
 	}
+	// Add pod index to be able to list pods for a given job.
+	if err := fieldIndexer.IndexField(ctx, &corev1.Pod{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
+		return err
+	}
```

  Well, indexing all Pods without a feature gate could indicate a performance hit for users who don't use the feature. 
  
  Indeed, we already have a number of features tracking Pods, like TopologyAwareSchedling or Pods integration. For now, all of them enable tracking of Pods conditionally.
  
  As we graduate TopologyAwareScheduling to Beta or even ElasticJobsViaWorkloadSlices we may need to also index Pods. However, the feature gate will give us extra time to collect feedback from users. Also, if the extra cost is felt by some users while in Beta, they will be able to upgrade Kueue by disabling the features requiring Pod tracking. 
  
  If we hear no issues from users about the extra cost of tracking Pods for a couple of releases, then we may just swallow it, and enable it in future releases.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T03:03:21Z

- **pkg/controller/jobframework/reconciler.go:1232** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T03:03:21Z
```diff
@@ -1151,6 +1199,46 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindActiveSlices(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
+	case 1:
+		// One active workload found for this job - typically, we are in a scale-up event, where previous
+		// workload is the "old" slice.
+		oldSlice := workloadSlices[0]
+		// Assert that we are processing a new workload-slice.
+		if wl.Name == oldSlice.Name {
+			// This should never happen.
+			// In the context of workload slices, prepareWorkload is only called during the
+			// "create new workload" flow. By definition, that means there should be no existing
+			// workload with the same name.
+			//
+			// If we reach this point—where we're preparing a new workload slice but there's already
+			// an existing workload with the same name—it suggests one of the following:
+			// - prepareWorkload is now being invoked in flows other than creation,
+			// - there is a bug or race condition, or
+			// - both.
+			return fmt.Errorf("unexpected workload-slice name collision: %s", wl.Name)
+		}
+		// Annotate new workload slice with the preemptible (old) workload slice.
+		metav1.SetMetaDataAnnotation(&wl.ObjectMeta, workloadslicing.WorkloadPreemptibleSliceNameKey, string(workload.Key(&oldSlice)))
```

  > Using the index we can quickly identify the old Workload slice without the annotation
  
  Since all workload slices have unique names, capturing the name in an annotation gives us a precise way to identify the preempted slice. This lets us locate the already admitted workload as a preemption target directly from the `cache.Snapshot` using `workload.Reference[workloadName]`. I think this approach is more efficient and less complex compared to using the workload owner index (like in `workloadslices.FindActiveSlices`) to search and filter through multiple possible workloads tied to the same Job.  Using `cache.Snapshot` also gives us a consistent view of the world from the scheduler’s perspective, which I think leads to more reliable behavior, even if there's some drift between the API server and the snapshot.
  
  > Plus, the annotation may confuse Kueue in case the "old" workload is manually deleted by the user.
  
  That’s a fair concern, but we do explicitly handle the case where the workload isn’t found in the `cache.Snapshot`, so I don’t think this would introduce more confusion than what we already have when using `workloadslices.FindActiveSlices` under similar conditions.
  
  Since Kueue already relies on annotations as internal details in various parts of the system, using one here to identify the preemptible workload seems consistent. Ideally, I would love to see this information included in the immutable Workload spec, but I realize that’s out of scope for this KEP.
  
  That said, I might be missing something, so I’d really appreciate if you could share more about why you prefer searching for preemptible workloads using the index rather than identifying one directly by name through an annotation.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T03:16:24Z

- **pkg/controller/jobframework/reconciler.go:1335** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T03:16:24Z
```diff
@@ -1240,8 +1330,9 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job Generic
 		}
 	}
 
-	// Wait until there are no active pods.
-	if job.IsActive() {
+	// Wait until there are no active pods, unless this is a workload-slice job.
+	// For workload-slice enabled job we allow for job to be "Active".
+	if job.IsActive() && !WorkloadSliceEnabled(job) {
```

  I could be wrong, but the way I interpret the `if job.IsActive()` block inside `handleJobWithNoWorkload` is as follows:
  
  In the **pre-workload-slice world**, when Kueue detects a Job without an associated Workload, it assumes that the Job must be in a suspended state. This applies in two common scenarios:
  
  * A **new Job** has just been created and doesn’t yet have a Workload. By default, Jobs are created in a suspended state,
  * An **updated Job** that caused the deletion of an outdated Workload is expected to return to a suspended state before proceeding,
  
  In the **workload-slice world**, however, we can encounter a valid situation (during **scale-up**) where we need to create a new workload slice to replace the previous one. This invokes the same path, `handleJobWithNoWorkload`, although a more accurate name in this case would be something like `handleJobWithNoCurrentWorkload`.
  
  The key difference is that during a scale-up event, we **cannot suspend the parent Job**, as doing so would result in deletion of currently active pods. That makes the following check:
  
  ```go
  // Wait until there are no active pods.
  if job.IsActive() {
  	log.V(2).Info("Job is suspended but still has active pods, waiting")
  	return nil
  }
  ```
  
  inapplicable in the workload-slice context. Hence, the proposed change:
  
  ```go
  if job.IsActive() && !WorkloadSliceEnabled(job)
  ```
  
  ensures we preserve the original logic for traditional Jobs while skipping this check for workload-slice-enabled Jobs where it's not appropriate.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T05:08:37Z

- **pkg/controller/jobframework/reconciler.go:1335** by [@mimowo](https://github.com/mimowo) — 2025-07-11T05:08:37Z
```diff
@@ -1240,8 +1330,9 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job Generic
 		}
 	}
 
-	// Wait until there are no active pods.
-	if job.IsActive() {
+	// Wait until there are no active pods, unless this is a workload-slice job.
+	// For workload-slice enabled job we allow for job to be "Active".
+	if job.IsActive() && !WorkloadSliceEnabled(job) {
```

  > In the workload-slice world, however, we can encounter a valid situation (during scale-up) where we need to create a new workload slice to replace the previous one.
  
  Right, but in this case the old still exists. 
  
  I was thinking about the possible case when there is no workload, eg user deleted it manually, I think it is fair to suspend the job and requeue. 
  
  However, since this requires manual user interaction we can call it edge case and reconsider in the future.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T05:53:01Z

- **pkg/controller/jobframework/reconciler.go:1232** by [@mimowo](https://github.com/mimowo) — 2025-07-11T05:53:01Z
```diff
@@ -1151,6 +1199,46 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindActiveSlices(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
+	case 1:
+		// One active workload found for this job - typically, we are in a scale-up event, where previous
+		// workload is the "old" slice.
+		oldSlice := workloadSlices[0]
+		// Assert that we are processing a new workload-slice.
+		if wl.Name == oldSlice.Name {
+			// This should never happen.
+			// In the context of workload slices, prepareWorkload is only called during the
+			// "create new workload" flow. By definition, that means there should be no existing
+			// workload with the same name.
+			//
+			// If we reach this point—where we're preparing a new workload slice but there's already
+			// an existing workload with the same name—it suggests one of the following:
+			// - prepareWorkload is now being invoked in flows other than creation,
+			// - there is a bug or race condition, or
+			// - both.
+			return fmt.Errorf("unexpected workload-slice name collision: %s", wl.Name)
+		}
+		// Annotate new workload slice with the preemptible (old) workload slice.
+		metav1.SetMetaDataAnnotation(&wl.ObjectMeta, workloadslicing.WorkloadPreemptibleSliceNameKey, string(workload.Key(&oldSlice)))
```

  I buy the performance argument. 
  
  It is always a judgmental call performance vs extending the API surface. Annotations are part of API surface so it means long term maintenance. Yes, we use annotations extensively in Kueue and we already have many cases where the annotations are named badly or could be replaced by indexers. 
  
  Since scheduler is very performance sensitive I'm ok with that.
  
  However I was confused by the name, so I would suggest to name it like 'workload-slice-replacement-for' I was even thinking about a more generic name like 'replacement-for'. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T05:59:08Z

- **pkg/controller/jobframework/reconciler.go:1335** by [@mimowo](https://github.com/mimowo) — 2025-07-11T05:59:08Z
```diff
@@ -1240,8 +1330,9 @@ func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, job Generic
 		}
 	}
 
-	// Wait until there are no active pods.
-	if job.IsActive() {
+	// Wait until there are no active pods, unless this is a workload-slice job.
+	// For workload-slice enabled job we allow for job to be "Active".
+	if job.IsActive() && !WorkloadSliceEnabled(job) {
```

  Thank you for explaining, and feel free to resolve.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-16T13:30:09Z

- **pkg/controller/jobframework/reconciler.go:778** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-16T13:30:08Z
```diff
@@ -754,6 +765,38 @@ func FindAncestorJobManagedByKueue(ctx context.Context, c client.Client, jobObj
 	}
 }
 
+// ensureWorkload ensures that a corresponding Kueue Workload exists for the given job.
+//
+// If workload slicing is enabled for the job, it attempts to manage the job using workload slices.
+// In this mode, only changes to PodSet.Count are considered compatible. Structural or semantic changes
+// to the pod sets result in an "incompatible" slice, and the function falls back to managing the job
+// using the standard "one workload per job" approach.
+//
+// Returns the associated workload (whether slice-based or not), or an error.
+func (r *JobReconciler) ensureWorkload(ctx context.Context, job GenericJob, jobObject client.Object) (*kueue.Workload, error) {
+	// If workload slicing is enabled for this job, use the slice-based processing path.
+	if WorkloadSliceEnabled(job) {
```

  Change is reverted, PTAL.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-16T13:30:37Z

- **pkg/controller/jobs/job/job_controller.go:379** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-16T13:30:36Z
```diff
@@ -370,6 +370,11 @@ func SetupIndexes(ctx context.Context, fieldIndexer client.FieldIndexer) error {
 	if err := fieldIndexer.IndexField(ctx, &batchv1.Job{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
 		return err
 	}
+	// Add pod index to be able to list pods for a given job.
+	if err := fieldIndexer.IndexField(ctx, &corev1.Pod{}, indexer.OwnerReferenceUID, indexer.IndexOwnerUID); err != nil {
+		return err
+	}
```

  updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-16T13:31:11Z

- **pkg/scheduler/scheduler.go:279** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-16T13:31:11Z
```diff
@@ -273,7 +273,10 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		preemptedWorkloads.Insert(e.preemptionTargets)
 		cq.AddUsage(usage)
 
-		if e.assignment.RepresentativeMode() == flavorassigner.Preempt {
+		// When placing a workload slice it is possible for a new workload to "Fit" and
+		// still have a preempted workload (old slice).
+		if mode := e.assignment.RepresentativeMode(); mode == flavorassigner.Preempt ||
+			(mode == flavorassigner.Fit && len(preemptedWorkloads) > 0) {
```

  Workload slice aggregation is updated, PTAL.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-16T13:33:12Z

- **pkg/workload/workload.go:1113** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-16T13:33:11Z
```diff
@@ -1104,3 +1107,25 @@ func SetSchedulingStatsEviction(wl *kueue.Workload, newEvictionState kueue.Workl
 	}
 	return false
 }
+
+// ValidateImmutablePodSet helper to validate PodSet immutability on all fields but PodSet.Count.
+func ValidateImmutablePodSet(new, old kueue.PodSet, path *field.Path) field.ErrorList {
+	if features.Enabled(features.ElasticJobsViaWorkloadSlices) {
```

  Updated, PTAL.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-16T13:37:12Z

- **pkg/controller/jobframework/reconciler.go:1232** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-16T13:37:12Z
```diff
@@ -1151,6 +1199,46 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindActiveSlices(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
+	case 1:
+		// One active workload found for this job - typically, we are in a scale-up event, where previous
+		// workload is the "old" slice.
+		oldSlice := workloadSlices[0]
+		// Assert that we are processing a new workload-slice.
+		if wl.Name == oldSlice.Name {
+			// This should never happen.
+			// In the context of workload slices, prepareWorkload is only called during the
+			// "create new workload" flow. By definition, that means there should be no existing
+			// workload with the same name.
+			//
+			// If we reach this point—where we're preparing a new workload slice but there's already
+			// an existing workload with the same name—it suggests one of the following:
+			// - prepareWorkload is now being invoked in flows other than creation,
+			// - there is a bug or race condition, or
+			// - both.
+			return fmt.Errorf("unexpected workload-slice name collision: %s", wl.Name)
+		}
+		// Annotate new workload slice with the preemptible (old) workload slice.
+		metav1.SetMetaDataAnnotation(&wl.ObjectMeta, workloadslicing.WorkloadPreemptibleSliceNameKey, string(workload.Key(&oldSlice)))
```

  Updated, PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T12:18:52Z

- **apis/kueue/v1beta1/constants.go:27** by [@mimowo](https://github.com/mimowo) — 2025-07-17T12:18:52Z
```diff
@@ -19,6 +19,12 @@ package v1beta1
 const (
 	ResourceInUseFinalizerName                 = "kueue.x-k8s.io/resource-in-use"
 	DefaultPodSetName          PodSetReference = "main"
+
+	// WorkloadSliceSchedulingGate is the name of the scheduling gate applied to Pods
+	// to delay their scheduling until the associated workload slice has been admitted.
+	// This gate ensures that Pods do not begin scheduling prematurely, maintaining
+	// proper sequencing in workload processing.
+	WorkloadSliceSchedulingGate = "kueue.x-k8s.io/workload-slice"
```

  ```suggestion
  	ElasticJobSchedulingGate = "kueue.x-k8s.io/elastic-job"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T12:31:59Z

- **apis/kueue/v1beta1/workload_types.go:648** by [@mimowo](https://github.com/mimowo) — 2025-07-17T12:31:59Z
```diff
@@ -639,6 +643,10 @@ const (
 	// due to non-recoverable node failures.
 	WorkloadEvictedDueToNodeFailures = "NodeFailures"
 
+	// WorkloadRemovedViaWorkloadSliceAggregation indicates that the workload instance was
+	// removed (typically from the internal cache only) as a result of workload slice aggregation.
+	WorkloadRemovedViaWorkloadSliceAggregation = "RemovedViaWorkloadSliceAggregation"
```

  ```suggestion
  	// WorkloadSliceReplacement indicates that the workload instance was
  	// replaced with a new slice.
  	WorkloadSliceReplacement = "WorkloadSliceReplacement"
  ```
  To align with KEP, and I think we shouldn't say Removed if it still exists.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T12:40:43Z

- **pkg/controller/jobframework/reconciler.go:1168** by [@mimowo](https://github.com/mimowo) — 2025-07-17T12:40:43Z
```diff
@@ -1129,7 +1160,12 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 		return nil, err
 	}
 
-	wl := NewWorkload(GetWorkloadNameForOwnerWithGVK(object.GetName(), object.GetUID(), job.GVK()), object, podSets, labelKeysToCopy)
+	var wl *kueue.Workload
+	if WorkloadSliceEnabled(job) {
+		wl = NewWorkload(GetWorkloadNameForOwnerWithGVKAndGeneration(object.GetName(), object.GetUID(), job.GVK(), object.GetGeneration()), object, podSets, labelKeysToCopy)
+	} else {
+		wl = NewWorkload(GetWorkloadNameForOwnerWithGVK(object.GetName(), object.GetUID(), job.GVK()), object, podSets, labelKeysToCopy)
+	}
```

  please encapsulate it in a interim function `newWorkload` to avoid code duplication:
  ```golang
  func newWorkloadName(job) string {
    if WorkloadSliceEnabled(job) {
     return GetWorkloadNameForOwnerWithGVKAndGeneration(object.GetName(), object.GetUID(), job.GVK(), object.GetGeneration())
    }
    return GetWorkloadNameForOwnerWithGVK(object.GetName(), object.GetUID(), job.GVK())
  }
  ```
  Then 
  ```golang
  wl := NewWorkload(newWorkloadName(job), object, podSets, labelKeysToCopy)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T12:42:01Z

- **pkg/controller/jobframework/reconciler.go:1205** by [@mimowo](https://github.com/mimowo) — 2025-07-17T12:42:00Z
```diff
@@ -1151,6 +1187,46 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindActiveSlices(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
```

  return nil 
  nit to avoid the need to read what is lower

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T12:45:51Z

- **pkg/workloadslicing/workloadslicing.go:163** by [@mimowo](https://github.com/mimowo) — 2025-07-17T12:45:51Z
```diff
@@ -0,0 +1,314 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"slices"
+	"sort"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Workload slicing refers to a specialized Kueue feature designed to support workload scaling up.
+// A workload slice represents a logical grouping of two workloads:
+// the "old slice" (preemptible workload) and the "new slice" (preemptor workload).
+
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
+const (
+	// EnabledAnnotationKey refers to the annotation key present on Job's that support
+	// workload slicing.
+	EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+	// EnabledAnnotationValue refers to the annotation value. To enable
+	// workload slicing for a given job, we match both annotation key and value.
+	EnabledAnnotationValue = "true"
+)
+
+// Enabled returns true if a provided job has slicing annotation indicator.
+// Indicator matched on both annotation key and value.
+func Enabled(object metav1.Object) bool {
+	if object == nil {
+		return false
+	}
+	annotations := object.GetAnnotations()
+	return len(annotations) > 0 && annotations[EnabledAnnotationKey] == EnabledAnnotationValue
+}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-slice-replacement-for"
+)
+
+// PreemptibleSliceKey returns a preemptible workload slice key if this workload
+// was annotated with such, otherwise, returns an empty string.
+func PreemptibleSliceKey(wl *kueue.Workload) string {
+	annotations := wl.GetAnnotations()
+	if len(annotations) == 0 {
+		return ""
+	}
+	return annotations[WorkloadPreemptibleSliceNameKey]
+}
+
+// Workload predicate definition to match workloads.
+type predicate func(wlSlice kueue.Workload) bool
+
+// not predicate inverts the provided predicate result, used in "exclusion" filters.
+func not(predicate predicate) predicate {
+	return func(wlSlice kueue.Workload) bool {
+		return !predicate(wlSlice)
+	}
+}
+
+// activeSlices predicate matches active non-deleted workload slices.
+func activeSlices() predicate {
+	return func(wlSlice kueue.Workload) bool {
+		return workload.IsActive(&wlSlice) && wlSlice.DeletionTimestamp.IsZero()
+	}
+}
+
+// FindSlices returns a sorted list of active workloads "owned by" the provided job object/gvk combination and
+// matching the provided predicates.
+func findSlices(ctx context.Context, clnt client.Client, jobObject client.Object, jobObjectGVK schema.GroupVersionKind, predicate predicate) ([]kueue.Workload, error) {
+	list := &kueue.WorkloadList{}
+	if err := clnt.List(ctx, list, client.InNamespace(jobObject.GetNamespace()), client.MatchingFields{fmt.Sprintf(".metadata.ownerReferences[%s.%s]", jobObjectGVK.Group, jobObjectGVK.Kind): jobObject.GetName()}); err != nil {
+		return nil, err
+	}
+
+	// Sort workloads by creation timestamp - oldest first.
+	sort.Slice(list.Items, func(i, j int) bool {
+		return list.Items[i].CreationTimestamp.Before(&list.Items[j].CreationTimestamp)
+	})
+
+	// Filter out workloads not matching the provided predicates.
+	// This is an exclusion match, i.e. it removes all entries that do not match the provided predicate.
+	return slices.DeleteFunc(list.Items, not(predicate)), nil
+}
+
+// FindActiveSlices returns a list of active workloads for a given job object/gvk combination.
+func FindActiveSlices(ctx context.Context, clnt client.Client, jobObject client.Object, jobObjectGVK schema.GroupVersionKind) ([]kueue.Workload, error) {
+	return findSlices(ctx, clnt, jobObject, jobObjectGVK, activeSlices())
+}
+
+// finishedOutOfSyncCondition helper returns workload "Finished" condition for updated, but
+// not yet admitted workload slices.
+func finishedOutOfSyncCondition() metav1.Condition {
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  kueue.WorkloadFinishedReasonOutOfSync,
+		Message: "The workload slice is out of sync with its parent job",
+	}
+}
+
+// Deactivate deactivates a given workload slice by setting its "Active" spec field to false.
+// It also applies the specified status condition patches to the workload slice.
+//
+// This function performs the following steps:
+//  1. It patches the spec of the workload slice to mark it as inactive (i.e., setting `Spec.Active` to false)
+//     if the workload slice is currently active.
+//  2. It applies any status conditions provided in the `conditions` parameter to the workload slice's status.
+//
+// Parameters:
+//   - ctx: The context for the operation, used for cancellation and logging.
+//   - clnt: The Kubernetes client used to interact with the API server.
+//   - workloadSlice: The workload slice to deactivate.
+//   - conditions: A variadic list of conditions to apply to the status of the workload slice.
+//
+// Returns:
+//   - error: An error, if any occurred during the process. If no error occurred, nil is returned.
+//
+// This function is useful for deactivating a workload slice and updating its status conditions in one operation.
+func Deactivate(ctx context.Context, clnt client.Client, workloadSlice *kueue.Workload, conditions ...metav1.Condition) error {
+	if err := clientutil.Patch(ctx, clnt, workloadSlice, true, func() (bool, error) {
+		// Patch spec to deactivate (if needed).
+		if workload.IsActive(workloadSlice) {
+			workloadSlice.Spec.Active = ptr.To(false)
+			return true, nil
+		}
+		return false, nil
+	}); err != nil {
+		return fmt.Errorf("failed to patch workload slice: %w", err)
+	}
```

  Why are we deactivating it? I would assume just adding the Finished condition is enough.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T12:50:56Z

- **pkg/scheduler/scheduler.go:314** by [@mimowo](https://github.com/mimowo) — 2025-07-17T12:50:56Z
```diff
@@ -298,6 +307,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			s.cache.WaitForPodsReady(ctx)
 			log.V(5).Info("Finished waiting for all admitted workloads to be in the PodsReady condition")
 		}
+
+		// Evict old workload-slice if any. Note: that oldWorkloadSlice is not nil only if
+		// this is a workload-slice enabled workload and there is an old slice to evict.
+		if oldWorkloadSlice != nil {
+			if err := s.aggregateWorkloadSlice(ctx, oldWorkloadSlice.WorkloadInfo.ClusterQueue, e.Obj, oldWorkloadSlice.WorkloadInfo.Obj.DeepCopy()); err != nil {
```

  ```suggestion
  			if err := s.replaceWorkloadSlice(ctx, oldWorkloadSlice.WorkloadInfo.ClusterQueue, e.Obj, oldWorkloadSlice.WorkloadInfo.Obj.DeepCopy()); err != nil {
  ```
  Or `finishWorkloadSlice`, I don't think we need yet another term "Aggregate". Same in other places using it.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T15:45:26Z

- **pkg/workloadslicing/workloadslicing.go:163** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T15:45:26Z
```diff
@@ -0,0 +1,314 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"slices"
+	"sort"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Workload slicing refers to a specialized Kueue feature designed to support workload scaling up.
+// A workload slice represents a logical grouping of two workloads:
+// the "old slice" (preemptible workload) and the "new slice" (preemptor workload).
+
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
+const (
+	// EnabledAnnotationKey refers to the annotation key present on Job's that support
+	// workload slicing.
+	EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+	// EnabledAnnotationValue refers to the annotation value. To enable
+	// workload slicing for a given job, we match both annotation key and value.
+	EnabledAnnotationValue = "true"
+)
+
+// Enabled returns true if a provided job has slicing annotation indicator.
+// Indicator matched on both annotation key and value.
+func Enabled(object metav1.Object) bool {
+	if object == nil {
+		return false
+	}
+	annotations := object.GetAnnotations()
+	return len(annotations) > 0 && annotations[EnabledAnnotationKey] == EnabledAnnotationValue
+}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-slice-replacement-for"
+)
+
+// PreemptibleSliceKey returns a preemptible workload slice key if this workload
+// was annotated with such, otherwise, returns an empty string.
+func PreemptibleSliceKey(wl *kueue.Workload) string {
+	annotations := wl.GetAnnotations()
+	if len(annotations) == 0 {
+		return ""
+	}
+	return annotations[WorkloadPreemptibleSliceNameKey]
+}
+
+// Workload predicate definition to match workloads.
+type predicate func(wlSlice kueue.Workload) bool
+
+// not predicate inverts the provided predicate result, used in "exclusion" filters.
+func not(predicate predicate) predicate {
+	return func(wlSlice kueue.Workload) bool {
+		return !predicate(wlSlice)
+	}
+}
+
+// activeSlices predicate matches active non-deleted workload slices.
+func activeSlices() predicate {
+	return func(wlSlice kueue.Workload) bool {
+		return workload.IsActive(&wlSlice) && wlSlice.DeletionTimestamp.IsZero()
+	}
+}
+
+// FindSlices returns a sorted list of active workloads "owned by" the provided job object/gvk combination and
+// matching the provided predicates.
+func findSlices(ctx context.Context, clnt client.Client, jobObject client.Object, jobObjectGVK schema.GroupVersionKind, predicate predicate) ([]kueue.Workload, error) {
+	list := &kueue.WorkloadList{}
+	if err := clnt.List(ctx, list, client.InNamespace(jobObject.GetNamespace()), client.MatchingFields{fmt.Sprintf(".metadata.ownerReferences[%s.%s]", jobObjectGVK.Group, jobObjectGVK.Kind): jobObject.GetName()}); err != nil {
+		return nil, err
+	}
+
+	// Sort workloads by creation timestamp - oldest first.
+	sort.Slice(list.Items, func(i, j int) bool {
+		return list.Items[i].CreationTimestamp.Before(&list.Items[j].CreationTimestamp)
+	})
+
+	// Filter out workloads not matching the provided predicates.
+	// This is an exclusion match, i.e. it removes all entries that do not match the provided predicate.
+	return slices.DeleteFunc(list.Items, not(predicate)), nil
+}
+
+// FindActiveSlices returns a list of active workloads for a given job object/gvk combination.
+func FindActiveSlices(ctx context.Context, clnt client.Client, jobObject client.Object, jobObjectGVK schema.GroupVersionKind) ([]kueue.Workload, error) {
+	return findSlices(ctx, clnt, jobObject, jobObjectGVK, activeSlices())
+}
+
+// finishedOutOfSyncCondition helper returns workload "Finished" condition for updated, but
+// not yet admitted workload slices.
+func finishedOutOfSyncCondition() metav1.Condition {
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  kueue.WorkloadFinishedReasonOutOfSync,
+		Message: "The workload slice is out of sync with its parent job",
+	}
+}
+
+// Deactivate deactivates a given workload slice by setting its "Active" spec field to false.
+// It also applies the specified status condition patches to the workload slice.
+//
+// This function performs the following steps:
+//  1. It patches the spec of the workload slice to mark it as inactive (i.e., setting `Spec.Active` to false)
+//     if the workload slice is currently active.
+//  2. It applies any status conditions provided in the `conditions` parameter to the workload slice's status.
+//
+// Parameters:
+//   - ctx: The context for the operation, used for cancellation and logging.
+//   - clnt: The Kubernetes client used to interact with the API server.
+//   - workloadSlice: The workload slice to deactivate.
+//   - conditions: A variadic list of conditions to apply to the status of the workload slice.
+//
+// Returns:
+//   - error: An error, if any occurred during the process. If no error occurred, nil is returned.
+//
+// This function is useful for deactivating a workload slice and updating its status conditions in one operation.
+func Deactivate(ctx context.Context, clnt client.Client, workloadSlice *kueue.Workload, conditions ...metav1.Condition) error {
+	if err := clientutil.Patch(ctx, clnt, workloadSlice, true, func() (bool, error) {
+		// Patch spec to deactivate (if needed).
+		if workload.IsActive(workloadSlice) {
+			workloadSlice.Spec.Active = ptr.To(false)
+			return true, nil
+		}
+		return false, nil
+	}); err != nil {
+		return fmt.Errorf("failed to patch workload slice: %w", err)
+	}
```

  > Why are we deactivating it? I would assume just adding the Finished condition is enough.
  
  So, it seems my misunderstanding stemmed from the side effects of activating the "Finished" condition on the workload. I assumed it would lead to workload deactivation, but it doesn’t appear to have that effect.
  
  Activating the "Finished" condition doesn't deactivate the workload. The current requirement for workload slicing is that only one slice should be active at a time, except during the "scale-up" transition, when two active slices are allowed. Having more than two active slices is currently considered an error (whether transient or otherwise).
  
  We could adjust our workload-slice "active slice" detection logic to avoid relying on the "spec" field and instead solely depend on the presence of the active "status.condition\[Finished]", but that feels somewhat unnatural. Do you see any potential adverse side effects in deactivating aggregated (replaced) workload slices?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T17:22:27Z

- **pkg/scheduler/scheduler.go:314** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T17:22:27Z
```diff
@@ -298,6 +307,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			s.cache.WaitForPodsReady(ctx)
 			log.V(5).Info("Finished waiting for all admitted workloads to be in the PodsReady condition")
 		}
+
+		// Evict old workload-slice if any. Note: that oldWorkloadSlice is not nil only if
+		// this is a workload-slice enabled workload and there is an old slice to evict.
+		if oldWorkloadSlice != nil {
+			if err := s.aggregateWorkloadSlice(ctx, oldWorkloadSlice.WorkloadInfo.ClusterQueue, e.Obj, oldWorkloadSlice.WorkloadInfo.Obj.DeepCopy()); err != nil {
```

  Updated: PTAL @mimowo

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T17:24:47Z

- **pkg/controller/jobframework/reconciler.go:1205** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T17:24:47Z
```diff
@@ -1151,6 +1187,46 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindActiveSlices(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
```

  > return nil nit to avoid the need to read what is lower
  
  I am assuming read by the reviewer since the  `return` statement followed immediately after the `switch`. Either way, it is updated. PTAL, @mimowo

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T17:25:08Z

- **pkg/controller/jobframework/reconciler.go:1168** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T17:25:08Z
```diff
@@ -1129,7 +1160,12 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 		return nil, err
 	}
 
-	wl := NewWorkload(GetWorkloadNameForOwnerWithGVK(object.GetName(), object.GetUID(), job.GVK()), object, podSets, labelKeysToCopy)
+	var wl *kueue.Workload
+	if WorkloadSliceEnabled(job) {
+		wl = NewWorkload(GetWorkloadNameForOwnerWithGVKAndGeneration(object.GetName(), object.GetUID(), job.GVK(), object.GetGeneration()), object, podSets, labelKeysToCopy)
+	} else {
+		wl = NewWorkload(GetWorkloadNameForOwnerWithGVK(object.GetName(), object.GetUID(), job.GVK()), object, podSets, labelKeysToCopy)
+	}
```

  Updated per suggestion, PTAL, @mimowo

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T17:25:30Z

- **apis/kueue/v1beta1/workload_types.go:648** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T17:25:30Z
```diff
@@ -639,6 +643,10 @@ const (
 	// due to non-recoverable node failures.
 	WorkloadEvictedDueToNodeFailures = "NodeFailures"
 
+	// WorkloadRemovedViaWorkloadSliceAggregation indicates that the workload instance was
+	// removed (typically from the internal cache only) as a result of workload slice aggregation.
+	WorkloadRemovedViaWorkloadSliceAggregation = "RemovedViaWorkloadSliceAggregation"
```

  Update per suggestion, PTAL, @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T17:31:32Z

- **pkg/workloadslicing/workloadslicing.go:163** by [@mimowo](https://github.com/mimowo) — 2025-07-17T17:31:32Z
```diff
@@ -0,0 +1,314 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"slices"
+	"sort"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Workload slicing refers to a specialized Kueue feature designed to support workload scaling up.
+// A workload slice represents a logical grouping of two workloads:
+// the "old slice" (preemptible workload) and the "new slice" (preemptor workload).
+
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
+const (
+	// EnabledAnnotationKey refers to the annotation key present on Job's that support
+	// workload slicing.
+	EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+	// EnabledAnnotationValue refers to the annotation value. To enable
+	// workload slicing for a given job, we match both annotation key and value.
+	EnabledAnnotationValue = "true"
+)
+
+// Enabled returns true if a provided job has slicing annotation indicator.
+// Indicator matched on both annotation key and value.
+func Enabled(object metav1.Object) bool {
+	if object == nil {
+		return false
+	}
+	annotations := object.GetAnnotations()
+	return len(annotations) > 0 && annotations[EnabledAnnotationKey] == EnabledAnnotationValue
+}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-slice-replacement-for"
+)
+
+// PreemptibleSliceKey returns a preemptible workload slice key if this workload
+// was annotated with such, otherwise, returns an empty string.
+func PreemptibleSliceKey(wl *kueue.Workload) string {
+	annotations := wl.GetAnnotations()
+	if len(annotations) == 0 {
+		return ""
+	}
+	return annotations[WorkloadPreemptibleSliceNameKey]
+}
+
+// Workload predicate definition to match workloads.
+type predicate func(wlSlice kueue.Workload) bool
+
+// not predicate inverts the provided predicate result, used in "exclusion" filters.
+func not(predicate predicate) predicate {
+	return func(wlSlice kueue.Workload) bool {
+		return !predicate(wlSlice)
+	}
+}
+
+// activeSlices predicate matches active non-deleted workload slices.
+func activeSlices() predicate {
+	return func(wlSlice kueue.Workload) bool {
+		return workload.IsActive(&wlSlice) && wlSlice.DeletionTimestamp.IsZero()
+	}
+}
+
+// FindSlices returns a sorted list of active workloads "owned by" the provided job object/gvk combination and
+// matching the provided predicates.
+func findSlices(ctx context.Context, clnt client.Client, jobObject client.Object, jobObjectGVK schema.GroupVersionKind, predicate predicate) ([]kueue.Workload, error) {
+	list := &kueue.WorkloadList{}
+	if err := clnt.List(ctx, list, client.InNamespace(jobObject.GetNamespace()), client.MatchingFields{fmt.Sprintf(".metadata.ownerReferences[%s.%s]", jobObjectGVK.Group, jobObjectGVK.Kind): jobObject.GetName()}); err != nil {
+		return nil, err
+	}
+
+	// Sort workloads by creation timestamp - oldest first.
+	sort.Slice(list.Items, func(i, j int) bool {
+		return list.Items[i].CreationTimestamp.Before(&list.Items[j].CreationTimestamp)
+	})
+
+	// Filter out workloads not matching the provided predicates.
+	// This is an exclusion match, i.e. it removes all entries that do not match the provided predicate.
+	return slices.DeleteFunc(list.Items, not(predicate)), nil
+}
+
+// FindActiveSlices returns a list of active workloads for a given job object/gvk combination.
+func FindActiveSlices(ctx context.Context, clnt client.Client, jobObject client.Object, jobObjectGVK schema.GroupVersionKind) ([]kueue.Workload, error) {
+	return findSlices(ctx, clnt, jobObject, jobObjectGVK, activeSlices())
+}
+
+// finishedOutOfSyncCondition helper returns workload "Finished" condition for updated, but
+// not yet admitted workload slices.
+func finishedOutOfSyncCondition() metav1.Condition {
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  kueue.WorkloadFinishedReasonOutOfSync,
+		Message: "The workload slice is out of sync with its parent job",
+	}
+}
+
+// Deactivate deactivates a given workload slice by setting its "Active" spec field to false.
+// It also applies the specified status condition patches to the workload slice.
+//
+// This function performs the following steps:
+//  1. It patches the spec of the workload slice to mark it as inactive (i.e., setting `Spec.Active` to false)
+//     if the workload slice is currently active.
+//  2. It applies any status conditions provided in the `conditions` parameter to the workload slice's status.
+//
+// Parameters:
+//   - ctx: The context for the operation, used for cancellation and logging.
+//   - clnt: The Kubernetes client used to interact with the API server.
+//   - workloadSlice: The workload slice to deactivate.
+//   - conditions: A variadic list of conditions to apply to the status of the workload slice.
+//
+// Returns:
+//   - error: An error, if any occurred during the process. If no error occurred, nil is returned.
+//
+// This function is useful for deactivating a workload slice and updating its status conditions in one operation.
+func Deactivate(ctx context.Context, clnt client.Client, workloadSlice *kueue.Workload, conditions ...metav1.Condition) error {
+	if err := clientutil.Patch(ctx, clnt, workloadSlice, true, func() (bool, error) {
+		// Patch spec to deactivate (if needed).
+		if workload.IsActive(workloadSlice) {
+			workloadSlice.Spec.Active = ptr.To(false)
+			return true, nil
+		}
+		return false, nil
+	}); err != nil {
+		return fmt.Errorf("failed to patch workload slice: %w", err)
+	}
```

  Let me maybe clarify, a replaced Wrokload is never going to be used again, right? This by definition matches the semantics of Finished workload.
  
  A Deactivated workload is a workload which is temporarily suspended, but might be activated again, this is not exactly what we have. 
  
  Sure, we can technically make the workload Deactivated and Finished, but Im not clear why. 
  
  Also, it has drawbacks, like an extra request, and more code.
  
  Also, please note there are two ways of deactivating a workload, Kueue controllers should use the DeactivationTarget interim condition so that this way it is compatible with ObjectRetentionPolicy.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T17:36:12Z

- **apis/kueue/v1beta1/workload_types.go:620** by [@mimowo](https://github.com/mimowo) — 2025-07-17T17:36:12Z
```diff
@@ -608,6 +608,10 @@ const (
 	// InCohortReclaimWhileBorrowingReason indicates the Workload was preempted
 	// due to reclamation within the cohort while borrowing.
 	InCohortReclaimWhileBorrowingReason string = "InCohortReclaimWhileBorrowing"
+
+	// WorkloadSliceReplacementReason indicates the Workload instance was
+	// replaced by another workload slice.
+	WorkloadSliceReplacementReason string = "WorkloadSliceReplaced"
```

  As you don't add the "Preempted" condition, please drop it from here, and only keep the one below.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T17:37:16Z

- **apis/kueue/v1beta1/workload_types.go:648** by [@mimowo](https://github.com/mimowo) — 2025-07-17T17:37:16Z
```diff
@@ -639,6 +643,10 @@ const (
 	// due to non-recoverable node failures.
 	WorkloadEvictedDueToNodeFailures = "NodeFailures"
 
+	// WorkloadSliceReplacement indicates that the workload instance was
+	// replaced with a new workload slice.
+	WorkloadSliceReplacement = "WorkloadSliceReplacement"
```

  ```suggestion
  	WorkloadSliceReplaced = "WorkloadSliceReplaced"
  ```
  seems like better indicating the reason

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T17:39:13Z

- **pkg/controller/jobframework/reconciler.go:1205** by [@mimowo](https://github.com/mimowo) — 2025-07-17T17:39:13Z
```diff
@@ -1151,6 +1187,46 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindActiveSlices(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
```

  yes, it was read, lgtm :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T17:40:23Z

- **pkg/controller/jobframework/reconciler.go:778** by [@mimowo](https://github.com/mimowo) — 2025-07-17T17:40:23Z
```diff
@@ -754,6 +765,38 @@ func FindAncestorJobManagedByKueue(ctx context.Context, c client.Client, jobObj
 	}
 }
 
+// ensureWorkload ensures that a corresponding Kueue Workload exists for the given job.
+//
+// If workload slicing is enabled for the job, it attempts to manage the job using workload slices.
+// In this mode, only changes to PodSet.Count are considered compatible. Structural or semantic changes
+// to the pod sets result in an "incompatible" slice, and the function falls back to managing the job
+// using the standard "one workload per job" approach.
+//
+// Returns the associated workload (whether slice-based or not), or an error.
+func (r *JobReconciler) ensureWorkload(ctx context.Context, job GenericJob, jobObject client.Object) (*kueue.Workload, error) {
+	// If workload slicing is enabled for this job, use the slice-based processing path.
+	if WorkloadSliceEnabled(job) {
```

  lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T17:42:58Z

- **pkg/metrics/metrics.go:289** by [@mimowo](https://github.com/mimowo) — 2025-07-17T17:42:58Z
```diff
@@ -255,6 +255,14 @@ The label 'reason' can have the following values:
 		}, []string{"cluster_queue", "reason"},
 	)
 
+	AggregatedWorkloadSlicesTotal = prometheus.NewCounterVec(
+		prometheus.CounterOpts{
+			Subsystem: constants.KueueName,
+			Name:      "aggregated_workload_slices_total",
+			Help:      `The number of aggregated workload slices per 'cluster_queue'`,
+		}, []string{"cluster_queue"},
+	)
```

  ```suggestion
  	ReplacedWorkloadSlicesTotal = prometheus.NewCounterVec(
  		prometheus.CounterOpts{
  			Subsystem: constants.KueueName,
  			Name:      "replaced_workload_slices_total",
  			Help:      `The number of replaced workload slices per 'cluster_queue'`,
  		}, []string{"cluster_queue"},
  	)
  ```
  Let's stick to replacement rather than aggregation consistently.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T17:43:20Z

- **pkg/metrics/metrics.go:511** by [@mimowo](https://github.com/mimowo) — 2025-07-17T17:43:20Z
```diff
@@ -500,6 +508,10 @@ func ReportEvictedWorkloads(cqName kueue.ClusterQueueReference, reason string) {
 	EvictedWorkloadsTotal.WithLabelValues(string(cqName), reason).Inc()
 }
 
+func ReportAggregatedWorkloadSlices(cqName kueue.ClusterQueueReference) {
```

  ```suggestion
  func ReportReplacedWorkloadSlices(cqName kueue.ClusterQueueReference) {
  ```
  nit, just to align naming

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T17:48:51Z

- **pkg/scheduler/scheduler.go:857** by [@mimowo](https://github.com/mimowo) — 2025-07-17T17:48:51Z
```diff
@@ -751,3 +835,37 @@ func (s *Scheduler) recordWorkloadAdmissionEvents(newWorkload, originalWorkload
 		}
 	}
 }
+
+// replaceWorkloadSlice handles the replacement of a workload slice by deactivating the old slice and
+// marking it as finished. It logs the replacement operation, records an event, and reports metrics.
+//
+// This function performs the following steps:
+//  1. Checks if the old workload slice is already finished by inspecting the "Finished" condition in its status.
+//     If the slice is already finished, the function logs a message and returns early.
+//  2. If the old slice is not finished, it deactivates the old slice and marks it with a "Finished" condition,
+//     indicating that the slice was replaced to accommodate the new workload slice.
+//  3. The function logs details about the replacement, including the reason for the removal and the associated message.
+//  4. An event is recorded for the old slice to indicate that the slice was aggregated (replaced) by the new slice.
+//  5. The function reports metrics for the aggregation of workload slices for the old queue.
+func (s *Scheduler) replaceWorkloadSlice(ctx context.Context, oldQueue kueue.ClusterQueueReference, newSlice, oldSlice *kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	if meta.IsStatusConditionTrue(oldSlice.Status.Conditions, kueue.WorkloadFinished) {
+		log.V(3).Info("Workload slice already aggregated", "old-slice", klog.KObj(oldSlice), "new-slice", klog.KObj(newSlice))
+		return nil
+	}
+	reason := kueue.WorkloadSliceReplacement
+	message := fmt.Sprintf("Removed to accommodate a workload (UID: %s, JobUID: %s) due to workload slice aggregation", newSlice.UID, newSlice.Labels[controllerconstants.JobUIDLabel])
```

  ```suggestion
  	message := fmt.Sprintf("Replaced to accommodate a workload (UID: %s, JobUID: %s) due to workload slice aggregation", newSlice.UID, newSlice.Labels[controllerconstants.JobUIDLabel])
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:09:01Z

- **pkg/scheduler/scheduler.go:282** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:09:01Z
```diff
@@ -273,10 +276,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		preemptedWorkloads.Insert(e.preemptionTargets)
 		cq.AddUsage(usage)
 
+		// Filter out the old workload slice from the preemption targets.
+		// The old workload slice is initially included in the preemption targets because it is treated
+		// as a preemptible target during flavor assignment. However, it should be evicted rather than preempted.
+		// Note: it is valid for either or both preemptionTargets and oldWorkloadSlice to be nil.
+		preemptionTargets, oldWorkloadSlice := findPreemptedSliceTarget(e.Obj, e.preemptionTargets)
```

  I see this is a bit of a workaround just to prevent calling IssuePreemptions for the workload. 
  
  TBH, I imagined the change differently - call IssuePreemptions, and let the workload get the "Preempted" condition, but additionally append "Finished" conditions. 
  
  I think the amount of extra code added to circumvent setting "Preempted" condition is making it questionable. Also, it means the handling of replacement is not done concurrently - as IssuePreemptions handles nicely sending concurrent requests, wdyt?
  
  EDIT: To clarify, I'm ok going the path of circumventing setting the Preempted condition, then I'm ok with that, but I'm interested to know better the motivation for this approach.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T18:09:14Z

- **apis/kueue/v1beta1/workload_types.go:620** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T18:09:14Z
```diff
@@ -608,6 +608,10 @@ const (
 	// InCohortReclaimWhileBorrowingReason indicates the Workload was preempted
 	// due to reclamation within the cohort while borrowing.
 	InCohortReclaimWhileBorrowingReason string = "InCohortReclaimWhileBorrowing"
+
+	// WorkloadSliceReplacementReason indicates the Workload instance was
+	// replaced by another workload slice.
+	WorkloadSliceReplacementReason string = "WorkloadSliceReplaced"
```

  Not sure I follow, I don't see "Preempted" (any more) - may be the comment is out-of-sync w/ updates.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T18:10:25Z

- **apis/kueue/v1beta1/workload_types.go:648** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T18:10:25Z
```diff
@@ -639,6 +643,10 @@ const (
 	// due to non-recoverable node failures.
 	WorkloadEvictedDueToNodeFailures = "NodeFailures"
 
+	// WorkloadSliceReplacement indicates that the workload instance was
+	// replaced with a new workload slice.
+	WorkloadSliceReplacement = "WorkloadSliceReplacement"
```

  Good suggestion, however, "big" problem with GitHub code suggestions for constants - it doesn't container the constant usage refactoring, thus, "Commit suggestion" option results in a broken build. :(

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:24:23Z

- **apis/kueue/v1beta1/workload_types.go:620** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:24:23Z
```diff
@@ -608,6 +608,10 @@ const (
 	// InCohortReclaimWhileBorrowingReason indicates the Workload was preempted
 	// due to reclamation within the cohort while borrowing.
 	InCohortReclaimWhileBorrowingReason string = "InCohortReclaimWhileBorrowing"
+
+	// WorkloadSliceReplacementReason indicates the Workload instance was
+	// replaced by another workload slice.
+	WorkloadSliceReplacementReason string = "WorkloadSliceReplaced"
```

  I was referring to the declaration of the constant for the reason - I think it is no longer needed as we now don't use the "Preempted" condition:
  
  <img width="1039" height="258" alt="image" src="https://github.com/user-attachments/assets/a9786a45-4271-4ca8-a977-2bab0a56ca19" />

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:25:55Z

- **pkg/scheduler/preemption/preemption.go:139** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:25:55Z
```diff
@@ -135,7 +136,8 @@ var HumanReadablePreemptionReasons = map[string]string{
 	kueue.InCohortReclamationReason:           "reclamation within the cohort",
 	kueue.InCohortFairSharingReason:           "Fair Sharing within the cohort",
 	kueue.InCohortReclaimWhileBorrowingReason: "reclamation within the cohort while borrowing",
-	"": "UNKNOWN",
+	kueue.WorkloadSliceReplacementReason:      "workload slice aggregation",
```

  I think this entry is also no longer needed since the code no longer is using the "Preempted" condition.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:30:26Z

- **pkg/scheduler/scheduler.go:314** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:30:25Z
```diff
@@ -298,6 +307,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			s.cache.WaitForPodsReady(ctx)
 			log.V(5).Info("Finished waiting for all admitted workloads to be in the PodsReady condition")
 		}
+
+		// Evict old workload-slice if any. Note: that oldWorkloadSlice is not nil only if
+		// this is a workload-slice enabled workload and there is an old slice to evict.
+		if oldWorkloadSlice != nil {
+			if err := s.replaceWorkloadSlice(ctx, oldWorkloadSlice.WorkloadInfo.ClusterQueue, e.Obj, oldWorkloadSlice.WorkloadInfo.Obj.DeepCopy()); err != nil {
```

  ```suggestion
  			if err := s.replaceWorkloadSlice(ctx, oldWorkloadSlice.WorkloadInfo.ClusterQueue, e.Obj, oldWorkloadSlice.WorkloadInfo.Obj); err != nil {
  ```
  It does not seem needed as the function `replaceWorkloadSlice` is not mutating the object.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T18:37:11Z

- **pkg/scheduler/scheduler.go:282** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T18:37:11Z
```diff
@@ -273,10 +276,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		preemptedWorkloads.Insert(e.preemptionTargets)
 		cq.AddUsage(usage)
 
+		// Filter out the old workload slice from the preemption targets.
+		// The old workload slice is initially included in the preemption targets because it is treated
+		// as a preemptible target during flavor assignment. However, it should be evicted rather than preempted.
+		// Note: it is valid for either or both preemptionTargets and oldWorkloadSlice to be nil.
+		preemptionTargets, oldWorkloadSlice := findPreemptedSliceTarget(e.Obj, e.preemptionTargets)
```

  > TBH, I imagined the change differently - call IssuePreemptions, and let the workload get the "Preempted" condition, but additionally append "Finished" conditions.
  
  That was my initial thought as well. However, there were a couple of issues that made this approach unworkable:
  
  1. As we discussed earlier, the terms "Preemption" and "Eviction" don't align well with the actual workload slice replacement functionality.
  2. The [workload.AdmissionStatusPatch](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload.go#L789) used in `IssuePreemptions` does not account for the "Finished" condition, which is not one of the [supported admission conditions](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload.go#L64). As a result, we would have to apply the "Finished" condition as a separate patch.
  
  In the grand scheme, `IssuePreemptions` wasn't a good fit for this update, and the current approach actually works out better. We don't need to handle "Preempt", "Evict", or reset any admission checks. Additionally, we don't need to deal with multiple preemption targets (since only one workload is being replaced). Instead, we simply deactivate the workload and apply the "Finished" condition.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:41:57Z

- **pkg/workloadslicing/workloadslicing.go:76** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:41:56Z
```diff
@@ -0,0 +1,314 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"slices"
+	"sort"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Workload slicing refers to a specialized Kueue feature designed to support workload scaling up.
+// A workload slice represents a logical grouping of two workloads:
+// the "old slice" (preemptible workload) and the "new slice" (preemptor workload).
+
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
+const (
+	// EnabledAnnotationKey refers to the annotation key present on Job's that support
+	// workload slicing.
+	EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+	// EnabledAnnotationValue refers to the annotation value. To enable
+	// workload slicing for a given job, we match both annotation key and value.
+	EnabledAnnotationValue = "true"
+)
+
+// Enabled returns true if a provided job has slicing annotation indicator.
+// Indicator matched on both annotation key and value.
+func Enabled(object metav1.Object) bool {
+	if object == nil {
+		return false
+	}
+	annotations := object.GetAnnotations()
+	return len(annotations) > 0 && annotations[EnabledAnnotationKey] == EnabledAnnotationValue
+}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-slice-replacement-for"
+)
+
+// PreemptibleSliceKey returns a preemptible workload slice key if this workload
+// was annotated with such, otherwise, returns an empty string.
+func PreemptibleSliceKey(wl *kueue.Workload) string {
```

  ```suggestion
  func PreemptibleSliceKey(wl *kueue.Workload) *workload.Reference {
  ```
  nit, but I would prefer strong typing, it seems easier to use then

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:46:11Z

- **pkg/scheduler/scheduler.go:512** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:46:11Z
```diff
@@ -462,20 +481,85 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || wl == nil || snap == nil {
+		return nil, nil
+	}
+	if workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl); workloadSlicePreemptionTarget != nil {
+		return []*preemption.Target{workloadSlicePreemptionTarget}, workloadSlicePreemptionTarget.WorkloadInfo
+	}
+	return nil, nil
+}
+
+// findPreemptedSliceTarget scans the provided list of preemption targets and removes
+// the one marked with the WorkloadSliceReplacementReason, if present.
+//
+// It returns a new slice with the preemptable workload slice target removed, along with
+// the removed target itself. If no such target is found, it returns the original slice and nil.
+//
+// This is typically used to separate the special-case handling of preempted workload slices
+// from general preemption logic.
+func findPreemptedSliceTarget(preemptor *kueue.Workload, targets []*preemption.Target) ([]*preemption.Target, *preemption.Target) {
+	sliceKey := workloadslicing.PreemptibleSliceKey(preemptor)
```

  Guard by the feature gate additionally.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:46:39Z

- **pkg/scheduler/scheduler.go:313** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:46:38Z
```diff
@@ -298,6 +307,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			s.cache.WaitForPodsReady(ctx)
 			log.V(5).Info("Finished waiting for all admitted workloads to be in the PodsReady condition")
 		}
+
+		// Evict old workload-slice if any. Note: that oldWorkloadSlice is not nil only if
+		// this is a workload-slice enabled workload and there is an old slice to evict.
+		if oldWorkloadSlice != nil {
```

  ```suggestion
  		if features.Enabled(features.ElasticJobsViaWorkloadSlices)  && oldWorkloadSlice != nil {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:47:12Z

- **pkg/scheduler/scheduler.go:853** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:47:11Z
```diff
@@ -751,3 +835,37 @@ func (s *Scheduler) recordWorkloadAdmissionEvents(newWorkload, originalWorkload
 		}
 	}
 }
+
+// replaceWorkloadSlice handles the replacement of a workload slice by deactivating the old slice and
+// marking it as finished. It logs the replacement operation, records an event, and reports metrics.
+//
+// This function performs the following steps:
+//  1. Checks if the old workload slice is already finished by inspecting the "Finished" condition in its status.
+//     If the slice is already finished, the function logs a message and returns early.
+//  2. If the old slice is not finished, it deactivates the old slice and marks it with a "Finished" condition,
+//     indicating that the slice was replaced to accommodate the new workload slice.
+//  3. The function logs details about the replacement, including the reason for the removal and the associated message.
+//  4. An event is recorded for the old slice to indicate that the slice was aggregated (replaced) by the new slice.
+//  5. The function reports metrics for the aggregation of workload slices for the old queue.
+func (s *Scheduler) replaceWorkloadSlice(ctx context.Context, oldQueue kueue.ClusterQueueReference, newSlice, oldSlice *kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	if meta.IsStatusConditionTrue(oldSlice.Status.Conditions, kueue.WorkloadFinished) {
+		log.V(3).Info("Workload slice already aggregated", "old-slice", klog.KObj(oldSlice), "new-slice", klog.KObj(newSlice))
```

  ```suggestion
  		log.V(3).Info("Workload slice already finished", "old-slice", klog.KObj(oldSlice), "new-slice", klog.KObj(newSlice))
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:50:52Z

- **pkg/scheduler/scheduler.go:282** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:50:52Z
```diff
@@ -273,10 +276,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		preemptedWorkloads.Insert(e.preemptionTargets)
 		cq.AddUsage(usage)
 
+		// Filter out the old workload slice from the preemption targets.
+		// The old workload slice is initially included in the preemption targets because it is treated
+		// as a preemptible target during flavor assignment. However, it should be evicted rather than preempted.
+		// Note: it is valid for either or both preemptionTargets and oldWorkloadSlice to be nil.
+		preemptionTargets, oldWorkloadSlice := findPreemptedSliceTarget(e.Obj, e.preemptionTargets)
```

  > The workload.AdmissionStatusPatch used in IssuePreemptions does not account for the "Finished" condition, which is not one of the supported admission conditions. As a result, we would have to apply the "Finished" condition as a separate patch.
  
  Oh, interesting finding - I didn't know that, I would assume adding the Fisnished condition to the list would be enough, but I agree this is a bigger change conceptually, and I'm not fully sure about its consequences.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T18:52:34Z

- **pkg/scheduler/scheduler.go:282** by [@mimowo](https://github.com/mimowo) — 2025-07-17T18:52:33Z
```diff
@@ -273,10 +276,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 		preemptedWorkloads.Insert(e.preemptionTargets)
 		cq.AddUsage(usage)
 
+		// Filter out the old workload slice from the preemption targets.
+		// The old workload slice is initially included in the preemption targets because it is treated
+		// as a preemptible target during flavor assignment. However, it should be evicted rather than preempted.
+		// Note: it is valid for either or both preemptionTargets and oldWorkloadSlice to be nil.
+		preemptionTargets, oldWorkloadSlice := findPreemptedSliceTarget(e.Obj, e.preemptionTargets)
```

  > Instead, we simply deactivate the workload and apply the "Finished" condition.
  
  Ok, so this deactivate is my remaining worry, but I also have a thread for this here: https://github.com/kubernetes-sigs/kueue/pull/5510#discussion_r2213249971, so feel free to consider this as resolved.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T19:08:03Z

- **pkg/scheduler/scheduler.go:512** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T19:08:03Z
```diff
@@ -462,20 +481,85 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || wl == nil || snap == nil {
+		return nil, nil
+	}
+	if workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl); workloadSlicePreemptionTarget != nil {
+		return []*preemption.Target{workloadSlicePreemptionTarget}, workloadSlicePreemptionTarget.WorkloadInfo
+	}
+	return nil, nil
+}
+
+// findPreemptedSliceTarget scans the provided list of preemption targets and removes
+// the one marked with the WorkloadSliceReplacementReason, if present.
+//
+// It returns a new slice with the preemptable workload slice target removed, along with
+// the removed target itself. If no such target is found, it returns the original slice and nil.
+//
+// This is typically used to separate the special-case handling of preempted workload slices
+// from general preemption logic.
+func findPreemptedSliceTarget(preemptor *kueue.Workload, targets []*preemption.Target) ([]*preemption.Target, *preemption.Target) {
+	sliceKey := workloadslicing.PreemptibleSliceKey(preemptor)
```

  In this and the next comment, we’re already guarded by the feature gate when generating `[]*preemption.Targets`. For example, if the feature gate is **not** enabled, the preemption targets will not include the old slice, unless it is a legitimate preemption target outside of workload slice replacement for other workloads. Given this, I think it’s sufficient. What do you think?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-17T19:08:23Z

- **pkg/scheduler/scheduler.go:313** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-17T19:08:23Z
```diff
@@ -298,6 +307,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			s.cache.WaitForPodsReady(ctx)
 			log.V(5).Info("Finished waiting for all admitted workloads to be in the PodsReady condition")
 		}
+
+		// Evict old workload-slice if any. Note: that oldWorkloadSlice is not nil only if
+		// this is a workload-slice enabled workload and there is an old slice to evict.
+		if oldWorkloadSlice != nil {
```

  see https://github.com/kubernetes-sigs/kueue/pull/5510#discussion_r2214087399

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T19:15:55Z

- **pkg/scheduler/scheduler.go:512** by [@mimowo](https://github.com/mimowo) — 2025-07-17T19:15:55Z
```diff
@@ -462,20 +481,85 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || wl == nil || snap == nil {
+		return nil, nil
+	}
+	if workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl); workloadSlicePreemptionTarget != nil {
+		return []*preemption.Target{workloadSlicePreemptionTarget}, workloadSlicePreemptionTarget.WorkloadInfo
+	}
+	return nil, nil
+}
+
+// findPreemptedSliceTarget scans the provided list of preemption targets and removes
+// the one marked with the WorkloadSliceReplacementReason, if present.
+//
+// It returns a new slice with the preemptable workload slice target removed, along with
+// the removed target itself. If no such target is found, it returns the original slice and nil.
+//
+// This is typically used to separate the special-case handling of preempted workload slices
+// from general preemption logic.
+func findPreemptedSliceTarget(preemptor *kueue.Workload, targets []*preemption.Target) ([]*preemption.Target, *preemption.Target) {
+	sliceKey := workloadslicing.PreemptibleSliceKey(preemptor)
```

  I understand, but I think it is still better to do the extra guard because:
  - extra guard improves readability for someone who is not very familiar with the feature. Suppose someone is debugging an incident, this person can immediately stop reading the code at this point. Otherwise the person needs to traceback one layer earlier to prove that this branch of code is not used
  - code evolves and some non-obvious dependencies between code in different packages may change over time and be overlooked during review

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-18T14:36:56Z

- **pkg/scheduler/scheduler.go:313** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-18T14:36:56Z
```diff
@@ -298,6 +307,16 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 			s.cache.WaitForPodsReady(ctx)
 			log.V(5).Info("Finished waiting for all admitted workloads to be in the PodsReady condition")
 		}
+
+		// Evict old workload-slice if any. Note: that oldWorkloadSlice is not nil only if
+		// this is a workload-slice enabled workload and there is an old slice to evict.
+		if oldWorkloadSlice != nil {
```

  Updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-18T14:37:11Z

- **pkg/scheduler/scheduler.go:512** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-18T14:37:11Z
```diff
@@ -462,20 +481,85 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || wl == nil || snap == nil {
+		return nil, nil
+	}
+	if workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl); workloadSlicePreemptionTarget != nil {
+		return []*preemption.Target{workloadSlicePreemptionTarget}, workloadSlicePreemptionTarget.WorkloadInfo
+	}
+	return nil, nil
+}
+
+// findPreemptedSliceTarget scans the provided list of preemption targets and removes
+// the one marked with the WorkloadSliceReplacementReason, if present.
+//
+// It returns a new slice with the preemptable workload slice target removed, along with
+// the removed target itself. If no such target is found, it returns the original slice and nil.
+//
+// This is typically used to separate the special-case handling of preempted workload slices
+// from general preemption logic.
+func findPreemptedSliceTarget(preemptor *kueue.Workload, targets []*preemption.Target) ([]*preemption.Target, *preemption.Target) {
+	sliceKey := workloadslicing.PreemptibleSliceKey(preemptor)
```

  Agreed, updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-18T14:37:46Z

- **pkg/scheduler/scheduler.go:853** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-18T14:37:46Z
```diff
@@ -751,3 +835,37 @@ func (s *Scheduler) recordWorkloadAdmissionEvents(newWorkload, originalWorkload
 		}
 	}
 }
+
+// replaceWorkloadSlice handles the replacement of a workload slice by deactivating the old slice and
+// marking it as finished. It logs the replacement operation, records an event, and reports metrics.
+//
+// This function performs the following steps:
+//  1. Checks if the old workload slice is already finished by inspecting the "Finished" condition in its status.
+//     If the slice is already finished, the function logs a message and returns early.
+//  2. If the old slice is not finished, it deactivates the old slice and marks it with a "Finished" condition,
+//     indicating that the slice was replaced to accommodate the new workload slice.
+//  3. The function logs details about the replacement, including the reason for the removal and the associated message.
+//  4. An event is recorded for the old slice to indicate that the slice was aggregated (replaced) by the new slice.
+//  5. The function reports metrics for the aggregation of workload slices for the old queue.
+func (s *Scheduler) replaceWorkloadSlice(ctx context.Context, oldQueue kueue.ClusterQueueReference, newSlice, oldSlice *kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	if meta.IsStatusConditionTrue(oldSlice.Status.Conditions, kueue.WorkloadFinished) {
+		log.V(3).Info("Workload slice already aggregated", "old-slice", klog.KObj(oldSlice), "new-slice", klog.KObj(newSlice))
```

  Updated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T14:40:32Z

- **pkg/controller/jobframework/reconciler.go:1168** by [@mimowo](https://github.com/mimowo) — 2025-07-18T14:40:32Z
```diff
@@ -1129,7 +1160,12 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 		return nil, err
 	}
 
-	wl := NewWorkload(GetWorkloadNameForOwnerWithGVK(object.GetName(), object.GetUID(), job.GVK()), object, podSets, labelKeysToCopy)
+	var wl *kueue.Workload
+	if WorkloadSliceEnabled(job) {
+		wl = NewWorkload(GetWorkloadNameForOwnerWithGVKAndGeneration(object.GetName(), object.GetUID(), job.GVK(), object.GetGeneration()), object, podSets, labelKeysToCopy)
+	} else {
+		wl = NewWorkload(GetWorkloadNameForOwnerWithGVK(object.GetName(), object.GetUID(), job.GVK()), object, podSets, labelKeysToCopy)
+	}
```

  lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T14:41:40Z

- **apis/kueue/v1beta1/constants.go:23** by [@mimowo](https://github.com/mimowo) — 2025-07-18T14:41:39Z
```diff
@@ -19,6 +19,12 @@ package v1beta1
 const (
 	ResourceInUseFinalizerName                 = "kueue.x-k8s.io/resource-in-use"
 	DefaultPodSetName          PodSetReference = "main"
+
+	// WorkloadSliceSchedulingGate is the name of the scheduling gate applied to Pods
```

  ```suggestion
  	// ElasticJobSchedulingGate is the name of the scheduling gate applied to Pods
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T14:42:48Z

- **apis/kueue/v1beta1/workload_types.go:620** by [@mimowo](https://github.com/mimowo) — 2025-07-18T14:42:48Z
```diff
@@ -608,6 +608,10 @@ const (
 	// InCohortReclaimWhileBorrowingReason indicates the Workload was preempted
 	// due to reclamation within the cohort while borrowing.
 	InCohortReclaimWhileBorrowingReason string = "InCohortReclaimWhileBorrowing"
+
+	// WorkloadSliceReplacementReason indicates the Workload instance was
+	// replaced by another workload slice.
+	WorkloadSliceReplacementReason string = "WorkloadSliceReplaced"
```

  The comment requesting to drop it is resolved, but it is still there, ptal.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T14:48:16Z

- **pkg/workloadslicing/workloadslicing.go:70** by [@mimowo](https://github.com/mimowo) — 2025-07-18T14:48:15Z
```diff
@@ -0,0 +1,270 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"slices"
+	"sort"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Workload slicing refers to a specialized Kueue feature designed to support workload scaling up.
+// A workload slice represents a logical grouping of two workloads:
+// the "old slice" (preemptible workload) and the "new slice" (preemptor workload).
+
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
+const (
+	// EnabledAnnotationKey refers to the annotation key present on Job's that support
+	// workload slicing.
+	EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+	// EnabledAnnotationValue refers to the annotation value. To enable
+	// workload slicing for a given job, we match both annotation key and value.
+	EnabledAnnotationValue = "true"
+)
+
+// Enabled returns true if a provided job has slicing annotation indicator.
+// Indicator matched on both annotation key and value.
+func Enabled(object metav1.Object) bool {
+	if object == nil {
+		return false
+	}
+	annotations := object.GetAnnotations()
+	return len(annotations) > 0 && annotations[EnabledAnnotationKey] == EnabledAnnotationValue
+}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-slice-replacement-for"
```

  ```suggestion
  	WorkloadSliceReplacementForKey = "kueue.x-k8s.io/workload-slice-replacement-for"
  ```
  and also rename in comment

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T14:48:49Z

- **pkg/workloadslicing/workloadslicing.go:64** by [@mimowo](https://github.com/mimowo) — 2025-07-18T14:48:49Z
```diff
@@ -0,0 +1,270 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"slices"
+	"sort"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Workload slicing refers to a specialized Kueue feature designed to support workload scaling up.
+// A workload slice represents a logical grouping of two workloads:
+// the "old slice" (preemptible workload) and the "new slice" (preemptor workload).
+
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
+const (
+	// EnabledAnnotationKey refers to the annotation key present on Job's that support
+	// workload slicing.
+	EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+	// EnabledAnnotationValue refers to the annotation value. To enable
+	// workload slicing for a given job, we match both annotation key and value.
+	EnabledAnnotationValue = "true"
+)
+
+// Enabled returns true if a provided job has slicing annotation indicator.
+// Indicator matched on both annotation key and value.
+func Enabled(object metav1.Object) bool {
+	if object == nil {
+		return false
+	}
+	annotations := object.GetAnnotations()
+	return len(annotations) > 0 && annotations[EnabledAnnotationKey] == EnabledAnnotationValue
```

  ```suggestion
  	return object.GetAnnotations()[EnabledAnnotationKey] == EnabledAnnotationValue
  ```
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T14:50:34Z

- **test/integration/singlecluster/webhook/core/workload_test.go:971** by [@mimowo](https://github.com/mimowo) — 2025-07-18T14:50:34Z
```diff
@@ -966,5 +967,27 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 				g.Expect(k8sClient.Status().Update(ctx, wl)).Should(testing.BeForbiddenError())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should allow workload podSets count update when ElasticJobsViaWorkloadSlices feature gate is enabled", func() {
```

  Please also add an integration test for Workload down size.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T14:52:07Z

- **pkg/workloadslicing/workloadslicing.go:96** by [@mimowo](https://github.com/mimowo) — 2025-07-18T14:52:07Z
```diff
@@ -0,0 +1,270 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"slices"
+	"sort"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Workload slicing refers to a specialized Kueue feature designed to support workload scaling up.
+// A workload slice represents a logical grouping of two workloads:
+// the "old slice" (preemptible workload) and the "new slice" (preemptor workload).
+
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
+const (
+	// EnabledAnnotationKey refers to the annotation key present on Job's that support
+	// workload slicing.
+	EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+	// EnabledAnnotationValue refers to the annotation value. To enable
+	// workload slicing for a given job, we match both annotation key and value.
+	EnabledAnnotationValue = "true"
+)
+
+// Enabled returns true if a provided job has slicing annotation indicator.
+// Indicator matched on both annotation key and value.
+func Enabled(object metav1.Object) bool {
+	if object == nil {
+		return false
+	}
+	annotations := object.GetAnnotations()
+	return len(annotations) > 0 && annotations[EnabledAnnotationKey] == EnabledAnnotationValue
+}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-slice-replacement-for"
+)
+
+// PreemptibleSliceKey returns a preemptible workload slice key if this workload
+// was annotated with such, otherwise, returns an empty string.
+func PreemptibleSliceKey(wl *kueue.Workload) *workload.Reference {
+	annotations := wl.GetAnnotations()
+	if len(annotations) == 0 {
+		return nil
+	}
+	key, found := annotations[WorkloadPreemptibleSliceNameKey]
+	if !found {
+		return nil
+	}
+	ref := workload.Reference(key)
+	return &ref
+}
+
+// Deactivate updates the status of a workload slice by applying the "Finished" condition.
+// The function checks if the "Finished" condition is already applied, and if so, does nothing (NOOP).
+// If the "Finished" condition is not present, it applies the condition with the provided `reason` and `message`.
+//
+// This function performs the following:
+// 1. It checks if the "Finished" condition is already applied. If true, it returns immediately, doing nothing.
+// 2. If the "Finished" condition is not set, it patches the workload slice's status to add the "Finished" condition.
+// 3. If the patch fails, it returns an error.
+func Deactivate(ctx context.Context, clnt client.Client, workloadSlice *kueue.Workload, reason, message string) error {
```

  ```suggestion
  func Finish(ctx context.Context, clnt client.Client, workloadSlice *kueue.Workload, reason, message string) error {
  ```

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-18T22:22:44Z

- **pkg/workloadslicing/workloadslicing.go:163** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-18T22:22:43Z
```diff
@@ -0,0 +1,314 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+// Package workloadslicing contains definitions to support workload slicing functionality.
+package workloadslicing
+
+import (
+	"context"
+	"errors"
+	"fmt"
+	"slices"
+	"sort"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime/schema"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	clientutil "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Workload slicing refers to a specialized Kueue feature designed to support workload scaling up.
+// A workload slice represents a logical grouping of two workloads:
+// the "old slice" (preemptible workload) and the "new slice" (preemptor workload).
+
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
+const (
+	// EnabledAnnotationKey refers to the annotation key present on Job's that support
+	// workload slicing.
+	EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+	// EnabledAnnotationValue refers to the annotation value. To enable
+	// workload slicing for a given job, we match both annotation key and value.
+	EnabledAnnotationValue = "true"
+)
+
+// Enabled returns true if a provided job has slicing annotation indicator.
+// Indicator matched on both annotation key and value.
+func Enabled(object metav1.Object) bool {
+	if object == nil {
+		return false
+	}
+	annotations := object.GetAnnotations()
+	return len(annotations) > 0 && annotations[EnabledAnnotationKey] == EnabledAnnotationValue
+}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-slice-replacement-for"
+)
+
+// PreemptibleSliceKey returns a preemptible workload slice key if this workload
+// was annotated with such, otherwise, returns an empty string.
+func PreemptibleSliceKey(wl *kueue.Workload) string {
+	annotations := wl.GetAnnotations()
+	if len(annotations) == 0 {
+		return ""
+	}
+	return annotations[WorkloadPreemptibleSliceNameKey]
+}
+
+// Workload predicate definition to match workloads.
+type predicate func(wlSlice kueue.Workload) bool
+
+// not predicate inverts the provided predicate result, used in "exclusion" filters.
+func not(predicate predicate) predicate {
+	return func(wlSlice kueue.Workload) bool {
+		return !predicate(wlSlice)
+	}
+}
+
+// activeSlices predicate matches active non-deleted workload slices.
+func activeSlices() predicate {
+	return func(wlSlice kueue.Workload) bool {
+		return workload.IsActive(&wlSlice) && wlSlice.DeletionTimestamp.IsZero()
+	}
+}
+
+// FindSlices returns a sorted list of active workloads "owned by" the provided job object/gvk combination and
+// matching the provided predicates.
+func findSlices(ctx context.Context, clnt client.Client, jobObject client.Object, jobObjectGVK schema.GroupVersionKind, predicate predicate) ([]kueue.Workload, error) {
+	list := &kueue.WorkloadList{}
+	if err := clnt.List(ctx, list, client.InNamespace(jobObject.GetNamespace()), client.MatchingFields{fmt.Sprintf(".metadata.ownerReferences[%s.%s]", jobObjectGVK.Group, jobObjectGVK.Kind): jobObject.GetName()}); err != nil {
+		return nil, err
+	}
+
+	// Sort workloads by creation timestamp - oldest first.
+	sort.Slice(list.Items, func(i, j int) bool {
+		return list.Items[i].CreationTimestamp.Before(&list.Items[j].CreationTimestamp)
+	})
+
+	// Filter out workloads not matching the provided predicates.
+	// This is an exclusion match, i.e. it removes all entries that do not match the provided predicate.
+	return slices.DeleteFunc(list.Items, not(predicate)), nil
+}
+
+// FindActiveSlices returns a list of active workloads for a given job object/gvk combination.
+func FindActiveSlices(ctx context.Context, clnt client.Client, jobObject client.Object, jobObjectGVK schema.GroupVersionKind) ([]kueue.Workload, error) {
+	return findSlices(ctx, clnt, jobObject, jobObjectGVK, activeSlices())
+}
+
+// finishedOutOfSyncCondition helper returns workload "Finished" condition for updated, but
+// not yet admitted workload slices.
+func finishedOutOfSyncCondition() metav1.Condition {
+	return metav1.Condition{
+		Type:    kueue.WorkloadFinished,
+		Status:  metav1.ConditionTrue,
+		Reason:  kueue.WorkloadFinishedReasonOutOfSync,
+		Message: "The workload slice is out of sync with its parent job",
+	}
+}
+
+// Deactivate deactivates a given workload slice by setting its "Active" spec field to false.
+// It also applies the specified status condition patches to the workload slice.
+//
+// This function performs the following steps:
+//  1. It patches the spec of the workload slice to mark it as inactive (i.e., setting `Spec.Active` to false)
+//     if the workload slice is currently active.
+//  2. It applies any status conditions provided in the `conditions` parameter to the workload slice's status.
+//
+// Parameters:
+//   - ctx: The context for the operation, used for cancellation and logging.
+//   - clnt: The Kubernetes client used to interact with the API server.
+//   - workloadSlice: The workload slice to deactivate.
+//   - conditions: A variadic list of conditions to apply to the status of the workload slice.
+//
+// Returns:
+//   - error: An error, if any occurred during the process. If no error occurred, nil is returned.
+//
+// This function is useful for deactivating a workload slice and updating its status conditions in one operation.
+func Deactivate(ctx context.Context, clnt client.Client, workloadSlice *kueue.Workload, conditions ...metav1.Condition) error {
+	if err := clientutil.Patch(ctx, clnt, workloadSlice, true, func() (bool, error) {
+		// Patch spec to deactivate (if needed).
+		if workload.IsActive(workloadSlice) {
+			workloadSlice.Spec.Active = ptr.To(false)
+			return true, nil
+		}
+		return false, nil
+	}); err != nil {
+		return fmt.Errorf("failed to patch workload slice: %w", err)
+	}
```

  Updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-18T22:31:31Z

- **apis/kueue/v1beta1/workload_types.go:648** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-18T22:31:31Z
```diff
@@ -639,6 +643,10 @@ const (
 	// due to non-recoverable node failures.
 	WorkloadEvictedDueToNodeFailures = "NodeFailures"
 
+	// WorkloadRemovedViaWorkloadSliceAggregation indicates that the workload instance was
+	// removed (typically from the internal cache only) as a result of workload slice aggregation.
+	WorkloadRemovedViaWorkloadSliceAggregation = "RemovedViaWorkloadSliceAggregation"
```

  I think this can be resolved?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-18T22:55:58Z

- **apis/kueue/v1beta1/workload_types.go:620** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-18T22:55:58Z
```diff
@@ -608,6 +608,10 @@ const (
 	// InCohortReclaimWhileBorrowingReason indicates the Workload was preempted
 	// due to reclamation within the cohort while borrowing.
 	InCohortReclaimWhileBorrowingReason string = "InCohortReclaimWhileBorrowing"
+
+	// WorkloadSliceReplacementReason indicates the Workload instance was
+	// replaced by another workload slice.
+	WorkloadSliceReplacementReason string = "WorkloadSliceReplaced"
```

  should be removed now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:28:30Z

- **pkg/controller/jobframework/reconciler.go:1212** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:28:30Z
```diff
@@ -1151,6 +1192,47 @@ func ConstructWorkload(ctx context.Context, c client.Client, job GenericJob, lab
 	return wl, nil
 }
 
+// prepareWorkloadSlice adds necessary workload slice annotations.
+func prepareWorkloadSlice(ctx context.Context, clnt client.Client, job GenericJob, wl *kueue.Workload) error {
+	// Lookup existing slice for a given job.
+	workloadSlices, err := workloadslicing.FindNotFinishedWorkloads(ctx, clnt, job.Object(), job.GVK())
+	if err != nil {
+		return fmt.Errorf("failure looking up workload slices: %w", err)
+	}
+
+	switch len(workloadSlices) {
+	case 0:
+		// No active workloads found for this job - noop.
+		return nil
+	case 1:
+		// One active workload found for this job - typically, we are in a scale-up event, where previous
+		// workload is the "old" slice.
+		oldSlice := workloadSlices[0]
+		// Assert that we are processing a new workload-slice.
+		if wl.Name == oldSlice.Name {
```

  As you say this should never happen, so I don't think it makes sense to keep the dead code around. I would suggest to drop it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:31:30Z

- **pkg/scheduler/scheduler.go:867** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:31:30Z
```diff
@@ -751,3 +840,32 @@ func (s *Scheduler) recordWorkloadAdmissionEvents(newWorkload, originalWorkload
 		}
 	}
 }
+
+// replaceWorkloadSlice handles the replacement of a workload slice by deactivating the old slice and
+// marking it as finished. It logs the replacement operation, records an event, and reports metrics.
+//
+// This function performs the following steps:
+//  1. Checks if the old workload slice is already finished by inspecting the "Finished" condition in its status.
+//     If the slice is already finished, the function logs a message and returns early.
+//  2. If the old slice is not finished, it deactivates the old slice and marks it with a "Finished" condition,
+//     indicating that the slice was replaced to accommodate the new workload slice.
+//  3. The function logs details about the replacement, including the reason for the removal and the associated message.
+//  4. An event is recorded for the old slice to indicate that the slice was aggregated (replaced) by the new slice.
+//  5. The function reports metrics for the aggregation of workload slices for the old queue.
+func (s *Scheduler) replaceWorkloadSlice(ctx context.Context, oldQueue kueue.ClusterQueueReference, newSlice, oldSlice *kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	if meta.IsStatusConditionTrue(oldSlice.Status.Conditions, kueue.WorkloadFinished) {
+		log.V(3).Info("Workload slice already finished", "old-slice", klog.KObj(oldSlice), "new-slice", klog.KObj(newSlice))
+		return nil
+	}
+	reason := kueue.WorkloadSliceReplaced
+	message := fmt.Sprintf("Replaced to accommodate a workload (UID: %s, JobUID: %s) due to workload slice aggregation", newSlice.UID, newSlice.Labels[controllerconstants.JobUIDLabel])
+	if err := workloadslicing.Finish(ctx, s.client, oldSlice, reason, message); err != nil {
+		return fmt.Errorf("failed to deactivate aggregated workload slice: %w", err)
+	}
+
+	log.V(3).Info("Removed", "old slice", klog.KObj(oldSlice), "new slice", klog.KObj(newSlice), "reason", reason, "message", message, "old-queue", klog.KRef("", string(oldQueue)))
```

  ```suggestion
  	log.V(3).Info("Replaced", "old slice", klog.KObj(oldSlice), "new slice", klog.KObj(newSlice), "reason", reason, "message", message, "old-queue", klog.KRef("", string(oldQueue)))
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:31:54Z

- **pkg/scheduler/scheduler.go:864** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:31:54Z
```diff
@@ -751,3 +840,32 @@ func (s *Scheduler) recordWorkloadAdmissionEvents(newWorkload, originalWorkload
 		}
 	}
 }
+
+// replaceWorkloadSlice handles the replacement of a workload slice by deactivating the old slice and
+// marking it as finished. It logs the replacement operation, records an event, and reports metrics.
+//
+// This function performs the following steps:
+//  1. Checks if the old workload slice is already finished by inspecting the "Finished" condition in its status.
+//     If the slice is already finished, the function logs a message and returns early.
+//  2. If the old slice is not finished, it deactivates the old slice and marks it with a "Finished" condition,
+//     indicating that the slice was replaced to accommodate the new workload slice.
+//  3. The function logs details about the replacement, including the reason for the removal and the associated message.
+//  4. An event is recorded for the old slice to indicate that the slice was aggregated (replaced) by the new slice.
+//  5. The function reports metrics for the aggregation of workload slices for the old queue.
+func (s *Scheduler) replaceWorkloadSlice(ctx context.Context, oldQueue kueue.ClusterQueueReference, newSlice, oldSlice *kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	if meta.IsStatusConditionTrue(oldSlice.Status.Conditions, kueue.WorkloadFinished) {
+		log.V(3).Info("Workload slice already finished", "old-slice", klog.KObj(oldSlice), "new-slice", klog.KObj(newSlice))
+		return nil
+	}
+	reason := kueue.WorkloadSliceReplaced
+	message := fmt.Sprintf("Replaced to accommodate a workload (UID: %s, JobUID: %s) due to workload slice aggregation", newSlice.UID, newSlice.Labels[controllerconstants.JobUIDLabel])
+	if err := workloadslicing.Finish(ctx, s.client, oldSlice, reason, message); err != nil {
+		return fmt.Errorf("failed to deactivate aggregated workload slice: %w", err)
```

  ```suggestion
  		return fmt.Errorf("failed to replace workload slice: %w", err)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:38:52Z

- **test/integration/singlecluster/webhook/core/workload_test.go:970** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:38:52Z
```diff
@@ -966,5 +966,68 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 				g.Expect(k8sClient.Status().Update(ctx, wl)).Should(testing.BeForbiddenError())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should not allow workload podSets count increase even when ElasticJobsViaWorkloadSlices feature gate is enabled", func() {
```

  Can you also add some "happy path" integration tests (for increase and decrease)? 
  
  I think a good place to look is `test/integration/singlecluster/controller/jobs/job/job_controller_test.go`, then we have section "Interacting with scheduler"?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:50:06Z

- **pkg/scheduler/scheduler.go:492** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:50:06Z
```diff
@@ -462,20 +480,91 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
```

  ```suggestion
  func replacedWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
  ```
  I would suggest using "replaced" terminology more consistently. We aren't really using preemption that much. wdyt? feel free to alse rename other places consistently.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:51:38Z

- **pkg/scheduler/scheduler.go:492** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:51:38Z
```diff
@@ -462,20 +480,91 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
```

  please move the new functions to `pkg/scheduler/workloadslicing.go`, just to offload the main scheduler file.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:52:18Z

- **pkg/scheduler/scheduler.go:513** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:52:18Z
```diff
@@ -462,20 +480,91 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || wl == nil || snap == nil {
+		return nil, nil
+	}
+	if workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl); workloadSlicePreemptionTarget != nil {
+		return []*preemption.Target{workloadSlicePreemptionTarget}, workloadSlicePreemptionTarget.WorkloadInfo
+	}
+	return nil, nil
+}
+
+// findPreemptedSliceTarget identifies and removes a preempted workload slice target from the given list of targets.
+// The function checks if Elastic Jobs via Workload Slices feature is enabled and if so, attempts to find a matching
+// workload slice in the target list for the provided preemptor. If a matching slice is found, it is removed from the list
+// and returned alongside the original target list.
+//
+// This function performs the following:
+// 1. It checks if the feature `ElasticJobsViaWorkloadSlices` is enabled. If not, it returns the target list unchanged with `nil` as the second return value.
+// 2. It generates a replacement key for the preemptor workload slice. If no replacement key is found, it returns the original target list unchanged with `nil`.
+// 3. It iterates over the list of preemption targets, looking for a target that matches the replacement key.
+// 4. If a matching target is found, it removes it from the list and returns the updated list and the removed target.
+// 5. If no matching target is found, it returns the original target list and `nil`.
+func findPreemptedSliceTarget(preemptor *kueue.Workload, targets []*preemption.Target) ([]*preemption.Target, *preemption.Target) {
```

  ```suggestion
  func findReplacedSliceTarget(preemptor *kueue.Workload, targets []*preemption.Target) ([]*preemption.Target, *preemption.Target) {
  ```
  As in other comments, please move to the new file.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:53:17Z

- **pkg/scheduler/preemption/preemption.go:590** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:53:17Z
```diff
@@ -568,3 +569,34 @@ func quotaReservationTime(wl *kueue.Workload, now time.Time) time.Time {
 	}
 	return cond.LastTransitionTime.Time
 }
+
+// PreemptibleWorkloadSliceTarget returns the preemption target for a given workload slice, if any.
+//
+// In the WorkloadSlice model, a newly created slice may preempt at most one existing slice.
+// This function identifies that potential target by reading a specific annotation on the
+// given workload's object, which encodes the key of the slice it may preempt.
+//
+// The function looks up the referenced slice in the scheduling snapshot for the same ClusterQueue.
+// If the referenced workload slice is found, it returns a Target pointing to that slice along with
+// the standard preemption reason. If no valid target is found (either the annotation is missing or
+// the slice cannot be resolved), the function returns nil.
+//
+// Parameters:
+//   - snapshot: the current scheduling snapshot containing all active workloads.
+//   - workloadInfo: the workload slice for which a potential preemption target is being resolved.
+//
+// Returns:
+//   - *Target: a reference to the workload slice that may be preempted, or nil if no valid target exists.
+func PreemptibleWorkloadSliceTarget(snapshot *cache.Snapshot, workloadInfo *workload.Info) *Target {
```

  I suggest to inline this function inside the calling function - it is not called from other places, and its reusability is questionable, so I prefer not to need to export it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:54:19Z

- **pkg/scheduler/scheduler.go:554** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:54:19Z
```diff
@@ -462,20 +480,91 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || wl == nil || snap == nil {
+		return nil, nil
+	}
+	if workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl); workloadSlicePreemptionTarget != nil {
+		return []*preemption.Target{workloadSlicePreemptionTarget}, workloadSlicePreemptionTarget.WorkloadInfo
+	}
+	return nil, nil
+}
+
+// findPreemptedSliceTarget identifies and removes a preempted workload slice target from the given list of targets.
+// The function checks if Elastic Jobs via Workload Slices feature is enabled and if so, attempts to find a matching
+// workload slice in the target list for the provided preemptor. If a matching slice is found, it is removed from the list
+// and returned alongside the original target list.
+//
+// This function performs the following:
+// 1. It checks if the feature `ElasticJobsViaWorkloadSlices` is enabled. If not, it returns the target list unchanged with `nil` as the second return value.
+// 2. It generates a replacement key for the preemptor workload slice. If no replacement key is found, it returns the original target list unchanged with `nil`.
+// 3. It iterates over the list of preemption targets, looking for a target that matches the replacement key.
+// 4. If a matching target is found, it removes it from the list and returns the updated list and the removed target.
+// 5. If no matching target is found, it returns the original target list and `nil`.
+func findPreemptedSliceTarget(preemptor *kueue.Workload, targets []*preemption.Target) ([]*preemption.Target, *preemption.Target) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		return targets, nil
+	}
+	sliceKey := workloadslicing.ReplacementForKey(preemptor)
+	if sliceKey == nil {
+		return targets, nil
+	}
+	for i := range targets {
+		if *sliceKey == workload.Key(targets[i].WorkloadInfo.Obj) {
+			return append(targets[:i], targets[i+1:]...), targets[i]
+		}
+	}
+	return targets, nil
+}
+
+// getInitialAssignments computes the initial resource flavor assignment and any required preemption targets
+// for a workload slice.
+//
+// The function attempts to assign resources to the provided workload slice using the current
+// snapshot of the scheduling state. It proceeds in the following steps:
+//
+//  1. It first checks for any preemptible workload slices that workload may replace, using an annotation-based lookup.
+//  2. It creates a flavor assigner to compute a full assignment scale-adjusted for preemptable workload slice targets
+//     based on either:
+//     - direct fit (no preemption needed), or
+//     - preemption (if needed and possible).
+//  3. If direct assignment isn't possible but preemption is enabled and viable, it includes any additional
+//     preemption targets obtained through the configured preemptor.
+//  4. If partial admission is enabled and the workload allows it, the function attempts to reduce pod counts
+//     across PodSets to find an assignable configuration—again checking for preemption if needed.
+//
+// Returns:
+//   - A flavorassigner.Assignment representing the selected (possibly reduced) flavor allocation.
+//   - A slice of preemption targets, which may include both explicitly annotated slices and those
+//     identified during scheduling.
+//
+// If no valid assignment can be made, returns the original full assignment with no preemption targets.
 func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, snap *cache.Snapshot) (flavorassigner.Assignment, []*preemption.Target) {
 	cq := snap.ClusterQueue(wl.ClusterQueue)
-	flvAssigner := flavorassigner.New(wl, cq, snap.ResourceFlavors, s.fairSharing.Enable, preemption.NewOracle(s.preemptor, snap))
+
+	preemptionTargets, preemptableWorkloadSlice := preemptableWorkloadSlice(wl, snap)
```

  ```suggestion
  	preemptionTargets, repalcedWorkloadSlice := extractReplacedWorkloadSlice(wl, snap)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-21T17:57:54Z

- **pkg/scheduler/scheduler.go:215** by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:57:54Z
```diff
@@ -212,7 +215,6 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 	skippedPreemptions := make(map[kueue.ClusterQueueReference]int)
 	for iterator.hasNext() {
 		e := iterator.pop()
-
```

  nit, pleave revert

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-22T05:44:31Z

- **pkg/scheduler/preemption/preemption.go:590** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-22T05:44:31Z
```diff
@@ -568,3 +569,34 @@ func quotaReservationTime(wl *kueue.Workload, now time.Time) time.Time {
 	}
 	return cond.LastTransitionTime.Time
 }
+
+// PreemptibleWorkloadSliceTarget returns the preemption target for a given workload slice, if any.
+//
+// In the WorkloadSlice model, a newly created slice may preempt at most one existing slice.
+// This function identifies that potential target by reading a specific annotation on the
+// given workload's object, which encodes the key of the slice it may preempt.
+//
+// The function looks up the referenced slice in the scheduling snapshot for the same ClusterQueue.
+// If the referenced workload slice is found, it returns a Target pointing to that slice along with
+// the standard preemption reason. If no valid target is found (either the annotation is missing or
+// the slice cannot be resolved), the function returns nil.
+//
+// Parameters:
+//   - snapshot: the current scheduling snapshot containing all active workloads.
+//   - workloadInfo: the workload slice for which a potential preemption target is being resolved.
+//
+// Returns:
+//   - *Target: a reference to the workload slice that may be preempted, or nil if no valid target exists.
+func PreemptibleWorkloadSliceTarget(snapshot *cache.Snapshot, workloadInfo *workload.Info) *Target {
```

  Updated: This function is refactored and moved into the workload-slicing package.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-22T05:44:53Z

- **pkg/scheduler/scheduler.go:554** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-22T05:44:53Z
```diff
@@ -462,20 +480,91 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || wl == nil || snap == nil {
+		return nil, nil
+	}
+	if workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl); workloadSlicePreemptionTarget != nil {
+		return []*preemption.Target{workloadSlicePreemptionTarget}, workloadSlicePreemptionTarget.WorkloadInfo
+	}
+	return nil, nil
+}
+
+// findPreemptedSliceTarget identifies and removes a preempted workload slice target from the given list of targets.
+// The function checks if Elastic Jobs via Workload Slices feature is enabled and if so, attempts to find a matching
+// workload slice in the target list for the provided preemptor. If a matching slice is found, it is removed from the list
+// and returned alongside the original target list.
+//
+// This function performs the following:
+// 1. It checks if the feature `ElasticJobsViaWorkloadSlices` is enabled. If not, it returns the target list unchanged with `nil` as the second return value.
+// 2. It generates a replacement key for the preemptor workload slice. If no replacement key is found, it returns the original target list unchanged with `nil`.
+// 3. It iterates over the list of preemption targets, looking for a target that matches the replacement key.
+// 4. If a matching target is found, it removes it from the list and returns the updated list and the removed target.
+// 5. If no matching target is found, it returns the original target list and `nil`.
+func findPreemptedSliceTarget(preemptor *kueue.Workload, targets []*preemption.Target) ([]*preemption.Target, *preemption.Target) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) {
+		return targets, nil
+	}
+	sliceKey := workloadslicing.ReplacementForKey(preemptor)
+	if sliceKey == nil {
+		return targets, nil
+	}
+	for i := range targets {
+		if *sliceKey == workload.Key(targets[i].WorkloadInfo.Obj) {
+			return append(targets[:i], targets[i+1:]...), targets[i]
+		}
+	}
+	return targets, nil
+}
+
+// getInitialAssignments computes the initial resource flavor assignment and any required preemption targets
+// for a workload slice.
+//
+// The function attempts to assign resources to the provided workload slice using the current
+// snapshot of the scheduling state. It proceeds in the following steps:
+//
+//  1. It first checks for any preemptible workload slices that workload may replace, using an annotation-based lookup.
+//  2. It creates a flavor assigner to compute a full assignment scale-adjusted for preemptable workload slice targets
+//     based on either:
+//     - direct fit (no preemption needed), or
+//     - preemption (if needed and possible).
+//  3. If direct assignment isn't possible but preemption is enabled and viable, it includes any additional
+//     preemption targets obtained through the configured preemptor.
+//  4. If partial admission is enabled and the workload allows it, the function attempts to reduce pod counts
+//     across PodSets to find an assignable configuration—again checking for preemption if needed.
+//
+// Returns:
+//   - A flavorassigner.Assignment representing the selected (possibly reduced) flavor allocation.
+//   - A slice of preemption targets, which may include both explicitly annotated slices and those
+//     identified during scheduling.
+//
+// If no valid assignment can be made, returns the original full assignment with no preemption targets.
 func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, snap *cache.Snapshot) (flavorassigner.Assignment, []*preemption.Target) {
 	cq := snap.ClusterQueue(wl.ClusterQueue)
-	flvAssigner := flavorassigner.New(wl, cq, snap.ResourceFlavors, s.fairSharing.Enable, preemption.NewOracle(s.preemptor, snap))
+
+	preemptionTargets, preemptableWorkloadSlice := preemptableWorkloadSlice(wl, snap)
```

  Updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-22T05:45:04Z

- **pkg/scheduler/scheduler.go:215** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-22T05:45:03Z
```diff
@@ -212,7 +215,6 @@ func (s *Scheduler) schedule(ctx context.Context) wait.SpeedSignal {
 	skippedPreemptions := make(map[kueue.ClusterQueueReference]int)
 	for iterator.hasNext() {
 		e := iterator.pop()
-
```

  reverted.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-22T16:38:25Z

- **pkg/scheduler/scheduler.go:492** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-22T16:38:25Z
```diff
@@ -462,20 +480,91 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
```

  done.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-22T16:39:16Z

- **pkg/scheduler/scheduler.go:513** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-22T16:39:16Z
```diff
@@ -462,20 +480,91 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// preemptableWorkloadSlice determines whether the given workload is eligible for
+// slice-based preemption under the ElasticJobsViaWorkloadSlices feature.
+//
+// If the feature gate `ElasticJobsViaWorkloadSlices` is not enabled, it returns nil.
+// Otherwise, it attempts to identify a preemptible workload slice target from the
+// provided snapshot that can be reclaimed to admit the given workload.
+//
+// Returns a list containing a single preemption target and the associated workload info
+// if a suitable preemptible slice is found, or nil if no such target exists.
+func preemptableWorkloadSlice(wl *workload.Info, snap *cache.Snapshot) ([]*preemption.Target, *workload.Info) {
+	if !features.Enabled(features.ElasticJobsViaWorkloadSlices) || wl == nil || snap == nil {
+		return nil, nil
+	}
+	if workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl); workloadSlicePreemptionTarget != nil {
+		return []*preemption.Target{workloadSlicePreemptionTarget}, workloadSlicePreemptionTarget.WorkloadInfo
+	}
+	return nil, nil
+}
+
+// findPreemptedSliceTarget identifies and removes a preempted workload slice target from the given list of targets.
+// The function checks if Elastic Jobs via Workload Slices feature is enabled and if so, attempts to find a matching
+// workload slice in the target list for the provided preemptor. If a matching slice is found, it is removed from the list
+// and returned alongside the original target list.
+//
+// This function performs the following:
+// 1. It checks if the feature `ElasticJobsViaWorkloadSlices` is enabled. If not, it returns the target list unchanged with `nil` as the second return value.
+// 2. It generates a replacement key for the preemptor workload slice. If no replacement key is found, it returns the original target list unchanged with `nil`.
+// 3. It iterates over the list of preemption targets, looking for a target that matches the replacement key.
+// 4. If a matching target is found, it removes it from the list and returns the updated list and the removed target.
+// 5. If no matching target is found, it returns the original target list and `nil`.
+func findPreemptedSliceTarget(preemptor *kueue.Workload, targets []*preemption.Target) ([]*preemption.Target, *preemption.Target) {
```

  Updated and moved into workloadslicing package.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-23T06:09:54Z

- **test/integration/singlecluster/webhook/core/workload_test.go:970** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-23T06:09:54Z
```diff
@@ -966,5 +966,68 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 				g.Expect(k8sClient.Status().Update(ctx, wl)).Should(testing.BeForbiddenError())
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
+
+		ginkgo.It("Should not allow workload podSets count increase even when ElasticJobsViaWorkloadSlices feature gate is enabled", func() {
```

  done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T06:38:56Z

- **pkg/scheduler/scheduler.go:500** by [@mimowo](https://github.com/mimowo) — 2025-07-23T06:38:56Z
```diff
@@ -462,20 +465,52 @@ func (s *Scheduler) getAssignments(log logr.Logger, wl *workload.Info, snap *cac
 	return assignment, targets
 }
 
+// getInitialAssignments computes the initial resource flavor assignment and any required preemption targets
+// for a workload slice.
+//
+// The function attempts to assign resources to the provided workload slice using the current
+// snapshot of the scheduling state. It proceeds in the following steps:
+//
+//  1. It first checks for any preemptible workload slices that workload may replace, using an annotation-based lookup.
+//  2. It creates a flavor assigner to compute a full assignment scale-adjusted for preemptable workload slice targets
+//     based on either:
+//     - direct fit (no preemption needed), or
+//     - preemption (if needed and possible).
+//  3. If direct assignment isn't possible but preemption is enabled and viable, it includes any additional
+//     preemption targets obtained through the configured preemptor.
+//  4. If partial admission is enabled and the workload allows it, the function attempts to reduce pod counts
+//     across PodSets to find an assignable configuration—again checking for preemption if needed.
+//
+// Returns:
+//   - A flavorassigner.Assignment representing the selected (possibly reduced) flavor allocation.
+//   - A slice of preemption targets, which may include both explicitly annotated slices and those
+//     identified during scheduling.
+//
+// If no valid assignment can be made, returns the original full assignment with no preemption targets.
 func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, snap *cache.Snapshot) (flavorassigner.Assignment, []*preemption.Target) {
 	cq := snap.ClusterQueue(wl.ClusterQueue)
-	flvAssigner := flavorassigner.New(wl, cq, snap.ResourceFlavors, s.fairSharing.Enable, preemption.NewOracle(s.preemptor, snap))
+
+	var preemptionTargets []*preemption.Target
+	var preemptableWorkloadSlice *workload.Info
+
+	workloadSlicePreemptionTarget := preemption.PreemptibleWorkloadSliceTarget(snap, wl)
+	if workloadSlicePreemptionTarget != nil {
+		preemptionTargets = []*preemption.Target{workloadSlicePreemptionTarget}
+		preemptableWorkloadSlice = workloadSlicePreemptionTarget.WorkloadInfo
+	}
```

  please mark as resolved

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T06:39:01Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:647** by [@mimowo](https://github.com/mimowo) — 2025-07-23T06:39:01Z
```diff
@@ -612,6 +643,22 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 		// Calculate representativeMode for this assignment as the worst mode among all requests.
 		representativeMode := fit
 		for rName, val := range requests {
+			// Ensure the same resource flavor is used for the workload slice as in the original admitted slice.
+			if a.preemptWorkloadSlice != nil {
```

  please mark as resolved

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T06:39:12Z

- **apis/kueue/v1beta1/workload_types.go:648** by [@mimowo](https://github.com/mimowo) — 2025-07-23T06:39:12Z
```diff
@@ -639,6 +643,10 @@ const (
 	// due to non-recoverable node failures.
 	WorkloadEvictedDueToNodeFailures = "NodeFailures"
 
+	// WorkloadRemovedViaWorkloadSliceAggregation indicates that the workload instance was
+	// removed (typically from the internal cache only) as a result of workload slice aggregation.
+	WorkloadRemovedViaWorkloadSliceAggregation = "RemovedViaWorkloadSliceAggregation"
```

  Yes

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T06:39:27Z

- **apis/kueue/v1beta1/workload_types.go:620** by [@mimowo](https://github.com/mimowo) — 2025-07-23T06:39:27Z
```diff
@@ -608,6 +608,10 @@ const (
 	// InCohortReclaimWhileBorrowingReason indicates the Workload was preempted
 	// due to reclamation within the cohort while borrowing.
 	InCohortReclaimWhileBorrowingReason string = "InCohortReclaimWhileBorrowing"
+
+	// WorkloadSliceReplacementReason indicates the Workload instance was
+	// replaced by another workload slice.
+	WorkloadSliceReplacementReason string = "WorkloadSliceReplaced"
```

  Thank you, please resolve
