# PR #5521: Remove redundant EXTRA_TAG variable from helm-chart-package.sh.

**Summary**: Remove redundant EXTRA_TAG variable from helm-chart-package.sh.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5521

**Last updated**: 2025-06-05T17:01:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `fix/chart-push-extratag` → `main`
- **Created**: 2025-06-05T14:29:53Z
- **Updated**: 2025-06-05T17:01:49Z
- **Merged**: 2025-06-05T16:56:38Z
- **Closed**: 2025-06-05T16:56:39Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+7 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Remove redundant EXTRA_TAG variable from helm-chart-package.sh.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:
Unfortunately, we can't push Helm chart images using the branch name tag like we do for Kueue images. Therefore, I think EXTRA_TAG variables is not usable here and we can drop this redundant variable.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-05T14:30:08Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c12e253e8c1c1a0728a631954a2bffcd8e1fd965 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6841c71332e6cb0008e0360f |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-05T14:36:27Z

I also tested it:

```
$ make helm-chart-push GIT_TAG=v0.12.2 HELM_CHART_REPO=localhost:5000/kueue/charts
```

```yaml
controllerManager:
  manager:
    image:
      repository: registry.k8s.io/kueue/kueue
      # This should be set to 'IfNotPresent' for released version
      pullPolicy: IfNotPresent
      tag: v0.12.2

kueueViz:
  backend:
    image:
      repository: "registry.k8s.io/kueue/kueueviz-backend"
      # This should be set to 'IfNotPresent' for released version
      pullPolicy: IfNotPresent
      tag: v0.12.2
  frontend:
    image:
      repository: "registry.k8s.io/kueue/kueueviz-frontend"
      # This should be set to 'IfNotPresent' for released version
      pullPolicy: IfNotPresent
      tag: v0.12.2
```

```
$ helm install kueue oci://localhost:5000/kueue/charts/kueue \                    
  --version=0.12.2 \
  --namespace  kueue-system \
  --create-namespace \
  --wait --timeout 300s \
  --set enableKueueViz=true
```

```
kueue-system         kueue-controller-manager-5bf4b7ddc6-z6p85    1/1     Running   0          9s
kueue-system         kueue-kueueviz-backend-6b5fc99755-z79nn      1/1     Running   0          9s
kueue-system         kueue-kueueviz-frontend-5dfc9fc8d6-5fwbn     1/1     Running   0          9s
```

For controller manager:
```yaml
spec:
  containers:
  - args:
    - --config=/controller_manager_config.yaml
    - --zap-log-level=2
    command:
    - /manager
    image: registry.k8s.io/kueue/kueue:v0.12.2
```

For KueueViz backend:
```yaml
spec:
  containers:
  - image: registry.k8s.io/kueue/kueueviz-backend:v0.12.2
```

For KueueViz frontend:
```yaml
spec:
  containers:
  - env:
    - name: REACT_APP_WEBSOCKET_URL
      value: wss://backend.kueueviz.local
    image: registry.k8s.io/kueue/kueueviz-frontend:v0.12.2
```

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-05T14:42:24Z

/cc @mimowo @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T14:44:55Z

thank you for the PR and testing. one hesitation I have which may require an adjustment is the image registry swap for helms. we chack if extra tag matched Denver. I think for GIT_TAG it will always match the regex. 

if this is the case we may need to do the adjustment detecting this is a git tag corresponding to a promoted version.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-05T14:55:10Z

> thank you for the PR and testing. one hesitation I have which may require an adjustment is the image registry swap for helms. we chack if extra tag matched Denver. I think for GIT_TAG it will always match the regex.
> 
> if this is the case we may need to do the adjustment detecting this is a git tag corresponding to a promoted version.

I tested it with devel:

```
$ make helm-chart-push GIT_TAG=v0.13.0-devel-61-gf946d0e5 HELM_CHART_REPO=localhost:5000/kueue/charts
```

```
controllerManager:
  manager:
    image:
      repository: us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue
      # This should be set to 'IfNotPresent' for released version
      pullPolicy: IfNotPresent
      tag: v0.13.0-devel-61-gf946d0e5

kueueViz:
  backend:
    image:
      repository: "us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend"
      # This should be set to 'IfNotPresent' for released version
      pullPolicy: IfNotPresent
      tag: v0.13.0-devel-61-gf946d0e5
  frontend:
    image:
      repository: "us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-frontend"
      # This should be set to 'IfNotPresent' for released version
      pullPolicy: IfNotPresent
      tag: v0.13.0-devel-61-gf946d0e5
```

```
kubectl get pods -n kueue-system
NAME                                       READY   STATUS    RESTARTS   AGE
kueue-controller-manager-688bf64cc-jzr84   1/1     Running   0          17s
kueue-kueueviz-backend-5c9bd5c6c4-7p9p5    1/1     Running   0          17s
kueue-kueueviz-frontend-dc67cd5c5-5m2qc    1/1     Running   0          17s
```

Looks like working fine. We have regex that applying only v0.12.2 and v0.12.2-rc1.

https://github.com/kubernetes-sigs/kueue/blob/a6e5ca11ab781c6ea0a8f5cb2ce4ad0437683210/hack/helm-chart-package.sh#L33

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T15:45:49Z

thank you, now thinking about this we don't need to match rc even. I remember this was another hack back when we had the date prefix. Because we need to change repo only for promoted images, rc is not promoted.

To confirm we actually had this issue, please check the chart pushed for 0.12.0-rc. I think it points to register.k8s.io, but was never promoted.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T15:47:38Z

If this is the case I'm ok to adjust the regex in follow up too, because it was broken anyway

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-05T16:05:13Z

I can't find this image on oci://registry.k8s.io/kueue/charts/kueue:

```
$ helm install kueue oci://registry.k8s.io/kueue/charts/kueue --version="0.12.0-rc.1" --create-namespace --namespace=kueue-system

Error: INSTALLATION FAILED: registry.k8s.io/kueue/charts/kueue:0.12.0-rc.1: not found
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T16:07:26Z

this is partly my point, I think the rc package is in our staging registry, but it references non existing images from the registry.k8s.io. ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T16:26:21Z

ok, found it, we have currently:

```
> helm template oci://us-central1-docker.pkg.dev/k8s-staging-images/kueue/charts/kueue --version 0.12.0-rc.3 | grep registry.k8s.io
        image: "registry.k8s.io/kueue/kueue:v0.12.0-rc.3"
```
but:

```
> docker pull registry.k8s.io/kueue/kueue:v0.12.0-rc.3                                          
Trying to pull registry.k8s.io/kueue/kueue:v0.12.0-rc.3...
Error: initializing source docker://registry.k8s.io/kueue/kueue:v0.12.0-rc.3: reading manifest v0.12.0-rc.3 in registry.k8s.io/kueue/kueue: manifest unknown: Failed to fetch "v0.12.0-rc.3"
```

so, the charts in staging refer to non-existent images in registry,k8s.io. This is not too bad, because these are just staging, but still, it is not self-consistent. In any case, we can leave it for follow up.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-05T16:33:02Z

LGTM, just one nit, cc @tenzen-y 

I think this will simplify the reasoning about the release process if we consistently rely just on GIT_TAG, which has the same value locally and on CI. We can do that after dropping the date prefix.

To align the release process going forward:
/cherry-pick release-0.12
/cherry-pick release-0.11

This will also allow us to test the release process on 0.11 and 0.12 braches before testing on 0.13.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-05T16:33:04Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.11`, `release-0.12` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5521#issuecomment-2945183213):

>LGTM, just one nit, cc @tenzen-y 
>
>I think this will simplify the reasoning about the release process if we consistently rely just on GIT_TAG, which has the same value locally and on CI. We can do that after dropping the date prefix.
>
>To align the release process going forward:
>/cherry-pick release-0.12
>/cherry-pick release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-05T16:36:28Z

LGTM label has been added.  <details>Git tree hash: 70df31304a12d53c2c83a9591ee97ec86ddb43af</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-05T16:36:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5521#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5521#pullrequestreview-2901010792" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-05T16:57:16Z

@mimowo: #5521 failed to apply on top of branch "release-0.11":
```
Applying: Remove redundant EXTRA_TAG variable from helm-chart-package.sh.
Using index info to reconstruct a base tree...
M	Makefile
M	hack/helm-chart-package.sh
Falling back to patching base and 3-way merge...
Auto-merging hack/helm-chart-package.sh
CONFLICT (content): Merge conflict in hack/helm-chart-package.sh
Auto-merging Makefile
CONFLICT (content): Merge conflict in Makefile
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Remove redundant EXTRA_TAG variable from helm-chart-package.sh.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5521#issuecomment-2945183213):

>LGTM, just one nit, cc @tenzen-y 
>
>I think this will simplify the reasoning about the release process if we consistently rely just on GIT_TAG, which has the same value locally and on CI. We can do that after dropping the date prefix.
>
>To align the release process going forward:
>/cherry-pick release-0.12
>/cherry-pick release-0.11
>
>This will also allow us to test the release process on 0.11 and 0.12 braches before testing on 0.13.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-05T16:57:52Z

@mimowo: new pull request created: #5524

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5521#issuecomment-2945183213):

>LGTM, just one nit, cc @tenzen-y 
>
>I think this will simplify the reasoning about the release process if we consistently rely just on GIT_TAG, which has the same value locally and on CI. We can do that after dropping the date prefix.
>
>To align the release process going forward:
>/cherry-pick release-0.12
>/cherry-pick release-0.11
>
>This will also allow us to test the release process on 0.11 and 0.12 braches before testing on 0.13.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T16:27:56Z

- **hack/helm-chart-package.sh:67** by [@mimowo](https://github.com/mimowo) — 2025-06-05T16:27:56Z
```diff
@@ -52,19 +49,15 @@ default_kueueviz_frontend_image_repo=$(${YQ} ".kueueViz.frontend.image.repositor
 readonly default_kueueviz_frontend_image_repo
 
 # Update the image repo, tag and policy
-${YQ}  e  ".controllerManager.manager.image.repository = \"${IMAGE_REGISTRY}/kueue\" | .controllerManager.manager.image.tag = \"${app_version}\" | .controllerManager.manager.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
-
-# Update the KueueViz images repo, tag and policy in values.yaml
-${YQ}  e  ".kueueViz.backend.image.repository = \"${IMAGE_REGISTRY}/kueueviz-backend\" | .kueueViz.backend.image.tag = \"${app_version}\" | .kueueViz.backend.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
-${YQ}  e  ".kueueViz.frontend.image.repository = \"${IMAGE_REGISTRY}/kueueviz-frontend\" | .kueueViz.frontend.image.tag = \"${app_version}\" | .kueueViz.frontend.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
+${YQ}  e  ".controllerManager.manager.image.repository = \"${IMAGE_REGISTRY}/kueue\" | .controllerManager.manager.image.tag = \"${GIT_TAG}\" | .controllerManager.manager.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
+${YQ}  e  ".kueueViz.backend.image.repository = \"${IMAGE_REGISTRY}/kueueviz-backend\" | .kueueViz.backend.image.tag = \"${GIT_TAG}\" | .kueueViz.backend.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
+${YQ}  e  ".kueueViz.frontend.image.repository = \"${IMAGE_REGISTRY}/kueueviz-frontend\" | .kueueViz.frontend.image.tag = \"${GIT_TAG}\" | .kueueViz.frontend.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
 
 # TODO: consider signing it
-${HELM} package --version "${chart_version}" --app-version "${app_version}" charts/kueue -d "${DEST_CHART_DIR}"
+${HELM} package --version "${chart_version}" --app-version "${GIT_TAG}" charts/kueue -d "${DEST_CHART_DIR}"
 
 # Revert the image changes
 ${YQ}  e  ".controllerManager.manager.image.repository = \"${default_image_repo}\" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = \"Always\"" -i charts/kueue/values.yaml
-
-# Revert the KueueViz image changes
```

  nit: the comment is ok and not related to the PR, let's keep it

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-05T16:35:06Z

- **hack/helm-chart-package.sh:67** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-05T16:35:06Z
```diff
@@ -52,19 +49,15 @@ default_kueueviz_frontend_image_repo=$(${YQ} ".kueueViz.frontend.image.repositor
 readonly default_kueueviz_frontend_image_repo
 
 # Update the image repo, tag and policy
-${YQ}  e  ".controllerManager.manager.image.repository = \"${IMAGE_REGISTRY}/kueue\" | .controllerManager.manager.image.tag = \"${app_version}\" | .controllerManager.manager.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
-
-# Update the KueueViz images repo, tag and policy in values.yaml
-${YQ}  e  ".kueueViz.backend.image.repository = \"${IMAGE_REGISTRY}/kueueviz-backend\" | .kueueViz.backend.image.tag = \"${app_version}\" | .kueueViz.backend.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
-${YQ}  e  ".kueueViz.frontend.image.repository = \"${IMAGE_REGISTRY}/kueueviz-frontend\" | .kueueViz.frontend.image.tag = \"${app_version}\" | .kueueViz.frontend.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
+${YQ}  e  ".controllerManager.manager.image.repository = \"${IMAGE_REGISTRY}/kueue\" | .controllerManager.manager.image.tag = \"${GIT_TAG}\" | .controllerManager.manager.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
+${YQ}  e  ".kueueViz.backend.image.repository = \"${IMAGE_REGISTRY}/kueueviz-backend\" | .kueueViz.backend.image.tag = \"${GIT_TAG}\" | .kueueViz.backend.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
+${YQ}  e  ".kueueViz.frontend.image.repository = \"${IMAGE_REGISTRY}/kueueviz-frontend\" | .kueueViz.frontend.image.tag = \"${GIT_TAG}\" | .kueueViz.frontend.image.pullPolicy = \"IfNotPresent\"" -i charts/kueue/values.yaml
 
 # TODO: consider signing it
-${HELM} package --version "${chart_version}" --app-version "${app_version}" charts/kueue -d "${DEST_CHART_DIR}"
+${HELM} package --version "${chart_version}" --app-version "${GIT_TAG}" charts/kueue -d "${DEST_CHART_DIR}"
 
 # Revert the image changes
 ${YQ}  e  ".controllerManager.manager.image.repository = \"${default_image_repo}\" | del(.controllerManager.manager.image.tag) | .controllerManager.manager.image.pullPolicy = \"Always\"" -i charts/kueue/values.yaml
-
-# Revert the KueueViz image changes
```

  Reverted both comments.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-05T16:36:21Z

/lgtm
/approve
thanks 👍
