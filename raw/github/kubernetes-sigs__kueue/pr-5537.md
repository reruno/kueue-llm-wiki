# PR #5537: Cleanup Makefile.

**Summary**: Cleanup Makefile.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5537

**Last updated**: 2025-06-06T17:36:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `cleanup/makefile` → `main`
- **Created**: 2025-06-06T12:35:44Z
- **Updated**: 2025-06-06T17:36:33Z
- **Merged**: 2025-06-06T17:34:39Z
- **Closed**: 2025-06-06T17:34:39Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+16 / -14**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Cleanup Makefile.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-06T12:35:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e10c3c088174a9a4a4032277bffc7ee9085dcf84 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/684321bba1ebbb0008958499 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T12:39:05Z

/cc @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-06T17:18:32Z

LGTM label has been added.  <details>Git tree hash: 9a53b0a85cbf3b5e9d52a8e625fa1f79738de023</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-06T17:18:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5537#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5537#pullrequestreview-2905561253" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-06T17:18:41Z

/cherrypick release-0.12
/cherrypick release-0.11

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-06T17:18:43Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.11`, `release-0.12` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5537#issuecomment-2949928706):

>/cherrypick release-0.12
>/cherrypick release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-06T17:35:18Z

@mimowo: new pull request created: #5550

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5537#issuecomment-2949928706):

>/cherrypick release-0.12
>/cherrypick release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-06T17:35:52Z

@mimowo: #5537 failed to apply on top of branch "release-0.11":
```
Applying: Cleanup Makefile.
.git/rebase-apply/patch:54: space before tab in indent.
  		$(KUSTOMIZE) edit set image backend=$(STAGING_IMAGE_REGISTRY)/kueueviz-backend:$(RELEASE_BRANCH) && \
.git/rebase-apply/patch:55: space before tab in indent.
  		$(KUSTOMIZE) edit set image frontend=$(STAGING_IMAGE_REGISTRY)/kueueviz-frontend:$(RELEASE_BRANCH))
warning: 2 lines add whitespace errors.
Using index info to reconstruct a base tree...
M	Makefile
M	cloudbuild.yaml
M	hack/helm-chart-package.sh
Falling back to patching base and 3-way merge...
Auto-merging hack/helm-chart-package.sh
Auto-merging cloudbuild.yaml
Auto-merging Makefile
CONFLICT (content): Merge conflict in Makefile
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Cleanup Makefile.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5537#issuecomment-2949928706):

>/cherrypick release-0.12
>/cherrypick release-0.11


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T12:37:59Z

- **cloudbuild.yaml:16** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T12:37:59Z
```diff
@@ -13,7 +13,6 @@ steps:
       - helm-chart-push
       - kueueviz-image-push
     env:
-      - IMAGE_REGISTRY=us-central1-docker.pkg.dev/k8s-staging-images/kueue
```

  I don't think this is necessary since we already have a default value in the Makefile.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T12:47:44Z

- **Makefile:238** by [@mimowo](https://github.com/mimowo) — 2025-06-06T12:47:44Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  Oh instead of maintaining this I would like to have a preparatory PR which defines 
  `IMAGE_EXTRA_TAG=git branch --show-current` unconditionally, and we drop this parameter from cloudbuild.
  In the current form this parameter is confusing imo, and complicates the code here for now reason.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T12:49:21Z

- **Makefile:278** by [@mimowo](https://github.com/mimowo) — 2025-06-06T12:49:21Z
```diff
@@ -280,10 +281,12 @@ ifndef ignore-not-found
   ignore-not-found = false
 endif
 
-clean-manifests = (cd config/components/manager && $(KUSTOMIZE) edit set image controller=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:$(RELEASE_BRANCH))
-clean-kueueviz-manifests = (cd config/components/kueueviz && \
-  $(KUSTOMIZE) edit set image backend=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend:$(RELEASE_BRANCH) && \
-  $(KUSTOMIZE) edit set image frontend=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-frontend:$(RELEASE_BRANCH))
+clean-manifests = \
```

  Please split the PR into smaller corresponding to specific cleanups. This seems just formatting change, this is very different that other changes in the file.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T12:49:58Z

- **cloudbuild.yaml:16** by [@mimowo](https://github.com/mimowo) — 2025-06-06T12:49:58Z
```diff
@@ -13,7 +13,6 @@ steps:
       - helm-chart-push
       - kueueviz-image-push
     env:
-      - IMAGE_REGISTRY=us-central1-docker.pkg.dev/k8s-staging-images/kueue
```

  nice one, the smaller the file the better. I also want to get rid of IMAGE_EXTRA_TAG at least (another comment).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T12:53:25Z

- **Makefile:42** by [@mimowo](https://github.com/mimowo) — 2025-06-06T12:53:25Z
```diff
@@ -33,28 +33,27 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-IMAGE_BUILD_EXTRA_OPTS ?=
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
+
+STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images/kueue
+IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)
+
 IMAGE_NAME := kueue
 IMAGE_NAME_KUEUEVIZ_BACKEND := kueueviz-backend
 IMAGE_NAME_KUEUEVIZ_FRONTEND := kueueviz-frontend
```

  Actually, can you send a separete PR which drops `IMAGE_NAME_KUEUEVIZ_BACKEND`. I don't think we need this as variable. Same for frontend once. I introduced them following the `IMAGE_NAME` example, but I think there is really no point parametrizing constants. Then we could drop `IMAGE_REPO_KUEUEVIZ_BACKEND` and just use 
  `$(IMAGE_REGISTRY)/kueueviz-backend` I think it will be easier to read.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T13:51:57Z

- **Makefile:42** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T13:51:57Z
```diff
@@ -33,28 +33,27 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-IMAGE_BUILD_EXTRA_OPTS ?=
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
+
+STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images/kueue
+IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)
+
 IMAGE_NAME := kueue
 IMAGE_NAME_KUEUEVIZ_BACKEND := kueueviz-backend
 IMAGE_NAME_KUEUEVIZ_FRONTEND := kueueviz-frontend
```

  #5539

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T14:16:03Z

- **Makefile:278** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T14:16:02Z
```diff
@@ -280,10 +281,12 @@ ifndef ignore-not-found
   ignore-not-found = false
 endif
 
-clean-manifests = (cd config/components/manager && $(KUSTOMIZE) edit set image controller=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:$(RELEASE_BRANCH))
-clean-kueueviz-manifests = (cd config/components/kueueviz && \
-  $(KUSTOMIZE) edit set image backend=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend:$(RELEASE_BRANCH) && \
-  $(KUSTOMIZE) edit set image frontend=us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-frontend:$(RELEASE_BRANCH))
+clean-manifests = \
```

  #5540

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T14:18:33Z

- **Makefile:238** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T14:18:32Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  We don't need to build this tag locally, for example in `make kind-image-build`. So I think IMAGE_EXTRA_TAG is still useful.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T14:43:49Z

- **Makefile:238** by [@mimowo](https://github.com/mimowo) — 2025-06-06T14:43:49Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  can you check this makes the build any longer? I doubt really because bother tags are identical so I would assume docker can reduplicate and just tag two images. 
  
  I prefer to avoid this nested ifs. this is error prone

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T14:44:38Z

- **Makefile:238** by [@mimowo](https://github.com/mimowo) — 2025-06-06T14:44:38Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  I think we keep the variable name, just make it unconditionally equal to branch name

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T14:59:06Z

- **Makefile:238** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T14:59:06Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  I'm wondering if we need to use RELEASE_BRANCH in this case, like we did for the importer and debug image. WDYT?
  
  https://github.com/kubernetes-sigs/kueue/blob/89614ab669b6a4748111bdfde5f581e3aa9f4903/Makefile#L359
  
  https://github.com/kubernetes-sigs/kueue/blob/89614ab669b6a4748111bdfde5f581e3aa9f4903/Makefile#L372

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T15:02:54Z

- **Makefile:238** by [@mimowo](https://github.com/mimowo) — 2025-06-06T15:02:54Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  Yes, this is partly the problem currently, that the strategy is complex, and inconcistent between images.
  
  I would like to have a simple strategy and consistent for all images we produce:
  - GIT_TAG=git describe --tags --dirty --always
  - EXTRA_IMAGE_TAG=git branch --show-current

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T15:03:51Z

- **Makefile:238** by [@mimowo](https://github.com/mimowo) — 2025-06-06T15:03:51Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  The only difference would be that we don't push importer and debug images, but the tagging strategy could be commonized.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T15:06:51Z

- **Makefile:238** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T15:06:51Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  > The only difference would be that we don't push importer and debug images, but the tagging strategy could be commonized.
  
  We push importer and debug images. 
  
  https://github.com/kubernetes-sigs/kueue/blob/4d7324dcd43b94ab9a9a5127bcf2d9f8d5401aa0/cloudbuild.yaml#L11-L12

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T15:09:40Z

- **Makefile:238** by [@mimowo](https://github.com/mimowo) — 2025-06-06T15:09:40Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  Ah, indeed, I meant promote actually.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T15:19:03Z

- **Makefile:238** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T15:19:03Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  > Ah, indeed, I meant promote actually.
  
  Ah, I see what you mean. But I'm still wondering – what if we use RELEASE_BRANCH? I think it should work that way, and we wouldn't need EXTRA_IMAGE_TAG at all.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T15:28:57Z

- **Makefile:238** by [@mimowo](https://github.com/mimowo) — 2025-06-06T15:28:57Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  yes I agree, using RELEASE BRANCH would do the same mostly, but I think it would be confusing, because we would use this variable outside of release.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T15:41:55Z

- **Makefile:238** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T15:41:55Z
```diff
@@ -238,15 +237,17 @@ image-local-push: image-local-build
 
 .PHONY: image-build
 image-build:
-	$(IMAGE_BUILD_CMD) -t $(IMAGE_TAG) \
+	$(IMAGE_BUILD_CMD) \
+		-t $(IMAGE_TAG) \
+		$(if $(IMAGE_EXTRA_TAG),-t $(IMAGE_REPO):$(IMAGE_EXTRA_TAG)) \
```

  #5543

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T16:59:43Z

- **Makefile:48** by [@mimowo](https://github.com/mimowo) — 2025-06-06T16:59:43Z
```diff
@@ -33,15 +33,20 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
+
+STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images/kueue
+IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)
+
 IMAGE_REPO := $(IMAGE_REGISTRY)/kueue
 IMAGE_REPO_KUEUEVIZ_BACKEND := $(IMAGE_REGISTRY)/kueueviz-backend
 IMAGE_REPO_KUEUEVIZ_FRONTEND := $(IMAGE_REGISTRY)/kueueviz-frontend
-IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_BACKEND ?= $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_FRONTEND ?= $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
-HELM_CHART_REPO := $(STAGING_IMAGE_REGISTRY)/kueue/charts
+
+IMAGE_TAG := $(IMAGE_REPO):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_BACKEND := $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_FRONTEND := $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
+
+HELM_CHART_REPO ?= $(STAGING_IMAGE_REGISTRY)/charts
```

  I don't think we need this variable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T17:01:09Z

- **Makefile:37** by [@mimowo](https://github.com/mimowo) — 2025-06-06T17:01:09Z
```diff
@@ -33,15 +33,20 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
```

  Yeah, this was rather weird that we call it "REGISTRY" at both levels - `us-central1-docker.pkg.dev/k8s-staging-images` and for `kueue`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T17:04:34Z

- **Makefile:48** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T17:04:34Z
```diff
@@ -33,15 +33,20 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
+
+STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images/kueue
+IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)
+
 IMAGE_REPO := $(IMAGE_REGISTRY)/kueue
 IMAGE_REPO_KUEUEVIZ_BACKEND := $(IMAGE_REGISTRY)/kueueviz-backend
 IMAGE_REPO_KUEUEVIZ_FRONTEND := $(IMAGE_REGISTRY)/kueueviz-frontend
-IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_BACKEND ?= $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_FRONTEND ?= $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
-HELM_CHART_REPO := $(STAGING_IMAGE_REGISTRY)/kueue/charts
+
+IMAGE_TAG := $(IMAGE_REPO):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_BACKEND := $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_FRONTEND := $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
+
+HELM_CHART_REPO ?= $(STAGING_IMAGE_REGISTRY)/charts
```

  I thought we change it when pushing to registry.k8s.io/kueue/charts, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T17:08:43Z

- **Makefile:48** by [@mimowo](https://github.com/mimowo) — 2025-06-06T17:08:43Z
```diff
@@ -33,15 +33,20 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
+
+STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images/kueue
+IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)
+
 IMAGE_REPO := $(IMAGE_REGISTRY)/kueue
 IMAGE_REPO_KUEUEVIZ_BACKEND := $(IMAGE_REGISTRY)/kueueviz-backend
 IMAGE_REPO_KUEUEVIZ_FRONTEND := $(IMAGE_REGISTRY)/kueueviz-frontend
-IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_BACKEND ?= $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_FRONTEND ?= $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
-HELM_CHART_REPO := $(STAGING_IMAGE_REGISTRY)/kueue/charts
+
+IMAGE_TAG := $(IMAGE_REPO):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_BACKEND := $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_FRONTEND := $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
+
+HELM_CHART_REPO ?= $(STAGING_IMAGE_REGISTRY)/charts
```

  we never push directly to `registry.k8s.io/kueue/charts`, this is why [here](https://github.com/kubernetes-sigs/kueue/pull/5537/files#diff-18a26ee2d548b66a6e7dce0e9427d2e23f7785f4bb14441ccf98d30489f95fe8R27) it is always using staging. This variable in `hack/helm-chart-package.sh` indicates where we push.
  
  We only later promote (copy the images into registry.k8s.io/kueue).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T17:10:32Z

- **Makefile:48** by [@mimowo](https://github.com/mimowo) — 2025-06-06T17:10:32Z
```diff
@@ -33,15 +33,20 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
+
+STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images/kueue
+IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)
+
 IMAGE_REPO := $(IMAGE_REGISTRY)/kueue
 IMAGE_REPO_KUEUEVIZ_BACKEND := $(IMAGE_REGISTRY)/kueueviz-backend
 IMAGE_REPO_KUEUEVIZ_FRONTEND := $(IMAGE_REGISTRY)/kueueviz-frontend
-IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_BACKEND ?= $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_FRONTEND ?= $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
-HELM_CHART_REPO := $(STAGING_IMAGE_REGISTRY)/kueue/charts
+
+IMAGE_TAG := $(IMAGE_REPO):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_BACKEND := $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_FRONTEND := $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
+
+HELM_CHART_REPO ?= $(STAGING_IMAGE_REGISTRY)/charts
```

  And actually the [release steps](https://github.com/kubernetes-sigs/kueue/blob/main/.github/ISSUE_TEMPLATE/NEW_RELEASE.md) are fairly well maintained, so there is no override for `HELM_CHART_REPO`. We only override `IMAGE_REGISTRY` when calling `make artifacts IMAGE_REGISTRY=registry.k8s.io/kueue GIT_TAG=$VERSION`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T17:10:32Z

- **Makefile:48** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T17:10:32Z
```diff
@@ -33,15 +33,20 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
+
+STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images/kueue
+IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)
+
 IMAGE_REPO := $(IMAGE_REGISTRY)/kueue
 IMAGE_REPO_KUEUEVIZ_BACKEND := $(IMAGE_REGISTRY)/kueueviz-backend
 IMAGE_REPO_KUEUEVIZ_FRONTEND := $(IMAGE_REGISTRY)/kueueviz-frontend
-IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_BACKEND ?= $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_FRONTEND ?= $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
-HELM_CHART_REPO := $(STAGING_IMAGE_REGISTRY)/kueue/charts
+
+IMAGE_TAG := $(IMAGE_REPO):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_BACKEND := $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_FRONTEND := $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
+
+HELM_CHART_REPO ?= $(STAGING_IMAGE_REGISTRY)/charts
```

  Ah, I see. Than you right. We don't need it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T17:11:58Z

- **Makefile:48** by [@mimowo](https://github.com/mimowo) — 2025-06-06T17:11:58Z
```diff
@@ -33,15 +33,20 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
+
+STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images/kueue
+IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)
+
 IMAGE_REPO := $(IMAGE_REGISTRY)/kueue
 IMAGE_REPO_KUEUEVIZ_BACKEND := $(IMAGE_REGISTRY)/kueueviz-backend
 IMAGE_REPO_KUEUEVIZ_FRONTEND := $(IMAGE_REGISTRY)/kueueviz-frontend
-IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_BACKEND ?= $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_FRONTEND ?= $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
-HELM_CHART_REPO := $(STAGING_IMAGE_REGISTRY)/kueue/charts
+
+IMAGE_TAG := $(IMAGE_REPO):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_BACKEND := $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_FRONTEND := $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
+
+HELM_CHART_REPO ?= $(STAGING_IMAGE_REGISTRY)/charts
```

  Actually, this is an example, why it is better not declare consts in Makefile via "?=" as variables, because then it is never clear if something is used or not.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-06T17:14:12Z

- **Makefile:48** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-06T17:14:12Z
```diff
@@ -33,15 +33,20 @@ CLI_PLATFORMS ?= linux/amd64,linux/arm64,darwin/amd64,darwin/arm64
 VIZ_PLATFORMS ?= linux/amd64,linux/arm64,linux/s390x,linux/ppc64le
 DOCKER_BUILDX_CMD ?= docker buildx
 IMAGE_BUILD_CMD ?= $(DOCKER_BUILDX_CMD) build
-STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images
-IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)/kueue
+
+STAGING_IMAGE_REGISTRY := us-central1-docker.pkg.dev/k8s-staging-images/kueue
+IMAGE_REGISTRY ?= $(STAGING_IMAGE_REGISTRY)
+
 IMAGE_REPO := $(IMAGE_REGISTRY)/kueue
 IMAGE_REPO_KUEUEVIZ_BACKEND := $(IMAGE_REGISTRY)/kueueviz-backend
 IMAGE_REPO_KUEUEVIZ_FRONTEND := $(IMAGE_REGISTRY)/kueueviz-frontend
-IMAGE_TAG ?= $(IMAGE_REPO):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_BACKEND ?= $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
-IMAGE_TAG_KUEUEVIZ_FRONTEND ?= $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
-HELM_CHART_REPO := $(STAGING_IMAGE_REGISTRY)/kueue/charts
+
+IMAGE_TAG := $(IMAGE_REPO):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_BACKEND := $(IMAGE_REPO_KUEUEVIZ_BACKEND):$(GIT_TAG)
+IMAGE_TAG_KUEUEVIZ_FRONTEND := $(IMAGE_REPO_KUEUEVIZ_FRONTEND):$(GIT_TAG)
+
+HELM_CHART_REPO ?= $(STAGING_IMAGE_REGISTRY)/charts
```

  Removed it from Makefile.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-06T17:18:26Z

/lgtm
/approve

There are a lot of changes recently to the build process, but I think they all go in the right direction, and even if there is somewhere a small bug, then we will fix. It will benefit the project long term.

Thank you 👍
