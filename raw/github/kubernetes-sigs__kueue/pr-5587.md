# PR #5587: Fix second pass entries outliving the workload delete

**Summary**: Fix second pass entries outliving the workload delete

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5587

**Last updated**: 2025-07-10T12:05:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `outliving-fixed` → `main`
- **Created**: 2025-06-09T14:28:12Z
- **Updated**: 2025-07-10T12:05:06Z
- **Merged**: 2025-06-10T12:32:25Z
- **Closed**: 2025-06-10T12:32:25Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `kind/flake`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 4   **+129 / -34**

## Description

#### What type of PR is this?

/kind bug
/kind flake 

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5511

#### Special notes for your reviewer:

The issue is that the forgetWorkload called on the secondPassQueue would hit the void while the workload awaits for 1s [here](https://github.com/kubernetes-sigs/kueue/blob/d2f9f2efcd066f007930e9f04fe7629ed8983a0a/pkg/queue/manager.go#L793). Now we prequeue such a workload, and if forgetWorkload is called in the meanwhile, then "enqueue" is forgotten either.

Adding a reliably reproducing integration test is tricky. Running the test https://github.com/kubernetes-sigs/kueue/pull/5513 in a loop reproduces in consistently, but requires multiple many repeats.

To prove the fix works:
- I ran the CI build, with commit https://github.com/kubernetes-sigs/kueue/pull/5587/commits/418b65e2b5fe12fed7c5125cccad3be629d9cb3a, and it passed [here]( https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/5587/pull-kueue-test-integration-baseline-main/1932086255051345920)
- I ran it locally looped >100 times and it passed consistently
- I revert the mitigation: https://github.com/kubernetes-sigs/kueue/pull/5461. Before the mitigation it was failing pretty often.
- I add unit tests

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: fix the scenario when deleted workload still lives in the cache.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-09T14:28:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0259053402d873bd13249f53ddc4b9c4dd6ebe0c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6846ef7eaf08160008364af5 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-5587--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-5587--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTU1ODctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.EF4MBDv9J1BhXJ85OhdEhBc9SwrKOrtSnF6Uxkx_bjo)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-5587--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-09T14:28:20Z

cc @PBundyra

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-10T06:09:31Z

/assign @PBundyra 
cc @tenzen-y 
/cherry-pick release-0.12

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-10T06:09:34Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.12` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5587#issuecomment-2957794278):

>/assign @PBundyra 
>cc @tenzen-y 
>/cherry-pick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-10T10:22:54Z

/lgtm
/hold
Feel free to unhold if you address the nit

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-10T10:23:01Z

LGTM label has been added.  <details>Git tree hash: b64464dc4c4f3527e75f17b2c02ea83dc56f9417</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-10T11:50:13Z

LGTM label has been added.  <details>Git tree hash: 74e8cd82ee88dfb920df5ad9e8f35d4f58283b03</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-10T11:50:13Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5587#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5587#pullrequestreview-2913199691" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-10T11:50:58Z

/unhold
I synced with @PBundyra that we don't need to align the CQ names in the PR

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-10T12:33:03Z

@mimowo: #5587 failed to apply on top of branch "release-0.12":
```
Applying: Fix the second pass entries outliving workload delete
Using index info to reconstruct a base tree...
M	pkg/queue/manager_test.go
M	pkg/queue/second_pass_queue.go
M	test/integration/singlecluster/tas/tas_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/tas/tas_test.go
Auto-merging pkg/queue/second_pass_queue.go
CONFLICT (content): Merge conflict in pkg/queue/second_pass_queue.go
Auto-merging pkg/queue/manager_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Fix the second pass entries outliving workload delete

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5587#issuecomment-2957794278):

>/assign @PBundyra 
>cc @tenzen-y 
>/cherry-pick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-10T10:18:58Z

- **test/integration/singlecluster/tas/tas_test.go:1** by [@PBundyra](https://github.com/PBundyra) — 2025-06-10T10:18:58Z
  Can we be consistent with the name changes and stick with either `cq` or `cluster-queue`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-10T10:28:22Z

- **test/integration/singlecluster/tas/tas_test.go:1** by [@mimowo](https://github.com/mimowo) — 2025-06-10T10:28:21Z
  Well, in this commit I just blindly reverted the mitigation PR. I'm not clear what is the benefit of consistent naming. Detecting flakes is maybe a benefit, otoh tracing the logs becomes harder. From the perspective of being able to trace the logs I would even say using "GenerateName" is better.
  
  In any case we are not consistent at all in a bigger picture, so I would keep it out of scope, and recommend opening issue if you feel it is the right direction:
  
  ![image](https://github.com/user-attachments/assets/21df129e-29d1-4f0d-b426-5246563081f6)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-10T10:49:37Z

Overall LGTM
Thank you

- **pkg/queue/manager_test.go:1297** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-10T10:47:06Z
```diff
@@ -1285,3 +1287,102 @@ func TestGetPendingWorkloadsInfo(t *testing.T) {
 		})
 	}
 }
+
+func TestQueueSecondPassIfNeeded(t *testing.T) {
+	now := time.Now()
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
```

  Why do we need this? Doesn't `utiltesting.NewFakeClient()` perform the same thing?

- **pkg/queue/manager_test.go:1355** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-10T10:48:42Z
```diff
@@ -1285,3 +1287,102 @@ func TestGetPendingWorkloadsInfo(t *testing.T) {
 		})
 	}
 }
+
+func TestQueueSecondPassIfNeeded(t *testing.T) {
+	now := time.Now()
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	baseWorkloadNeedingSecondPass := utiltesting.MakeWorkload("foo", "default").
+		Queue("tas-main").
+		PodSets(*utiltesting.MakePodSet("one", 1).
+			RequiredTopologyRequest(corev1.LabelHostname).
+			Request(corev1.ResourceCPU, "1").
+			Obj()).
+		ReserveQuota(
+			utiltesting.MakeAdmission("tas-main", "one").
+				Assignment(corev1.ResourceCPU, "tas-default", "1000m").
+				DelayedTopologyRequest(kueue.DelayedTopologyRequestStatePending).
+				AssignmentPodCount(1).Obj(),
+		).
+		AdmissionCheck(kueue.AdmissionCheckState{
+			Name:  "prov-check",
+			State: kueue.CheckStateReady,
+		}).
+		Obj()
+	baseWorkloadNotNeedingSecondPass := utiltesting.MakeWorkload("bar", "default").
+		Queue("tas-main").
+		PodSets(*utiltesting.MakePodSet("one", 1).
+			RequiredTopologyRequest(corev1.LabelHostname).
+			Request(corev1.ResourceCPU, "1").
+			Obj()).
+		Obj()
+
+	cases := map[string]struct {
+		workloads []*kueue.Workload
+		deleted   sets.Set[workload.WorkloadReference]
+		passTime  time.Duration
+		wantReady sets.Set[workload.WorkloadReference]
+	}{
+		"single queued workload checked immediately": {
+			workloads: []*kueue.Workload{baseWorkloadNeedingSecondPass.DeepCopy()},
+		},
+		"single queued workload checked after 1s": {
+			workloads: []*kueue.Workload{
+				baseWorkloadNeedingSecondPass.DeepCopy(),
+				baseWorkloadNotNeedingSecondPass.DeepCopy(),
+			},
+			passTime:  time.Second,
+			wantReady: sets.New(workload.Key(baseWorkloadNeedingSecondPass)),
+		},
+		"single queued workload deleted in the meanwhile": {
+			workloads: []*kueue.Workload{
+				baseWorkloadNeedingSecondPass.DeepCopy(),
+				baseWorkloadNotNeedingSecondPass.DeepCopy(),
+			},
+			deleted:  sets.New(workload.Key(baseWorkloadNeedingSecondPass)),
+			passTime: time.Second,
+		},
+		"two queued workloads, one deleted in the meanwhile": {
+			workloads: []*kueue.Workload{
+				baseWorkloadNeedingSecondPass.DeepCopy(),
+				func() *kueue.Workload {
+					wl := baseWorkloadNeedingSecondPass.DeepCopy()
+					wl.Name = "second"
+					return wl
+				}(),
```

  Let's use `Clone` instead of anonymous func.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-10T11:05:39Z

- **pkg/queue/manager_test.go:1297** by [@mimowo](https://github.com/mimowo) — 2025-06-10T11:05:39Z
```diff
@@ -1285,3 +1287,102 @@ func TestGetPendingWorkloadsInfo(t *testing.T) {
 		})
 	}
 }
+
+func TestQueueSecondPassIfNeeded(t *testing.T) {
+	now := time.Now()
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
```

  I just copy-pasted from another test, let me try if I can drop.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-10T11:36:43Z

- **pkg/queue/manager_test.go:1297** by [@mimowo](https://github.com/mimowo) — 2025-06-10T11:36:43Z
```diff
@@ -1285,3 +1287,102 @@ func TestGetPendingWorkloadsInfo(t *testing.T) {
 		})
 	}
 }
+
+func TestQueueSecondPassIfNeeded(t *testing.T) {
+	now := time.Now()
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
```

  Done, dropped and it works, maybe we could also clean it in other places as a follow up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-10T11:36:48Z

- **pkg/queue/manager_test.go:1355** by [@mimowo](https://github.com/mimowo) — 2025-06-10T11:36:48Z
```diff
@@ -1285,3 +1287,102 @@ func TestGetPendingWorkloadsInfo(t *testing.T) {
 		})
 	}
 }
+
+func TestQueueSecondPassIfNeeded(t *testing.T) {
+	now := time.Now()
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
+	baseWorkloadNeedingSecondPass := utiltesting.MakeWorkload("foo", "default").
+		Queue("tas-main").
+		PodSets(*utiltesting.MakePodSet("one", 1).
+			RequiredTopologyRequest(corev1.LabelHostname).
+			Request(corev1.ResourceCPU, "1").
+			Obj()).
+		ReserveQuota(
+			utiltesting.MakeAdmission("tas-main", "one").
+				Assignment(corev1.ResourceCPU, "tas-default", "1000m").
+				DelayedTopologyRequest(kueue.DelayedTopologyRequestStatePending).
+				AssignmentPodCount(1).Obj(),
+		).
+		AdmissionCheck(kueue.AdmissionCheckState{
+			Name:  "prov-check",
+			State: kueue.CheckStateReady,
+		}).
+		Obj()
+	baseWorkloadNotNeedingSecondPass := utiltesting.MakeWorkload("bar", "default").
+		Queue("tas-main").
+		PodSets(*utiltesting.MakePodSet("one", 1).
+			RequiredTopologyRequest(corev1.LabelHostname).
+			Request(corev1.ResourceCPU, "1").
+			Obj()).
+		Obj()
+
+	cases := map[string]struct {
+		workloads []*kueue.Workload
+		deleted   sets.Set[workload.WorkloadReference]
+		passTime  time.Duration
+		wantReady sets.Set[workload.WorkloadReference]
+	}{
+		"single queued workload checked immediately": {
+			workloads: []*kueue.Workload{baseWorkloadNeedingSecondPass.DeepCopy()},
+		},
+		"single queued workload checked after 1s": {
+			workloads: []*kueue.Workload{
+				baseWorkloadNeedingSecondPass.DeepCopy(),
+				baseWorkloadNotNeedingSecondPass.DeepCopy(),
+			},
+			passTime:  time.Second,
+			wantReady: sets.New(workload.Key(baseWorkloadNeedingSecondPass)),
+		},
+		"single queued workload deleted in the meanwhile": {
+			workloads: []*kueue.Workload{
+				baseWorkloadNeedingSecondPass.DeepCopy(),
+				baseWorkloadNotNeedingSecondPass.DeepCopy(),
+			},
+			deleted:  sets.New(workload.Key(baseWorkloadNeedingSecondPass)),
+			passTime: time.Second,
+		},
+		"two queued workloads, one deleted in the meanwhile": {
+			workloads: []*kueue.Workload{
+				baseWorkloadNeedingSecondPass.DeepCopy(),
+				func() *kueue.Workload {
+					wl := baseWorkloadNeedingSecondPass.DeepCopy()
+					wl.Name = "second"
+					return wl
+				}(),
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-10T11:48:44Z

- **pkg/queue/manager_test.go:1297** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-10T11:48:43Z
```diff
@@ -1285,3 +1287,102 @@ func TestGetPendingWorkloadsInfo(t *testing.T) {
 		})
 	}
 }
+
+func TestQueueSecondPassIfNeeded(t *testing.T) {
+	now := time.Now()
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
```

  SGTM, feel free to open it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-10T11:50:06Z

Thank you
/lgtm
/approve

If you believe that you already got agreement with @PBundyra, feel free to merge this one.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-10T12:02:56Z

- **pkg/queue/manager_test.go:1297** by [@mimowo](https://github.com/mimowo) — 2025-06-10T12:02:56Z
```diff
@@ -1285,3 +1287,102 @@ func TestGetPendingWorkloadsInfo(t *testing.T) {
 		})
 	}
 }
+
+func TestQueueSecondPassIfNeeded(t *testing.T) {
+	now := time.Now()
+
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %s", err)
+	}
```

  https://github.com/kubernetes-sigs/kueue/issues/5601
