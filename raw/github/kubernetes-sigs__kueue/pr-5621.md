# PR #5621: Documentation describing the TAS failed node replacement

**Summary**: Documentation describing the TAS failed node replacement

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5621

**Last updated**: 2025-06-16T12:05:37Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `hot_swap_docu` → `main`
- **Created**: 2025-06-11T10:38:58Z
- **Updated**: 2025-06-16T12:05:37Z
- **Merged**: 2025-06-16T12:04:58Z
- **Closed**: 2025-06-16T12:04:58Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 1   **+25 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Documentation paragraph for TAS failed node replacement.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-11T10:39:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3deea88e6351784c3aad0f65121f4414e0884b59 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/684be9461a1160000859ee51 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-5621--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-5621--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTU2MjEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.gsw_aFP5PasmNTh89JjYBwL7Oc7fM5z4Km3cPQzxx1k)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-5621--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-11T14:07:32Z

It looks good overall, but makes me wonder we should have some "hands on" tutorial, similar as for WaitForPodsReady or Objects retention policies. Opened: https://github.com/kubernetes-sigs/kueue/issues/5626. Since this is substantially more work, I consider this out of scope for the PR.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-12T11:23:21Z

/approve
Leaving lgtm to some another involved reviewer for another pair of eyes. Thank you 👍 
/cherrypick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-12T11:23:23Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5621#issuecomment-2966243289):

>/approve
>Leaving lgtm to some another involved reviewer for another pair of eyes. Thank you 👍 
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-12T11:23:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5621#issuecomment-2966243289" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5621#" title="Author self-approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-16T11:46:38Z

Thanks
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-16T11:46:44Z

LGTM label has been added.  <details>Git tree hash: 4a2c6bb9a2c15a4ae22c36a998b5b7c4667e66dd</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-06-16T12:05:37Z

@mimowo: new pull request created: #5661

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5621#issuecomment-2966243289):

>/approve
>Leaving lgtm to some another involved reviewer for another pair of eyes. Thank you 👍 
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-11T12:17:19Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:125** by [@PBundyra](https://github.com/PBundyra) — 2025-06-11T12:17:19Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
+to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the feature gate `TASFailedNodeReplacement` to `true`. With this feature, 
```

  Can we reference the installation guide regarding feature gates?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-11T12:17:40Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:128** by [@PBundyra](https://github.com/PBundyra) — 2025-06-11T12:17:40Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
+to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the feature gate `TASFailedNodeReplacement` to `true`. With this feature, 
+upon node failure, TAS tries to find a replacement of the broken node for all
+the affected workloads, without changing the rest of the topoogy assignment.
+Currently this works for only a single node failure and the node is assumed
```

  Can we describe the workload gets evicted if multiple node fail?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-11T12:18:12Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:121** by [@PBundyra](https://github.com/PBundyra) — 2025-06-11T12:18:12Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
```

  Can we start with it accounts also for node deletions, not only failures?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-11T12:20:38Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:123** by [@PBundyra](https://github.com/PBundyra) — 2025-06-11T12:20:38Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
```

  Can we elaborate on that? I think it's little misleading to pass the buck on core scheduler, the issue exists because we Kueue with TAS injects nodeSelectors to a particular node, which in case of failure are stale

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-06-11T13:40:44Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:121** by [@gabesaba](https://github.com/gabesaba) — 2025-06-11T13:35:41Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
```

  s/assigmend/assignment

- **site/content/en/docs/concepts/topology_aware_scheduling.md:121** by [@gabesaba](https://github.com/gabesaba) — 2025-06-11T13:36:50Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
```

  podset may map to many nodes. pod to node mapping is more precise

- **site/content/en/docs/concepts/topology_aware_scheduling.md:127** by [@gabesaba](https://github.com/gabesaba) — 2025-06-11T13:38:07Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
+to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the feature gate `TASFailedNodeReplacement` to `true`. With this feature, 
+upon node failure, TAS tries to find a replacement of the broken node for all
+the affected workloads, without changing the rest of the topoogy assignment.
```

  topoogy

- **site/content/en/docs/concepts/topology_aware_scheduling.md:126** by [@gabesaba](https://github.com/gabesaba) — 2025-06-11T13:38:54Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
+to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the feature gate `TASFailedNodeReplacement` to `true`. With this feature, 
+upon node failure, TAS tries to find a replacement of the broken node for all
```

  ```suggestion
  TAS tries to find a replacement upon node failure or deletion for all
  ```

- **site/content/en/docs/concepts/topology_aware_scheduling.md:122** by [@gabesaba](https://github.com/gabesaba) — 2025-06-11T13:40:38Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
```

  ```suggestion
  which occur during the runtime of a workload, the broken node cannot be automatically replaced
  ```

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-11T13:53:37Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:122** by [@pajakd](https://github.com/pajakd) — 2025-06-11T13:53:37Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-11T13:53:43Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:125** by [@pajakd](https://github.com/pajakd) — 2025-06-11T13:53:43Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
+to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the feature gate `TASFailedNodeReplacement` to `true`. With this feature, 
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-11T13:53:46Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:128** by [@pajakd](https://github.com/pajakd) — 2025-06-11T13:53:46Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
+to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the feature gate `TASFailedNodeReplacement` to `true`. With this feature, 
+upon node failure, TAS tries to find a replacement of the broken node for all
+the affected workloads, without changing the rest of the topoogy assignment.
+Currently this works for only a single node failure and the node is assumed
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-11T13:53:48Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:121** by [@pajakd](https://github.com/pajakd) — 2025-06-11T13:53:48Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-11T13:53:51Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:123** by [@pajakd](https://github.com/pajakd) — 2025-06-11T13:53:50Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
```

  Oh, sure -- I ment that by "TAS finds a fixed assignment". Rephrased.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-11T13:53:56Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:121** by [@pajakd](https://github.com/pajakd) — 2025-06-11T13:53:56Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-11T13:53:59Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:121** by [@pajakd](https://github.com/pajakd) — 2025-06-11T13:53:59Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-11T13:54:02Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:127** by [@pajakd](https://github.com/pajakd) — 2025-06-11T13:54:02Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
+to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the feature gate `TASFailedNodeReplacement` to `true`. With this feature, 
+upon node failure, TAS tries to find a replacement of the broken node for all
+the affected workloads, without changing the rest of the topoogy assignment.
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-11T13:54:04Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:126** by [@pajakd](https://github.com/pajakd) — 2025-06-11T13:54:04Z
```diff
@@ -117,6 +117,18 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assigmend of nodes to PodSets and in case of any node failures,
+during the runtime of a workload, the broken node cannot be automatically replaced
+by Kubernetes scheduler. To mitigate this issue, a node hot swap was introduced 
+to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the feature gate `TASFailedNodeReplacement` to `true`. With this feature, 
+upon node failure, TAS tries to find a replacement of the broken node for all
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-06-11T14:46:58Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:121** by [@mwielgus](https://github.com/mwielgus) — 2025-06-11T14:46:58Z
```diff
@@ -117,6 +117,20 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assignment of pods to nodes and injects a NodeSelector to make
```

  This depends on the actual topological labels specified in the configuration. Please include an explanation what happens if topology ends on the rack level.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-11T14:52:38Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:120** by [@mimowo](https://github.com/mimowo) — 2025-06-11T14:50:55Z
```diff
@@ -117,6 +117,20 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
```

  please also add a note in this section that since finding replacement node within the old domain (like rack) may not be possible we recommend using waitForPodsReady.recoveryTimeout, to prevent workload waiting for the replacement indefinetly

- **site/content/en/docs/concepts/topology_aware_scheduling.md:131** by [@mimowo](https://github.com/mimowo) — 2025-06-11T14:52:26Z
```diff
@@ -117,6 +117,20 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assignment of pods to nodes and injects a NodeSelector to make
+sure the pods get scheduled on the selected nodes. But this means that in case 
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. To mitigate this issue, a node hot swap was 
+introduced to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) 
+`TASFailedNodeReplacement` to `true`. With this feature, TAS tries to find a 
+replacement upon node failure or deletion for all the affected workloads, without
+changing the rest of the topology assignment. Currently this works for only a single
+node failure and in case of multiple failures, the workload gets evicted. The node is 
+assumed to have failed if its  `conditions.Status.Ready` is not `True` or if the node 
```

  please add a note that it is assumed failed after 30s.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-12T07:49:54Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:131** by [@pajakd](https://github.com/pajakd) — 2025-06-12T07:49:54Z
```diff
@@ -117,6 +117,20 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assignment of pods to nodes and injects a NodeSelector to make
+sure the pods get scheduled on the selected nodes. But this means that in case 
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. To mitigate this issue, a node hot swap was 
+introduced to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) 
+`TASFailedNodeReplacement` to `true`. With this feature, TAS tries to find a 
+replacement upon node failure or deletion for all the affected workloads, without
+changing the rest of the topology assignment. Currently this works for only a single
+node failure and in case of multiple failures, the workload gets evicted. The node is 
+assumed to have failed if its  `conditions.Status.Ready` is not `True` or if the node 
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-12T07:49:58Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:120** by [@pajakd](https://github.com/pajakd) — 2025-06-12T07:49:58Z
```diff
@@ -117,6 +117,20 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-12T08:02:05Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:135** by [@mimowo](https://github.com/mimowo) — 2025-06-12T08:02:05Z
```diff
@@ -128,8 +128,14 @@ set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-r
 replacement upon node failure or deletion for all the affected workloads, without
 changing the rest of the topology assignment. Currently this works for only a single
 node failure and in case of multiple failures, the workload gets evicted. The node is 
-assumed to have failed if its  `conditions.Status.Ready` is not `True` or if the node 
-is missing (removed from the cluster). 
+assumed to have failed if its `conditions.Status.Ready` is not `True` for at least 30 
+seconds or if the node is missing (removed from the cluster). 
+
+Note that finding a replacement node within the old domain (like rack) may not always 
+be possible. Hence, we recommend configuring `waitForPodsReady.recoveryTimeout`, to prevent 
```

  ```suggestion
  be possible. Hence, we recommend using [WaitForPodsReady](link) and configuring `waitForPodsReady.recoveryTimeout`, to prevent 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-12T08:10:58Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:135** by [@mimowo](https://github.com/mimowo) — 2025-06-12T08:10:58Z
```diff
@@ -132,7 +132,7 @@ assumed to have failed if its `conditions.Status.Ready` is not `True` for at lea
 seconds or if the node is missing (removed from the cluster). 
 
 Note that finding a replacement node within the old domain (like rack) may not always 
-be possible. Hence, we recommend configuring `waitForPodsReady.recoveryTimeout`, to prevent 
+be possible. Hence, we recommend using [WaitForPodsReady](link) and configuring `waitForPodsReady.recoveryTimeout`, to prevent 
```

  ```suggestion
  be possible. Hence, we recommend using [WaitForPodsReady](/docs/tasks/manage/setup_wait_for_pods_ready/) and configuring `waitForPodsReady.recoveryTimeout`, to prevent 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-12T08:14:24Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:135** by [@mimowo](https://github.com/mimowo) — 2025-06-12T08:14:23Z
```diff
@@ -132,10 +132,9 @@ assumed to have failed if its `conditions.Status.Ready` is not `True` for at lea
 seconds or if the node is missing (removed from the cluster). 
 
 Note that finding a replacement node within the old domain (like rack) may not always 
-be possible. Hence, we recommend using [WaitForPodsReady](link) and configuring `waitForPodsReady.recoveryTimeout`, to prevent 
-the workloads from waiting for the replacement indefinetly.
-
-
+be possible. Hence, we recommend using [WaitForPodsReady](site/content/en/docs/tasks/manage/setup_wait_for_pods_ready.md)
```

  You may double check if this link renders well, checking some other places we prefix the link with `/docs/tasks/manage/`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-12T08:15:08Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:135** by [@mimowo](https://github.com/mimowo) — 2025-06-12T08:15:08Z
```diff
@@ -132,10 +132,9 @@ assumed to have failed if its `conditions.Status.Ready` is not `True` for at lea
 seconds or if the node is missing (removed from the cluster). 
 
 Note that finding a replacement node within the old domain (like rack) may not always 
-be possible. Hence, we recommend using [WaitForPodsReady](link) and configuring `waitForPodsReady.recoveryTimeout`, to prevent 
-the workloads from waiting for the replacement indefinetly.
-
-
+be possible. Hence, we recommend using [WaitForPodsReady](site/content/en/docs/tasks/manage/setup_wait_for_pods_ready.md)
```

  Example links: https://github.com/kubernetes-sigs/kueue/blob/53429e7fc77ed9989e04b1ee39058ee14104019c/site/content/en/docs/concepts/workload_priority_class.md?plain=1#L114-L115

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-12T08:16:12Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:120** by [@mimowo](https://github.com/mimowo) — 2025-06-12T08:16:12Z
```diff
@@ -117,6 +117,20 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
```

  Yes, the mechanism only works actually when `kubernetes.io/hostname` is the lowest topological label. We can just say this is a pre-requisite for enabling the feature.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-12T08:28:17Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:127** by [@PBundyra](https://github.com/PBundyra) — 2025-06-12T08:28:17Z
```diff
@@ -117,6 +117,26 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assignment of pods to nodes and injects a NodeSelector to make
+sure the pods get scheduled on the selected nodes. But this means that in case 
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. To mitigate this issue, a node hot swap was 
+introduced to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) 
+`TASFailedNodeReplacement` to `true` and the lowest lowest topological label has to be
```

  ```suggestion
  `TASFailedNodeReplacement` to `true` and the lowest topological label has to be
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-12T08:31:36Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:127** by [@PBundyra](https://github.com/PBundyra) — 2025-06-12T08:31:36Z
```diff
@@ -117,6 +117,26 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assignment of pods to nodes and injects a NodeSelector to make
+sure the pods get scheduled on the selected nodes. But this means that in case 
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. To mitigate this issue, a node hot swap was 
+introduced to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) 
+`TASFailedNodeReplacement` to `true` and the lowest lowest topological label has to be
```

  WDYT about starting the section with this assumption? Something along the lines:
  
  `When the lowest level of Topology is set to `kubernetes.io/hostname` Kueue injects NodeSelectors that point to the exact Node selected by TAS. As a result, in case of Node's failure or deletion, which occur during Workload's runtime, NodeSelectors get stale and Pods cannot migrate to a different, healthy Node.`

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-12T08:31:53Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:121** by [@pajakd](https://github.com/pajakd) — 2025-06-12T08:31:52Z
```diff
@@ -117,6 +117,20 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assignment of pods to nodes and injects a NodeSelector to make
```

  I guess this comment https://github.com/kubernetes-sigs/kueue/pull/5621#discussion_r2142027255 should be in this thread.
  
  Added an explanation that this works only if the hostname is the lowest level.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-12T08:44:54Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:127** by [@mimowo](https://github.com/mimowo) — 2025-06-12T08:44:53Z
```diff
@@ -117,6 +117,26 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assignment of pods to nodes and injects a NodeSelector to make
+sure the pods get scheduled on the selected nodes. But this means that in case 
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. To mitigate this issue, a node hot swap was 
+introduced to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) 
+`TASFailedNodeReplacement` to `true` and the lowest lowest topological label has to be
```

  Yeah, but I'm not sure about the proposal either. I think the root cause for this being hard to read is that it mixes layers of abstraction (feature description / goal, feature enablement and feature mechanics) into one blob paragraph.
  
  I would suggest to it split into paragraphs:
  1. problem statement (In order to avoid costly re-scheduling of the entire TAS workload we introduce the node hot swap feature)
  2. feature enablement (feature gate + lowest level=kubernetes.io/hostname`)
  3. mechanics and when it activates (30s after failure, or deletion, single vs multiple node failures)
  4. recommendation for recoveryTimeout

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-12T11:49:54Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:131** by [@PBundyra](https://github.com/PBundyra) — 2025-06-12T11:49:53Z
```diff
@@ -117,6 +117,30 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+When the lowest level of Topology is set to node, TAS finds a fixed assignment
+of pods to nodes and injects a NodeSelector to make sure the pods get scheduled
+on the selected nodes. But this means that in case
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. In order to avoid costly re-scheduling
+of the entire TAS workload we introduce the node hot swap feature to Kueue
+(starting from version 0.12).
+
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacement` to `true` and the lowest topological label has to be
+`kubernetes.io/hostname`.
```

  Can we use a note to be consistent with other places? E.g. https://github.com/kubernetes-sigs/kueue/commit/2c2178d8a5e382997a3aa0fcfcd0a5970af71e61#diff-6dd0f1f3dcfbbd90420e7557f53bfeaf635376335f77403c44ead1cb616450ddR9-R15

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-12T11:52:03Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:135** by [@PBundyra](https://github.com/PBundyra) — 2025-06-12T11:52:03Z
```diff
@@ -117,6 +117,30 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+When the lowest level of Topology is set to node, TAS finds a fixed assignment
+of pods to nodes and injects a NodeSelector to make sure the pods get scheduled
+on the selected nodes. But this means that in case
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. In order to avoid costly re-scheduling
+of the entire TAS workload we introduce the node hot swap feature to Kueue
+(starting from version 0.12).
+
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacement` to `true` and the lowest topological label has to be
+`kubernetes.io/hostname`.
+
+With this feature, TAS tries to find a replacement of the failed or deleted node for
+all the affected workloads, without changing the rest of the topology assignment.
+Currently this works only for a single node failure and in case of multiple failures,
```

  ```suggestion
  Currently this works only for a single node failure at the time and in case of multiple failures,
  ```

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-12T12:26:23Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:127** by [@pajakd](https://github.com/pajakd) — 2025-06-12T12:26:23Z
```diff
@@ -117,6 +117,26 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+TAS finds a fixed assignment of pods to nodes and injects a NodeSelector to make
+sure the pods get scheduled on the selected nodes. But this means that in case 
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. To mitigate this issue, a node hot swap was 
+introduced to Kueue (starting from version 0.12). To enable the feature, you have to 
+set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/) 
+`TASFailedNodeReplacement` to `true` and the lowest lowest topological label has to be
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-12T12:26:36Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:131** by [@pajakd](https://github.com/pajakd) — 2025-06-12T12:26:36Z
```diff
@@ -117,6 +117,30 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+When the lowest level of Topology is set to node, TAS finds a fixed assignment
+of pods to nodes and injects a NodeSelector to make sure the pods get scheduled
+on the selected nodes. But this means that in case
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. In order to avoid costly re-scheduling
+of the entire TAS workload we introduce the node hot swap feature to Kueue
+(starting from version 0.12).
+
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacement` to `true` and the lowest topological label has to be
+`kubernetes.io/hostname`.
```

  Done PTAL

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-12T14:53:59Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:133** by [@PBundyra](https://github.com/PBundyra) — 2025-06-12T14:53:58Z
```diff
@@ -117,6 +117,32 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+When the lowest level of Topology is set to node, TAS finds a fixed assignment
+of pods to nodes and injects a NodeSelector to make sure the pods get scheduled
+on the selected nodes. But this means that in case
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. In order to avoid costly re-scheduling
+of the entire TAS workload we introduce the node hot swap feature to Kueue
+(starting from version 0.12).
+
+{{% alert title="Note" color="primary" %}}
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacement` to `true` and the lowest topological label has to be
+`kubernetes.io/hostname`.
+{{% /alert %}}
```

  Can we move the note to the top of the section and mention in it the feature works for v0.12+

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-06-13T09:16:58Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:133** by [@pajakd](https://github.com/pajakd) — 2025-06-13T09:16:58Z
```diff
@@ -117,6 +117,32 @@ Check also [PodSet updates in ProvisioningRequestConfig](site/content/en/docs/ad
 to see how you can configure Kueue if you want to restrict scheduling to the
 newly provisioned nodes (assuming the provisioning class supports it).
 
+### Hot swap support
+When the lowest level of Topology is set to node, TAS finds a fixed assignment
+of pods to nodes and injects a NodeSelector to make sure the pods get scheduled
+on the selected nodes. But this means that in case
+of any node failures or deletions, which occur during the runtime of a workload,
+the workload cannot run on any other nodes. In order to avoid costly re-scheduling
+of the entire TAS workload we introduce the node hot swap feature to Kueue
+(starting from version 0.12).
+
+{{% alert title="Note" color="primary" %}}
+To enable the feature, you have to set the [feature gate](https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/)
+`TASFailedNodeReplacement` to `true` and the lowest topological label has to be
+`kubernetes.io/hostname`.
+{{% /alert %}}
```

  Done.
