# PR #5632: [AFS] Support preemption based on historical LQ's usage

**Summary**: [AFS] Support preemption based on historical LQ's usage

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5632

**Last updated**: 2025-08-12T10:15:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `afs-preemption` → `main`
- **Created**: 2025-06-12T13:24:05Z
- **Updated**: 2025-08-12T10:15:49Z
- **Merged**: 2025-07-04T14:57:29Z
- **Closed**: 2025-07-04T14:57:29Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 12   **+295 / -92**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
- Adds `AdmissionScope` field to the interal ClusterQueue representation and CQ Snapshot
- Adds `LocalQueueUsage` field to Workload info to represent the historical LQ's usage in Snapshot
- Populates the `LocalQueueUsage` field if the AFS feature gate is enabled, and the CQ AdmissionMode is set to `UsageBasedAdmissionFairSharing`
- Order candidates for preemption based on the LQ's historical usage

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5827 

#### Special notes for your reviewer:
KEP update: https://github.com/kubernetes-sigs/kueue/pull/5817

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
AFS: preemption candidates are now ordered within ClusterQueue with respect to LQ's usage. 
The ordering of candidates coming from other ClusterQueues is unchanged.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-12T13:24:13Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 7659a659c640675e64a2009cb18e8a846b3cf2d1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6867e6229fc1110008f0c3cf |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-27T15:04:58Z

/assign @mimowo 
PTAL

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-27T15:08:50Z

/assign @mbobrovskyi 
Could give it a pass as well?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-27T15:49:30Z

> Please update the KEP to reflect the logic change, so that it is clear what is the scope of the changes.

In particular, I'm wondering about questions like:
1. there is no API change, so should it be a bugfix rather than a feature? 
2. how the candidates for preemption are ordered within cohorts for reclamation? (a) Does it mean the usage of LQ targeting different CQs is compared with each other? (b) is DRF ignored for regular fair sharing?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-30T10:23:33Z

> how the candidates for preemption are ordered within cohorts for reclamation? (a) Does it mean the usage of LQ targeting different CQs is compared with each other? (b) is DRF ignored for regular fair sharing?

The usage is only taken into account when ordering Workloads targeting the same CQ, so:

(a) No, the usage of LQs targeting different CQs is not compared
(b) DRF would still be taken into account as usual

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-30T10:51:12Z

> The usage is only taken into account when ordering Workloads targeting the same CQ, so:

Awesome, this makes sense, but is not entirely obvious, so I would like to add such a note to the KEP.

It would also be great to add a test  showing that, maybe a unit test in scheduler_test.go.

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-07-01T09:51:32Z

Changes have been describe in KEP here: https://github.com/kubernetes-sigs/kueue/pull/5817

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-07-01T15:38:04Z

/assign @pajakd

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-04T09:19:21Z

It is a couple of time I looked at the PR and there were some flakes, could you please loop the test 100 times locally on or CI to make sure there are not flakes before we merge?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-04T09:21:55Z

/release-note-edit
```release-note
AdmissionFairSharing: preemption candidates are now ordered within ClusterQueue with respect to LQ's usage. 
The ordering of candidates coming from other ClusterQueues is unchanged.
```

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-07-04T10:42:17Z

cc @mwysokin

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-04T14:07:04Z

/lgtm
/approve
As I synced with @PBundyra there is some missing test coverage, but he will not be able to finish it before vacation.  We have ticketed adding the remaining tests https://github.com/kubernetes-sigs/kueue/issues/5876, and Patryk is tentatively the assignee. 

I think this is a reasonable decision given this is still an alpha feature, and the new code is well isolated from the pre-existing code by the feature gate.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-04T14:07:10Z

LGTM label has been added.  <details>Git tree hash: 9a51d67119a415dfeaf5407b8049fa6c40d293d9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-04T14:07:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5632#issuecomment-3036407488" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5632#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-04T14:34:18Z

/lgtm
Just a unit test adjustment for the feature gate.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-04T14:34:24Z

LGTM label has been added.  <details>Git tree hash: 5c2fd07e3a86a7b06b37217582d14a5533c522bc</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-28T15:04:15Z

/release-note-edit

```release-note
AFS: preemption candidates are now ordered within ClusterQueue with respect to LQ's usage. 
The ordering of candidates coming from other ClusterQueues is unchanged.
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-27T15:09:37Z

- **pkg/cache/snapshot.go:162** by [@mimowo](https://github.com/mimowo) — 2025-06-27T15:09:36Z
```diff
@@ -157,26 +159,41 @@ func (c *Cache) Snapshot(ctx context.Context) (*Snapshot, error) {
 
 // snapshotClusterQueue creates a copy of ClusterQueue that includes
 // references to immutable objects and deep copies of changing ones.
-func snapshotClusterQueue(c *clusterQueue) *ClusterQueueSnapshot {
+func (c *Cache) snapshotClusterQueue(ctx context.Context, cq *clusterQueue) *ClusterQueueSnapshot {
```

  If you rename the c as cq, please submit a preparatory PR to make the diff easier to dijest.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-27T15:09:59Z

- **pkg/scheduler/preemption/classical/candidate_generator.go:88** by [@mimowo](https://github.com/mimowo) — 2025-06-27T15:09:59Z
```diff
@@ -85,7 +87,6 @@ func NewCandidateIterator(hierarchicalReclaimCtx *HierarchicalPreemptionCtx, frs
 	sort.Slice(hierarchyCandidates, func(i, j int) bool {
 		return ordering(hierarchyCandidates[i].wl, hierarchyCandidates[j].wl, hierarchicalReclaimCtx.Cq.Name, clock.Now())
 	})
-
```

  nit, revert formatting

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-27T15:12:49Z

- **pkg/cache/snapshot.go:162** by [@PBundyra](https://github.com/PBundyra) — 2025-06-27T15:12:49Z
```diff
@@ -157,26 +159,41 @@ func (c *Cache) Snapshot(ctx context.Context) (*Snapshot, error) {
 
 // snapshotClusterQueue creates a copy of ClusterQueue that includes
 // references to immutable objects and deep copies of changing ones.
-func snapshotClusterQueue(c *clusterQueue) *ClusterQueueSnapshot {
+func (c *Cache) snapshotClusterQueue(ctx context.Context, cq *clusterQueue) *ClusterQueueSnapshot {
```

  I am changing the whole signature of the function and not only the naming so maybe we could keep it within this PR?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-27T15:13:36Z

- **site/static/examples/tas/sample-job-preferred.yaml:8** by [@mimowo](https://github.com/mimowo) — 2025-06-27T15:12:15Z
```diff
@@ -5,8 +5,8 @@ metadata:
   labels:
     kueue.x-k8s.io/queue-name: tas-user-queue
 spec:
-  parallelism: 40
-  completions: 40
+  parallelism: 4
```

  why this example is modified? Can we update it in a follow PR for documentation update?

- **site/static/examples/jobs/sample-job.yaml:7** by [@mimowo](https://github.com/mimowo) — 2025-06-27T15:12:27Z
```diff
@@ -4,7 +4,7 @@ metadata:
   generateName: sample-job-
   namespace: default
   labels:
-    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/queue-name: lq-a
```

  Why changing this?

- **pkg/workload/workload.go:301** by [@mimowo](https://github.com/mimowo) — 2025-06-27T15:13:32Z
```diff
@@ -294,7 +298,7 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
-func (i *Info) LocalQueueUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+func (i *Info) GetLocalQueueUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
```

  Using Get prefix just to avoid conflict with LocalQueueUsage is a code smell. Let's try to unexport LocalQueueUsage. Or maybe the name `GetLocalQueueUsage` is wrong

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-27T15:14:59Z

- **pkg/cache/snapshot.go:191** by [@mimowo](https://github.com/mimowo) — 2025-06-27T15:14:59Z
```diff
@@ -157,26 +159,41 @@ func (c *Cache) Snapshot(ctx context.Context) (*Snapshot, error) {
 
 // snapshotClusterQueue creates a copy of ClusterQueue that includes
 // references to immutable objects and deep copies of changing ones.
-func snapshotClusterQueue(c *clusterQueue) *ClusterQueueSnapshot {
+func (c *Cache) snapshotClusterQueue(ctx context.Context, cq *clusterQueue) *ClusterQueueSnapshot {
+	log := ctrl.LoggerFrom(ctx)
 	cc := &ClusterQueueSnapshot{
-		Name:                          c.Name,
-		ResourceGroups:                make([]ResourceGroup, len(c.ResourceGroups)),
-		FlavorFungibility:             c.FlavorFungibility,
-		FairWeight:                    c.FairWeight,
-		AllocatableResourceGeneration: c.AllocatableResourceGeneration,
-		Workloads:                     maps.Clone(c.Workloads),
-		Preemption:                    c.Preemption,
-		NamespaceSelector:             c.NamespaceSelector,
-		Status:                        c.Status,
-		AdmissionChecks:               utilmaps.DeepCopySets(c.AdmissionChecks),
-		ResourceNode:                  c.resourceNode.Clone(),
+		Name:                          cq.Name,
+		ResourceGroups:                make([]ResourceGroup, len(cq.ResourceGroups)),
+		FlavorFungibility:             cq.FlavorFungibility,
+		FairWeight:                    cq.FairWeight,
+		AllocatableResourceGeneration: cq.AllocatableResourceGeneration,
+		Workloads:                     maps.Clone(cq.Workloads),
+		Preemption:                    cq.Preemption,
+		NamespaceSelector:             cq.NamespaceSelector,
+		Status:                        cq.Status,
+		AdmissionChecks:               utilmaps.DeepCopySets(cq.AdmissionChecks),
+		ResourceNode:                  cq.resourceNode.Clone(),
 		TASFlavors:                    make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot),
-		tasOnly:                       c.isTASOnly(),
-		flavorsForProvReqACs:          c.flavorsWithProvReqAdmissionCheck(),
+		tasOnly:                       cq.isTASOnly(),
+		flavorsForProvReqACs:          cq.flavorsWithProvReqAdmissionCheck(),
 	}
-	for i, rg := range c.ResourceGroups {
+	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := queue.AfsResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if afsEnabled {
+		for _, wl := range cc.Workloads {
+			usage, err := wl.GetLocalQueueUsage(ctx, c.client, resourceWeights)
+			if err != nil {
+				log.V(2).Error(err, "Error determining LocalQueue usage")
```

  Silencing errors is typically a code smell, can we propage the error up?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-27T15:16:02Z

I haven't investigated the logic yet, just a couple of quick comments as it looks early stages. Please update the KEP to reflect the logic change, so that it is clear what is the scope of the changes.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-27T15:16:36Z

- **pkg/cache/snapshot.go:162** by [@mimowo](https://github.com/mimowo) — 2025-06-27T15:16:35Z
```diff
@@ -157,26 +159,41 @@ func (c *Cache) Snapshot(ctx context.Context) (*Snapshot, error) {
 
 // snapshotClusterQueue creates a copy of ClusterQueue that includes
 // references to immutable objects and deep copies of changing ones.
-func snapshotClusterQueue(c *clusterQueue) *ClusterQueueSnapshot {
+func (c *Cache) snapshotClusterQueue(ctx context.Context, cq *clusterQueue) *ClusterQueueSnapshot {
```

  I propose not, because it blows the size of the diff unnecesarily

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-27T15:28:17Z

- **site/static/examples/tas/sample-job-preferred.yaml:8** by [@PBundyra](https://github.com/PBundyra) — 2025-06-27T15:28:17Z
```diff
@@ -5,8 +5,8 @@ metadata:
   labels:
     kueue.x-k8s.io/queue-name: tas-user-queue
 spec:
-  parallelism: 40
-  completions: 40
+  parallelism: 4
```

  Removed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-27T15:28:24Z

- **site/static/examples/jobs/sample-job.yaml:7** by [@PBundyra](https://github.com/PBundyra) — 2025-06-27T15:28:24Z
```diff
@@ -4,7 +4,7 @@ metadata:
   generateName: sample-job-
   namespace: default
   labels:
-    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/queue-name: lq-a
```

  Removed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-01T10:03:56Z

- **pkg/cache/snapshot.go:191** by [@PBundyra](https://github.com/PBundyra) — 2025-07-01T10:03:56Z
```diff
@@ -157,26 +159,41 @@ func (c *Cache) Snapshot(ctx context.Context) (*Snapshot, error) {
 
 // snapshotClusterQueue creates a copy of ClusterQueue that includes
 // references to immutable objects and deep copies of changing ones.
-func snapshotClusterQueue(c *clusterQueue) *ClusterQueueSnapshot {
+func (c *Cache) snapshotClusterQueue(ctx context.Context, cq *clusterQueue) *ClusterQueueSnapshot {
+	log := ctrl.LoggerFrom(ctx)
 	cc := &ClusterQueueSnapshot{
-		Name:                          c.Name,
-		ResourceGroups:                make([]ResourceGroup, len(c.ResourceGroups)),
-		FlavorFungibility:             c.FlavorFungibility,
-		FairWeight:                    c.FairWeight,
-		AllocatableResourceGeneration: c.AllocatableResourceGeneration,
-		Workloads:                     maps.Clone(c.Workloads),
-		Preemption:                    c.Preemption,
-		NamespaceSelector:             c.NamespaceSelector,
-		Status:                        c.Status,
-		AdmissionChecks:               utilmaps.DeepCopySets(c.AdmissionChecks),
-		ResourceNode:                  c.resourceNode.Clone(),
+		Name:                          cq.Name,
+		ResourceGroups:                make([]ResourceGroup, len(cq.ResourceGroups)),
+		FlavorFungibility:             cq.FlavorFungibility,
+		FairWeight:                    cq.FairWeight,
+		AllocatableResourceGeneration: cq.AllocatableResourceGeneration,
+		Workloads:                     maps.Clone(cq.Workloads),
+		Preemption:                    cq.Preemption,
+		NamespaceSelector:             cq.NamespaceSelector,
+		Status:                        cq.Status,
+		AdmissionChecks:               utilmaps.DeepCopySets(cq.AdmissionChecks),
+		ResourceNode:                  cq.resourceNode.Clone(),
 		TASFlavors:                    make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot),
-		tasOnly:                       c.isTASOnly(),
-		flavorsForProvReqACs:          c.flavorsWithProvReqAdmissionCheck(),
+		tasOnly:                       cq.isTASOnly(),
+		flavorsForProvReqACs:          cq.flavorsWithProvReqAdmissionCheck(),
 	}
-	for i, rg := range c.ResourceGroups {
+	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := queue.AfsResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if afsEnabled {
+		for _, wl := range cc.Workloads {
+			usage, err := wl.GetLocalQueueUsage(ctx, c.client, resourceWeights)
+			if err != nil {
+				log.V(2).Error(err, "Error determining LocalQueue usage")
```

  This function is called by a scheduler where so far we only log errors and continue with another Workload. Is there anything you'd suggest doing instead? Also the `wl.LocalQueueUsage` is a pointer and later on we check if this is not nil

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-01T15:36:57Z

- **pkg/scheduler/preemption/classical/candidate_generator.go:88** by [@PBundyra](https://github.com/PBundyra) — 2025-07-01T15:36:57Z
```diff
@@ -85,7 +87,6 @@ func NewCandidateIterator(hierarchicalReclaimCtx *HierarchicalPreemptionCtx, frs
 	sort.Slice(hierarchyCandidates, func(i, j int) bool {
 		return ordering(hierarchyCandidates[i].wl, hierarchyCandidates[j].wl, hierarchicalReclaimCtx.Cq.Name, clock.Now())
 	})
-
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-01T15:37:08Z

- **pkg/workload/workload.go:301** by [@PBundyra](https://github.com/PBundyra) — 2025-07-01T15:37:08Z
```diff
@@ -294,7 +298,7 @@ func dropExcludedResources(input corev1.ResourceList, excludedPrefixes []string)
 	return res
 }
 
-func (i *Info) LocalQueueUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
+func (i *Info) GetLocalQueueUsage(ctx context.Context, c client.Client, resWeights map[corev1.ResourceName]float64) (float64, error) {
```

  Addressed

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-02T07:38:09Z

- **pkg/scheduler/preemption/preemption.go:548** by [@pajakd](https://github.com/pajakd) — 2025-07-02T07:38:09Z
```diff
@@ -530,7 +539,11 @@ func CandidatesOrdering(a, b *workload.Info, cq kueue.ClusterQueueReference, now
 	if aInCQ != bInCQ {
 		return !aInCQ
 	}
-
+	if resourceUsagePreemptionEnabled(a, b) {
+		if a.LocalQueueFSUsage != b.LocalQueueFSUsage {
+			return *a.LocalQueueFSUsage > *b.LocalQueueFSUsage
+		}
+	}
```

  I have some observations looking at the CandidatesOrdering function. It is composed of independent conditions -- I suggest (in a followup PR) to make each condition a separate function and CandidatesOrdering to accept a sequence of conditions as argument something like this 
  ```
  type orderFunc func(a, b *workload.Info, cq kueue.ClusterQueueReference, now time.Time) *bool
  
  var afsCandidateOrdering = []orderFunc{
  	byEvicted,
  	byInClusterQueue,
  	byResourceUsage,
  	byPriority,
  	byTimestamp,
  }
  
  var classicalCandidateOrdering = []orderFunc{
  	byEvicted,
  	byInClusterQueue,
  	byPriority,
  	byTimestamp,
  }
  
  func CandidatesOrdering(a, b *workload.Info, cq kueue.ClusterQueueReference, now time.Time, conditions []orderFunc) bool {
  	for _, fn := range conditions {
  		if res := fn(a, b, cq, now); res != nil {
  			return *res
  		}
  	}
  	// Arbitrary comparison for deterministic sorting.
  	return a.Obj.UID < b.Obj.UID
  }
  
  ```
  Or sth similar -- to make it future-proof. WDYT?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-02T09:20:40Z

- **pkg/scheduler/preemption/preemption.go:546** by [@pajakd](https://github.com/pajakd) — 2025-07-02T09:20:40Z
```diff
@@ -530,7 +539,11 @@ func CandidatesOrdering(a, b *workload.Info, cq kueue.ClusterQueueReference, now
 	if aInCQ != bInCQ {
 		return !aInCQ
 	}
-
+	if resourceUsagePreemptionEnabled(a, b) {
+		if a.LocalQueueFSUsage != b.LocalQueueFSUsage {
+			return *a.LocalQueueFSUsage > *b.LocalQueueFSUsage
```

  Just to understand. If LQ1 is having workloads using only GPUs and LQ2 is having workloads using only CPUs, then this allows us to compare the usages (even though they are of different resource types)? The trick is that we have resource weights that determine which (and by how much) each resource unit is more important, right? So, the user has to correctly configure the weights to correctly reflect the importance of each resource?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-02T09:45:01Z

- **test/integration/singlecluster/scheduler/preemption_test.go:268** by [@pajakd](https://github.com/pajakd) — 2025-07-02T09:45:01Z
```diff
@@ -209,6 +210,181 @@ var _ = ginkgo.Describe("Preemption", func() {
 		})
 	})
 
+	ginkgo.When("In a single ClusterQueue with AdmissionScope set to UsageBasedFairSharing", func() {
+		var (
+			cq  *kueue.ClusterQueue
+			lq1 *kueue.LocalQueue
+			lq2 *kueue.LocalQueue
+			lq3 *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(features.SetEnable(features.AdmissionFairSharing, true)).To(gomega.Succeed())
+			cq = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas("alpha").Resource(corev1.ResourceCPU, "4").Obj()).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lq1 = testing.MakeLocalQueue("lq1", ns.Name).
+				ClusterQueue(cq.Name).
+				Obj()
+			util.MustCreate(ctx, k8sClient, lq1)
+
+			lq2 = testing.MakeLocalQueue("lq2", ns.Name).
+				ClusterQueue(cq.Name).
+				Obj()
+			util.MustCreate(ctx, k8sClient, lq2)
+
+			lq3 = testing.MakeLocalQueue("lq3", ns.Name).
+				ClusterQueue(cq.Name).
+				Obj()
+			util.MustCreate(ctx, k8sClient, lq3)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(features.SetEnable(features.AdmissionFairSharing, false)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		})
+
+		ginkgo.It("Should preempt Workload with lower LQ's usage over priorities/timestamp", func() {
+			ginkgo.By("Creating an initial mid priority Workload submitted to LQ with higher usage")
+			midWl1 := testing.MakeWorkload("mid-wl-1", ns.Name).
+				Queue(kueue.LocalQueueName(lq1.Name)).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "2").
+				Obj()
+			util.MustCreate(ctx, k8sClient, midWl1)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, midWl1)
+
+			ginkgo.By("Creating an initial mid priority Workload submitted to LQ with lower usage")
+			midWl2 := testing.MakeWorkload("mid-wl-2", ns.Name).
+				Queue(kueue.LocalQueueName(lq2.Name)).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "2").
```

  Can we initially have `midWl2` usage higher than `midWl1` and then bump the LQ1 usage even higher? If their usage is the same it might happen that we would preempt `midWl1` even without the increase?
  (edit: ah I guess you rely on timestamps -- we will preempt `midWl2` because it was admitted more recently. Ok its fine)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T09:55:42Z

- **pkg/cache/snapshot.go:191** by [@mimowo](https://github.com/mimowo) — 2025-07-02T09:55:41Z
```diff
@@ -157,26 +159,41 @@ func (c *Cache) Snapshot(ctx context.Context) (*Snapshot, error) {
 
 // snapshotClusterQueue creates a copy of ClusterQueue that includes
 // references to immutable objects and deep copies of changing ones.
-func snapshotClusterQueue(c *clusterQueue) *ClusterQueueSnapshot {
+func (c *Cache) snapshotClusterQueue(ctx context.Context, cq *clusterQueue) *ClusterQueueSnapshot {
+	log := ctrl.LoggerFrom(ctx)
 	cc := &ClusterQueueSnapshot{
-		Name:                          c.Name,
-		ResourceGroups:                make([]ResourceGroup, len(c.ResourceGroups)),
-		FlavorFungibility:             c.FlavorFungibility,
-		FairWeight:                    c.FairWeight,
-		AllocatableResourceGeneration: c.AllocatableResourceGeneration,
-		Workloads:                     maps.Clone(c.Workloads),
-		Preemption:                    c.Preemption,
-		NamespaceSelector:             c.NamespaceSelector,
-		Status:                        c.Status,
-		AdmissionChecks:               utilmaps.DeepCopySets(c.AdmissionChecks),
-		ResourceNode:                  c.resourceNode.Clone(),
+		Name:                          cq.Name,
+		ResourceGroups:                make([]ResourceGroup, len(cq.ResourceGroups)),
+		FlavorFungibility:             cq.FlavorFungibility,
+		FairWeight:                    cq.FairWeight,
+		AllocatableResourceGeneration: cq.AllocatableResourceGeneration,
+		Workloads:                     maps.Clone(cq.Workloads),
+		Preemption:                    cq.Preemption,
+		NamespaceSelector:             cq.NamespaceSelector,
+		Status:                        cq.Status,
+		AdmissionChecks:               utilmaps.DeepCopySets(cq.AdmissionChecks),
+		ResourceNode:                  cq.resourceNode.Clone(),
 		TASFlavors:                    make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot),
-		tasOnly:                       c.isTASOnly(),
-		flavorsForProvReqACs:          c.flavorsWithProvReqAdmissionCheck(),
+		tasOnly:                       cq.isTASOnly(),
+		flavorsForProvReqACs:          cq.flavorsWithProvReqAdmissionCheck(),
 	}
-	for i, rg := range c.ResourceGroups {
+	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := queue.AfsResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if afsEnabled {
+		for _, wl := range cc.Workloads {
+			usage, err := wl.GetLocalQueueUsage(ctx, c.client, resourceWeights)
+			if err != nil {
+				log.V(2).Error(err, "Error determining LocalQueue usage")
```

  I would suggest to handle the error at top level in scheduler. This is the decision we went for in TAS: https://github.com/kubernetes-sigs/kueue/blob/fa3a7682301ea1d72006e00701b84df4d9b08ded/pkg/scheduler/scheduler.go#L191-L195

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:23:41Z

- **pkg/cache/snapshot.go:191** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:23:40Z
```diff
@@ -157,26 +159,41 @@ func (c *Cache) Snapshot(ctx context.Context) (*Snapshot, error) {
 
 // snapshotClusterQueue creates a copy of ClusterQueue that includes
 // references to immutable objects and deep copies of changing ones.
-func snapshotClusterQueue(c *clusterQueue) *ClusterQueueSnapshot {
+func (c *Cache) snapshotClusterQueue(ctx context.Context, cq *clusterQueue) *ClusterQueueSnapshot {
+	log := ctrl.LoggerFrom(ctx)
 	cc := &ClusterQueueSnapshot{
-		Name:                          c.Name,
-		ResourceGroups:                make([]ResourceGroup, len(c.ResourceGroups)),
-		FlavorFungibility:             c.FlavorFungibility,
-		FairWeight:                    c.FairWeight,
-		AllocatableResourceGeneration: c.AllocatableResourceGeneration,
-		Workloads:                     maps.Clone(c.Workloads),
-		Preemption:                    c.Preemption,
-		NamespaceSelector:             c.NamespaceSelector,
-		Status:                        c.Status,
-		AdmissionChecks:               utilmaps.DeepCopySets(c.AdmissionChecks),
-		ResourceNode:                  c.resourceNode.Clone(),
+		Name:                          cq.Name,
+		ResourceGroups:                make([]ResourceGroup, len(cq.ResourceGroups)),
+		FlavorFungibility:             cq.FlavorFungibility,
+		FairWeight:                    cq.FairWeight,
+		AllocatableResourceGeneration: cq.AllocatableResourceGeneration,
+		Workloads:                     maps.Clone(cq.Workloads),
+		Preemption:                    cq.Preemption,
+		NamespaceSelector:             cq.NamespaceSelector,
+		Status:                        cq.Status,
+		AdmissionChecks:               utilmaps.DeepCopySets(cq.AdmissionChecks),
+		ResourceNode:                  cq.resourceNode.Clone(),
 		TASFlavors:                    make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot),
-		tasOnly:                       c.isTASOnly(),
-		flavorsForProvReqACs:          c.flavorsWithProvReqAdmissionCheck(),
+		tasOnly:                       cq.isTASOnly(),
+		flavorsForProvReqACs:          cq.flavorsWithProvReqAdmissionCheck(),
 	}
-	for i, rg := range c.ResourceGroups {
+	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := queue.AfsResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if afsEnabled {
+		for _, wl := range cc.Workloads {
+			usage, err := wl.GetLocalQueueUsage(ctx, c.client, resourceWeights)
+			if err != nil {
+				log.V(2).Error(err, "Error determining LocalQueue usage")
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:24:55Z

- **pkg/scheduler/preemption/preemption.go:546** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:24:55Z
```diff
@@ -530,7 +539,11 @@ func CandidatesOrdering(a, b *workload.Info, cq kueue.ClusterQueueReference, now
 	if aInCQ != bInCQ {
 		return !aInCQ
 	}
-
+	if resourceUsagePreemptionEnabled(a, b) {
+		if a.LocalQueueFSUsage != b.LocalQueueFSUsage {
+			return *a.LocalQueueFSUsage > *b.LocalQueueFSUsage
```

  Exactly

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:25:50Z

- **pkg/scheduler/preemption/preemption.go:546** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:25:50Z
```diff
@@ -530,7 +539,11 @@ func CandidatesOrdering(a, b *workload.Info, cq kueue.ClusterQueueReference, now
 	if aInCQ != bInCQ {
 		return !aInCQ
 	}
-
+	if resourceUsagePreemptionEnabled(a, b) {
+		if a.LocalQueueFSUsage != b.LocalQueueFSUsage {
+			return *a.LocalQueueFSUsage > *b.LocalQueueFSUsage
```

  On the practical side, usually GPUs are much more scare resource than CPUs/memory so admins would probably configure it the way GPUs matter the most

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:31:00Z

- **pkg/scheduler/preemption/preemption.go:548** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:31:00Z
```diff
@@ -530,7 +539,11 @@ func CandidatesOrdering(a, b *workload.Info, cq kueue.ClusterQueueReference, now
 	if aInCQ != bInCQ {
 		return !aInCQ
 	}
-
+	if resourceUsagePreemptionEnabled(a, b) {
+		if a.LocalQueueFSUsage != b.LocalQueueFSUsage {
+			return *a.LocalQueueFSUsage > *b.LocalQueueFSUsage
+		}
+	}
```

  Could be, but I'd like how inlined and transparent it is now, so when looking at the conditions we don't have to search for the comparator function to understand what it does. The only difference now between  `afsCandidateOrdering` and `classicalCandidateOrdering` would be `byResourceUsage` and we would still need an additional logic to determine which one of these two to use. I think if we added more criteria and the combination of criteria would differ a lot this would be make more sense

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T10:32:44Z

- **pkg/cache/snapshot.go:162** by [@mimowo](https://github.com/mimowo) — 2025-07-02T10:32:44Z
```diff
@@ -157,26 +159,41 @@ func (c *Cache) Snapshot(ctx context.Context) (*Snapshot, error) {
 
 // snapshotClusterQueue creates a copy of ClusterQueue that includes
 // references to immutable objects and deep copies of changing ones.
-func snapshotClusterQueue(c *clusterQueue) *ClusterQueueSnapshot {
+func (c *Cache) snapshotClusterQueue(ctx context.Context, cq *clusterQueue) *ClusterQueueSnapshot {
```

  IIUC this is already addressed, please rebase and feel free to resolve

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T11:13:42Z

- **pkg/cache/snapshot.go:162** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T11:13:41Z
```diff
@@ -157,26 +159,41 @@ func (c *Cache) Snapshot(ctx context.Context) (*Snapshot, error) {
 
 // snapshotClusterQueue creates a copy of ClusterQueue that includes
 // references to immutable objects and deep copies of changing ones.
-func snapshotClusterQueue(c *clusterQueue) *ClusterQueueSnapshot {
+func (c *Cache) snapshotClusterQueue(ctx context.Context, cq *clusterQueue) *ClusterQueueSnapshot {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T11:14:02Z

- **test/integration/singlecluster/scheduler/preemption_test.go:268** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T11:14:02Z
```diff
@@ -209,6 +210,181 @@ var _ = ginkgo.Describe("Preemption", func() {
 		})
 	})
 
+	ginkgo.When("In a single ClusterQueue with AdmissionScope set to UsageBasedFairSharing", func() {
+		var (
+			cq  *kueue.ClusterQueue
+			lq1 *kueue.LocalQueue
+			lq2 *kueue.LocalQueue
+			lq3 *kueue.LocalQueue
+		)
+
+		ginkgo.BeforeEach(func() {
+			gomega.Expect(features.SetEnable(features.AdmissionFairSharing, true)).To(gomega.Succeed())
+			cq = testing.MakeClusterQueue("cq").
+				ResourceGroup(*testing.MakeFlavorQuotas("alpha").Resource(corev1.ResourceCPU, "4").Obj()).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Preemption(kueue.ClusterQueuePreemption{
+					WithinClusterQueue: kueue.PreemptionPolicyLowerPriority,
+				}).
+				Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lq1 = testing.MakeLocalQueue("lq1", ns.Name).
+				ClusterQueue(cq.Name).
+				Obj()
+			util.MustCreate(ctx, k8sClient, lq1)
+
+			lq2 = testing.MakeLocalQueue("lq2", ns.Name).
+				ClusterQueue(cq.Name).
+				Obj()
+			util.MustCreate(ctx, k8sClient, lq2)
+
+			lq3 = testing.MakeLocalQueue("lq3", ns.Name).
+				ClusterQueue(cq.Name).
+				Obj()
+			util.MustCreate(ctx, k8sClient, lq3)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(features.SetEnable(features.AdmissionFairSharing, false)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+		})
+
+		ginkgo.It("Should preempt Workload with lower LQ's usage over priorities/timestamp", func() {
+			ginkgo.By("Creating an initial mid priority Workload submitted to LQ with higher usage")
+			midWl1 := testing.MakeWorkload("mid-wl-1", ns.Name).
+				Queue(kueue.LocalQueueName(lq1.Name)).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "2").
+				Obj()
+			util.MustCreate(ctx, k8sClient, midWl1)
+			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, midWl1)
+
+			ginkgo.By("Creating an initial mid priority Workload submitted to LQ with lower usage")
+			midWl2 := testing.MakeWorkload("mid-wl-2", ns.Name).
+				Queue(kueue.LocalQueueName(lq2.Name)).
+				Priority(midPriority).
+				Request(corev1.ResourceCPU, "2").
```

  Correct

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T11:23:24Z

- **pkg/cache/snapshot.go:33** by [@mimowo](https://github.com/mimowo) — 2025-07-02T11:23:24Z
```diff
@@ -25,10 +25,12 @@ import (
 	"github.com/go-logr/logr"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/hierarchy"
+	"sigs.k8s.io/kueue/pkg/queue"
```

  Don't add this dependency.
  
  Instead place AfsResourceWeights in some dedicated package like `pkg/util/afs`, similarly as we do for TAS. Then, you can name it without "Afs" in name.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T11:25:32Z

- **pkg/cache/snapshot.go:33** by [@mimowo](https://github.com/mimowo) — 2025-07-02T11:25:31Z
```diff
@@ -25,10 +25,12 @@ import (
 	"github.com/go-logr/logr"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/hierarchy"
+	"sigs.k8s.io/kueue/pkg/queue"
```

  There is really no conceptual dependency between the packages, and for the ease of development there used to be cases the dependency would go either way. So, I prefer to just keep them independent.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T11:23:38Z

- **pkg/util/admissionfairsharing/admission_fair_sharing.go:27** by [@mimowo](https://github.com/mimowo) — 2025-07-03T11:23:38Z
```diff
@@ -0,0 +1,34 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissionfairsharing
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+func AfsResourceWeights(cqAdmissionScope *kueue.AdmissionScope, afsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  ```suggestion
  func ResourceWeights(cqAdmissionScope *kueue.AdmissionScope, afsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
  ```
  no need to repeat as it is in the afs context already

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T11:25:24Z

- **pkg/cache/snapshot.go:189** by [@mimowo](https://github.com/mimowo) — 2025-07-03T11:25:24Z
```diff
@@ -177,7 +182,21 @@ func snapshotClusterQueue(cq *clusterQueue) *ClusterQueueSnapshot {
 	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
-	return cc
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := afs.AfsResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if afsEnabled {
```

  nit. use `if !afsEnabled { return cc, nil } ` to safe indentetion

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T11:26:01Z

- **pkg/cache/snapshot.go:197** by [@mimowo](https://github.com/mimowo) — 2025-07-03T11:26:01Z
```diff
@@ -177,7 +182,21 @@ func snapshotClusterQueue(cq *clusterQueue) *ClusterQueueSnapshot {
 	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
-	return cc
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := afs.AfsResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if afsEnabled {
+		for _, wl := range cc.Workloads {
+			usage, err := wl.CalcLocalQueueFSUsage(ctx, c.client, resourceWeights)
+			if err != nil {
+				return nil, fmt.Errorf("failed to calculate LocalQueue FS usage for LocalQueue %v", client.ObjectKey{Namespace: wl.Obj.Namespace, Name: string(wl.Obj.Spec.QueueName)})
+			} else {
+				wl.LocalQueueFSUsage = &usage
+			}
```

  The else if not needed, I think

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-03T11:30:52Z

- **pkg/cache/snapshot.go:33** by [@PBundyra](https://github.com/PBundyra) — 2025-07-03T11:30:52Z
```diff
@@ -25,10 +25,12 @@ import (
 	"github.com/go-logr/logr"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/klog/v2"
+	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/features"
 	"sigs.k8s.io/kueue/pkg/hierarchy"
+	"sigs.k8s.io/kueue/pkg/queue"
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-03T11:32:41Z

- **pkg/cache/snapshot.go:197** by [@PBundyra](https://github.com/PBundyra) — 2025-07-03T11:32:41Z
```diff
@@ -177,7 +182,21 @@ func snapshotClusterQueue(cq *clusterQueue) *ClusterQueueSnapshot {
 	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
-	return cc
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := afs.AfsResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if afsEnabled {
+		for _, wl := range cc.Workloads {
+			usage, err := wl.CalcLocalQueueFSUsage(ctx, c.client, resourceWeights)
+			if err != nil {
+				return nil, fmt.Errorf("failed to calculate LocalQueue FS usage for LocalQueue %v", client.ObjectKey{Namespace: wl.Obj.Namespace, Name: string(wl.Obj.Spec.QueueName)})
+			} else {
+				wl.LocalQueueFSUsage = &usage
+			}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-03T11:32:46Z

- **pkg/cache/snapshot.go:189** by [@PBundyra](https://github.com/PBundyra) — 2025-07-03T11:32:45Z
```diff
@@ -177,7 +182,21 @@ func snapshotClusterQueue(cq *clusterQueue) *ClusterQueueSnapshot {
 	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
-	return cc
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := afs.AfsResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if afsEnabled {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-03T11:32:54Z

- **pkg/util/admissionfairsharing/admission_fair_sharing.go:27** by [@PBundyra](https://github.com/PBundyra) — 2025-07-03T11:32:54Z
```diff
@@ -0,0 +1,34 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissionfairsharing
+
+import (
+	corev1 "k8s.io/api/core/v1"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta1"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+func AfsResourceWeights(cqAdmissionScope *kueue.AdmissionScope, afsConfig *config.AdmissionFairSharing) (bool, map[corev1.ResourceName]float64) {
```

  Agree, done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-04T09:15:39Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:444** by [@pajakd](https://github.com/pajakd) — 2025-07-04T09:15:38Z
```diff
@@ -428,13 +440,17 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.MustCreate(ctx, k8sClient, lqC)
 		})
 
+		ginkgo.AfterEach(func() {
+			gomega.Expect(features.SetEnable(features.AdmissionFairSharing, false)).To(gomega.Succeed())
```

  We should perhaps perform cleanup after each test (delete workloads etc). This could be a reason for the flakiness (because we add a second test and there might be some leftovers from the first one).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-04T12:55:40Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:444** by [@pajakd](https://github.com/pajakd) — 2025-07-04T12:55:40Z
```diff
@@ -428,13 +440,17 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.MustCreate(ctx, k8sClient, lqC)
 		})
 
+		ginkgo.AfterEach(func() {
+			gomega.Expect(features.SetEnable(features.AdmissionFairSharing, false)).To(gomega.Succeed())
```

  I think my above comment is not helpful... 
  
  But now I think I found the reason :) The two workloads created at the beginning of the test (wlHighA and the unnamed one) use the same amount of CPU resource -- do we assume that the usage will decay according to the half-life formula (I guess the update happens once every 5mins by default)? If not there is nothing that differentiates the workloads from the perspective of the fair sharing. Cannot we just make the second workload use less resource?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-04T13:40:37Z

- **test/integration/singlecluster/scheduler/fairsharing/fair_sharing_test.go:444** by [@PBundyra](https://github.com/PBundyra) — 2025-07-04T13:40:37Z
```diff
@@ -428,13 +440,17 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.MustCreate(ctx, k8sClient, lqC)
 		})
 
+		ginkgo.AfterEach(func() {
+			gomega.Expect(features.SetEnable(features.AdmissionFairSharing, false)).To(gomega.Succeed())
```

  Thanks for pointing this out, it indeed causes flakiness, after multiple iteration on this test I've missed it

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-04T13:49:08Z

- **pkg/scheduler/preemption/preemption.go:543** by [@mimowo](https://github.com/mimowo) — 2025-07-04T13:49:07Z
```diff
@@ -531,6 +540,11 @@ func CandidatesOrdering(a, b *workload.Info, cq kueue.ClusterQueueReference, now
 		return !aInCQ
 	}
 
+	if resourceUsagePreemptionEnabled(a, b) {
```

  @PBundyra please check the feature gate as an extra safety measure

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-04T13:49:55Z

- **pkg/cache/snapshot.go:199** by [@mimowo](https://github.com/mimowo) — 2025-07-04T13:49:55Z
```diff
@@ -177,7 +182,21 @@ func snapshotClusterQueue(cq *clusterQueue) *ClusterQueueSnapshot {
 	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
-	return cc
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := afs.ResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if !afsEnabled {
+		return cc, nil
+	}
+	for _, wl := range cc.Workloads {
+		usage, err := wl.CalcLocalQueueFSUsage(ctx, c.client, resourceWeights)
+		if err != nil {
+			return nil, fmt.Errorf("failed to calculate LocalQueue FS usage for LocalQueue %v", client.ObjectKey{Namespace: wl.Obj.Namespace, Name: string(wl.Obj.Spec.QueueName)})
+		}
+		wl.LocalQueueFSUsage = &usage
+	}
+	return cc, nil
```

  Wrap the new code with the feature gate as the extra safety measure

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-04T13:52:12Z

- **pkg/scheduler/preemption/preemption.go:543** by [@PBundyra](https://github.com/PBundyra) — 2025-07-04T13:52:12Z
```diff
@@ -531,6 +540,11 @@ func CandidatesOrdering(a, b *workload.Info, cq kueue.ClusterQueueReference, now
 		return !aInCQ
 	}
 
+	if resourceUsagePreemptionEnabled(a, b) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-04T14:01:00Z

- **pkg/cache/snapshot.go:199** by [@PBundyra](https://github.com/PBundyra) — 2025-07-04T14:00:59Z
```diff
@@ -177,7 +182,21 @@ func snapshotClusterQueue(cq *clusterQueue) *ClusterQueueSnapshot {
 	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
-	return cc
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := afs.ResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if !afsEnabled {
+		return cc, nil
+	}
+	for _, wl := range cc.Workloads {
+		usage, err := wl.CalcLocalQueueFSUsage(ctx, c.client, resourceWeights)
+		if err != nil {
+			return nil, fmt.Errorf("failed to calculate LocalQueue FS usage for LocalQueue %v", client.ObjectKey{Namespace: wl.Obj.Namespace, Name: string(wl.Obj.Spec.QueueName)})
+		}
+		wl.LocalQueueFSUsage = &usage
+	}
+	return cc, nil
```

  The check on feature gate is part of `afs.ResourceWeights` function

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-04T14:02:04Z

- **pkg/cache/snapshot.go:199** by [@mimowo](https://github.com/mimowo) — 2025-07-04T14:02:03Z
```diff
@@ -177,7 +182,21 @@ func snapshotClusterQueue(cq *clusterQueue) *ClusterQueueSnapshot {
 	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
-	return cc
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := afs.ResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if !afsEnabled {
+		return cc, nil
+	}
+	for _, wl := range cc.Workloads {
+		usage, err := wl.CalcLocalQueueFSUsage(ctx, c.client, resourceWeights)
+		if err != nil {
+			return nil, fmt.Errorf("failed to calculate LocalQueue FS usage for LocalQueue %v", client.ObjectKey{Namespace: wl.Obj.Namespace, Name: string(wl.Obj.Spec.QueueName)})
+		}
+		wl.LocalQueueFSUsage = &usage
+	}
+	return cc, nil
```

  yeah, but it is not self-explanatory given the function name, so easy to break, also  what about this code
  
  ```
  	if cq.AdmissionScope != nil {
  		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
  	}
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-04T14:03:59Z

- **pkg/cache/snapshot.go:199** by [@mimowo](https://github.com/mimowo) — 2025-07-04T14:03:59Z
```diff
@@ -177,7 +182,21 @@ func snapshotClusterQueue(cq *clusterQueue) *ClusterQueueSnapshot {
 	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
-	return cc
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := afs.ResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if !afsEnabled {
+		return cc, nil
+	}
+	for _, wl := range cc.Workloads {
+		usage, err := wl.CalcLocalQueueFSUsage(ctx, c.client, resourceWeights)
+		if err != nil {
+			return nil, fmt.Errorf("failed to calculate LocalQueue FS usage for LocalQueue %v", client.ObjectKey{Namespace: wl.Obj.Namespace, Name: string(wl.Obj.Spec.QueueName)})
+		}
+		wl.LocalQueueFSUsage = &usage
+	}
+	return cc, nil
```

  Going forward maybe it is not so clean code that afsEnabled is returned by ResourceWeights.  I think this precondition should be checked externally as suggested here, rather than in the bottom of the function. However, I know this is pre-existing to the PR, we can cleanup  later.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-04T14:04:04Z

- **pkg/cache/snapshot.go:199** by [@PBundyra](https://github.com/PBundyra) — 2025-07-04T14:04:04Z
```diff
@@ -177,7 +182,21 @@ func snapshotClusterQueue(cq *clusterQueue) *ClusterQueueSnapshot {
 	for i, rg := range cq.ResourceGroups {
 		cc.ResourceGroups[i] = rg.Clone()
 	}
-	return cc
+	if cq.AdmissionScope != nil {
+		cc.AdmissionScope = *cq.AdmissionScope.DeepCopy()
+	}
+	afsEnabled, resourceWeights := afs.ResourceWeights(&cc.AdmissionScope, c.admissionFairSharing)
+	if !afsEnabled {
+		return cc, nil
+	}
+	for _, wl := range cc.Workloads {
+		usage, err := wl.CalcLocalQueueFSUsage(ctx, c.client, resourceWeights)
+		if err != nil {
+			return nil, fmt.Errorf("failed to calculate LocalQueue FS usage for LocalQueue %v", client.ObjectKey{Namespace: wl.Obj.Namespace, Name: string(wl.Obj.Spec.QueueName)})
+		}
+		wl.LocalQueueFSUsage = &usage
+	}
+	return cc, nil
```

  Added
