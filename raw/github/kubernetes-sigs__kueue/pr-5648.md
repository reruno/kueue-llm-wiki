# PR #5648: Update KEP-77 with additional details and focus on batchv1/Job

**Summary**: Update KEP-77 with additional details and focus on batchv1/Job

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5648

**Last updated**: 2025-07-23T15:09:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ichekrygin](https://github.com/ichekrygin)
- **Branch**: `kep-77-update` → `main`
- **Created**: 2025-06-14T05:27:42Z
- **Updated**: 2025-07-23T15:09:24Z
- **Merged**: 2025-07-22T17:08:27Z
- **Closed**: 2025-07-22T17:08:28Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@andrewsykim](https://github.com/andrewsykim), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+382 / -109**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
This PR updates the already accepted KEP-77 by fleshing out additional design details and aligning it with recent changes and constructs in Kueue. These updates provide a clearer path for both contributors and reviewers and reflect the current direction of the implementation.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5527

#### Special notes for your reviewer:
This PR enhances KEP-77 by expanding on the design to support batch/v1.Job horizontal scaling and aligning terminology and workflows with current Kueue capabilities.

#### Does this PR introduce a user-facing change?

<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-14T05:27:51Z

Hi @ichekrygin. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-14T05:28:49Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | af1e7e6cd72f3fc335187b482361ab289ca8d059 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/687e62465cc27d0007d276c4 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-27T18:28:59Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-27T19:17:39Z

I'm planning to review this early next week.

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-07T22:51:58Z

Thank you, @mimowo, for the detailed review and insights. I’ve addressed and/or responded to all the comments in your review. Could you please take another look when you have a moment?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-08T11:27:38Z

We only add release notes to PRs which directly have user-facing consequences.
/release-note-edit
```release-note
NONE
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:02:53Z

I'm thinking about renaming the feature (and associated things like feature gate or KEP title) from DynamicallySizedJobs to ElasticJobs. It is shorter to pronounce and captures the very same idea. 

Wdyt @tenzen-y @ichekrygin ?

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:05:30Z

> I'm thinking about renaming the feature (and associated things like feature gate or KEP title) from DynamicallySizedJobs to ElasticJobs. It is shorter to pronounce and captures the very same idea.
> 
> Wdyt @tenzen-y @ichekrygin ?

`ElasticJobs` SGTM, as long as we use `WorkloadSlices` as the implementation detail.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-08T17:50:29Z

Second pass. When the remarks are addressed I'm happy to LGTM. cc @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-08T20:01:27Z

> Second pass. When the remarks are addressed I'm happy to LGTM. cc @tenzen-y

ACK

Thank you for driving this enhancement, @mimowo @ichekrygin 
After some discussion with @mimowo offline, I agreed to implement the WorkloadSlice mechanism for both scale up / down use case. But the agreement was a conditional one since when we design the WokrloadSlice mechanism, we wanted to evaludate the approach only by scale down case for evaluation. OTOH, this KEP tries to introduce the scale up case.

So, I agreed to handle this apporach as step for supporting Dynamically workloads in Kueue. After some evaluation, we might migrate to resize request approach (https://github.com/kubernetes-sigs/kueue/issues/5897) for more request based way. Anyway, this seems not have API schema change, and this will introduce only annotations and schedulingGates which means that will not bring us any breaking change even if we deprecate this approach.

Let me see this proposal if there are no edge cases that could cause problem.

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T21:19:50Z

> But the agreement was a conditional one since when we design the WokrloadSlice mechanism, we wanted to evaludate the approach only by scale down case for evaluation

I suspect you're referring to [PR #5510](https://github.com/kubernetes-sigs/kueue/pull/5510), which includes all three phases:

* Scale Down
* Scale Up
* Slice Aggregation

To summarize:

* **Scale Down** is very straightforward and doesn't involve any functional change beyond relaxing immutability on `workload.spec.podSets[].count`.
* **Scale Up** and **Slice Aggregation** should be implemented together as a single unit. This is where the majority (99%) of the functional changes outlined in the KEP reside.

I’m looking forward to your review and any questions or comments you might have.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-09T07:30:04Z

+1 on supporting Scale Up day one, as it is the main use-case actually. 

As for Scale Down, indeed it is "for free" for batch Jobs thanks to the ReclaimablePods feature. However, it will not be "for free" for other Job types which don't support reclaimable Pods (most of them). So, we may need the more generic approach of using WorkloadSlices also for scale Down when we start supporting other Job CRDs. However, I'm ok to explore this in the following iterations of the feature while in Alpha.

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T15:54:06Z

> When they enable the PartialAdmission, the scale-up result might be weird.

I’m not 100% certain, but my expectation is that scale-up should reflect in the **minimum required pod count** as the actual outcome of the operation. In other words, if we scale a hypothetical workload from `replicas=5` and `minReplicas=3`, the result should include an update to either just `minReplicas`, or to both `minReplicas` and `replicas`, depending on the semantics of the scale-up intent.

It would be interesting to review which workloads that define `minReplicas` currently support scaling operations, and how they handle scaling behavior outside the context of Kueue. That could give us a better sense of expectations and consistency across APIs.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T17:23:25Z

> > When they enable the PartialAdmission, the scale-up result might be weird.
> 
> I’m not 100% certain, but my expectation is that scale-up should reflect in the minimum required pod count as the actual outcome of the operation. In other words, if we scale a hypothetical workload from replicas=5 and minReplicas=3, the result should include an update to either just minReplicas, or to both minReplicas and replicas, depending on the semantics of the scale-up intent.

In your described situation, the Workload should have replica=5 and minReplicas=5 after scale-up horizontally by Kueue?

> It would be interesting to review which workloads that define minReplicas currently support scaling operations, and how they handle scaling behavior outside the context of Kueue. That could give us a better sense of expectations and consistency across APIs.

The Kueue supports PartialAdmission only for batch/v1 Job (https://kueue.sigs.k8s.io/docs/overview/#job-integrated-features). So, I mentioned about this one.

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T19:02:44Z

> The Kueue supports PartialAdmission only for batch/v1 Job

Great callout, @tenzen-y, thank you for raising this, you're absolutely right that this highlights a gap in the current KEP.

After reviewing Kueue’s current behavior and requirements around `PartialAdmission`, I’m leaning toward the conclusion that we **should not support combining `PartialAdmission` with `ElasticJobs`at the Job instance level**, and here’s why:

* `PartialAdmission` depends on the **static nature of the workload**, specifically, it assumes (reasonably, for the current model) that workloads are immutable in terms of `podSets[].count`. Based on that, it adjusts the Job’s spec to reflect the minimum allowed parallelism at scheduling time. This value is fixed and does not change, even if the ClusterQueue's capacity increases later,
* In practice, `PartialAdmission` takes over control of `job.spec.parallelism`, and Kueue enforces this immutability through the admission webhook. For example:

  ```
  admission webhook "vjob.kb.io" denied the request: spec.parallelism: Forbidden: cannot change when partial admission is enabled and the job is not suspended
  ```
* Allowing the `ElasticJobs` feature to be used in combination with `PartialAdmission` for a specific job instance would introduce behavioral changes to how `PartialAdmission` works, and that’s something we probably want to avoid, at least during the alpha phase, and possibly altogether,
* If we were to support this combination, it’s currently unclear what the expected outcome would be when a user attempts to scale a job that was already “capped” by `PartialAdmission`. Should the system allow scale-up? Should it reject the update? There’s no obvious or consistent answer right now,

**In summary**, I suggest we explicitly call out this limitation as a *feature* of the `ElasticJobs` design: a given Job instance should not have both `PartialAdmission` and `ElasticJob` enabled. To be clear, this is a per-job limitation, **not** a global feature-level conflict, we can absolutely have both features enabled in the system, just not at the same time on the same Job.

Does that sound reasonable to you? Would love your thoughts.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-14T14:46:42Z

> > The Kueue supports PartialAdmission only for batch/v1 Job
> 
> Great callout, @tenzen-y, thank you for raising this, you're absolutely right that this highlights a gap in the current KEP.
> 
> After reviewing Kueue’s current behavior and requirements around `PartialAdmission`, I’m leaning toward the conclusion that we **should not support combining `PartialAdmission` with `ElasticJobs`at the Job instance level**, and here’s why:
> 
> * `PartialAdmission` depends on the **static nature of the workload**, specifically, it assumes (reasonably, for the current model) that workloads are immutable in terms of `podSets[].count`. Based on that, it adjusts the Job’s spec to reflect the minimum allowed parallelism at scheduling time. This value is fixed and does not change, even if the ClusterQueue's capacity increases later,
> * In practice, `PartialAdmission` takes over control of `job.spec.parallelism`, and Kueue enforces this immutability through the admission webhook. For example:
>   ```
>   admission webhook "vjob.kb.io" denied the request: spec.parallelism: Forbidden: cannot change when partial admission is enabled and the job is not suspended
>   ```
> * Allowing the `ElasticJobs` feature to be used in combination with `PartialAdmission` for a specific job instance would introduce behavioral changes to how `PartialAdmission` works, and that’s something we probably want to avoid, at least during the alpha phase, and possibly altogether,
> * If we were to support this combination, it’s currently unclear what the expected outcome would be when a user attempts to scale a job that was already “capped” by `PartialAdmission`. Should the system allow scale-up? Should it reject the update? There’s no obvious or consistent answer right now,
> 
> **In summary**, I suggest we explicitly call out this limitation as a _feature_ of the `ElasticJobs` design: a given Job instance should not have both `PartialAdmission` and `ElasticJob` enabled. To be clear, this is a per-job limitation, **not** a global feature-level conflict, we can absolutely have both features enabled in the system, just not at the same time on the same Job.
> 
> Does that sound reasonable to you? Would love your thoughts.

@ichekrygin Thank you for summarizing that. I think that we should consider how to support the combination of ElasticJobViaWorkloadSlice and PartialAdmission in a natural way.
However, I'm ok with moving the extra consideration to Beta promotion.

So, could you mention that in the Beta graduation criteria? If you can summarize the above investigation in `Future Work` section, it would be more better.

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-14T17:43:49Z

> could you mention that in the Beta graduation criteria?

done.

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-14T18:57:04Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-17T11:52:10Z

LGTM, I'm happy to move it forward as for Alpha in 0.13. One remaining remark I would like to address: https://github.com/kubernetes-sigs/kueue/pull/5648/files#r2213064962

cc @tenzen-y let me know if you have more comments requiring action.

### Comment by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-21T15:55:56Z

Hello @mimowo @tenzen-y thank you for the review and feedback. 
I have reviewed all comments and updated KEP per provided feed back.

Note: WorkloadSliceReplacement -> WorkloadSliceReplaced change was discussed in the context of https://github.com/kubernetes-sigs/kueue/pull/5510.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-21T17:27:06Z

Thank you! It is a complex, but long awaited KEP :) The number 77 says everything, I'm super happy you took the challenge. 

/lgtm
/approve
Leaving final tagging to @tenzen-y 
/hold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-21T17:27:13Z

LGTM label has been added.  <details>Git tree hash: da4dd0a6729042ce86b2bbe045daced832609f4b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-22T16:49:43Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5648#" title="Author self-approved">ichekrygin</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5648#issuecomment-3097682496" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5648#pullrequestreview-3043961500" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2025-06-25T20:24:19Z

- **keps/77-dynamically-sized-jobs/README.md:39** by [@andrewsykim](https://github.com/andrewsykim) — 2025-06-25T20:24:14Z
```diff
@@ -33,142 +33,250 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism. 
+The primary goal is to allow jobs to scale their pod count without requiring suspension and requeueing, improving scheduling efficiency and reducing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+While the initial focus is on batchv1/Job type, the long-term goal is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types like RayClusters.
```

  If possible, I suggest we focus on both batchv1/Job and RayCluster, because it is very common to enable autoscaling for RayCluster which was the initial motivation for this proposal. I'm happy to help review, test and implement anything specific to RayCluster if needed to make this happen.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-06-25T23:12:50Z

- **keps/77-dynamically-sized-jobs/README.md:39** by [@ichekrygin](https://github.com/ichekrygin) — 2025-06-25T23:12:50Z
```diff
@@ -33,142 +33,250 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism. 
+The primary goal is to allow jobs to scale their pod count without requiring suspension and requeueing, improving scheduling efficiency and reducing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+While the initial focus is on batchv1/Job type, the long-term goal is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types like RayClusters.
```

  Of course, would you be open to partnering on the implementation and validation of this feature for RayCluster? The main reason I focused exclusively on batchv1/Job for Phase I is that it's a well-known, widely used type that doesn't require domain-specific knowledge. In contrast, RayCluster does, and that's an area I'm currently less familiar with.

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2025-06-26T00:56:48Z

- **keps/77-dynamically-sized-jobs/README.md:39** by [@andrewsykim](https://github.com/andrewsykim) — 2025-06-26T00:56:48Z
```diff
@@ -33,142 +33,250 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism. 
+The primary goal is to allow jobs to scale their pod count without requiring suspension and requeueing, improving scheduling efficiency and reducing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+While the initial focus is on batchv1/Job type, the long-term goal is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types like RayClusters.
```

  Yes, happy to partner on the implementation, @weizhaowz can you help w/ the RayCluster part of the proposal / implementation?

### Review by [@weizhaowz](https://github.com/weizhaowz) (commented) — 2025-06-26T04:58:48Z

- **keps/77-dynamically-sized-jobs/README.md:39** by [@weizhaowz](https://github.com/weizhaowz) — 2025-06-26T04:58:48Z
```diff
@@ -33,142 +33,250 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism. 
+The primary goal is to allow jobs to scale their pod count without requiring suspension and requeueing, improving scheduling efficiency and reducing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+While the initial focus is on batchv1/Job type, the long-term goal is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types like RayClusters.
```

  Hi @andrewsykim and @ichekrygin, yes, I am happy to work on the RayCluster part of the proposal / implementation.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-06-26T21:31:48Z

- **keps/77-dynamically-sized-jobs/README.md:39** by [@ichekrygin](https://github.com/ichekrygin) — 2025-06-26T21:31:48Z
```diff
@@ -33,142 +33,250 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism. 
+The primary goal is to allow jobs to scale their pod count without requiring suspension and requeueing, improving scheduling efficiency and reducing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+While the initial focus is on batchv1/Job type, the long-term goal is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types like RayClusters.
```

  Updated, PTAL. 
  
  Please review the remaining KEP, specifically "Phases for MVP," and suggest RayClusters MVP targets if needed.

### Review by [@weizhaowz](https://github.com/weizhaowz) (commented) — 2025-06-27T05:04:40Z

- **keps/77-dynamically-sized-jobs/README.md:67** by [@weizhaowz](https://github.com/weizhaowz) — 2025-06-27T03:25:01Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
```

  FYI: sometimes RayJob might not be a parent object of the RayCluster, as RayJob can be submitted to an existent RayCluster.

- **keps/77-dynamically-sized-jobs/README.md:94** by [@weizhaowz](https://github.com/weizhaowz) — 2025-06-27T04:25:40Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
```

  I have questions on two corner cases: 1. the parent workload is created but not admitted yet, and the owner decided to increase parallelism, so workslices are created as well, is it possible for the the workslice get admitted before the ownerpreference workload(I guess not, because if they are with the same priority and the parent workload is in front of the workslice in the local queue)? 2. is it possible that the parent workload completes before the workslice is admitted? If so, will the workslices be marked as "Finished"? Thanks.

- **keps/77-dynamically-sized-jobs/README.md:327** by [@weizhaowz](https://github.com/weizhaowz) — 2025-06-27T04:59:40Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked for Deactivation and then transitioned to the Finished state.
+Under the current design proposal, all deactivated slices are retained indefinitely and are not garbage collected.
+To address potential resource buildup, a `PreemptedWorkloadSliceHistory` mechanism whether as a configuration option or an API field on the Workload—could be introduced to limit the number of retained inactive slices, 
+similar to how `revisionHistoryLimit` is used in Kubernetes Deployments to manage ReplicaSet history.
 
-Scaling down a Job won’t involve the creation of Workload Slices, instead it’ll involve an update to the current workload, no requeuing.
+Preempted Workload slices that fail admission requirements are not marked as Finished, as they were never actually started (i.e., never admitted or executed). 
 
-1. Compare Pod Counts: Within the job framework, check if the PodSet.Count of the job matches the Workload.Spec.PodSets[1].Count (worker group).
-2. Synchronize Workload: If the pod counts don't match, update the workload to align with the job's PodSet.Count.
-3. Trigger Resource Updates:  By updating the workload in cache and in queue, we'll signal the following:
-- The cluster queue resource usage should recalculate, ensuring accurate resource management.
-- The scheduler should re-evaluate and update PodSetAssignments related to the workload.
+Similar to current functionality, all Workloads associated with a Job (including slices) will be deleted when the parent Job is removed.
 
+### Pod Scheduling Gates
 
-#### Job controller
+Workload slices operate independently of the `spec.suspend` mechanism where applicable, relying instead on pod scheduling gates for admission control. 
+This approach is automatically enabled for supported Job types through defaulting. When a Workload (either initial or a slice) is created, all associated pods are initialized with scheduling gates—effectively "gated" from scheduling. 
+These gates are removed only upon the admission of the corresponding Workload, ensuring controlled and deliberate execution.
 
-Rework *equivalentToWorkload()* and *reconcile()* to account for potential differences between the job’s number of replicas and the running workload’s PodSet.Count. 
+```golang
+const (
+...
+  // WorkloadSliceSchedulingGate is the name of the scheduling gate applied to Pods
+  // to delay their scheduling until the associated workload slice has been admitted.
+  // This gate ensures that Pods do not begin scheduling prematurely, maintaining
+  // proper sequencing in workload processing.
+  WorkloadSliceSchedulingGate = "kueue.x-k8s.io/workload-slice"
+)
+```
 
-Given these changes, check if the delta is positive or negative indicating a scaleup/scaledown. If it’s a scaledown the reconciler should trigger an update on the workload to match the new job’s spec and no quota needs to be checked or accounted for, the cluster queue should update the workload resource usage. 
+## Phases for MVP (alpha)
 
-**Important:** in Phase 1 if there's a scale up, the behaviour will be suspend and requeue. This will be temporary while Phase 2 and 3 are not completed.
+### Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.
 
-### Phase 2 - Aggregating Workload Slices
+Scaling up and down for `batch/v1.Job` will be the initial phase of the MVP, as it builds on stable and well-understood Kubernetes components without requiring additional integration work.
 
-In Phase 2, aggregating Workload Slices into the parent workload will be implemented. This doesn't represent a usable feature to end users, but it can be reviewed independently from the phase 3.
+#### Scale Down
 
-### Phase 3 - Scale up with Workload Slices and Scheduling Gates
+1. Create a Job with workload-slice enablement and a parallelism value greater than 1. 
+2. Observe that a corresponding Workload is created and admitted, and that the Job's pods are in the Running state, matching the specified parallelism. 
+3. Update the Job by reducing its parallelism value. 
+4. Confirm that the existing Workload is updated accordingly, with WorkloadSpec.PodSets[0].Count reflecting the new parallelism value. 
+5. Observe that the number of running pods is reduced, while the remaining pods continue to run uninterrupted.
 
-In Phase 3, scale up will be implemented by introducing Workload Slices and adding Pod scheduling gates as part of Kueue’s mutating admission for the job. 
+#### Scale Up
 
-When a job scales, its webhook would be modified to intercept and "gate" all pods. Every time there’s a resize, you create a dependable (child) workload slice and once it's admitted, you increase the count in the original workload, delete the old workload and remove the schedulingGates. 
-- Pros: You are able to hold the pods added by Kuberay
-- Cons: The fact of having schedulingGates, means we need an API call for every pod, because all pods that are created by the job are going to have schedulingGates. We need to remove those gates and for every pod you need to make API calls.
+1. Create a `batch/v1.Job` with workload-slice enablement and an initial `parallelism` value greater than 0.
+2. Verify that a corresponding `Workload` is created and admitted, and that the Job’s pods are in the `Running` state, matching the specified parallelism.
+3. Increase the Job’s `parallelism` value to trigger a scale-up event.
+4. Confirm that a new `Workload` slice is created and admitted, reflecting the increased pod count, while the previous slice is deactivated and marked as `Finished`.
+5. Observe that the total number of running pods increases to match the updated parallelism, with the original pods continuing to run without disruption.
 
-#### Scheduler
-Since every scale up will have its own individual workload they should proceed to the current scheduling cycle and continue the normal admission process.
-However, we need to ensure that the Workload Slice is assigned the same resource flavor as the parent workload. 
+### Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration
+This phase mirrors the steps from Phase 1 but is adapted specifically for RayCluster workloads, taking into account their autoscaling behavior and internal lifecycle management.
```

  For RayCluster, other than autoscaling, the replicas could be updated manually by the users as well, do we want to prioritize autoscaling resizing or support both types of resizing in phase 2? @andrewsykim

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2025-06-27T14:34:37Z

- **keps/77-dynamically-sized-jobs/README.md:327** by [@andrewsykim](https://github.com/andrewsykim) — 2025-06-27T14:34:37Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked for Deactivation and then transitioned to the Finished state.
+Under the current design proposal, all deactivated slices are retained indefinitely and are not garbage collected.
+To address potential resource buildup, a `PreemptedWorkloadSliceHistory` mechanism whether as a configuration option or an API field on the Workload—could be introduced to limit the number of retained inactive slices, 
+similar to how `revisionHistoryLimit` is used in Kubernetes Deployments to manage ReplicaSet history.
 
-Scaling down a Job won’t involve the creation of Workload Slices, instead it’ll involve an update to the current workload, no requeuing.
+Preempted Workload slices that fail admission requirements are not marked as Finished, as they were never actually started (i.e., never admitted or executed). 
 
-1. Compare Pod Counts: Within the job framework, check if the PodSet.Count of the job matches the Workload.Spec.PodSets[1].Count (worker group).
-2. Synchronize Workload: If the pod counts don't match, update the workload to align with the job's PodSet.Count.
-3. Trigger Resource Updates:  By updating the workload in cache and in queue, we'll signal the following:
-- The cluster queue resource usage should recalculate, ensuring accurate resource management.
-- The scheduler should re-evaluate and update PodSetAssignments related to the workload.
+Similar to current functionality, all Workloads associated with a Job (including slices) will be deleted when the parent Job is removed.
 
+### Pod Scheduling Gates
 
-#### Job controller
+Workload slices operate independently of the `spec.suspend` mechanism where applicable, relying instead on pod scheduling gates for admission control. 
+This approach is automatically enabled for supported Job types through defaulting. When a Workload (either initial or a slice) is created, all associated pods are initialized with scheduling gates—effectively "gated" from scheduling. 
+These gates are removed only upon the admission of the corresponding Workload, ensuring controlled and deliberate execution.
 
-Rework *equivalentToWorkload()* and *reconcile()* to account for potential differences between the job’s number of replicas and the running workload’s PodSet.Count. 
+```golang
+const (
+...
+  // WorkloadSliceSchedulingGate is the name of the scheduling gate applied to Pods
+  // to delay their scheduling until the associated workload slice has been admitted.
+  // This gate ensures that Pods do not begin scheduling prematurely, maintaining
+  // proper sequencing in workload processing.
+  WorkloadSliceSchedulingGate = "kueue.x-k8s.io/workload-slice"
+)
+```
 
-Given these changes, check if the delta is positive or negative indicating a scaleup/scaledown. If it’s a scaledown the reconciler should trigger an update on the workload to match the new job’s spec and no quota needs to be checked or accounted for, the cluster queue should update the workload resource usage. 
+## Phases for MVP (alpha)
 
-**Important:** in Phase 1 if there's a scale up, the behaviour will be suspend and requeue. This will be temporary while Phase 2 and 3 are not completed.
+### Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.
 
-### Phase 2 - Aggregating Workload Slices
+Scaling up and down for `batch/v1.Job` will be the initial phase of the MVP, as it builds on stable and well-understood Kubernetes components without requiring additional integration work.
 
-In Phase 2, aggregating Workload Slices into the parent workload will be implemented. This doesn't represent a usable feature to end users, but it can be reviewed independently from the phase 3.
+#### Scale Down
 
-### Phase 3 - Scale up with Workload Slices and Scheduling Gates
+1. Create a Job with workload-slice enablement and a parallelism value greater than 1. 
+2. Observe that a corresponding Workload is created and admitted, and that the Job's pods are in the Running state, matching the specified parallelism. 
+3. Update the Job by reducing its parallelism value. 
+4. Confirm that the existing Workload is updated accordingly, with WorkloadSpec.PodSets[0].Count reflecting the new parallelism value. 
+5. Observe that the number of running pods is reduced, while the remaining pods continue to run uninterrupted.
 
-In Phase 3, scale up will be implemented by introducing Workload Slices and adding Pod scheduling gates as part of Kueue’s mutating admission for the job. 
+#### Scale Up
 
-When a job scales, its webhook would be modified to intercept and "gate" all pods. Every time there’s a resize, you create a dependable (child) workload slice and once it's admitted, you increase the count in the original workload, delete the old workload and remove the schedulingGates. 
-- Pros: You are able to hold the pods added by Kuberay
-- Cons: The fact of having schedulingGates, means we need an API call for every pod, because all pods that are created by the job are going to have schedulingGates. We need to remove those gates and for every pod you need to make API calls.
+1. Create a `batch/v1.Job` with workload-slice enablement and an initial `parallelism` value greater than 0.
+2. Verify that a corresponding `Workload` is created and admitted, and that the Job’s pods are in the `Running` state, matching the specified parallelism.
+3. Increase the Job’s `parallelism` value to trigger a scale-up event.
+4. Confirm that a new `Workload` slice is created and admitted, reflecting the increased pod count, while the previous slice is deactivated and marked as `Finished`.
+5. Observe that the total number of running pods increases to match the updated parallelism, with the original pods continuing to run without disruption.
 
-#### Scheduler
-Since every scale up will have its own individual workload they should proceed to the current scheduling cycle and continue the normal admission process.
-However, we need to ensure that the Workload Slice is assigned the same resource flavor as the parent workload. 
+### Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration
+This phase mirrors the steps from Phase 1 but is adapted specifically for RayCluster workloads, taking into account their autoscaling behavior and internal lifecycle management.
```

  I think focusing on autoscaling only is fine. I suspect the underlying mechanism to handle autoscaling or manual scaling will be the same anyways

### Review by [@andrewsykim](https://github.com/andrewsykim) (commented) — 2025-06-27T14:35:29Z

- **keps/77-dynamically-sized-jobs/README.md:67** by [@andrewsykim](https://github.com/andrewsykim) — 2025-06-27T14:35:29Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
```

  I think Kueue disallow RayJob submitting jobs to an existing cluster. See the first limitation in https://kueue.sigs.k8s.io/docs/tasks/run/rayjobs/#c-limitations

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-06-27T18:04:03Z

- **keps/77-dynamically-sized-jobs/README.md:94** by [@ichekrygin](https://github.com/ichekrygin) — 2025-06-27T18:04:03Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
```

  > 1. the parent workload is created but not admitted yet, and the owner decided to increase parallelism, so workslices are created as well, is it possible for the the workslice get admitted before the ownerpreference workload(I guess not, because if they are with the same priority and the parent workload is in front of the workslice in the local queue)?
  
  For workloads that are not yet admitted, specifically, those without a quota reservation, we update the existing Workload with the new requirements during a scale-up event. This aligns with current Kueue behavior (see [reconciler.go#L814](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/reconciler.go#L814)). In other words, we do not create a new WorkloadSlice for non-admitted workloads during scale-up.
  
  Regarding edge cases or race conditions,
  In theory, it’s possible for the job reconciler (ReconcileGenericJob) to operate on a stale version of the Workload, for instance, just after the scheduler has admitted it but before the reconciler has observed the update. In that scenario, the reconciler's attempt to update the outdated Workload will result in a conflict. This will trigger a requeue and a subsequent reconcile, at which point it will observe the current admitted state and proceed to scale the workload accordingly, i.e., by creating one or more WorkloadSlices.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-06-27T18:22:18Z

- **keps/77-dynamically-sized-jobs/README.md:94** by [@ichekrygin](https://github.com/ichekrygin) — 2025-06-27T18:22:17Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
```

  > 2. is it possible that the parent workload completes before the workslice is admitted? If so, will the workslices be marked as "Finished"?
  
  I suspect it is not likely, and in practice, it should not happen (frequently?).
  
  A `WorkloadSlice` is created as a response to a scale-up event. In the case of dynamic resizing for `batch/v1.Job`, all newly created pods are **schedule-gated** until the corresponding `WorkloadSlice` is both created and admitted. This means the job cannot progress toward completion based on scaled-up replicas unless the slice is admitted. Therefore, if the job completes, it typically indicates one of two things:
  
  * The job met its completion criteria using the currently admitted workload(s), or
  * The job was deleted (manually or otherwise).
  
  In the deletion case, all associated `Workload` and `WorkloadSlice` objects are garbage collected automatically,
  In the completion case, Kueue marks the current active `Workload` (which may be the original or a slice) as `Finished`.
  
  Now, if I understand your question correctly, you're asking whether a race is possible where the parent job completes *while* we're in the middle of a scale-up, potentially leading to two active `Workload` objects:
  
  1. The original `Workload`, which may still be undergoing preemption to make room for,
  2. A newly created `WorkloadSlice` with the increased parallelism, created just before the job completes.
  
  In this rare case, yes, we may briefly observe both `Workload` objects active. However, the job reconciler (`ReconcileGenericJob`) only processes **one** `Workload` per job at a time. It should:
  
  * First, update the new `WorkloadSlice` to mark it as `Finished`, since the job has already completed, and then
  * On a subsequent reconcile, update the original `Workload` (if still present and not already preempted), marking it as `Finished` as well.
  
  This logic ensures consistency and clean termination, even across boundary cases like this.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-06-27T18:24:27Z

- **keps/77-dynamically-sized-jobs/README.md:94** by [@ichekrygin](https://github.com/ichekrygin) — 2025-06-27T18:24:27Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
```

  Excellent observations and insightful questions, @weizhaowz, please keep them coming!

### Review by [@weizhaowz](https://github.com/weizhaowz) (commented) — 2025-06-30T21:42:28Z

- **keps/77-dynamically-sized-jobs/README.md:94** by [@weizhaowz](https://github.com/weizhaowz) — 2025-06-30T21:42:28Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
```

  Thanks for the question @ichekrygin

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T14:19:05Z

First pass, but it LGTM as for Alpha IMO. 

cc @tenzen-y

- **keps/77-dynamically-sized-jobs/README.md:61** by [@mimowo](https://github.com/mimowo) — 2025-07-02T13:13:46Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
```

  I think we can drop the specific goal for "Job". I think the ultimate goal of the KEP should be to support dynamic resize for all workload types supporting it. Including JobSet, Ray, Kubeflow, or even StatefulSet. We can add other Jobs to graduation criteria for Beta.

- **keps/77-dynamically-sized-jobs/README.md:497** by [@mimowo](https://github.com/mimowo) — 2025-07-02T13:30:39Z
```diff
@@ -260,18 +459,39 @@ Major milestones might include:
 
 ## Drawbacks
 
-<!--
-Why should this KEP _not_ be implemented?
--->
+- Adds complexity to Kueue’s scheduling and preemption model.
+- Users must understand new slice lifecycle semantics, which differ from traditional Workload behavior.
+- Leveraging PodSchedulingGates together with ResourceQuota enforcement may result out-of-quota namespaces (see section below for more details).
 
+### ResourceQuota
 
-## Alternatives
+`ResourceQuota` in Kubernetes is a **namespace-scoped object** that limits how much compute, storage, and object-count-based resources a namespace can consume. It is used to enforce fair resource usage in multi-tenant environments and to prevent any single team or workload from consuming excessive cluster resources.
+
+Quota enforcement occurs during the **pod admission phase**. When a pod is created, the API server checks whether the requested resources exceed the available quota in the namespace. If they do, the creation request is rejected.
+
+`ResourceQuota` includes all pods by default, with a few specific exceptions:
+
+| Pod Type / State         | Counted in Quota? | Notes                                |
+| ------------------------ | ----------------- | ------------------------------------ |
+| Running / Pending Pods   | ✅ Yes             | If they belong to the namespace      |
+| Succeeded / Failed Pods  | ❌ No              | Considered completed and inactive    |
+| Terminating Pods         | ❌ No              | Identified by a `deletionTimestamp`  |
+| Mirror Pods              | ❌ No              | Created directly by the kubelet      |
+| Pods outside quota scope | ❌ No              | If the `ResourceQuota` uses `scopes` |
 
+---
 
-### Ignore Resize from Kuberay
+### PodScheduling Readiness and ResourceQuota
+
+**PodScheduling Readiness** is a mechanism that ensures a pod is only eligible for scheduling when it meets specific conditions. This is especially useful for multi-stage scheduling, resource reservation, or coordination with external systems.
+
+An important observation: by default, pods that are gated using `PodSchedulingGates` are still counted toward the namespace's `ResourceQuota` if they do not match any of the exclusion conditions listed above.
+
+As a result, when `PodSchedulingGates` are used in a namespace with quota enforcement, it is possible to exhaust the available quota with ScheduleGated pods that are not yet schedulable. This can block additional pods from being admitted, even though none are actively running.
+
+
+
+## Alternatives
 
-- Idea: Ignoring the scale up or rejecting the scale up from the auto scaler and storing the value in an annotation so that Kueue takes a decision based on the annotation. We’d need a signal from Kueue to hold this in the raycluster_webhook and identify when the resize comes from Kueue, it has to be accepted.
-- Pros: No need to intercept the pods and no need of using schedulingGates
-- Cons: There would be a permanent race bewteen Kueue and Kuberay autoscaler to change the counter in the RayCluster replica number for the worker group. 
-- Exploration: See if autoscaler would indicate a desired size in the spec without altering the number of replicas directly. 
-- Discarded: Higher complexity than gating/ungating pods via SchedulingGates
+- Require users to manage resizing manually by recreating jobs.
+- Defer support for elastic workloads to higher-level controllers (e.g., RayOperator), leaving Kueue unaware of scale operations.
```

  Please add using "resizable Workload objects", by "resizeRequests" embedded into the Workload object.
  
  Please also add to Beta requirements a re-evaluation of the WorkloadSlice approach vs "ResizeRequest" approach.

- **keps/77-dynamically-sized-jobs/README.md:479** by [@mimowo](https://github.com/mimowo) — 2025-07-02T13:32:34Z
```diff
@@ -239,10 +398,50 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
-The feature starts at the alpha level, with a feature gate.
+Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *Dynamically Sized Jobs*, tailored to your current implementation of `WorkloadSlice` and phased MVP plan.
 
-In the Alpha version, Dynamically Sized Jobs will support RayCluster resizing. 
-- Scale up/down of replica workers
+---
+
+### Graduation Criteria
+
+#### Alpha
+
+* [x] Feature is gated by the `DynamicallySizedJob` feature gate.
+* [x] WorkloadSlice is implemented as an internal Kueue construct (no new CRDs).
+* [x] Scale-up events result in the creation of new `WorkloadSlice`s; scale-downs result in updates to the original `Workload`.
+* [x] Pod-level gating using `PodSchedulingGates` is integrated with slice admission.
+* [x] Sticky flavor behavior is enforced: scale-ups must reuse the original assigned flavor.
+* [x] Superseded slices are deactivated and marked as `Finished` once the new slice is admitted.
+* [x] Support for `batch/v1.Job` in both single-cluster and multi-cluster (multiKueue) configurations.
+* [x] Integration tests cover basic scale-up, scale-down, and resource flavor constraints.
+* [x] Feature opt-in is supported via workload annotation (`kueue.x-k8s.io/dynamically-sized-job: "true"`).
+* [x] Documented enablement steps, slice behavior, and known limitations.
+
+#### Beta
+
+* [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
+* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
+* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, deactivated, failed admission).
+* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
+* [ ] Formal conformance tests validate end-to-end behavior for:
+    * Horizontal scale-up and scale-down
+    * Slice transitions (preemption/deactivation)
+    * Sticky flavor enforcement
+    * Multi-cluster propagation
+* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
+* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
+* [ ] Slice preemption is consistently handled and visible in workload status conditions.
+* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
```

  I would like to also add the re-evaluate the WorkloadResize approach.

- **keps/77-dynamically-sized-jobs/README.md:479** by [@mimowo](https://github.com/mimowo) — 2025-07-02T13:34:17Z
```diff
@@ -239,10 +398,50 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
-The feature starts at the alpha level, with a feature gate.
+Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *Dynamically Sized Jobs*, tailored to your current implementation of `WorkloadSlice` and phased MVP plan.
 
-In the Alpha version, Dynamically Sized Jobs will support RayCluster resizing. 
-- Scale up/down of replica workers
+---
+
+### Graduation Criteria
+
+#### Alpha
+
+* [x] Feature is gated by the `DynamicallySizedJob` feature gate.
+* [x] WorkloadSlice is implemented as an internal Kueue construct (no new CRDs).
+* [x] Scale-up events result in the creation of new `WorkloadSlice`s; scale-downs result in updates to the original `Workload`.
+* [x] Pod-level gating using `PodSchedulingGates` is integrated with slice admission.
+* [x] Sticky flavor behavior is enforced: scale-ups must reuse the original assigned flavor.
+* [x] Superseded slices are deactivated and marked as `Finished` once the new slice is admitted.
+* [x] Support for `batch/v1.Job` in both single-cluster and multi-cluster (multiKueue) configurations.
+* [x] Integration tests cover basic scale-up, scale-down, and resource flavor constraints.
+* [x] Feature opt-in is supported via workload annotation (`kueue.x-k8s.io/dynamically-sized-job: "true"`).
+* [x] Documented enablement steps, slice behavior, and known limitations.
+
+#### Beta
+
+* [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
+* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
+* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, deactivated, failed admission).
+* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
+* [ ] Formal conformance tests validate end-to-end behavior for:
+    * Horizontal scale-up and scale-down
+    * Slice transitions (preemption/deactivation)
+    * Sticky flavor enforcement
+    * Multi-cluster propagation
+* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
+* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
+* [ ] Slice preemption is consistently handled and visible in workload status conditions.
+* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
```

  Please also add a point to extend the support for all workload types supported in Kueue which can be dynamically resized (including JobSet, RayJob, Kubeflow Jobs, StatefulSet, etc).

- **keps/77-dynamically-sized-jobs/README.md:197** by [@mimowo](https://github.com/mimowo) — 2025-07-02T13:41:00Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  What if the revision is changed, but it does not need require the replacement workload? Say, a user modified metadata (eg added label or annotation)? Would it still create a new workload?

- **keps/77-dynamically-sized-jobs/README.md:173** by [@mimowo](https://github.com/mimowo) — 2025-07-02T13:47:54Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  isn't it the same as "kueue.x-k8s.io/dynamically-sized-job"?

- **keps/77-dynamically-sized-jobs/README.md:214** by [@mimowo](https://github.com/mimowo) — 2025-07-02T14:13:07Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
```

  Deactivation currently means something different in Kueue. A deactivated workload is not finished, but just evicted and may be reactivated. Maybe call it just:
  type: Finished
  reason: WorkloadSliceAggregated

- **keps/77-dynamically-sized-jobs/README.md:210** by [@mimowo](https://github.com/mimowo) — 2025-07-02T14:13:47Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
```

  "Preemption" is implied if this is reason for the "Preempted" condition. Maybe we can call it "WorkloadSliceReplacement", wdyt?

- **keps/77-dynamically-sized-jobs/README.md:233** by [@mimowo](https://github.com/mimowo) — 2025-07-02T14:16:48Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked for Deactivation and then transitioned to the Finished state.
```

  This is actually related to my other comment about the reason names, why do we go via "Deactivation"?

- **keps/77-dynamically-sized-jobs/README.md:234** by [@mimowo](https://github.com/mimowo) — 2025-07-02T14:18:20Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked for Deactivation and then transitioned to the Finished state.
+Under the current design proposal, all deactivated slices are retained indefinitely and are not garbage collected.
```

  To mitigate this issue we can actually recommend using [ObjectRetentionPolicy](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_object_retention_policy/). For example, we can require that enabling WorkloadSlices requires configuring ObjectRetentionPolicy, wdyt?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-07T22:18:07Z

- **keps/77-dynamically-sized-jobs/README.md:497** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-07T22:18:07Z
```diff
@@ -260,18 +459,39 @@ Major milestones might include:
 
 ## Drawbacks
 
-<!--
-Why should this KEP _not_ be implemented?
--->
+- Adds complexity to Kueue’s scheduling and preemption model.
+- Users must understand new slice lifecycle semantics, which differ from traditional Workload behavior.
+- Leveraging PodSchedulingGates together with ResourceQuota enforcement may result out-of-quota namespaces (see section below for more details).
 
+### ResourceQuota
 
-## Alternatives
+`ResourceQuota` in Kubernetes is a **namespace-scoped object** that limits how much compute, storage, and object-count-based resources a namespace can consume. It is used to enforce fair resource usage in multi-tenant environments and to prevent any single team or workload from consuming excessive cluster resources.
+
+Quota enforcement occurs during the **pod admission phase**. When a pod is created, the API server checks whether the requested resources exceed the available quota in the namespace. If they do, the creation request is rejected.
+
+`ResourceQuota` includes all pods by default, with a few specific exceptions:
+
+| Pod Type / State         | Counted in Quota? | Notes                                |
+| ------------------------ | ----------------- | ------------------------------------ |
+| Running / Pending Pods   | ✅ Yes             | If they belong to the namespace      |
+| Succeeded / Failed Pods  | ❌ No              | Considered completed and inactive    |
+| Terminating Pods         | ❌ No              | Identified by a `deletionTimestamp`  |
+| Mirror Pods              | ❌ No              | Created directly by the kubelet      |
+| Pods outside quota scope | ❌ No              | If the `ResourceQuota` uses `scopes` |
 
+---
 
-### Ignore Resize from Kuberay
+### PodScheduling Readiness and ResourceQuota
+
+**PodScheduling Readiness** is a mechanism that ensures a pod is only eligible for scheduling when it meets specific conditions. This is especially useful for multi-stage scheduling, resource reservation, or coordination with external systems.
+
+An important observation: by default, pods that are gated using `PodSchedulingGates` are still counted toward the namespace's `ResourceQuota` if they do not match any of the exclusion conditions listed above.
+
+As a result, when `PodSchedulingGates` are used in a namespace with quota enforcement, it is possible to exhaust the available quota with ScheduleGated pods that are not yet schedulable. This can block additional pods from being admitted, even though none are actively running.
+
+
+
+## Alternatives
 
-- Idea: Ignoring the scale up or rejecting the scale up from the auto scaler and storing the value in an annotation so that Kueue takes a decision based on the annotation. We’d need a signal from Kueue to hold this in the raycluster_webhook and identify when the resize comes from Kueue, it has to be accepted.
-- Pros: No need to intercept the pods and no need of using schedulingGates
-- Cons: There would be a permanent race bewteen Kueue and Kuberay autoscaler to change the counter in the RayCluster replica number for the worker group. 
-- Exploration: See if autoscaler would indicate a desired size in the spec without altering the number of replicas directly. 
-- Discarded: Higher complexity than gating/ungating pods via SchedulingGates
+- Require users to manage resizing manually by recreating jobs.
+- Defer support for elastic workloads to higher-level controllers (e.g., RayOperator), leaving Kueue unaware of scale operations.
```

  @mimowo, thank you for the suggestion regarding resizeRequest. Do you have a GitHub issue (or any other link) I could reference to point others to this alternative?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-07T22:26:06Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-07T22:26:06Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  It’s similar, but ultimately serves a different purpose.
  
  The annotation `kueue.x-k8s.io/dynamically-sized-job: "true"` is used to indicate that a given Job is enabled for the dynamic resize feature (in addition to requiring the feature flag to be set).
  
  In contrast, the `kueue.x-k8s.io/workload-preemptible-slice` annotation is applied to a new workload instance (i.e., a slice) and refers to the name of the existing workload (the “old slice”) that it is intended to preempt.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-07T22:26:43Z

- **keps/77-dynamically-sized-jobs/README.md:214** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-07T22:26:43Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
```

  @mimowo, thank you for clarification - removing.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-07T22:31:48Z

- **keps/77-dynamically-sized-jobs/README.md:233** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-07T22:31:48Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked for Deactivation and then transitioned to the Finished state.
```

  It was my misunderstanding of Kueue’s workload deactivation process prior to marking it as finished.
  If deactivation is not required, we can skip that phase and transition the workload directly to _Finished_.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-07T22:36:30Z

- **keps/77-dynamically-sized-jobs/README.md:234** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-07T22:36:30Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked for Deactivation and then transitioned to the Finished state.
+Under the current design proposal, all deactivated slices are retained indefinitely and are not garbage collected.
```

  We could, although the Kubernetes `deployment.spec.revisionHistoryLimit` model might be a better fit for this use case. WDYT?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-07T22:50:00Z

- **keps/77-dynamically-sized-jobs/README.md:197** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-07T22:50:00Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  Excellent question, @mimowo. 
  
  The `GetWorkloadNameForOwnerWithGVK` function is invoked only within the `ConstructWorkload` context. Therefore, if there are any valid changes to the **Job** object that result in a new revision, but do not modify the pod count (e.g., replicas, parallelism, etc.) in a way that increases it, those changes will effectively be ignored, as they do not result in a new workload (slice) being created.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T05:19:46Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T05:19:46Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  Foreshadowing future **multi-Kueue** support, the `kueue.x-k8s.io/workload-preempting-slice` annotation will be set on the old workload to indicate the name of the new slice that is preempting it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T05:31:32Z

- **keps/77-dynamically-sized-jobs/README.md:497** by [@mimowo](https://github.com/mimowo) — 2025-07-08T05:31:32Z
```diff
@@ -260,18 +459,39 @@ Major milestones might include:
 
 ## Drawbacks
 
-<!--
-Why should this KEP _not_ be implemented?
--->
+- Adds complexity to Kueue’s scheduling and preemption model.
+- Users must understand new slice lifecycle semantics, which differ from traditional Workload behavior.
+- Leveraging PodSchedulingGates together with ResourceQuota enforcement may result out-of-quota namespaces (see section below for more details).
 
+### ResourceQuota
 
-## Alternatives
+`ResourceQuota` in Kubernetes is a **namespace-scoped object** that limits how much compute, storage, and object-count-based resources a namespace can consume. It is used to enforce fair resource usage in multi-tenant environments and to prevent any single team or workload from consuming excessive cluster resources.
+
+Quota enforcement occurs during the **pod admission phase**. When a pod is created, the API server checks whether the requested resources exceed the available quota in the namespace. If they do, the creation request is rejected.
+
+`ResourceQuota` includes all pods by default, with a few specific exceptions:
+
+| Pod Type / State         | Counted in Quota? | Notes                                |
+| ------------------------ | ----------------- | ------------------------------------ |
+| Running / Pending Pods   | ✅ Yes             | If they belong to the namespace      |
+| Succeeded / Failed Pods  | ❌ No              | Considered completed and inactive    |
+| Terminating Pods         | ❌ No              | Identified by a `deletionTimestamp`  |
+| Mirror Pods              | ❌ No              | Created directly by the kubelet      |
+| Pods outside quota scope | ❌ No              | If the `ResourceQuota` uses `scopes` |
 
+---
 
-### Ignore Resize from Kuberay
+### PodScheduling Readiness and ResourceQuota
+
+**PodScheduling Readiness** is a mechanism that ensures a pod is only eligible for scheduling when it meets specific conditions. This is especially useful for multi-stage scheduling, resource reservation, or coordination with external systems.
+
+An important observation: by default, pods that are gated using `PodSchedulingGates` are still counted toward the namespace's `ResourceQuota` if they do not match any of the exclusion conditions listed above.
+
+As a result, when `PodSchedulingGates` are used in a namespace with quota enforcement, it is possible to exhaust the available quota with ScheduleGated pods that are not yet schedulable. This can block additional pods from being admitted, even though none are actively running.
+
+
+
+## Alternatives
 
-- Idea: Ignoring the scale up or rejecting the scale up from the auto scaler and storing the value in an annotation so that Kueue takes a decision based on the annotation. We’d need a signal from Kueue to hold this in the raycluster_webhook and identify when the resize comes from Kueue, it has to be accepted.
-- Pros: No need to intercept the pods and no need of using schedulingGates
-- Cons: There would be a permanent race bewteen Kueue and Kuberay autoscaler to change the counter in the RayCluster replica number for the worker group. 
-- Exploration: See if autoscaler would indicate a desired size in the spec without altering the number of replicas directly. 
-- Discarded: Higher complexity than gating/ungating pods via SchedulingGates
+- Require users to manage resizing manually by recreating jobs.
+- Defer support for elastic workloads to higher-level controllers (e.g., RayOperator), leaving Kueue unaware of scale operations.
```

  I will open today

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T05:43:54Z

- **keps/77-dynamically-sized-jobs/README.md:234** by [@mimowo](https://github.com/mimowo) — 2025-07-08T05:43:54Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked for Deactivation and then transitioned to the Finished state.
+Under the current design proposal, all deactivated slices are retained indefinitely and are not garbage collected.
```

  We need a mechanism that will be JobCRD independent. Since we already have the ObjectRetentionPolicy Im sceptical if we need more code for GC. At least for Alpha it does not seem required. Also the two mechanisms could race sometimes.
  
  Maybe going forward we consider an extension to ObjectRetentionPolicy to specify FinishedWorkloadLimit, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T06:43:54Z

- **keps/77-dynamically-sized-jobs/README.md:497** by [@mimowo](https://github.com/mimowo) — 2025-07-08T06:43:54Z
```diff
@@ -260,18 +459,39 @@ Major milestones might include:
 
 ## Drawbacks
 
-<!--
-Why should this KEP _not_ be implemented?
--->
+- Adds complexity to Kueue’s scheduling and preemption model.
+- Users must understand new slice lifecycle semantics, which differ from traditional Workload behavior.
+- Leveraging PodSchedulingGates together with ResourceQuota enforcement may result out-of-quota namespaces (see section below for more details).
 
+### ResourceQuota
 
-## Alternatives
+`ResourceQuota` in Kubernetes is a **namespace-scoped object** that limits how much compute, storage, and object-count-based resources a namespace can consume. It is used to enforce fair resource usage in multi-tenant environments and to prevent any single team or workload from consuming excessive cluster resources.
+
+Quota enforcement occurs during the **pod admission phase**. When a pod is created, the API server checks whether the requested resources exceed the available quota in the namespace. If they do, the creation request is rejected.
+
+`ResourceQuota` includes all pods by default, with a few specific exceptions:
+
+| Pod Type / State         | Counted in Quota? | Notes                                |
+| ------------------------ | ----------------- | ------------------------------------ |
+| Running / Pending Pods   | ✅ Yes             | If they belong to the namespace      |
+| Succeeded / Failed Pods  | ❌ No              | Considered completed and inactive    |
+| Terminating Pods         | ❌ No              | Identified by a `deletionTimestamp`  |
+| Mirror Pods              | ❌ No              | Created directly by the kubelet      |
+| Pods outside quota scope | ❌ No              | If the `ResourceQuota` uses `scopes` |
 
+---
 
-### Ignore Resize from Kuberay
+### PodScheduling Readiness and ResourceQuota
+
+**PodScheduling Readiness** is a mechanism that ensures a pod is only eligible for scheduling when it meets specific conditions. This is especially useful for multi-stage scheduling, resource reservation, or coordination with external systems.
+
+An important observation: by default, pods that are gated using `PodSchedulingGates` are still counted toward the namespace's `ResourceQuota` if they do not match any of the exclusion conditions listed above.
+
+As a result, when `PodSchedulingGates` are used in a namespace with quota enforcement, it is possible to exhaust the available quota with ScheduleGated pods that are not yet schedulable. This can block additional pods from being admitted, even though none are actively running.
+
+
+
+## Alternatives
 
-- Idea: Ignoring the scale up or rejecting the scale up from the auto scaler and storing the value in an annotation so that Kueue takes a decision based on the annotation. We’d need a signal from Kueue to hold this in the raycluster_webhook and identify when the resize comes from Kueue, it has to be accepted.
-- Pros: No need to intercept the pods and no need of using schedulingGates
-- Cons: There would be a permanent race bewteen Kueue and Kuberay autoscaler to change the counter in the RayCluster replica number for the worker group. 
-- Exploration: See if autoscaler would indicate a desired size in the spec without altering the number of replicas directly. 
-- Discarded: Higher complexity than gating/ungating pods via SchedulingGates
+- Require users to manage resizing manually by recreating jobs.
+- Defer support for elastic workloads to higher-level controllers (e.g., RayOperator), leaving Kueue unaware of scale operations.
```

  Opened, ptal: https://github.com/kubernetes-sigs/kueue/issues/5897. 
  
  This is not a blocker to the effort by any means. Having a prototype for both options would be great for decision making in long run.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T06:52:52Z

- **keps/77-dynamically-sized-jobs/README.md:497** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T06:52:51Z
```diff
@@ -260,18 +459,39 @@ Major milestones might include:
 
 ## Drawbacks
 
-<!--
-Why should this KEP _not_ be implemented?
--->
+- Adds complexity to Kueue’s scheduling and preemption model.
+- Users must understand new slice lifecycle semantics, which differ from traditional Workload behavior.
+- Leveraging PodSchedulingGates together with ResourceQuota enforcement may result out-of-quota namespaces (see section below for more details).
 
+### ResourceQuota
 
-## Alternatives
+`ResourceQuota` in Kubernetes is a **namespace-scoped object** that limits how much compute, storage, and object-count-based resources a namespace can consume. It is used to enforce fair resource usage in multi-tenant environments and to prevent any single team or workload from consuming excessive cluster resources.
+
+Quota enforcement occurs during the **pod admission phase**. When a pod is created, the API server checks whether the requested resources exceed the available quota in the namespace. If they do, the creation request is rejected.
+
+`ResourceQuota` includes all pods by default, with a few specific exceptions:
+
+| Pod Type / State         | Counted in Quota? | Notes                                |
+| ------------------------ | ----------------- | ------------------------------------ |
+| Running / Pending Pods   | ✅ Yes             | If they belong to the namespace      |
+| Succeeded / Failed Pods  | ❌ No              | Considered completed and inactive    |
+| Terminating Pods         | ❌ No              | Identified by a `deletionTimestamp`  |
+| Mirror Pods              | ❌ No              | Created directly by the kubelet      |
+| Pods outside quota scope | ❌ No              | If the `ResourceQuota` uses `scopes` |
 
+---
 
-### Ignore Resize from Kuberay
+### PodScheduling Readiness and ResourceQuota
+
+**PodScheduling Readiness** is a mechanism that ensures a pod is only eligible for scheduling when it meets specific conditions. This is especially useful for multi-stage scheduling, resource reservation, or coordination with external systems.
+
+An important observation: by default, pods that are gated using `PodSchedulingGates` are still counted toward the namespace's `ResourceQuota` if they do not match any of the exclusion conditions listed above.
+
+As a result, when `PodSchedulingGates` are used in a namespace with quota enforcement, it is possible to exhaust the available quota with ScheduleGated pods that are not yet schedulable. This can block additional pods from being admitted, even though none are actively running.
+
+
+
+## Alternatives
 
-- Idea: Ignoring the scale up or rejecting the scale up from the auto scaler and storing the value in an annotation so that Kueue takes a decision based on the annotation. We’d need a signal from Kueue to hold this in the raycluster_webhook and identify when the resize comes from Kueue, it has to be accepted.
-- Pros: No need to intercept the pods and no need of using schedulingGates
-- Cons: There would be a permanent race bewteen Kueue and Kuberay autoscaler to change the counter in the RayCluster replica number for the worker group. 
-- Exploration: See if autoscaler would indicate a desired size in the spec without altering the number of replicas directly. 
-- Discarded: Higher complexity than gating/ungating pods via SchedulingGates
+- Require users to manage resizing manually by recreating jobs.
+- Defer support for elastic workloads to higher-level controllers (e.g., RayOperator), leaving Kueue unaware of scale operations.
```

  Added, PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T11:28:41Z

- **keps/77-dynamically-sized-jobs/README.md:233** by [@mimowo](https://github.com/mimowo) — 2025-07-08T11:28:41Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked for Deactivation and then transitioned to the Finished state.
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T11:30:45Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@mimowo](https://github.com/mimowo) — 2025-07-08T11:30:45Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  If it is not needed 0.13, then I would suggest to drop for now, for the ease of reviewing, and so to maximize chances for 0.13.
  
  We can add it later when working on the multi-kueue support. I think we will not be able to squeeze MultiKueue into 0.13 anyway.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T15:27:28Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T15:27:28Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  `kueue.x-k8s.io/workload-preemptible-slice` - needed for `0.13` (currently in this document)
  `kueue.x-k8s.io/workload-preempting-slice` - no needed for `0.13` (current not in this document)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:13:21Z

- **keps/77-dynamically-sized-jobs/README.md:45** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:13:21Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
```

  ```suggestion
  Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
  ```
  It does not need to be stated in the first sentence basically.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:20:44Z

- **keps/77-dynamically-sized-jobs/README.md:66** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:20:44Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
```

  TBH I'm not sure what is meant in this sentence, the KEP aims to support resizing of parent objects. I see this was taken from the previous version of the KEP. However, I would suggest either clarify the intention or drop the point.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:22:37Z

- **keps/77-dynamically-sized-jobs/README.md:65** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:22:36Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
```

  Actually I suggest to drop it from here. This is just non-goal for Alpha, and we have it in goals for Beta.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T16:22:54Z

- **keps/77-dynamically-sized-jobs/README.md:66** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:22:54Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
```

  My understanding,  it is not a goal of this proposal to orchestrate (initiate, coordinate, etc.) parent Job resizing.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T16:23:17Z

- **keps/77-dynamically-sized-jobs/README.md:66** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:23:17Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
```

  That said, I am ok removing this item altogether.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:27:07Z

- **keps/77-dynamically-sized-jobs/README.md:66** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:27:06Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
```

  > My understanding, it is not a goal of this proposal to orchestrate (initiate, coordinate, etc.) parent Job resizing.
  
  Oh I see, this makes sense. Maybe keep it then as: `Initiation or orchestration of resizing of the Job objects`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:29:45Z

- **keps/77-dynamically-sized-jobs/README.md:137** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:29:44Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
```

  ```suggestion
  ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
  ```
  wdyt? Or DynamicallySizedJobsViaWorkloadSlices

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:33:27Z

- **keps/77-dynamically-sized-jobs/README.md:150** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:33:26Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
```

  I would not declare it specifically as a constant.  We don't do it for other boolean annotations and labels

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T16:34:24Z

- **keps/77-dynamically-sized-jobs/README.md:66** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:34:24Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
```

  Updated, PTAL.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T16:34:50Z

- **keps/77-dynamically-sized-jobs/README.md:137** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:34:50Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
```

  Update (in the latest change).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:34:52Z

- **keps/77-dynamically-sized-jobs/README.md:146** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:34:51Z
```diff
@@ -7,168 +7,280 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
```

  ```suggestion
    // EnabledAnnotationKey refers to the annotation key present on Job's that support
    // workload slicing.
    // This annotation is alpha-level.
    EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
  ```

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T16:35:54Z

- **keps/77-dynamically-sized-jobs/README.md:150** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:35:54Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
```

  It is w/in a `const` block (not showing) here. 
  https://github.com/kubernetes-sigs/kueue/pull/5510/files#diff-782ca8fda9b00ad0a088dac459ec6225ee53016ef8a64a82c33fb418aeea86bf

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:36:21Z

- **keps/77-dynamically-sized-jobs/README.md:170** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:36:21Z
```diff
@@ -7,168 +7,280 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
```

  ```suggestion
  	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
  	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
  	// This annotation is alpha-level.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:37:49Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:37:49Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  sgtm, thanks for explaining

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:39:55Z

- **keps/77-dynamically-sized-jobs/README.md:229** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:39:55Z
```diff
@@ -7,168 +7,282 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSliceReplacement"
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked as Finished.
```

  ```suggestion
  Preempted (i.e., aggregated) Workload slices are marked as Finished.
  ```

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T16:40:10Z

- **keps/77-dynamically-sized-jobs/README.md:67** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:40:10Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
```

  @weizhaowz, @andrewsykim  - can we resolve this conversation?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T16:40:21Z

- **keps/77-dynamically-sized-jobs/README.md:327** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:40:21Z
```diff
@@ -33,142 +33,251 @@
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSlicePreemption"
+...
+// WorkloadEvictedByWorkloadSliceAggregation indicates that the workload was
+// deactivated as a result of workload slice aggregation.
+WorkloadEvictedByWorkloadSliceAggregation = "WorkloadSliceAggregation"	
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked for Deactivation and then transitioned to the Finished state.
+Under the current design proposal, all deactivated slices are retained indefinitely and are not garbage collected.
+To address potential resource buildup, a `PreemptedWorkloadSliceHistory` mechanism whether as a configuration option or an API field on the Workload—could be introduced to limit the number of retained inactive slices, 
+similar to how `revisionHistoryLimit` is used in Kubernetes Deployments to manage ReplicaSet history.
 
-Scaling down a Job won’t involve the creation of Workload Slices, instead it’ll involve an update to the current workload, no requeuing.
+Preempted Workload slices that fail admission requirements are not marked as Finished, as they were never actually started (i.e., never admitted or executed). 
 
-1. Compare Pod Counts: Within the job framework, check if the PodSet.Count of the job matches the Workload.Spec.PodSets[1].Count (worker group).
-2. Synchronize Workload: If the pod counts don't match, update the workload to align with the job's PodSet.Count.
-3. Trigger Resource Updates:  By updating the workload in cache and in queue, we'll signal the following:
-- The cluster queue resource usage should recalculate, ensuring accurate resource management.
-- The scheduler should re-evaluate and update PodSetAssignments related to the workload.
+Similar to current functionality, all Workloads associated with a Job (including slices) will be deleted when the parent Job is removed.
 
+### Pod Scheduling Gates
 
-#### Job controller
+Workload slices operate independently of the `spec.suspend` mechanism where applicable, relying instead on pod scheduling gates for admission control. 
+This approach is automatically enabled for supported Job types through defaulting. When a Workload (either initial or a slice) is created, all associated pods are initialized with scheduling gates—effectively "gated" from scheduling. 
+These gates are removed only upon the admission of the corresponding Workload, ensuring controlled and deliberate execution.
 
-Rework *equivalentToWorkload()* and *reconcile()* to account for potential differences between the job’s number of replicas and the running workload’s PodSet.Count. 
+```golang
+const (
+...
+  // WorkloadSliceSchedulingGate is the name of the scheduling gate applied to Pods
+  // to delay their scheduling until the associated workload slice has been admitted.
+  // This gate ensures that Pods do not begin scheduling prematurely, maintaining
+  // proper sequencing in workload processing.
+  WorkloadSliceSchedulingGate = "kueue.x-k8s.io/workload-slice"
+)
+```
 
-Given these changes, check if the delta is positive or negative indicating a scaleup/scaledown. If it’s a scaledown the reconciler should trigger an update on the workload to match the new job’s spec and no quota needs to be checked or accounted for, the cluster queue should update the workload resource usage. 
+## Phases for MVP (alpha)
 
-**Important:** in Phase 1 if there's a scale up, the behaviour will be suspend and requeue. This will be temporary while Phase 2 and 3 are not completed.
+### Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.
 
-### Phase 2 - Aggregating Workload Slices
+Scaling up and down for `batch/v1.Job` will be the initial phase of the MVP, as it builds on stable and well-understood Kubernetes components without requiring additional integration work.
 
-In Phase 2, aggregating Workload Slices into the parent workload will be implemented. This doesn't represent a usable feature to end users, but it can be reviewed independently from the phase 3.
+#### Scale Down
 
-### Phase 3 - Scale up with Workload Slices and Scheduling Gates
+1. Create a Job with workload-slice enablement and a parallelism value greater than 1. 
+2. Observe that a corresponding Workload is created and admitted, and that the Job's pods are in the Running state, matching the specified parallelism. 
+3. Update the Job by reducing its parallelism value. 
+4. Confirm that the existing Workload is updated accordingly, with WorkloadSpec.PodSets[0].Count reflecting the new parallelism value. 
+5. Observe that the number of running pods is reduced, while the remaining pods continue to run uninterrupted.
 
-In Phase 3, scale up will be implemented by introducing Workload Slices and adding Pod scheduling gates as part of Kueue’s mutating admission for the job. 
+#### Scale Up
 
-When a job scales, its webhook would be modified to intercept and "gate" all pods. Every time there’s a resize, you create a dependable (child) workload slice and once it's admitted, you increase the count in the original workload, delete the old workload and remove the schedulingGates. 
-- Pros: You are able to hold the pods added by Kuberay
-- Cons: The fact of having schedulingGates, means we need an API call for every pod, because all pods that are created by the job are going to have schedulingGates. We need to remove those gates and for every pod you need to make API calls.
+1. Create a `batch/v1.Job` with workload-slice enablement and an initial `parallelism` value greater than 0.
+2. Verify that a corresponding `Workload` is created and admitted, and that the Job’s pods are in the `Running` state, matching the specified parallelism.
+3. Increase the Job’s `parallelism` value to trigger a scale-up event.
+4. Confirm that a new `Workload` slice is created and admitted, reflecting the increased pod count, while the previous slice is deactivated and marked as `Finished`.
+5. Observe that the total number of running pods increases to match the updated parallelism, with the original pods continuing to run without disruption.
 
-#### Scheduler
-Since every scale up will have its own individual workload they should proceed to the current scheduling cycle and continue the normal admission process.
-However, we need to ensure that the Workload Slice is assigned the same resource flavor as the parent workload. 
+### Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration
+This phase mirrors the steps from Phase 1 but is adapted specifically for RayCluster workloads, taking into account their autoscaling behavior and internal lifecycle management.
```

  Similar to above: @weizhaowz, @andrewsykim  - can we resolve this conversation?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:41:45Z

- **keps/77-dynamically-sized-jobs/README.md:270** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:41:44Z
```diff
@@ -7,168 +7,282 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSliceReplacement"
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked as Finished.
+Under the current design proposal, all finished workload slices are retained indefinitely and are not garbage collected.
+To address potential resource buildup, a `PreemptedWorkloadSliceHistory` mechanism whether as a configuration option or an API field on the Workload—could be introduced to limit the number of retained inactive slices, 
+similar to how `revisionHistoryLimit` is used in Kubernetes Deployments to manage ReplicaSet history.
 
-Scaling down a Job won’t involve the creation of Workload Slices, instead it’ll involve an update to the current workload, no requeuing.
+Preempted Workload slices that fail admission requirements are not marked as Finished, as they were never actually started (i.e., never admitted or executed). 
 
-1. Compare Pod Counts: Within the job framework, check if the PodSet.Count of the job matches the Workload.Spec.PodSets[1].Count (worker group).
-2. Synchronize Workload: If the pod counts don't match, update the workload to align with the job's PodSet.Count.
-3. Trigger Resource Updates:  By updating the workload in cache and in queue, we'll signal the following:
-- The cluster queue resource usage should recalculate, ensuring accurate resource management.
-- The scheduler should re-evaluate and update PodSetAssignments related to the workload.
+Similar to current functionality, all Workloads associated with a Job (including slices) will be deleted when the parent Job is removed.
 
+### Pod Scheduling Gates
 
-#### Job controller
+Workload slices operate independently of the `spec.suspend` mechanism where applicable, relying instead on pod scheduling gates for admission control. 
+This approach is automatically enabled for supported Job types through defaulting. When a Workload (either initial or a slice) is created, all associated pods are initialized with scheduling gates—effectively "gated" from scheduling. 
+These gates are removed only upon the admission of the corresponding Workload, ensuring controlled and deliberate execution.
 
-Rework *equivalentToWorkload()* and *reconcile()* to account for potential differences between the job’s number of replicas and the running workload’s PodSet.Count. 
+```golang
+const (
+...
+  // WorkloadSliceSchedulingGate is the name of the scheduling gate applied to Pods
+  // to delay their scheduling until the associated workload slice has been admitted.
+  // This gate ensures that Pods do not begin scheduling prematurely, maintaining
+  // proper sequencing in workload processing.
```

  ```suggestion
    // proper sequencing in workload processing.
    // This scheduling gate is alpha-level.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:43:22Z

- **keps/77-dynamically-sized-jobs/README.md:251** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:43:22Z
```diff
@@ -7,168 +7,282 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSliceReplacement"
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are first marked as Finished.
+Under the current design proposal, all finished workload slices are retained indefinitely and are not garbage collected.
+To address potential resource buildup, a `PreemptedWorkloadSliceHistory` mechanism whether as a configuration option or an API field on the Workload—could be introduced to limit the number of retained inactive slices, 
+similar to how `revisionHistoryLimit` is used in Kubernetes Deployments to manage ReplicaSet history.
 
-Scaling down a Job won’t involve the creation of Workload Slices, instead it’ll involve an update to the current workload, no requeuing.
+Preempted Workload slices that fail admission requirements are not marked as Finished, as they were never actually started (i.e., never admitted or executed). 
 
-1. Compare Pod Counts: Within the job framework, check if the PodSet.Count of the job matches the Workload.Spec.PodSets[1].Count (worker group).
-2. Synchronize Workload: If the pod counts don't match, update the workload to align with the job's PodSet.Count.
-3. Trigger Resource Updates:  By updating the workload in cache and in queue, we'll signal the following:
-- The cluster queue resource usage should recalculate, ensuring accurate resource management.
-- The scheduler should re-evaluate and update PodSetAssignments related to the workload.
+Similar to current functionality, all Workloads associated with a Job (including slices) will be deleted when the parent Job is removed.
 
+### Pod Scheduling Gates
 
-#### Job controller
+Workload slices operate independently of the `spec.suspend` mechanism where applicable, relying instead on pod scheduling gates for admission control. 
+This approach is automatically enabled for supported Job types through defaulting. When a Workload (either initial or a slice) is created, all associated pods are initialized with scheduling gates—effectively "gated" from scheduling. 
+These gates are removed only upon the admission of the corresponding Workload, ensuring controlled and deliberate execution.
 
-Rework *equivalentToWorkload()* and *reconcile()* to account for potential differences between the job’s number of replicas and the running workload’s PodSet.Count. 
+```golang
+const (
+...
+  // WorkloadSliceSchedulingGate is the name of the scheduling gate applied to Pods
+  // to delay their scheduling until the associated workload slice has been admitted.
+  // This gate ensures that Pods do not begin scheduling prematurely, maintaining
+  // proper sequencing in workload processing.
+  WorkloadSliceSchedulingGate = "kueue.x-k8s.io/workload-slice"
```

  ```suggestion
    ElasticJobSchedulingGate = "kueue.x-k8s.io/elastic-job"
  ```
  wdyt? I think that the mechanism for gating pods is quite likely to be common regardless if we go with WorkloadResize or WorkloadSlices.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T16:49:38Z

- **keps/77-dynamically-sized-jobs/README.md:232** by [@mimowo](https://github.com/mimowo) — 2025-07-08T16:49:38Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSliceReplacement"
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are marked as Finished.
+Under the current design proposal, all finished workload slices are retained indefinitely and are not garbage collected.
+To address potential resource buildup, a `PreemptedWorkloadSliceHistory` mechanism whether as a configuration option or an API field on the Workload—could be introduced to limit the number of retained inactive slices, 
+similar to how `revisionHistoryLimit` is used in Kubernetes Deployments to manage ReplicaSet history.
```

  Let's defer this to Beta (add the graduation point). For alpha, if we aim 0.13 we will not have time for it. Also, I think ObjectRetentionPolicy should handle it in most cases.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T16:52:42Z

- **keps/77-dynamically-sized-jobs/README.md:232** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:52:42Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSliceReplacement"
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are marked as Finished.
+Under the current design proposal, all finished workload slices are retained indefinitely and are not garbage collected.
+To address potential resource buildup, a `PreemptedWorkloadSliceHistory` mechanism whether as a configuration option or an API field on the Workload—could be introduced to limit the number of retained inactive slices, 
+similar to how `revisionHistoryLimit` is used in Kubernetes Deployments to manage ReplicaSet history.
```

  Agreed, it is already in "Beta" section.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T16:59:53Z

- **keps/77-dynamically-sized-jobs/README.md:232** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T16:59:53Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSliceReplacement"
+```
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+##### Resource Flavors
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
 
-### Garbage Collecting Workload Slices
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
 
-## Phases for MVP (alpha)
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+Preempted (i.e., aggregated) Workload slices are marked as Finished.
+Under the current design proposal, all finished workload slices are retained indefinitely and are not garbage collected.
+To address potential resource buildup, a `PreemptedWorkloadSliceHistory` mechanism whether as a configuration option or an API field on the Workload—could be introduced to limit the number of retained inactive slices, 
+similar to how `revisionHistoryLimit` is used in Kubernetes Deployments to manage ReplicaSet history.
```

  For posterity, as [currently defined](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_object_retention_policy/#set-up-a-retention-policy):
  
  ```yaml
  objectRetentionPolicies:
    workloads:
      afterFinished: "1h"
      afterDeactivatedByKueue: "1h"
  ```
  
  the `objectRetentionPolicy` is exclusively duration-driven, which may not be the best fit for garbage collecting *aggregated* workload slices.
  
  As you suggested, adding a `finishedWorkloadLimit` (or similar) field to the policy definition would be preferable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T17:30:38Z

- **keps/77-dynamically-sized-jobs/README.md:137** by [@mimowo](https://github.com/mimowo) — 2025-07-08T17:30:38Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
```

  lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T17:42:14Z

- **keps/77-dynamically-sized-jobs/README.md:205** by [@mimowo](https://github.com/mimowo) — 2025-07-08T17:42:14Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
```

  I'm not sure I understand that. I would assume the new Workload will also have all 10 Pods in the assignment (in the `status.assignment` structure). Since we "preempt" the old workload it will release the 3 units of quota.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T17:48:01Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@mimowo](https://github.com/mimowo) — 2025-07-08T17:48:01Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  Aside from preemption I think it makes sense to deactivate the old workload. If we only preempt it I'm not sure how we can prevent re-scheduling. 
  In other words I would propose: `Preempted + Deactivated -> Finished`.
  
  So, we would have the following conditions:
  ```yaml
  - type: Preempted
    reason: WorkloadSliceReplacement
  - type: Evicted
    reason: DeactivatedDueToWorkloadSliceReplacement
  - type: Finished  # added later
    reason: WorkloadSliceReplacement
  ```
  wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T17:48:26Z

- **keps/77-dynamically-sized-jobs/README.md:210** by [@mimowo](https://github.com/mimowo) — 2025-07-08T17:48:26Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
+```golang
+// WorkloadSlicePreemptionReason indicates the Workload was preempted due to
+// the workload slice succession (roll-up/aggregation).
+WorkloadSlicePreemptionReason string = "WorkloadSliceReplacement"
```

  ```suggestion
  // WorkloadSliceReplacementReason indicates the Workload was preempted due to
  // the workload slice succession (roll-up/aggregation).
  // This reason is alpha-level.
  WorkloadSliceReplacementReason string = "WorkloadSliceReplacement"
  ```

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T17:59:09Z

- **keps/77-dynamically-sized-jobs/README.md:205** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T17:59:09Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
```

  I understand, this part can definitely be a bit confusing.
  
  The key detail is that **WorkloadSlice** preemption is _logical_, not _physical_. That is, the existing pods from the old slice are not evicted, they remain running and intact. As a result, while the new **Workload** includes all 10 pods in its spec, the scheduler only needs to assign quota for the additional 7 pods, the delta, since the first 3 will continue to run (i.e., will not be preempted).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T18:17:35Z

- **keps/77-dynamically-sized-jobs/README.md:205** by [@mimowo](https://github.com/mimowo) — 2025-07-08T18:17:35Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
```

  Preemption and admission of the replacement workload is not an atomic operation. I think what should happen is:
  1. the "old" slice is preempted and the quota of 3 is released
  2. the "new" workload worth 10 is admitted (thus delta is 7)
  
  However, the current wording suggests this is atomic operation. IIUC the "logical" quota increase is not reflected by any structures, either at the API level nor in memory. It is just "conceptual". Let me know if I'm missing something. 
  
  If my mental model is correct I would suggest the following re-phrasing (but also let me know if I'm missing something):
  
  ```
  For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods. Once the "new" Workload is at the queue head it preempts the "old" workload which releases quota
  corresponding to 3 Pods. Then, the replacement Workload is admitted and books the quota corresponding to 
  10 Pods. Note that in the process the quota usage was increased by 7 Pods.
  ```

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T19:26:26Z

- **keps/77-dynamically-sized-jobs/README.md:205** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T19:26:26Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
```

  >Preemption and admission of the replacement workload is not an atomic operation
  
  While preemption and admission are not strictly atomic operations, they occur within the same scheduling cycle, specifically within the same closure of workload processing ([scheduler.go#L213](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/scheduler.go#L213)). Barring transient errors, for every candidate workload, preemption is *immediately* followed by admission. We could think of this as **quasi-atomic** behavior from a scheduling perspective.
  
  What’s important to emphasize is that while the *workload object* is preempted, the **pods** associated with the old workload are not. More importantly, those existing pods are effectively *absorbed* into the new workload.
  
  In the example at hand, preempting an old workload with 3 running pods and admitting a new workload requesting 10 pods (7 of which are pending and schedule-gated), the new workload will effectively consist of 3 running pods and 7 un-gated pods. This transition avoids disruption while maintaining quota integrity.
  
  >  IIUC the "logical" quota increase is not reflected by any structures, either at the API level nor in memory
  
  That’s correct. While the quota increase is not explicitly reflected in any structure, either at the API level or in memory, it is implicitly represented by the new desired state: scaling from 3 to 10 pods.
  
  A close analogy is how Kubernetes Deployments handle scaling. The updated Deployment declares a new desired replica count (e.g., from 3 to 10), rather than expressing the scale direction or magnitude (e.g., "+7"), which results in the creation of a new ReplicaSet. That ultimately yields 3 existing pods carried over, and 7 new pods created to fulfill the new desired state.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T19:50:39Z

- **keps/77-dynamically-sized-jobs/README.md:205** by [@mimowo](https://github.com/mimowo) — 2025-07-08T19:50:39Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
```

  Thank you for the summary, I think when writing my last suggestion my understanding was fully inlined with the summary.
  
  I find it acceptable as is, but pointing out as a place to potentially work on clarifying. And I'm aware the proposal is not perfect either. Maybe we could just emphasize a little more the increase is just eventual.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T21:05:28Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T21:05:28Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  That aligns with my expectations as well.
  Here is an example from the POC:
  
  ```yaml
  - lastTransitionTime: "2025-07-08T20:09:24Z"
    message: Quota reserved in ClusterQueue demo
    observedGeneration: 1
    reason: QuotaReserved
    status: "True"
    type: QuotaReserved
  - lastTransitionTime: "2025-07-08T20:09:30Z"
    message: 'Preempted to accommodate a workload (UID: a112b551-a595-4356-875d-e075aee52bac, JobUID: 88d348ca-317a-47a0-bd92-786f95f2608e) due to workload slice aggregation'
    observedGeneration: 1
    reason: Preempted
    status: "True"
    type: Evicted
  - lastTransitionTime: "2025-07-08T20:09:24Z"
    message: The workload is admitted
    observedGeneration: 1
    reason: Admitted
    status: "True"
    type: Admitted
  - lastTransitionTime: "2025-07-08T20:09:30Z"
    message: 'Preempted to accommodate a workload (UID: a112b551-a595-4356-875d-e075aee52bac, JobUID: 88d348ca-317a-47a0-bd92-786f95f2608e) due to workload slice aggregation'
    reason: WorkloadSlicePreemption
    status: "True"
    type: Preempted
  - lastTransitionTime: "2025-07-08T20:09:30Z"
    message: WorkloadSliceAggregation
    reason: WorkloadSlicePreemption
    status: "True"
    type: Finished
  ```
  
  Note: `Evicted` and `Preempted` are added by current Kueue functionality, while only `Finished` is added by WorkloadSlicing.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-08T21:05:55Z

- **keps/77-dynamically-sized-jobs/README.md:205** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-08T21:05:55Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
```

  Updated, capturing additional details. PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T07:24:37Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@mimowo](https://github.com/mimowo) — 2025-07-09T07:24:36Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  > while only Finished is added by WorkloadSlicing.
  
  Yes, but since "Finished" is added later it creates a small window when the preempted workload might be re-admitted. IIUC this could happen if you suddenly have enough quota to admit both the "old" workload and the "new" replacement workload at the same time. 
  
  To prevent that from happening I see the possible approaches:
  1. add `Finished` directly from Scheduler while preempting
  2. tweak the reason for `Evicted` from `Preempted` to `DeactivatedDueToWorkloadSliceReplacement` - as deactivated workloads are not considered for re-admission
  
  I'm totally ok with both approaches. We can leave it as implementation detail I think.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T07:43:07Z

- **keps/77-dynamically-sized-jobs/README.md:205** by [@mimowo](https://github.com/mimowo) — 2025-07-09T07:43:07Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
```

  sgtm, thank you

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-09T07:54:52Z

- **keps/77-dynamically-sized-jobs/README.md:150** by [@mimowo](https://github.com/mimowo) — 2025-07-09T07:54:52Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
```

  Indeed we define the "true" and "false" consts in a couple of places in code: https://github.com/kubernetes-sigs/kueue/blob/cbe5ba6a6cb44160aae5a3b3e2b4bb461aac2ece/pkg/controller/jobs/pod/constants/constants.go#L30-L37
  
  However:
  1. the consts aren't really part of KEPs, eg.: https://github.com/kubernetes-sigs/kueue/tree/main/keps/976-plain-pods
  2. there is no even such practice in the core k8s, for example:
    - [rbac.authorization.kubernetes.io/autoupdate](https://github.com/kubernetes/kubernetes/blob/9e204ba03c2c95fd20dd18fcc6c40c3b16576592/pkg/apis/rbac/types.go#L40)
    - [safe-to-evict](https://github.com/kubernetes/autoscaler/blob/cc01c8756f900917021f7dda448db78290e572cf/cluster-autoscaler/utils/drain/drain.go#L146) in autoscaler
   
  So I think it is totally ok to drop it from KEP and keep just implementation detail. We may or may not extract "true" as a constant then.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-10T05:33:27Z

- **keps/77-dynamically-sized-jobs/README.md:205** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-10T05:33:27Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
```

  So... ahem, my previous comment wasn’t entirely accurate.
  
  > We could think of this as quasi-atomic behavior from a scheduling perspective.
  
  I overlooked the fact that during workload preemption, we *abandon* (or `continue`) scheduling the current workload and retry it in the next scheduling cycle. Just wanted to call that out here. That said, I still believe the behavior as described in the KEP remains accurate.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T15:14:06Z

- **keps/77-dynamically-sized-jobs/README.md:66** by [@mimowo](https://github.com/mimowo) — 2025-07-10T15:14:06Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
```

  lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T16:55:38Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@mimowo](https://github.com/mimowo) — 2025-07-10T16:55:38Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  I think this annotation isn't really necessary. We can effectively use the Fieldindexer as suggested in the impl PR: https://github.com/kubernetes-sigs/kueue/pull/5510#discussion_r2198243591

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T09:37:12Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@mimowo](https://github.com/mimowo) — 2025-07-11T09:37:12Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  Ok, as discussed in the related thread, I'm ok to keep it for optimizing scheduler performance. However, I prefer a more self-explanatory name like `kueue.x-k8s.io/workload-slice-replacement-for`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-11T13:40:09Z

When they enable the PartialAdmission, the scale-up result might be weird. 
Because even though request scale up from 2 -> 3, the Job is re-admitted with 2 count again as a partial admission mode, the scale-up result is 2 which is the same as an old Workload slice.

However, that is out of scope for this issue, I think.

We might need to consider in the future how to refine the experience this feature collaboration across partial admission adn DynamicJob.

- **keps/77-dynamically-sized-jobs/README.md:126** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T12:36:35Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
```

  ```suggestion
  WorkloadSlices in Kueue are enabled through a combination of a Kueue feature gate and an opt-in annotation on individual Workload objects. 
  ```
  
  Does this mean `Kueue` feature gate?

- **keps/77-dynamically-sized-jobs/README.md:123** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T12:37:03Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
```

  ```suggestion
  At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kueue feature gate, which controls whether the controller logic for slicing is active. 
  ```
  
  Same as above.

- **keps/77-dynamically-sized-jobs/README.md:141** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T12:40:25Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
```

  Could you update this one as well?
  
  https://github.com/kubernetes-sigs/kueue/blob/fe984bcd6f2796ad822f3a3d7e546ef94fdfb157/keps/77-dynamically-sized-jobs/kep.yaml

- **keps/77-dynamically-sized-jobs/README.md:197** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T12:48:43Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  Could you mention the algorithm for new workload name generation? Especially, suffix `75e0c` and `e7080`.

- **keps/77-dynamically-sized-jobs/README.md:197** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T12:50:20Z
```diff
@@ -7,168 +7,286 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed batch/v1.Job workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  - Support dynamic updates to `batch/v1.Job` parallelism, allowing changes to pod count without deleting existing pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  I think that this is an important point. @ichekrygin Could you mention it in this document?

- **keps/77-dynamically-sized-jobs/README.md:206** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T12:54:22Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
 
-### Garbage Collecting Workload Slices
+While preemption and admission are not strictly atomic operations, they occur within the same scheduling cycle, specifically within the same closure of workload processing (scheduler.go#L213). Barring transient errors, for every candidate workload, preemption is immediately followed by admission. We could think of this as quasi-atomic behavior from a scheduling perspective.
```

  ```suggestion
  While preemption and admission are not strictly atomic operations, they occur within the same scheduling cycle, specifically within the same closure of workload processing. Barring transient errors, for every candidate workload, preemption is immediately followed by admission. We could think of this as quasi-atomic behavior from a scheduling perspective.
  ```
  
  I would avoid mentioning the actual code line since indeed, the mention will go out of date soon.

- **keps/77-dynamically-sized-jobs/README.md:206** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T13:02:27Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  I totally agree with Evicted with DeactivatedDueToWorkloadSliceReplacement instead of Preempted.
  
  My additional question is what if they enable objectRetentionPolicy (https://kueue.sigs.k8s.io/docs/tasks/manage/setup_object_retention_policy/#set-up-a-retention-policy)?
  
  Which condition lastTransitionTime (Evicted vs Finished) is respected?

- **keps/77-dynamically-sized-jobs/README.md:173** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T13:18:24Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  I think this could be moved to status field.
  However, let's postpone the API expansion decision later, then use this annotation for now.
  @ichekrygin Could you replace the annotation name as @mimowo mentioned?

- **keps/77-dynamically-sized-jobs/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T13:19:42Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
```

  When there are old and new Workload (slice) in the cluster, how to identify the latest one from batch/v1 Job object?

- **keps/77-dynamically-sized-jobs/README.md:199** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T13:25:07Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+#### Scheduling and Preemption
```

  What if the flavor assigner assigns a different flavor to a new Workload from an old Workload?
  
  Will you implement any mechanism to assign the same flavor to a new Workload as an old Workload?
  Will the flavor assigner filter out the different flavors from an old Workload?

- **keps/77-dynamically-sized-jobs/README.md:150** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T13:26:52Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
```

  If this is just for backward compatibility, we probably can abandon this annotation when we promote the Workload API version to v1beta2.
  
  But, that is not now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T14:34:19Z

- **keps/77-dynamically-sized-jobs/README.md:150** by [@mimowo](https://github.com/mimowo) — 2025-07-11T14:34:19Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
```

  > If this is just for backward compatibility, 
  
  I think backwards-compatibility is the major reason, however there are more which make me think we want "kueue.x-k8s.io/elastic-job" in the long run (even while in Beta, enabled by default):
  1. the mechanism is based on scheduling gates, and this would mean performance hit for users not using ElasticJobs. I think removing the scheduling gate from every Pod can be felt by some large-scale users. Sure, for most users it will not be an issue, so we may have "AssumeAllJobsAreElastic" in global configuration, but I would defer it for later.
  2. some users may actually prefer to still evict the Job when resizing, for example so that the Job has a chance to switch flavor. Maybe the old flavor is to small to accommodate it.
  
  I think there is definitely room for some knobs which will make it easier to use by users (like AssumeAllJobsAreElastic) , but I would also prefer to defer it for later graduation stages.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T15:39:51Z

- **keps/77-dynamically-sized-jobs/README.md:126** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T15:39:51Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
```

  Hi @tenzen-y, I might be misunderstanding the question, but the intent was to refer to a Kubernetes-style feature gate defined within Kueue, a “Kueue feature gate,” so to speak.
  
  Let me know if you’d prefer I clarify or update the wording, happy to adjust.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T15:47:58Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T15:47:58Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  > I think this could be moved to status field.
  
  To be honest, neither the annotation nor, even less so, the status field are ideal places to define desired state. If we follow the Kubernetes paradigm strictly, the only appropriate place to express desired state is the object’s `spec`.
  
  Specifically, the reason why the status is not suitable is that we should be able to wipe the entire status clean and still be able to reconcile the object, in this case, the Workload, based solely on its spec to reach the desired state. The annotation is a bit of a "gray-area" in this regard. I understand that Kueue currently relies on the status subresource as a kind of “quasi-spec,” and that may be acceptable as an interim solution.
  
  That said, I agree that we should revisit this during an API review and possibly consider a refactor for better alignment with core Kubernetes conventions.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T15:57:18Z

- **keps/77-dynamically-sized-jobs/README.md:197** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T15:57:18Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  I may not fully understand the request. The algorithm id identical to the current implementation, and includes just one extra field (generation) during "hash suffix" value calculation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T16:04:09Z

- **keps/77-dynamically-sized-jobs/README.md:126** by [@mimowo](https://github.com/mimowo) — 2025-07-11T16:04:09Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
```

  If my chime in. Kueue docs say "Kueue uses a similar mechanism to configure features as described in Kubernetes Feature Gates." 
  https://kueue.sigs.k8s.io/docs/installation/#change-the-feature-gates-configuration
  
  I think it is fair to drop the Kueue /Kubernetes prefix in this context, it is pretty clear we mean Kueue feature gate.
  
  In kubernetes documentation we rarely, or never, say Kubernetes feature gate. mostly we just say 'feature gate' as it is clear from context.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-11T17:06:41Z

- **keps/77-dynamically-sized-jobs/README.md:126** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T17:06:41Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
```

  https://kueue.sigs.k8s.io/docs/installation/#change-the-feature-gates-configuration is Kueue feature gate.
  https://kubernetes.io/docs/reference/command-line-tools-reference/feature-gates/ is Kubernetes feature gate.
  
  I'm ok with either approach (dropping "Kubernetes" term vs replacing "Kubernetes" with "Kueue").

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-11T17:11:45Z

- **keps/77-dynamically-sized-jobs/README.md:197** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T17:11:45Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  Sorry for your confusion. I want to clarify what is format to compile this hash (75e0c).
  The current form is `$PREFIX_NAME-$HASH` where $HASH is compiled by group, kind, owner name, and owner UID.
  Will this proposal try to add new element, generation to $HASH?
  If yes, I want to mention that here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-11T17:18:09Z

- **keps/77-dynamically-sized-jobs/README.md:150** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-11T17:18:08Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
```

  > the mechanism is based on scheduling gates, and this would mean performance hit for users not using ElasticJobs. I think removing the scheduling gate from every Pod can be felt by some large-scale users. Sure, for most users it will not be an issue, so we may have "AssumeAllJobsAreElastic" in global configuration, but I would defer it for later.
  
  Actually, as my initial thought was moving this annotation functionality to Global Config (AssumeAllJobsAreElastic).
  However, I wanted to think more if the config is useful. In other words, for now, this should be annotation. But, in the future versioin, it might not be annotation.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T17:51:46Z

- **keps/77-dynamically-sized-jobs/README.md:126** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T17:51:46Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
```

  If there is a consensus (as it appears), I would leave it as "Kueue feature gate".

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T17:51:58Z

- **keps/77-dynamically-sized-jobs/README.md:123** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T17:51:57Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
```

  Related to: https://github.com/kubernetes-sigs/kueue/pull/5648#discussion_r2201442433

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T17:53:25Z

- **keps/77-dynamically-sized-jobs/README.md:126** by [@mimowo](https://github.com/mimowo) — 2025-07-11T17:53:25Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
```

  I'm personally also ok with either of the two options. Kueue feature gate is ok.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T18:00:16Z

- **keps/77-dynamically-sized-jobs/README.md:197** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T18:00:16Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  > If yes, I want to mention that here.
  
  Just to clarify, @tenzen-y , this KEP doesn’t go into detail about *how* the unique name or suffix is generated, it intentionally leaves those as implementation details. What it does specify is the requirement that, for workload slices, we generate a unique suffix for different object revisions when creating a new workload slice, and it includes examples to illustrate that.
  
  Are you suggesting that we should include more detail about how the suffix is currently computed, and how that might change in light of the new requirements?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T18:04:55Z

- **keps/77-dynamically-sized-jobs/README.md:177** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T18:04:55Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
```

  > how to identify the latest
  
  The simplest way is via `creationTimestamp`.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T18:10:30Z

- **keps/77-dynamically-sized-jobs/README.md:199** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T18:10:30Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+#### Scheduling and Preemption
```

  This is actually captured in the **Design Details** section:
  
  > *Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups.*
  
  > Will you implement any mechanism to assign the same flavor to a new Workload as an old Workload?
  
  Yes, that’s already a standing requirement.
  
  > Will the flavor assigner filter out the different flavors from an old Workload?
  
  I’m not sure I fully understand the question, could you clarify what you mean by “filter out”?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T19:27:38Z

- **keps/77-dynamically-sized-jobs/README.md:150** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T19:27:37Z
```diff
@@ -7,168 +7,281 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally. 
+- Support resize for other Kueue jobs such as Job, JobSet, etc (future).
+- Resizing of the Workload parent objects (`batchv1/Job`, RayJobs, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/dynamically-sized-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// WorkloadSlices enables workload-slices support.
+WorkloadSlices featuregate.Feature = "DynamicallySizedJob"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  EnabledAnnotationKey = "kueue.x-k8s.io/dynamically-sized-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
```

  done.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T19:30:16Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T19:30:16Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  done.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T19:30:48Z

- **keps/77-dynamically-sized-jobs/README.md:126** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T19:30:47Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
```

  updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T19:31:05Z

- **keps/77-dynamically-sized-jobs/README.md:141** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T19:31:05Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
```

  updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-11T23:10:34Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-11T23:10:34Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  Thinking about this a bit more:
  
  ```yaml
  - type: Preempted
    reason: WorkloadSliceReplacement
  - type: Evicted
    reason: DeactivatedDueToWorkloadSliceReplacement
  - type: Finished  # added later
    reason: WorkloadSliceReplacement
  ```
  
  In the context of replacing an old workload slice with a new one, does this really qualify as **preemption**?
  
  To me, it feels more like an **eviction without preemption**. Preemption generally implies that the workload might eventually be re-admitted, as @mimowo pointed out:
  
  > Preempted workload might be re-admitted
  
  But in this case, the intent is to evict and **replace** the old workload slice, not pause or defer it for later. That seems like a different semantic.
  
  It reminds me of the distinction in Kubernetes between a Pod evicted by the scheduler (and still eligible for rescheduling) versus a Pod evicted by the kubelet and marked as `Failed`, with no opportunity for rescheduling. For workload slices, we’re closer to the latter case.
  
  So maybe we could simplify this by marking the old slice as **evicted and finished**, without using `Preempted` at all. What do you think?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T07:08:03Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@mimowo](https://github.com/mimowo) — 2025-07-14T07:08:03Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  > > But in this case, the intent is to evict and replace the old workload slice, not pause or defer it for later. That seems like a different semantic.
  
  I would say the intent of the user is more higher level - resize the Job. Workload is not really API meant for users, mostly for internal mechanics of Kueue, and potentially admins and automation (like UIs).
  
  > It reminds me of the distinction in Kubernetes between a Pod evicted by the scheduler (and still eligible for rescheduling) versus a Pod evicted by the kubelet and marked as Failed, with no opportunity for rescheduling. For workload slices, we’re closer to the latter case.
  
  +1 
  
  > So maybe we could simplify this by marking the old slice as evicted and finished, without using Preempted at all. What do you think?
  
  I think the most important is setting `Finished`. I'm not seeing a conceptual difference between Evicted and Preempted in this context, both are technical conditions which are added naturally with the current code paths. Since we are using the code paths for preemption I'm still ok to set `Preempted` condition - not setting it would probably require extra code. 
  
  In summary, I'm ok with either: 
  1. `Finished` only - minimalist API approach
  2. `Finished + Evicted(Deactivated) + Preempted` - conditions which are added naturally by reusing the code paths for Preemption
  
  Options when `Evicted` is set, but not `Preempted` feel a bit artificial to me. Let me know if I'm missing something.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-14T14:21:45Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-14T14:21:45Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  IIUC, in kubernetes, a Pod is terminated by eviction even when the Pod is running.
  In Kueue, a Pod is terminated by eviction only when the Pod is not running (not healthy).
  
  So, if we do not add the `Preempted` condition when Workload replacement, we will introduce a new semantics to Kueue workload eviction.
  
  I think that we should have extra discussion for expanding workload eviction responsibilities which means the Kueue can issue eviscion against running health workloads.
  
  Hence, we would like to keep the workload eviction for now and add Preempted to replace the workload.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-14T14:23:11Z

- **keps/77-dynamically-sized-jobs/README.md:126** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-14T14:23:11Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
```

  LGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-14T14:23:25Z

- **keps/77-dynamically-sized-jobs/README.md:141** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-14T14:23:25Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
```

  LGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-14T14:33:01Z

- **keps/77-dynamically-sized-jobs/README.md:197** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-14T14:33:01Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  > Are you suggesting that we should include more detail about how the suffix is currently computed, and how that might change in light of the new requirements?
  
  I don't say that we should describe a detailed algorithm here. IIUC, including generation is a key point when we compile the workload name suffix hash. Because the current hash compiling mechanism could occur conflic between old and new WorkloadSlices.
  
  So, I would like to describe that why do we need to change the hash computing way, and what are differences between old and new hash computing algorithms.
  
  For example, we could say that the current Wokrload name suffix hash computing mechanism does not compatible with the new WorkloadSlice mechanism since it does not allow us to distinguish between old and new WokrloadSlices due to no consideration of generation. So, the new WorkloadSlice name suffix generation mechanism uses Job's `.metadata. generation` in addition to the current hash computing elements.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-14T14:34:08Z

- **keps/77-dynamically-sized-jobs/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-14T14:34:08Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
```

  I see. SGTM.
  Could you mention that we always respect the latest created WorkloadSlice?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-14T14:41:47Z

- **keps/77-dynamically-sized-jobs/README.md:199** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-14T14:41:47Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+#### Scheduling and Preemption
```

  > This is actually captured in the Design Details section:
  
  >> Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups.
  >> 
  >> Will you implement any mechanism to assign the same flavor to a new Workload as an old Workload?
  
  > Yes, that’s already a standing requirement.
  
  Oh, I see. I was missing the description. Thank you, SGTM.
  
  >> Will the flavor assigner filter out the different flavors from an old Workload?
  
  > I’m not sure I fully understand the question, could you clarify what you mean by “filter out”?
  
  I thought that the flavor assigner tries to reschedule the flavors against the new WorkloadSlice. However, as your description (`inherits the resource flavor assignment of the original admitted slice`), the flavor assigner does not try to find the new flavors to accommodate the new WorkloadSlice. The assigner just retries the same flavor with increased count, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T14:44:13Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@mimowo](https://github.com/mimowo) — 2025-07-14T14:44:13Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  Except for Pod integration, in Kueue the unit of preemption (and eviction) is Workload (like Job). When Workload is preempted the Job gets suspended, and all Pods deleted. Kueue may already evict a running, healthy workload, if it is low-priority (for example).
  
  EDIT: this process is a bit different in case of "Replacement", because in case of replacement the Job is never suspended.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-14T16:10:59Z

- **keps/77-dynamically-sized-jobs/README.md:199** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-14T16:10:59Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+#### Scheduling and Preemption
```

  > The assigner just retries the same flavor with increased count, right?
  
  Yes, that is correct.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-14T16:35:51Z

- **keps/77-dynamically-sized-jobs/README.md:199** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-14T16:35:50Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+#### Scheduling and Preemption
```

  > > The assigner just retries the same flavor with increased count, right?
  > 
  > Yes, that is correct.
  
  Thank you for describing that. SGTM.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-14T17:49:39Z

- **keps/77-dynamically-sized-jobs/README.md:197** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-14T17:49:39Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  > For example, we could say that the current Wokrload name suffix ...
  
  @tenzen-y , I believe the KEP already says this in more or less the same words under the "Naming" section. PTAL and let me know if you'd like me to update or clarify anything there.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-14T18:08:42Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-14T18:08:41Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  > In summary, I'm ok with either:
  > 
  > 1. `Finished` only - minimalist API approach
  > 2. `Finished + Evicted(Deactivated) + Preempted` - conditions which are added naturally by reusing the code paths for Preemption
  > 
  > Options when `Evicted` is set, but not `Preempted` feel a bit artificial to me. Let me know if I'm missing something.
  
  I lean toward the first option, mainly for the sake of simplicity, both in how we apply status conditions and, more importantly, in how we check them.
  
  Having three separate conditions to reflect essentially the same state feels unnecessarily complex and risks introducing ambiguity. For example, it could lead to inconsistent or unexpected combinations, like `Preempted` without `Evicted`, or vice versa.
  
  Specifically regarding Kueue’s current usage of `Preempted` and `Evicted`, from what I can tell, they are always applied together. I might be missing something, but I haven’t come across any cases where one is set without the other. If that’s correct, maybe we could even consolidate those into a single condition, possibly just `Evicted`, but that is outside of this KEP context. 
  
  In the case of workload slice aggregation, here I am using the term “aggregation” deliberately to distinguish this from traditional preemption or eviction. It’s really neither in the strict sense, we’re just replacing one workload with another. I did consider introducing a new `Aggregated` condition, but decided against it to avoid introducing yet another condition with ripple effects across the codebase.
  
  So, if setting a single `Finished` condition with a reason like `"WorkloadSliceAggregation"` covers the intent and already supported but the current Kueue's workload lifecycle handling, I’d definitely vote for that!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T18:14:27Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@mimowo](https://github.com/mimowo) — 2025-07-14T18:14:27Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  I think this is conceptually a spec field indeed, rather than status. 
  
  However Workload API is Beta level and this feature is alpha level, so it is likely we will need to change to API (for example if we decided on WorkloadResize approch).
  
  Also the annotation is primarily introduced for performane optimization which could probably also be achieved with field indexing, so Im not convinced fully we need it at all. 
  
  Maybe we should add a point to Beta graduation to revisit the annotation as I see three legitimate options going forward:
  1. keep as annotation as is
  2. drop in favor of an indexed field
  3. turn into a Workload spec field
  
  My personal preference is 2. to minimize API surface, and defer performance optimizations until we are proven to need them, avoiding premature optimization. Otoh I understand that scheduler performance is very important.
  
  Im ok with either 1. and 3. as we promote the fearure to Beta.
  
  wdyt ?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-14T18:49:09Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-14T18:49:09Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  > Also the annotation is primarily introduced for performane optimization which could probably also be achieved with field indexing, so Im not convinced fully we need it at all.
  
  Performance is certainly one motivation, but in my opinion, it's not the most important one.
  
  Conceptually, the notion of the "old" slice only becomes relevant when we need to create a "new" slice. The key here is that the new slice is explicitly constructed to replace one, and only one, specific workload instance. That precise relationship is what the annotation helps capture.
  
  In current Kueue behavior, the idea of searching for a Job’s workload comes up when we “ensure one” workload for a Job. For standard (non–workload-slice-enabled) Jobs, there can be at most one workload, so the state is binary: either 0 or 1. If the currently admitted workload becomes outdated, Kueue deletes it and creates a new one, there’s no preemption or eviction involved. If the workload is not admitted, it gets updated in place.
  
  In summary, in current Kueue job-to-workload search (via indexing), there is little to no room for ambiguity. A workload either:
  
  * Doesn’t exist and needs to be created, or
  * Exists and is:
  
    * In-sync with the Job (whether admitted or not), in which case nothing needs to be done, or
    * Out-of-sync with the Job, and:
  
      * If **admitted**, the old workload is deleted and a new one is created,
      * If **not admitted**, the workload is updated in place.
  
  Workload slicing introduces a more nuanced model where this binary assumption no longer holds. While we still search for the current workload and evaluate whether it aligns with the Job, we cannot simply delete an out-of-sync slice (e.g., during scale-up). Instead, we create a new workload slice and annotate it with the name of the specific workload it is meant to replace.
  
  Granted, the slice model enforces that there are at most two active workloads for a Job (only one of which is admitted), but this introduces a theoretical risk: searching for a workload in different contexts (e.g., job handling vs. workload scheduling) could, in theory, return different slices. This shouldn’t happen under normal conditions, but by using a single source of truth during job handling, and explicitly linking the new slice to the one it is replacing via annotation, we eliminate even that small possibility of mistakenly evicting the wrong workload.
  
  So while performance is part of the motivation, the annotation serves a more critical role: providing a clear and unambiguous link between the new and old workload slices during slice replacement.
  
  That said, if the use of annotations becomes a blocker, I’m okay with falling back to the search-based approach, especially since that logic is already in place. I just wanted to highlight the subtle risks of ambiguity when relying solely on search results across different contexts.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T18:58:18Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@mimowo](https://github.com/mimowo) — 2025-07-14T18:58:18Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  sgtm, Im fine with 1. The only reason for 3 I was thinking about is that it probably saves a single 'if' as by default these would be set
  Setting the conditikns is also coupled with metrics updates, however, we probably need to revisit metrics for the feature anyway.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-14T20:12:26Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-14T20:12:25Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  Updated, added subsection: Preemption Disambiguation.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-17T06:51:09Z

- **keps/77-dynamically-sized-jobs/README.md:206** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-17T06:51:08Z
```diff
@@ -7,168 +7,283 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
+
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
 
-### Pod Scheduling Gates
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Preempted status condition, including an appropriate reason and message.
```

  I'm fine with opt 1 as a first step. Let us revisit how we should define the state transition for the Workload replacing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T11:17:18Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@mimowo](https://github.com/mimowo) — 2025-07-17T11:17:18Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  > That said, if the use of annotations becomes a blocker,
  
  It is not a blocker to me while in alpha, but rename it to `kueue.x-k8s.io/workload-slice-replacement-for`, and add a point in graduation criteria for Beta to revisit this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-17T12:20:38Z

- **keps/77-dynamically-sized-jobs/README.md:227** by [@mimowo](https://github.com/mimowo) — 2025-07-17T12:20:38Z
```diff
@@ -7,168 +7,332 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Preemption Disambiguation.](#preemption-disambiguation)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
+  - [Limitations and Incompatibilities](#limitations-and-incompatibilities)
+    - [PartialAdmission](#partialadmission)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kueue feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
+
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
+
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing 
+preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+While preemption and admission are not strictly atomic operations, they occur within the same scheduling cycle, specifically within the same closure of workload processing. Barring transient errors, for every candidate workload, preemption is immediately followed by admission. We could think of this as quasi-atomic behavior from a scheduling perspective.
+
+What’s important to emphasize is that while the workload object is preempted, the pods associated with the old workload are not. More importantly, those existing pods are effectively absorbed into the new workload.
+
+In the example at hand, preempting an old workload with 3 running pods and admitting a new workload requesting 10 pods (7 of which are pending and schedule-gated), the new workload will effectively consist of 3 running pods and 7 un-gated pods. This transition avoids disruption while maintaining quota integrity.
+
+##### Preemption Disambiguation.
+
+Here, we’re using the term “preemption” to describe the process of replacing an old workload slice with a new one. 
+At the same time, Kueue already uses the concept of workload preemption to mean evicting a workload and deferring its scheduling until capacity becomes available.
+
+In the context of workload slices, it may be more helpful to think of this replacement process as **aggregation** rather than preemption. 
+Aggregation involves terminating the old slice and activating the new one. In the strict sense, neither eviction nor preemption (as traditionally defined) actually occurs during this process.
+
+As such, we mark the old slice with the `Finished` condition. This ensures it is removed from the in-memory ClusterQueue snapshot, and its resource usage is released and attributed to the new slice that replaces it.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Finished status condition, including an appropriate reason and message.
+```yaml
+- type: Finished
+  status: True
+  reason: WorkloadSliceAggregation
```

  ```suggestion
    reason: WorkloadSliceReplacement
  ```
  nit: seems like more natural word  to describe it. I think Aggregation is more historical as in the previous design we would "aggregate" potentially multiple workload slices. In the new design we just replace one with another.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T12:46:41Z

- **keps/77-dynamically-sized-jobs/README.md:117** by [@mimowo](https://github.com/mimowo) — 2025-07-18T12:46:40Z
```diff
@@ -7,168 +7,332 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Preemption Disambiguation.](#preemption-disambiguation)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
+  - [Limitations and Incompatibilities](#limitations-and-incompatibilities)
+    - [PartialAdmission](#partialadmission)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
```

  ```suggestion
  The replaced slice will be marked as finished.
  ```
  nit, as we discussed on the implementation PR, the deactivation is not needed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T12:47:00Z

- **keps/77-dynamically-sized-jobs/README.md:323** by [@mimowo](https://github.com/mimowo) — 2025-07-18T12:47:00Z
```diff
@@ -7,168 +7,332 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Preemption Disambiguation.](#preemption-disambiguation)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
+  - [Limitations and Incompatibilities](#limitations-and-incompatibilities)
+    - [PartialAdmission](#partialadmission)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kueue feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
+
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
+
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing 
+preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+While preemption and admission are not strictly atomic operations, they occur within the same scheduling cycle, specifically within the same closure of workload processing. Barring transient errors, for every candidate workload, preemption is immediately followed by admission. We could think of this as quasi-atomic behavior from a scheduling perspective.
+
+What’s important to emphasize is that while the workload object is preempted, the pods associated with the old workload are not. More importantly, those existing pods are effectively absorbed into the new workload.
+
+In the example at hand, preempting an old workload with 3 running pods and admitting a new workload requesting 10 pods (7 of which are pending and schedule-gated), the new workload will effectively consist of 3 running pods and 7 un-gated pods. This transition avoids disruption while maintaining quota integrity.
+
+##### Preemption Disambiguation.
+
+Here, we’re using the term “preemption” to describe the process of replacing an old workload slice with a new one. 
+At the same time, Kueue already uses the concept of workload preemption to mean evicting a workload and deferring its scheduling until capacity becomes available.
+
+In the context of workload slices, it may be more helpful to think of this replacement process as **aggregation** rather than preemption. 
+Aggregation involves terminating the old slice and activating the new one. In the strict sense, neither eviction nor preemption (as traditionally defined) actually occurs during this process.
+
+As such, we mark the old slice with the `Finished` condition. This ensures it is removed from the in-memory ClusterQueue snapshot, and its resource usage is released and attributed to the new slice that replaces it.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Finished status condition, including an appropriate reason and message.
+```yaml
+- type: Finished
+  status: True
+  reason: WorkloadSliceAggregation
+  message: 'Removed to accommodate a workload (UID: a112b551-a595-4356-875d-e075aee52bac, JobUID: 88d348ca-317a-47a0-bd92-786f95f2608e) due to workload slice aggregation'
+  observedGeneration: 1
+  lastTransitionTime: "2025-07-08T20:09:24Z"
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Resource Flavors
+
+In the context of WorkloadSlice scheduling, when a workload qualifies for multiple resource flavors, i.e., more than one flavor can satisfy the resource requirements and selector constraints, the scheduler must determine which flavor to assign. 
+Kueue currently does not support combining resources from multiple flavors within a single PodSet; however, multiple PodSets within the same workload may be admitted using different resource flavors.
+
+Flavor selection is influenced by factors such as quota availability, taint tolerations, node label selectors, and, where applicable, topology-aware scheduling policies.
+
+When scaling up a previously admitted workload slice, the current implementation enforces a sticky flavor constraint, meaning that the new slice must reuse the originally assigned flavor, even if other eligible flavors have available capacity. 
+This approach avoids unpredictable flavor switching but may lead to the new slice remaining in a Pending state if the original flavor lacks sufficient resources.
+
+Future enhancements may introduce support for controlled flavor reassignment or user-defined policies to express preferences or tolerances for such transitions.
+
+#### Garbage Collection of Preempted Workload Slices
+Currently, a Workload can become outdated, marked by Kueue as "out-of-sync", and subsequently transitioned to the "Finished" state.
+This also applies to outdated Workload slices: when multiple successive updates are issued for a given Job, any superseded slices are marked as "Finished" to reflect their obsolescence.
+
+Preempted (i.e., aggregated) Workload slices are marked as Finished.
+Under the current design proposal, all finished workload slices are retained indefinitely and are not garbage collected.
+To address potential resource buildup, a `PreemptedWorkloadSliceHistory` mechanism whether as a configuration option or an API field on the Workload, could be introduced to limit the number of retained inactive slices, 
+similar to how `revisionHistoryLimit` is used in Kubernetes Deployments to manage ReplicaSet history.
+
+Preempted Workload slices that fail admission requirements are not marked as Finished, as they were never actually started (i.e., never admitted or executed). 
+
+Similar to current functionality, all Workloads associated with a Job (including slices) will be deleted when the parent Job is removed.
 
 ### Pod Scheduling Gates
 
-Inside the job's webhook, implement schedulingGate injection for pods on creation time.
-The Pods will be ungated following a similar behavior as to how a job is suspended and then unsuspended in the when admitted.
-When the job scales up, the new pods will be gated due to the schedulingGates injection in the webhook.
+Workload slices operate independently of the `spec.suspend` mechanism where applicable, relying instead on pod scheduling gates for admission control. 
+This approach is automatically enabled for supported Job types through defaulting. When a Workload (either initial or a slice) is created, all associated pods are initialized with scheduling gates, effectively "gated" from scheduling. 
+These gates are removed only upon the admission of the corresponding Workload, ensuring controlled and deliberate execution.
+
+```golang
+const (
+...
+  // WorkloadSliceSchedulingGate is the name of the scheduling gate applied to Pods
+  // to delay their scheduling until the associated workload slice has been admitted.
+  // This gate ensures that Pods do not begin scheduling prematurely, maintaining
+  // proper sequencing in workload processing.
+  // This scheduling gate is alpha-level.
+  ElasticJobSchedulingGate = "kueue.x-k8s.io/elastic-job"
+)
+```
 
-After the creation of each individual Workload Slice and admission of a Workload Slice, the **workload_scheduling_gates_controller** should be in charge of removing the scheduling gates from each pod. All worker pods from the same worker group share the same pod template, so we only need to ungate the number of pods to match the number of admitted pods, this should be a counter. We don’t want to accidentally ungate too many pods since race conditions could happen and we also don’t want to double count. It's worth mentioning that for the case of recreated pods (i.e. machine failure for example), these pods will go through the admission/scheduling check again, Kueue is responsible fo removing the scheduling gates when there's available quota and resources to spend on the Job. 
+### Limitations and Incompatibilities
 
-### Garbage Collecting Workload Slices
+This section captures all known limitations and incompatibilities introduced by, or resulting from, enabling the ElasticJobs feature.
 
-The logic of folding and deleting would be isolated in this controller. We don’t necessarily have to say that this Workload Slice belongs to a specific pod. This controller will look at the Workload objects and check whether they have the Admitted condition or not.
+#### PartialAdmission
 
-1. The controller increments the `.status.admission.podSetAssignments.count` and passes the UID of the workload you are folding to the parent workload.
-If we still don’t see the workload being deleted, we at least know it has been counted towards the parent workload and the controller shouldn't fold it again.
+A given Job instance cannot have both `PartialAdmission` and `ElasticJob` enabled. This is a **per-Job limitation**, not a global feature-level conflict. It is entirely valid to have both features enabled in the system, just not simultaneously on the same Job.
 
-## Phases for MVP (alpha)
+**Rationale**
 
-### Phase 1 - Scale Down
-Scaling down will be the first phase towards MVP because it can be implemented without introducing the Workload Slices. 
+* `PartialAdmission` depends on the static nature of a workload. Specifically, it assumes, reasonably, given the current model, that workloads are immutable in terms of `podSets[].count`. Based on this assumption, it adjusts the Job’s spec to reflect the minimum allowed parallelism at scheduling time. This value is fixed and will not change, even if the ClusterQueue’s capacity increases later.
 
-Scaling down a Job won’t involve the creation of Workload Slices, instead it’ll involve an update to the current workload, no requeuing.
+* In practice, `PartialAdmission` takes ownership of `job.spec.parallelism`, and Kueue enforces this immutability through the admission webhook. For example:
 
-1. Compare Pod Counts: Within the job framework, check if the PodSet.Count of the job matches the Workload.Spec.PodSets[1].Count (worker group).
-2. Synchronize Workload: If the pod counts don't match, update the workload to align with the job's PodSet.Count.
-3. Trigger Resource Updates:  By updating the workload in cache and in queue, we'll signal the following:
-- The cluster queue resource usage should recalculate, ensuring accurate resource management.
-- The scheduler should re-evaluate and update PodSetAssignments related to the workload.
+  ```text
+  admission webhook "vjob.kb.io" denied the request: spec.parallelism: Forbidden: cannot change when partial admission is enabled and the job is not suspended
+  ```
 
+**Validation**
 
-#### Job controller
+To avoid delayed or implicit validation errors, any Kueue-integrated Job that supports `ElasticJobs` should include admission validation logic to reject the following annotation combination:
 
-Rework *equivalentToWorkload()* and *reconcile()* to account for potential differences between the job’s number of replicas and the running workload’s PodSet.Count. 
+```yaml
+annotations:
+  kueue.x-k8s.io/dynamically-sized-job: "true"
+  kueue.x-k8s.io/job-min-parallelism: "1"
+```
 
-Given these changes, check if the delta is positive or negative indicating a scaleup/scaledown. If it’s a scaledown the reconciler should trigger an update on the workload to match the new job’s spec and no quota needs to be checked or accounted for, the cluster queue should update the workload resource usage. 
+## Phases for MVP (alpha)
 
-**Important:** in Phase 1 if there's a scale up, the behaviour will be suspend and requeue. This will be temporary while Phase 2 and 3 are not completed.
+### Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.
 
-### Phase 2 - Aggregating Workload Slices
+Scaling up and down for `batch/v1.Job` will be the initial phase of the MVP, as it builds on stable and well-understood Kubernetes components without requiring additional integration work.
 
-In Phase 2, aggregating Workload Slices into the parent workload will be implemented. This doesn't represent a usable feature to end users, but it can be reviewed independently from the phase 3.
+#### Scale Down
 
-### Phase 3 - Scale up with Workload Slices and Scheduling Gates
+1. Create a Job with workload-slice enablement and a parallelism value greater than 1. 
+2. Observe that a corresponding Workload is created and admitted, and that the Job's pods are in the Running state, matching the specified parallelism. 
+3. Update the Job by reducing its parallelism value. 
+4. Confirm that the existing Workload is updated accordingly, with WorkloadSpec.PodSets[0].Count reflecting the new parallelism value. 
+5. Observe that the number of running pods is reduced, while the remaining pods continue to run uninterrupted.
 
-In Phase 3, scale up will be implemented by introducing Workload Slices and adding Pod scheduling gates as part of Kueue’s mutating admission for the job. 
+#### Scale Up
 
-When a job scales, its webhook would be modified to intercept and "gate" all pods. Every time there’s a resize, you create a dependable (child) workload slice and once it's admitted, you increase the count in the original workload, delete the old workload and remove the schedulingGates. 
-- Pros: You are able to hold the pods added by Kuberay
-- Cons: The fact of having schedulingGates, means we need an API call for every pod, because all pods that are created by the job are going to have schedulingGates. We need to remove those gates and for every pod you need to make API calls.
+1. Create a `batch/v1.Job` with workload-slice enablement and an initial `parallelism` value greater than 0.
+2. Verify that a corresponding `Workload` is created and admitted, and that the Job’s pods are in the `Running` state, matching the specified parallelism.
+3. Increase the Job’s `parallelism` value to trigger a scale-up event.
+4. Confirm that a new `Workload` slice is created and admitted, reflecting the increased pod count, while the previous slice is deactivated and marked as `Finished`.
```

  ```suggestion
  4. Confirm that a new `Workload` slice is created and admitted, reflecting the increased pod count, while the previous slice is marked as `Finished`.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T12:47:23Z

- **keps/77-dynamically-sized-jobs/README.md:406** by [@mimowo](https://github.com/mimowo) — 2025-07-18T12:47:22Z
```diff
@@ -209,18 +373,59 @@ extending the production code to implement this enhancement.
 -->
 The code will adhere to regular best practices for unit tests and coverage.
 
-
-
 #### Integration tests
+
 Integration tests will be executed against mocked clients for Jobs 
 that will provide predefined responses and allow to test various scenarios, 
 including situations like:
 
-* Job has a scale up, workload slices get created, pods are gated
-* Job has a scale up, gated pods are admitted, pods get ungated and assigned same flavors as parent workload
-* Workload slices are correctly folded and deleted
-* Job has a scale down, Workload spec reflects podset count values
-* When Kueue preempt a resized Job, it should preempt it as a whole
+#### Scale-down
+* Job scale down, cluster/local queue correctly reflect resource flavor used capacity.
+
+Here’s a revised and polished version of your text with improved grammar, structure, and clarity:
+
+---
+
+#### Integration Tests
+
+Integration tests will be executed against mocked clients for `batchv1/Job` resources. These mocks will simulate predefined responses, enabling validation of various scheduling and workload slice scenarios, including the following cases:
+
+---
+
+#### Scale-Down
+
+* A `Job` is scaled down.
+* The ClusterQueue and/or local queue correctly reflect the reduced resource usage for the assigned resource flavor.
+
+---
+
+#### Scale-Up
+
+* A `batchv1/Job` is updated to increase its `parallelism` value.
+* New pods are created in the `ScheduleGated` state.
+* A new `WorkloadSlice` is created and admitted.
+* The previous `WorkloadSlice` is deactivated and marked as "Finished."
```

  ```suggestion
  * The previous `WorkloadSlice` is marked as "Finished."
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T12:47:40Z

- **keps/77-dynamically-sized-jobs/README.md:459** by [@mimowo](https://github.com/mimowo) — 2025-07-18T12:47:39Z
```diff
@@ -239,10 +444,53 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
-The feature starts at the alpha level, with a feature gate.
+Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *Dynamically Sized Jobs*, tailored to your current implementation of `WorkloadSlice` and phased MVP plan.
+
+---
 
-In the Alpha version, Dynamically Sized Jobs will support RayCluster resizing. 
-- Scale up/down of replica workers
+### Graduation Criteria
+
+#### Alpha
+
+* [x] Feature is gated by the `ElasticJobsViaWorkloadSlices` feature gate.
+* [x] WorkloadSlice is implemented as an internal Kueue construct (no new CRDs).
+* [x] Scale-up events result in the creation of new `WorkloadSlice`s; scale-downs result in updates to the original `Workload`.
+* [x] Pod-level gating using `PodSchedulingGates` is integrated with slice admission.
+* [x] Sticky flavor behavior is enforced: scale-ups must reuse the original assigned flavor.
+* [x] Superseded slices are deactivated and marked as `Finished` once the new slice is admitted.
```

  ```suggestion
  * [x] Superseded slices are marked as `Finished` once the new slice is admitted.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T12:49:39Z

- **keps/77-dynamically-sized-jobs/README.md:473** by [@mimowo](https://github.com/mimowo) — 2025-07-18T12:49:38Z
```diff
@@ -239,10 +444,53 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
-The feature starts at the alpha level, with a feature gate.
+Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *Dynamically Sized Jobs*, tailored to your current implementation of `WorkloadSlice` and phased MVP plan.
+
+---
 
-In the Alpha version, Dynamically Sized Jobs will support RayCluster resizing. 
-- Scale up/down of replica workers
+### Graduation Criteria
+
+#### Alpha
+
+* [x] Feature is gated by the `ElasticJobsViaWorkloadSlices` feature gate.
+* [x] WorkloadSlice is implemented as an internal Kueue construct (no new CRDs).
+* [x] Scale-up events result in the creation of new `WorkloadSlice`s; scale-downs result in updates to the original `Workload`.
+* [x] Pod-level gating using `PodSchedulingGates` is integrated with slice admission.
+* [x] Sticky flavor behavior is enforced: scale-ups must reuse the original assigned flavor.
+* [x] Superseded slices are deactivated and marked as `Finished` once the new slice is admitted.
+* [x] Support for `batch/v1.Job` in both single-cluster and multi-cluster (multiKueue) configurations.
+* [x] Integration tests cover basic scale-up, scale-down, and resource flavor constraints.
+* [x] Feature opt-in is supported via workload annotation (`kueue.x-k8s.io/elastic-job: "true"`).
+* [x] Documented enablement steps, slice behavior, and known limitations.
+
+#### Beta
+
+* [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
+* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
+* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, deactivated, failed admission).
+* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
+* [ ] Formal conformance tests validate end-to-end behavior for:
+    * Horizontal scale-up and scale-down
+    * Slice transitions (preemption/deactivation)
```

  ```suggestion
      * Slice replacements
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T12:50:10Z

- **keps/77-dynamically-sized-jobs/README.md:469** by [@mimowo](https://github.com/mimowo) — 2025-07-18T12:50:10Z
```diff
@@ -239,10 +444,53 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
-The feature starts at the alpha level, with a feature gate.
+Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *Dynamically Sized Jobs*, tailored to your current implementation of `WorkloadSlice` and phased MVP plan.
+
+---
 
-In the Alpha version, Dynamically Sized Jobs will support RayCluster resizing. 
-- Scale up/down of replica workers
+### Graduation Criteria
+
+#### Alpha
+
+* [x] Feature is gated by the `ElasticJobsViaWorkloadSlices` feature gate.
+* [x] WorkloadSlice is implemented as an internal Kueue construct (no new CRDs).
+* [x] Scale-up events result in the creation of new `WorkloadSlice`s; scale-downs result in updates to the original `Workload`.
+* [x] Pod-level gating using `PodSchedulingGates` is integrated with slice admission.
+* [x] Sticky flavor behavior is enforced: scale-ups must reuse the original assigned flavor.
+* [x] Superseded slices are deactivated and marked as `Finished` once the new slice is admitted.
+* [x] Support for `batch/v1.Job` in both single-cluster and multi-cluster (multiKueue) configurations.
+* [x] Integration tests cover basic scale-up, scale-down, and resource flavor constraints.
+* [x] Feature opt-in is supported via workload annotation (`kueue.x-k8s.io/elastic-job: "true"`).
+* [x] Documented enablement steps, slice behavior, and known limitations.
+
+#### Beta
+
+* [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
+* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
+* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, deactivated, failed admission).
```

  ```suggestion
  * [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-18T14:42:55Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-18T14:42:55Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  I don't want to consider alternative solution for now.
  I just left a comment to revisit this annotation after the first release.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-18T14:50:35Z

- **keps/77-dynamically-sized-jobs/README.md:197** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-18T14:50:35Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  As I can see "Naming" section, it leaves example for workload name for before and after of this feature.
  My expectation is obviously to mention that the new Name generating algorithm contains the Job generation (.metadata.generation).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-18T14:54:53Z

- **keps/77-dynamically-sized-jobs/README.md:227** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-18T14:54:53Z
```diff
@@ -7,168 +7,332 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Preemption Disambiguation.](#preemption-disambiguation)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
+  - [Limitations and Incompatibilities](#limitations-and-incompatibilities)
+    - [PartialAdmission](#partialadmission)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kueue feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
+
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
+
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing 
+preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+While preemption and admission are not strictly atomic operations, they occur within the same scheduling cycle, specifically within the same closure of workload processing. Barring transient errors, for every candidate workload, preemption is immediately followed by admission. We could think of this as quasi-atomic behavior from a scheduling perspective.
+
+What’s important to emphasize is that while the workload object is preempted, the pods associated with the old workload are not. More importantly, those existing pods are effectively absorbed into the new workload.
+
+In the example at hand, preempting an old workload with 3 running pods and admitting a new workload requesting 10 pods (7 of which are pending and schedule-gated), the new workload will effectively consist of 3 running pods and 7 un-gated pods. This transition avoids disruption while maintaining quota integrity.
+
+##### Preemption Disambiguation.
+
+Here, we’re using the term “preemption” to describe the process of replacing an old workload slice with a new one. 
+At the same time, Kueue already uses the concept of workload preemption to mean evicting a workload and deferring its scheduling until capacity becomes available.
+
+In the context of workload slices, it may be more helpful to think of this replacement process as **aggregation** rather than preemption. 
+Aggregation involves terminating the old slice and activating the new one. In the strict sense, neither eviction nor preemption (as traditionally defined) actually occurs during this process.
+
+As such, we mark the old slice with the `Finished` condition. This ensures it is removed from the in-memory ClusterQueue snapshot, and its resource usage is released and attributed to the new slice that replaces it.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Finished status condition, including an appropriate reason and message.
+```yaml
+- type: Finished
+  status: True
+  reason: WorkloadSliceAggregation
```

  +1 on WorkloadSliceReplacement.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-18T14:57:59Z

- **keps/77-dynamically-sized-jobs/kep.yaml:17** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-18T14:56:13Z
```diff
@@ -2,22 +2,25 @@ title:
 kep-number: 77
 authors:
   - "@vicentefb"
+  - "@ichekrygin"
 status: provisional
 creation-date: 2024-03-14
 reviewers:
   - "@andrewsykim"
   - "@alculquicondor"
   - "@astefanutti"
   - "@tenzen-y"
+  - "@mimowo"
 approvers:
   - "@alculquicondor"
+  - "@mimowo"
 
```

  ```suggestion
    - "@tenzen-y"
  ```

- **keps/77-dynamically-sized-jobs/kep.yaml:26** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-18T14:56:30Z
```diff
@@ -2,22 +2,25 @@ title:
 kep-number: 77
 authors:
   - "@vicentefb"
+  - "@ichekrygin"
 status: provisional
 creation-date: 2024-03-14
 reviewers:
   - "@andrewsykim"
   - "@alculquicondor"
   - "@astefanutti"
   - "@tenzen-y"
+  - "@mimowo"
 approvers:
   - "@alculquicondor"
+  - "@mimowo"
 
 # The target maturity stage in the current dev cycle for this KEP.
 stage: alpha
 
 # The milestone at which this feature was, or is targeted to be, at each stage.
 milestone:
-  alpha: "v0.8"
+  alpha: "v0.13"
 
 feature-gates:
   - name: DynamicallySizedJobs
```

  Could you correct FG name?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T14:59:42Z

- **keps/77-dynamically-sized-jobs/README.md:484** by [@mimowo](https://github.com/mimowo) — 2025-07-18T14:59:42Z
```diff
@@ -239,10 +444,53 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
-The feature starts at the alpha level, with a feature gate.
+Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *Dynamically Sized Jobs*, tailored to your current implementation of `WorkloadSlice` and phased MVP plan.
+
+---
 
-In the Alpha version, Dynamically Sized Jobs will support RayCluster resizing. 
-- Scale up/down of replica workers
+### Graduation Criteria
+
+#### Alpha
+
+* [x] Feature is gated by the `ElasticJobsViaWorkloadSlices` feature gate.
+* [x] WorkloadSlice is implemented as an internal Kueue construct (no new CRDs).
+* [x] Scale-up events result in the creation of new `WorkloadSlice`s; scale-downs result in updates to the original `Workload`.
+* [x] Pod-level gating using `PodSchedulingGates` is integrated with slice admission.
+* [x] Sticky flavor behavior is enforced: scale-ups must reuse the original assigned flavor.
+* [x] Superseded slices are deactivated and marked as `Finished` once the new slice is admitted.
+* [x] Support for `batch/v1.Job` in both single-cluster and multi-cluster (multiKueue) configurations.
+* [x] Integration tests cover basic scale-up, scale-down, and resource flavor constraints.
+* [x] Feature opt-in is supported via workload annotation (`kueue.x-k8s.io/elastic-job: "true"`).
+* [x] Documented enablement steps, slice behavior, and known limitations.
+
+#### Beta
+
+* [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
+* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
+* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, deactivated, failed admission).
+* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
+* [ ] Formal conformance tests validate end-to-end behavior for:
+    * Horizontal scale-up and scale-down
+    * Slice transitions (preemption/deactivation)
+    * Sticky flavor enforcement
+    * Multi-cluster propagation
+* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
+* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
+* [ ] Slice preemption is consistently handled and visible in workload status conditions.
+* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
+* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
+* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
+* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
+
```

  add
  ```
  * [ ] Re-evaluate the approach for ungating Pods to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
  ```

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-21T15:34:56Z

- **keps/77-dynamically-sized-jobs/README.md:173** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-21T15:34:56Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
```

  Updated, PTAL and advice if this can be resolved (or resolve).

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-21T15:43:15Z

- **keps/77-dynamically-sized-jobs/README.md:227** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-21T15:43:15Z
```diff
@@ -7,168 +7,332 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Preemption Disambiguation.](#preemption-disambiguation)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
+  - [Limitations and Incompatibilities](#limitations-and-incompatibilities)
+    - [PartialAdmission](#partialadmission)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+### Enablement
+
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kueue feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
+
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
+```
+
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
+
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
+
+#### Scheduling and Preemption
+Kueue’s scheduler and preemptor components will be augmented to support workload slice processing, specifically by enforcing 
+preemption of the old workload slice regardless of queue capacity to make room for the new slice. 
+
+Scheduling assignment is based on pod count capacity, accounting for the existing workload slice's allocated pods. 
+For example, scaling a job up from `3` pods to `10` pods results in the creation of a new Workload representing all `10` pods; 
+however, the scheduling assignment will reflect only the `7` additional pods needed to admit the new slice, since `3` pods are already accounted for by the existing slice.
+
+While preemption and admission are not strictly atomic operations, they occur within the same scheduling cycle, specifically within the same closure of workload processing. Barring transient errors, for every candidate workload, preemption is immediately followed by admission. We could think of this as quasi-atomic behavior from a scheduling perspective.
+
+What’s important to emphasize is that while the workload object is preempted, the pods associated with the old workload are not. More importantly, those existing pods are effectively absorbed into the new workload.
+
+In the example at hand, preempting an old workload with 3 running pods and admitting a new workload requesting 10 pods (7 of which are pending and schedule-gated), the new workload will effectively consist of 3 running pods and 7 un-gated pods. This transition avoids disruption while maintaining quota integrity.
+
+##### Preemption Disambiguation.
+
+Here, we’re using the term “preemption” to describe the process of replacing an old workload slice with a new one. 
+At the same time, Kueue already uses the concept of workload preemption to mean evicting a workload and deferring its scheduling until capacity becomes available.
+
+In the context of workload slices, it may be more helpful to think of this replacement process as **aggregation** rather than preemption. 
+Aggregation involves terminating the old slice and activating the new one. In the strict sense, neither eviction nor preemption (as traditionally defined) actually occurs during this process.
+
+As such, we mark the old slice with the `Finished` condition. This ensures it is removed from the in-memory ClusterQueue snapshot, and its resource usage is released and attributed to the new slice that replaces it.
+
+A Workload that is preempted by a WorkloadSlice will be correctly marked with a Finished status condition, including an appropriate reason and message.
+```yaml
+- type: Finished
+  status: True
+  reason: WorkloadSliceAggregation
```

  Updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-21T15:51:57Z

- **keps/77-dynamically-sized-jobs/README.md:484** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-21T15:51:57Z
```diff
@@ -239,10 +444,53 @@ milestones with these graduation criteria:
 [deprecation-policy]: https://kubernetes.io/docs/reference/using-api/deprecation-policy/
 -->
 
-The feature starts at the alpha level, with a feature gate.
+Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *Dynamically Sized Jobs*, tailored to your current implementation of `WorkloadSlice` and phased MVP plan.
+
+---
 
-In the Alpha version, Dynamically Sized Jobs will support RayCluster resizing. 
-- Scale up/down of replica workers
+### Graduation Criteria
+
+#### Alpha
+
+* [x] Feature is gated by the `ElasticJobsViaWorkloadSlices` feature gate.
+* [x] WorkloadSlice is implemented as an internal Kueue construct (no new CRDs).
+* [x] Scale-up events result in the creation of new `WorkloadSlice`s; scale-downs result in updates to the original `Workload`.
+* [x] Pod-level gating using `PodSchedulingGates` is integrated with slice admission.
+* [x] Sticky flavor behavior is enforced: scale-ups must reuse the original assigned flavor.
+* [x] Superseded slices are deactivated and marked as `Finished` once the new slice is admitted.
+* [x] Support for `batch/v1.Job` in both single-cluster and multi-cluster (multiKueue) configurations.
+* [x] Integration tests cover basic scale-up, scale-down, and resource flavor constraints.
+* [x] Feature opt-in is supported via workload annotation (`kueue.x-k8s.io/elastic-job: "true"`).
+* [x] Documented enablement steps, slice behavior, and known limitations.
+
+#### Beta
+
+* [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
+* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
+* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, deactivated, failed admission).
+* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
+* [ ] Formal conformance tests validate end-to-end behavior for:
+    * Horizontal scale-up and scale-down
+    * Slice transitions (preemption/deactivation)
+    * Sticky flavor enforcement
+    * Multi-cluster propagation
+* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
+* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
+* [ ] Slice preemption is consistently handled and visible in workload status conditions.
+* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
+* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
+* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
+* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
+
```

  Added.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-07-21T15:52:21Z

- **keps/77-dynamically-sized-jobs/kep.yaml:26** by [@ichekrygin](https://github.com/ichekrygin) — 2025-07-21T15:52:21Z
```diff
@@ -2,22 +2,25 @@ title:
 kep-number: 77
 authors:
   - "@vicentefb"
+  - "@ichekrygin"
 status: provisional
 creation-date: 2024-03-14
 reviewers:
   - "@andrewsykim"
   - "@alculquicondor"
   - "@astefanutti"
   - "@tenzen-y"
+  - "@mimowo"
 approvers:
   - "@alculquicondor"
+  - "@mimowo"
 
 # The target maturity stage in the current dev cycle for this KEP.
 stage: alpha
 
 # The milestone at which this feature was, or is targeted to be, at each stage.
 milestone:
-  alpha: "v0.8"
+  alpha: "v0.13"
 
 feature-gates:
   - name: DynamicallySizedJobs
```

  Updated.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-22T16:48:55Z

- **keps/77-dynamically-sized-jobs/README.md:197** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-22T16:48:55Z
```diff
@@ -7,168 +7,290 @@
   - [Non-Goals](#non-goals)
 - [Proposal](#proposal)
   - [User Stories](#user-stories)
-    - [Story 1 - RayCluster w/ autoscaling](#story-1---raycluster-w-autoscaling)
+    - [Story 1 - batchv1/Job scale-down](#story-1---batchv1job-scale-down)
+    - [Story 2 - batchv1/Job scale-up](#story-2---batchv1job-scale-up)
+    - [Story 3 – batch/v1.Job in Multi-Cluster Configuration](#story-3--batchv1job-in-multi-cluster-configuration)
   - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
 - [Design Details](#design-details)
-  - [Workload Slices](#workload-slices)
-  - [Creating Workload Slices](#creating-workload-slices)
+  - [Enablement](#enablement)
+    - [Features](#features)
+    - [WorkloadSliceAnnotation](#workloadsliceannotation)
+  - [Processing of Workload Slices](#processing-of-workload-slices)
+    - [Creation](#creation)
+      - [Naming](#naming)
+    - [Scheduling and Preemption](#scheduling-and-preemption)
+      - [Resource Flavors](#resource-flavors)
+    - [Garbage Collection of Preempted Workload Slices](#garbage-collection-of-preempted-workload-slices)
   - [Pod Scheduling Gates](#pod-scheduling-gates)
-  - [Garbage Collecting Workload Slices](#garbage-collecting-workload-slices)
 - [Phases for MVP (alpha)](#phases-for-mvp-alpha)
-  - [Phase 1 - Scale Down](#phase-1---scale-down)
-    - [Job controller](#job-controller)
-  - [Phase 2 - Aggregating Workload Slices](#phase-2---aggregating-workload-slices)
-  - [Phase 3 - Scale up with Workload Slices and Scheduling Gates](#phase-3---scale-up-with-workload-slices-and-scheduling-gates)
-    - [Scheduler](#scheduler)
+  - [Phase 1 - batchv1/Job WorkloadSlices Support in Single-Cluster Configuration.](#phase-1---batchv1job-workloadslices-support-in-single-cluster-configuration)
+    - [Scale Down](#scale-down)
+    - [Scale Up](#scale-up)
+  - [Phase 2 – RayCluster WorkloadSlice Support in Single-Cluster Configuration](#phase-2--raycluster-workloadslice-support-in-single-cluster-configuration)
+  - [Phase 3 – Enabling Workload Slicing for batch/v1.Job in Multi-Cluster Configuration](#phase-3--enabling-workload-slicing-for-batchv1job-in-multi-cluster-configuration)
 - [Additional Details](#additional-details)
   - [Test Plan](#test-plan)
     - [Unit Tests](#unit-tests)
     - [Integration tests](#integration-tests)
+    - [Scale-down](#scale-down-1)
+    - [Scale-Down](#scale-down-2)
+    - [Resource Flavor Handling](#resource-flavor-handling)
   - [Graduation Criteria](#graduation-criteria)
-- [Implementation History](#implementation-history)
-- [Drawbacks](#drawbacks)
+  - [PodScheduling Readiness and ResourceQuota](#podscheduling-readiness-and-resourcequota)
 - [Alternatives](#alternatives)
-  - [Ignore Resize from Kuberay](#ignore-resize-from-kuberay)
 <!-- /toc -->
 
 ## Summary
 
-Enable dynamic sizing of Kueue jobs. For the MVP, we will only focus on supporting RayClusters with autoscaling enabled, but other resources that can benefit from dynamic sizing should be supported eventually.
+Enable dynamic resizing of Kueue-managed workloads by supporting in-place horizontal scaling of job parallelism.
+The primary goal is to allow dynamic adjustment of job parallelism without requiring suspension and requeueing, thereby improving scheduling efficiency and minimizing disruption during resource fluctuations.
 
-See: [Support dynamically sized (elastic) jobs #77](https://github.com/kubernetes-sigs/kueue/issues/77)
+In addition, we aim to extend dynamic resizing support to RayCluster workloads. 
+While the initial focus is on batch/v1.Job and RayCluster, the long-term objective is to generalize support for dynamically sized (elastic) workloads across other Kueue-integrated resource types.
 
 ## Motivation
 
-Kueue currently lacks support for resizing jobs. When a job is resized, Kueue will recreate the Workload representation of the job, leading to a disruptive suspend and requeue process. This limitation hinders the usability of Kueue for resources like RayCluster that sometimes have autoscaling capabilities enabled.
+Kueue currently lacks native support for resizing jobs. Any change in job size leads to the recreation of the associated Workload, resulting in job suspension and requeueing. 
+This disrupts execution and hinders usability for elastic workloads like RayCluster, which rely on in-place autoscaling. 
 
-To properly support RayCluster, Kueue needs to gracefully handle the scale up and scale down of RayCluster nodes. Concretely this means that scaling the resources used by a RayCluster is appropriately reflected in the respective ClusterQueue without needing to suspend the entire RayCluster.
+To support such scenarios, Kueue must gracefully handle horizontal scale-up and scale-down operations without disrupting admitted jobs or re-acquiring quota.
 
 ### Goals
 
-- Gracefully handle resize operations for Kueue jobs (i.e. update quota usage without suspend, enqueue scale ups)
-- Autoscaling RayCluster works with Kueue since it has an autoscaler and there is high demand (MVP)s
-
+- Gracefully handle resize operations for Kueue-managed jobs by updating quota usage without suspending the job or terminating running pods.
+  
 ### Non-Goals
 
-- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally 
-- Support resize for other Kueue jobs such as Job, JobSet, etc (future)
-- Resizing of RayJobs
-- Partial Preemption
+- Vertical scaling of workloads – Kueue will only handle resize operations that scale Pods horizontally.
+- Orchestration or initiation of resizing for the parent workload objects (e.g., `batch/v1.Job`, `RayJob`, etc.)
+- Partial Preemption.
 
 ## Proposal
 
 Update the Job framework reconciler and introduce new controllers to orchestrate dynamic resizing of jobs. We are only interested in horizontal scaling of jobs (e.g. scaling more replicas). At a high level, this will be accomplished by:
-- Creating Workload Slice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
-- Adding default scheduling gates to control the scheduling of new pods based on their admission status.
-- Dynamically adjust quotas in ClusterQueue based on scaling events.
+- Creating WorkloadSlice objects that represent incremental scale up of jobs. This gives us per-replica control of workload admission. Workload Slices will be garbage collected and consolidated with their parent workloads after successful admission.
+  - Adding default scheduling gates to control the scheduling of new pods based on their admission status.
+  - Dynamically adjust quotas in ClusterQueue based on scaling events.
 
-For the MVP, we will only focus on admission of **RayClusters** with autoscaling enabled.
+For the MVP, dynamic resizing will be available through an opt-in mechanism for selected, supported Kueue frameworks—starting with batch/v1.Job and, optionally, RayCluster workloads with autoscaling enabled. 
+Additionally, WorkloadSlice will be supported in the multiKueue context as part of the MVP scope.
 
 ### User Stories
 
-#### Story 1 - RayCluster w/ autoscaling
+#### Story 1 - batchv1/Job scale-down
+
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job's Pods. The Pods begin running as usual.
+3. The user updates the Job by reducing its parallelism, triggering a scale-down event:
+   1. The associated Workload is updated to reflect the new, lower pod count in its spec. 
+   2. The local queue’s capacity is updated accordingly to reflect the reduced resource usage. 
+   3. One running Job pod is terminated, while the remaining Pods continue running uninterrupted.
+
+#### Story 2 - batchv1/Job scale-up
+1. The user creates a `batch/v1.Job` with workload-slice enablement explicitly opted in.
+2. Kueue admits the Job based on its requested resources, creating the corresponding Workload and Job’s Pods. The Pods begin running as usual.
+3. The user updates the Job by increasing its parallelism, triggering a scale-up event:
+   1. A new WorkloadSlice is created to represent the additional pod capacity requested.
+   2. The new Pods are created in a gated state (via PodSchedulingGates) and held from scheduling until the slice is admitted. 
+   3. Kueue evaluates available resources; if sufficient, the slice is admitted and the scheduling gates on the new Pods are removed. 
+   4. The newly admitted Pods begin scheduling and running, resulting in the Job operating with the increased parallelism level.
 
-1. The user creates a RayCluster with `enableInTreeAutoscaling: true`.
-2. Kueue admits the RayCluster based on the requested resources in the head pod and worker pod. 
-3. Given the demand of the job, the Ray Autoscaler adjusts the replicas field as it adds or removes Pods from the cluster.
-4. Kueue will not suspend and requeue the RayCluster, instead it will dynamically update ClusterQueue usage based on the scaling event. 
+#### Story 3 – batch/v1.Job in Multi-Cluster Configuration
+
+This story mirrors the behaviors in Stories 1 and 2 but applies them within a multi-cluster (MultiKueue) setup, 
+where WorkloadSlices are propagated, admitted, and scheduled across multiple clusters.
 
 ### Notes/Constraints/Caveats (Optional)
 
-If Kueue needs to preempt the resized RayCluster, it would preempt it as a whole, regardless of whether the RayCluster was previously scaled up.
+If Kueue needs to preempt a resized Job, it will preempt the entire Job as a single unit, regardless of whether the Job has undergone a scale-up operation.
 
 ## Design Details
 
-### Workload Slices
-To support horizontal scaling of jobs, we will introduce the concept of a "Workload Slice”. A Workload Slice is a Workload object with an owner reference to the original Workload for a job. Workload Slices represent per-replica changes to a job that were not initially accounted for when the job was created. **It's an internal state, not an extra CRD.**
+To support horizontal scaling of Jobs, we will introduce the concept of a "WorkloadSlice". A WorkloadSlice is a Workload object with an owner reference to the original Workload for a Job. 
+Workload Slices represent per-replica changes to a Job that were not initially accounted for when the Job was created. **It's an internal state, not an extra CRD.**
 
-The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. Once a Workload Slice is admitted, it will be garbage collected and its resources will be aggregated into the admission status of the parent workload.
-- Workload Slices will be submitted to the same LocalQueue that's referenced by the top-level Workload. 
+The benefit of Workload Slices is that Kueue can evaluate admission on a per-replica basis without changing the existing semantics of the Workload API. 
+Once a WorkloadSlice is admitted, it will replace the original/old WorkloadSlice. 
+The latter will be deactivated and marked as finished.
+- Each WorkloadSlice inherits the resource flavor assignment of the original admitted slice, enforcing flavor consistency across scale-ups. 
 - In MultiKueue, Workload Slices would go into both clusters (management and workload) in a multi-cluster environment.
-- Workload Slices will be created by Kueue and use identical PodTemplates (which is already enforced by Kuberay in the case for RayCluster).
-- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted.
+- Workload Slices will be created by Kueue and use identical PodTemplate.
+- Workload Slices will belong to the same resource flavor as the top-level Workload that was initially admitted (for more details see "Resource Flavors" subsection)
+
+### Enablement
 
-The parent Workload should have a condition that reflects the scaling progression status. 
+WorkloadSlices in Kueue are enabled through a combination of a Kubernetes feature gate and an opt-in annotation on individual Workload objects. 
+At the cluster level, the WorkloadSlices feature must be enabled via the corresponding Kubernetes feature gate, which controls whether the controller logic for slicing is active. 
+
+Once the feature gate is enabled, individual Workload objects can opt into slicing by including the `kueue.x-k8s.io/elastic-job: "true"` annotation. 
+When both conditions are met, Kueue treats the Workload as eligible for partitioning into one or more WorkloadSlice objects, enabling fine-grained scheduling and 
+execution across multiple clusters or within a single cluster. If the feature gate is disabled or the annotation is omitted (or set to "false"), 
+the system defaults to the traditional single-Workload scheduling path for full backward compatibility.
+
+#### Features
+```golang
+// owner: @ichekrygin
+// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/77-dynamically-sized-jobs
+//
+// ElasticJobsViaWorkloadSlices enables support for horizontal-scale in jobs.
+ElasticJobsViaWorkloadSlices featuregate.Feature = "ElasticJobsViaWorkloadSlices"
+```
 
+#### WorkloadSliceAnnotation
 ```golang
+// Workload slicing can be enabled for a specific integration job instance,
+// provided that the integration supports workload slicing.
 const (
-  ...
-  ...
-	// WorkloadResizeRequested means that the Workload is in the process of scaling up or down
-	WorkloadResizeRequested = "ResizeRequested"
+  // EnabledAnnotationKey refers to the annotation key present on Job's that support
+  // workload slicing.
+  // This annotation is alpha-level.
+  EnabledAnnotationKey = "kueue.x-k8s.io/elastic-job"
+  // EnabledAnnotationValue refers to the annotation value. To enable
+  // workload slicing for a given job, we match both annotation key and value.
+  EnabledAnnotationValue = "true"
 )
-
 ```
 
-### Creating Workload Slices
+### Processing of Workload Slices
+
+#### Creation
+The creation of WorkloadSlices in Kueue is initiated when a Kueue Job with slicing enabled, via both annotation and feature gate, is admitted for execution. 
+Upon admission, the `jobframework.JobReconciler` evaluates the Job’s PodSets and resource requirements, then creates a new or additional (slice) Workload object 
+that represents a partition of the original workload. Each slice contains a `PodSet.Count` identical to the Job’s definition, along with scheduling metadata needed for handling preemption of the old workload (slice).
 
-The [GenericJob interface (7e778f5)](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobframework/interface.go#L30-L55) will be updated to handle resize operations of jobs.
+At most two active workload slices can exist for any single Job in Kueue: the current admitted slice and a newly created slice during a scaling operation. 
+This ensures a controlled transition between slices while maintaining consistency and avoiding resource contention and race conditions.
+
+To facilitate precise resource scheduling and effective workload preemption, every new job configured with slice enablement will be appropriately annotated using `EnabledAnnotationKey/Value` mentioned above.
+Furthermore, each individual workload slice will record the identity of any preempting slice it encounters.
 
 ```golang
-type GenericJob interface {
-  ...
-  ...
-  ResizeJob(wl *kueue.Workload) error
-}
+
+const (
+	// WorkloadPreemptibleSliceNameKey is the annotation key used to capture an "old" workload slice
+	// that will be preempted by the "new" workload slice, i.e., this annotates a "new" workload slice.
+	// This annotation is alpha-level.
+	WorkloadPreemptibleSliceNameKey = "kueue.x-k8s.io/workload-preemptible-slice"
+)
 ```
-Jobs implementing the ResizeJob method will create a Workload Slice for every new replica of a job. 
 
-On scale down to M we will change the original Workload's resources and then on scale up to N we will create N-M WorkloadSlice objects that go through admission and scheduling gate checks, i.e. the Workload can "lose" the original (first time admission) quota when scaling down before scaling up.
+##### Naming
+Currently, Kueue uses a deterministic Workload naming strategy by generating consistent names for the same Job instance via `GetWorkloadNameForOwnerWithGVK`.
+As a result, Workload names are not unique across different revisions of the same owner object (e.g., a Job), which makes this approach unsuitable for representing Workload slices.
+
+To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.
+For example, `bachv1/Job`: 
+```yaml
+annotations:
+  kueue.x-k8s.io/elastic-job: "true"
+creationTimestamp: "2025-06-12T21:32:41Z"
+generation: 1
+labels:
+  kueue.x-k8s.io/queue-name: demo
+name: demo-slice
+namespace: demo
+resourceVersion: "190175"
+uid: 78d1e61a-c399-4c09-800f-a0bbfb5fe71e
+```
 
-### Pod Scheduling Gates
+will result in a Workload name similar to: `job-demo-slice-75e0c`
+Whereas, changing `generation: 2`: will result in a different name similar to: `job-demo-slice-e7080`
```

  I chatted with @ichekrygin offline. 
  As a result, I found that I might have checked the outdated commit of this KEP.
  Especially, the following section has satisfied my expectations.
  
  > To address this, a new naming strategy will be introduced specifically for Workload slices, incorporating the owner object's generation value to ensure name uniqueness across revisions.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-22T16:49:33Z

Thank you for this awesome work!
/lgtm
/approve

/hold cancel
