# PR #5677: Documentation: Update describing two phase admission cycle using ProvisioningRequest

**Summary**: Documentation: Update describing two phase admission cycle using ProvisioningRequest

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5677

**Last updated**: 2025-08-04T06:42:16Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@JasminPradhan](https://github.com/JasminPradhan)
- **Branch**: `desadmissioncycle` → `main`
- **Created**: 2025-06-17T20:31:10Z
- **Updated**: 2025-08-04T06:42:16Z
- **Merged**: 2025-08-01T15:31:38Z
- **Closed**: 2025-08-01T15:31:38Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+78 / -5**

## Description

…rovisioningRequest

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation


#### What this PR does / why we need it:

This PR enhances the the [Admission Section](https://kueue.sigs.k8s.io/docs/concepts/#admission) in the Kueue official documentation by:
- Clarifying the two-step admission cycle,
- Adding detailed explanation of the ProvisioningRequest admission check
- Fixing a few broken or outdated links in the [here](site/content/en/docs/concepts/_index.md) for better navigation and reference.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Updates Admission section for #5172 

#### Special notes for your reviewer:
As my first contribution to Kueue, I’ve focused on:  
- Improving documentation clarity for the admission workflow  
- Adding technical examples 
- Ensuring accuracy in describing ProvisioningRequest integration  

Please feel free to suggest improvements and corrections—I’m eager to learn and refine this further!  
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
NONE
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-17T20:31:19Z

Welcome @JasminPradhan! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-17T20:31:20Z

Hi @JasminPradhan. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-17T20:31:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3c80991922d894a9d2cb470d961973f93babec47 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/687bb98bb46b190008e38ba0 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-5677--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-5677--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTU2NzctLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.XKMcvmZuzITmGHLKArbKCt7o3sWH7Hp--nMGuAav-04)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-5677--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-20T09:24:04Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-20T09:24:34Z

/release-note-edit
```release-note
NONE
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-20T09:25:15Z

/assign @PBundyra 
could you take a look as the issue author?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:15:59Z

Thanks for the PR @JasminPradhan! Can we update the PR description and use `Part of #5172` instead of `Fixes` - `fixes` closes the issue after the PR is merged, and I believe we should keep it open for follow-ups

### Comment by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-20T16:02:26Z

Thanks for the review and suggestions @PBundyra . I’ll make all the required changes as requested and update you soon!

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T09:59:39Z

Feel free to resolve my comments if you already addressed them

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-30T09:42:59Z

Hi @JasminPradhan!
Feel free to resolve any comments if you addressed them

### Comment by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-30T20:39:37Z

> Hi @JasminPradhan! Feel free to resolve any comments if you addressed them

Yes Sure!!

### Comment by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-07-02T05:26:55Z

Hi @PBundyra @tenzen-y ! Sorry for the delay. Lately I had a lot on my plate. I will push the changes soon!

### Comment by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-07-02T15:59:06Z

Hi! I've created a new page for Admission and moved the relevant content there, along with making the corrections we discussed earlier. I'm not entirely sure about the title, date, weight, and description fields—happy to update those based on your suggestions. Thanks again!

### Comment by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-07-15T17:17:15Z

@PBundyra @tenzen-y @mimowo

### Comment by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-07-29T08:38:48Z

@mimowo @PBundyra @tenzen-y Please review the changes

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T09:47:33Z

@JasminPradhan let me know if you have time to work on the remaining comment. Otherwise we can merge as is, and follow up. This is already substantial improvement.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T15:09:35Z

/lgtm
/approve
/cherrypick website
Merging as this is a great improvement already, @JasminPradhan feel free to address https://github.com/kubernetes-sigs/kueue/pull/5677#discussion_r2210126813 in a follow up

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T15:09:38Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5677#issuecomment-3144902856):

>/lgtm
>/approve
>/cherrypick website
>Merging as this is a great improvement already, @JasminPradhan feel free to address https://github.com/kubernetes-sigs/kueue/pull/5677#discussion_r2210126813 in a follow up


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-01T15:09:43Z

LGTM label has been added.  <details>Git tree hash: 060ed69afb9347345a9c17f1cd483c1406ec4e11</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-01T15:09:44Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5677#" title="Author self-approved">JasminPradhan</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5677#issuecomment-3144902856" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T15:32:17Z

@mimowo: new pull request created: #6367

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5677#issuecomment-3144902856):

>/lgtm
>/approve
>/cherrypick website
>Merging as this is a great improvement already, @JasminPradhan feel free to address https://github.com/kubernetes-sigs/kueue/pull/5677#discussion_r2210126813 in a follow up


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-08-03T14:58:06Z

Hey @mimowo ! Just saw this. I was away on a short 3 day trip and couldn’t get to it in time. Thanks so much for merging. 
I may not get to the follow-up right away, but I’ll keep it in mind going forward.
Thanks a lot to @PBundyra  @tenzen-y as well for all the reviews, feedback and guidance. Really appreciate it!!

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-04T06:42:16Z

@JasminPradhan thank you for the effort. PTAL: https://github.com/kubernetes-sigs/kueue/issues/6394

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:00:44Z

- **site/content/en/docs/concepts/_index.md:60** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:00:43Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
```

  Can you double check if this works? I'm afraid it doesnt in the preview website:
  
  https://deploy-preview-5677--kubernetes-sigs-kueue.netlify.app/docs/concepts/
  
  I think it's unnecessary to add `site/conent/en` prefix

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:00:58Z

- **site/content/en/docs/concepts/_index.md:72** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:00:58Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
```

  Same with this link

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:01:47Z

- **site/content/en/docs/concepts/_index.md:71** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:01:47Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
```

  ```suggestion
  - Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](docs/concepts/admission_check.md) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
  ```
  
  Could we add a hyperlink here?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:03:27Z

- **site/content/en/docs/concepts/_index.md:76** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:03:27Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
```

  ```suggestion
  Without AdmissionChecks or TopologyAwareScheduling, Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:04:00Z

- **site/content/en/docs/concepts/_index.md:78** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:04:00Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
+
+[ProvisioningRequest AdmissionCheck](keps/1136-provisioning-request-support/README.md) handles this issue by ensuring that the physical resources can be provisioned before admitting Workloads.
```

  Can we fix the link please

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:05:10Z

- **site/content/en/docs/concepts/_index.md:78** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:05:09Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
+
+[ProvisioningRequest AdmissionCheck](keps/1136-provisioning-request-support/README.md) handles this issue by ensuring that the physical resources can be provisioned before admitting Workloads.
```

  We have also docummented this specific AdmissionCheck here, so maybe we could reference it?
  https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:05:57Z

- **site/content/en/docs/concepts/_index.md:80** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:05:56Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
+
+[ProvisioningRequest AdmissionCheck](keps/1136-provisioning-request-support/README.md) handles this issue by ensuring that the physical resources can be provisioned before admitting Workloads.
+Kueue's enhanced admission process now consists of two sequential checks before admitting a workload:
+- Quota Validation: It uses Kueue's existing quota management system to check if the workload fits within available cluster quotas. It verifies "logical" resource availability. <br>
```

  ```suggestion
  - Quota Reservation: It uses Kueue's existing quota management system to check if the workload fits within available cluster quotas. It verifies "logical" resource availability. <br>
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:07:36Z

- **site/content/en/docs/concepts/_index.md:81** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:07:36Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
+
+[ProvisioningRequest AdmissionCheck](keps/1136-provisioning-request-support/README.md) handles this issue by ensuring that the physical resources can be provisioned before admitting Workloads.
+Kueue's enhanced admission process now consists of two sequential checks before admitting a workload:
+- Quota Validation: It uses Kueue's existing quota management system to check if the workload fits within available cluster quotas. It verifies "logical" resource availability. <br>
+- Capacity Guarantee: It uses Provisioning Request and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to ensure actual resources will exist. It verifies "physical" resource availability. <br> The Kueue controller creates a ProvisioningRequest object, attaches PodTemplates from the Workload, applies configuration from ProvisioningRequestConfig and sets owner reference to Workload. <br> Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status as `Provisioned=true` if capacity guaranteed, `Provisioned=false` if scaling is in progress, and `Failed=true` if cannot provision.
```

  Can we use bullet points to make reading potential ProvisioningRequest's state easier?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:08:41Z

- **site/content/en/docs/concepts/_index.md:87** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:08:40Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
+
+[ProvisioningRequest AdmissionCheck](keps/1136-provisioning-request-support/README.md) handles this issue by ensuring that the physical resources can be provisioned before admitting Workloads.
+Kueue's enhanced admission process now consists of two sequential checks before admitting a workload:
+- Quota Validation: It uses Kueue's existing quota management system to check if the workload fits within available cluster quotas. It verifies "logical" resource availability. <br>
+- Capacity Guarantee: It uses Provisioning Request and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to ensure actual resources will exist. It verifies "physical" resource availability. <br> The Kueue controller creates a ProvisioningRequest object, attaches PodTemplates from the Workload, applies configuration from ProvisioningRequestConfig and sets owner reference to Workload. <br> Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status as `Provisioned=true` if capacity guaranteed, `Provisioned=false` if scaling is in progress, and `Failed=true` if cannot provision.
+
+Lets understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job needing 16 GPUs*
+
+- Step 1 (Quota Check): ClusterQueue has 32 GPU quota available, since requested is quota available, 16 GPUs are reserved.
```

  ```suggestion
  - Step 1 (Quota Reservation): ClusterQueue has 32 GPU quota available, since requested is quota available, 16 GPUs are reserved.
  ```
  
  Can we be consistent not to confuse readers?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:09:12Z

- **site/content/en/docs/concepts/_index.md:89** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:09:12Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
+
+[ProvisioningRequest AdmissionCheck](keps/1136-provisioning-request-support/README.md) handles this issue by ensuring that the physical resources can be provisioned before admitting Workloads.
+Kueue's enhanced admission process now consists of two sequential checks before admitting a workload:
+- Quota Validation: It uses Kueue's existing quota management system to check if the workload fits within available cluster quotas. It verifies "logical" resource availability. <br>
+- Capacity Guarantee: It uses Provisioning Request and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to ensure actual resources will exist. It verifies "physical" resource availability. <br> The Kueue controller creates a ProvisioningRequest object, attaches PodTemplates from the Workload, applies configuration from ProvisioningRequestConfig and sets owner reference to Workload. <br> Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status as `Provisioned=true` if capacity guaranteed, `Provisioned=false` if scaling is in progress, and `Failed=true` if cannot provision.
+
+Lets understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job needing 16 GPUs*
+
+- Step 1 (Quota Check): ClusterQueue has 32 GPU quota available, since requested is quota available, 16 GPUs are reserved.
+
+- Step 2 (Capacity Check): A ProvisioningRequest is created for 16 GPUs. Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes ready.
```

  ```suggestion
  - Step 2 (Admission Check): Kueue creates a ProvisioningRequest requesting  16 GPUs. Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:11:13Z

- **site/content/en/docs/concepts/_index.md:97** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:11:13Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
+
+[ProvisioningRequest AdmissionCheck](keps/1136-provisioning-request-support/README.md) handles this issue by ensuring that the physical resources can be provisioned before admitting Workloads.
+Kueue's enhanced admission process now consists of two sequential checks before admitting a workload:
+- Quota Validation: It uses Kueue's existing quota management system to check if the workload fits within available cluster quotas. It verifies "logical" resource availability. <br>
+- Capacity Guarantee: It uses Provisioning Request and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to ensure actual resources will exist. It verifies "physical" resource availability. <br> The Kueue controller creates a ProvisioningRequest object, attaches PodTemplates from the Workload, applies configuration from ProvisioningRequestConfig and sets owner reference to Workload. <br> Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status as `Provisioned=true` if capacity guaranteed, `Provisioned=false` if scaling is in progress, and `Failed=true` if cannot provision.
+
+Lets understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job needing 16 GPUs*
+
+- Step 1 (Quota Check): ClusterQueue has 32 GPU quota available, since requested is quota available, 16 GPUs are reserved.
+
+- Step 2 (Capacity Check): A ProvisioningRequest is created for 16 GPUs. Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes ready.
+
+Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload
+
+Outcome:
+*Job starts immediately with all 16 GPUs available.*
+
+<h4>Failure Handling: </h4>
+If the capacity check fails, incase of temporary issues like cloud capacity shortages, exponential backoff retries is triggered while maintaining the workload's queue position and reserved quota. For permanent failures, the AdmissionCheck is marked Rejected, quota is released, and the workload is evicted, requiring user resubmission.
```

  Indeed holding reserved quota was what we did in the past, but we don't support it anymore. Currently if the admission check fails we release the quota and requeue the Workload

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-20T10:12:18Z

- **site/content/en/docs/concepts/_index.md:100** by [@PBundyra](https://github.com/PBundyra) — 2025-06-20T10:12:18Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
+
+[ProvisioningRequest AdmissionCheck](keps/1136-provisioning-request-support/README.md) handles this issue by ensuring that the physical resources can be provisioned before admitting Workloads.
+Kueue's enhanced admission process now consists of two sequential checks before admitting a workload:
+- Quota Validation: It uses Kueue's existing quota management system to check if the workload fits within available cluster quotas. It verifies "logical" resource availability. <br>
+- Capacity Guarantee: It uses Provisioning Request and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to ensure actual resources will exist. It verifies "physical" resource availability. <br> The Kueue controller creates a ProvisioningRequest object, attaches PodTemplates from the Workload, applies configuration from ProvisioningRequestConfig and sets owner reference to Workload. <br> Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status as `Provisioned=true` if capacity guaranteed, `Provisioned=false` if scaling is in progress, and `Failed=true` if cannot provision.
+
+Lets understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job needing 16 GPUs*
+
+- Step 1 (Quota Check): ClusterQueue has 32 GPU quota available, since requested is quota available, 16 GPUs are reserved.
+
+- Step 2 (Capacity Check): A ProvisioningRequest is created for 16 GPUs. Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes ready.
+
+Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload
+
+Outcome:
+*Job starts immediately with all 16 GPUs available.*
+
+<h4>Failure Handling: </h4>
+If the capacity check fails, incase of temporary issues like cloud capacity shortages, exponential backoff retries is triggered while maintaining the workload's queue position and reserved quota. For permanent failures, the AdmissionCheck is marked Rejected, quota is released, and the workload is evicted, requiring user resubmission.
+
+<!-- A Workload is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
+are `Ready`. -->
```

  Is this relevant? If no, can we remove it?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-20T11:02:23Z

- **site/content/en/docs/concepts/_index.md:78** by [@mimowo](https://github.com/mimowo) — 2025-06-20T11:02:23Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Earlier, Kueue admissions were mainly based on quota checks- if sufficient quota existed, the workload is admitted. However, just because the quota (logical resource) is available, doesn't mean there are enough actual(physical) compute resources in the cluster to schedule all pods successfully. This is critical in cloud environments where autoscaling provides nodes on-demand. 
+
+[ProvisioningRequest AdmissionCheck](keps/1136-provisioning-request-support/README.md) handles this issue by ensuring that the physical resources can be provisioned before admitting Workloads.
```

  Yes, it is much better to provide documentation to other doc pages. KEPs we only reference as "supplementary materials", or "see also".

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-21T03:51:04Z

- **site/content/en/docs/concepts/_index.md:72** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-21T03:51:04Z
```diff
@@ -57,16 +57,47 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/site/content/en/docs/concepts/cluster_queue.md#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all AdmissionChecks configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform certain validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](site/content/en/docs/concepts/admission_check.md) are mark `Ready`.
```

  @PBundyra should I remove Quota Reservation here as it's already mentioned in the Glossary section [here](https://kueue.sigs.k8s.io/docs/concepts/#quota-reservation) (above Admission) and directly start with the Admission Checks?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-23T08:20:10Z

- **site/content/en/docs/concepts/_index.md:71** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:20:09Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
```

  ```suggestion
  - Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable controllers that can perform validations such as policy checks, compliance, etc.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-23T08:21:03Z

- **site/content/en/docs/concepts/_index.md:76** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:21:03Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
```

  ```suggestion
  Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the Workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-23T08:22:35Z

- **site/content/en/docs/concepts/_index.md:85** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:22:35Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
```

  ```suggestion
      - `Provisioned=false`: Provisioning in progress
  ```
  
  ProvisioningRequest doesn't expose ETA yet so Kueue doesnt suface it

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-23T08:24:30Z

- **site/content/en/docs/concepts/_index.md:87** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:24:30Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
+    - `Failed=true`: Cannot provision
+    - `BookingExpired=true`: Treated same as `Failed=true`  
```

  This is only true if Workload has been not admitted yet, for Admitted Workloads the condition is ignored

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-23T08:31:19Z

- **site/content/en/docs/concepts/_index.md:111** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:31:19Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
+    - `Failed=true`: Cannot provision
+    - `BookingExpired=true`: Treated same as `Failed=true`  
+    - `CapacityRevoked=true`: Evicts workloads with retry support 
+
+Let's understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job requiring 16 GPUs*
+
+- Step 1 (Quota Reservation): ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+
+- Step 2 (Admission Check): Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
+  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+
+  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload
+
+Outcome:
+*Job starts immediately with all 16 GPUs available.*
+
+<h4> Failure Handling: </h4> 
+
+- If the admission check fails due to temporary issues (e.g., cloud capacity shortages), the system releases the reserved quota, requeues the workload, and triggers exponential backoff retries.
+Kueue creates new ProvisioningRequest with `-attempt<N>` suffix each retry.
+
+- For permanent failures the AdmissionCheck is marked `Rejected` and the workload is evicted, requiring user resubmission.
+
+{{% alert title="Note" color="primary" %}} The `KeepQuotaForProvReqRetry` feature gate (v0.9) temporarily preserved quota during retries, but current implementations ALWAYS release quota and requeue workloads on failure.{{% /alert %}}
```

  ```suggestion
  - For permanent failures the AdmissionCheck is marked `Rejected`, the Workload is evicted and the quota it reserved is released. The Workload gets deactivated and to requeue it, a user needs to set `.status.active` field to `true`.
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-23T08:31:37Z

- **site/content/en/docs/concepts/_index.md:111** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:31:36Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
+    - `Failed=true`: Cannot provision
+    - `BookingExpired=true`: Treated same as `Failed=true`  
+    - `CapacityRevoked=true`: Evicts workloads with retry support 
+
+Let's understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job requiring 16 GPUs*
+
+- Step 1 (Quota Reservation): ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+
+- Step 2 (Admission Check): Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
+  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+
+  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload
+
+Outcome:
+*Job starts immediately with all 16 GPUs available.*
+
+<h4> Failure Handling: </h4> 
+
+- If the admission check fails due to temporary issues (e.g., cloud capacity shortages), the system releases the reserved quota, requeues the workload, and triggers exponential backoff retries.
+Kueue creates new ProvisioningRequest with `-attempt<N>` suffix each retry.
+
+- For permanent failures the AdmissionCheck is marked `Rejected` and the workload is evicted, requiring user resubmission.
+
+{{% alert title="Note" color="primary" %}} The `KeepQuotaForProvReqRetry` feature gate (v0.9) temporarily preserved quota during retries, but current implementations ALWAYS release quota and requeue workloads on failure.{{% /alert %}}
```

  Since this is deprecated I think we can omit documenting that

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-23T08:32:28Z

- **site/content/en/docs/concepts/_index.md:106** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T08:32:28Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
+    - `Failed=true`: Cannot provision
+    - `BookingExpired=true`: Treated same as `Failed=true`  
+    - `CapacityRevoked=true`: Evicts workloads with retry support 
+
+Let's understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job requiring 16 GPUs*
+
+- Step 1 (Quota Reservation): ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+
+- Step 2 (Admission Check): Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
+  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+
+  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload
+
+Outcome:
+*Job starts immediately with all 16 GPUs available.*
+
+<h4> Failure Handling: </h4> 
+
+- If the admission check fails due to temporary issues (e.g., cloud capacity shortages), the system releases the reserved quota, requeues the workload, and triggers exponential backoff retries.
```

  Can we reference `retryStrategy` here? https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-23T09:35:49Z

- **site/content/en/docs/concepts/_index.md:87** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-23T09:35:49Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
+    - `Failed=true`: Cannot provision
+    - `BookingExpired=true`: Treated same as `Failed=true`  
```

  So should I just mention that condition when the status checks are required or remove them?

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-23T09:36:43Z

- **site/content/en/docs/concepts/_index.md:111** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-23T09:36:42Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
+    - `Failed=true`: Cannot provision
+    - `BookingExpired=true`: Treated same as `Failed=true`  
+    - `CapacityRevoked=true`: Evicts workloads with retry support 
+
+Let's understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job requiring 16 GPUs*
+
+- Step 1 (Quota Reservation): ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+
+- Step 2 (Admission Check): Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
+  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+
+  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload
+
+Outcome:
+*Job starts immediately with all 16 GPUs available.*
+
+<h4> Failure Handling: </h4> 
+
+- If the admission check fails due to temporary issues (e.g., cloud capacity shortages), the system releases the reserved quota, requeues the workload, and triggers exponential backoff retries.
+Kueue creates new ProvisioningRequest with `-attempt<N>` suffix each retry.
+
+- For permanent failures the AdmissionCheck is marked `Rejected` and the workload is evicted, requiring user resubmission.
+
+{{% alert title="Note" color="primary" %}} The `KeepQuotaForProvReqRetry` feature gate (v0.9) temporarily preserved quota during retries, but current implementations ALWAYS release quota and requeue workloads on failure.{{% /alert %}}
```

  Okay

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-23T09:37:50Z

- **site/content/en/docs/concepts/_index.md:106** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-23T09:37:50Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
+    - `Failed=true`: Cannot provision
+    - `BookingExpired=true`: Treated same as `Failed=true`  
+    - `CapacityRevoked=true`: Evicts workloads with retry support 
+
+Let's understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job requiring 16 GPUs*
+
+- Step 1 (Quota Reservation): ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+
+- Step 2 (Admission Check): Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
+  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+
+  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload
+
+Outcome:
+*Job starts immediately with all 16 GPUs available.*
+
+<h4> Failure Handling: </h4> 
+
+- If the admission check fails due to temporary issues (e.g., cloud capacity shortages), the system releases the reserved quota, requeues the workload, and triggers exponential backoff retries.
```

  Sure! Will do that.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-06-23T09:45:16Z

- **site/content/en/docs/concepts/_index.md:87** by [@PBundyra](https://github.com/PBundyra) — 2025-06-23T09:45:16Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
+    - `Failed=true`: Cannot provision
+    - `BookingExpired=true`: Treated same as `Failed=true`  
```

  I think it's useful to have the documentation about states of ProvisioningRequest, but let's make sure it doesn't confuse users. My proposal:
  
  ```
      - `Provisioned=false`: Provisioning in progress
      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
      - `Failed=true`: CA couldn't provision the capacity
      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it
      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
  ```

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-23T13:47:21Z

- **site/content/en/docs/concepts/_index.md:87** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-23T13:47:20Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable AdmissionChecks controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+- Quota Reservation: Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other workloads from claiming it. This step verifies logical resource availability. <br>
+- Capacity Guarantee: This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) to verify physical resource availability. 
+  - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
+  - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+    - `Provisioned=true`: Capacity guaranteed
+    - `Provisioned=false`: Scaling in progress (Kueue surfaces ETA via Kubernetes events)
+    - `Failed=true`: Cannot provision
+    - `BookingExpired=true`: Treated same as `Failed=true`  
```

  Got it!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T06:50:55Z

I left an early feedback.

- **site/content/en/docs/concepts/_index.md:67** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T06:26:36Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
```

  ```suggestion
  _Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-phase admission cycle for Workload scheduling: 
  ```
  
  Could you use the `two-phase` instead of `two-step` to keep consistency in codes and keps?

- **site/content/en/docs/concepts/_index.md:74** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T06:27:25Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this Workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
```

  ```suggestion
  #### Example: Provisioning AdmissionCheck
  ```
  
  Could you use the markdown style level annotation?

- **site/content/en/docs/concepts/_index.md:69** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T06:29:48Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
```

  Could you keep this minimal description and add an `Admission` dedicated page under the concept page?

- **site/content/en/docs/concepts/_index.md:69** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T06:37:59Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this Workload and other Workloads are prevented from using the same resources. 
```

  ```suggestion
  - Quota Reservation: When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue, which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and ResourceFlavors can accommodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents Workloads from using the same resources.
  ```
  
  1. It would be better to use active voice as much as possible.
  2. It would be better to mention just Kueue instead of Kueue scheduler.

- **site/content/en/docs/concepts/_index.md:71** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T06:41:47Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this Workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable controllers that can perform validations such as policy checks, compliance, etc.
```

  ```suggestion
  - Admission Checks: After the quota reservation, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable controllers that can perform validations such as policy checks, compliance, etc.
  ```

- **site/content/en/docs/concepts/_index.md:76** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T06:49:39Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this Workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
+
+Without AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Kueue admissions were mainly based on quota checks - if sufficient quota existed, the Workload was admitted. While quota reservation ensures logical resource availability, it doesn't guarantee physical resources exist to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cloud environments.
```

  ```suggestion
  When unconfigured AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/), Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admits the Workload. While quota reservation ensures logical resource availability, it doesn't guarantee that physical resources exist to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cluster-autoscaler deployment environments.
  ```
  
  I think this is not only for the cloud environment.

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-24T14:15:59Z

- **site/content/en/docs/concepts/_index.md:67** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-24T14:15:59Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
```

  okay

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-24T14:17:20Z

- **site/content/en/docs/concepts/_index.md:74** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-24T14:17:20Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this Workload and other Workloads are prevented from using the same resources. 
+
+- Admission Checks: After the quota is reserved, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue concurrently. These are pluggable controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+<h4> Example: Provisioning AdmissionCheck </h4>
```

  got it

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-24T14:23:35Z

- **site/content/en/docs/concepts/_index.md:69** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-24T14:23:35Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
```

  Alright @tenzen-y ! I will move the contents to a new page and add reference to that page. So if I make another dedicated page for admissions, then should I add more information like diagrams or code snippets?

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-24T14:24:12Z

- **site/content/en/docs/concepts/_index.md:69** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-24T14:24:12Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). Kueue uses a two-step admission cycle for Workload scheduling: 
+
+- Quota Reservation : When a Workload is submitted, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue scheduler checks if the resources (CPU, memory, GPUs, etc.) requested by the Workload can be satisfied using the targeted ClusterQueue's available quota and resource flavors. If the quota is available, the resources are reserved for this Workload and other Workloads are prevented from using the same resources. 
```

  Got it 👍🏽

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-26T17:59:02Z

- **site/content/en/docs/concepts/_index.md:69** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-26T17:59:02Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
```

  The diagrams and code snippets might be useful. However, in this PR, let us focus on just preparing describing page. You could learn what to look for on the concept page on the Preemption page style: https://kueue.sigs.k8s.io/docs/concepts/preemption/

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-06-26T19:51:41Z

- **site/content/en/docs/concepts/_index.md:69** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-06-26T19:51:41Z
```diff
@@ -57,16 +57,58 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
 ### Admission
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
```

  Got it👍

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-16T10:00:59Z

- **site/content/en/docs/concepts/_index.md:67** by [@mimowo](https://github.com/mimowo) — 2025-07-16T10:00:59Z
```diff
@@ -57,16 +57,14 @@ network throughput between the Pods.
 ### Quota Reservation
 
 _Quota reservation_ is the process during through which the kueue scheduler locks the resources needed by a workload within the targeted
-[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+[ClusterQueues ResourceGroups](/docs/concepts/cluster_queue#resource-groups)
 
 Quota reservation is sometimes referred to as _workload scheduling_ or _job scheduling_,
 but it should not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/).
 
-### Admission
+### [Admission](docs/concepts/admission/)
 
-_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload
-is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
-are `Ready`.
+_Admission_ is the process of allowing a Workload to start (Pods to be created). A Workload is admitted when it has a Quota Reservation and all its AdmissionCheckStates are `Ready`. 
```

  ```suggestion
  _Admission_ is the process of allowing a Workload to start (Pods to be created). 
  
  A Workload is admitted when:
  - it has the `QuotaReservation` condition
  - the physical Node capacity allows to run the Workload, when [Topology-Aware Scheduling](<link here>) is used
  - all ( optional) AdmissionChecks are in the `Ready` state. 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-16T10:02:52Z

- **site/content/en/docs/concepts/admission.md:6** by [@mimowo](https://github.com/mimowo) — 2025-07-16T10:02:52Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Admission"
+date: 
+weight: 
+description: >
+  Kueue's admission process where quota reservation meets physical capacity guarantees, using ProvisioningRequest for autoscaling integration.
```

  ```suggestion
    Kueue's admission process determines when a Job should be started.
  ```
  The current formulation suggests checking physical capacity, or `ProvisioningRequest` are necessary step.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-16T10:29:19Z

- **site/content/en/docs/concepts/admission.md:11** by [@mimowo](https://github.com/mimowo) — 2025-07-16T10:29:18Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Admission"
+date: 
+weight: 
+description: >
+  Kueue's admission process where quota reservation meets physical capacity guarantees, using ProvisioningRequest for autoscaling integration.
+---
+
+Kueue's admission process determines whether a Workload can begin execution. It involves verifying logical resource availability through quota reservation, followed by performing AdmissionChecks to validate physical resource provisioning and policy compliance. 
+
+Kueue implements this through a two-phase admission cycle: 
```

  ```suggestion
  Kueue's admission process determines whether a Workload can begin execution. 
  
  It involves verifying:
  - logical resource availability via quota reservation,
  - physical resource availability via Topology-Aware Scheduling, when used,
  - optional AdmissionChecks for additional admission guards. 
  
  Kueue implements this through a two-phase admission cycle: 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-16T11:35:42Z

- **site/content/en/docs/concepts/admission.md:13** by [@mimowo](https://github.com/mimowo) — 2025-07-16T11:35:42Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Admission"
+date: 
+weight: 
+description: >
+  Kueue's admission process where quota reservation meets physical capacity guarantees, using ProvisioningRequest for autoscaling integration.
+---
+
+Kueue's admission process determines whether a Workload can begin execution. It involves verifying logical resource availability through quota reservation, followed by performing AdmissionChecks to validate physical resource provisioning and policy compliance. 
+
+Kueue implements this through a two-phase admission cycle: 
+
+1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. 
```

  ```suggestion
  1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
  Topology-Aware Scheduling is enabled.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-16T11:37:39Z

- **site/content/en/docs/concepts/admission.md:15** by [@mimowo](https://github.com/mimowo) — 2025-07-16T11:37:38Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Admission"
+date: 
+weight: 
+description: >
+  Kueue's admission process where quota reservation meets physical capacity guarantees, using ProvisioningRequest for autoscaling integration.
+---
+
+Kueue's admission process determines whether a Workload can begin execution. It involves verifying logical resource availability through quota reservation, followed by performing AdmissionChecks to validate physical resource provisioning and policy compliance. 
+
+Kueue implements this through a two-phase admission cycle: 
+
+1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. 
+
+2. **Admission Checks:** After the quota reservation, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue, concurrently. These are the pluggable controllers that can perform validations such as policy checks, compliance, etc.
```

  ```suggestion
  2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. These can be either built-in like MultiKueue or ProvisioningRequest, or are the pluggable
  controllers that can perform validations such as policy checks, compliance, etc.
  ```
  Also, provide links to MultiKueue, ProvisioningRequest when mentioning the concepts.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-16T11:38:26Z

- **site/content/en/docs/concepts/admission.md:16** by [@mimowo](https://github.com/mimowo) — 2025-07-16T11:38:26Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Admission"
+date: 
+weight: 
+description: >
+  Kueue's admission process where quota reservation meets physical capacity guarantees, using ProvisioningRequest for autoscaling integration.
+---
+
+Kueue's admission process determines whether a Workload can begin execution. It involves verifying logical resource availability through quota reservation, followed by performing AdmissionChecks to validate physical resource provisioning and policy compliance. 
+
+Kueue implements this through a two-phase admission cycle: 
+
+1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. 
+
+2. **Admission Checks:** After the quota reservation, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue, concurrently. These are the pluggable controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
```

  ```suggestion
  The Workload is admitted once all [AdmissionChecks](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-16T11:40:03Z

- **site/content/en/docs/concepts/admission.md:25** by [@mimowo](https://github.com/mimowo) — 2025-07-16T11:40:03Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Admission"
+date: 
+weight: 
+description: >
+  Kueue's admission process where quota reservation meets physical capacity guarantees, using ProvisioningRequest for autoscaling integration.
+---
+
+Kueue's admission process determines whether a Workload can begin execution. It involves verifying logical resource availability through quota reservation, followed by performing AdmissionChecks to validate physical resource provisioning and policy compliance. 
+
+Kueue implements this through a two-phase admission cycle: 
+
+1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. 
+
+2. **Admission Checks:** After the quota reservation, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue, concurrently. These are the pluggable controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+## Provisioning AdmissionCheck 
```

  This is a special case, I think it deserves a dedicated page. I would suggest it is a subpage to [Admission Check](https://kueue.sigs.k8s.io/docs/concepts/admission_check/). Then, we could move the MultiKueue there in follow up. I would prefer the ProvisioningRequest AdmissionCheck and MultiKueue are at the same level of organization.

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-07-19T15:36:15Z

- **site/content/en/docs/concepts/admission.md:25** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-07-19T15:36:15Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Admission"
+date: 
+weight: 
+description: >
+  Kueue's admission process where quota reservation meets physical capacity guarantees, using ProvisioningRequest for autoscaling integration.
+---
+
+Kueue's admission process determines whether a Workload can begin execution. It involves verifying logical resource availability through quota reservation, followed by performing AdmissionChecks to validate physical resource provisioning and policy compliance. 
+
+Kueue implements this through a two-phase admission cycle: 
+
+1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. 
+
+2. **Admission Checks:** After the quota reservation, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue, concurrently. These are the pluggable controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+## Provisioning AdmissionCheck 
```

  Hey! I have made changes as per your feedback! I’d like to make sure I fully understand this suggestion of yours before proceeding.
  Specifically, I’m unsure what you mean by "ProvisioningRequest AdmissionCheck and MultiKueue are at the same level of organization." Are you suggesting that both should be separate subpages under Admission Check, or something else like moving the content under Admission Check? Any examples or a more detailed explanation would be greatly appreciated!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-29T14:39:09Z

- **site/content/en/docs/concepts/admission.md:25** by [@mimowo](https://github.com/mimowo) — 2025-07-29T14:39:09Z
```diff
@@ -0,0 +1,63 @@
+---
+title: "Admission"
+date: 
+weight: 
+description: >
+  Kueue's admission process where quota reservation meets physical capacity guarantees, using ProvisioningRequest for autoscaling integration.
+---
+
+Kueue's admission process determines whether a Workload can begin execution. It involves verifying logical resource availability through quota reservation, followed by performing AdmissionChecks to validate physical resource provisioning and policy compliance. 
+
+Kueue implements this through a two-phase admission cycle: 
+
+1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. 
+
+2. **Admission Checks:** After the quota reservation, Kueue executes all [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue, concurrently. These are the pluggable controllers that can perform validations such as policy checks, compliance, etc.
+These checks can be external or internal and determine if additional criteria are met before the Workload is admitted. The Workload is admitted once all its [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are marked `Ready`.
+
+## Provisioning AdmissionCheck 
```

  > Are you suggesting that both should be separate subpages under Admission Check, or something else like moving the content under Admission Check?
  
  Yes, exactly. MultiKueue and ProvisioningRequests are both examples of built-in AdmissionChecks. As such, I believe it it makes sense to treat them equally and put description of both as subpages to the main page describing the AdmissionCheck concept.
