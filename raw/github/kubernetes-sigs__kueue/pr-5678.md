# PR #5678: KEP: Reconcile jobs only in opt-in namespaces

**Summary**: KEP: Reconcile jobs only in opt-in namespaces

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5678

**Last updated**: 2025-07-08T18:07:29Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PannagaRao](https://github.com/PannagaRao)
- **Branch**: `kep-job-reconcile` → `main`
- **Created**: 2025-06-18T03:56:11Z
- **Updated**: 2025-07-08T18:07:29Z
- **Merged**: 2025-07-08T18:07:27Z
- **Closed**: 2025-07-08T18:07:27Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo)
- **Changed files**: 2   **+39 / -1**

## Description

#### What type of PR is this?
/kind bug
/kind documentation

#### What this PR does / why we need it:

This PR updates the KEP to reflect that Kueue will reconcile Jobs only in namespaces selected by managedJobsNamespaceSelector, ensuring consistent behavior across all supported integrations, including Pods, Deployments, and StatefulSets.

#### Which issue(s) this PR fixes:

Fixes # https://github.com/kubernetes-sigs/kueue/issues/5260

#### Special notes for your reviewer:

Once this change is implemented, namespaces will need to match managedJobsNamespaceSelector (e.g., carry the appropriate label) for their Jobs to be reconciled by Kueue—even if those Jobs specify a queue-name. This makes opting into Kueue management explicit for all integrations, including Jobs, and no longer optional by default.

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2025-06-18T03:56:15Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: PannagaRao / name: Pannaga Rao Bhoja Ramamanohara  (d47918c92e7eabade7e753fa700c28001cfa5f39)</li></ul><!-- Date Modified: 2025-07-08 17:49:56.088547 -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-18T03:56:19Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d47918c92e7eabade7e753fa700c28001cfa5f39 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/686d5a431f739500085de6ec |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-18T03:56:20Z

Hi @PannagaRao. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2025-06-18T19:52:47Z

/release-note none

### Comment by [@kannon92](https://github.com/kannon92) — 2025-06-18T19:53:04Z

/ok-to-test

### Comment by [@kannon92](https://github.com/kannon92) — 2025-06-18T19:53:39Z

cc @dgrove-oss @mimowo @tenzen-y 

Thank you for adding this KEP update.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-06-18T19:53:52Z

/release-note-none

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-20T12:02:06Z

/remove-kind bug
This is a KEP, so documentation.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-24T10:59:15Z

I'm planning to get to the PR just after 0.12.3 is released (aiming today)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-25T17:07:42Z

LGTM label has been added.  <details>Git tree hash: e37a21e86311f60618cec1a579d901abba6e8c25</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-25T17:11:02Z

> Fixes # https://github.com/kubernetes-sigs/kueue/issues/5260

Instead of Fixes please use prefix "Part of" or "KEP for", because "Fixes" is recognized by the robot which would auto-close the entire issue, which would not be done at this point.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-25T17:12:59Z

just to make it immediately clear this is KEP update just by title
/retitle KEP: Reconcile jobs only in opt-in namespaces

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-26T15:21:00Z

> I had some initial hestiatations around making the setup more difficult, but I'm ok with the KEP, because:
> 
> 1. it makes the handling consistent for Jobs and Pod-based integrations which is very nice feature
> 2. it allows to skip Jobs from excluded namespaces without using VAP to enforce that (which was my initial thought), but VAP still remains an option is someone wants.
> 3. it actually makes the code simpler because there is no dependency between `manageJobsWithoutQueueName` and `managedJobsNamespaceSelector` - they become independent parameters
> 
> /lgtm /approve /hold In case @tenzen-y would also like to add something.

ACK, landing here right now.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-27T10:00:04Z

> OTOH, the specification change is acceptable since this is still the beta feature. So, I would request an additional feature gate to this behavior change to make time for the exsting users. I'm wondering if we can introduce `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`.
> 
> @mimowo Any thoughts?

Indeed, since `manageJobsWithoutQueueName` is set as `false` by default, and `ManagedJobsNamespaceSelector` is set by default to:

```golang
	cfg.ManagedJobsNamespaceSelector = cmp.Or(cfg.ManagedJobsNamespaceSelector, &metav1.LabelSelector{
		MatchExpressions: []metav1.LabelSelectorRequirement{
			{
				Key:      corev1.LabelMetadataName,
				Operator: metav1.LabelSelectorOpNotIn,
				Values:   []string{"kube-system", *cfg.Namespace},
			},
		},
	})
```
So, it means that, because `ManagedJobsNamespaceSelector` was ignored, it meant it is possible to use Kueue for Jobs submitted to the kueue-system and kube-system namespaces by default. However, this capability will be lost. 

I think it is very unlikely anyone would rely on this, so I'm ok with the change, but we should have some "bailout" feature gate which will allow us to revert the behavior if it is problematic for some users. If we don't have any issues reported for 2 releases I'm ok to drop the feature gate. So +1 for ManagedJobsNamespaceSelectorForJobsWithoutQueueName.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-27T14:19:04Z

> > OTOH, the specification change is acceptable since this is still the beta feature. So, I would request an additional feature gate to this behavior change to make time for the exsting users. I'm wondering if we can introduce `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`.
> > @mimowo Any thoughts?
> 
> Indeed, since `manageJobsWithoutQueueName` is set as `false` by default, and `ManagedJobsNamespaceSelector` is set by default to:
> 
> ```go
> 	cfg.ManagedJobsNamespaceSelector = cmp.Or(cfg.ManagedJobsNamespaceSelector, &metav1.LabelSelector{
> 		MatchExpressions: []metav1.LabelSelectorRequirement{
> 			{
> 				Key:      corev1.LabelMetadataName,
> 				Operator: metav1.LabelSelectorOpNotIn,
> 				Values:   []string{"kube-system", *cfg.Namespace},
> 			},
> 		},
> 	})
> ```
> 
> So, it means that, because `ManagedJobsNamespaceSelector` was ignored, it meant it is possible to use Kueue for Jobs submitted to the kueue-system and kube-system namespaces by default. However, this capability will be lost.
> 
> I think it is very unlikely anyone would rely on this, so I'm ok with the change, but we should have some "bailout" feature gate which will allow us to revert the behavior if it is problematic for some users. If we don't have any issues reported for 2 releases I'm ok to drop the feature gate. So +1 for ManagedJobsNamespaceSelectorForJobsWithoutQueueName.

Yes, that's right. The "bailout" feature gate is what I wanted to say.
So, after a couple of releases, the "bailout" feature gate will removed, and proposed here the behavior is always applied to their cluster. This is just a mitigation to make migration period for the exsting feature enabled users.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-01T06:50:13Z

@PannagaRao please also mention the feature gate  ManagedJobsNamespaceSelectorForJobsWithoutQueueName

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-03T14:20:27Z

> @PannagaRao please also mention the feature gate ManagedJobsNamespaceSelectorForJobsWithoutQueueName

@PannagaRao Could you address that?

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2025-07-07T14:57:44Z

> > @PannagaRao please also mention the feature gate ManagedJobsNamespaceSelectorForJobsWithoutQueueName
> 
> @PannagaRao Could you address that?

I have mentioned the new feature gate. PTAL!

### Comment by [@kannon92](https://github.com/kannon92) — 2025-07-07T18:31:02Z

one nit on feature gate name but I don't think this needs to block feature.

https://github.com/kubernetes-sigs/kueue/pull/5678/files#r2190785449

Otherwise, this LGTM!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-07T18:31:10Z

LGTM label has been added.  <details>Git tree hash: 4e6f105180181aa3cd03f6d39fb6a8ad30db7d3d</details>

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2025-07-08T17:51:04Z

> Additionally, we should add new FG name to
> 
> https://github.com/kubernetes-sigs/kueue/blob/ede15f32bf4645cf71dd165655c6c567c4535785/keps/3589-manage-jobs-selectively/kep.yaml#L25-L27
> 
> as well.
> And, could you add `@tenzen-y` to
> 
> https://github.com/kubernetes-sigs/kueue/blob/ede15f32bf4645cf71dd165655c6c567c4535785/keps/3589-manage-jobs-selectively/kep.yaml#L11
> 
> ?

I have made the suggested changes. PTAL!

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-08T17:52:17Z

/lgtm
Leaving final approve to @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-08T17:52:23Z

LGTM label has been added.  <details>Git tree hash: b191e51d95e010a31b8e0fb64680f0c1fe530167</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-08T17:53:58Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5678#pullrequestreview-2958996324" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5678#" title="Author self-approved">PannagaRao</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5678#pullrequestreview-2998606057" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-08T17:54:01Z

/hold cancel

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-25T17:02:44Z

- **keps/3589-manage-jobs-selectively/README.md:113** by [@mimowo](https://github.com/mimowo) — 2025-06-25T17:02:43Z
```diff
@@ -80,12 +81,18 @@ We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
-1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
-exactly those instances of supported Kinds that have a `queue-name` label.
-2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
-that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
-have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
+`namespaceSelector` is used by the Pod integration.  
+We revise the behavior of the `managedJobsNamespaceSelector` configuration field 
+to not only govern reconciliation of jobs without a queue-name label, but also to enforce that only namespaces explicitly opted-in via `managedJobsNamespaceSelector` are managed by Kueue, 
+even when jobs are labeled with `kueue.x-k8s.io/queue-name`.
+Specifically,
+1. If a job's namespace does not match the `managedJobsNamespaceSelector`, 
+job will not be reconciled by Kueue — regardless of whether it has a queue-name label.
+2. If a job's namespace does match the selector and (a) manageJobsWithoutQueueName=false,
+ Kueue will manage exactly those instances of supported Kinds that have a queue-name label. (b) manageJobsWithoutQueueName=true, will manage all instances of supported Kinds with or without queue-name.
+
+This change brings the Job integration into consistent alignment with Pod, Deployment, 
```

  I see, I confirmed that for the Pod integration and configuring:
  ```yaml
        manageJobsWithoutQueueName: false
        managedJobsNamespaceSelector:
          matchExpressions:
            - key: kubernetes.io/metadata.name
              operator: NotIn
              values: [ kube-system, kueue-system ]
  ```
  and created a Pod inside the kueue-system namespace. The Pod was not managed by Kueue despite having the queue-name label,
  
  So, indeed, it would be nice to align the behavior one way or another.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-25T17:04:26Z

- **keps/3589-manage-jobs-selectively/README.md:141** by [@mimowo](https://github.com/mimowo) — 2025-06-25T17:04:26Z
```diff
@@ -110,6 +117,16 @@ managedJobsNamespaceSelector:
   matchLabels:
     kueue.x-k8s.io/managed-namespace: true
 ```
+#### Story 2
+
+Cluster admins want to opt in only selected namespaces to Kueue management. A user in a namespace not selected by managedJobsNamespaceSelector should not be able to submit a Kueue-managed workload by manually setting the queue-name label. This change ensures that only workloads in opted-in namespaces are ever reconciled, regardless of how they are labeled.
```

  My initial thought for the story was to just prevent creating the Jobs in the namespaces excluded by `managedJobsNamespaceSelector` with VAP if the Jobs have `queue-name` label. However, adding VAP for multiple Job types can be tedious and error prone. Also, the VAP-based approach is still ok even with this KEP.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-25T17:07:35Z

I had some initial hestiatations around making the setup more difficult, but I'm ok with the KEP, because:
1. it makes the handling consistent for Jobs and Pod-based integrations which is very nice feature
2. it allows to skip Jobs from excluded namespaces without using VAP to enforce that (which was my initial thought), but VAP still remains an option is someone wants.
3. it actually makes the code simpler because there is no dependency between `manageJobsWithoutQueueName` and `managedJobsNamespaceSelector` - they become independent parameters

/lgtm
/approve
/hold
In case @tenzen-y would also like to add something.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-26T16:10:17Z

I can fully understand strory 2 and basically agree with introducing this enhancemnt.
However, this change will break the exisiting user environment since this feature has already been shipped to users as enabled by default.

OTOH, the specification change is acceptable since this is still the beta feature.
So, I would request an additional feature gate to this behavior change to make time for the exsting users.
I'm wondering if we can introduce `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`.

@mimowo Any thoughts?

- **keps/3589-manage-jobs-selectively/README.md:88** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-26T15:47:01Z
```diff
@@ -80,12 +81,18 @@ We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
-1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
-exactly those instances of supported Kinds that have a `queue-name` label.
-2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
-that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
-have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
```

  Could you keep this one since this has already been released to the public space as a beta feature?

- **keps/3589-manage-jobs-selectively/README.md:98** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-26T15:57:46Z
```diff
@@ -80,12 +81,18 @@ We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
-1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
-exactly those instances of supported Kinds that have a `queue-name` label.
-2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
-that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
-have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
+`namespaceSelector` is used by the Pod integration.  
+We revise the behavior of the `managedJobsNamespaceSelector` configuration field 
+to not only govern reconciliation of jobs without a queue-name label, but also to enforce that only namespaces explicitly opted-in via `managedJobsNamespaceSelector` are managed by Kueue, 
+even when jobs are labeled with `kueue.x-k8s.io/queue-name`.
```

  ```suggestion
  ### Since Kueue v0.13
  
  The `managedJobsNamespaceSelector` configuration enforces that only namespaces explicitly opted in the field are managed by Kueue as opposed to governing reconciliation of a Job without a queue-name label, even when jobs are labeled with `kueue.x-k8s.io/queue-name`.
  ```

- **keps/3589-manage-jobs-selectively/README.md:83** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-26T15:59:56Z
```diff
@@ -80,12 +81,18 @@ We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
```

  ```
  ### Until v0.12
  
  We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
  to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
  ```

- **keps/3589-manage-jobs-selectively/README.md:139** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-26T16:03:33Z
```diff
@@ -110,6 +117,16 @@ managedJobsNamespaceSelector:
   matchLabels:
     kueue.x-k8s.io/managed-namespace: true
 ```
+#### Story 2
+
+Cluster admins want to opt in only selected namespaces to Kueue management. A user in a namespace not selected by managedJobsNamespaceSelector should not be able to submit a Kueue-managed workload by manually setting the queue-name label. This change ensures that only workloads in opted-in namespaces are ever reconciled, regardless of how they are labeled.
+
+Cluster admin has to label namespaces that are subject to management in order for the reconciliation to work.
+```yaml
+managedJobsNamespaceSelector:
+  matchLabels:
+    kueue.x-k8s.io/managed-namespace: true
```

  ```suggestion
      example.com/sample: true
  ```
  
  Do you indicate introducing this label? If not, could you avoid using the `kusus.x-k8s.io` domain?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-06-26T16:51:28Z

- **keps/3589-manage-jobs-selectively/README.md:139** by [@PannagaRao](https://github.com/PannagaRao) — 2025-06-26T16:51:28Z
```diff
@@ -110,6 +117,16 @@ managedJobsNamespaceSelector:
   matchLabels:
     kueue.x-k8s.io/managed-namespace: true
 ```
+#### Story 2
+
+Cluster admins want to opt in only selected namespaces to Kueue management. A user in a namespace not selected by managedJobsNamespaceSelector should not be able to submit a Kueue-managed workload by manually setting the queue-name label. This change ensures that only workloads in opted-in namespaces are ever reconciled, regardless of how they are labeled.
+
+Cluster admin has to label namespaces that are subject to management in order for the reconciliation to work.
+```yaml
+managedJobsNamespaceSelector:
+  matchLabels:
+    kueue.x-k8s.io/managed-namespace: true
```

  We would have to introduce a label indicating managed namespace. Do you suggest using sample value here and the actual one in implementation story?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-26T17:31:11Z

- **keps/3589-manage-jobs-selectively/README.md:139** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-26T17:31:11Z
```diff
@@ -110,6 +117,16 @@ managedJobsNamespaceSelector:
   matchLabels:
     kueue.x-k8s.io/managed-namespace: true
 ```
+#### Story 2
+
+Cluster admins want to opt in only selected namespaces to Kueue management. A user in a namespace not selected by managedJobsNamespaceSelector should not be able to submit a Kueue-managed workload by manually setting the queue-name label. This change ensures that only workloads in opted-in namespaces are ever reconciled, regardless of how they are labeled.
+
+Cluster admin has to label namespaces that are subject to management in order for the reconciliation to work.
+```yaml
+managedJobsNamespaceSelector:
+  matchLabels:
+    kueue.x-k8s.io/managed-namespace: true
```

  Does that mean the ManagedJobsNamespaceSelector introduces the `kueue.x-k8s.io/managed-namespace` label?
  Why do we need to introduce the dedicated static label, `kueue.x-k8s.io/managed-namespace`?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-26T17:32:41Z

- **keps/3589-manage-jobs-selectively/README.md:139** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-26T17:32:40Z
```diff
@@ -110,6 +117,16 @@ managedJobsNamespaceSelector:
   matchLabels:
     kueue.x-k8s.io/managed-namespace: true
 ```
+#### Story 2
+
+Cluster admins want to opt in only selected namespaces to Kueue management. A user in a namespace not selected by managedJobsNamespaceSelector should not be able to submit a Kueue-managed workload by manually setting the queue-name label. This change ensures that only workloads in opted-in namespaces are ever reconciled, regardless of how they are labeled.
+
+Cluster admin has to label namespaces that are subject to management in order for the reconciliation to work.
+```yaml
+managedJobsNamespaceSelector:
+  matchLabels:
+    kueue.x-k8s.io/managed-namespace: true
```

  In other words, we should allow cluster admins to use an arbitrary label for the ManageJobNamespaceSelector feature.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-06-30T18:42:53Z

- **keps/3589-manage-jobs-selectively/README.md:139** by [@PannagaRao](https://github.com/PannagaRao) — 2025-06-30T18:42:53Z
```diff
@@ -110,6 +117,16 @@ managedJobsNamespaceSelector:
   matchLabels:
     kueue.x-k8s.io/managed-namespace: true
 ```
+#### Story 2
+
+Cluster admins want to opt in only selected namespaces to Kueue management. A user in a namespace not selected by managedJobsNamespaceSelector should not be able to submit a Kueue-managed workload by manually setting the queue-name label. This change ensures that only workloads in opted-in namespaces are ever reconciled, regardless of how they are labeled.
+
+Cluster admin has to label namespaces that are subject to management in order for the reconciliation to work.
+```yaml
+managedJobsNamespaceSelector:
+  matchLabels:
+    kueue.x-k8s.io/managed-namespace: true
```

  Right. That makes sense. But for writing tests to verify the behavior we will need to use a label

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-06-30T18:52:18Z

- **keps/3589-manage-jobs-selectively/README.md:88** by [@PannagaRao](https://github.com/PannagaRao) — 2025-06-30T18:52:18Z
```diff
@@ -80,12 +81,18 @@ We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
-1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
-exactly those instances of supported Kinds that have a `queue-name` label.
-2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
-that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
-have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
```

  Yes, I have revised the PR to keep the older version as well

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-06-30T18:54:13Z

- **keps/3589-manage-jobs-selectively/README.md:98** by [@PannagaRao](https://github.com/PannagaRao) — 2025-06-30T18:54:13Z
```diff
@@ -80,12 +81,18 @@ We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
-1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
-exactly those instances of supported Kinds that have a `queue-name` label.
-2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
-that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
-have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
+`namespaceSelector` is used by the Pod integration.  
+We revise the behavior of the `managedJobsNamespaceSelector` configuration field 
+to not only govern reconciliation of jobs without a queue-name label, but also to enforce that only namespaces explicitly opted-in via `managedJobsNamespaceSelector` are managed by Kueue, 
+even when jobs are labeled with `kueue.x-k8s.io/queue-name`.
```

  I have added a Since Kueue v0.13 line. PTAL!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-01T06:49:30Z

- **keps/3589-manage-jobs-selectively/README.md:139** by [@mimowo](https://github.com/mimowo) — 2025-07-01T06:49:30Z
```diff
@@ -110,6 +117,16 @@ managedJobsNamespaceSelector:
   matchLabels:
     kueue.x-k8s.io/managed-namespace: true
 ```
+#### Story 2
+
+Cluster admins want to opt in only selected namespaces to Kueue management. A user in a namespace not selected by managedJobsNamespaceSelector should not be able to submit a Kueue-managed workload by manually setting the queue-name label. This change ensures that only workloads in opted-in namespaces are ever reconciled, regardless of how they are labeled.
+
+Cluster admin has to label namespaces that are subject to management in order for the reconciliation to work.
+```yaml
+managedJobsNamespaceSelector:
+  matchLabels:
+    kueue.x-k8s.io/managed-namespace: true
```

  I'm ok with the label for tests, but I support @tenzen-y suggestion not put it in the `kueue.x-k8s.io` domain. 
  
  I think we could consider standardizing such a label to be natively supported by Kueue, but I would keep it out of scope for the KEP.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-01T14:29:03Z

- **keps/3589-manage-jobs-selectively/README.md:139** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T14:29:03Z
```diff
@@ -110,6 +117,16 @@ managedJobsNamespaceSelector:
   matchLabels:
     kueue.x-k8s.io/managed-namespace: true
 ```
+#### Story 2
+
+Cluster admins want to opt in only selected namespaces to Kueue management. A user in a namespace not selected by managedJobsNamespaceSelector should not be able to submit a Kueue-managed workload by manually setting the queue-name label. This change ensures that only workloads in opted-in namespaces are ever reconciled, regardless of how they are labeled.
+
+Cluster admin has to label namespaces that are subject to management in order for the reconciliation to work.
+```yaml
+managedJobsNamespaceSelector:
+  matchLabels:
+    kueue.x-k8s.io/managed-namespace: true
```

  > Right. That makes sense. But for writing tests to verify the behavior we will need to use a label
  
  That is not my point. My point is using `kueue.x-k8s.io` domain indicates that Kueue upstream repository delivers the dedicated functionality for the label (`kueue.x-k8s.io/managed-namespace`).
  
  So, we should use another label format like `example.com/sample`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-01T14:30:49Z

- **keps/3589-manage-jobs-selectively/README.md:83** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T14:30:16Z
```diff
@@ -79,14 +80,29 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+Until v0.12
```

  ```suggestion
  ### Until v0.12
  ```
  
  Let us explicitly specify the section.

- **keps/3589-manage-jobs-selectively/README.md:93** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T14:30:25Z
```diff
@@ -79,14 +80,29 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+Since Kueue v0.13
```

  ```suggestion
  ### Since Kueue v0.13
  ```

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-07-02T18:06:12Z

- **keps/3589-manage-jobs-selectively/README.md:83** by [@PannagaRao](https://github.com/PannagaRao) — 2025-07-02T18:06:12Z
```diff
@@ -79,14 +80,29 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+Until v0.12
```

  Done

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-07-02T18:06:19Z

- **keps/3589-manage-jobs-selectively/README.md:93** by [@PannagaRao](https://github.com/PannagaRao) — 2025-07-02T18:06:19Z
```diff
@@ -79,14 +80,29 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+Since Kueue v0.13
```

  Done

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-07T18:29:45Z

- **keps/3589-manage-jobs-selectively/README.md:97** by [@kannon92](https://github.com/kannon92) — 2025-07-07T18:29:45Z
```diff
@@ -79,14 +82,37 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+### Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+### Since Kueue v0.13
+
+We introduce a feature gate `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`. 
```

  From what I understand, the behavior from v0.12 and below was that ManagedJobsNamespaceSelector always worked when `managedJobsWithoutQueueName = true.
  
  In your case, you are trying to make ManagedJobsNamespaceSelector work for both cases.
  
  So I would prefer a name like:
  
  ```suggestion
  We introduce a feature gate `ManagedJobsNamespaceSelectorForJobs`. 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-07T18:53:25Z

- **keps/3589-manage-jobs-selectively/README.md:97** by [@mimowo](https://github.com/mimowo) — 2025-07-07T18:53:25Z
```diff
@@ -79,14 +82,37 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+### Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+### Since Kueue v0.13
+
+We introduce a feature gate `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`. 
```

  +1 this approach sounds even better to me
  
  We can start with that feature gate as beta and graduate it to GA after 2 releases or so. 
  
  cc @tenzen-y, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T09:40:58Z

- **keps/3589-manage-jobs-selectively/README.md:97** by [@mimowo](https://github.com/mimowo) — 2025-07-08T09:40:58Z
```diff
@@ -79,14 +82,37 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+### Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+### Since Kueue v0.13
+
+We introduce a feature gate `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`. 
```

  I take it back, the proposed name `ManagedJobsNamespaceSelectorForJobs` is clear either.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T09:42:00Z

- **keps/3589-manage-jobs-selectively/README.md:97** by [@mimowo](https://github.com/mimowo) — 2025-07-08T09:42:00Z
```diff
@@ -79,14 +82,37 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+### Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+### Since Kueue v0.13
+
+We introduce a feature gate `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`. 
```

  What we really mean is `RespectManagedJobsNamespaceSelectorWhenManageJobsWithoutQueueNameIsFalse`, but this is ridiculously long :)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-08T09:43:48Z

- **keps/3589-manage-jobs-selectively/README.md:97** by [@mimowo](https://github.com/mimowo) — 2025-07-08T09:43:48Z
```diff
@@ -79,14 +82,37 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+### Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+### Since Kueue v0.13
+
+We introduce a feature gate `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`. 
```

  As mentioned in the comment https://github.com/kubernetes-sigs/kueue/pull/5678#discussion_r2190785449 it is now respected in "both cases", meaning always. So Maybe `ManagedJobsNamespaceSelectorAlwaysRespected` (so that it also has the common prefix with the other feature gate for discoverability). 
  
   wdyt @kannon92 , @tenzen-y ?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-08T11:40:26Z

- **keps/3589-manage-jobs-selectively/README.md:97** by [@kannon92](https://github.com/kannon92) — 2025-07-08T11:40:26Z
```diff
@@ -79,14 +82,37 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+### Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+### Since Kueue v0.13
+
+We introduce a feature gate `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`. 
```

  That works for me.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-08T15:08:33Z

- **keps/3589-manage-jobs-selectively/README.md:97** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-08T15:08:33Z
```diff
@@ -79,14 +82,37 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+### Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+### Since Kueue v0.13
+
+We introduce a feature gate `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`. 
```

  SGTM on `ManagedJobsNamespaceSelectorAlwaysRespected `. Thank you for proposing better name.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-08T15:10:06Z

Additionally, we should add new FG name to https://github.com/kubernetes-sigs/kueue/blob/ede15f32bf4645cf71dd165655c6c567c4535785/keps/3589-manage-jobs-selectively/kep.yaml#L25-L27 as well.

And, could you add `@tenzen-y` to https://github.com/kubernetes-sigs/kueue/blob/ede15f32bf4645cf71dd165655c6c567c4535785/keps/3589-manage-jobs-selectively/kep.yaml#L11 ?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2025-07-08T17:50:32Z

- **keps/3589-manage-jobs-selectively/README.md:97** by [@PannagaRao](https://github.com/PannagaRao) — 2025-07-08T17:50:32Z
```diff
@@ -79,14 +82,37 @@ Consider any scope for filtering the processing of Jobs submitted without a
 We add a `managedJobsNamespaceSelector` of type `*metav1.LabelSelector`
 to the top level of the Kueue `Configuration` struct (same level as `manageJobsWithoutQueueName`).
 
+### Until v0.12
+
 We use this new configuration for all integrations in the same way that the current
-`namespaceSelector` is used by the Pod integration.  Specifically,
+`namespaceSelector` is used by the Pod integration. Specifically,
 1. If `manageJobsWithoutQueueName` is false, `managedJobsNamespaceSelector` has no effect: Kueue will manage
 exactly those instances of supported Kinds that have a `queue-name` label.
 2. If `manageJobsWithoutQueueName` is true, then Kueue will (a) manage all instances of supported Kinds
 that have a `queue-name` label and (b) will manage all instances of supported Kinds that do not
 have a `queue-name` label if they are in namespaces that match `managedJobsNamespaceSelector`.
 
+### Since Kueue v0.13
+
+We introduce a feature gate `ManagedJobsNamespaceSelectorForJobsWithoutQueueName`. 
```

  I have updated the name of new FG. PTAL!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-08T17:53:51Z

Thank you!
/lgtm
/approve
