# PR #5698: Improve accuracy of flavor assignment using preemption simulation in more cases

**Summary**: Improve accuracy of flavor assignment using preemption simulation in more cases

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5698

**Last updated**: 2025-09-25T08:33:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@gabesaba](https://github.com/gabesaba)
- **Branch**: `no_candidates_found` → `main`
- **Created**: 2025-06-20T14:28:17Z
- **Updated**: 2025-09-25T08:33:47Z
- **Merged**: 2025-06-20T15:40:52Z
- **Closed**: 2025-06-20T15:40:52Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+105 / -68**

## Description

#### What type of PR is this?
/kind cleanup

#### What this PR does / why we need it:
We use the simulation preemption results for more accurate flavor assignment in the case when `val <= rQuota.Nominal || mayReclaimInHierarchy`. This allows upgrading to a flavor where preemption is possible.

Contributes to #4135

#### Which issue(s) this PR fixes:


#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
Use simulation of preemption for more accurate flavor assignment. 
In particular, the previous heuristic would wrongly state that preemption
in a flavor was possible even if no preemption candidates could be found. 

Additionally, in scenarios when preemption while borrowing is enabled,
the flavor in which reclaim is possible is preferred over flavor where 
priority-based preemption is required. This is consistent with prioritizing 
flavors when preemption without borrowing is used.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-20T14:28:24Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | be262a974ec9e07815133cc60ead2d28e5695481 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6855700355242500083a7812 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-5698--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-5698--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTU2OTgtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.5dXXW490ebjymKVhvY3GONx4CiRWD_116O-0MzakTtc)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-5698--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-20T15:39:24Z

/lgtm
/approve
The nits are non-blocking, I know that @gabesaba is ooo next week, so I prefer to merge and we can adjust in a follow up.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-20T15:39:30Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5698#" title="Author self-approved">gabesaba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5698#issuecomment-2992093562" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/scheduler/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/OWNERS)~~ [gabesaba,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-20T15:39:30Z

LGTM label has been added.  <details>Git tree hash: e2a00d45fb6d6a6f0a849facac3127118425d501</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-23T12:57:21Z

Since this PR introduces a user-facing change I wouldn't call it cleanup, but more of a bugfix for rare scenario.

I'm going to look a bit more to understand the consequences and determine if we want to cherry-pick this.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-24T07:05:24Z

I have investigated the PR in more depth after some local play. I'm working on a follow up PR to add tests, because in the current for the new tests pass also on the previous implementation.

After the analysis this is not just a cleanup because it has user-facing consequences. It also does not feel like a new feature. I prefer to categorize it as a bugfix, as it improves flavor assignment under some circumstances. 

The PR actually makes two changes worth a release note, let me reflect:

/release-note-edit
```release-note
Use simulation of preemption for more accurate flavor assignment. 
In particular, the previous heuristic would wrongly state that preemption
in a flavor was possible even if no preemption candidates could be found. 

Additionally, in scenarios when preemption while borrowing is enabled,
the flavor in which reclaim is possible is preferred over flavor where 
priority-based preemption is required. This is consistent with prioritizing 
flavors when preemption without borrowing is used. 
```

/remove-kind cleanup
/kind bug

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-24T07:57:44Z

/retitle Improve accuracy of flavor assignment using preemption simulation in more cases

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-06-20T14:32:04Z

- **pkg/scheduler/scheduler_test.go:3807** by [@gabesaba](https://github.com/gabesaba) — 2025-06-20T14:31:11Z
```diff
@@ -3787,7 +3804,7 @@ func TestLastSchedulingContext(t *testing.T) {
 						*utiltesting.MakeFlavorQuotas("on-demand").
 							Resource(corev1.ResourceCPU, "50", "50").Obj(),
 						*utiltesting.MakeFlavorQuotas("spot").
-							Resource(corev1.ResourceCPU, "100", "0").Obj(),
+							Resource(corev1.ResourceCPU, "0", "0").Obj(),
```

  Otherwise, with new logic, we schedule in this flavor since we found out that there are no preemption targets in `on-demand`, and try `spot` within the same scheduling cycle

- **pkg/scheduler/scheduler_test.go:3735** by [@gabesaba](https://github.com/gabesaba) — 2025-06-20T14:32:03Z
```diff
@@ -3732,46 +3789,6 @@ func TestLastSchedulingContext(t *testing.T) {
 		wantAdmissionsOnFirstSchedule  map[workload.Reference]kueue.Admission
 		wantAdmissionsOnSecondSchedule map[workload.Reference]kueue.Admission
 	}{
-		{
```

  we find the correct assignment in the first scheduling cycle; this test is no longer relevant. "can't preempt" in the test name was the case where we assigned Preempt mode, but there were no targets.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-20T14:34:46Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:770** by [@mimowo](https://github.com/mimowo) — 2025-06-20T14:34:46Z
```diff
@@ -737,23 +762,15 @@ func (a *FlavorAssigner) fitsResourceQuota(log logr.Logger, fr resources.FlavorR
 		return fit, borrow, nil
 	}
 
-	// Check if preemption is possible
-	mode := noFit
-	// For single-level hierarchies, mayReclaimInHierarchy = true iff val <= rQuota.Nominal
-	preemptionPossibility := a.oracle.SimulatePreemption(log, a.cq, *a.wl, fr, val)
-	if val <= rQuota.Nominal || mayReclaimInHierarchy {
-		mode = preempt
-		if preemptionPossibility == preemptioncommon.Reclaim {
-			mode = reclaim
-		}
-	} else if a.canPreemptWhileBorrowing() && (preemptionPossibility != preemptioncommon.NoCandidates) {
-		mode = preempt
-	}
-
+	// Preempt
 	status.appendf("insufficient unused quota for %s in flavor %s, %s more needed",
 		fr.Resource, fr.Flavor, resources.ResourceQuantityString(fr.Resource, val-available))
 
-	return mode, borrow, &status
+	if val <= rQuota.Nominal || mayReclaimInHierarchy || a.canPreemptWhileBorrowing() {
+		mode := fromPreemptionPosibility(a.oracle.SimulatePreemption(log, a.cq, *a.wl, fr, val))
```

  ```suggestion
  		mode := fromPreemptionPossibility(a.oracle.SimulatePreemption(log, a.cq, *a.wl, fr, val))
  ```
  nit

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-23T14:26:02Z

- **pkg/scheduler/scheduler_test.go:3807** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-23T14:25:55Z
```diff
@@ -3787,7 +3804,7 @@ func TestLastSchedulingContext(t *testing.T) {
 						*utiltesting.MakeFlavorQuotas("on-demand").
 							Resource(corev1.ResourceCPU, "50", "50").Obj(),
 						*utiltesting.MakeFlavorQuotas("spot").
-							Resource(corev1.ResourceCPU, "100", "0").Obj(),
+							Resource(corev1.ResourceCPU, "0", "0").Obj(),
```

  @mimowo @gabesaba Is this the correct case? I'm suspecting @gabesaba wanted to use the following:
  
  ```suggestion
  							Resource(corev1.ResourceCPU, "0").Obj(),
  ```
  
  Because nominal=0 and borrowing=0 indicate that this cq does not have any capabilities to use the spot instance.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T07:58:24Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:770** by [@mimowo](https://github.com/mimowo) — 2025-06-24T07:58:24Z
```diff
@@ -737,23 +762,15 @@ func (a *FlavorAssigner) fitsResourceQuota(log logr.Logger, fr resources.FlavorR
 		return fit, borrow, nil
 	}
 
-	// Check if preemption is possible
-	mode := noFit
-	// For single-level hierarchies, mayReclaimInHierarchy = true iff val <= rQuota.Nominal
-	preemptionPossibility := a.oracle.SimulatePreemption(log, a.cq, *a.wl, fr, val)
-	if val <= rQuota.Nominal || mayReclaimInHierarchy {
-		mode = preempt
-		if preemptionPossibility == preemptioncommon.Reclaim {
-			mode = reclaim
-		}
-	} else if a.canPreemptWhileBorrowing() && (preemptionPossibility != preemptioncommon.NoCandidates) {
-		mode = preempt
-	}
-
+	// Preempt
 	status.appendf("insufficient unused quota for %s in flavor %s, %s more needed",
 		fr.Resource, fr.Flavor, resources.ResourceQuantityString(fr.Resource, val-available))
 
-	return mode, borrow, &status
+	if val <= rQuota.Nominal || mayReclaimInHierarchy || a.canPreemptWhileBorrowing() {
+		mode := fromPreemptionPosibility(a.oracle.SimulatePreemption(log, a.cq, *a.wl, fr, val))
```

  This is addressed in https://github.com/kubernetes-sigs/kueue/pull/5702

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T08:04:23Z

- **pkg/scheduler/scheduler_test.go:3807** by [@mimowo](https://github.com/mimowo) — 2025-06-24T08:04:23Z
```diff
@@ -3787,7 +3804,7 @@ func TestLastSchedulingContext(t *testing.T) {
 						*utiltesting.MakeFlavorQuotas("on-demand").
 							Resource(corev1.ResourceCPU, "50", "50").Obj(),
 						*utiltesting.MakeFlavorQuotas("spot").
-							Resource(corev1.ResourceCPU, "100", "0").Obj(),
+							Resource(corev1.ResourceCPU, "0", "0").Obj(),
```

  It actually does not matter (both options pass). The intention is to make the second flavor "noFit". I propose to change it to "10" to make it a bit more realistic, and added a comment there: https://github.com/kubernetes-sigs/kueue/pull/5735/files#diff-130e36b28b6a84d355879caa6b7927c98ec18c63adad6ab4fc486458763080bfR3918-R3922

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T08:05:03Z

- **pkg/scheduler/scheduler_test.go:3735** by [@mimowo](https://github.com/mimowo) — 2025-06-24T08:05:03Z
```diff
@@ -3732,46 +3789,6 @@ func TestLastSchedulingContext(t *testing.T) {
 		wantAdmissionsOnFirstSchedule  map[workload.Reference]kueue.Admission
 		wantAdmissionsOnSecondSchedule map[workload.Reference]kueue.Admission
 	}{
-		{
```

  I see, but this logic change should be reflected in a dedicated test. I add this one in a follow up: https://github.com/kubernetes-sigs/kueue/pull/5735/files#diff-130e36b28b6a84d355879caa6b7927c98ec18c63adad6ab4fc486458763080bfR280

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-06-24T08:06:34Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:749** by [@mimowo](https://github.com/mimowo) — 2025-06-24T08:06:34Z
```diff
@@ -737,23 +762,15 @@ func (a *FlavorAssigner) fitsResourceQuota(log logr.Logger, fr resources.FlavorR
 		return fit, borrow, nil
 	}
 
-	// Check if preemption is possible
-	mode := noFit
-	// For single-level hierarchies, mayReclaimInHierarchy = true iff val <= rQuota.Nominal
-	preemptionPossibility := a.oracle.SimulatePreemption(log, a.cq, *a.wl, fr, val)
-	if val <= rQuota.Nominal || mayReclaimInHierarchy {
-		mode = preempt
-		if preemptionPossibility == preemptioncommon.Reclaim {
-			mode = reclaim
-		}
-	} else if a.canPreemptWhileBorrowing() && (preemptionPossibility != preemptioncommon.NoCandidates) {
```

  The PR does not actually add a unit test for the logic change when `val <= rQuota.Nominal || mayReclaimInHierarchy` is false, but `a.canPreemptWhileBorrowing()` is true. 
  
  I add it in the follow up: https://github.com/kubernetes-sigs/kueue/pull/5735/files#diff-130e36b28b6a84d355879caa6b7927c98ec18c63adad6ab4fc486458763080bfR2797

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-24T08:47:27Z

- **pkg/scheduler/scheduler_test.go:3807** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-24T08:47:27Z
```diff
@@ -3787,7 +3804,7 @@ func TestLastSchedulingContext(t *testing.T) {
 						*utiltesting.MakeFlavorQuotas("on-demand").
 							Resource(corev1.ResourceCPU, "50", "50").Obj(),
 						*utiltesting.MakeFlavorQuotas("spot").
-							Resource(corev1.ResourceCPU, "100", "0").Obj(),
+							Resource(corev1.ResourceCPU, "0", "0").Obj(),
```

  Thank you. I will take a look that.
