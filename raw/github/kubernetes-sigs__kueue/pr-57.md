# PR #57: Track usage in capacity status

**Summary**: Track usage in capacity status

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/57

**Last updated**: 2022-02-28T16:40:58Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `capacity_status` → `main`
- **Created**: 2022-02-23T22:25:28Z
- **Updated**: 2022-02-28T16:40:58Z
- **Merged**: 2022-02-28T16:40:57Z
- **Closed**: 2022-02-28T16:40:58Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: _none_
- **Changed files**: 13   **+435 / -54**

## Description

#### What type of PR is this?

/kind feature
/kind api-change

#### What this PR does / why we need it:

- Add new status fields
- Reconcile when there are QueuedWorkload updates

This is important for the observability of kueue operations

#### Which issue(s) this PR fixes:

Fixes #7

#### Special notes for your reviewer:

I'm putting a minimal check in the integration test. I plan to add a more comprehensive test as part of #68

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-02-23T22:25:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/57#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-24T22:32:29Z

/hold
(because I don't want a single LGTM to cause a merge of the PR)

/assign @ahg-g @ArangoGutierrez

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T21:46:57Z

Looks good to me if you want to squash, i can lgtm if there are no pending comments from others

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T14:42:24Z

rebased

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T16:32:52Z

/lgtm

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-02-28T16:33:44Z

/retest

why do we keep getting this failure?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-28T16:39:39Z

/hold cancel

## Reviews

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-23T22:41:34Z

testing it locally, so far looks good, for now just one nit

- **config/manager/kustomization.yaml:15** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-23T22:30:32Z
```diff
@@ -12,5 +12,4 @@ apiVersion: kustomize.config.k8s.io/v1beta1
 kind: Kustomization
 images:
 - name: controller
-  newName: kueue
-  newTag: latest
+  newName: gcr.io/acondor-gke-dev/kueue
```

  betrayed by kustomize

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-02-25T03:57:38Z

some comments.

- **api/v1alpha1/capacity_types.go:245** by [@denkensk](https://github.com/denkensk) — 2022-02-25T03:23:55Z
```diff
@@ -236,6 +236,22 @@ type Quota struct {
 
 // CapacityStatus defines the observed state of Capacity
 type CapacityStatus struct {
+	// usedResources are the resources (by flavor) currently in use by the
+	// workloads assigned to this capacity
+	UsedResources map[corev1.ResourceName]map[string]Usage `json:"usedResources,omitempty"`
+
+	// runningWorkloads is the number of workloads currently assigned to this
+	// capacity.
+	RunningWorkloads int32 `json:"runningWorkloads,omitempty"`
```

  Running is a little confuse. Actually the workload is just assigned.
  
  RunningWorkloads --> AssignedWorkloads

- **pkg/capacity/capacity.go:312** by [@denkensk](https://github.com/denkensk) — 2022-02-25T03:27:28Z
```diff
@@ -305,6 +306,36 @@ func (c *Cache) ForgetWorkload(w *kueue.QueuedWorkload) error {
 	return nil
 }
 
+// Usage reports the used resources and number of workloads assigned to the
+// capacity.
+func (c *Cache) Usage(capObj *kueue.Capacity) (map[corev1.ResourceName]map[string]kueue.Usage, int, error) {
+	c.Lock()
```

  We can choose to use RWLock here?

- **test/integration/scheduler/scheduler_test.go:85** by [@denkensk](https://github.com/denkensk) — 2022-02-25T03:44:27Z
```diff
@@ -74,6 +75,16 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, timeout, interval).Should(gomega.BeTrue())
 		gomega.Expect(createdProdJob1.Spec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor))
 
+		// TODO(#68): Put this test in the suite for core controllers, independent of the scheduler.
+		ginkgo.By("checking capacity usage is updated")
+		gomega.Eventually(func() bool {
+			var capObj kueue.Capacity
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: "prod-capacity"}, &capObj); err != nil {
+				return false
+			}
+			return capObj.Status.RunningWorkloads == 1
```

  Also check the usage？

- **pkg/controller/core/capacity_controller.go:150** by [@denkensk](https://github.com/denkensk) — 2022-02-25T03:56:53Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
+	}
+	// Shallow copy enough for now.
+	oldStatus := capObj.Status
+	capObj.Status.UsedResources = usage
+	capObj.Status.RunningWorkloads = int32(workloads)
+	if !equality.Semantic.DeepEqual(oldStatus, capObj.Status) {
+		err = r.client.Status().Update(ctx, &capObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *CapacityReconciler) Create(e event.CreateEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity create event")
 	ctx := ctrl.LoggerInto(context.Background(), log)
 	if err := r.cache.AddCapacity(ctx, c); err != nil {
 		log.Error(err, "Failed to add capacity to cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Delete(e event.DeleteEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "capacity", klog.KObj(c))
 	r.cache.DeleteCapacity(c)
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Update(e event.UpdateEvent) bool {
-	c := e.ObjectNew.(*kueue.Capacity)
-	log := r.log.WithValues("queue", klog.KObj(c))
+	c, match := e.ObjectNew.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
+	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity update event")
 	if err := r.cache.UpdateCapacity(c); err != nil {
 		log.Error(err, "Failed to update capacity in cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type assignedWorkloadHandler struct{}
+
+func (h *assignedWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	w := e.Object.(*kueue.QueuedWorkload)
+	if w.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(w))
+	}
+}
+
+func (h *assignedWorkloadHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldW := e.ObjectOld.(*kueue.QueuedWorkload)
+	if oldW.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(oldW))
+	}
+	newW := e.ObjectNew.(*kueue.QueuedWorkload)
+	if newW.Spec.AssignedCapacity != "" && newW.Spec.AssignedCapacity != oldW.Spec.AssignedCapacity {
```

  I'm curious about the circumstances under which this `Spec.AssignedCapacity` would change

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-25T12:16:05Z

tested locally and works, just a few comments

- **pkg/controller/core/capacity_controller.go:177** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-25T12:12:32Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
+	}
+	// Shallow copy enough for now.
+	oldStatus := capObj.Status
+	capObj.Status.UsedResources = usage
+	capObj.Status.RunningWorkloads = int32(workloads)
+	if !equality.Semantic.DeepEqual(oldStatus, capObj.Status) {
+		err = r.client.Status().Update(ctx, &capObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *CapacityReconciler) Create(e event.CreateEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity create event")
 	ctx := ctrl.LoggerInto(context.Background(), log)
 	if err := r.cache.AddCapacity(ctx, c); err != nil {
 		log.Error(err, "Failed to add capacity to cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Delete(e event.DeleteEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "capacity", klog.KObj(c))
 	r.cache.DeleteCapacity(c)
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Update(e event.UpdateEvent) bool {
-	c := e.ObjectNew.(*kueue.Capacity)
-	log := r.log.WithValues("queue", klog.KObj(c))
+	c, match := e.ObjectNew.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
+	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity update event")
 	if err := r.cache.UpdateCapacity(c); err != nil {
 		log.Error(err, "Failed to update capacity in cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type assignedWorkloadHandler struct{}
+
+func (h *assignedWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	w := e.Object.(*kueue.QueuedWorkload)
+	if w.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(w))
+	}
+}
+
+func (h *assignedWorkloadHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldW := e.ObjectOld.(*kueue.QueuedWorkload)
+	if oldW.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(oldW))
+	}
+	newW := e.ObjectNew.(*kueue.QueuedWorkload)
+	if newW.Spec.AssignedCapacity != "" && newW.Spec.AssignedCapacity != oldW.Spec.AssignedCapacity {
+		q.Add(requestForWorkloadCapacity(newW))
+	}
+}
+
+func (h *assignedWorkloadHandler) Delete(e event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	w := e.Object.(*kueue.QueuedWorkload)
+	if w.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(w))
+	}
+}
+
+func (h *assignedWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
+}
+
+func requestForWorkloadCapacity(w *kueue.QueuedWorkload) reconcile.Request {
+	return reconcile.Request{
+		NamespacedName: types.NamespacedName{
+			Name: string(w.Spec.AssignedCapacity),
+		},
+	}
 }
 
 // SetupWithManager sets up the controller with the Manager.
 func (r *CapacityReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.Capacity{}).
+		Watches(&source.Kind{Type: &kueue.QueuedWorkload{}}, &assignedWorkloadHandler{}).
```

  ```suggestion
  		Watches(&source.Kind{Type: &kueue.QueuedWorkload{}}, &assignedWorkloadHandler{}).
  		WithLogger(r.log).
  ```

- **pkg/controller/core/capacity_controller.go:72** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-25T12:14:51Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
```

  Maybe in this scenario we want to requeue? 
  ```go
  	// Requeue tells the Controller to requeue the reconcile key.  Defaults to false.
  	Requeue bool
  ```
  to trigger a new try to reconcile the state of the capacity?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-25T16:38:20Z

- **api/v1alpha1/capacity_types.go:254** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T00:20:40Z
```diff
@@ -236,6 +236,22 @@ type Quota struct {
 
 // CapacityStatus defines the observed state of Capacity
 type CapacityStatus struct {
+	// usedResources are the resources (by flavor) currently in use by the
+	// workloads assigned to this capacity
+	UsedResources map[corev1.ResourceName]map[string]Usage `json:"usedResources,omitempty"`
+
+	// runningWorkloads is the number of workloads currently assigned to this
+	// capacity.
+	RunningWorkloads int32 `json:"runningWorkloads,omitempty"`
+}
+
+type Usage struct {
+	// Total is the total quantity of the resource used.
+	Total *resource.Quantity `json:"total,omitempty"`
+
+	// Borrowing is the used quantity past the guaranteed quota, borrowed from
+	// the cohort.
+	Borrowing *resource.Quantity `json:"borrowing,omitempty"`
```

  Borrowed?

- **api/v1alpha1/capacity_types.go:251** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T00:21:14Z
```diff
@@ -236,6 +236,22 @@ type Quota struct {
 
 // CapacityStatus defines the observed state of Capacity
 type CapacityStatus struct {
+	// usedResources are the resources (by flavor) currently in use by the
+	// workloads assigned to this capacity
+	UsedResources map[corev1.ResourceName]map[string]Usage `json:"usedResources,omitempty"`
+
+	// runningWorkloads is the number of workloads currently assigned to this
+	// capacity.
+	RunningWorkloads int32 `json:"runningWorkloads,omitempty"`
+}
+
+type Usage struct {
+	// Total is the total quantity of the resource used.
+	Total *resource.Quantity `json:"total,omitempty"`
```

  excluding or including borrowed quantity? please clarify in the comment

- **pkg/capacity/capacity_test.go:648** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T16:22:14Z
```diff
@@ -563,23 +561,165 @@ func TestCacheWorkloadOperations(t *testing.T) {
 	}
 }
 
+func TestCapacityUsage(t *testing.T) {
+	capacity := kueue.Capacity{
+		ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+		Spec: kueue.CapacitySpec{
+			RequestableResources: []kueue.Resource{
+				{
+					Name: corev1.ResourceCPU,
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "default",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("10"),
+								Ceiling:    resource.MustParse("20"),
+							},
+						},
+					},
+				},
+				{
+					Name: "example.com/gpu",
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "model_a",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+						{
+							Name: "model_b",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+					},
+				},
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "one"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "8",
+							"example.com/gpu":  "5",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_a",
+						},
+					},
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "two"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "5",
+							"example.com/gpu":  "6",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_b",
+						},
+					},
+				},
+			},
+		},
+	}
+	cases := map[string]struct {
+		workloads         []kueue.QueuedWorkload
+		wantUsedResources map[corev1.ResourceName]map[string]kueue.Usage
+		wantWorkloads     int
+	}{
+		"single no borrowing": {
```

  add a test case for a finished workload

- **pkg/controller/core/capacity_controller.go:100** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T16:26:10Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
+	}
+	// Shallow copy enough for now.
+	oldStatus := capObj.Status
+	capObj.Status.UsedResources = usage
+	capObj.Status.RunningWorkloads = int32(workloads)
+	if !equality.Semantic.DeepEqual(oldStatus, capObj.Status) {
+		err = r.client.Status().Update(ctx, &capObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *CapacityReconciler) Create(e event.CreateEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity create event")
 	ctx := ctrl.LoggerInto(context.Background(), log)
 	if err := r.cache.AddCapacity(ctx, c); err != nil {
 		log.Error(err, "Failed to add capacity to cache")
 	}
-	return false
+	return true
```

  please add a comment somewhere explaining that all event handlers will be followed by executing Reconcile.

- **pkg/controller/core/capacity_controller.go:140** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T16:36:58Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
+	}
+	// Shallow copy enough for now.
+	oldStatus := capObj.Status
+	capObj.Status.UsedResources = usage
+	capObj.Status.RunningWorkloads = int32(workloads)
+	if !equality.Semantic.DeepEqual(oldStatus, capObj.Status) {
+		err = r.client.Status().Update(ctx, &capObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *CapacityReconciler) Create(e event.CreateEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity create event")
 	ctx := ctrl.LoggerInto(context.Background(), log)
 	if err := r.cache.AddCapacity(ctx, c); err != nil {
 		log.Error(err, "Failed to add capacity to cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Delete(e event.DeleteEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "capacity", klog.KObj(c))
 	r.cache.DeleteCapacity(c)
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Update(e event.UpdateEvent) bool {
-	c := e.ObjectNew.(*kueue.Capacity)
-	log := r.log.WithValues("queue", klog.KObj(c))
+	c, match := e.ObjectNew.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
+	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity update event")
 	if err := r.cache.UpdateCapacity(c); err != nil {
 		log.Error(err, "Failed to update capacity in cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type assignedWorkloadHandler struct{}
+
+func (h *assignedWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	w := e.Object.(*kueue.QueuedWorkload)
+	if w.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(w))
```

  I know this is related to kubebuilder/controller-runtime, but it would be good to add a comment explaining that those adds trigger the execution of the Reconcile logic.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-25T17:15:36Z

- **api/v1alpha1/capacity_types.go:245** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T16:37:11Z
```diff
@@ -236,6 +236,22 @@ type Quota struct {
 
 // CapacityStatus defines the observed state of Capacity
 type CapacityStatus struct {
+	// usedResources are the resources (by flavor) currently in use by the
+	// workloads assigned to this capacity
+	UsedResources map[corev1.ResourceName]map[string]Usage `json:"usedResources,omitempty"`
+
+	// runningWorkloads is the number of workloads currently assigned to this
+	// capacity.
+	RunningWorkloads int32 `json:"runningWorkloads,omitempty"`
```

  yes... not sure what I was thinking.

- **pkg/capacity/capacity.go:312** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T16:41:30Z
```diff
@@ -305,6 +306,36 @@ func (c *Cache) ForgetWorkload(w *kueue.QueuedWorkload) error {
 	return nil
 }
 
+// Usage reports the used resources and number of workloads assigned to the
+// capacity.
+func (c *Cache) Usage(capObj *kueue.Capacity) (map[corev1.ResourceName]map[string]kueue.Usage, int, error) {
+	c.Lock()
```

  yes, it makes sense now that there are more than one reader

- **pkg/controller/core/capacity_controller.go:72** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T16:43:28Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
```

  No, because we should receive the event, which will put the Capacity in the workqueue.
  
  Although it doesn't really matter, there is nothing to do if the capacity is deleted.

- **pkg/controller/core/capacity_controller.go:150** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T16:45:28Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
+	}
+	// Shallow copy enough for now.
+	oldStatus := capObj.Status
+	capObj.Status.UsedResources = usage
+	capObj.Status.RunningWorkloads = int32(workloads)
+	if !equality.Semantic.DeepEqual(oldStatus, capObj.Status) {
+		err = r.client.Status().Update(ctx, &capObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *CapacityReconciler) Create(e event.CreateEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity create event")
 	ctx := ctrl.LoggerInto(context.Background(), log)
 	if err := r.cache.AddCapacity(ctx, c); err != nil {
 		log.Error(err, "Failed to add capacity to cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Delete(e event.DeleteEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "capacity", klog.KObj(c))
 	r.cache.DeleteCapacity(c)
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Update(e event.UpdateEvent) bool {
-	c := e.ObjectNew.(*kueue.Capacity)
-	log := r.log.WithValues("queue", klog.KObj(c))
+	c, match := e.ObjectNew.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
+	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity update event")
 	if err := r.cache.UpdateCapacity(c); err != nil {
 		log.Error(err, "Failed to update capacity in cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type assignedWorkloadHandler struct{}
+
+func (h *assignedWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	w := e.Object.(*kueue.QueuedWorkload)
+	if w.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(w))
+	}
+}
+
+func (h *assignedWorkloadHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldW := e.ObjectOld.(*kueue.QueuedWorkload)
+	if oldW.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(oldW))
+	}
+	newW := e.ObjectNew.(*kueue.QueuedWorkload)
+	if newW.Spec.AssignedCapacity != "" && newW.Spec.AssignedCapacity != oldW.Spec.AssignedCapacity {
```

  It shouldn't happen... but I tried to make the code resilient to that.
  
  I guess one case could be reassignment after preemption, if the user decides to change the queue for the workload, or the queue changes capacity 🤷

- **pkg/controller/core/capacity_controller.go:177** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T16:47:17Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
+	}
+	// Shallow copy enough for now.
+	oldStatus := capObj.Status
+	capObj.Status.UsedResources = usage
+	capObj.Status.RunningWorkloads = int32(workloads)
+	if !equality.Semantic.DeepEqual(oldStatus, capObj.Status) {
+		err = r.client.Status().Update(ctx, &capObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *CapacityReconciler) Create(e event.CreateEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity create event")
 	ctx := ctrl.LoggerInto(context.Background(), log)
 	if err := r.cache.AddCapacity(ctx, c); err != nil {
 		log.Error(err, "Failed to add capacity to cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Delete(e event.DeleteEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "capacity", klog.KObj(c))
 	r.cache.DeleteCapacity(c)
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Update(e event.UpdateEvent) bool {
-	c := e.ObjectNew.(*kueue.Capacity)
-	log := r.log.WithValues("queue", klog.KObj(c))
+	c, match := e.ObjectNew.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
+	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity update event")
 	if err := r.cache.UpdateCapacity(c); err != nil {
 		log.Error(err, "Failed to update capacity in cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type assignedWorkloadHandler struct{}
+
+func (h *assignedWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	w := e.Object.(*kueue.QueuedWorkload)
+	if w.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(w))
+	}
+}
+
+func (h *assignedWorkloadHandler) Update(e event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldW := e.ObjectOld.(*kueue.QueuedWorkload)
+	if oldW.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(oldW))
+	}
+	newW := e.ObjectNew.(*kueue.QueuedWorkload)
+	if newW.Spec.AssignedCapacity != "" && newW.Spec.AssignedCapacity != oldW.Spec.AssignedCapacity {
+		q.Add(requestForWorkloadCapacity(newW))
+	}
+}
+
+func (h *assignedWorkloadHandler) Delete(e event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	w := e.Object.(*kueue.QueuedWorkload)
+	if w.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(w))
+	}
+}
+
+func (h *assignedWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
+}
+
+func requestForWorkloadCapacity(w *kueue.QueuedWorkload) reconcile.Request {
+	return reconcile.Request{
+		NamespacedName: types.NamespacedName{
+			Name: string(w.Spec.AssignedCapacity),
+		},
+	}
 }
 
 // SetupWithManager sets up the controller with the Manager.
 func (r *CapacityReconciler) SetupWithManager(mgr ctrl.Manager) error {
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.Capacity{}).
+		Watches(&source.Kind{Type: &kueue.QueuedWorkload{}}, &assignedWorkloadHandler{}).
```

  I tried that before, but the manager adds extra names... so it looks like this:
  
  `capacity-reconciler.controller.capacity`
  
  Better just have `controller.capacity` to distinguish from the event handler logs.
  
  Maybe I should rename the handler log to `capacity-handler`?

- **test/integration/scheduler/scheduler_test.go:85** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T16:48:05Z
```diff
@@ -74,6 +75,16 @@ var _ = ginkgo.Describe("Scheduler", func() {
 		}, timeout, interval).Should(gomega.BeTrue())
 		gomega.Expect(createdProdJob1.Spec.Template.Spec.NodeSelector[labelKey]).Should(gomega.Equal(onDemandFlavor))
 
+		// TODO(#68): Put this test in the suite for core controllers, independent of the scheduler.
+		ginkgo.By("checking capacity usage is updated")
+		gomega.Eventually(func() bool {
+			var capObj kueue.Capacity
+			if err := k8sClient.Get(ctx, types.NamespacedName{Name: "prod-capacity"}, &capObj); err != nil {
+				return false
+			}
+			return capObj.Status.RunningWorkloads == 1
```

  Will do as part of #68

- **api/v1alpha1/capacity_types.go:251** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T16:51:17Z
```diff
@@ -236,6 +236,22 @@ type Quota struct {
 
 // CapacityStatus defines the observed state of Capacity
 type CapacityStatus struct {
+	// usedResources are the resources (by flavor) currently in use by the
+	// workloads assigned to this capacity
+	UsedResources map[corev1.ResourceName]map[string]Usage `json:"usedResources,omitempty"`
+
+	// runningWorkloads is the number of workloads currently assigned to this
+	// capacity.
+	RunningWorkloads int32 `json:"runningWorkloads,omitempty"`
+}
+
+type Usage struct {
+	// Total is the total quantity of the resource used.
+	Total *resource.Quantity `json:"total,omitempty"`
```

  Including. Clarified.

- **api/v1alpha1/capacity_types.go:254** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T16:51:23Z
```diff
@@ -236,6 +236,22 @@ type Quota struct {
 
 // CapacityStatus defines the observed state of Capacity
 type CapacityStatus struct {
+	// usedResources are the resources (by flavor) currently in use by the
+	// workloads assigned to this capacity
+	UsedResources map[corev1.ResourceName]map[string]Usage `json:"usedResources,omitempty"`
+
+	// runningWorkloads is the number of workloads currently assigned to this
+	// capacity.
+	RunningWorkloads int32 `json:"runningWorkloads,omitempty"`
+}
+
+type Usage struct {
+	// Total is the total quantity of the resource used.
+	Total *resource.Quantity `json:"total,omitempty"`
+
+	// Borrowing is the used quantity past the guaranteed quota, borrowed from
+	// the cohort.
+	Borrowing *resource.Quantity `json:"borrowing,omitempty"`
```

  Sg.

- **pkg/capacity/capacity_test.go:648** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T17:14:07Z
```diff
@@ -563,23 +561,165 @@ func TestCacheWorkloadOperations(t *testing.T) {
 	}
 }
 
+func TestCapacityUsage(t *testing.T) {
+	capacity := kueue.Capacity{
+		ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+		Spec: kueue.CapacitySpec{
+			RequestableResources: []kueue.Resource{
+				{
+					Name: corev1.ResourceCPU,
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "default",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("10"),
+								Ceiling:    resource.MustParse("20"),
+							},
+						},
+					},
+				},
+				{
+					Name: "example.com/gpu",
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "model_a",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+						{
+							Name: "model_b",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+					},
+				},
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "one"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "8",
+							"example.com/gpu":  "5",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_a",
+						},
+					},
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "two"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "5",
+							"example.com/gpu":  "6",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_b",
+						},
+					},
+				},
+			},
+		},
+	}
+	cases := map[string]struct {
+		workloads         []kueue.QueuedWorkload
+		wantUsedResources map[corev1.ResourceName]map[string]kueue.Usage
+		wantWorkloads     int
+	}{
+		"single no borrowing": {
```

  Good suggestion, but the capacity cache has no knowledge of finished workload. It happens in the event handler of the queued workload. Should we push that down to the cache?
  The current implementation is similar to kube-scheduler.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-25T17:22:49Z

- **pkg/controller/core/capacity_controller.go:100** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T17:18:52Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
+	}
+	// Shallow copy enough for now.
+	oldStatus := capObj.Status
+	capObj.Status.UsedResources = usage
+	capObj.Status.RunningWorkloads = int32(workloads)
+	if !equality.Semantic.DeepEqual(oldStatus, capObj.Status) {
+		err = r.client.Status().Update(ctx, &capObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *CapacityReconciler) Create(e event.CreateEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity create event")
 	ctx := ctrl.LoggerInto(context.Background(), log)
 	if err := r.cache.AddCapacity(ctx, c); err != nil {
 		log.Error(err, "Failed to add capacity to cache")
 	}
-	return false
+	return true
```

  Done

- **pkg/controller/core/capacity_controller.go:140** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T17:22:15Z
```diff
@@ -47,47 +55,121 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
+	if err := r.client.Get(ctx, req.NamespacedName, &capObj); err != nil {
+		// we'll ignore not-found errors, since there is nothing to do.
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
+	log := ctrl.LoggerFrom(ctx).WithValues("capacity", klog.KObj(&capObj))
+	ctx = ctrl.LoggerInto(ctx, log)
+	log.V(2).Info("Reconciling Capacity")
+
+	usage, workloads, err := r.cache.Usage(&capObj)
+	if err != nil {
+		log.Error(err, "Failed getting usage from capacity cache")
+		// This is likely because the capacity was recently removed and we didn't
+		// process that event yet.
+		return ctrl.Result{}, err
+	}
+	// Shallow copy enough for now.
+	oldStatus := capObj.Status
+	capObj.Status.UsedResources = usage
+	capObj.Status.RunningWorkloads = int32(workloads)
+	if !equality.Semantic.DeepEqual(oldStatus, capObj.Status) {
+		err = r.client.Status().Update(ctx, &capObj)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	}
 	return ctrl.Result{}, nil
 }
 
 func (r *CapacityReconciler) Create(e event.CreateEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity create event")
 	ctx := ctrl.LoggerInto(context.Background(), log)
 	if err := r.cache.AddCapacity(ctx, c); err != nil {
 		log.Error(err, "Failed to add capacity to cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Delete(e event.DeleteEvent) bool {
-	c := e.Object.(*kueue.Capacity)
+	c, match := e.Object.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
 	r.log.V(2).Info("Queue delete event", "capacity", klog.KObj(c))
 	r.cache.DeleteCapacity(c)
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Update(e event.UpdateEvent) bool {
-	c := e.ObjectNew.(*kueue.Capacity)
-	log := r.log.WithValues("queue", klog.KObj(c))
+	c, match := e.ObjectNew.(*kueue.Capacity)
+	if !match {
+		// No need to interact with the cache for other objects.
+		return true
+	}
+	log := r.log.WithValues("capacity", klog.KObj(c))
 	log.V(2).Info("Capacity update event")
 	if err := r.cache.UpdateCapacity(c); err != nil {
 		log.Error(err, "Failed to update capacity in cache")
 	}
-	return false
+	return true
 }
 
 func (r *CapacityReconciler) Generic(e event.GenericEvent) bool {
 	r.log.V(3).Info("Ignore generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
-	return false
+	return true
+}
+
+type assignedWorkloadHandler struct{}
+
+func (h *assignedWorkloadHandler) Create(e event.CreateEvent, q workqueue.RateLimitingInterface) {
+	w := e.Object.(*kueue.QueuedWorkload)
+	if w.Spec.AssignedCapacity != "" {
+		q.Add(requestForWorkloadCapacity(w))
```

  Done

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-25T17:48:24Z

- **pkg/controller/core/capacity_controller.go:58** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-25T17:48:20Z
```diff
@@ -47,47 +55,126 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
```

  Should we do something like: 
  ```suggestion
  	var capObj kueue.Capacity
  	capObj..ObjectMeta.Name = req.NamespacedName.Name
  ```
  considering multiple capacities in the cluster, to prevent reconciling on the one we don't want and having to requeue

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-25T18:31:50Z

- **pkg/controller/core/capacity_controller.go:58** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T18:31:50Z
```diff
@@ -47,47 +55,126 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
```

  Not sure if I understand your suggestion. `r.client.Get` is already receiving the name of the capacity.

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-25T18:35:01Z

- **pkg/controller/core/capacity_controller.go:58** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-02-25T18:35:01Z
```diff
@@ -47,47 +55,126 @@ func NewCapacityReconciler(cache *capacity.Cache) *CapacityReconciler {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=capacities/finalizers,verbs=update
 
 func (r *CapacityReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	// No-op. All work is done in the event handler.
+	var capObj kueue.Capacity
```

  ah my bad, I was thinking on something diff, you are right

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-25T20:53:26Z

- **pkg/capacity/capacity_test.go:648** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T20:53:26Z
```diff
@@ -563,23 +561,165 @@ func TestCacheWorkloadOperations(t *testing.T) {
 	}
 }
 
+func TestCapacityUsage(t *testing.T) {
+	capacity := kueue.Capacity{
+		ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+		Spec: kueue.CapacitySpec{
+			RequestableResources: []kueue.Resource{
+				{
+					Name: corev1.ResourceCPU,
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "default",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("10"),
+								Ceiling:    resource.MustParse("20"),
+							},
+						},
+					},
+				},
+				{
+					Name: "example.com/gpu",
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "model_a",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+						{
+							Name: "model_b",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+					},
+				},
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "one"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "8",
+							"example.com/gpu":  "5",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_a",
+						},
+					},
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "two"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "5",
+							"example.com/gpu":  "6",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_b",
+						},
+					},
+				},
+			},
+		},
+	}
+	cases := map[string]struct {
+		workloads         []kueue.QueuedWorkload
+		wantUsedResources map[corev1.ResourceName]map[string]kueue.Usage
+		wantWorkloads     int
+	}{
+		"single no borrowing": {
```

  Add it in the integration test then.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-02-25T21:24:35Z

- **pkg/capacity/capacity_test.go:648** by [@alculquicondor](https://github.com/alculquicondor) — 2022-02-25T21:24:34Z
```diff
@@ -563,23 +561,165 @@ func TestCacheWorkloadOperations(t *testing.T) {
 	}
 }
 
+func TestCapacityUsage(t *testing.T) {
+	capacity := kueue.Capacity{
+		ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+		Spec: kueue.CapacitySpec{
+			RequestableResources: []kueue.Resource{
+				{
+					Name: corev1.ResourceCPU,
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "default",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("10"),
+								Ceiling:    resource.MustParse("20"),
+							},
+						},
+					},
+				},
+				{
+					Name: "example.com/gpu",
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "model_a",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+						{
+							Name: "model_b",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+					},
+				},
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "one"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "8",
+							"example.com/gpu":  "5",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_a",
+						},
+					},
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "two"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "5",
+							"example.com/gpu":  "6",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_b",
+						},
+					},
+				},
+			},
+		},
+	}
+	cases := map[string]struct {
+		workloads         []kueue.QueuedWorkload
+		wantUsedResources map[corev1.ResourceName]map[string]kueue.Usage
+		wantWorkloads     int
+	}{
+		"single no borrowing": {
```

  Sg. But I prefer to leave it for #68, which I'm already working on.

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-02-25T21:45:49Z

- **pkg/capacity/capacity_test.go:648** by [@ahg-g](https://github.com/ahg-g) — 2022-02-25T21:45:49Z
```diff
@@ -563,23 +561,165 @@ func TestCacheWorkloadOperations(t *testing.T) {
 	}
 }
 
+func TestCapacityUsage(t *testing.T) {
+	capacity := kueue.Capacity{
+		ObjectMeta: metav1.ObjectMeta{Name: "foo"},
+		Spec: kueue.CapacitySpec{
+			RequestableResources: []kueue.Resource{
+				{
+					Name: corev1.ResourceCPU,
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "default",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("10"),
+								Ceiling:    resource.MustParse("20"),
+							},
+						},
+					},
+				},
+				{
+					Name: "example.com/gpu",
+					Flavors: []kueue.ResourceFlavor{
+						{
+							Name: "model_a",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+						{
+							Name: "model_b",
+							Quota: kueue.Quota{
+								Guaranteed: resource.MustParse("5"),
+								Ceiling:    resource.MustParse("10"),
+							},
+						},
+					},
+				},
+			},
+		},
+	}
+	workloads := []kueue.QueuedWorkload{
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "one"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "8",
+							"example.com/gpu":  "5",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_a",
+						},
+					},
+				},
+			},
+		},
+		{
+			ObjectMeta: metav1.ObjectMeta{Name: "two"},
+			Spec: kueue.QueuedWorkloadSpec{
+				AssignedCapacity: "foo",
+				Pods: []kueue.PodSet{
+					{
+						Count: 1,
+						Spec: utiltesting.PodSpecForRequest(map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "5",
+							"example.com/gpu":  "6",
+						}),
+						AssignedFlavors: map[corev1.ResourceName]string{
+							corev1.ResourceCPU: "default",
+							"example.com/gpu":  "model_b",
+						},
+					},
+				},
+			},
+		},
+	}
+	cases := map[string]struct {
+		workloads         []kueue.QueuedWorkload
+		wantUsedResources map[corev1.ResourceName]map[string]kueue.Usage
+		wantWorkloads     int
+	}{
+		"single no borrowing": {
```

  Ok

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-02-25T22:14:33Z

/lgtm
