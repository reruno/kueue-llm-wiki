# PR #570: Validate queue-name annotation is a valid crd name

**Summary**: Validate queue-name annotation is a valid crd name

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/570

**Last updated**: 2023-03-18T18:48:05Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `add-test-for-queue-name` → `main`
- **Created**: 2023-02-15T08:07:13Z
- **Updated**: 2023-03-18T18:48:05Z
- **Merged**: 2023-02-15T18:12:34Z
- **Closed**: 2023-02-15T18:12:34Z
- **Labels**: `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 2   **+60 / -26**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #562

#### Special notes for your reviewer:

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-02-15T08:07:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4141cc52771f905b3377558f5ef41c8d2d13e9bc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/63ed1be9efa133000795f8f9 |

### Comment by [@mimowo](https://github.com/mimowo) — 2023-02-15T08:22:27Z

/assign @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2023-02-15T17:53:30Z

Rebased

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-02-15T18:11:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/570#pullrequestreview-1300093118" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/570#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-02-15T12:01:19Z

Thank you!

/lgtm
/assign @alculquicondor

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-15T14:50:47Z

- **pkg/controller/workload/job/job_webhook.go:123** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-15T14:48:35Z
```diff
@@ -110,13 +113,20 @@ func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.
 func validateUpdate(oldJob, newJob *batchv1.Job) error {
 	suspendPath := field.NewPath("job", "spec", "suspend")
 
-	if queueName(oldJob) == "" && queueName(newJob) != "" && !*newJob.Spec.Suspend {
-		return field.Forbidden(suspendPath, "suspend should be true when adding the queue name")
+	if queueName(oldJob) != queueName(newJob) {
+		if queueName(oldJob) == "" && !*newJob.Spec.Suspend {
+			return field.Forbidden(suspendPath, "suspend should be true when adding the queue name")
+		}
+		if !*newJob.Spec.Suspend {
+			return field.Forbidden(suspendPath, "should not update queue name when job is unsuspend")
+		}
+		if value, exists := newJob.Annotations[constants.QueueAnnotation]; exists {
```

  instead of rewriting this code, call `validateCreate(newJob)`

- **pkg/controller/workload/job/job_webhook.go:96** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-15T14:48:52Z
```diff
@@ -89,9 +90,11 @@ func (w *JobWebhook) ValidateCreate(ctx context.Context, obj runtime.Object) err
 }
 
 func validateCreate(job *batchv1.Job) error {
-	if value, exists := job.Annotations[constants.ParentWorkloadAnnotation]; exists {
-		if errs := validation.IsDNS1123Subdomain(value); len(errs) > 0 {
-			return field.Invalid(parentWorkloadKeyPath, value, strings.Join(errs, ","))
+	for _, crdNameAnnotation := range []string{constants.ParentWorkloadAnnotation, constants.QueueAnnotation} {
+		if value, exists := job.Annotations[crdNameAnnotation]; exists {
+			if errs := validation.IsDNS1123Subdomain(value); len(errs) > 0 {
+				return field.Invalid(annotationsPath.Key(crdNameAnnotation), value, strings.Join(errs, ","))
```

  aggregate all errors. You can use `field.ErrorList`

- **pkg/controller/workload/job/job_webhook.go:113** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-15T14:49:09Z
```diff
@@ -110,13 +113,20 @@ func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.
 func validateUpdate(oldJob, newJob *batchv1.Job) error {
```

  ```suggestion
  func validateUpdate(oldJob, newJob *batchv1.Job) field.ErrorList {
  ```

- **pkg/controller/workload/job/job_webhook.go:118** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-15T14:49:50Z
```diff
@@ -110,13 +113,20 @@ func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.
 func validateUpdate(oldJob, newJob *batchv1.Job) error {
 	suspendPath := field.NewPath("job", "spec", "suspend")
 
-	if queueName(oldJob) == "" && queueName(newJob) != "" && !*newJob.Spec.Suspend {
-		return field.Forbidden(suspendPath, "suspend should be true when adding the queue name")
+	if queueName(oldJob) != queueName(newJob) {
+		if queueName(oldJob) == "" && !*newJob.Spec.Suspend {
+			return field.Forbidden(suspendPath, "suspend should be true when adding the queue name")
```

  ```suggestion
  			return field.Forbidden(suspendPath, "suspend must be true when adding the queue name")
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-02-15T16:58:37Z

- **pkg/controller/workload/job/job_webhook.go:123** by [@mimowo](https://github.com/mimowo) — 2023-02-15T16:58:37Z
```diff
@@ -110,13 +113,20 @@ func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.
 func validateUpdate(oldJob, newJob *batchv1.Job) error {
 	suspendPath := field.NewPath("job", "spec", "suspend")
 
-	if queueName(oldJob) == "" && queueName(newJob) != "" && !*newJob.Spec.Suspend {
-		return field.Forbidden(suspendPath, "suspend should be true when adding the queue name")
+	if queueName(oldJob) != queueName(newJob) {
+		if queueName(oldJob) == "" && !*newJob.Spec.Suspend {
+			return field.Forbidden(suspendPath, "suspend should be true when adding the queue name")
+		}
+		if !*newJob.Spec.Suspend {
+			return field.Forbidden(suspendPath, "should not update queue name when job is unsuspend")
+		}
+		if value, exists := newJob.Annotations[constants.QueueAnnotation]; exists {
```

  Calling `validateCreate` means validating the `parent-annotation` each time, but we already test that it is immutable, so it is enough to test it at creation.  WDYT?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-15T17:03:40Z

- **pkg/controller/workload/job/job_webhook.go:123** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-15T17:03:40Z
```diff
@@ -110,13 +113,20 @@ func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.
 func validateUpdate(oldJob, newJob *batchv1.Job) error {
 	suspendPath := field.NewPath("job", "spec", "suspend")
 
-	if queueName(oldJob) == "" && queueName(newJob) != "" && !*newJob.Spec.Suspend {
-		return field.Forbidden(suspendPath, "suspend should be true when adding the queue name")
+	if queueName(oldJob) != queueName(newJob) {
+		if queueName(oldJob) == "" && !*newJob.Spec.Suspend {
+			return field.Forbidden(suspendPath, "suspend should be true when adding the queue name")
+		}
+		if !*newJob.Spec.Suspend {
+			return field.Forbidden(suspendPath, "should not update queue name when job is unsuspend")
+		}
+		if value, exists := newJob.Annotations[constants.QueueAnnotation]; exists {
```

  Calling both functions is the general pattern we follow in k8s validation. Right now it's relatively easy to see what needs to be validated in update without calling validateCreate, but it might be harder in the future.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-15T17:45:33Z

- **pkg/controller/workload/job/job_webhook.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-15T17:43:45Z
```diff
@@ -103,25 +107,30 @@ func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.
 	newJob := newObj.(*batchv1.Job)
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Validating update", "job", klog.KObj(newJob))
+	return aggregateErrors(validateUpdate(oldJob, newJob))
+}
 
-	return validateUpdate(oldJob, newJob)
+func aggregateErrors(allErrs field.ErrorList) error {
+	if len(allErrs) > 0 {
+		return allErrs.ToAggregate()
```

  `ToAggregate` already handles nil/empty.

- **pkg/controller/workload/job/job_webhook.go:118** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-15T17:44:44Z
```diff
@@ -103,25 +107,30 @@ func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.
 	newJob := newObj.(*batchv1.Job)
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Validating update", "job", klog.KObj(newJob))
+	return aggregateErrors(validateUpdate(oldJob, newJob))
+}
 
-	return validateUpdate(oldJob, newJob)
+func aggregateErrors(allErrs field.ErrorList) error {
+	if len(allErrs) > 0 {
+		return allErrs.ToAggregate()
+	}
+	return nil
 }
 
-func validateUpdate(oldJob, newJob *batchv1.Job) error {
+func validateUpdate(oldJob, newJob *batchv1.Job) field.ErrorList {
+	var allErrs field.ErrorList
 	suspendPath := field.NewPath("job", "spec", "suspend")
 
-	if queueName(oldJob) == "" && queueName(newJob) != "" && !*newJob.Spec.Suspend {
-		return field.Forbidden(suspendPath, "suspend should be true when adding the queue name")
+	if queueName(oldJob) != queueName(newJob) && !*newJob.Spec.Suspend {
+		allErrs = append(allErrs, field.Forbidden(suspendPath, "should not update queue name when job is unsuspend"))
```

  must not

- **pkg/controller/workload/job/job_webhook.go:114** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-15T17:45:09Z
```diff
@@ -103,25 +107,30 @@ func (w *JobWebhook) ValidateUpdate(ctx context.Context, oldObj, newObj runtime.
 	newJob := newObj.(*batchv1.Job)
 	log := ctrl.LoggerFrom(ctx).WithName("job-webhook")
 	log.V(5).Info("Validating update", "job", klog.KObj(newJob))
+	return aggregateErrors(validateUpdate(oldJob, newJob))
+}
 
-	return validateUpdate(oldJob, newJob)
+func aggregateErrors(allErrs field.ErrorList) error {
+	if len(allErrs) > 0 {
+		return allErrs.ToAggregate()
+	}
+	return nil
 }
 
-func validateUpdate(oldJob, newJob *batchv1.Job) error {
+func validateUpdate(oldJob, newJob *batchv1.Job) field.ErrorList {
+	var allErrs field.ErrorList
```

  ```suggestion
  	allErrs := validateCreate(newJob)
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-15T18:11:45Z

/lgtm
/approve
