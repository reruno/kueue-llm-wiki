# PR #5754: Update Helm Template to support custom ingress host

**Summary**: Update Helm Template to support custom ingress host

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5754

**Last updated**: 2025-06-30T13:13:21Z

---

## Metadata

- **State**: closed
- **Author**: [@btwseeu78](https://github.com/btwseeu78)
- **Branch**: `chore/fix-helm-template` → `main`
- **Created**: 2025-06-24T17:06:34Z
- **Updated**: 2025-06-30T13:13:21Z
- **Merged**: —
- **Closed**: 2025-06-30T12:17:21Z
- **Labels**: `kind/feature`, `release-note`, `size/XL`, `do-not-merge/hold`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: _none_
- **Changed files**: 13   **+346 / -186**

## Description

/kind bug
/kind cleanup
/kind feature

#### What this PR does / why we need it:
1. Current helm chart has no way to configure host for ingress in kueueviz it ends up referring  a random TLS-Secret(bug)
2. PriorityClass only added to manager not extended to kueueviz ,  frontend or backend deployments

Fixes #
-  https://github.com/kubernetes-sigs/kueue/issues/5437


#### Does this PR introduce a user-facing change?
```release-note
1. Add Support for configuring host for ingress in kueueviz.
2. changing PriorityClassName support extended to kueueviz  frontend or backend deployments.
3. Add imagePullSecrets for kueueViz frontend and backend deployments
4. Added test cases for kueueviz components in helm unit-test 
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2025-06-24T17:06:38Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: btwseeu78 / name: Arpan Chatterjee  (d8ae2da89b808b3a015d08a447b5b13b0aad5bbc, 90958ef0a0c058f0075bdfccaf0c57b658e2abed, f0026efe91dbbcc78fc4288e434f8cdb3a935919)</li></ul><!-- Date Modified: 2025-06-30 06:35:04.333959 -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-24T17:06:41Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5754#" title="Author self-approved">btwseeu78</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-24T17:06:43Z

Welcome @btwseeu78! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-24T17:06:43Z

Hi @btwseeu78. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-24T17:06:52Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f0026efe91dbbcc78fc4288e434f8cdb3a935919 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6862301602d41400080d3077 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-24T17:39:29Z

@btwseeu78 please sign CLA

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-24T17:39:47Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-24T17:40:24Z

@btwseeu78 please also fix the release note to remove the `do-not-merge/release-note-label-needed` label

### Comment by [@mimowo](https://github.com/mimowo) — 2025-06-24T17:41:52Z

/remove-kind cleanup
/remove-kind bug
I think this is actually a feature, but which can be cherry-picked. We did it in the past, see [example](https://github.com/kubernetes-sigs/kueue/pull/5631)

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-24T19:13:38Z

/retest-required

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-24T19:46:10Z

```bash
❯ make helm-unit-test
go: helm.sh/helm/v3@v3.18.1 requires go >= 1.24.0; switching to go1.24.4
/Users/arpan-k8s/GolandProjects/kueue/bin/helm unittest charts/kueue --strict --debug

### Chart [ kueue ] charts/kueue

 PASS  test kueueviz deployment charts/kueue/tests/kueueviz_test.yaml
 PASS  test manager deployment  charts/kueue/tests/manager_test.yaml

Charts:      1 passed, 1 total
Test Suites: 2 passed, 2 total
Tests:       10 passed, 10 total
Snapshot:    0 passed, 0 total
Time:        48.912042ms
```
not very sure why prow test failed,some guide will be appreciated

### Comment by [@kannon92](https://github.com/kannon92) — 2025-06-24T20:04:56Z

> ```shell
> ❯ make helm-unit-test
> go: helm.sh/helm/v3@v3.18.1 requires go >= 1.24.0; switching to go1.24.4
> /Users/arpan-k8s/GolandProjects/kueue/bin/helm unittest charts/kueue --strict --debug
> 
> ### Chart [ kueue ] charts/kueue
> 
>  PASS  test kueueviz deployment charts/kueue/tests/kueueviz_test.yaml
>  PASS  test manager deployment  charts/kueue/tests/manager_test.yaml
> 
> Charts:      1 passed, 1 total
> Test Suites: 2 passed, 2 total
> Tests:       10 passed, 10 total
> Snapshot:    0 passed, 0 total
> Time:        48.912042ms
> ```
> 
> not very sure why prow test failed,some guide will be appreciated

I've not dug much into it but I can reproduce your failure if I run `make verify`.

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-24T20:48:20Z

/test pull-kueue-verify-main

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-24T21:08:08Z

@kannon92  The problem is on step update-helm in makefile , its regenerates all the manifests which is overwriting this changes.so there is no way to update helm than generating it,i will check the modifications,it was a nice learn :)

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T09:11:06Z

/retest-required

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-06-24T17:44:03Z

- **charts/kueue/values.yaml:219** by [@kannon92](https://github.com/kannon92) — 2025-06-24T17:44:03Z
```diff
@@ -209,6 +209,14 @@ mutatingWebhook:
   reinvocationPolicy: Never
 kueueViz:
   backend:
+    ingress:
+      # -- KueueViz dashboard frontend ingress class name
+      ingressClassName: "ingress-nginx"
+      # -- KueueViz dashboard frontend ingress host
+      host: "frontend.kueueviz.local"
+      # -- KueueViz dashboard frontend ingress tls secret name
+      tlsSecretName: "kueueviz-frontend-tls"
+    priorityClassName:
```

  This seems like a bug.
  
  We should set a value for this for defaults.
  
  Or don't set it.
  
  /hold

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-06-24T17:45:49Z

It would be worth adding a helm unit test for this.

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-24T18:23:13Z

- **charts/kueue/values.yaml:219** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-24T18:23:13Z
```diff
@@ -209,6 +209,14 @@ mutatingWebhook:
   reinvocationPolicy: Never
 kueueViz:
   backend:
+    ingress:
+      # -- KueueViz dashboard frontend ingress class name
+      ingressClassName: "ingress-nginx"
+      # -- KueueViz dashboard frontend ingress host
+      host: "frontend.kueueviz.local"
+      # -- KueueViz dashboard frontend ingress tls secret name
+      tlsSecretName: "kueueviz-frontend-tls"
+    priorityClassName:
```

  i will check and add tests as recommended

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T08:03:14Z

- **charts/kueue/Chart.yaml:19** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T08:03:14Z
```diff
@@ -16,7 +16,7 @@ type: application
 # NOTE: Do not modify manually. In Kueue, the version and appVersion are
 # overridden to GIT_TAG when building the artifacts, including the helm charts,
 # via Makefile.
-version: 0.12.3
+version: 0.12.4
```

  Please revert.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T08:03:30Z

- **charts/kueue/README.md:3** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T08:03:29Z
```diff
@@ -1,6 +1,6 @@
 # kueue
 
-![Version: 0.12.3](https://img.shields.io/badge/Version-0.12.3-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: v0.12.3](https://img.shields.io/badge/AppVersion-v0.12.3-informational?style=flat-square)
+![Version: 0.12.4](https://img.shields.io/badge/Version-0.12.4-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: v0.12.3](https://img.shields.io/badge/AppVersion-v0.12.3-informational?style=flat-square)
```

  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T08:03:51Z

- **charts/kueue/values.yaml:26** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T08:03:51Z
```diff
@@ -23,7 +23,7 @@ controllerManager:
   #    enabled: true
   manager:
     # -- ControllerManager's pod priorityClassName
-    priorityClassName:
+    priorityClassName: ""
```

  And this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T08:04:36Z

- **hack/processing-plan.yaml:577** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T08:04:35Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  Why you delete this?

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-25T08:23:12Z

- **hack/processing-plan.yaml:577** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T08:23:12Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  This is generating helm chart from yaml-processor which can be useful for initial bootstrap but this is creating issues in generating complex logic inside helm, probably the reason the `manager deployment` was already out of this pipeline, i have done the same kueueviz components, generating the same through processor and creating new modification through it can be complex and extra work. we have unittest or lint in place to validate the errors.
  
  This is my suggestion, i would love to hear more thoughts and suggestions.

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-25T08:24:26Z

- **hack/processing-plan.yaml:577** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T08:24:26Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  Problem is probably template multi-line complex logic through the template engine,which is time consuming and not easy to add the features.

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-25T08:29:57Z

- **charts/kueue/values.yaml:26** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T08:29:57Z
```diff
@@ -23,7 +23,7 @@ controllerManager:
   #    enabled: true
   manager:
     # -- ControllerManager's pod priorityClassName
-    priorityClassName:
+    priorityClassName: ""
```

  i think providing default value is useful, it will be ignored by our curr template as blank value

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T08:31:37Z

- **Makefile:186** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T08:31:37Z
```diff
@@ -180,6 +180,10 @@ helm-verify: helm helm-lint ## run helm template and detect any rendering failur
 	$(HELM) template charts/kueue --set managerConfig.controllerManagerConfigYaml="managedJobsNamespaceSelector:\n  matchExpressions:\n    - key: kubernetes.io/metadata.name\n      operator: In\n      values: [ kube-system ]" > /dev/null
 # test priorityClassName option
 	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.frontend.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.backend.priorityClassName="system-cluster-critical" > /dev/null
```

  Is it required to split it? Maybe we can also join with `controllerManager.manager.priorityClassName` case? WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T08:33:08Z

- **charts/kueue/tests/kueueviz_test.yaml:113** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T08:33:08Z
```diff
@@ -0,0 +1,113 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          priorityClassName: ""
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          priorityClassName: ""
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should not render the ingress value in backend if set
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            # -- KueueViz dashboard frontend ingress class name
+            ingressClassName: "ingress-nginx"
+            # -- KueueViz dashboard frontend ingress host
+            host: "backend.kueueviz.local"
+            # -- KueueViz dashboard frontend ingress tls secret name
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.local"
+  - it: Render Backend Ingress
+    template: kueueviz/backend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            # -- KueueViz dashboard backend ingress class name
+            ingressClassName: "ingress-internal"
+            # -- KueueViz dashboard backend ingress host
+            host: "backend.kueueviz.in"
+            # -- KueueViz dashboard backend ingress tls secret name
+            tlsSecretName: "kueueviz-backend-tls"
+    asserts:
+      - equal:
+          path: spec.ingressClassName
+          value: "ingress-internal"
+      - equal:
+          path: spec.rules[0].host
+          value: "backend.kueueviz.in"
+      - equal:
+          path: spec.tls[0].secretName
+          value: "kueueviz-backend-tls"
+
+  - it: Render Frontend Ingress
+    template: kueueviz/frontend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          ingress:
+            # -- KueueViz dashboard frontend ingress class name
+            ingressClassName: "ingress-external"
+            # -- KueueViz dashboard frontend ingress host
+            host: "frontend.kueueviz.in"
+            # -- KueueViz dashboard frontend ingress tls secret name
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.ingressClassName
+          value: "ingress-external"
+      - equal:
+          path: spec.rules[0].host
+          value: "frontend.kueueviz.in"
+      - equal:
+          path: spec.tls[0].secretName
+          value: "kueueviz-frontend-tls"
```

  nit: new line

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T08:35:21Z

- **hack/processing-plan.yaml:577** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T08:35:21Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  Yeah, we're trying to use yaml-processor to generate all the charts for consistency.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T08:37:59Z

- **Makefile:186** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T08:37:59Z
```diff
@@ -180,6 +180,10 @@ helm-verify: helm helm-lint ## run helm template and detect any rendering failur
 	$(HELM) template charts/kueue --set managerConfig.controllerManagerConfigYaml="managedJobsNamespaceSelector:\n  matchExpressions:\n    - key: kubernetes.io/metadata.name\n      operator: In\n      values: [ kube-system ]" > /dev/null
 # test priorityClassName option
 	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.frontend.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.backend.priorityClassName="system-cluster-critical" > /dev/null
```

  It looks like `helm-verify` is growing, and I think it would be a good idea to move it to a separate `.sh` file. But that can be done in a follow-up.

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-25T08:38:05Z

- **Makefile:186** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T08:38:05Z
```diff
@@ -180,6 +180,10 @@ helm-verify: helm helm-lint ## run helm template and detect any rendering failur
 	$(HELM) template charts/kueue --set managerConfig.controllerManagerConfigYaml="managedJobsNamespaceSelector:\n  matchExpressions:\n    - key: kubernetes.io/metadata.name\n      operator: In\n      values: [ kube-system ]" > /dev/null
 # test priorityClassName option
 	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.frontend.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.backend.priorityClassName="system-cluster-critical" > /dev/null
```

  its there already as a separate test, also i think let it be like this kueueViz is optional component so i think we can test them separtely

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T08:40:36Z

- **charts/kueue/values.yaml:26** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T08:40:35Z
```diff
@@ -23,7 +23,7 @@ controllerManager:
   #    enabled: true
   manager:
     # -- ControllerManager's pod priorityClassName
-    priorityClassName:
+    priorityClassName: ""
```

  We already discussed it in https://github.com/kubernetes-sigs/kueue/pull/5641#discussion_r2145335120. And we take a decision to leave `priorityClassName` blank instead of empty string.

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-25T08:59:26Z

- **hack/processing-plan.yaml:577** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T08:59:26Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  Yes as i said the manager component is already not templates using it, i think we can maybe run better tests.I am not denying usefulness but its lot of extra work to generate template for another template engine.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T10:12:02Z

- **hack/processing-plan.yaml:577** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T10:12:01Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  > Yes as i said the manager component is already not templates using it
  
  Yes, we need to use it for manager component as well.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T10:18:33Z

- **hack/processing-plan.yaml:577** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T10:18:33Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  What’s the problem with configuring yaml-processor? To update a template, you just add a new operation in processing-plan.yaml. Or I missing something?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T10:21:01Z

- **charts/kueue/tests/kueueviz_test.yaml:57** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T10:21:01Z
```diff
@@ -0,0 +1,113 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should not render the ingress value in backend if set
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            # -- KueueViz dashboard frontend ingress class name
```

  nit: do we need this comments in tests?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T10:24:53Z

- **charts/kueue/tests/kueueviz_test.yaml:8** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T10:24:52Z
```diff
@@ -0,0 +1,113 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided
```

  ```suggestion
    - it: should set the pod priorityClassName on the backend Deployment when provided
  ```
  
  Maybe like this? We have a similar test case for the frontend.

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-25T10:29:26Z

- **charts/kueue/tests/kueueviz_test.yaml:8** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T10:29:26Z
```diff
@@ -0,0 +1,113 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided
```

  ok i will do the chnages.for the other test cases as well.

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-25T10:30:10Z

- **charts/kueue/tests/kueueviz_test.yaml:57** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T10:30:10Z
```diff
@@ -0,0 +1,113 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should not render the ingress value in backend if set
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            # -- KueueViz dashboard frontend ingress class name
```

  we can probably remove them,i will work on that

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-25T10:32:31Z

- **hack/processing-plan.yaml:577** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T10:32:31Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  I think to add multiline helm blocks there might be some issues while setting it also the append on list items can create unexpected result,if you believe that's an absolute must then i will probably check that as well.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-25T12:28:35Z

- **hack/processing-plan.yaml:577** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-25T12:28:35Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  > I think to add multiline helm blocks there might be some issues while setting it
  
  Yes, for this you can use `postOperations` and `INSERT_TEXT` it should add multiline block. For example:
  
  https://github.com/kubernetes-sigs/kueue/blob/ca21c269cd4308b06460dea6b2c01b095620cbc1/hack/processing-plan.yaml#L21C5-L28

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-25T12:44:04Z

- **hack/processing-plan.yaml:577** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-25T12:44:03Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  I will give it a try then, also i found there is imagePullSecrets Missing in components on kueueViz

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-27T14:45:13Z

- **hack/processing-plan.yaml:577** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-27T14:45:12Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  can you help with this please:
  
  i want to add:
  ```yaml
  
    {{- if .Values.kueueViz.frontend.ingress.ingressClassName }}
    ingressClassName: {{ .Values.kueueViz.frontend.ingress.ingressClassName | default "ingress-nginx" }}
    {{- end }}
    
    ```
    
    i cant to add it via post operations since its not generic to all ingress and metadata.name will not work as condition,
    can you help me with it also operations support multi line substitution ? i don't see any example.
   
    i still think we need to find better solution.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-27T15:13:14Z

- **hack/processing-plan.yaml:577** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-27T15:13:14Z
```diff
@@ -574,58 +574,6 @@ files:
             {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-workload-codeflare-dev-v1beta2-appwrapper"'
-  - path: ./config/components/kueueviz/*.yaml
```

  Try this one:
  
  ```yaml
      ....
      postOperations:
         ....
        - type: INSERT_TEXT
          key: .spec
          value: |
            {{- if .Values.kueueViz.backend.ingress.ingressClassName }}
            ingressClassName: {{ .Values.kueueViz.backend.ingress.ingressClassName | default "ingress-nginx" }}
            {{- end }}
          indentation: 2
          onFileCondition: '.kind == "Ingress" and .metadata.name | contains("kueueviz-backend-ingress")'
        - type: INSERT_TEXT
          key: .spec
          value: |
            {{- if .Values.kueueViz.frontend.ingress.ingressClassName }}
            ingressClassName: {{ .Values.kueueViz.frontend.ingress.ingressClassName | default "ingress-nginx" }}
            {{- end }}
          indentation: 2
          onFileCondition: '.kind == "Ingress" and .metadata.name | contains("kueueviz-frontend-ingress")'
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T11:23:41Z

- **hack/processing-plan.yaml:656** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:23:41Z
```diff
@@ -603,105 +620,75 @@ files:
         key: .spec.rules.[].http.paths.[].backend.service.name
         value: '"{{ include \"kueue.fullname\" . }}-"'
         onFileCondition: '.kind == "Ingress"'
-      - type: APPEND
-        key: .spec.tls.[].secretName
-        value: '"{{ include \"kueue.fullname\" . }}-"'
-        onFileCondition: '.kind == "Ingress"'
       - type: APPEND
         key: .roleRef.name
         value: '"{{ include \"kueue.fullname\" . }}-"'
         onFileCondition: '.kind == "ClusterRoleBinding"'
       - type: UPDATE
         key: .subjects.[].namespace
         value: '"{{ .Release.Namespace }}"'
+        onFileCondition: 'kind == "Deployment"'
       - type: UPDATE
         key: .metadata.namespace
         value: '"{{ .Release.Namespace }}"'
-    postOperations:
-      - type: INSERT_TEXT
-        position: START
-        value: |
-          {{- if .Values.enableKueueViz }}
-      - type: INSERT_TEXT
-        position: END
+      - type: INSERT_OBJECT
+        addKeyIfMissing: true
+        key: .spec.template.spec.containers[0].env
+        onFileCondition: '.kind == "Deployment" and .metadata.name | contains("kueueviz-frontend")'
         value: |
-          {{- end }}
-  - path: ./config/components/internalcert/*.yaml
-    outputDir: ./charts/kueue/templates/internalcert
-    excludes:
-      - kustomization.yaml
-    continueOnError: true
-    operations:
-      - type: APPEND
-        key: .metadata.name
-        value: '"{{ include \"kueue.fullname\" . }}-"'
-      - type: UPDATE
-        key: .metadata.namespace
-        value: '"{{ .Release.Namespace }}"'
+            - name: KUEUEVIZ_BACKEND_URL
+              value: wss://{{ .Values.kueueViz.backend.ingress.host | default "backend.kueueviz.local" }}
     postOperations:
       - type: INSERT_TEXT
-        key: .metadata
-        value: |
-          labels:
-          {{- include "kueue.labels" . | nindent 4 }}
+        key: .spec
         indentation: 2
-      - type: INSERT_TEXT
-        position: START
         value: |
-          {{- if not .Values.enableCertManager }}
+          tls:
+            - hosts:
+              - {{ .Values.kueueViz.backend.ingress.host | default "backend.kueueviz.local" }}
+              secretName: '{{ .Values.kueueViz.backend.ingress.tlsSecretName | default "kueueviz-backend-tls" }}'
+        onFileCondition: '.kind == "Ingress" and .metadata.name | contains("kueueviz-backend-ingress")'
       - type: INSERT_TEXT
-        position: END
+        key: .spec
+        indentation: 2
         value: |
-          {{- end }}
-  - path: ./config/components/prometheus/*.yaml
```

  Please revert it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T11:24:18Z

- **hack/processing-plan.yaml:629** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:24:18Z
```diff
@@ -603,105 +620,75 @@ files:
         key: .spec.rules.[].http.paths.[].backend.service.name
         value: '"{{ include \"kueue.fullname\" . }}-"'
         onFileCondition: '.kind == "Ingress"'
-      - type: APPEND
-        key: .spec.tls.[].secretName
-        value: '"{{ include \"kueue.fullname\" . }}-"'
-        onFileCondition: '.kind == "Ingress"'
       - type: APPEND
         key: .roleRef.name
         value: '"{{ include \"kueue.fullname\" . }}-"'
         onFileCondition: '.kind == "ClusterRoleBinding"'
       - type: UPDATE
         key: .subjects.[].namespace
         value: '"{{ .Release.Namespace }}"'
+        onFileCondition: 'kind == "Deployment"'
       - type: UPDATE
         key: .metadata.namespace
         value: '"{{ .Release.Namespace }}"'
-    postOperations:
-      - type: INSERT_TEXT
-        position: START
-        value: |
-          {{- if .Values.enableKueueViz }}
-      - type: INSERT_TEXT
-        position: END
+      - type: INSERT_OBJECT
+        addKeyIfMissing: true
+        key: .spec.template.spec.containers[0].env
+        onFileCondition: '.kind == "Deployment" and .metadata.name | contains("kueueviz-frontend")'
         value: |
-          {{- end }}
-  - path: ./config/components/internalcert/*.yaml
```

  And this.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T11:25:49Z

- **hack/processing-plan.yaml:538** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:25:49Z
```diff
@@ -361,217 +361,217 @@ files:
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-batch-v1-job"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-jobset-x-k8s-io-v1alpha2-jobset"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-jaxjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-paddlejob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-pytorchjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-tfjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-xgboostjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-leaderworkerset-x-k8s-io-v1-leaderworkerset"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v2beta1-mpijob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-ray-io-v1-raycluster"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-ray-io-v1-rayjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-workload-codeflare-dev-v1beta2-appwrapper"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-batch-v1-job"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-jobset-x-k8s-io-v1alpha2-jobset"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-jaxjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-paddlejob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-pytorchjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-tfjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-xgboostjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
```

  Could you please create a separate PR for the formatting changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T11:31:41Z

- **charts/kueue/templates/kueueviz/backend-ingress.yaml:12** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:31:41Z
```diff
@@ -8,12 +8,15 @@ metadata:
     nginx.ingress.kubernetes.io/rewrite-target: /
     nginx.ingress.kubernetes.io/ssl-redirect: "true"
 spec:
+  {{- if .Values.kueueViz.backend.ingress.ingressClassName }}
+  ingressClassName: {{ .Values.kueueViz.backend.ingress.ingressClassName | default "ingress-nginx" }}
```

  ```suggestion
    ingressClassName: {{ .Values.kueueViz.backend.ingress.ingressClassName }}
  ```
  
  Do we need default if we have if condition?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T11:32:06Z

- **charts/kueue/templates/kueueviz/backend-ingress.yaml:29** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:32:06Z
```diff
@@ -23,4 +26,4 @@ spec:
                 name: '{{ include "kueue.fullname" . }}-kueueviz-backend'
                 port:
                   number: 8080
-{{- end }}
+{{- end }}
```

  Why this changes happens?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T11:32:56Z

- **charts/kueue/templates/kueueviz/backend-ingress.yaml:12** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:32:56Z
```diff
@@ -8,12 +8,15 @@ metadata:
     nginx.ingress.kubernetes.io/rewrite-target: /
     nginx.ingress.kubernetes.io/ssl-redirect: "true"
 spec:
+  {{- if .Values.kueueViz.backend.ingress.ingressClassName }}
+  ingressClassName: {{ .Values.kueueViz.backend.ingress.ingressClassName | default "ingress-nginx" }}
```

  Or maybe we don't need if condition?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T11:33:15Z

- **charts/kueue/templates/kueueviz/cluster-role-binding.yaml:14** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:33:15Z
```diff
@@ -11,5 +11,5 @@ roleRef:
 subjects:
   - kind: ServiceAccount
     name: default
-    namespace: '{{ .Release.Namespace }}'
-{{- end }}
+    namespace: system
```

  Please revert.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T11:36:46Z

- **charts/kueue/templates/kueueviz/frontend-deployment.yaml:38** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:36:46Z
```diff
@@ -14,20 +14,27 @@ spec:
       labels:
         app: kueueviz-frontend
     spec:
+    {{- with .Values.kueueViz.imagePullSecrets  }}
+      imagePullSecrets:
+      {{- toYaml . | nindent 8 }}
+    {{- end }}
+    {{- with  .Values.kueueViz.priorityClassName }}
+      priorityClassName: {{ . }}
+    {{- end }}
       containers:
         - name: frontend
           image: '{{ .Values.kueueViz.frontend.image.repository }}:{{ .Values.kueueViz.frontend.image.tag | default .Chart.AppVersion }}'
           imagePullPolicy: '{{ .Values.kueueViz.frontend.image.pullPolicy }}'
           ports:
             - containerPort: 8080
-          env:
-            - name: REACT_APP_WEBSOCKET_URL
-              value: "wss://backend.kueueviz.local"
           resources:
             limits:
               cpu: 500m
               memory: 512Mi
             requests:
               cpu: 500m
               memory: 512Mi
-{{- end }}
+          env:
+            - name: KUEUEVIZ_BACKEND_URL
```

  ```suggestion
              - name: REACT_APP_WEBSOCKET_URL
  ```
  
  Why this variables is changed?

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-06-30T12:17:07Z

- **hack/processing-plan.yaml:538** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-30T12:17:07Z
```diff
@@ -361,217 +361,217 @@ files:
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-batch-v1-job"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-jobset-x-k8s-io-v1alpha2-jobset"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-jaxjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-paddlejob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-pytorchjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-tfjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v1-xgboostjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-leaderworkerset-x-k8s-io-v1-leaderworkerset"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-kubeflow-org-v2beta1-mpijob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-ray-io-v1-raycluster"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "ValidatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/validate-ray-io-v1-rayjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-workload-codeflare-dev-v1beta2-appwrapper"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-batch-v1-job"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-jobset-x-k8s-io-v1alpha2-jobset"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-jaxjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-paddlejob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-pytorchjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-tfjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
         onFileCondition: '.kind == "MutatingWebhookConfiguration"'
         onItemCondition: '.webhooks.[].clientConfig.service.path == "/mutate-kubeflow-org-v1-xgboostjob"'
       - type: INSERT_TEXT
         key: .webhooks.[].name
         value: |
-            {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
-            namespaceSelector:
-              {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
-            {{- end }}
+          {{- if (hasKey $managerConfig "managedJobsNamespaceSelector") }}
+          namespaceSelector:
+            {{- toYaml $managerConfig.managedJobsNamespaceSelector | nindent 6 }}
+          {{- end }}
```

  There is only generation changes, This PR went too big with most garbage.
  
  i will create a new MR and refer this one as ref so we don't loose the details.
