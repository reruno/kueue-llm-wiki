# PR #5796: refactor: improve error handling with client.IgnoreNotFound in controllers

**Summary**: refactor: improve error handling with client.IgnoreNotFound in controllers

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5796

**Last updated**: 2025-07-01T15:18:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@xigang](https://github.com/xigang)
- **Branch**: `not_found` → `main`
- **Created**: 2025-06-28T03:12:04Z
- **Updated**: 2025-07-01T15:18:51Z
- **Merged**: 2025-07-01T05:44:31Z
- **Closed**: 2025-07-01T05:44:31Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 6   **+37 / -26**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

/kind bug

#### What this PR does / why we need it:

follow-up: https://github.com/kubernetes-sigs/kueue/pull/5786#issuecomment-3012681011


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-28T03:12:14Z

Hi @xigang. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-28T03:12:33Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 51f2814c041eca29348945573cbff9db7b93a0f6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6862aa6fc8b9f200083d4d9b |

### Comment by [@xigang](https://github.com/xigang) — 2025-06-28T03:13:46Z

/cc @mbobrovskyi 

PTAL.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-28T04:27:57Z

/ok-to-test

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-28T04:46:36Z

Also maybe here:

https://github.com/kubernetes-sigs/kueue/blob/89f1c037aab465d5d8faf3b0668e5c7cf10c8b46/pkg/controller/admissionchecks/multikueue/admissioncheck.go#L128

https://github.com/kubernetes-sigs/kueue/blob/89f1c037aab465d5d8faf3b0668e5c7cf10c8b46/pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go#L61

https://github.com/kubernetes-sigs/kueue/blob/89f1c037aab465d5d8faf3b0668e5c7cf10c8b46/pkg/controller/admissionchecks/multikueue/admissioncheck.go#L124

https://github.com/kubernetes-sigs/kueue/blob/89f1c037aab465d5d8faf3b0668e5c7cf10c8b46/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L425

### Comment by [@xigang](https://github.com/xigang) — 2025-06-28T09:12:34Z

> Also maybe here:
> 
> https://github.com/kubernetes-sigs/kueue/blob/89f1c037aab465d5d8faf3b0668e5c7cf10c8b46/pkg/controller/admissionchecks/multikueue/admissioncheck.go#L128
> 
> https://github.com/kubernetes-sigs/kueue/blob/89f1c037aab465d5d8faf3b0668e5c7cf10c8b46/pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go#L61
> 
> https://github.com/kubernetes-sigs/kueue/blob/89f1c037aab465d5d8faf3b0668e5c7cf10c8b46/pkg/controller/admissionchecks/multikueue/admissioncheck.go#L124
> 
> https://github.com/kubernetes-sigs/kueue/blob/89f1c037aab465d5d8faf3b0668e5c7cf10c8b46/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L425

Done.

### Comment by [@xigang](https://github.com/xigang) — 2025-06-30T06:13:49Z

> What if the cache still has non-existent objects?
Did you check if there is no any path to have outdated cache information?

Here, we check whether the object exists when sending an Update request to the APIServer using client.IgnoreNotFound, and we do not use data from the informer cache.

### Comment by [@xigang](https://github.com/xigang) — 2025-06-30T06:32:51Z

@mbobrovskyi PTAL. Thanks!

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-30T07:07:16Z

> Here, we check whether the object exists when sending an Update request to the APIServer using client.IgnoreNotFound, and we do not use data from the informer cache.

I meant Kueue cache mechanism instead of informer cache.

### Comment by [@xigang](https://github.com/xigang) — 2025-06-30T07:19:51Z

@tenzen-y The Kueue cache can indeed run into stale data issues. What I’m thinking is to ensure eventual consistency by handling Delete events in the Informer’s EventHandler. Alternatively, we could proactively remove the object from the Kueue cache when a NotFound error occurs.

Any thoughts on this?

### Comment by [@xigang](https://github.com/xigang) — 2025-06-30T10:09:37Z

@mbobrovskyi Fixed, please take another look.😄

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:05:15Z

Wow. Thank you for the quick fix!

Overall LGTM. Just one point.

I think we should add an IgnoreNotFound for this:

https://github.com/kubernetes-sigs/kueue/blob/0cd304dfaf87dc672b7aef79f72816de07281c65/pkg/controller/core/workload_controller.go#L339-L358

But it should be placed outside the function, not inside. WDYT?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-30T11:30:37Z

> @tenzen-y The Kueue cache can indeed run into stale data issues. What I’m thinking is to ensure eventual consistency by handling Delete events in the Informer’s EventHandler. Alternatively, we could proactively remove the object from the Kueue cache when a NotFound error occurs.
> 
> Any thoughts on this?

As I checked the controller-runtime implementations, the reconcile requests are directly pushed to the workqueue w/o predicate and event handlers when reconciliation failed: https://github.com/kubernetes-sigs/controller-runtime/blob/cacd627d0b9bc46b592cf5f96f57f1308313618e/pkg/internal/controller/controller.go#L345-L347

So, NVM my above comment.

### Comment by [@xigang](https://github.com/xigang) — 2025-06-30T11:35:20Z

> But it should be placed outside the function, not inside. WDYT?

@mbobrovskyi I personally feel we might not want to expose `IgnoreNotFound` externally. It might be cleaner and more appropriate to keep it together with `ApplyAdmissionStatus`. What do you think?

### Comment by [@xigang](https://github.com/xigang) — 2025-06-30T11:37:07Z

@tenzen-y That's right — if the reconcile fails, the key will be requeued into the workqueue.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:42:19Z

> @mbobrovskyi I personally feel we might not want to expose IgnoreNotFound externally. It might be cleaner and more appropriate to keep it together with ApplyAdmissionStatus. What do you think?

In that case, we would need to duplicate the check multiple times within the function. The tricky part is that we can't easily distinguish whether it's a genuine "not found" error or just a case where the object isn't returned but no error is raised. As a result, in Reconcile(), we might proceed to the next step even if it's actually a NotFound error.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T11:43:36Z

Could you please also close resolved comments? It is very hard to understand which one is Done.

### Comment by [@xigang](https://github.com/xigang) — 2025-06-30T11:59:50Z

> Could you please also close resolved comments? It is very hard to understand which one is Done.

All resolved comments have been closed.

### Comment by [@xigang](https://github.com/xigang) — 2025-06-30T15:03:20Z


> I think we should add an IgnoreNotFound for this:
> 
> https://github.com/kubernetes-sigs/kueue/blob/0cd304dfaf87dc672b7aef79f72816de07281c65/pkg/controller/core/workload_controller.go#L339-L358
> 
> But it should be placed outside the function, not inside. WDYT?

@mbobrovskyi Fixed. Please take another look.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-30T17:53:54Z

LGTM label has been added.  <details>Git tree hash: a04c20c1203eabafc961ff482948ee4ccaadb99b</details>

### Comment by [@xigang](https://github.com/xigang) — 2025-07-01T00:02:02Z

@mimowo @tenzen-y For approve. Thanks!

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-01T05:18:29Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-01T05:18:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5796#pullrequestreview-2972164531" title="LGTM">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5796#issuecomment-3021814213" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5796#" title="Author self-approved">xigang</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@xigang](https://github.com/xigang) — 2025-07-01T14:50:07Z

@tenzen-y I'll check it now.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T15:01:21Z

@tenzen-y Thank you for the thorough review!
@xigang Could you please create a follow-up PR to address this?

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-28T04:41:04Z

- **pkg/controller/core/workload_controller.go:176** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-28T04:31:54Z
```diff
@@ -169,7 +169,11 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	if len(wl.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
 		// manual deletion triggered by the user
-		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
+		err := workload.RemoveFinalizer(ctx, r.client, &wl)
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		return ctrl.Result{}, nil
```

  ```suggestion
  			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
  			return ctrl.Result{}, client.IgnoreNotFound(err)
  ```
  
  And also other places.

- **pkg/controller/core/workload_controller.go:277** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-28T04:34:05Z
```diff
@@ -264,8 +272,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		reportWorkloadEvictedOnce := workload.WorkloadEvictionStateInc(&wl, kueue.WorkloadDeactivated, "")
 		updated = workload.ResetChecksOnEviction(&wl, r.clock.Now()) || updated
 		if updated {
-			if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
-				return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			if client.IgnoreNotFound(err) != nil {
+				return ctrl.Result{}, err
```

  ```suggestion
  				return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
  ```
  
  This is not related changes.

### Review by [@xigang](https://github.com/xigang) (commented) — 2025-06-28T09:12:03Z

- **pkg/controller/core/workload_controller.go:277** by [@xigang](https://github.com/xigang) — 2025-06-28T09:12:03Z
```diff
@@ -264,8 +272,9 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		reportWorkloadEvictedOnce := workload.WorkloadEvictionStateInc(&wl, kueue.WorkloadDeactivated, "")
 		updated = workload.ResetChecksOnEviction(&wl, r.clock.Now()) || updated
 		if updated {
-			if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
-				return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			if client.IgnoreNotFound(err) != nil {
+				return ctrl.Result{}, err
```

  Done.

### Review by [@xigang](https://github.com/xigang) (commented) — 2025-06-28T09:12:16Z

- **pkg/controller/core/workload_controller.go:176** by [@xigang](https://github.com/xigang) — 2025-06-28T09:12:16Z
```diff
@@ -169,7 +169,11 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	if len(wl.OwnerReferences) == 0 && !wl.DeletionTimestamp.IsZero() {
 		// manual deletion triggered by the user
-		return ctrl.Result{}, workload.RemoveFinalizer(ctx, r.client, &wl)
+		err := workload.RemoveFinalizer(ctx, r.client, &wl)
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		return ctrl.Result{}, nil
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-06-30T04:42:41Z

What if the cache still has non-existent objects?
Did you check if there is no any path to have outdated cache information?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T08:25:23Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:128** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T08:25:23Z
```diff
@@ -125,7 +125,7 @@ func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 		if err != nil {
 			log.V(2).Error(err, "Updating check condition", "newCondition", newCondition)
 		}
```

  ```suggestion
  		if client.IgnoreNotFound(err) != nil {
  			log.V(2).Error(err, "Updating check condition", "newCondition", newCondition)
  			return reconcile.Result{}, client.IgnoreNotFound(err)
  		}
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T08:30:19Z

- **pkg/controller/core/workload_controller.go:246** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T08:30:18Z
```diff
@@ -238,7 +239,11 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if updated {
-			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			if client.IgnoreNotFound(err) != nil {
+				return ctrl.Result{}, err
+			}
+			return ctrl.Result{}, nil
```

  ```suggestion
  			return ctrl.Result{}, client.IgnoreNotFound(err)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T08:30:53Z

- **pkg/controller/core/workload_controller.go:300** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T08:30:53Z
```diff
@@ -287,20 +293,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	lqActive := ptr.Deref(lq.Spec.StopPolicy, kueue.None) == kueue.None
 	if lqExists && lqActive && isDisabledRequeuedByLocalQueueStopped(&wl) {
 		workload.SetRequeuedCondition(&wl, kueue.WorkloadLocalQueueRestarted, "The LocalQueue was restarted after being stopped", true)
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		return ctrl.Result{}, nil
```

  ```suggestion
  		return ctrl.Result{}, client.IgnoreNotFound(err)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T08:31:49Z

- **pkg/controller/core/workload_controller.go:318** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T08:31:49Z
```diff
@@ -287,20 +293,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	lqActive := ptr.Deref(lq.Spec.StopPolicy, kueue.None) == kueue.None
 	if lqExists && lqActive && isDisabledRequeuedByLocalQueueStopped(&wl) {
 		workload.SetRequeuedCondition(&wl, kueue.WorkloadLocalQueueRestarted, "The LocalQueue was restarted after being stopped", true)
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		return ctrl.Result{}, nil
 	}
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
 		// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
 		cq := kueue.ClusterQueue{}
-		if err := r.client.Get(ctx, types.NamespacedName{Name: string(cqName)}, &cq); err != nil {
+		err := r.client.Get(ctx, types.NamespacedName{Name: string(cqName)}, &cq)
+		if client.IgnoreNotFound(err) != nil {
 			return ctrl.Result{}, err
 		}
 		// If stopped cluster queue is started we need to set the WorkloadRequeued condition to true.
 		if isDisabledRequeuedByClusterQueueStopped(&wl) && ptr.Deref(cq.Spec.StopPolicy, kueue.None) == kueue.None {
 			workload.SetRequeuedCondition(&wl, kueue.WorkloadClusterQueueRestarted, "The ClusterQueue was restarted after being stopped", true)
-			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			if client.IgnoreNotFound(err) != nil {
+				return ctrl.Result{}, err
+			}
+			return ctrl.Result{}, nil
```

  ```suggestion
  			return ctrl.Result{}, client.IgnoreNotFound(err)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T08:41:15Z

- **pkg/controller/core/workload_controller.go:439** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T08:41:15Z
```diff
@@ -419,7 +435,8 @@ func (r *WorkloadReconciler) reconcileMaxExecutionTime(ctx context.Context, wl *
 	if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadDeactivationTarget) {
 		workload.SetDeactivationTarget(wl, kueue.WorkloadMaximumExecutionTimeExceeded, "exceeding the maximum execution time")
 		wl.Status.AccumulatedPastExexcutionTimeSeconds = nil
-		if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock); err != nil {
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		if client.IgnoreNotFound(err) != nil {
```

  Please revert this change and check the usage of reconcileMaxExecutionTime in the Reconcile function:
  
  https://github.com/kubernetes-sigs/kueue/blob/9c4ab1a87fbb1889247c4a4e92d231bee6732e31/pkg/controller/core/workload_controller.go#L364-L367
  
  With the current implementation, reconciliation proceeds to the next steps, but we actually need to stop it on the error.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T08:43:08Z

- **pkg/controller/core/workload_controller.go:531** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T08:43:08Z
```diff
@@ -510,13 +527,15 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 	if !lqExists || !lq.DeletionTimestamp.IsZero() {
 		log.V(3).Info("Workload is inadmissible because the LocalQueue is terminating or missing", "localQueue", klog.KRef("", string(wl.Spec.QueueName)))
 		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("LocalQueue %s is terminating or missing", wl.Spec.QueueName), r.clock.Now())
-		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		return true, client.IgnoreNotFound(err)
```

  Also here is the same.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T08:43:27Z

- **pkg/controller/core/workload_controller.go:538** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T08:43:26Z
```diff
@@ -510,13 +527,15 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 	if !lqExists || !lq.DeletionTimestamp.IsZero() {
 		log.V(3).Info("Workload is inadmissible because the LocalQueue is terminating or missing", "localQueue", klog.KRef("", string(wl.Spec.QueueName)))
 		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("LocalQueue %s is terminating or missing", wl.Spec.QueueName), r.clock.Now())
-		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		return true, client.IgnoreNotFound(err)
 	}
 
 	if queueStopPolicy != kueue.None {
 		log.V(3).Info("Workload is inadmissible because the LocalQueue is stopped", "localQueue", klog.KRef("", string(wl.Spec.QueueName)))
 		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("LocalQueue %s is stopped", wl.Spec.QueueName), r.clock.Now())
-		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		return true, client.IgnoreNotFound(err)
```

  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T08:44:05Z

- **pkg/controller/core/workload_controller.go:582** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T08:44:04Z
```diff
@@ -559,13 +578,15 @@ func (r *WorkloadReconciler) reconcileOnClusterQueueActiveState(ctx context.Cont
 	if !cqExists || !cq.DeletionTimestamp.IsZero() {
 		log.V(3).Info("Workload is inadmissible because the ClusterQueue is terminating or missing", "clusterQueue", klog.KRef("", string(cqName)))
 		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("ClusterQueue %s is terminating or missing", cqName), r.clock.Now())
-		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		return true, client.IgnoreNotFound(err)
```

  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T08:44:09Z

- **pkg/controller/core/workload_controller.go:589** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T08:44:09Z
```diff
@@ -559,13 +578,15 @@ func (r *WorkloadReconciler) reconcileOnClusterQueueActiveState(ctx context.Cont
 	if !cqExists || !cq.DeletionTimestamp.IsZero() {
 		log.V(3).Info("Workload is inadmissible because the ClusterQueue is terminating or missing", "clusterQueue", klog.KRef("", string(cqName)))
 		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("ClusterQueue %s is terminating or missing", cqName), r.clock.Now())
-		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		return true, client.IgnoreNotFound(err)
 	}
 
 	if queueStopPolicy != kueue.None {
 		log.V(3).Info("Workload is inadmissible because the ClusterQueue is stopped", "clusterQueue", klog.KRef("", string(cqName)))
 		_ = workload.UnsetQuotaReservationWithCondition(wl, kueue.WorkloadInadmissible, fmt.Sprintf("ClusterQueue %s is stopped", cqName), r.clock.Now())
-		return true, workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock)
+		return true, client.IgnoreNotFound(err)
```

  And here.

### Review by [@xigang](https://github.com/xigang) (commented) — 2025-06-30T09:00:20Z

- **pkg/controller/admissionchecks/multikueue/admissioncheck.go:128** by [@xigang](https://github.com/xigang) — 2025-06-30T09:00:20Z
```diff
@@ -125,7 +125,7 @@ func (a *ACReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 		if err != nil {
 			log.V(2).Error(err, "Updating check condition", "newCondition", newCondition)
 		}
```

  Done.

### Review by [@xigang](https://github.com/xigang) (commented) — 2025-06-30T09:58:41Z

- **pkg/controller/core/workload_controller.go:246** by [@xigang](https://github.com/xigang) — 2025-06-30T09:58:41Z
```diff
@@ -238,7 +239,11 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if updated {
-			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			if client.IgnoreNotFound(err) != nil {
+				return ctrl.Result{}, err
+			}
+			return ctrl.Result{}, nil
```

  Done

### Review by [@xigang](https://github.com/xigang) (commented) — 2025-06-30T09:59:52Z

- **pkg/controller/core/workload_controller.go:300** by [@xigang](https://github.com/xigang) — 2025-06-30T09:59:52Z
```diff
@@ -287,20 +293,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	lqActive := ptr.Deref(lq.Spec.StopPolicy, kueue.None) == kueue.None
 	if lqExists && lqActive && isDisabledRequeuedByLocalQueueStopped(&wl) {
 		workload.SetRequeuedCondition(&wl, kueue.WorkloadLocalQueueRestarted, "The LocalQueue was restarted after being stopped", true)
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		return ctrl.Result{}, nil
```

  Done

### Review by [@xigang](https://github.com/xigang) (commented) — 2025-06-30T10:00:19Z

- **pkg/controller/core/workload_controller.go:318** by [@xigang](https://github.com/xigang) — 2025-06-30T10:00:18Z
```diff
@@ -287,20 +293,29 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	lqActive := ptr.Deref(lq.Spec.StopPolicy, kueue.None) == kueue.None
 	if lqExists && lqActive && isDisabledRequeuedByLocalQueueStopped(&wl) {
 		workload.SetRequeuedCondition(&wl, kueue.WorkloadLocalQueueRestarted, "The LocalQueue was restarted after being stopped", true)
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		if client.IgnoreNotFound(err) != nil {
+			return ctrl.Result{}, err
+		}
+		return ctrl.Result{}, nil
 	}
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
 		// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
 		cq := kueue.ClusterQueue{}
-		if err := r.client.Get(ctx, types.NamespacedName{Name: string(cqName)}, &cq); err != nil {
+		err := r.client.Get(ctx, types.NamespacedName{Name: string(cqName)}, &cq)
+		if client.IgnoreNotFound(err) != nil {
 			return ctrl.Result{}, err
 		}
 		// If stopped cluster queue is started we need to set the WorkloadRequeued condition to true.
 		if isDisabledRequeuedByClusterQueueStopped(&wl) && ptr.Deref(cq.Spec.StopPolicy, kueue.None) == kueue.None {
 			workload.SetRequeuedCondition(&wl, kueue.WorkloadClusterQueueRestarted, "The ClusterQueue was restarted after being stopped", true)
-			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			if client.IgnoreNotFound(err) != nil {
+				return ctrl.Result{}, err
+			}
+			return ctrl.Result{}, nil
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2025-06-30T17:53:48Z

/lgtm
Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-01T14:44:58Z

@xigang @mbobrovskyi @mimowo It looks like this PR introduced unexpected code paths.

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:427** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-30T21:48:44Z
```diff
@@ -422,7 +422,9 @@ func (c *clustersReconciler) Reconcile(ctx context.Context, req reconcile.Reques
 			return reconcile.Result{RequeueAfter: ptr.Deref(retryAfter, 0)}, nil
 		}
 	}
-	return reconcile.Result{}, c.updateStatus(ctx, cluster, true, "Active", "Connected")
+
+	err = c.updateStatus(ctx, cluster, true, "Active", "Connected")
+	return reconcile.Result{}, client.IgnoreNotFound(err)
```

  ```suggestion
  	return reconcile.Result{}, client.IgnoreNotFound(c.updateStatus(ctx, cluster, true, "Active", "Connected"))
  ```
  
  nit: we can directly return the error.

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-30T21:51:04Z
```diff
@@ -58,7 +58,9 @@ func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 
 	if currentCondition.Status != newCondition.Status {
 		apimeta.SetStatusCondition(&ac.Status.Conditions, newCondition)
-		return reconcile.Result{}, a.client.Status().Update(ctx, ac)
+		if err := a.client.Status().Update(ctx, ac); err != nil {
+			return reconcile.Result{}, client.IgnoreNotFound(err)
+		}
```

  ```suggestion
  		return reconcile.Result{}, client.IgnoreNotFound(a.client.Status().Update(ctx, ac))
  ```
  
  I think that this error checking is no needed.

- **pkg/controller/core/workload_controller.go:243** by [@tenzen-y](https://github.com/tenzen-y) — 2025-06-30T21:51:45Z
```diff
@@ -238,7 +239,8 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if updated {
-			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
```

  ```suggestion
  			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
  ```

- **pkg/controller/core/workload_controller.go:272** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T14:37:50Z
```diff
@@ -264,7 +266,8 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		reportWorkloadEvictedOnce := workload.WorkloadEvictionStateInc(&wl, kueue.WorkloadDeactivated, "")
 		updated = workload.ResetChecksOnEviction(&wl, r.clock.Now()) || updated
 		if updated {
-			if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			if client.IgnoreNotFound(err) != nil {
 				return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
 			}
```

  ```suggestion
  			if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
  				if apierrors.IsNotFound(err) {
  					return ctrl.Restul{}, nil
  				}
  				return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
  			}
  ```
  
  This could lead to an unexpected eviction count by Prometheus when the `err` is NotFound.

- **pkg/controller/core/workload_controller.go:294** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T14:38:19Z
```diff
@@ -287,20 +290,23 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	lqActive := ptr.Deref(lq.Spec.StopPolicy, kueue.None) == kueue.None
 	if lqExists && lqActive && isDisabledRequeuedByLocalQueueStopped(&wl) {
 		workload.SetRequeuedCondition(&wl, kueue.WorkloadLocalQueueRestarted, "The LocalQueue was restarted after being stopped", true)
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
```

  ```suggestion
  		return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
  ```

- **pkg/controller/core/workload_controller.go:303** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T14:40:32Z
```diff
@@ -287,20 +290,23 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	lqActive := ptr.Deref(lq.Spec.StopPolicy, kueue.None) == kueue.None
 	if lqExists && lqActive && isDisabledRequeuedByLocalQueueStopped(&wl) {
 		workload.SetRequeuedCondition(&wl, kueue.WorkloadLocalQueueRestarted, "The LocalQueue was restarted after being stopped", true)
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
 		// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
 		cq := kueue.ClusterQueue{}
-		if err := r.client.Get(ctx, types.NamespacedName{Name: string(cqName)}, &cq); err != nil {
+		err := r.client.Get(ctx, types.NamespacedName{Name: string(cqName)}, &cq)
+		if client.IgnoreNotFound(err) != nil {
 			return ctrl.Result{}, err
```

  ```suggestion
  		if err := r.client.Get(ctx, types.NamespacedName{Name: string(cqName)}, &cq); err != nil {
  			return ctrl.Result{}, client.IgnoreNotFound(err)
  		}
  ```
  
  This also leads to an unexpected following steps.

- **pkg/controller/core/workload_controller.go:309** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T14:41:00Z
```diff
@@ -287,20 +290,23 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	lqActive := ptr.Deref(lq.Spec.StopPolicy, kueue.None) == kueue.None
 	if lqExists && lqActive && isDisabledRequeuedByLocalQueueStopped(&wl) {
 		workload.SetRequeuedCondition(&wl, kueue.WorkloadLocalQueueRestarted, "The LocalQueue was restarted after being stopped", true)
-		return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
 		// because we need to react to API cluster cq events, the list of checks from a cache can lead to race conditions
 		cq := kueue.ClusterQueue{}
-		if err := r.client.Get(ctx, types.NamespacedName{Name: string(cqName)}, &cq); err != nil {
+		err := r.client.Get(ctx, types.NamespacedName{Name: string(cqName)}, &cq)
+		if client.IgnoreNotFound(err) != nil {
 			return ctrl.Result{}, err
 		}
 		// If stopped cluster queue is started we need to set the WorkloadRequeued condition to true.
 		if isDisabledRequeuedByClusterQueueStopped(&wl) && ptr.Deref(cq.Spec.StopPolicy, kueue.None) == kueue.None {
 			workload.SetRequeuedCondition(&wl, kueue.WorkloadClusterQueueRestarted, "The ClusterQueue was restarted after being stopped", true)
-			return ctrl.Result{}, workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
```

  ```suggestion
  			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
  ```

- **pkg/controller/core/workload_controller.go:321** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T14:42:17Z
```diff
@@ -310,7 +316,8 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	// If the workload is admitted, updating the status here would set the Admitted condition to
 	// false before the workloads eviction.
 	if !workload.IsAdmitted(&wl) && workload.SyncAdmittedCondition(&wl, r.clock.Now()) {
-		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+		if client.IgnoreNotFound(err) != nil {
 			return ctrl.Result{}, err
```

  ```suggestion
  		if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
  			return ctrl.Result{}, client.IgnoreNotFount(err)
  ```
  
  This is also a new bug.

- **pkg/controller/core/workload_controller.go:452** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T14:42:58Z
```diff
@@ -442,7 +449,7 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 		}
 		workload.SetDeactivationTarget(wl, kueue.WorkloadEvictedByAdmissionCheck, fmt.Sprintf("Admission check(s): %v, were rejected", stringsutils.Join(rejectedCheckNames, ",")))
 		if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock); err != nil {
-			return false, client.IgnoreNotFound(err)
+			return false, err
```

  Why do we need this change?
  I have the same question in a couple of other places.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T14:56:00Z

- **pkg/controller/core/workload_controller.go:272** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T14:56:00Z
```diff
@@ -264,7 +266,8 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		reportWorkloadEvictedOnce := workload.WorkloadEvictionStateInc(&wl, kueue.WorkloadDeactivated, "")
 		updated = workload.ResetChecksOnEviction(&wl, r.clock.Now()) || updated
 		if updated {
-			if err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock); err != nil {
+			err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock)
+			if client.IgnoreNotFound(err) != nil {
 				return ctrl.Result{}, fmt.Errorf("setting eviction: %w", err)
 			}
```

  Nice catch!

### Review by [@xigang](https://github.com/xigang) (commented) — 2025-07-01T15:02:47Z

- **pkg/controller/core/workload_controller.go:360** by [@xigang](https://github.com/xigang) — 2025-07-01T15:02:47Z
```diff
@@ -330,24 +337,24 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	if workload.HasQuotaReservation(&wl) {
 		if evictionTriggered, err := r.reconcileCheckBasedEviction(ctx, &wl); evictionTriggered || err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if updated, err := r.reconcileOnLocalQueueActiveState(ctx, &wl, lqExists, &lq); updated || err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if updated, err := r.reconcileOnClusterQueueActiveState(ctx, &wl, cqName); updated || err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		podsReadyRecheckAfter, err := r.reconcileNotReadyTimeout(ctx, req, &wl)
 		if err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 		maxExecRecheckAfter, err := r.reconcileMaxExecutionTime(ctx, &wl)
 		if err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		// get the minimun non-zero value
```

  @tenzen-y Does this part need to be reverted?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-01T15:07:06Z

- **pkg/controller/core/workload_controller.go:360** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-01T15:07:06Z
```diff
@@ -330,24 +337,24 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	if workload.HasQuotaReservation(&wl) {
 		if evictionTriggered, err := r.reconcileCheckBasedEviction(ctx, &wl); evictionTriggered || err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if updated, err := r.reconcileOnLocalQueueActiveState(ctx, &wl, lqExists, &lq); updated || err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if updated, err := r.reconcileOnClusterQueueActiveState(ctx, &wl, cqName); updated || err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		podsReadyRecheckAfter, err := r.reconcileNotReadyTimeout(ctx, req, &wl)
 		if err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 		maxExecRecheckAfter, err := r.reconcileMaxExecutionTime(ctx, &wl)
 		if err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		// get the minimun non-zero value
```

  I don't think so.
  I pointed out Line 452, 465, 514, 563, 634, and 648.

### Review by [@xigang](https://github.com/xigang) (commented) — 2025-07-01T15:18:51Z

- **pkg/controller/core/workload_controller.go:360** by [@xigang](https://github.com/xigang) — 2025-07-01T15:18:51Z
```diff
@@ -330,24 +337,24 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	if workload.HasQuotaReservation(&wl) {
 		if evictionTriggered, err := r.reconcileCheckBasedEviction(ctx, &wl); evictionTriggered || err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if updated, err := r.reconcileOnLocalQueueActiveState(ctx, &wl, lqExists, &lq); updated || err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		if updated, err := r.reconcileOnClusterQueueActiveState(ctx, &wl, cqName); updated || err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		podsReadyRecheckAfter, err := r.reconcileNotReadyTimeout(ctx, req, &wl)
 		if err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 		maxExecRecheckAfter, err := r.reconcileMaxExecutionTime(ctx, &wl)
 		if err != nil {
-			return ctrl.Result{}, err
+			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
 
 		// get the minimun non-zero value
```

  These lines have already been reverted. https://github.com/kubernetes-sigs/kueue/pull/5832
  
  Please take a look.
