# PR #5815: Helm: Parametrize setting IngressClass,priorityClass and imagePullSecret for KueueViz

**Summary**: Helm: Parametrize setting IngressClass,priorityClass and imagePullSecret for KueueViz

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5815

**Last updated**: 2025-07-22T13:02:22Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@btwseeu78](https://github.com/btwseeu78)
- **Branch**: `feat/fix-helm-template-generation` → `main`
- **Created**: 2025-06-30T12:59:56Z
- **Updated**: 2025-07-22T13:02:22Z
- **Merged**: 2025-07-14T10:56:24Z
- **Closed**: 2025-07-14T10:56:24Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 10   **+267 / -21**

## Description

/kind bug
/kind cleanup
/kind feature

#### What this PR does / why we need it:
1. Current helm chart has no way to configure host for ingress in kueueviz it ends up referring  a random TLS-Secret(bug)
2. PriorityClass only added to manager not extended to kueueviz ,  frontend or backend deployments

Fixes #
-  https://github.com/kubernetes-sigs/kueue/issues/5437

#### Previously Closed MR(for info):
https://github.com/kubernetes-sigs/kueue/pull/5754

#### Does this PR introduce a user-facing change?
```release-note
Helm: introduce new parameters to configure KueueViz installation:
- kueueViz.backend.ingress and kueueViz.frontend.ingress to configure ingress
- kueueViz.imagePullSecrets and kueueViz.priorityClassName
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-30T13:00:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2b96c51720edfe47bbe84d68c268c330e750006c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6870af4790752300082661b6 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-06-30T13:00:07Z

Hi @btwseeu78. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T13:08:41Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-01T07:39:40Z

@btwseeu78  could you please address the remaining comments.

This is not really a bug, but a small feature. I'm ok to cherry-pick small helm features like this.
/remove-kind bug

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T09:54:59Z

@mimowo i am waiting for feedback from @mbobrovskyi , both of the comments belong to same topic on generating helm from the processing plan.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-01T10:19:46Z

ack @mbobrovskyi ptal

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T15:14:58Z

/test pull-kueue-test-e2e-tas-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-01T15:39:48Z

LGTM label has been added.  <details>Git tree hash: 895a6264a00c5295a2ca6297f92d8b478eb80ae5</details>

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-02T13:00:25Z

@mbobrovskyi  you need add the lgtm label again , there was new line i just added a commit for it, sorry to bother and thanks for overall help and guide it means a lot.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-02T13:41:05Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-02T13:41:12Z

LGTM label has been added.  <details>Git tree hash: 731c686f6008bce71dd1e22e339b160951cab67c</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-02T13:41:18Z

/cc @mimowo @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-02T14:04:43Z

/retitle Helm: Parametrize setting IngressClass,priorityClass and imagePullSecret for KueueViz

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-02T14:10:34Z

"4. Added test cases for kueueviz components in helm unit-test " should not be in release note.

/release-note-edit
```release-note
Helm: introduce new parameters to configure KueueViz installation:
- kueueViz.backend.ingress and kueueViz.frontend.ingress to configure ingress
- kueueViz.imagePullSecrets and kueueViz.priorityClassName
```

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-03T12:27:24Z

/retest-required

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-03T16:27:58Z

@mimowo its not possible to rebase since latest addition to processing plan completely breaks the manifest generation add.
probably due to : [](https://github.com/kubernetes-sigs/kueue/pull/5820), the PR itself is not an issue but procession plan is definitely an issue, since its depends on hierarchy

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-03T16:41:34Z

> @mimowo its not possible to rebase since latest addition to processing plan completely breaks the manifest generation add.

Yes, I merged today a PR touching the processing plan and so it probably conflicts.

Maybe you can re-apply the changes manually onto latest main?

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-03T16:57:40Z

Same issue i checked, i will need to rebase and fix it taking both into account i will do it this weekend

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-09T09:33:21Z

@btwseeu78 please address the remaining comments and I'm happy to merge.

While cherrypick to 0.11 is out of question, would you like to cherrypick to 0.12?

### Comment by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-11T06:31:38Z

> @btwseeu78 please address the remaining comments and I'm happy to merge.
> 
> While cherrypick to 0.11 is out of question, would you like to cherrypick to 0.12?

yes im fine with .12

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-14T10:29:44Z

/lgtm
/approve
Let's try:
/cherrypick release-0.12

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-07-14T10:29:46Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.12` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5815#issuecomment-3068866616):

>/lgtm
>/approve
>Let's try:
>/cherrypick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-14T10:29:51Z

LGTM label has been added.  <details>Git tree hash: a881f929003ed0b8db9e453d8132f3c28fba4165</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-14T10:29:52Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5815#" title="Author self-approved">btwseeu78</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5815#pullrequestreview-2975986640" title="LGTM">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5815#issuecomment-3068866616" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-07-14T10:57:01Z

@mimowo: #5815 failed to apply on top of branch "release-0.12":
```
Applying: Enable priorityClass ,imagepullsecrets and ingress for kueueviz
.git/rebase-apply/patch:179: trailing whitespace.
  - it: should set the pod priorityClassName when provided for frontend deployment 
.git/rebase-apply/patch:196: trailing whitespace.
          priorityClassName:       
.git/rebase-apply/patch:218: trailing whitespace.
      kueueViz:  
.git/rebase-apply/patch:281: trailing whitespace.
          
.git/rebase-apply/patch:434: trailing whitespace.
        onFileCondition: '.kind == "Deployment" and  .metadata.name | contains("backend")' 
warning: 5 lines add whitespace errors.
Using index info to reconstruct a base tree...
M	Makefile
M	charts/kueue/README.md
M	charts/kueue/values.yaml
M	hack/processing-plan.yaml
Falling back to patching base and 3-way merge...
Auto-merging hack/processing-plan.yaml
Auto-merging charts/kueue/values.yaml
Auto-merging charts/kueue/README.md
CONFLICT (content): Merge conflict in charts/kueue/README.md
Auto-merging Makefile
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Enable priorityClass ,imagepullsecrets and ingress for kueueviz

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5815#issuecomment-3068866616):

>/lgtm
>/approve
>Let's try:
>/cherrypick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-14T11:03:19Z

@btwseeu78  if you want feel free to prepare the cherrypick for 0.12. Otherwise I'm also find to just wait a bit for 0.13

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-22T13:02:22Z

> @btwseeu78 if you want feel free to prepare the cherrypick for 0.12. Otherwise I'm also find to just wait a bit for 0.13

SGTM

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T13:14:30Z

- **hack/processing-plan.yaml:583** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T13:14:29Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
```

  Shouldn't we just replace the environment variable value instead? Using `type: UPDATE`.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-06-30T13:16:11Z

- **hack/processing-plan.yaml:585** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-06-30T13:16:11Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
+        key: .spec.template.spec.containers[0].env
+      - type: DELETE
```

  The same question here. It looks like much easier just update it instead of delete and add on postOperations.

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-07-01T10:08:56Z

- **hack/processing-plan.yaml:583** by [@btwseeu78](https://github.com/btwseeu78) — 2025-06-30T14:50:05Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
```

  ```yaml
  value: '"wss://{{ .Values.kueueViz.backend.ingress.host | default \"backend.kueueviz.local\" }}"'
  ```
  i tried with this but it did not worked with the default.the templating engine does not taking the strings correctly.
  
  the result is : 
  ```yaml
        env:
          - name: REACT_APP_WEBSOCKET_URL
            value: "{{ .Values.kueueViz.backend.ingress.host | default \"backend.kueueviz.local\" }}"
    ```
  which causing issue in helm because of this escape sequence.

- **hack/processing-plan.yaml:583** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T05:52:11Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
```

  @mbobrovskyi i am open to suggestion if you can suggest me for one block, i will update the rest

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T11:04:09Z

- **hack/processing-plan.yaml:583** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T11:04:09Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
```

  ```yaml
        - type: UPDATE
          key: .spec.template.spec.containers.[].env.[].value
          value: '"wss://{{ .Values.kueueViz.backend.ingress.host | default \"backend.kueueviz.local\" }}"'
          onFileCondition: '.kind == "Deployment" and .metadata.name | contains("kueueviz-frontend")'
          onItemCondition: '.spec.template.spec.containers.[].env.[].name == "REACT_APP_WEBSOCKET_URL"'
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T11:19:30Z

- **hack/processing-plan.yaml:583** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T11:19:30Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
```

  I think I figured out the problem. Try changing the quotes in `config/components/kueueviz/frontend-deployment.yaml`:
  
  https://github.com/kubernetes-sigs/kueue/blob/63a7f6432040a3ee99a4b97aa00a217af9640840/config/components/kueueviz/frontend-deployment.yaml#L25
  
  It looks like the YAML processor preserves the same quote style as the original.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T11:30:01Z

- **hack/processing-plan.yaml:585** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T11:30:01Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
+        key: .spec.template.spec.containers[0].env
+      - type: DELETE
```

  Please also remove redundant: 
  
  https://github.com/kubernetes-sigs/kueue/blob/63a7f6432040a3ee99a4b97aa00a217af9640840/hack/processing-plan.yaml#L619-L622

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T12:14:03Z

- **hack/processing-plan.yaml:585** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T12:14:03Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
+        key: .spec.template.spec.containers[0].env
+      - type: DELETE
```

  ```yaml
        - type: UPDATE
          key: .spec.tls.[].secretName
          value: '"{{ .Values.kueueViz.backend.ingress.tlsSecretName | default \"kueueviz-backend-tls\" }}"'
          onFileCondition: '.kind == "Ingress" and .metadata.name | contains("kueueviz-backend-ingress")'
          onItemCondition: '.spec.tls.[].secretName == "kueueviz-tls-secret"'
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T12:20:06Z

- **hack/processing-plan.yaml:585** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T12:20:06Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
+        key: .spec.template.spec.containers[0].env
+      - type: DELETE
```

  ```yaml
        - type: UPDATE
          key: .spec.tls.[].hosts
          value: '["{{ .Values.kueueViz.backend.ingress.host | default \"backend.kueueviz.local\" }}"]'
          onFileCondition: '.kind == "Ingress" and .metadata.name | contains("kueueviz-backend-ingress")'
          onItemCondition: '.spec.tls.[].hosts.[] == "backend.kueueviz.local"'
  ```

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-07-01T13:10:43Z

- **hack/processing-plan.yaml:583** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T13:10:43Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
```

  > ```yaml
  >       - type: UPDATE
  >         key: .spec.template.spec.containers.[].env.[].value
  >         value: '"wss://{{ .Values.kueueViz.backend.ingress.host | default \"backend.kueueviz.local\" }}"'
  >         onFileCondition: '.kind == "Deployment" and .metadata.name | contains("kueueviz-frontend")'
  >         onItemCondition: '.spec.template.spec.containers.[].env.[].name == "REACT_APP_WEBSOCKET_URL"'
  > ```
  
  this will be same as i had with:
  
  ```yaml
            env:
              - name: REACT_APP_WEBSOCKET_URL
                value: "wss://{{ .Values.kueueViz.backend.ingress.host | default \"backend.kueueviz.local\" }}"
  ```

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-07-01T13:11:11Z

- **hack/processing-plan.yaml:583** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T13:11:11Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
```

  Other Update patch is working as expected
  so i will add them

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T13:12:03Z

- **hack/processing-plan.yaml:583** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T13:12:03Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
```

  Try to change quotes on the config/components/kueueviz/frontend-deployment.yaml.

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-07-01T13:31:36Z

Updated the recommended changes except changes in ENV variable

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-07-01T13:38:44Z

- **hack/processing-plan.yaml:583** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T13:38:44Z
```diff
@@ -580,6 +580,19 @@ files:
       - kustomization.yaml
     continueOnError: true
     operations:
+      - type: DELETE
```

  Done! working

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T14:01:32Z

- **Makefile:184** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T14:00:13Z
```diff
@@ -180,6 +180,10 @@ helm-verify: helm helm-lint ## run helm template and detect any rendering failur
 	$(HELM) template charts/kueue --set managerConfig.controllerManagerConfigYaml="managedJobsNamespaceSelector:\n  matchExpressions:\n    - key: kubernetes.io/metadata.name\n      operator: In\n      values: [ kube-system ]" > /dev/null
 # test priorityClassName option
 	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test priorityClassName option
+	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
```

  Is it duplication?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T14:04:19Z

- **Makefile:186** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T14:04:18Z
```diff
@@ -180,6 +180,10 @@ helm-verify: helm helm-lint ## run helm template and detect any rendering failur
 	$(HELM) template charts/kueue --set managerConfig.controllerManagerConfigYaml="managedJobsNamespaceSelector:\n  matchExpressions:\n    - key: kubernetes.io/metadata.name\n      operator: In\n      values: [ kube-system ]" > /dev/null
 # test priorityClassName option
 	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test priorityClassName option
+	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.priorityClassName="system-cluster-critical" > /dev/null
```

  Can we also check other variables?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T14:07:22Z

- **charts/kueue/templates/kueueviz/frontend-deployment.yaml:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T14:07:22Z
```diff
@@ -22,7 +29,7 @@ spec:
             - containerPort: 8080
           env:
             - name: REACT_APP_WEBSOCKET_URL
-              value: "wss://backend.kueueviz.local"
+              value: wss://{{ .Values.kueueViz.backend.ingress.host | default "backend.kueueviz.local" }}
```

  ```suggestion
                value: 'wss://{{ .Values.kueueViz.backend.ingress.host | default "backend.kueueviz.local" }}'
  ```
  
  Do we need single quotes like we have in other places?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T14:16:01Z

- **charts/kueue/templates/kueueviz/backend-ingress.yaml:17** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T14:16:00Z
```diff
@@ -8,12 +8,15 @@ metadata:
     nginx.ingress.kubernetes.io/rewrite-target: /
     nginx.ingress.kubernetes.io/ssl-redirect: "true"
 spec:
+  {{- if .Values.kueueViz.backend.ingress.ingressClassName }}
+  ingressClassName: {{ .Values.kueueViz.backend.ingress.ingressClassName }}
+  {{- end }}
   tls:
     - hosts:
-        - backend.kueueviz.local
-      secretName: '{{ include "kueue.fullname" . }}-kueueviz-tls-secret'
+        - '{{ .Values.kueueViz.backend.ingress.host | default "backend.kueueviz.local" }}'
+      secretName: '{{ .Values.kueueViz.backend.ingress.tlsSecretName | default "kueueviz-backend-tls" }}'
```

  Should we add `kueue.fullname` prefix for default? Like we did it before?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T14:17:48Z

- **charts/kueue/templates/kueueviz/frontend-ingress.yaml:17** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T14:17:48Z
```diff
@@ -8,12 +8,15 @@ metadata:
     nginx.ingress.kubernetes.io/rewrite-target: /
     nginx.ingress.kubernetes.io/ssl-redirect: "true"
 spec:
+  {{- if .Values.kueueViz.frontend.ingress.ingressClassName }}
+  ingressClassName: {{ .Values.kueueViz.frontend.ingress.ingressClassName }}
+  {{- end }}
   tls:
     - hosts:
-        - frontend.kueueviz.local
-      secretName: '{{ include "kueue.fullname" . }}-kueueviz-tls-secret'
+        - '{{ .Values.kueueViz.frontend.ingress.host | default "frontend.kueueviz.local" }}'
+      secretName: '{{ .Values.kueueViz.frontend.ingress.tlsSecretName | default "kueueviz-frontend-tls" }}'
```

  And here.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T14:46:21Z

- **charts/kueue/templates/kueueviz/backend-ingress.yaml:17** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T14:46:21Z
```diff
@@ -8,12 +8,15 @@ metadata:
     nginx.ingress.kubernetes.io/rewrite-target: /
     nginx.ingress.kubernetes.io/ssl-redirect: "true"
 spec:
+  {{- if .Values.kueueViz.backend.ingress.ingressClassName }}
+  ingressClassName: {{ .Values.kueueViz.backend.ingress.ingressClassName }}
+  {{- end }}
   tls:
     - hosts:
-        - backend.kueueviz.local
-      secretName: '{{ include "kueue.fullname" . }}-kueueviz-tls-secret'
+        - '{{ .Values.kueueViz.backend.ingress.host | default "backend.kueueviz.local" }}'
+      secretName: '{{ .Values.kueueViz.backend.ingress.tlsSecretName | default "kueueviz-backend-tls" }}'
```

  Actually, maybe it's okay because we also need to add this prefix in values.yaml:
  
  https://github.com/kubernetes-sigs/kueue/blob/89b4e6a1d405cfc5036be83542ad6439c37d0f67/charts/kueue/values.yaml#L220

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-07-01T15:01:47Z

- **Makefile:186** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T14:22:34Z
```diff
@@ -180,6 +180,10 @@ helm-verify: helm helm-lint ## run helm template and detect any rendering failur
 	$(HELM) template charts/kueue --set managerConfig.controllerManagerConfigYaml="managedJobsNamespaceSelector:\n  matchExpressions:\n    - key: kubernetes.io/metadata.name\n      operator: In\n      values: [ kube-system ]" > /dev/null
 # test priorityClassName option
 	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test priorityClassName option
+	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.priorityClassName="system-cluster-critical" > /dev/null
```

  yes i need to fix this line, and did not added lot of lines on verify since we can just test the edge conditions we dont use , i have added test cases in unittest for all features .

- **Makefile:186** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T14:24:24Z
```diff
@@ -180,6 +180,10 @@ helm-verify: helm helm-lint ## run helm template and detect any rendering failur
 	$(HELM) template charts/kueue --set managerConfig.controllerManagerConfigYaml="managedJobsNamespaceSelector:\n  matchExpressions:\n    - key: kubernetes.io/metadata.name\n      operator: In\n      values: [ kube-system ]" > /dev/null
 # test priorityClassName option
 	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test priorityClassName option
+	$(HELM) template charts/kueue --set controllerManager.manager.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.priorityClassName="system-cluster-critical" > /dev/null
```

  i removed the redundency

- **charts/kueue/templates/kueueviz/backend-ingress.yaml:17** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T14:32:46Z
```diff
@@ -8,12 +8,15 @@ metadata:
     nginx.ingress.kubernetes.io/rewrite-target: /
     nginx.ingress.kubernetes.io/ssl-redirect: "true"
 spec:
+  {{- if .Values.kueueViz.backend.ingress.ingressClassName }}
+  ingressClassName: {{ .Values.kueueViz.backend.ingress.ingressClassName }}
+  {{- end }}
   tls:
     - hosts:
-        - backend.kueueviz.local
-      secretName: '{{ include "kueue.fullname" . }}-kueueviz-tls-secret'
+        - '{{ .Values.kueueViz.backend.ingress.host | default "backend.kueueviz.local" }}'
+      secretName: '{{ .Values.kueueViz.backend.ingress.tlsSecretName | default "kueueviz-backend-tls" }}'
```

  i don't think its matters much here, the only need is frontend REACT_ENV should be equal to backend host

- **charts/kueue/templates/kueueviz/frontend-deployment.yaml:32** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T14:59:57Z
```diff
@@ -22,7 +29,7 @@ spec:
             - containerPort: 8080
           env:
             - name: REACT_APP_WEBSOCKET_URL
-              value: "wss://backend.kueueviz.local"
+              value: wss://{{ .Values.kueueViz.backend.ingress.host | default "backend.kueueviz.local" }}
```

  chnaged the details

- **charts/kueue/templates/kueueviz/frontend-ingress.yaml:17** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T15:01:40Z
```diff
@@ -8,12 +8,15 @@ metadata:
     nginx.ingress.kubernetes.io/rewrite-target: /
     nginx.ingress.kubernetes.io/ssl-redirect: "true"
 spec:
+  {{- if .Values.kueueViz.frontend.ingress.ingressClassName }}
+  ingressClassName: {{ .Values.kueueViz.frontend.ingress.ingressClassName }}
+  {{- end }}
   tls:
     - hosts:
-        - frontend.kueueviz.local
-      secretName: '{{ include "kueue.fullname" . }}-kueueviz-tls-secret'
+        - '{{ .Values.kueueViz.frontend.ingress.host | default "frontend.kueueviz.local" }}'
+      secretName: '{{ .Values.kueueViz.frontend.ingress.tlsSecretName | default "kueueviz-frontend-tls" }}'
```

  i think default being a constant value is fine.it does not impact core functionality nor connectivity.also without any host it will notwork for most use cases in general apart from serving the support for wss

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-01T15:21:22Z

Overall LGTM

- **charts/kueue/tests/kueue_test.yaml:64** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T15:09:26Z
```diff
@@ -0,0 +1,123 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should  render the ingress value of backend ingress in frontend deployment as ENV variables
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "backend.kueueviz.local"
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.local"
+  - it: Render  Ingress for Backend Component
+    template: kueueviz/backend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        
```

  ```suggestion
  ```
  nit

- **charts/kueue/tests/kueue_test.yaml:45** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T15:09:40Z
```diff
@@ -0,0 +1,123 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should  render the ingress value of backend ingress in frontend deployment as ENV variables
```

  ```suggestion
    - it: should render the ingress value of backend ingress in frontend deployment as ENV variables
  ```
  nit

- **charts/kueue/tests/kueue_test.yaml:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T15:10:12Z
```diff
@@ -0,0 +1,123 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should  render the ingress value of backend ingress in frontend deployment as ENV variables
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "backend.kueueviz.local"
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.local"
+  - it: Render  Ingress for Backend Component
```

  ```suggestion
    - it: should render Ingress for backend component
  ```
  nit

- **charts/kueue/tests/kueue_test.yaml:81** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T15:10:27Z
```diff
@@ -0,0 +1,123 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should  render the ingress value of backend ingress in frontend deployment as ENV variables
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "backend.kueueviz.local"
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.local"
+  - it: Render  Ingress for Backend Component
+    template: kueueviz/backend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        
+        backend:
+          ingress:
+            ingressClassName: "ingress-internal"
+            host: "backend.kueueviz.in"
+            tlsSecretName: "kueueviz-backend-tls"
+    asserts:
+      - equal:
+          path: spec.ingressClassName
+          value: "ingress-internal"
+      - equal:
+          path: spec.rules[0].host
+          value: "backend.kueueviz.in"
+      - equal:
+          path: spec.tls[0].secretName
+          value: "kueueviz-backend-tls"
+
+  - it: Render  Ingress for Frontend Component
```

  ```suggestion
    - it: should render Ingress for frontend component
  ```

- **charts/kueue/tests/kueue_test.yaml:69** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T15:16:22Z
```diff
@@ -0,0 +1,123 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should  render the ingress value of backend ingress in frontend deployment as ENV variables
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "backend.kueueviz.local"
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.local"
+  - it: Render  Ingress for Backend Component
+    template: kueueviz/backend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        
+        backend:
+          ingress:
+            ingressClassName: "ingress-internal"
+            host: "backend.kueueviz.in"
+            tlsSecretName: "kueueviz-backend-tls"
```

  Please avoid using the default value.

- **charts/kueue/tests/kueue_test.yaml:91** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T15:16:32Z
```diff
@@ -0,0 +1,123 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should  render the ingress value of backend ingress in frontend deployment as ENV variables
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "backend.kueueviz.local"
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.local"
+  - it: Render  Ingress for Backend Component
+    template: kueueviz/backend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        
+        backend:
+          ingress:
+            ingressClassName: "ingress-internal"
+            host: "backend.kueueviz.in"
+            tlsSecretName: "kueueviz-backend-tls"
+    asserts:
+      - equal:
+          path: spec.ingressClassName
+          value: "ingress-internal"
+      - equal:
+          path: spec.rules[0].host
+          value: "backend.kueueviz.in"
+      - equal:
+          path: spec.tls[0].secretName
+          value: "kueueviz-backend-tls"
+
+  - it: Render  Ingress for Frontend Component
+    template: kueueviz/frontend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        ingressClassName: "ingress-external"
+        frontend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "frontend.kueueviz.in"
+            tlsSecretName: "kueueviz-frontend-tls"
```

  And here.

- **charts/kueue/tests/kueue_test.yaml:86** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T15:20:30Z
```diff
@@ -0,0 +1,123 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should  render the ingress value of backend ingress in frontend deployment as ENV variables
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "backend.kueueviz.local"
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.local"
+  - it: Render  Ingress for Backend Component
+    template: kueueviz/backend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        
+        backend:
+          ingress:
+            ingressClassName: "ingress-internal"
+            host: "backend.kueueviz.in"
+            tlsSecretName: "kueueviz-backend-tls"
+    asserts:
+      - equal:
+          path: spec.ingressClassName
+          value: "ingress-internal"
+      - equal:
+          path: spec.rules[0].host
+          value: "backend.kueueviz.in"
+      - equal:
+          path: spec.tls[0].secretName
+          value: "kueueviz-backend-tls"
+
+  - it: Render  Ingress for Frontend Component
+    template: kueueviz/frontend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        ingressClassName: "ingress-external"
```

  ```suggestion
  ```

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-07-01T15:33:30Z

- **charts/kueue/tests/kueue_test.yaml:69** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-01T15:33:30Z
```diff
@@ -0,0 +1,123 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should  render the ingress value of backend ingress in frontend deployment as ENV variables
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "backend.kueueviz.local"
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.local"
+  - it: Render  Ingress for Backend Component
+    template: kueueviz/backend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        
+        backend:
+          ingress:
+            ingressClassName: "ingress-internal"
+            host: "backend.kueueviz.in"
+            tlsSecretName: "kueueviz-backend-tls"
```

  changes are done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (approved) — 2025-07-01T15:39:42Z

/lgtm

Awesome! Thank you so much!

- **charts/kueue/tests/kueue_test.yaml:129** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-01T15:37:52Z
```diff
@@ -0,0 +1,122 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should render the ingress value of backend ingress in frontend deployment as ENV variables
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "backend.kueueviz.fr"
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.fr"
+  - it: Should Render Ingress for Backend Component
+    template: kueueviz/backend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:  
+        backend:
+          ingress:
+            ingressClassName: "nginx-external-ingress"
+            host: "backend.kueueviz.in"
+            tlsSecretName: "kueueviz-backend-tls-secret"
+    asserts:
+      - equal:
+          path: spec.ingressClassName
+          value: "nginx-external-ingress"
+      - equal:
+          path: spec.rules[0].host
+          value: "backend.kueueviz.in"
+      - equal:
+          path: spec.tls[0].secretName
+          value: "kueueviz-backend-tls-secret"
+
+  - it: Should Render Ingress for Frontend Component
+    template: kueueviz/frontend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        ingressClassName: "nginx-external"
+        frontend:
+          ingress:
+            ingressClassName: "nginx-external"
+            host: "frontend.kueueviz.in"
+            tlsSecretName: "frontend-tls"
+    asserts:
+      - equal:
+          path: spec.ingressClassName
+          value: "nginx-external"
+      - equal:
+          path: spec.rules[0].host
+          value: "frontend.kueueviz.in"
+      - equal:
+          path: spec.tls[0].secretName
+          value: "frontend-tls"
+  - it: should set the imagePullSecrets for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        imagePullSecrets:
+          - name: "my-secret"
+    asserts:
+      - equal:
+          path: spec.template.spec.imagePullSecrets[0].name
+          value: "my-secret"
+  - it: should set the imagePullSecrets for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        imagePullSecrets:
+          - name: "my-secret"
+    asserts:
+      - equal:
+          path: spec.template.spec.imagePullSecrets[0].name
+          value: "my-secret"
```

  ```suggestion
            value: "my-secret"
            
  ```
  
  nit: new line

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T14:10:20Z

- **charts/kueue/README.md:138** by [@mimowo](https://github.com/mimowo) — 2025-07-02T14:10:20Z
```diff
@@ -126,9 +126,17 @@ The following table lists the configurable parameters of the kueue chart and the
 | kueueViz.backend.image.pullPolicy | string | `"Always"` | KueueViz dashboard backend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.backend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend"` | KueueViz dashboard backend image repository |
 | kueueViz.backend.image.tag | string | `"main"` | KueueViz dashboard backend image tag |
+| kueueViz.backend.ingress.host | string | `"backend.kueueviz.local"` | KueueViz dashboard backend ingress host |
+| kueueViz.backend.ingress.ingressClassName | string | `nil` | KueueViz dashboard backend ingress class name |
+| kueueViz.backend.ingress.tlsSecretName | string | `"kueueviz-backend-tls"` | KueueViz dashboard backend ingress tls secret name |
 | kueueViz.frontend.image.pullPolicy | string | `"Always"` | KueueViz dashboard frontend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.frontend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-frontend"` | KueueViz dashboard frontend image repository |
 | kueueViz.frontend.image.tag | string | `"main"` | KueueViz dashboard frontend image tag |
+| kueueViz.frontend.ingress.host | string | `"frontend.kueueviz.local"` | KueueViz dashboard frontend ingress host |
+| kueueViz.frontend.ingress.ingressClassName | string | `nil` | KueueViz dashboard frontend ingress class name |
+| kueueViz.frontend.ingress.tlsSecretName | string | `"kueueviz-frontend-tls"` | KueueViz dashboard frontend ingress tls secret name |
+| kueueViz.imagePullSecrets | list | `[]` |  |
```

  Please add description what this parameter is controlling

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T14:10:25Z

- **charts/kueue/README.md:139** by [@mimowo](https://github.com/mimowo) — 2025-07-02T14:10:24Z
```diff
@@ -126,9 +126,17 @@ The following table lists the configurable parameters of the kueue chart and the
 | kueueViz.backend.image.pullPolicy | string | `"Always"` | KueueViz dashboard backend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.backend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend"` | KueueViz dashboard backend image repository |
 | kueueViz.backend.image.tag | string | `"main"` | KueueViz dashboard backend image tag |
+| kueueViz.backend.ingress.host | string | `"backend.kueueviz.local"` | KueueViz dashboard backend ingress host |
+| kueueViz.backend.ingress.ingressClassName | string | `nil` | KueueViz dashboard backend ingress class name |
+| kueueViz.backend.ingress.tlsSecretName | string | `"kueueviz-backend-tls"` | KueueViz dashboard backend ingress tls secret name |
 | kueueViz.frontend.image.pullPolicy | string | `"Always"` | KueueViz dashboard frontend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.frontend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-frontend"` | KueueViz dashboard frontend image repository |
 | kueueViz.frontend.image.tag | string | `"main"` | KueueViz dashboard frontend image tag |
+| kueueViz.frontend.ingress.host | string | `"frontend.kueueviz.local"` | KueueViz dashboard frontend ingress host |
+| kueueViz.frontend.ingress.ingressClassName | string | `nil` | KueueViz dashboard frontend ingress class name |
+| kueueViz.frontend.ingress.tlsSecretName | string | `"kueueviz-frontend-tls"` | KueueViz dashboard frontend ingress tls secret name |
+| kueueViz.imagePullSecrets | list | `[]` |  |
+| kueueViz.priorityClassName | string | `nil` |  |
```

  Please add description what this parameter is controlling

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T07:13:46Z

- **charts/kueue/values.yaml:211** by [@mimowo](https://github.com/mimowo) — 2025-07-03T07:13:46Z
```diff
@@ -208,7 +208,16 @@ mutatingWebhook:
   # -- MutatingWebhookConfiguration's reinvocationPolicy
   reinvocationPolicy: Never
 kueueViz:
+  imagePullSecrets: []
```

  Add comments so that the documentation is generated

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T07:16:59Z

- **charts/kueue/README.md:138** by [@mimowo](https://github.com/mimowo) — 2025-07-03T07:16:59Z
```diff
@@ -126,9 +126,17 @@ The following table lists the configurable parameters of the kueue chart and the
 | kueueViz.backend.image.pullPolicy | string | `"Always"` | KueueViz dashboard backend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.backend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend"` | KueueViz dashboard backend image repository |
 | kueueViz.backend.image.tag | string | `"main"` | KueueViz dashboard backend image tag |
+| kueueViz.backend.ingress.host | string | `"backend.kueueviz.local"` | KueueViz dashboard backend ingress host |
+| kueueViz.backend.ingress.ingressClassName | string | `nil` | KueueViz dashboard backend ingress class name |
+| kueueViz.backend.ingress.tlsSecretName | string | `"kueueviz-backend-tls"` | KueueViz dashboard backend ingress tls secret name |
 | kueueViz.frontend.image.pullPolicy | string | `"Always"` | KueueViz dashboard frontend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.frontend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-frontend"` | KueueViz dashboard frontend image repository |
 | kueueViz.frontend.image.tag | string | `"main"` | KueueViz dashboard frontend image tag |
+| kueueViz.frontend.ingress.host | string | `"frontend.kueueviz.local"` | KueueViz dashboard frontend ingress host |
+| kueueViz.frontend.ingress.ingressClassName | string | `nil` | KueueViz dashboard frontend ingress class name |
+| kueueViz.frontend.ingress.tlsSecretName | string | `"kueueviz-frontend-tls"` | KueueViz dashboard frontend ingress tls secret name |
+| kueueViz.imagePullSecrets | list | `[]` |  |
```

  Actually, I think it is better to move the parameters to per component frontend / backend. Yes, it means a bit of duplication, but it feels better to make it consistent and per Pod. wdyt?

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-07-03T12:21:51Z

- **charts/kueue/README.md:138** by [@btwseeu78](https://github.com/btwseeu78) — 2025-07-03T12:21:51Z
```diff
@@ -126,9 +126,17 @@ The following table lists the configurable parameters of the kueue chart and the
 | kueueViz.backend.image.pullPolicy | string | `"Always"` | KueueViz dashboard backend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.backend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend"` | KueueViz dashboard backend image repository |
 | kueueViz.backend.image.tag | string | `"main"` | KueueViz dashboard backend image tag |
+| kueueViz.backend.ingress.host | string | `"backend.kueueviz.local"` | KueueViz dashboard backend ingress host |
+| kueueViz.backend.ingress.ingressClassName | string | `nil` | KueueViz dashboard backend ingress class name |
+| kueueViz.backend.ingress.tlsSecretName | string | `"kueueviz-backend-tls"` | KueueViz dashboard backend ingress tls secret name |
 | kueueViz.frontend.image.pullPolicy | string | `"Always"` | KueueViz dashboard frontend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.frontend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-frontend"` | KueueViz dashboard frontend image repository |
 | kueueViz.frontend.image.tag | string | `"main"` | KueueViz dashboard frontend image tag |
+| kueueViz.frontend.ingress.host | string | `"frontend.kueueviz.local"` | KueueViz dashboard frontend ingress host |
+| kueueViz.frontend.ingress.ingressClassName | string | `nil` | KueueViz dashboard frontend ingress class name |
+| kueueViz.frontend.ingress.tlsSecretName | string | `"kueueviz-frontend-tls"` | KueueViz dashboard frontend ingress tls secret name |
+| kueueViz.imagePullSecrets | list | `[]` |  |
```

  apart from priorityClass and imagepullSecret its dedicated per componnet.
  
  we can technically set diff priorityClass but kueueViz has one frontend and backend so any of this pending means the entire set is down. same for imagepullsecrets, i doubt that people will use 3 diff repo. i want to split the repo to ,registry ,repository and tags. registry can be set globally,but for later in other MR or dicussion we can decide.
  i will fix the readme

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T12:49:40Z

- **charts/kueue/README.md:138** by [@mimowo](https://github.com/mimowo) — 2025-07-03T12:49:40Z
```diff
@@ -126,9 +126,17 @@ The following table lists the configurable parameters of the kueue chart and the
 | kueueViz.backend.image.pullPolicy | string | `"Always"` | KueueViz dashboard backend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.backend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend"` | KueueViz dashboard backend image repository |
 | kueueViz.backend.image.tag | string | `"main"` | KueueViz dashboard backend image tag |
+| kueueViz.backend.ingress.host | string | `"backend.kueueviz.local"` | KueueViz dashboard backend ingress host |
+| kueueViz.backend.ingress.ingressClassName | string | `nil` | KueueViz dashboard backend ingress class name |
+| kueueViz.backend.ingress.tlsSecretName | string | `"kueueviz-backend-tls"` | KueueViz dashboard backend ingress tls secret name |
 | kueueViz.frontend.image.pullPolicy | string | `"Always"` | KueueViz dashboard frontend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.frontend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-frontend"` | KueueViz dashboard frontend image repository |
 | kueueViz.frontend.image.tag | string | `"main"` | KueueViz dashboard frontend image tag |
+| kueueViz.frontend.ingress.host | string | `"frontend.kueueviz.local"` | KueueViz dashboard frontend ingress host |
+| kueueViz.frontend.ingress.ingressClassName | string | `nil` | KueueViz dashboard frontend ingress class name |
+| kueueViz.frontend.ingress.tlsSecretName | string | `"kueueviz-frontend-tls"` | KueueViz dashboard frontend ingress tls secret name |
+| kueueViz.imagePullSecrets | list | `[]` |  |
```

  Yeah, I know in typical case having two params means duplication, but technically they can be different, so I would suggest to have two params (one for frontend one for backend).

### Review by [@btwseeu78](https://github.com/btwseeu78) (commented) — 2025-07-07T17:27:38Z

modified the processing plan to incorporate previous change in processing plan.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-09T02:46:40Z

LGTM. Just few nits.

- **Makefile:189** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-09T02:37:58Z
```diff
@@ -187,6 +187,10 @@ helm-verify: helm helm-lint ## run helm template and detect any rendering failur
 # test kueueViz frontend nodeSelector and tolerations
 	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.frontend.nodeSelector.nodetype=infra --set 'kueueViz.frontend.tolerations[0].key=node-role.kubernetes.io/master' --set 'kueueViz.frontend.tolerations[0].operator=Exists' --set 'kueueViz.frontend.tolerations[0].effect=NoSchedule' > /dev/null
 
```

  nit: remove line

- **Makefile:192** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-09T02:38:10Z
```diff
@@ -187,6 +187,10 @@ helm-verify: helm helm-lint ## run helm template and detect any rendering failur
 # test kueueViz frontend nodeSelector and tolerations
 	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.frontend.nodeSelector.nodetype=infra --set 'kueueViz.frontend.tolerations[0].key=node-role.kubernetes.io/master' --set 'kueueViz.frontend.tolerations[0].operator=Exists' --set 'kueueViz.frontend.tolerations[0].effect=NoSchedule' > /dev/null
 
+# test kueueViz priorityClassName options for backend
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.backend.priorityClassName="system-cluster-critical" > /dev/null
+# test kueueViz priorityClassName options for frontend
+	$(HELM) template charts/kueue --set enableKueueViz=true --set kueueViz.backend.priorityClassName="system-cluster-critical" > /dev/null
```

  nit: add new line

- **charts/kueue/values.yaml:220** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-09T02:39:40Z
```diff
@@ -217,6 +217,19 @@ kueueViz:
     nodeSelector: {}
     # -- KueueViz backend tolerations
     tolerations: []
+    # -- Sets ImagePullSecrets for KueueViz dashboard backend and frontend deployments
```

  ```suggestion
  ```
  I think this no need.

- **charts/kueue/README.md:131** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-09T02:40:53Z
```diff
@@ -128,12 +128,22 @@ The following table lists the configurable parameters of the kueue chart and the
 | kueueViz.backend.image.pullPolicy | string | `"Always"` | KueueViz dashboard backend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.backend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend"` | KueueViz dashboard backend image repository |
 | kueueViz.backend.image.tag | string | `"main"` | KueueViz dashboard backend image tag |
+| kueueViz.backend.imagePullSecrets | list | `[]` | Sets ImagePullSecrets for KueueViz dashboard backend deployments This is useful when the images are in a private registry. |
```

  ```suggestion
  | kueueViz.backend.imagePullSecrets | list | `[]` | Sets ImagePullSecrets for KueueViz dashboard backend deployments. This is useful when the images are in a private registry. |
  ```
  nit

- **charts/kueue/README.md:141** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-09T02:41:19Z
```diff
@@ -128,12 +128,22 @@ The following table lists the configurable parameters of the kueue chart and the
 | kueueViz.backend.image.pullPolicy | string | `"Always"` | KueueViz dashboard backend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.backend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-backend"` | KueueViz dashboard backend image repository |
 | kueueViz.backend.image.tag | string | `"main"` | KueueViz dashboard backend image tag |
+| kueueViz.backend.imagePullSecrets | list | `[]` | Sets ImagePullSecrets for KueueViz dashboard backend deployments This is useful when the images are in a private registry. |
+| kueueViz.backend.ingress.host | string | `"backend.kueueviz.local"` | KueueViz dashboard backend ingress host |
+| kueueViz.backend.ingress.ingressClassName | string | `nil` | KueueViz dashboard backend ingress class name |
+| kueueViz.backend.ingress.tlsSecretName | string | `"kueueviz-backend-tls"` | KueueViz dashboard backend ingress tls secret name |
 | kueueViz.backend.nodeSelector | object | `{}` | KueueViz backend nodeSelector |
+| kueueViz.backend.priorityClassName | string | `nil` | Enable PriorityClass for KueueViz dashboard backend deployments |
 | kueueViz.backend.tolerations | list | `[]` | KueueViz backend tolerations |
 | kueueViz.frontend.image.pullPolicy | string | `"Always"` | KueueViz dashboard frontend image pullPolicy. This should be set to 'IfNotPresent' for released version |
 | kueueViz.frontend.image.repository | string | `"us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueueviz-frontend"` | KueueViz dashboard frontend image repository |
 | kueueViz.frontend.image.tag | string | `"main"` | KueueViz dashboard frontend image tag |
+| kueueViz.frontend.imagePullSecrets | list | `[]` | Sets ImagePullSecrets for KueueViz dashboard frontend deployments This is useful when the images are in a private registry. |
```

  ```suggestion
  | kueueViz.frontend.imagePullSecrets | list | `[]` | Sets ImagePullSecrets for KueueViz dashboard frontend deployments. This is useful when the images are in a private registry. |
  ```

- **charts/kueue/values.yaml:246** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-09T02:41:47Z
```diff
@@ -230,6 +243,19 @@ kueueViz:
     nodeSelector: {}
     # -- KueueViz frontend tolerations
     tolerations: []
+    # -- Sets ImagePullSecrets for KueueViz dashboard backend and frontend deployments
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-07-09T02:47:48Z

- **charts/kueue/tests/kueue_test.yaml:131** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-09T02:47:48Z
```diff
@@ -0,0 +1,131 @@
+suite: test kueueviz deployment
+templates:
+  - kueueviz/backend-deployment.yaml
+  - kueueviz/frontend-deployment.yaml
+  - kueueviz/backend-ingress.yaml
+  - kueueviz/frontend-ingress.yaml
+tests:
+  - it: should set the pod priorityClassName when provided for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          priorityClassName:
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should set the pod priorityClassName when provided for frontend deployment 
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          priorityClassName: "foo"
+    asserts:
+      - equal:
+          path: spec.template.spec.priorityClassName
+          value: foo
+  - it: should not render the pod priorityClassName if not set for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          priorityClassName:       
+    asserts:
+      - notExists:
+          path: spec.template.spec.priorityClassName
+  - it: should render the ingress value of backend ingress in frontend deployment as ENV variables
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          ingress:
+            ingressClassName: "ingress-external"
+            host: "backend.kueueviz.fr"
+            tlsSecretName: "kueueviz-frontend-tls"
+    asserts:
+      - equal:
+          path: spec.template.spec.containers[0].env[0].value
+          value: "wss://backend.kueueviz.fr"
+  - it: Should Render Ingress for Backend Component
+    template: kueueviz/backend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:  
+        backend:
+          ingress:
+            ingressClassName: "nginx-external-ingress"
+            host: "backend.kueueviz.in"
+            tlsSecretName: "kueueviz-backend-tls-secret"
+    asserts:
+      - equal:
+          path: spec.ingressClassName
+          value: "nginx-external-ingress"
+      - equal:
+          path: spec.rules[0].host
+          value: "backend.kueueviz.in"
+      - equal:
+          path: spec.tls[0].secretName
+          value: "kueueviz-backend-tls-secret"
+
+  - it: Should Render Ingress for Frontend Component
+    template: kueueviz/frontend-ingress.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        ingressClassName: "nginx-external"
+        frontend:
+          ingress:
+            ingressClassName: "nginx-external"
+            host: "frontend.kueueviz.in"
+            tlsSecretName: "frontend-tls"
+    asserts:
+      - equal:
+          path: spec.ingressClassName
+          value: "nginx-external"
+      - equal:
+          path: spec.rules[0].host
+          value: "frontend.kueueviz.in"
+      - equal:
+          path: spec.tls[0].secretName
+          value: "frontend-tls"
+  - it: should set the imagePullSecrets for backend deployment
+    template: kueueviz/backend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        backend:
+          imagePullSecrets:
+            - name: "my-secret"
+    asserts:
+      - equal:
+          path: spec.template.spec.imagePullSecrets[0].name
+          value: "my-secret"
+  - it: should set the imagePullSecrets for frontend deployment
+    template: kueueviz/frontend-deployment.yaml
+    set:
+      enableKueueViz: true
+      kueueViz:
+        frontend:
+          imagePullSecrets:
+            - name: "my-secret"
+    asserts:
+      - equal:
+          path: spec.template.spec.imagePullSecrets[0].name
+          value: "my-secret"
+
+          
```

  ```suggestion
  
  ```
  nit
