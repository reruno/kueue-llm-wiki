# PR #5816: [AFS] Describe entry penalty in KEP

**Summary**: [AFS] Describe entry penalty in KEP

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5816

**Last updated**: 2025-08-12T10:15:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `afs-kep` → `main`
- **Created**: 2025-06-30T13:36:57Z
- **Updated**: 2025-08-12T10:15:49Z
- **Merged**: 2025-07-02T11:31:25Z
- **Closed**: 2025-07-02T11:31:25Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 2   **+23 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Discuss in KEP the risk and mitigation related to AFS

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #5339

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-06-30T13:37:04Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 13a137d06637f6182c0c28272d08fa332492b788 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6865101c01c5df000849b4fe |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-06-30T13:37:12Z

/assign @mimowo 
cc @mwielgus @mbobrovskyi

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-01T13:46:03Z

/remove-kind bug
/kind documentation

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-02T11:01:33Z

LGTM label has been added.  <details>Git tree hash: d909f524f39e4176da3e0283e67a5c775584e754</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-02T11:01:35Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5816#pullrequestreview-2978706014" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5816#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-01T07:29:19Z

- **keps/4136-admission-fair-sharing/README.md:207** by [@mimowo](https://github.com/mimowo) — 2025-07-01T07:29:19Z
```diff
@@ -204,6 +204,22 @@ some guaranteed capacity and at the same time, allow them to fairly share some b
 
 * Increased complexity of the project.
 
+* Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
```

   Risk & mitigations section is for risks which we don't directly address in the design, but with secondary measures, like documentations etc. 
   
   So, I think this belongs to Design Details, under a dedicated section like "Entry penalty"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-01T08:27:19Z

- **keps/4136-admission-fair-sharing/README.md:221** by [@mimowo](https://github.com/mimowo) — 2025-07-01T08:27:19Z
```diff
@@ -204,6 +204,22 @@ some guaranteed capacity and at the same time, allow them to fairly share some b
 
 * Increased complexity of the project.
 
+* Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+**Mitigation:** Add an entrance penalty to FairSharingStatus every time Kueue admits a Workload. The penalty should be calculated with the same
+formula as regular usage with the assumption the amount of time had passed since the last update is equal to the above-mentioned interval.
```

  As suggested in the other comment. This is not a mitigation. 
  
  Maybe we can rephrase is similarly below (see placeholders):
  
  The penalty is calculated with the same formula as regular usage [[provide link]] with the assumption the amount of time had passed since the last update is equal to the [[name of the interval]] interval.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-01T09:41:02Z

- **keps/4136-admission-fair-sharing/README.md:207** by [@PBundyra](https://github.com/PBundyra) — 2025-07-01T09:41:02Z
```diff
@@ -204,6 +204,22 @@ some guaranteed capacity and at the same time, allow them to fairly share some b
 
 * Increased complexity of the project.
 
+* Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-01T09:41:26Z

- **keps/4136-admission-fair-sharing/README.md:221** by [@PBundyra](https://github.com/PBundyra) — 2025-07-01T09:41:26Z
```diff
@@ -204,6 +204,22 @@ some guaranteed capacity and at the same time, allow them to fairly share some b
 
 * Increased complexity of the project.
 
+* Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+**Mitigation:** Add an entrance penalty to FairSharingStatus every time Kueue admits a Workload. The penalty should be calculated with the same
+formula as regular usage with the assumption the amount of time had passed since the last update is equal to the above-mentioned interval.
```

  Clarified with explicit name and formula

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-01T13:46:34Z

- **keps/4136-admission-fair-sharing/README.md:228** by [@mimowo](https://github.com/mimowo) — 2025-07-01T13:46:34Z
```diff
@@ -208,6 +225,8 @@ some guaranteed capacity and at the same time, allow them to fairly share some b
 
 * Increased complexity of the project.
 
+
```

  revert such distractions

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-01T13:49:10Z

- **keps/4136-admission-fair-sharing/README.md:196** by [@mimowo](https://github.com/mimowo) — 2025-07-01T13:49:10Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
```

  ```suggestion
  The mechanism as implemented in Kueue 0.12 can be exploited, because a tenant
  can submit thousands of jobs which get scheduled in a short period of time. 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-01T13:49:22Z

- **keps/4136-admission-fair-sharing/README.md:198** by [@mimowo](https://github.com/mimowo) — 2025-07-01T13:49:21Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
```

  ```suggestion
  E.g. Let's consider:
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-01T13:51:16Z

- **keps/4136-admission-fair-sharing/README.md:206** by [@mimowo](https://github.com/mimowo) — 2025-07-01T13:51:16Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
```

  ```suggestion
  Here, all Jobs submitted by Tenant B  within the next 5min get scheduled.
  Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-01T13:54:06Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@mimowo](https://github.com/mimowo) — 2025-07-01T13:54:05Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  Why this particular formula, is there some motivation / intuition behind it?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-01T13:55:19Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@mimowo](https://github.com/mimowo) — 2025-07-01T13:55:19Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  I'm not sure what it means "amount of time had passed"  in this context, because it is not present in the formula.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T06:27:07Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@mimowo](https://github.com/mimowo) — 2025-07-02T06:27:07Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  Is the actual interpretation of the penalty: "usage which would have been added if the Job started in the previous sampling interval"? This feels arbitrary, why not "two sampling intervals ago", or any custom.
  
  Maybe we should have a dedicated API param like "entryPenaltyMultiplier" instead in the future. However, I prefer to be conservative about the API, so I'm happy to start with the proposed penalty, but add a note along the lines: the choice entry penalty is arbitrary, but we propose it for alpha to be conservative about API. We may re-evaluate the need to make the penalty configurable based on user-feedback.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T09:29:57Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T09:29:57Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  > Is the actual interpretation of the penalty: "usage which would have been added if the Job started in the previous sampling interval"?
  
  Exactly
  
  > Maybe we should have a dedicated API param like "entryPenaltyMultiplier" instead in the future. However, I prefer to be conservative about the API, so I'm happy to start with the proposed penalty, but add a note along the lines: the choice entry penalty is arbitrary, but we propose it for alpha to be conservative about API. We may re-evaluate the need to make the penalty configurable based on user-feedback.
  
  It's quite arbitrary indeed. I like the idea of possible API in the future, and I'm happy to extend it after we collect users' feedback

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-07-02T10:39:17Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@mwielgus](https://github.com/mwielgus) — 2025-07-02T10:39:17Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+The mechanism as implemented in Kueue 0.12 can be exploited, because a tenant
+can submit thousands of jobs which get scheduled in a short period of time. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+Here, all Jobs submitted by Tenant B  within the next 5min get scheduled.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  What how can I define A in this formula?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:41:43Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:41:43Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+The mechanism as implemented in Kueue 0.12 can be exploited, because a tenant
+can submit thousands of jobs which get scheduled in a short period of time. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+Here, all Jobs submitted by Tenant B  within the next 5min get scheduled.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  `A` remains the same as above which is `A = 1 - 0.5 ^ (sampling/half_life_decay)`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:42:56Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:42:56Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  Yes, this sentence might a bit misleading. What I meant is `A = 1 - 0.5 ^ (usageSamlpingInterval/usageHalfLifeDecayTime)`

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-07-02T10:45:07Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@mwielgus](https://github.com/mwielgus) — 2025-07-02T10:45:07Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  Well, it is not quite arbitrary. The goal here is to have eventual fairness. Ideally, at the time of admission we should know how long the workload would be running and then decide whether it is fair and OK to admit it or not. As there is no such information yet, we need to somehow approximate it. So here comes the formula A * resources. It assumes that the workload would be running/starting at least for A seconds/minutes. So when the next workload from the same LQ is processed, there is some rough idea about almost certain usage that has not yet been updated.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:52:56Z

- **keps/4136-admission-fair-sharing/README.md:228** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:52:56Z
```diff
@@ -208,6 +225,8 @@ some guaranteed capacity and at the same time, allow them to fairly share some b
 
 * Increased complexity of the project.
 
+
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:53:06Z

- **keps/4136-admission-fair-sharing/README.md:198** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:53:06Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:53:16Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:53:16Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  Clarified in the KEP

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:53:27Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:53:27Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+The mechanism as implemented in Kueue 0.12 can be exploited, because a tenant
+can submit thousands of jobs which get scheduled in a short period of time. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+Here, all Jobs submitted by Tenant B  within the next 5min get scheduled.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  Clarified in the KEP

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-07-02T10:53:43Z

- **keps/4136-admission-fair-sharing/README.md:208** by [@PBundyra](https://github.com/PBundyra) — 2025-07-02T10:53:43Z
```diff
@@ -190,6 +191,22 @@ within AdmissionScope and then other mechanisms are applied as usual.
 workloads based on the LocalQueue resource usage when considering workloads within the same ClusterQueue. 
 Workloads from different ClusterQueues are not compared against each other using the resource usage dimension.
 
+### Entry penalty
+Because of the interval between updating FairSharingStatus the mechanism can be exploited. We may end up in a situation where one tenant
+can submit thousands of jobs which will be prioritized because they had slightly lower FairSharingStatus. 
+
+E.g. Let's assume:
+```
+Tenant's A FairSharingStatus: 0.1 CPU
+Tenant's B FairSharingStatus: 0.05 CPU
+usageSamplingInterval: 5mins
+```
+
+In the worst case scenario all of the jobs submitted by Tenant B will be prioritized and could be admitted for the next 5mins.
+Even if Tenant B submitted a job that consumed 1000 CPUs, consecutive jobs would still be prioritized because of the delay in updating the status.
+
+Hence, since v0.13 Kueue adds an entry penalty to FairSharingStatus every time it admits a Workload. The penalty should be calculated with similar formula: `penalty = A * requested_resource`. We assume the amount of time had passed since the last update is equal to the `usageSamplingInterval`.
```

  Clarified in the KEP

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-02T11:01:27Z

/lgtm
/approve
Awesome, thank you!
