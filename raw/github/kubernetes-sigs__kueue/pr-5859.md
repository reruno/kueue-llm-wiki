# PR #5859: chore: Replace utilmaps.MergeKeepFirst with utilmaps.Copy()

**Summary**: chore: Replace utilmaps.MergeKeepFirst with utilmaps.Copy()

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5859

**Last updated**: 2025-08-12T15:07:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `replace-merge-keep-first` → `main`
- **Created**: 2025-07-03T07:01:08Z
- **Updated**: 2025-08-12T15:07:31Z
- **Merged**: 2025-08-12T15:07:09Z
- **Closed**: 2025-08-12T15:07:09Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 5   **+111 / -28**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

Instead of a DIY merge function, it would be better using the std `maps.Copy()` function.

Updated: Based on https://github.com/kubernetes-sigs/kueue/pull/5859#discussion_r2182016774 discussion, we decided to std maps.Copy wrap function to initialize dst map with an one-line when the dst is null.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-03T07:02:16Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5210760a5efa469d26596e6e4c08f219a5c71492 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/689b4ff6260e840008bc0592 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-12T14:55:40Z

@mimowo Sorry for the late fixes. I addressed your comments based on https://github.com/kubernetes-sigs/kueue/pull/5859#discussion_r2182016774 discussion. PTAL, thanks.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-12T15:03:44Z

/lgtm 
/approve
Thank you👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-12T15:03:51Z

LGTM label has been added.  <details>Git tree hash: ea49eb5888253a3086be737d6859bbc4da771c34</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-12T15:03:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5859#issuecomment-3179742362" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5859#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-03T07:02:56Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-03T07:02:55Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  The `maps.Copy` does not check if the dst map (`wl.Labels`) is nil.
  So, in the case of a null dst map, a null pointer access will occur.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T07:04:47Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@mimowo](https://github.com/mimowo) — 2025-07-03T07:04:47Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  Could both `wl.Labels,` and `labelsToCopy` be nil at the same time? 
  
  If yes: will not `maps.Copy(wl.Labels, labelsToCopy)` fail?
  if no: we can drop the ` && labelsToCopy != nil {` check

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-03T07:09:20Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-03T07:09:20Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  If we drop the `&& labelsToCopy != nil {` check, the wl.Labels is initialized as an empty map, `map[string]string{}` instead of nil even if `labelsToCopy` is nil.
  
  Eventually, this function returns an empty map instead of nil map when there is no merge operation.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-03T07:10:09Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-03T07:10:08Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  That's why I added `labelsToCoy != nil` verification.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-03T07:11:18Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-03T07:11:18Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  > Could both wl.Labels, and labelsToCopy be nil at the same time?
  
  Yes, but no failure since `for k, v := range map` is not performed when map is nil.
  
  https://go.dev/play/p/bb4iMhdsxI4

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T07:22:01Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@mimowo](https://github.com/mimowo) — 2025-07-03T07:22:01Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  Hm, maybe but TBH checking the condition is non trivial to follow, I actually prefer the previous version to hide the complexities, because the previous version nicely encapsulates the technical complexities, offloading the business logic. In the new version the business logic is mixed with the technical allocations which makes it tricky (imo).
  
  
  Maybe we could use `maps.Copy` inside `MergeKeepFirst`?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-03T07:26:56Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-03T07:26:56Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  In that case, let us define the utilmaps.Copy instead of MergeKeepFirst to align with std lib.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T07:35:14Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@mimowo](https://github.com/mimowo) — 2025-07-03T07:35:14Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  TBH `MergeKeepFirst` reads good to me, but I'm ok with a different name. Still, the semantic is different, because std Copy puts the items "in place", while `MergeKeepFirst` returns the new map.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-03T07:48:03Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-03T07:48:03Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  I think our case does not require a new map instance. So, Copy also sounds ok.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-03T07:50:54Z

- **pkg/controller/jobs/pod/pod_controller.go:1055** by [@mimowo](https://github.com/mimowo) — 2025-07-03T07:50:54Z
```diff
@@ -1048,7 +1049,10 @@ func (p *Pod) ConstructComposableWorkload(ctx context.Context, c client.Client,
 	if err != nil {
 		return nil, err
 	}
-	wl.Labels = maps.MergeKeepFirst(wl.Labels, labelsToCopy)
+	if wl.Labels == nil && labelsToCopy != nil {
+		wl.Labels = make(map[string]string, len(labelsToCopy))
+	}
+	maps.Copy(wl.Labels, labelsToCopy)
```

  sgtm
