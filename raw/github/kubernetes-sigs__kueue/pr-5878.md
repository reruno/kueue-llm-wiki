# PR #5878: Group PodSets together for finding common flavor

**Summary**: Group PodSets together for finding common flavor

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5878

**Last updated**: 2025-07-29T12:58:12Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@lchrzaszcz](https://github.com/lchrzaszcz)
- **Branch**: `find-flavor-for-grouped-pod-sets` → `main`
- **Created**: 2025-07-04T14:44:24Z
- **Updated**: 2025-07-29T12:58:12Z
- **Merged**: 2025-07-18T17:02:28Z
- **Closed**: 2025-07-18T17:02:28Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 12   **+369 / -40**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

To be able to co-locate leaders and workers in LeaderWorkerSet we need to ensure it is possible to find the same flavor for two (or more) PodSets.

This PR introduces `PodSetGroup` as a way to request flavor finding for a group of PodSets.

Details are described in KEP update here: https://github.com/kubernetes-sigs/kueue/pull/5895

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Related to https://github.com/kubernetes-sigs/kueue/issues/4531

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-04T14:44:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a475ac3f47a02b612f8b350eec3a35d422fa63f1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/687a7811073e59000830ecd1 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-04T14:44:33Z

Hi @lchrzaszcz. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-04T14:47:04Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:57:17Z

/release-note-edit
```release-note
NONE
```
We will add the release note in the follow up PR.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-18T15:02:04Z

@lchrzaszcz please mark all addressed comments as resolved, comments like https://github.com/kubernetes-sigs/kueue/pull/5878#discussion_r2189972119 are noise at this point.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-18T15:05:57Z

@lchrzaszcz: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-main-1-30 | 96e63c2c8096e5c7e01c780b32fb8795d705b096 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/5878/pull-kueue-test-e2e-main-1-30/1941146770616619008) | true | `/test pull-kueue-test-e2e-main-1-30`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=5878). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Alchrzaszcz). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-18T17:01:49Z

/lgtm
/approve
None of the comments are blocking:
-  https://github.com/kubernetes-sigs/kueue/pull/5878#discussion_r2216104567 
- https://github.com/kubernetes-sigs/kueue/pull/5878#discussion_r2216084445

we will open PRs or issues for them.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-18T17:01:56Z

LGTM label has been added.  <details>Git tree hash: cd818f095034563981626f75523cbbdf53063310</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-18T17:01:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5878#" title="Author self-approved">lchrzaszcz</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5878#issuecomment-3090101559" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-18T17:12:16Z

> we will open PRs or issues for them.

Created issues for both:
- https://github.com/kubernetes-sigs/kueue/issues/6029
- https://github.com/kubernetes-sigs/kueue/issues/6028

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-04T14:48:21Z

- **apis/kueue/v1beta1/workload_types.go:132** by [@mimowo](https://github.com/mimowo) — 2025-07-04T14:48:20Z
```diff
@@ -125,6 +125,8 @@ type PodSetTopologyRequest struct {
 	// For example, in the context of JobSet this value is read from jobset.sigs.k8s.io/replicatedjob-replicas.
 	SubGroupCount *int32 `json:"subGroupCount,omitempty"`
 
+	PodSetGroup *string `json:"podSetGroup,omitempty"`
```

  nit, add some comment

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-04T14:49:38Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:426** by [@mimowo](https://github.com/mimowo) — 2025-07-04T14:49:38Z
```diff
@@ -416,6 +417,21 @@ func (a *FlavorAssigner) Assign(log logr.Logger, counts []int32) Assignment {
 	return a.assignFlavors(log, counts)
 }
 
+type IndexedPodSet struct {
+	OriginalIndex    int
+	PodSet           *workload.PodSetResources
+	PodSetAssignment *PodSetAssignment
+}
+
+func contains(slice []string, item string) bool {
```

  slices.Contains instead? https://pkg.go.dev/slices#Contains

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-07T12:11:16Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:489** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-07T12:11:16Z
```diff
@@ -466,24 +485,72 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
+		groupKey := strconv.Itoa(i)
+		if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
```

  Add `a.wl.Obj.Spec.PodSets[i].TopologyRequest.PodSetGroup != nil` as well.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-07T12:16:12Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:508** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-07T12:16:12Z
```diff
@@ -466,24 +485,72 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
+		groupKey := strconv.Itoa(i)
+		if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
+			groupKey = *a.wl.Obj.Spec.PodSets[i].TopologyRequest.PodSetGroup
+		}
+
+		if !contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(requests))
+		for _, podset := range podSets {
+			psIDs = append(psIDs, podset.OriginalIndex)
+
+			for resName := range podset.PodSet.Requests {
```

  Check for preexisting functions in `resources` package. There should be a function `add`.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-07T12:22:05Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:626** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-07T12:22:05Z
```diff
@@ -566,16 +633,24 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 
 	status := &Status{}
 	requests = filterRequestedResources(requests, resourceGroup.CoveredResources)
-	ps := &a.wl.Obj.Spec.PodSets[psID]
-	podSpec := &ps.Template.Spec
+
+	podSets := make([]*kueue.PodSet, len(psIDs))
+	selectors := make([]nodeaffinity.RequiredNodeAffinity, len(psIDs))
+
+	for idx, psID := range psIDs {
+		ps := &a.wl.Obj.Spec.PodSets[psID]
+		podSets[idx] = ps
+
+		selector := flavorSelector(&ps.Template.Spec, resourceGroup.LabelKeys)
+		selectors[idx] = selector
+	}
 
 	var bestAssignment ResourceAssignment
 	bestAssignmentMode := noFit
 
 	// We will only check against the flavors' labels for the resource.
-	selector := flavorSelector(podSpec, resourceGroup.LabelKeys)
 	attemptedFlavorIdx := -1
-	idx := a.wl.LastAssignment.NextFlavorToTryForPodSetResource(psID, resName)
+	idx := a.wl.LastAssignment.NextFlavorToTryForPodSetResource(psIDs[0], resName)
```

  Consider using max of those values across all podsets.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-07T12:24:32Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:667** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-07T12:24:31Z
```diff
@@ -585,26 +660,39 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		tryNextFlavor := false
+		for psIdx, psID := range psIDs {
+			podSpec := podSets[psIdx].Template.Spec
+			selector := selectors[psIdx]
```

  Move closer to the actual usage.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-07T12:27:54Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:664** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-07T12:27:54Z
```diff
@@ -585,26 +660,39 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		tryNextFlavor := false
```

  The name `tryNextFlavor` clashes with existing concept of trying next flavor. Let's rename it to something else.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-07T12:30:45Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:664** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-07T12:30:45Z
```diff
@@ -585,26 +660,39 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		tryNextFlavor := false
```

  We could consider using `message` and appending it to `status` at the end of loop. We could use that to flag errors and to continue onto next flavor. We should rename `message` to something more verbose like `flavorMessage`/`flavorAssignmentMessage`.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-07T12:36:16Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:542** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-07T12:36:15Z
```diff
@@ -466,24 +485,72 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
+		groupKey := strconv.Itoa(i)
+		if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
+			groupKey = *a.wl.Obj.Spec.PodSets[i].TopologyRequest.PodSetGroup
+		}
+
+		if !contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(requests))
+		for _, podset := range podSets {
+			psIDs = append(psIDs, podset.OriginalIndex)
+
+			for resName := range podset.PodSet.Requests {
+				requests[resName] = requests[resName] + podset.PodSet.Requests[resName]
+			}
+		}
+		psAssignment := PodSetAssignment{
+			Name:     "irrelevant",
+			Flavors:  make(ResourceAssignment),
+			Requests: requests.ToResourceList(),
+			Count:    1,
+		}
+
+		for resName := range requests {
 			if _, found := psAssignment.Flavors[resName]; found {
 				// This resource got assigned the same flavor as its resource group.
 				// No need to compute again.
 				continue
 			}
-			flavors, status := a.findFlavorForPodSetResource(log, i, podSet.Requests, resName, assignment.Usage.Quota)
+			flavors, status := a.findFlavorForPodSetResource(log, psIDs, requests, resName, assignment.Usage.Quota)
+
 			if status.IsError() || len(flavors) == 0 {
 				psAssignment.Flavors = nil
 				psAssignment.Status = status
 				break
 			}
 			psAssignment.append(flavors, status)
 		}
+		for _, podSet := range podSets {
+			podSetFlavors := make(ResourceAssignment)
+			for resource := range podSet.PodSet.Requests {
+				if psAssignment.Flavors[resource] != nil {
+					podSetFlavors[resource] = psAssignment.Flavors[resource]
+				}
+			}
+
+			if len(podSetFlavors) == 0 {
```

  Check if we can get rid of that "if".

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-07T12:39:28Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:536** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-07T12:39:28Z
```diff
@@ -466,24 +485,72 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
+		groupKey := strconv.Itoa(i)
+		if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
+			groupKey = *a.wl.Obj.Spec.PodSets[i].TopologyRequest.PodSetGroup
+		}
+
+		if !contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(requests))
+		for _, podset := range podSets {
+			psIDs = append(psIDs, podset.OriginalIndex)
+
+			for resName := range podset.PodSet.Requests {
+				requests[resName] = requests[resName] + podset.PodSet.Requests[resName]
+			}
+		}
+		psAssignment := PodSetAssignment{
+			Name:     "irrelevant",
+			Flavors:  make(ResourceAssignment),
+			Requests: requests.ToResourceList(),
+			Count:    1,
+		}
+
+		for resName := range requests {
 			if _, found := psAssignment.Flavors[resName]; found {
 				// This resource got assigned the same flavor as its resource group.
 				// No need to compute again.
 				continue
 			}
-			flavors, status := a.findFlavorForPodSetResource(log, i, podSet.Requests, resName, assignment.Usage.Quota)
+			flavors, status := a.findFlavorForPodSetResource(log, psIDs, requests, resName, assignment.Usage.Quota)
+
 			if status.IsError() || len(flavors) == 0 {
 				psAssignment.Flavors = nil
 				psAssignment.Status = status
 				break
 			}
 			psAssignment.append(flavors, status)
 		}
+		for _, podSet := range podSets {
+			podSetFlavors := make(ResourceAssignment)
+			for resource := range podSet.PodSet.Requests {
```

  Check if we have util function for that. There should be a function `FilterKeys`.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-07T12:44:28Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:211** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-07T12:44:28Z
```diff
@@ -208,7 +208,7 @@ func (r *Reconciler) podSets(lws *leaderworkersetv1.LeaderWorkerSet) ([]kueue.Po
 		}
 		if features.Enabled(features.TopologyAwareScheduling) {
 			topologyRequest, err := jobframework.NewPodSetTopologyRequest(
-				&lws.Spec.LeaderWorkerTemplate.LeaderTemplate.ObjectMeta).Build()
+				&lws.Spec.LeaderWorkerTemplate.LeaderTemplate.ObjectMeta).PodSetGroup(ptr.To("lws-group")).Build()
```

  Define this group only if appropriate annotation is defined.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-08T17:48:32Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:211** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-08T17:48:32Z
```diff
@@ -208,7 +208,7 @@ func (r *Reconciler) podSets(lws *leaderworkersetv1.LeaderWorkerSet) ([]kueue.Po
 		}
 		if features.Enabled(features.TopologyAwareScheduling) {
 			topologyRequest, err := jobframework.NewPodSetTopologyRequest(
-				&lws.Spec.LeaderWorkerTemplate.LeaderTemplate.ObjectMeta).Build()
+				&lws.Spec.LeaderWorkerTemplate.LeaderTemplate.ObjectMeta).PodSetGroup(ptr.To("lws-group")).Build()
```

  Done

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-08T17:48:38Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:536** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-08T17:48:38Z
```diff
@@ -466,24 +485,72 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
+		groupKey := strconv.Itoa(i)
+		if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
+			groupKey = *a.wl.Obj.Spec.PodSets[i].TopologyRequest.PodSetGroup
+		}
+
+		if !contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(requests))
+		for _, podset := range podSets {
+			psIDs = append(psIDs, podset.OriginalIndex)
+
+			for resName := range podset.PodSet.Requests {
+				requests[resName] = requests[resName] + podset.PodSet.Requests[resName]
+			}
+		}
+		psAssignment := PodSetAssignment{
+			Name:     "irrelevant",
+			Flavors:  make(ResourceAssignment),
+			Requests: requests.ToResourceList(),
+			Count:    1,
+		}
+
+		for resName := range requests {
 			if _, found := psAssignment.Flavors[resName]; found {
 				// This resource got assigned the same flavor as its resource group.
 				// No need to compute again.
 				continue
 			}
-			flavors, status := a.findFlavorForPodSetResource(log, i, podSet.Requests, resName, assignment.Usage.Quota)
+			flavors, status := a.findFlavorForPodSetResource(log, psIDs, requests, resName, assignment.Usage.Quota)
+
 			if status.IsError() || len(flavors) == 0 {
 				psAssignment.Flavors = nil
 				psAssignment.Status = status
 				break
 			}
 			psAssignment.append(flavors, status)
 		}
+		for _, podSet := range podSets {
+			podSetFlavors := make(ResourceAssignment)
+			for resource := range podSet.PodSet.Requests {
```

  Done

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-08T17:48:46Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:542** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-08T17:48:46Z
```diff
@@ -466,24 +485,72 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
+		groupKey := strconv.Itoa(i)
+		if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
+			groupKey = *a.wl.Obj.Spec.PodSets[i].TopologyRequest.PodSetGroup
+		}
+
+		if !contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(requests))
+		for _, podset := range podSets {
+			psIDs = append(psIDs, podset.OriginalIndex)
+
+			for resName := range podset.PodSet.Requests {
+				requests[resName] = requests[resName] + podset.PodSet.Requests[resName]
+			}
+		}
+		psAssignment := PodSetAssignment{
+			Name:     "irrelevant",
+			Flavors:  make(ResourceAssignment),
+			Requests: requests.ToResourceList(),
+			Count:    1,
+		}
+
+		for resName := range requests {
 			if _, found := psAssignment.Flavors[resName]; found {
 				// This resource got assigned the same flavor as its resource group.
 				// No need to compute again.
 				continue
 			}
-			flavors, status := a.findFlavorForPodSetResource(log, i, podSet.Requests, resName, assignment.Usage.Quota)
+			flavors, status := a.findFlavorForPodSetResource(log, psIDs, requests, resName, assignment.Usage.Quota)
+
 			if status.IsError() || len(flavors) == 0 {
 				psAssignment.Flavors = nil
 				psAssignment.Status = status
 				break
 			}
 			psAssignment.append(flavors, status)
 		}
+		for _, podSet := range podSets {
+			podSetFlavors := make(ResourceAssignment)
+			for resource := range podSet.PodSet.Requests {
+				if psAssignment.Flavors[resource] != nil {
+					podSetFlavors[resource] = psAssignment.Flavors[resource]
+				}
+			}
+
+			if len(podSetFlavors) == 0 {
```

  Done

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-08T17:49:00Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:664** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-08T17:49:00Z
```diff
@@ -585,26 +660,39 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		tryNextFlavor := false
```

  Done, used `message` as error indicator.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-08T17:49:05Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:667** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-08T17:49:05Z
```diff
@@ -585,26 +660,39 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		tryNextFlavor := false
+		for psIdx, psID := range psIDs {
+			podSpec := podSets[psIdx].Template.Spec
+			selector := selectors[psIdx]
```

  Done

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-08T17:49:17Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:508** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-08T17:49:17Z
```diff
@@ -466,24 +485,72 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
+		groupKey := strconv.Itoa(i)
+		if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
+			groupKey = *a.wl.Obj.Spec.PodSets[i].TopologyRequest.PodSetGroup
+		}
+
+		if !contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(requests))
+		for _, podset := range podSets {
+			psIDs = append(psIDs, podset.OriginalIndex)
+
+			for resName := range podset.PodSet.Requests {
```

  Done

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-08T17:49:22Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:489** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-08T17:49:21Z
```diff
@@ -466,24 +485,72 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
+		groupKey := strconv.Itoa(i)
+		if a.wl.Obj.Spec.PodSets[i].TopologyRequest != nil {
```

  Done

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-08T17:49:31Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:426** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-08T17:49:30Z
```diff
@@ -416,6 +417,21 @@ func (a *FlavorAssigner) Assign(log logr.Logger, counts []int32) Assignment {
 	return a.assignFlavors(log, counts)
 }
 
+type IndexedPodSet struct {
+	OriginalIndex    int
+	PodSet           *workload.PodSetResources
+	PodSetAssignment *PodSetAssignment
+}
+
+func contains(slice []string, item string) bool {
```

  Done

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-08T17:49:35Z

- **apis/kueue/v1beta1/workload_types.go:132** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-08T17:49:35Z
```diff
@@ -125,6 +125,8 @@ type PodSetTopologyRequest struct {
 	// For example, in the context of JobSet this value is read from jobset.sigs.k8s.io/replicatedjob-replicas.
 	SubGroupCount *int32 `json:"subGroupCount,omitempty"`
 
+	PodSetGroup *string `json:"podSetGroup,omitempty"`
```

  Done

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-07-18T08:47:07Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:494** by [@gabesaba](https://github.com/gabesaba) — 2025-07-17T13:49:41Z
```diff
@@ -466,24 +477,62 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
+		tr := a.wl.Obj.Spec.PodSets[i].TopologyRequest
+		if tr != nil && tr.PodSetGroup != nil {
+			groupKey = *tr.PodSetGroup
+		}
+
+		if !slices.Contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+
```

  ```suggestion
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:479** by [@gabesaba](https://github.com/gabesaba) — 2025-07-17T14:00:33Z
```diff
@@ -466,24 +477,62 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
```

  Could conflict with a podsetGroupName of [0-9]?

- **pkg/scheduler/flavorassigner/flavorassigner.go:449** by [@gabesaba](https://github.com/gabesaba) — 2025-07-18T08:42:25Z
```diff
@@ -437,14 +445,17 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		},
 	}
 
+	groupedRequests := make(map[string][]IndexedPodSet)
+	var groupsOrder []string
```

  What about creating a new data structure (may just use these two under the hood). So that the caller doesn't have to update both.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-18T08:49:40Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:479** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-18T08:49:40Z
```diff
@@ -466,24 +477,62 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
```

  Yup, I was thinking about introducing a prefix like "_internal" or "_reserved" and documenting that no one should use those. WDYT?

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-18T08:50:06Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:494** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-18T08:50:06Z
```diff
@@ -466,24 +477,62 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
+		tr := a.wl.Obj.Spec.PodSets[i].TopologyRequest
+		if tr != nil && tr.PodSetGroup != nil {
+			groupKey = *tr.PodSetGroup
+		}
+
+		if !slices.Contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+
```

  Just fixed that :)

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-07-18T08:57:54Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:479** by [@gabesaba](https://github.com/gabesaba) — 2025-07-18T08:57:54Z
```diff
@@ -466,24 +477,62 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
```

  This is fine by me. Alternatively, you could make the user defined groups match an id-pattern like [a-z][a-z0-9]*

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T13:56:18Z

- **apis/kueue/v1alpha1/topology_types.go:92** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:12:52Z
```diff
@@ -88,6 +88,10 @@ const (
 	// NodeToReplaceAnnotation is an annotation on a Workload. It holds a
 	// name of a failed node running at least one pod of this workload.
 	NodeToReplaceAnnotation = "alpha.kueue.x-k8s.io/node-to-replace"
+
+	// PodSetGroupName describes a name of the group of PodSets. PodSet Group
```

  ```suggestion
  	// PodSetGroupName is an annotation indicating the name of the group of PodSets. PodSet Group
  ```

- **apis/kueue/v1beta1/workload_types.go:132** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:14:06Z
```diff
@@ -125,6 +125,12 @@ type PodSetTopologyRequest struct {
 	// For example, in the context of JobSet this value is read from jobset.sigs.k8s.io/replicatedjob-replicas.
 	SubGroupCount *int32 `json:"subGroupCount,omitempty"`
 
+	// PodSetGroup indicates the name of the group of PodSets to which this PodSet belongs to.
+	// PodSets with the same `PodSetGroup` should be assigned the same ResourceFlavor
+	//
+	// +optional
+	PodSetGroup *string `json:"podSetGroup,omitempty"`
```

  ```suggestion
  	PodSetGroupName *string `json:"podSetGroupName,omitempty"`
  ```
  To align with the KEP: https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-scheduling#internal-apis

- **pkg/scheduler/flavorassigner/flavorassigner.go:421** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:15:22Z
```diff
@@ -416,6 +418,12 @@ func (a *FlavorAssigner) Assign(log logr.Logger, counts []int32) Assignment {
 	return a.assignFlavors(log, counts)
 }
 
+type IndexedPodSet struct {
```

  Don't export if not used externally (does not seem to be), same for the fields in the struct.

- **pkg/scheduler/flavorassigner/flavorassigner.go:482** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:18:08Z
```diff
@@ -466,23 +477,69 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
+		tr := a.wl.Obj.Spec.PodSets[i].TopologyRequest
+		if tr != nil && tr.PodSetGroup != nil {
```

  ```suggestion
  		if tr := a.wl.Obj.Spec.PodSets[i].TopologyRequest; tr != nil && tr.PodSetGroup != nil {
  ```
  it is preferrable to scope variables for readability, especially in long functions

- **pkg/scheduler/flavorassigner/flavorassigner.go:479** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:19:12Z
```diff
@@ -466,24 +477,62 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
```

  I like the approach ^ to restrict the user-defined values via the annotation, to something like `[a-z][a-z0-9]*` sgtm.
  
  "_internal" or "_reserved" would work either, but seems like overkill, then we need to think about cases, what if user says `_internal_1` :)
  
  The purpose of the annotation is to group a handful of PodSets (currently capped by 8), so a very limited syntax is enough.
  
  I'm ok either in this PR (webhook validation + unit test), or to open a follow up issue.

- **pkg/scheduler/flavorassigner/flavorassigner.go:449** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:39:28Z
```diff
@@ -437,14 +445,17 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		},
 	}
 
+	groupedRequests := make(map[string][]IndexedPodSet)
+	var groupsOrder []string
```

  I like this idea, mainly because it allows to offload the logic here with technical details.
  
  We could approach it:
  1. generically, like `InsertionOrderMultimap` 
  2. specific, just a new file in the package, and maybe name `podSetGroups`
  
  I'm leaning (only slightly) for (2.) as I don't know any other places where we could reuse it for now.
  
  I'm ok in this PR, or to open an issue for follow up.

- **pkg/scheduler/flavorassigner/flavorassigner.go:521** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:45:13Z
```diff
@@ -466,23 +477,69 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
+		tr := a.wl.Obj.Spec.PodSets[i].TopologyRequest
+		if tr != nil && tr.PodSetGroup != nil {
+			groupKey = *tr.PodSetGroup
+		}
+
+		if !slices.Contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(podSets))
+		for idx, podset := range podSets {
+			psIDs[idx] = podset.OriginalIndex
+			requests.Add(podset.PodSet.Requests)
+		}
+
+		groupFlavors := make(ResourceAssignment)
+		for _, ips := range podSets {
+			for resName := range ips.PodSetAssignment.Flavors {
+				groupFlavors[resName] = ips.PodSetAssignment.Flavors[resName]
+				break
+			}
+		}
+		var groupStatus *Status
+
+		for resName := range requests {
+			if _, found := groupFlavors[resName]; found {
 				// This resource got assigned the same flavor as its resource group.
 				// No need to compute again.
 				continue
 			}
-			flavors, status := a.findFlavorForPodSetResource(log, i, podSet.Requests, resName, assignment.Usage.Quota)
+			flavors, status := a.findFlavorForPodSetResource(log, psIDs, requests, resName, assignment.Usage.Quota)
 			if status.IsError() || len(flavors) == 0 {
-				psAssignment.Flavors = nil
-				psAssignment.Status = status
+				groupFlavors = nil
+				groupStatus = status
 				break
 			}
-			psAssignment.append(flavors, status)
+			maps.Copy(groupFlavors, flavors)
+			if groupStatus == nil {
+				groupStatus = status
+			} else if status != nil {
+				groupStatus.reasons = append(groupStatus.reasons, status.reasons...)
+			}
```

  Why is this `append` removed? It seems it nicely encapsulated the technical detail of lazy initialization. Maybe the name was suboptimal?

- **pkg/scheduler/flavorassigner/flavorassigner.go:626** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:46:04Z
```diff
@@ -566,16 +614,24 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 
 	status := &Status{}
 	requests = filterRequestedResources(requests, resourceGroup.CoveredResources)
-	ps := &a.wl.Obj.Spec.PodSets[psID]
-	podSpec := &ps.Template.Spec
+
+	podSets := make([]*kueue.PodSet, len(psIDs))
+	selectors := make([]nodeaffinity.RequiredNodeAffinity, len(psIDs))
+
+	for idx, psID := range psIDs {
+		ps := &a.wl.Obj.Spec.PodSets[psID]
+		podSets[idx] = ps
+
+		selector := flavorSelector(&ps.Template.Spec, resourceGroup.LabelKeys)
+		selectors[idx] = selector
```

  ```suggestion
  		selectors[idx] = flavorSelector(&ps.Template.Spec, resourceGroup.LabelKeys)
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:637** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:54:45Z
```diff
@@ -585,26 +641,37 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		var flavorUnmatchMessage *string
```

  Instead of using `flavorUnmatchMessage` why don't we just collect the messages in `status` directly, as before?
  
  For one it makes the diff larger, but also feels constraining to have only one message.

- **pkg/util/testing/wrappers.go:449** by [@mimowo](https://github.com/mimowo) — 2025-07-18T13:55:03Z
```diff
@@ -446,6 +446,14 @@ func (p *PodSetWrapper) RequiredTopologyRequest(level string) *PodSetWrapper {
 	return p
 }
 
+func (p *PodSetWrapper) PodSetGroup(podSetGroup string) *PodSetWrapper {
```

  ```suggestion
  func (p *PodSetWrapper) PodSetGroup(name string) *PodSetWrapper {
  ```

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-18T14:51:04Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:521** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-18T14:51:04Z
```diff
@@ -466,23 +477,69 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
+		tr := a.wl.Obj.Spec.PodSets[i].TopologyRequest
+		if tr != nil && tr.PodSetGroup != nil {
+			groupKey = *tr.PodSetGroup
+		}
+
+		if !slices.Contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(podSets))
+		for idx, podset := range podSets {
+			psIDs[idx] = podset.OriginalIndex
+			requests.Add(podset.PodSet.Requests)
+		}
+
+		groupFlavors := make(ResourceAssignment)
+		for _, ips := range podSets {
+			for resName := range ips.PodSetAssignment.Flavors {
+				groupFlavors[resName] = ips.PodSetAssignment.Flavors[resName]
+				break
+			}
+		}
+		var groupStatus *Status
+
+		for resName := range requests {
+			if _, found := groupFlavors[resName]; found {
 				// This resource got assigned the same flavor as its resource group.
 				// No need to compute again.
 				continue
 			}
-			flavors, status := a.findFlavorForPodSetResource(log, i, podSet.Requests, resName, assignment.Usage.Quota)
+			flavors, status := a.findFlavorForPodSetResource(log, psIDs, requests, resName, assignment.Usage.Quota)
 			if status.IsError() || len(flavors) == 0 {
-				psAssignment.Flavors = nil
-				psAssignment.Status = status
+				groupFlavors = nil
+				groupStatus = status
 				break
 			}
-			psAssignment.append(flavors, status)
+			maps.Copy(groupFlavors, flavors)
+			if groupStatus == nil {
+				groupStatus = status
+			} else if status != nil {
+				groupStatus.reasons = append(groupStatus.reasons, status.reasons...)
+			}
```

  I removed it, because I am no longer using `psAssignment` to keep flavors and statuses. I could create a separate struct such as `groupAssignment` containing `groupFlavors` and `groupStatus` and implement similar `append` method on that. WDYT?

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-18T14:51:48Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:626** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-18T14:51:48Z
```diff
@@ -566,16 +633,24 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 
 	status := &Status{}
 	requests = filterRequestedResources(requests, resourceGroup.CoveredResources)
-	ps := &a.wl.Obj.Spec.PodSets[psID]
-	podSpec := &ps.Template.Spec
+
+	podSets := make([]*kueue.PodSet, len(psIDs))
+	selectors := make([]nodeaffinity.RequiredNodeAffinity, len(psIDs))
+
+	for idx, psID := range psIDs {
+		ps := &a.wl.Obj.Spec.PodSets[psID]
+		podSets[idx] = ps
+
+		selector := flavorSelector(&ps.Template.Spec, resourceGroup.LabelKeys)
+		selectors[idx] = selector
+	}
 
 	var bestAssignment ResourceAssignment
 	bestAssignmentMode := noFit
 
 	// We will only check against the flavors' labels for the resource.
-	selector := flavorSelector(podSpec, resourceGroup.LabelKeys)
 	attemptedFlavorIdx := -1
-	idx := a.wl.LastAssignment.NextFlavorToTryForPodSetResource(psID, resName)
+	idx := a.wl.LastAssignment.NextFlavorToTryForPodSetResource(psIDs[0], resName)
```

  FlavorFungibility requires more thought. I would postpone it for the follow up.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-18T15:03:03Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:637** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-18T15:03:03Z
```diff
@@ -585,26 +641,37 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		var flavorUnmatchMessage *string
```

  I think we could update `status` directly, but we would have to introduce variable like `shouldSkipFlavor` to continue the flavor-loop. Previous code could just do `continue` right after updating `status`, but in new code we are in another nested loop iterating over PodSets, so if any PodSet fails, I am breaking from PodSet-loop and seeing that `flavorUnmatchMessage` is set, so it means I have to continue in Flavor-loop as well.
  
  All in all we have two options:
  - Keep the current implementation with `flavorUnmatchMessage`
  - Update `status` directly and introduce a variable `shouldContinueToNextFlavor`/`shouldSkipFlavor`
  
  WDYT? Do you have other ideas?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T15:14:57Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:521** by [@mimowo](https://github.com/mimowo) — 2025-07-18T15:14:57Z
```diff
@@ -466,23 +477,69 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
+		tr := a.wl.Obj.Spec.PodSets[i].TopologyRequest
+		if tr != nil && tr.PodSetGroup != nil {
+			groupKey = *tr.PodSetGroup
+		}
+
+		if !slices.Contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(podSets))
+		for idx, podset := range podSets {
+			psIDs[idx] = podset.OriginalIndex
+			requests.Add(podset.PodSet.Requests)
+		}
+
+		groupFlavors := make(ResourceAssignment)
+		for _, ips := range podSets {
+			for resName := range ips.PodSetAssignment.Flavors {
+				groupFlavors[resName] = ips.PodSetAssignment.Flavors[resName]
+				break
+			}
+		}
+		var groupStatus *Status
+
+		for resName := range requests {
+			if _, found := groupFlavors[resName]; found {
 				// This resource got assigned the same flavor as its resource group.
 				// No need to compute again.
 				continue
 			}
-			flavors, status := a.findFlavorForPodSetResource(log, i, podSet.Requests, resName, assignment.Usage.Quota)
+			flavors, status := a.findFlavorForPodSetResource(log, psIDs, requests, resName, assignment.Usage.Quota)
 			if status.IsError() || len(flavors) == 0 {
-				psAssignment.Flavors = nil
-				psAssignment.Status = status
+				groupFlavors = nil
+				groupStatus = status
 				break
 			}
-			psAssignment.append(flavors, status)
+			maps.Copy(groupFlavors, flavors)
+			if groupStatus == nil {
+				groupStatus = status
+			} else if status != nil {
+				groupStatus.reasons = append(groupStatus.reasons, status.reasons...)
+			}
```

  Thx, for explaining, looking into this and will propose path forward.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T15:15:01Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:637** by [@mimowo](https://github.com/mimowo) — 2025-07-18T15:15:01Z
```diff
@@ -585,26 +641,37 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		var flavorUnmatchMessage *string
```

  Thx, for explaining, looking into this and will propose path forward.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T15:41:35Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:521** by [@mimowo](https://github.com/mimowo) — 2025-07-18T15:41:35Z
```diff
@@ -466,23 +477,69 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
+		tr := a.wl.Obj.Spec.PodSets[i].TopologyRequest
+		if tr != nil && tr.PodSetGroup != nil {
+			groupKey = *tr.PodSetGroup
+		}
+
+		if !slices.Contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(podSets))
+		for idx, podset := range podSets {
+			psIDs[idx] = podset.OriginalIndex
+			requests.Add(podset.PodSet.Requests)
+		}
+
+		groupFlavors := make(ResourceAssignment)
+		for _, ips := range podSets {
+			for resName := range ips.PodSetAssignment.Flavors {
+				groupFlavors[resName] = ips.PodSetAssignment.Flavors[resName]
+				break
+			}
+		}
+		var groupStatus *Status
+
+		for resName := range requests {
+			if _, found := groupFlavors[resName]; found {
 				// This resource got assigned the same flavor as its resource group.
 				// No need to compute again.
 				continue
 			}
-			flavors, status := a.findFlavorForPodSetResource(log, i, podSet.Requests, resName, assignment.Usage.Quota)
+			flavors, status := a.findFlavorForPodSetResource(log, psIDs, requests, resName, assignment.Usage.Quota)
 			if status.IsError() || len(flavors) == 0 {
-				psAssignment.Flavors = nil
-				psAssignment.Status = status
+				groupFlavors = nil
+				groupStatus = status
 				break
 			}
-			psAssignment.append(flavors, status)
+			maps.Copy(groupFlavors, flavors)
+			if groupStatus == nil {
+				groupStatus = status
+			} else if status != nil {
+				groupStatus.reasons = append(groupStatus.reasons, status.reasons...)
+			}
```

  Let's keep as is in this PR.
  
  However,  I think it will be much easier going forward if Status was always initialized in psAssignment (and here). There are many places in code which need to now handle the fallback when Status is nil, this is error prone. I will open an issue for it. wdyt?

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-18T15:46:34Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:521** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-18T15:46:34Z
```diff
@@ -466,23 +477,69 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
+		tr := a.wl.Obj.Spec.PodSets[i].TopologyRequest
+		if tr != nil && tr.PodSetGroup != nil {
+			groupKey = *tr.PodSetGroup
+		}
+
+		if !slices.Contains(groupsOrder, groupKey) {
+			groupsOrder = append(groupsOrder, groupKey)
+		}
+
+		groupedRequests[groupKey] = append(groupedRequests[groupKey], IndexedPodSet{OriginalIndex: i, PodSet: &podSet, PodSetAssignment: &psAssignment})
+	}
+
+	for _, groupKey := range groupsOrder {
+		podSets := groupedRequests[groupKey]
+		requests := make(resources.Requests)
+		psIDs := make([]int, len(podSets))
+		for idx, podset := range podSets {
+			psIDs[idx] = podset.OriginalIndex
+			requests.Add(podset.PodSet.Requests)
+		}
+
+		groupFlavors := make(ResourceAssignment)
+		for _, ips := range podSets {
+			for resName := range ips.PodSetAssignment.Flavors {
+				groupFlavors[resName] = ips.PodSetAssignment.Flavors[resName]
+				break
+			}
+		}
+		var groupStatus *Status
+
+		for resName := range requests {
+			if _, found := groupFlavors[resName]; found {
 				// This resource got assigned the same flavor as its resource group.
 				// No need to compute again.
 				continue
 			}
-			flavors, status := a.findFlavorForPodSetResource(log, i, podSet.Requests, resName, assignment.Usage.Quota)
+			flavors, status := a.findFlavorForPodSetResource(log, psIDs, requests, resName, assignment.Usage.Quota)
 			if status.IsError() || len(flavors) == 0 {
-				psAssignment.Flavors = nil
-				psAssignment.Status = status
+				groupFlavors = nil
+				groupStatus = status
 				break
 			}
-			psAssignment.append(flavors, status)
+			maps.Copy(groupFlavors, flavors)
+			if groupStatus == nil {
+				groupStatus = status
+			} else if status != nil {
+				groupStatus.reasons = append(groupStatus.reasons, status.reasons...)
+			}
```

  Sure, let's do it that way. I think we could also introduce something like PodSetGroupAssignment, which would include all PodSetAssignments and update them as we update the Grouped one. That way the code here would be clean and at the same time we would have all Statuses in all podsets up-to-date all the time.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-18T15:56:32Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:449** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-18T15:56:32Z
```diff
@@ -437,14 +445,17 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		},
 	}
 
+	groupedRequests := make(map[string][]IndexedPodSet)
+	var groupsOrder []string
```

  I followed the (2.) and introduced a new file `podset_groups.go` with struct that hides the orderd map complexity.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T15:58:38Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:637** by [@mimowo](https://github.com/mimowo) — 2025-07-18T15:58:38Z
```diff
@@ -585,26 +641,37 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		var flavorUnmatchMessage *string
```

  Hm, so if you just into status, and use break, then you may also get more messages, say for untolerated taints? 
  
  It does not seem like an issue, but I see it would be a change compared to current semantics.
  
  In that case, if we want to preserve the semantics, I would suggest to extract the code which iterates over pod sets `for psIdx, psID := range psIDs {` to a helper function, passing the status there. Once you add the message for one PodSet you can escape downstream logic (and for other PodSets) with return. wdyt?
  
  If you anticipate complications I'm fine with a follow up.

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-18T16:01:08Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:479** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-18T16:01:08Z
```diff
@@ -466,24 +477,62 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
```

  I would like to open a follow up issue and address it in next PR. The risk that there will be a clash of group names is limited.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T16:06:01Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:637** by [@mimowo](https://github.com/mimowo) — 2025-07-18T16:06:01Z
```diff
@@ -585,26 +641,37 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			status.appendf("flavor %s not found", fName)
 			continue
 		}
-		if features.Enabled(features.TopologyAwareScheduling) {
-			if message := checkPodSetAndFlavorMatchForTAS(a.cq, ps, flavor); message != nil {
-				log.Error(nil, *message)
-				status.appendf("%s", *message)
-				continue
+
+		var flavorUnmatchMessage *string
```

  also, the function is getting big, the new code increases indentation, so I think it is fair to refactor in this PR, but up to you. We can follow up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T16:16:03Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:449** by [@mimowo](https://github.com/mimowo) — 2025-07-18T16:16:03Z
```diff
@@ -437,6 +445,11 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 		},
 	}
 
+	// groupedRequests := make(map[string][]indexedPodSet)
+	// var groupsOrder []string
```

  Looks like leftovers

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-18T16:45:09Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:479** by [@mimowo](https://github.com/mimowo) — 2025-07-18T16:45:08Z
```diff
@@ -466,24 +477,62 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
```

  sgtm

### Review by [@lchrzaszcz](https://github.com/lchrzaszcz) (commented) — 2025-07-29T12:58:12Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:479** by [@lchrzaszcz](https://github.com/lchrzaszcz) — 2025-07-29T12:58:12Z
```diff
@@ -466,24 +477,62 @@ func (a *FlavorAssigner) assignFlavors(log logr.Logger, counts []int32) Assignme
 			}
 		}
 
-		for resName := range podSet.Requests {
-			if _, found := psAssignment.Flavors[resName]; found {
+		groupKey := strconv.Itoa(i)
```

  I have created an issue to track that: https://github.com/kubernetes-sigs/kueue/issues/6242
