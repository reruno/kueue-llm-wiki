# PR #589: Prefactor works of implementing job interface

**Summary**: Prefactor works of implementing job interface

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/589

**Last updated**: 2023-03-02T08:20:42Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kerthcet](https://github.com/kerthcet)
- **Branch**: `cleanup/preparation-to-job-interface` → `main`
- **Created**: 2023-02-21T09:07:15Z
- **Updated**: 2023-03-02T08:20:42Z
- **Merged**: 2023-03-02T06:08:55Z
- **Closed**: 2023-03-02T06:08:55Z
- **Labels**: `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 1   **+36 / -42**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

prefactor work before https://github.com/kubernetes-sigs/kueue/pull/544

The job controller work flow would like:
1. handling job is finished
2. handling workload is nil
3. processing only main job related logics
4. handling job is suspended
5. handling job is unsuspended

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-02-21T09:07:20Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e33b886565ddaf0dd2a22173fc555a365e173b7d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6400112b44a3ab0008bb81df |

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-02-21T09:08:23Z

cc @mimowo

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-02-27T14:18:25Z

Updated, PTAL.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-01T15:04:39Z

/approve
@kerthcet just a couple of nits

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-01T15:05:10Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/589#issuecomment-1450301324" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/589#" title="Author self-approved">kerthcet</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-03-02T03:08:42Z

ping @mimowo for LGTM

### Comment by [@mimowo](https://github.com/mimowo) — 2023-03-02T06:07:23Z

/lgtm
Thanks!

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-02-21T10:32:08Z

/lgtm
/assign @alculquicondor 
Thanks!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-21T18:15:03Z

- **pkg/controller/workload/job/job_controller.go:210** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-21T18:07:13Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
```

  `isStandaloneJob`

- **pkg/controller/workload/job/job_controller.go:246** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-21T18:12:48Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  in this case, shouldn't we should suspend the dependent job?
  
  so maybe it should be `if wl == nil && isStandaloneJob` and below (step 6) it should say `if wl == nil || wl.Spec.Admission == nil`

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-22T03:41:57Z

- **pkg/controller/workload/job/job_controller.go:246** by [@kerthcet](https://github.com/kerthcet) — 2023-02-22T03:38:56Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  We only create workload here, leaving the suspend logics to another  reconciler. Passing a nil workload downwards is somehow dangerous.
  
  After creating the workload:
  1. if dependent job is suspended, jump to step5.
  2. if dependent job is unsuspended, jump to step6.

- **pkg/controller/workload/job/job_controller.go:210** by [@kerthcet](https://github.com/kerthcet) — 2023-02-22T03:41:09Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
```

  done

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-22T04:09:44Z

- **pkg/controller/workload/job/job_controller.go:246** by [@kerthcet](https://github.com/kerthcet) — 2023-02-22T04:09:43Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  But one corner case is both parent and child jobs are unsuspended, then we delete the workload manually. Then if the workload:
  - admitted immediately again with the same assignment: it works well
  - admitted immediately again with a different assignment: the node affinity may be changed so not working as expected
  - unadmitted, jobs will be suspended finally.
  
  Then we may have a potential bug here. We can suspend all the jobs here when workload is nil .

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-02-22T08:49:59Z

- **pkg/controller/workload/job/job_controller.go:182** by [@tenzen-y](https://github.com/tenzen-y) — 2023-02-22T08:49:59Z
```diff
@@ -178,11 +178,8 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 		// grab the Workload object, extract the owner...
 		wl := o.(*kueue.Workload)
 		owner := metav1.GetControllerOf(wl)
-		if owner == nil {
-			return nil
-		}
 		// ...make sure it's a Job...
-		if owner.APIVersion != "batch/v1" || owner.Kind != "Job" {
+		if owner == nil || owner.APIVersion != "batch/v1" || owner.Kind != "Job" {
```

  ```suggestion
  		if owner == nil || owner.APIVersion != batchv1.SchemeGroupVersion.String() || owner.Kind != "Job" {
  ```
  
  Can we avoid hardcoding the apiVersion?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-22T15:59:22Z

/assign @mimowo

- **pkg/controller/workload/job/job_controller.go:246** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-22T15:58:12Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  it could also be the case that somehow the parent job controller is crashing (and not able to create a workload) and somehow a user set the child job to suspend=false. We don't want that child job to continue running.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-27T02:57:42Z

- **pkg/controller/workload/job/job_controller.go:246** by [@kerthcet](https://github.com/kerthcet) — 2023-02-27T02:57:42Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  open a ticket here https://github.com/kubernetes-sigs/kueue/issues/595

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-02-27T10:32:59Z

- **pkg/controller/workload/job/job_controller.go:246** by [@mimowo](https://github.com/mimowo) — 2023-02-27T10:32:59Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  @kerthcet can you double check this is an issue currently, we have this integration test: https://github.com/kubernetes-sigs/kueue/blob/b2a5e386d7e9c0e3346660dd01001734f631d7fd/test/integration/controller/job/job_controller_test.go#L245
  
  IIUC the code to ensure that is here: https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/workload/job/job_controller.go#L483-L495

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-27T10:56:49Z

- **pkg/controller/workload/job/job_controller.go:246** by [@kerthcet](https://github.com/kerthcet) — 2023-02-27T10:56:43Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  Sure, I'll take charge of this.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-27T10:57:09Z

- **pkg/controller/workload/job/job_controller.go:182** by [@kerthcet](https://github.com/kerthcet) — 2023-02-27T10:57:09Z
```diff
@@ -178,11 +178,8 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 		// grab the Workload object, extract the owner...
 		wl := o.(*kueue.Workload)
 		owner := metav1.GetControllerOf(wl)
-		if owner == nil {
-			return nil
-		}
 		// ...make sure it's a Job...
-		if owner.APIVersion != "batch/v1" || owner.Kind != "Job" {
+		if owner == nil || owner.APIVersion != "batch/v1" || owner.Kind != "Job" {
```

  Absolutely.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-27T13:31:24Z

- **pkg/controller/workload/job/job_controller.go:246** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-27T13:31:23Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  I agree. This shouldn't be a problem in the existing code.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-27T13:33:36Z

- **pkg/controller/workload/job/job_controller.go:246** by [@kerthcet](https://github.com/kerthcet) — 2023-02-27T13:33:36Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  Yes, I think it will not happen, I just complicated the situation about `admitted immediately again with a different assignment: the node affinity may be changed so not working as expected`, the admitted workload should be create at first, then at that moment, the job will be suspended.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-02-27T13:35:39Z

- **pkg/controller/workload/job/job_controller.go:246** by [@kerthcet](https://github.com/kerthcet) — 2023-02-27T13:35:39Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isMainJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isMainJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
-	wl, err := r.ensureAtMostOneWorkload(ctx, &job, childWorkloads)
+	wl, err := r.ensureAtMostOneWorkload(ctx, &job)
 	if err != nil {
 		log.Error(err, "Getting existing workloads")
 		return ctrl.Result{}, err
 	}
 
-	jobFinishedCond, jobFinished := jobFinishedCondition(&job)
-	// 2. create new workload if none exists
-	if wl == nil {
-		// Nothing to do if the job is finished
-		if jobFinished {
+	// 2. handle job is finished.
+	if jobFinishedCond, jobFinished := jobFinishedCondition(&job); jobFinished {
+		if !isMainJob || wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 			return ctrl.Result{}, nil
 		}
-		if pwName != "" {
+		condition := generateFinishedCondition(jobFinishedCond)
+		apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
+		err := r.client.Status().Update(ctx, wl)
+		if err != nil {
+			log.Error(err, "Updating workload status")
+		}
+		return ctrl.Result{}, err
+	}
+
+	// 3. handle workload is nil.
+	if wl == nil {
+		if !isMainJob {
 			return ctrl.Result{}, nil
```

  > it could also be the case that somehow the parent job controller is crashing (and not able to create a workload) and somehow a user set the child job to suspend=false. We don't want that child job to continue running.
  
  This will also be covered by https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/workload/job/job_controller.go#L483-L495

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-02-27T16:52:13Z

- **pkg/controller/workload/job/job_controller.go:254** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-27T16:47:37Z
```diff
@@ -252,21 +251,8 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 		return ctrl.Result{}, err
 	}
 
-	if pwName == "" {
-		// 3. handle a finished job, if it's the main job.
-		if jobFinished {
-			if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-				return ctrl.Result{}, nil
-			}
-			condition := generateFinishedCondition(jobFinishedCond)
-			apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-			err := r.client.Status().Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload status")
-			}
-			return ctrl.Result{}, err
-		}
-
+	// 4. handle logics only with main job.
```

  ```suggestion
  	// 4. handle WaitForPodsReady only for a standalone job.
  ```

- **pkg/controller/workload/job/job_controller.go:221** by [@alculquicondor](https://github.com/alculquicondor) — 2023-02-27T16:52:01Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isStandaloneJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isStandaloneJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
```

  Add a comment here that if there was no workload, this will suspend the job.
  
  Although I find it confusing. Maybe we should move that part out of the function and call `stopJob` here.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-03-02T02:53:05Z

- **pkg/controller/workload/job/job_controller.go:221** by [@kerthcet](https://github.com/kerthcet) — 2023-03-02T02:53:04Z
```diff
@@ -210,39 +207,41 @@ func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.R
 	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&job))
 	ctx = ctrl.LoggerInto(ctx, log)
 
-	pwName := parentWorkload(&job)
+	isStandaloneJob := parentWorkloadName(&job) == ""
 
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
-	if queueName(&job) == "" && pwName == "" && !r.manageJobsWithoutQueueName {
+	if !r.manageJobsWithoutQueueName && queueName(&job) == "" && isStandaloneJob {
 		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
 		return ctrl.Result{}, nil
 	}
 
 	log.V(2).Info("Reconciling Job")
 
-	var childWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &childWorkloads, client.InNamespace(req.Namespace),
-		client.MatchingFields{ownerKey: req.Name}); err != nil {
-		log.Error(err, "Unable to list child workloads")
-		return ctrl.Result{}, err
-	}
-
 	// 1. make sure there is only a single existing instance of the workload
```

  That's kind of thing I already done in job interface library, let's visit this part on that one.
