# PR #5923: add metric for duration between workload podsReady and preemption

**Summary**: add metric for duration between workload podsReady and preemption

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5923

**Last updated**: 2025-07-16T12:55:06Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@amy](https://github.com/amy)
- **Branch**: `metrics` → `main`
- **Created**: 2025-07-09T23:22:50Z
- **Updated**: 2025-07-16T12:55:06Z
- **Merged**: 2025-07-16T12:54:25Z
- **Closed**: 2025-07-16T12:54:25Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 6   **+113 / -12**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

#### What this PR does / why we need it:
This adds a metric that measures the time between when a workload starts running and preemption. It ultimately measures workload runtime when the workload does not run to completion.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Adds the `pods_ready_to_evicted_time_seconds` metric that measures the time between workload's start,
based on the PodsReady condition, and its eviction.
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2025-07-09T23:22:54Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: amy / name: Amy Chen  (ce4869fb596df141cd73b3b3a622cd597c03dab3)</li></ul><!-- Date Modified: 2025-07-16 12:20:21.734762 -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-09T23:22:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ce4869fb596df141cd73b3b3a622cd597c03dab3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6877990407dad10008013dd9 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-09T23:23:00Z

Hi @amy. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@amy](https://github.com/amy) — 2025-07-09T23:24:35Z

/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-10T09:17:48Z

/ok-to-test

### Comment by [@amy](https://github.com/amy) — 2025-07-10T16:47:54Z

/retest

### Comment by [@amy](https://github.com/amy) — 2025-07-10T21:04:28Z

I will be making changes that updates this code. 
- Currently this PR is getting the time between admission and preemption
- Updates will get the time between pod starting to run and preemption. This is because after admission, a pod can go into podReadyTimeout after admission and never start running. There may be other states after admission that prevent a pod from running.

### Comment by [@amy](https://github.com/amy) — 2025-07-14T17:47:42Z

/hold cancel

### Comment by [@amy](https://github.com/amy) — 2025-07-14T20:45:07Z

Ready for merging if the next review has no comments! Not sure if I have merge permissions on my end.

### Comment by [@amy](https://github.com/amy) — 2025-07-15T17:08:48Z

Made separate PR for localQueue eviction metric consistency bug: https://github.com/kubernetes-sigs/kueue/pull/5984
Context: https://github.com/kubernetes-sigs/kueue/pull/5923#discussion_r2206686279

Need to merge #5984 prior to this one so that the failing unit test can pass.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-16T07:44:51Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-16T07:45:30Z

@amy please rebase, and I'm happy to merge it

### Comment by [@amy](https://github.com/amy) — 2025-07-16T12:49:41Z

@mimowo hurray! Rebased and tests pass. Ready to merge 🎉

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-16T12:52:40Z

LGTM label has been added.  <details>Git tree hash: 74b41b8ba599c600111979fa006df66283bc3dc7</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-16T12:52:41Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5923#" title="Author self-approved">amy</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5923#pullrequestreview-3024818373" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-16T12:54:36Z

Proposal, to clarify a bit more.
/release-note-edit
```release-note
Adds the `pods_ready_to_evicted_time_seconds` metric that measures the time between workload's start,
based on the PodsReady condition, and its eviction.
```

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T09:19:37Z

- **pkg/scheduler/preemption/preemption.go:183** by [@mimowo](https://github.com/mimowo) — 2025-07-10T09:19:37Z
```diff
@@ -174,11 +174,13 @@ func (p *Preemptor) IssuePreemptions(ctx context.Context, preemptor *workload.In
 
 			log.V(3).Info("Preempted", "targetWorkload", klog.KObj(target.WorkloadInfo.Obj), "preemptingWorkload", klog.KObj(preemptor.Obj), "reason", target.Reason, "message", message, "targetClusterQueue", klog.KRef("", string(target.WorkloadInfo.ClusterQueue)))
 			p.recorder.Eventf(target.WorkloadInfo.Obj, corev1.EventTypeNormal, "Preempted", message)
-			metrics.ReportPreemption(preemptor.ClusterQueue, target.Reason, target.WorkloadInfo.ClusterQueue)
+			durationToPreemption := workload.AdmittedToPreemptedTime(target.WorkloadInfo.Obj)
+			metrics.ReportPreemption(preemptor.ClusterQueue, target.Reason, target.WorkloadInfo.ClusterQueue, durationToPreemption)
 		} else {
 			log.V(3).Info("Preemption ongoing", "targetWorkload", klog.KObj(target.WorkloadInfo.Obj), "preemptingWorkload", klog.KObj(preemptor.Obj))
 		}
 		successfullyPreempted.Add(1)
+
```

  nit, revert formatting

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T09:20:28Z

- **pkg/workload/workload.go:616** by [@mimowo](https://github.com/mimowo) — 2025-07-10T09:20:28Z
```diff
@@ -600,6 +600,25 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// AdmittedToPreemptedTime is the amount of time it takes a workload from being admitted into the queue to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func AdmittedToPreemptedTime(wl *kueue.Workload) *time.Duration {
+	var admitted *time.Time
+	var preempted *time.Time
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadAdmitted); c != nil {
+		admitted = &c.LastTransitionTime.Time
+	}
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPreempted); c != nil {
+		preempted = &c.LastTransitionTime.Time
+	}
+	if admitted != nil && preempted != nil {
+		timeToPreemption := preempted.Sub(*admitted)
+		return &timeToPreemption
```

  ```suggestion
  		return ptr.To(preempted.Sub(*admitted))
  ```
  nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T09:25:43Z

- **pkg/metrics/metrics.go:147** by [@mimowo](https://github.com/mimowo) — 2025-07-10T09:25:42Z
```diff
@@ -144,6 +144,15 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	quotaReservedPreemptionTime = prometheus.NewHistogramVec(
```

  I'm wondering if we could make this metric more generic, for example `evicted_workloads_time_seconds` and with labels `cluster_queue` and `reason`. Then `reason=Preempted` would carry the information you want, wdyt?
  
  Note that we already have [`EvictedWorkloadsTotal`](https://github.com/kubernetes-sigs/kueue/blob/621787ee506ae9301afb4fa2305baf9296d794f5/pkg/metrics/metrics.go#L253) using this pattern.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-10T17:04:16Z

- **pkg/controller/core/workload_controller.go:506** by [@mimowo](https://github.com/mimowo) — 2025-07-10T17:04:16Z
```diff
@@ -503,7 +503,8 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		if err == nil {
 			cqName := lq.Spec.ClusterQueue
 			if slices.Contains(r.queues.GetClusterQueueNames(), cqName) {
-				metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByLocalQueueStopped)
+				durationToPreemption := workload.AdmittedToPreemptedTime(wl)
```

  I would prefer to eliminate calling `AdmittedToPreemptedTime` from multiple places. Particularly because maybe in the future we will want to have metrics for `quotaReservationToPreemption` or `podsReadyToPreemption`. 
  
  One way to do it would be to change the `metrics.ReportEvictedWorkloads` function to accept `(evictedWorkload, cqName, reason)`. Then we could resolve the time inside once. Alternatively, add a wrapper function like `ReportEvictedWorkload(evictedWorkload, cqName, reason)` and call the pre-existing function `ReportEvictedWorkloads`.
  
  let me know if you hit some complications.

### Review by [@amy](https://github.com/amy) (commented) — 2025-07-10T20:58:00Z

- **pkg/controller/core/workload_controller.go:506** by [@amy](https://github.com/amy) — 2025-07-10T20:58:00Z
```diff
@@ -503,7 +503,8 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		if err == nil {
 			cqName := lq.Spec.ClusterQueue
 			if slices.Contains(r.queues.GetClusterQueueNames(), cqName) {
-				metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByLocalQueueStopped)
+				durationToPreemption := workload.AdmittedToPreemptedTime(wl)
```

  Yeah getting an import cycle when I try to import "sigs.k8s.io/kueue/pkg/workload" into pkg/metrics/metrics.go. I don't think replacing the parameter with a workload-ish/runtime.Object interface will work.

### Review by [@amy](https://github.com/amy) (commented) — 2025-07-11T04:23:38Z

- **pkg/workload/workload.go:612** by [@amy](https://github.com/amy) — 2025-07-11T04:23:38Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
+	var podsReady *time.Time
+	var preempted *time.Time
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady); c != nil {
```

  Is this the correct status to look for? We need to know the time between when a plain pod is running to eviction. I want to make sure this status isn't specific to kueue's `all-or-nothing` feature.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T12:32:25Z

- **pkg/controller/core/workload_controller.go:506** by [@mimowo](https://github.com/mimowo) — 2025-07-11T12:32:24Z
```diff
@@ -503,7 +503,8 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		if err == nil {
 			cqName := lq.Spec.ClusterQueue
 			if slices.Contains(r.queues.GetClusterQueueNames(), cqName) {
-				metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByLocalQueueStopped)
+				durationToPreemption := workload.AdmittedToPreemptedTime(wl)
```

  I see, in that case, could we have a new function workload.ReportEvictedWorkload() which would compute the duration, and then call metrics.ReportEvictedWorkloads. This way all the places could call workload.ReportEvictedWorkload, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T12:36:15Z

- **pkg/workload/workload.go:612** by [@mimowo](https://github.com/mimowo) — 2025-07-11T12:36:15Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
+	var podsReady *time.Time
+	var preempted *time.Time
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady); c != nil {
```

  Ah, I see, in that case "Admitted" only tells you when Kueue admitted a workload. However, since then kube-scheduler still needs to schedule  the pods + the pods need to start running, which can take time in case of AI workloads to for example pull the image.
  
  In that case, it seems you are looking for the time when "PodsReady=true" condition until Evicted. In that case I would suggest to call the metric "WorkloadsWithPodsReadyToEvictedTimeSeconds`, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T12:36:51Z

- **pkg/workload/workload.go:609** by [@mimowo](https://github.com/mimowo) — 2025-07-11T12:36:50Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
```

  `
  ```suggestion
  func PodsReadyToEvictedTime(wl *kueue.Workload) (*time.Duration, string) {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T12:37:06Z

- **pkg/workload/workload.go:609** by [@mimowo](https://github.com/mimowo) — 2025-07-11T12:37:06Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
```

  here, the string will indicate the reason for eviction

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T12:37:57Z

- **pkg/workload/workload.go:615** by [@mimowo](https://github.com/mimowo) — 2025-07-11T12:37:57Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
+	var podsReady *time.Time
+	var preempted *time.Time
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady); c != nil {
+		if c.Status == metav1.ConditionTrue {
+			podsReady = &c.LastTransitionTime.Time
+		}
+	}
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPreempted); c != nil {
```

  As suggest in the other comments, and by the metric name, let's check `WorkloadEvicted` condition, and retrieve its reason too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-11T12:38:20Z

- **pkg/workload/workload.go:612** by [@mimowo](https://github.com/mimowo) — 2025-07-11T12:38:20Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
+	var podsReady *time.Time
+	var preempted *time.Time
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady); c != nil {
```

  ```suggestion
  	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady); c != nil && c.Status == metav1.ConditionTrue {
  ```

### Review by [@amy](https://github.com/amy) (commented) — 2025-07-12T04:01:17Z

- **pkg/workload/workload.go:615** by [@amy](https://github.com/amy) — 2025-07-12T04:01:17Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
+	var podsReady *time.Time
+	var preempted *time.Time
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady); c != nil {
+		if c.Status == metav1.ConditionTrue {
+			podsReady = &c.LastTransitionTime.Time
+		}
+	}
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPreempted); c != nil {
```

  ```
  WorkloadsWithPodsReadyToEvictedTimeSeconds = prometheus.NewHistogramVec(
  ...
  		}, []string{"cluster_queue", "reason"}, # the metric has the reason
  	)
  ```
  
  `workload.ReportPreemption()`:
  1. calls `WorkloadsWithPodsReadyToEvictedTime(wl)` to get duration
  2. feeds duration and reason to `metrics.ReportPreemption(...)`
  Seen here: https://github.com/kubernetes-sigs/kueue/pull/5923/files#diff-a7490e7f5efa2a59ab508f592b67dd6e7530b9d2e1b19a1b6a7438e6c2386905R1079

### Review by [@amy](https://github.com/amy) (commented) — 2025-07-12T04:01:27Z

- **pkg/workload/workload.go:609** by [@amy](https://github.com/amy) — 2025-07-12T04:01:27Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
```

  See: https://github.com/kubernetes-sigs/kueue/pull/5923#discussion_r2202296643

### Review by [@amy](https://github.com/amy) (commented) — 2025-07-12T05:05:59Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:2020** by [@amy](https://github.com/amy) — 2025-07-12T05:05:59Z
```diff
@@ -1977,6 +1978,47 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectPendingWorkloadsMetric(cq, 0, 0)
 			util.ExpectReservingActiveWorkloadsMetric(cq, 1)
 
+			podsReadyCond := metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionTrue,
+				Reason:  "test",
+				Message: "test",
+			}
+			apimeta.SetStatusCondition(&wl1.Status.Conditions, podsReadyCond)
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Update(ctx, wl1)).Should(gomega.Succeed())
+			util.ExpectPodsReadyCondition(ctx, k8sClient, client.ObjectKeyFromObject(wl1))
+			/*
+				[FAILED] Timed out after 45.001s.
+				  The function passed to Eventually failed at /Users/amychen/Gopath/src/github.com/kubernetes-sigs/kueue/test/util/util.go:371 with:
+				  pods are ready
+				  Expected
+				      <[]v1.Condition | len:2, cap:2>: [
+				          {
+				              Type: "QuotaReserved",
+				              Status: "True",
+				              ObservedGeneration: 1,
+				              LastTransitionTime: {
+				                  Time: 2025-07-11T21:42:54-07:00,
+				              },
+				              Reason: "QuotaReserved",
+				              Message: "Quota reserved in ClusterQueue cluster-queue",
+				          },
+				          {
+				              Type: "Admitted",
+				              Status: "True",
+				              ObservedGeneration: 1,
+				              LastTransitionTime: {
+				                  Time: 2025-07-11T21:42:54-07:00,
+				              },
+				              Reason: "Admitted",
+				              Message: "The workload is admitted",
+				          },
+				      ]
+				  to have condition type PodsReady and status True
+				  In [It] at: /Users/amychen/Gopath/src/github.com/kubernetes-sigs/kueue/test/integration/singlecluster/scheduler/scheduler_test.go:1990 @ 07/11/25 21:43:39.95
+			*/
```

  @mimowo I'm not sure if this is the correct way I should be adding this test to integration. 
  
  Goal: Have an integration test that sets the podReady condition to true. So that we can assert that the `WorkloadsWithPodsReadyToEvictedTimeSeconds` metric works as expected. 
  
  Issue: The way I am currently attempting to test fails the ExpectPodsReadyCondition despite manually setting it. I've included the error above.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T07:13:36Z

- **pkg/workload/workload.go:615** by [@mimowo](https://github.com/mimowo) — 2025-07-14T07:13:36Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
+	var podsReady *time.Time
+	var preempted *time.Time
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady); c != nil {
+		if c.Status == metav1.ConditionTrue {
+			podsReady = &c.LastTransitionTime.Time
+		}
+	}
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPreempted); c != nil {
```

  lgtm, yes we don't need to actually retrieve (return) the reason from this function

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T07:13:48Z

- **pkg/workload/workload.go:609** by [@mimowo](https://github.com/mimowo) — 2025-07-14T07:13:48Z
```diff
@@ -604,6 +604,26 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// PodsReadyToPreemptedTime is the amount of time it takes a workload's pods running to getting preempted.
+// This measures runtime of workloads that do not run to completion (ie are preempted).
+func PodsReadyToPreemptedTime(wl *kueue.Workload) *time.Duration {
```

  +1, thanks for clarifying

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T07:38:38Z

- **pkg/metrics/metrics.go:147** by [@mimowo](https://github.com/mimowo) — 2025-07-14T07:38:38Z
```diff
@@ -144,6 +144,21 @@ The label 'result' can have the following values:
 		}, []string{"cluster_queue"},
 	)
 
+	WorkloadsWithPodsReadyToEvictedTimeSeconds = prometheus.NewHistogramVec(
```

  ```suggestion
  	PodsReadyToEvictedTimeSeconds = prometheus.NewHistogramVec(
  ```
  wdyt? It is shorter, and also aligned with `quotaReservedWaitTime` in terms of naming. The "WorkloadsWith" seems repetative and not reflected in the metrics name anyway.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T07:41:53Z

- **pkg/workload/workload.go:609** by [@mimowo](https://github.com/mimowo) — 2025-07-14T07:41:53Z
```diff
@@ -604,6 +604,24 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// WorkloadsWithPodsReadyToEvictedTime is the amount of time it takes a workload's pods running to getting evicted.
+// This measures runtime of workloads that do not run to completion (ie are evicted).
+func WorkloadsWithPodsReadyToEvictedTime(wl *kueue.Workload) *time.Duration {
```

  Do not export this function as it is only used from within the pkg/workload

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T07:59:16Z

- **pkg/workload/workload.go:615** by [@mimowo](https://github.com/mimowo) — 2025-07-14T07:59:16Z
```diff
@@ -604,6 +604,24 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// WorkloadsWithPodsReadyToEvictedTime is the amount of time it takes a workload's pods running to getting evicted.
+// This measures runtime of workloads that do not run to completion (ie are evicted).
+func WorkloadsWithPodsReadyToEvictedTime(wl *kueue.Workload) *time.Duration {
+	var podsReady *time.Time
+	var evicted *time.Time
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadPodsReady); c != nil && c.Status == metav1.ConditionTrue {
+		podsReady = &c.LastTransitionTime.Time
+	}
+	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); c != nil {
```

  ```suggestion
  	if c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted); c != nil && c.Status == metav1.ConditionTrue {
  ```
  EDIT: I think this was actually a subtle bug - it is possible that we have Evicted=False (from previous run), but PodsRead=true, from new run. In that case we would have negative time. We may actually want to prevent negative times nonetheless.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T08:20:12Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:2020** by [@mimowo](https://github.com/mimowo) — 2025-07-14T08:20:12Z
```diff
@@ -1977,6 +1978,47 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectPendingWorkloadsMetric(cq, 0, 0)
 			util.ExpectReservingActiveWorkloadsMetric(cq, 1)
 
+			podsReadyCond := metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionTrue,
+				Reason:  "test",
+				Message: "test",
+			}
+			apimeta.SetStatusCondition(&wl1.Status.Conditions, podsReadyCond)
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Update(ctx, wl1)).Should(gomega.Succeed())
+			util.ExpectPodsReadyCondition(ctx, k8sClient, client.ObjectKeyFromObject(wl1))
+			/*
+				[FAILED] Timed out after 45.001s.
+				  The function passed to Eventually failed at /Users/amychen/Gopath/src/github.com/kubernetes-sigs/kueue/test/util/util.go:371 with:
+				  pods are ready
+				  Expected
+				      <[]v1.Condition | len:2, cap:2>: [
+				          {
+				              Type: "QuotaReserved",
+				              Status: "True",
+				              ObservedGeneration: 1,
+				              LastTransitionTime: {
+				                  Time: 2025-07-11T21:42:54-07:00,
+				              },
+				              Reason: "QuotaReserved",
+				              Message: "Quota reserved in ClusterQueue cluster-queue",
+				          },
+				          {
+				              Type: "Admitted",
+				              Status: "True",
+				              ObservedGeneration: 1,
+				              LastTransitionTime: {
+				                  Time: 2025-07-11T21:42:54-07:00,
+				              },
+				              Reason: "Admitted",
+				              Message: "The workload is admitted",
+				          },
+				      ]
+				  to have condition type PodsReady and status True
+				  In [It] at: /Users/amychen/Gopath/src/github.com/kubernetes-sigs/kueue/test/integration/singlecluster/scheduler/scheduler_test.go:1990 @ 07/11/25 21:43:39.95
+			*/
```

  I think the reason is that normally the PodsReady condition is set by the Job controller, see [generatePodsReadyCondition](https://github.com/kubernetes-sigs/kueue/blob/f242d0d78a7419e66e075aeed65d763d80de83c1/pkg/controller/jobframework/reconciler.go#L1278). However, this controller is not enabled by default in the integration test suite. So, I think it makes sense to set the status manually, as [here](https://github.com/kubernetes-sigs/kueue/blob/f242d0d78a7419e66e075aeed65d763d80de83c1/test/integration/singlecluster/scheduler/podsready/scheduler_test.go#L160-L170).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T08:21:01Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:2020** by [@mimowo](https://github.com/mimowo) — 2025-07-14T08:21:00Z
```diff
@@ -1977,6 +1978,47 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			util.ExpectPendingWorkloadsMetric(cq, 0, 0)
 			util.ExpectReservingActiveWorkloadsMetric(cq, 1)
 
+			podsReadyCond := metav1.Condition{
+				Type:    kueue.WorkloadPodsReady,
+				Status:  metav1.ConditionTrue,
+				Reason:  "test",
+				Message: "test",
+			}
+			apimeta.SetStatusCondition(&wl1.Status.Conditions, podsReadyCond)
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), wl1)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Update(ctx, wl1)).Should(gomega.Succeed())
+			util.ExpectPodsReadyCondition(ctx, k8sClient, client.ObjectKeyFromObject(wl1))
+			/*
+				[FAILED] Timed out after 45.001s.
+				  The function passed to Eventually failed at /Users/amychen/Gopath/src/github.com/kubernetes-sigs/kueue/test/util/util.go:371 with:
+				  pods are ready
+				  Expected
+				      <[]v1.Condition | len:2, cap:2>: [
+				          {
+				              Type: "QuotaReserved",
+				              Status: "True",
+				              ObservedGeneration: 1,
+				              LastTransitionTime: {
+				                  Time: 2025-07-11T21:42:54-07:00,
+				              },
+				              Reason: "QuotaReserved",
+				              Message: "Quota reserved in ClusterQueue cluster-queue",
+				          },
+				          {
+				              Type: "Admitted",
+				              Status: "True",
+				              ObservedGeneration: 1,
+				              LastTransitionTime: {
+				                  Time: 2025-07-11T21:42:54-07:00,
+				              },
+				              Reason: "Admitted",
+				              Message: "The workload is admitted",
+				          },
+				      ]
+				  to have condition type PodsReady and status True
+				  In [It] at: /Users/amychen/Gopath/src/github.com/kubernetes-sigs/kueue/test/integration/singlecluster/scheduler/scheduler_test.go:1990 @ 07/11/25 21:43:39.95
+			*/
```

  I would suggest to have a dedicated "It" for the new scenario rather than expanding an existing one. Maybe also move the test under `integration/singlecluster/scheduler/podsready`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-14T17:11:12Z

- **pkg/workload/workload.go:611** by [@mimowo](https://github.com/mimowo) — 2025-07-14T17:11:12Z
```diff
@@ -604,6 +604,25 @@ func QueuedWaitTime(wl *kueue.Workload, clock clock.Clock) time.Duration {
 	return clock.Since(queuedTime)
 }
 
+// workloadsWithPodsReadyToEvictedTime is the amount of time it takes a workload's pods running to getting evicted.
+// This measures runtime of workloads that do not run to completion (ie are evicted).
+func workloadsWithPodsReadyToEvictedTime(wl *kueue.Workload) *time.Duration {
+	var podsReady *time.Time
+	var evicted *time.Time
```

  This is a non-blocking nit, but I think it will read better if we move "evicted" down where needed. 
  Also, we can short-circuit the function: `if podsReady== nil {  return nil}` - this way we don't unnecessarily lookup the WorkloadEvicted condition. wdyt?

### Review by [@amy](https://github.com/amy) (commented) — 2025-07-14T22:05:57Z

- **pkg/controller/core/workload_controller_test.go:1651** by [@amy](https://github.com/amy) — 2025-07-14T22:05:57Z
```diff
@@ -1641,6 +1641,14 @@ func TestReconcile(t *testing.T) {
 					Message: "The LocalQueue is stopped",
 				}).
 				Obj(),
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
+					EventType: corev1.EventTypeNormal,
+					Reason:    "EvictedDueToLocalQueueStopped",
+					Message:   "The LocalQueue is stopped",
+				},
+			},
```

  @mimowo you probably want to double check that this updated test is correct.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-15T07:30:54Z

- **pkg/controller/core/workload_controller.go:509** by [@mimowo](https://github.com/mimowo) — 2025-07-15T07:30:54Z
```diff
@@ -503,7 +503,7 @@ func (r *WorkloadReconciler) reconcileOnLocalQueueActiveState(ctx context.Contex
 		if err == nil {
 			cqName := lq.Spec.ClusterQueue
 			if slices.Contains(r.queues.GetClusterQueueNames(), cqName) {
-				metrics.ReportEvictedWorkloads(cqName, kueue.WorkloadEvictedByLocalQueueStopped)
+				workload.ReportEvictedWorkload(r.recorder, wl, cqName, kueue.WorkloadEvictedByLocalQueueStopped, "The LocalQueue is stopped")
 				if features.Enabled(features.LocalQueueMetrics) {
 					metrics.ReportLocalQueueEvictedWorkloads(metrics.LQRefFromWorkload(wl), kueue.WorkloadEvictedByLocalQueueStopped)
 				}
```

  I think this should be dropped now, because `workload.ReportEvictedWorkload` already does it, so we would double-count.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-15T07:32:49Z

- **test/integration/singlecluster/scheduler/podsready/scheduler_test.go:175** by [@mimowo](https://github.com/mimowo) — 2025-07-15T07:32:49Z
```diff
@@ -170,6 +170,36 @@ var _ = ginkgo.Describe("SchedulerWithWaitForPodsReady", func() {
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, devClusterQ.Name, devWl)
 		})
 
+		ginkgo.It("Should emit the PodsReadyToEvictedTimeSeconds metric", func() {
+			ginkgo.By("check the workload gets admitted")
+			ginkgo.By("checking the prod workload gets admitted")
```

  ```suggestion
  			ginkgo.By("create a workload and await its admission")
  ```
  non-blocking nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-15T07:40:40Z

- **pkg/controller/core/workload_controller_test.go:1651** by [@mimowo](https://github.com/mimowo) — 2025-07-15T07:40:40Z
```diff
@@ -1641,6 +1641,14 @@ func TestReconcile(t *testing.T) {
 					Message: "The LocalQueue is stopped",
 				}).
 				Obj(),
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
+					EventType: corev1.EventTypeNormal,
+					Reason:    "EvictedDueToLocalQueueStopped",
+					Message:   "The LocalQueue is stopped",
+				},
+			},
```

  +1. The reason is that inside `reconcileOnLocalQueueActiveState` we called the metrics bump skipping the event emission. I think the fact the event was not emitted for eviction in this scenario could be called a consistency bug, because we would emit the event in all other eviction scenarios. 
  
  If you extract most of the PR (except adding the new metric), then I merge it as bugfix and cherry-pick onto 0.12 and 0.11. It is not really a must, because it is a minor bug no-one called yet for.  OTOH, someone may call it in the future. Are you up to?

### Review by [@amy](https://github.com/amy) (commented) — 2025-07-15T13:44:49Z

- **pkg/controller/core/workload_controller_test.go:1651** by [@amy](https://github.com/amy) — 2025-07-15T13:44:49Z
```diff
@@ -1641,6 +1641,14 @@ func TestReconcile(t *testing.T) {
 					Message: "The LocalQueue is stopped",
 				}).
 				Obj(),
+			wantEvents: []utiltesting.EventRecord{
+				{
+					Key:       types.NamespacedName{Name: "wl", Namespace: "ns"},
+					EventType: corev1.EventTypeNormal,
+					Reason:    "EvictedDueToLocalQueueStopped",
+					Message:   "The LocalQueue is stopped",
+				},
+			},
```

  oh cool! Thanks for pointing out the fix. Added it here as well.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-16T12:52:34Z

/lgtm
/approve
Thank you for the contribution 👍
