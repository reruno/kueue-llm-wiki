# PR #5981: [KEP-2349]: Add MultiKueue support for external custom job

**Summary**: [KEP-2349]: Add MultiKueue support for external custom job

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/5981

**Last updated**: 2025-09-02T10:29:31Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@khrm](https://github.com/khrm)
- **Branch**: `f/kep-externaljob-multikueue` → `main`
- **Created**: 2025-07-15T09:48:14Z
- **Updated**: 2025-09-02T10:29:31Z
- **Merged**: 2025-09-02T09:53:13Z
- **Closed**: 2025-09-02T09:53:13Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+285 / -0**

## Description

This KEP documents how we can add MultiKueue support for external custom job by using configmap and generic multikueue adaptor.

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:
This introduces a new design for adding MultiKueue support for custom job.

#### Which issue(s) this PR fixes:
KEP for #2349
#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-15T09:48:21Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 135555c646ce3d8139a90592f24682bfb6e73140 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68b5de1008625a0008059dba |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-15T09:48:24Z

Welcome @khrm! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-15T09:48:25Z

Hi @khrm. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-16T08:12:27Z

/ok-to-test
I will return to reviewing this after 0.13 is out.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-07-18T14:44:34Z

Maybe @mszadkow or @mwielgus  could be a good reviewer here?

### Comment by [@khrm](https://github.com/khrm) — 2025-07-21T12:32:05Z

Instead of just a Jsonpatch, we should use CEL format like we have for [mutating-admission-policy](https://kubernetes.io/docs/reference/access-authn-authz/mutating-admission-policy/)

I will modify KEP with that approach.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-07-21T12:35:25Z

> Instead of just a Jsonpatch, we should use CEL format like we have for [mutating-admission-policy](https://kubernetes.io/docs/reference/access-authn-authz/mutating-admission-policy/)
> 
> I will modify KEP with that approach.

I’d be careful with MAP. Are you interested in supporting this feature on older K8s clusters? I think MAP will be beta in 1.34 which is not out yet.

### Comment by [@gbenhaim](https://github.com/gbenhaim) — 2025-07-21T13:07:51Z

I would prefer an alternative approach for supporting external frameworks which is similar to how the jobframework works. In short, I think that external frameworks should run their own multikueue admission check and Kueue should provide them the framework to write it. I've explained this approach in https://github.com/kubernetes-sigs/kueue/issues/6117

### Comment by [@khrm](https://github.com/khrm) — 2025-07-21T16:06:49Z

@kannon92 I don't mean supporting MAP. I meant changing our design to how MAP works. So for this KEP,  changing it to use CEL instead of only Jsonpatch.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T10:12:49Z

@khrm huge thank you for working on this and presenting on wg-batch. I will be reviewing the KEP today and next week,

/assign

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T10:29:53Z

> I would prefer an alternative approach for supporting external frameworks which is similar to how the jobframework works. In short, I think that external frameworks should run their own multikueue admission check and Kueue should provide them the framework to write it. I've explained this approach in #6117

We discussed this during the WG-batch yesterday (you may check the recordings).

In a nutshell the approach proposed in this KEP has the following advantages:
- it avoids the need for external frameworks to read the secret, and maintain connections to worker clusters
- currently the MultiKueue AC has coupled the code for Job management and AC management. 

In the alternative design we have two choices: (a) ask developers of the external jobs to implement both Job, AC management and secret management which is super hard and error prone, or (b) do a lot of ground work to decouple. None of that is ideal.

So, I like this MAP-like approach that may not be perfect for all Jobs, but it would be enough for 95% of frameworks. Maybe we need both approaches in the long run, but I would like to start simple to unblock some users as soon as possible / feasible.

### Comment by [@khrm](https://github.com/khrm) — 2025-08-01T15:20:39Z

 
 
Thanks, @mimowo  . Based on your suggestion, I have changed the KEP.

@tenzen-y @kannon92 Can you guys also look into this?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-05T09:36:58Z

```
Fixes #
https://github.com/kubernetes-sigs/kueue/issues/2349
```
nit, change the descirption to `KEP for #2349`. We don't want to say "Fixes" becasue then the bot will close the issue as soon as the KEP is merged.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-05T10:59:06Z

I will check this after coming back from OOO in the next week.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-05T11:02:47Z

@khrm it seems to me the KEP makes the mechanism very light weight, do you maybe have some prototype already? I think we could start using default placing of "managedBy" and "status" fields.

### Comment by [@khrm](https://github.com/khrm) — 2025-08-05T17:52:22Z

Thanks for the review. I would update with changes requested. At present, I am attending Kubecon.

### Comment by [@gbenhaim](https://github.com/gbenhaim) — 2025-08-07T21:20:26Z

@khrm will the MultiKueuWatcher be implemented for the generic job? Any modifications needed to the interface? https://github.com/kubernetes-sigs/kueue/blob/d9645bd2be2ed7d767a3e71c2f6c3e8906c9eccf/keps/693-multikueue/README.md?plain=1#L300

Specifically for tekton pipelineruns we would want to update the pipelinerun in the master cluster each time the pipelinerun in the worker changes.

### Comment by [@khrm](https://github.com/khrm) — 2025-08-08T08:54:48Z

@gbenhaim Yes. It will be implemented. If you look into the code, we generally implement MultiKueueWatcher as part of the same adaptor struct. e.g.: https://github.com/kubernetes-sigs/kueue/blob/d9645bd2be2ed7d767a3e71c2f6c3e8906c9eccf/pkg/controller/jobs/jobset/jobset_multikueue_adapter.go#L121

 Somewhat like this for Generic then:
```
 func (w *GenericAdaptor) GetEmptyList() client.ObjectList {
 	list := &unstructured.UnstructuredList{}
 	list.SetGroupVersionKind(schema.GroupVersionKind{
 		Group:   w.gvk.Group,
 		Version: w.gvk.Version,
 		Kind:    w.gvk.Kind "List",
 	})
 return list
 }

 func (w *GenericAdaptor) WorkloadKeyFor(o runtime.Object) (types.NamespacedName, error) {
 	unstructuredObj, isUnstructured := o.(*unstructured.Unstructured)
 if !isUnstructured {
 return types.NamespacedName{}, fmt.Errorf("not an unstructured object, got type: %T", o)
 	}

 	objGVK := unstructuredObj.GroupVersionKind()
 if objGVK != w.gvk {
 return types.NamespacedName{}, fmt.Errorf("unexpected GVK: expected %s, got %s for object %s", w.gvk, objGVK, klog.KObj(unstructuredObj))
 	}

 	labels := unstructuredObj.GetLabels()
 	prebuiltWl, hasPrebuiltWorkload := labels[constants.PrebuiltWorkloadLabel]
 if !hasPrebuiltWorkload {
 return types.NamespacedName{}, fmt.Errorf("no prebuilt workload found for %s: %s", w.gvk.Kind, klog.KObj(unstructuredObj))
 	}

 return types.NamespacedName{Name: prebuiltWl, Namespace: unstructuredObj.GetNamespace()}, nil
 }
 ```
 
 There might be some issue with this snippet.

### Comment by [@khrm](https://github.com/khrm) — 2025-08-14T11:33:10Z

/retest

### Comment by [@khrm](https://github.com/khrm) — 2025-08-14T13:09:58Z

I have updated the KEP with review comments. Can this be review again?
/cc @tenzen-y and @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-14T13:10:03Z

@khrm: GitHub didn't allow me to request PR reviews from the following users: and.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/5981#issuecomment-3188400653):

>I have updated the KEP with review comments. Can this be review again?
>/cc @tenzen-y and @mimowo


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@khrm](https://github.com/khrm) — 2025-08-21T11:30:10Z

I think I should create an issue in https://github.com/kubernetes/enhancements and get KEP # instead of using Kueue repo.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T18:55:09Z

> I have updated the KEP with review comments. Can this be review again? /cc @tenzen-y and @mimowo

Sorry for the delay. I finally landed here right now.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T18:55:31Z

> I think I should create an issue in https://github.com/kubernetes/enhancements and get KEP # instead of using Kueue repo.

Why do you want to open PR in k/enhancement?

### Comment by [@khrm](https://github.com/khrm) — 2025-08-22T09:23:41Z

@tenzen-y Approach given in #2458 won't work. There's no mechanism to communicate how to sync resource from manager to worker. If you look into that PR, it looks incomplete.

We need two things to sync from manager to worker:
1. Access to secret in kueue namespace for workers.
2. Mechanism to sync.

### Comment by [@khrm](https://github.com/khrm) — 2025-08-22T15:55:49Z




> > I think I should create an issue in https://github.com/kubernetes/enhancements and get KEP # instead of using Kueue repo.
> 
> Why do you want to open PR in k/enhancement?

Not PR. An issue to get correct KEP # .

### Comment by [@kannon92](https://github.com/kannon92) — 2025-08-22T16:25:14Z

> Not PR. An issue to get correct KEP # .

You can take the issue number #2349 from kubernetes-sigs/kueue.

### Comment by [@kannon92](https://github.com/kannon92) — 2025-08-22T16:25:33Z

/retitle [KEP-2349]: Add MultiKueue support for external custom job

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T09:36:45Z

> @tenzen-y Approach given in #2458 won't work. There's no mechanism to communicate how to sync resource from manager to worker. If you look into that PR, it looks incomplete.
> 
> We need two things to sync from manager to worker:
> 
> 1. Access to secret in kueue namespace for workers.
> 2. Mechanism to sync.

Yes. So, the #2458 is still opening and has not been merged. I am proposing to move #2458 forward. I don't say that we should provide the same interface as #2458. We can reconsider the exposing intefaces.

### Comment by [@khrm](https://github.com/khrm) — 2025-09-01T10:00:29Z

> Yes. So, the #2458 is still opening and has not been merged. I am proposing to move #2458 forward. I don't say that we should provide the same interface as #2458. We can reconsider the exposing intefaces.

@tenzen-y Let's say we define this interface in external controller. Will this external controller access the same secret? How do you propose to solve this?

Moreover, would it make sense for the Kueue community to have a generic, JSON Patch-based adapter for this? It would cover most use cases and be analogous to using a declarative Kubernetes Admission Policy instead of writing a custom webhook.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T10:15:14Z

> > Yes. So, the #2458 is still opening and has not been merged. I am proposing to move #2458 forward. I don't say that we should provide the same interface as #2458. We can reconsider the exposing intefaces.
> 
> @tenzen-y Let's say we define this interface in external controller. Will this external controller access the same secret? How do you propose to solve this?

Why can't you store the secret in MultiKueueCluster?
https://github.com/kubernetes-sigs/kueue/blob/8f4f4e9bfdaab335fe0dead0e929b9df0afea744/apis/kueue/v1beta1/multikueue_types.go#L47

> Moreover, would it make sense for the Kueue community to have a generic, JSON Patch-based adapter for this? It would cover most use cases and be analogous to using a declarative Kubernetes Admission Policy instead of writing a custom webhook.

I don't think so. Exposing such full control w/ external API (Config / CRD) causes the request to add more and more fields or flexibilities under the proposing API to satisfy their requirements. I don't say that we shouldn't support their custom use cases.
The internal API allows us to support their custom usecase w/o breaking. If we implement the API, we always care about backward compatibility when we support it.

### Comment by [@khrm](https://github.com/khrm) — 2025-09-01T10:29:12Z

> Why can't you store the secret in MultiKueueCluster?

Yes, but now we will need to give access to external controller. 

> I don't think so. Exposing such full control w/ external API (Config / CRD) causes the request to add more and more fields or flexibilities under the proposing API to satisfy their requirements. I don't say that we shouldn't support their custom use cases.
> The internal API allows us to support their custom usecase w/o breaking. If we implement the API, we always care about backward compatibility when we support it.

That's true, but is alternative is to write same code multiple times?  That's why AdmissionPolicy was added, since most of the webhook are the same code. If you want to have more control, write an external controller.

Maybe, we can have a generic limited controller and at the same time an external controller support. That's why I earlier I had written two solutions.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T10:41:04Z

> > Why can't you store the secret in MultiKueueCluster?
> 
> Yes, but now we will need to give access to external controller.

Why you don't want to give the permisions to the external controller?

> > I don't think so. Exposing such full control w/ external API (Config / CRD) causes the request to add more and more fields or flexibilities under the proposing API to satisfy their requirements. I don't say that we shouldn't support their custom use cases.
> > The internal API allows us to support their custom usecase w/o breaking. If we implement the API, we always care about backward compatibility when we support it.
> 
> That's true, but is alternative is to write same code multiple times? That's why AdmissionPolicy was added, since most of the webhook are the same code. If you want to have more control, write an external controller.
> 
> Maybe, we can have a generic limited controller and at the same time an external controller support. That's why I earlier I had written two solutions.

We can expose some of library that the external controller can leverage.
The same code could be mitigated by the public library, but there are not any ways to avoid API breaks.

### Comment by [@khrm](https://github.com/khrm) — 2025-09-01T10:45:22Z

@tenzen-y Can you look into the alternative 4 then? I think you are proposing something similar. 
Earlier, I had written that as a proposed solution also but removed it.

### Comment by [@khrm](https://github.com/khrm) — 2025-09-01T10:57:43Z

@tenzen-y 
>  but there are not any ways to avoid API breaks.

What if we say that generic adaptor will be limited in certain ways. I think it reduces API breakage chances. No addition of functionality to generic adaptor. If more features are required, then create an external controller. 

> Why you don't want to give the permisions to the external controller?

It's possible to give access, but we want to limit access to worker cluster.  We would need to ensure that external controller's SA can be access by the same set of people that have access to kueue-system SA.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T11:07:37Z

> @tenzen-y
> 
> > but there are not any ways to avoid API breaks.
> 
> What if we say that generic adaptor will be limited in certain ways. I think it reduces API breakage chances. No addition of functionality to generic adaptor. If more features are required, then create an external controller.
> 
> > Why you don't want to give the permisions to the external controller?
> 
> It's possible to give access, but we want to limit access to worker cluster. We would need to ensure that external controller's SA can be access by the same set of people that have access to kueue-system SA.

OK, let me check that.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T12:49:27Z

I still think API fields, except for externalFrameworks, are not needed since the external-controller can implement `MultiKueueAdapter`.

### Comment by [@khrm](https://github.com/khrm) — 2025-09-01T13:17:28Z

> I still think API fields, except for externalFrameworks, are not needed since the external-controller can implement `MultiKueueAdapter`.

Yes, externalFrameworks can only have an array of gvk string like `tekton.dev/v1/PipelineRun`  or kind:gv like `kind`: `PipelineRun`,   if we don't implement generic adaptor.

### Comment by [@khrm](https://github.com/khrm) — 2025-09-01T13:47:11Z

@tenzen-y @mimowo I have refined this KEP further.  I have reduced the API surface while keeping a default adaptor. Check now.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-01T14:59:27Z

Let me summarize my view, and reasoning why I like this approach. 

First, over time three major alternatives are proposed:
1. Support creating separate MultiKueue-like ACs by users and managing them [KEP](https://github.com/kubernetes-sigs/kueue/pull/2458)
2. Declarative API indicating how to create and sync Jobs (this KEP)
3. De-couple AC management and Job management and delegate Job creation and syncing to external controller

I think (1.) is clearly inferior to (2.) and (3.) as it requires opening API to external MultiKueue controllers which is very committal,  and the most complex. Writing ACs is complex, and MK AC is particularly complex, so I really want users to avoid implementing them.

The downsides of (3.):
a) still API is needed to skip handling of  Jobs by the built-in MultiKueue admission check of unknown Job CRDs. This would need to be a list of GVK which are externally handled.
b) it requires huge effort on the developers of the external controllers to read the secret and maintain connections with all the worker clusters. I think it would remain a legitimate feature request to support (2.) for the ease of implementing MK support even if we succeeded with (3.).
c) connecting with external clusters may likely result in errors which need to be reported by API, and the best place would be workload.admissionChecks[*] which is reserved to ACs, not external Job controllers.

I think the most likely outcome of (1.) and (3.) due to complexity is users forking or copy/pasting the Kueue's implementation and hacking on top of it. Design which essentially requires users to copy-paste Kueue code is far from ideal.

A valid concern raised by @tenzen-y about this proposal is that it requires commitment to the API to be maintained long-term. However:
- in the minimal form the API is really small (just the list of GVKs for which the mechanism is enabled). I think this is also unavoidable if we go with (3.). 
- it is not blocking the external solution if we need to ever. For example, as the KEP proposes we could have [ControllerType](https://github.com/kubernetes-sigs/kueue/pull/5981/files#diff-359284c3c284a904713acde8d57992e7dd5020400b955f25b990b7a35b712ae4R101) field indicating "External" if we ever need to have it. 
- long term does not need to be forever - there is [deprecation guideline in k8s](https://kubernetes.io/docs/reference/using-api/deprecation-guide/) which allows to deprecate and remove beta features

Yes, in the extended form the API is a more complex list of patches resembling MAP, but we don't yet have an indication it needs to be extended (AFAIK). Yes, Pods would not be supported by managedBy, but we can support them by a dedicated adapter. I expect most external jobs to be simple managedBy + status sync.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T16:08:06Z

> I think (1.) is clearly inferior to (2.) and (3.) as it requires opening API to external MultiKueue controllers which is very committal, and the most complex. Writing ACs is complex, and MK AC is particularly complex, so I really want users to avoid implementing them.

I also want external-controller developers to rely on the kueue-controller-manager AC approval mechanism. So, I am thinking that we should establish an interface to make those mechanisms easy for the external. That's my proposal and don't indicate that external developers implement (or copy/paste) AC approval mechanism. But, if we can introduce only `externalFrameworks` to realize the possibilities for the external Jobs. That's also acceptable. If we want more flexibility (Patch / ControllerName) as proposed in this KEP, we should consider establishing interface as opposed to expose some of APIs.

As a summary, in the short-term solution, we can introduce __ONLY__ `.multiKueue.externalFrameworks` to make the duration of feature shipment short.
In the long term, or for more flexibility, we should establish the AC approval mechanism and expose it as an interface to external controllers.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-01T16:30:23Z

> I also want external-controller developers to rely on the kueue-controller-manager AC approval mechanism. 

Me too, this KEP is achieving this actually.

> So, I am thinking that we should establish an interface to make those mechanisms easy for the external. That's my proposal and don't indicate that external developers implement (or copy/paste) AC approval mechanism.

I'm not clear how to achieve it other than in the KEP. I expect this would require exposing huge amount of code as a function at compile time. I think it will be really hard to ensure the interface does not break users in the future.

>  If we want more flexibility (Patch / ControllerName) as proposed in this KEP, we should consider establishing interface as opposed to expose some of APIs.

The KEP didn't mention ControllerName but ControllerType. The idea of controllerType is to discriminate between "External" and "Generic" in the future if we want to support both (2.) and (3.) long term. To me, I hope (2.) would be enough long term actually.

EDIT: I don't see use cases for the patches yet, but they may arise. Also, we may need to be able to specify the location of the `managedBy` field. In Kubeflow 1.x it is under spec.runPolicy.managedBy. Since Kubeflow 1.x has a built-in integration we don't need the API field for now, but it shows that some flexibility might be needed.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T16:39:18Z

> I'm not clear how to achieve it other than in the KEP. I expect this would require exposing huge amount of code as a function at compile time. I think it will be really hard to ensure the interface does not break users in the future.

We can break the interface anytime, even with a significant break. That's why I want to select the interface rather than exposed API.

> The KEP didn't mention ControllerName but ControllerType. The idea of controllerType is to discriminate between "External" and "Generic" in the future if we want to support both (2.) and (3.) long term. To me, I hope (2.) would be enough long term actually.
> 
> EDIT: I don't see use cases for the patches yet, but they may arise. Also, we may need to be able to specify the location of the managedBy field. In Kubeflow 1.x it is under spec.runPolicy.managedBy. Since Kubeflow 1.x has a built-in integration we don't need the API field for now, but it shows that some flexibility might be needed.

Yes, `controllerType` is what I wanted to mention. Rather than exposing the placement for managedBy, I would like to select (3). But, I agree that (3) needs significant effort. So, I would like to scrape the use cases by basic line proposed in this KEP. Which step should be customizable, which step should not be customizable.

### Comment by [@khrm](https://github.com/khrm) — 2025-09-01T17:38:05Z

@tenzen-y @mimowo I updated the KEP based on the slack discussion.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-01T18:01:59Z

LGTM label has been added.  <details>Git tree hash: d5788a62018bed4e6778dddf116d7d296bad7f05</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-02T09:52:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/5981#" title="Author self-approved">khrm</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5981#pullrequestreview-3175777721" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/5981#pullrequestreview-3174314317" title="LGTM">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-15T09:50:22Z

/assign @tenzen-y @mimowo

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-15T13:42:31Z

- **keps/2349-multikueue-external-custom-job-support/kep.yaml:10** by [@kannon92](https://github.com/kannon92) — 2025-07-15T13:42:31Z
```diff
@@ -0,0 +1,29 @@
+title: MultiKueue external custom Job support
+kep-number: 2349
+authors:
+  - "@khrm"
+status: implementable
+creation-date: 2025-07-15
+reviewers:
+  - "@tenzen-y"
+  - "@mimowo"
+  - "@trasc"
```

  I don't think this person is reviewing Kueue work.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-15T13:43:47Z

- **keps/2349-multikueue-external-custom-job-support/kep.yaml:10** by [@khrm](https://github.com/khrm) — 2025-07-15T13:43:47Z
```diff
@@ -0,0 +1,29 @@
+title: MultiKueue external custom Job support
+kep-number: 2349
+authors:
+  - "@khrm"
+status: implementable
+creation-date: 2025-07-15
+reviewers:
+  - "@tenzen-y"
+  - "@mimowo"
+  - "@trasc"
```

  OK. I would add you.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-15T13:47:14Z

- **keps/2349-multikueue-external-custom-job-support/kep.yaml:10** by [@kannon92](https://github.com/kannon92) — 2025-07-15T13:47:13Z
```diff
@@ -0,0 +1,29 @@
+title: MultiKueue external custom Job support
+kep-number: 2349
+authors:
+  - "@khrm"
+status: implementable
+creation-date: 2025-07-15
+reviewers:
+  - "@tenzen-y"
+  - "@mimowo"
+  - "@trasc"
```

  I'm not sure I know enough about this area to be an official reviewer..

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-15T13:52:32Z

- **keps/2349-multikueue-external-custom-job-support/kep.yaml:10** by [@khrm](https://github.com/khrm) — 2025-07-15T13:52:32Z
```diff
@@ -0,0 +1,29 @@
+title: MultiKueue external custom Job support
+kep-number: 2349
+authors:
+  - "@khrm"
+status: implementable
+creation-date: 2025-07-15
+reviewers:
+  - "@tenzen-y"
+  - "@mimowo"
+  - "@trasc"
```

  OK. I added name from the Owner file.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-17T15:41:11Z

- **keps/2349-multikueue-external-custom-job-support/README.md:185** by [@kannon92](https://github.com/kannon92) — 2025-07-17T15:41:11Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
```

  Has there been a discussion about adding managedBy to tekton?
  
  For other workloads, upstream communities have added these fields (Kubeflow, k8s Job, k8s JobSet).

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-17T16:54:10Z

- **keps/2349-multikueue-external-custom-job-support/README.md:185** by [@khrm](https://github.com/khrm) — 2025-07-17T16:54:10Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
```

  I was going to add this discussion soon. I wrote an appwrapper controller and I was missing that.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-18T14:40:38Z

- **keps/2349-multikueue-external-custom-job-support/README.md:85** by [@kannon92](https://github.com/kannon92) — 2025-07-18T14:40:37Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
```

  To me, this feels like something that should live with the external controller. This sounds like you are asking Kueue to create a mutating webhook for your controller.
  
  Why can't you add this logic into tekton-kueue?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-18T14:43:42Z

- **keps/2349-multikueue-external-custom-job-support/README.md:76** by [@kannon92](https://github.com/kannon92) — 2025-07-18T14:43:42Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
```

  I really don't like the idea of putting this information into the kueue configuration. In other KEPs we explored the idea of singleton CRs. We could explore a CRD for this.
  
  I'm still not sure on this design.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-18T23:33:44Z

- **keps/2349-multikueue-external-custom-job-support/README.md:76** by [@khrm](https://github.com/khrm) — 2025-07-18T23:33:44Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
```

  I was thinking about a CRD too, but configmap could also suffice.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-18T23:34:16Z

- **keps/2349-multikueue-external-custom-job-support/README.md:85** by [@khrm](https://github.com/khrm) — 2025-07-18T23:34:15Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
```

  Let's say we want to remove something from manager cluster before copying to worker cluster, how will external controller handle this? The external controller itself shouldn't require the secret or mechanism to copy from manager to worker. Same during the sync from worker to manager.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-21T17:21:25Z

- **keps/2349-multikueue-external-custom-job-support/README.md:123** by [@khrm](https://github.com/khrm) — 2025-07-21T17:21:22Z
```diff
@@ -0,0 +1,151 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
```

  @gbenhaim I think you are talking about this implementation.

### Review by [@gbenhaim](https://github.com/gbenhaim) (commented) — 2025-07-21T18:14:45Z

- **keps/2349-multikueue-external-custom-job-support/README.md:123** by [@gbenhaim](https://github.com/gbenhaim) — 2025-07-21T18:14:44Z
```diff
@@ -0,0 +1,151 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
```

  Not exactly, I'm taking about implementing a multikueue ACC for the external framework and reuse most of the code from the kueue repository (by importing it). I don't think it's the external framework controller job to sync the workloads and job between clusters

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-21T19:21:08Z

- **keps/2349-multikueue-external-custom-job-support/README.md:123** by [@khrm](https://github.com/khrm) — 2025-07-21T19:21:08Z
```diff
@@ -0,0 +1,151 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
```

  MultiKueue ACC can sync workload CR but to sync job CR, you need to have a way to define how to sync. Isn't that how the first approach is working?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-07-25T13:22:17Z

- **keps/2349-multikueue-external-custom-job-support/README.md:186** by [@mwielgus](https://github.com/mwielgus) — 2025-07-25T13:22:16Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
+
+This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization.
+- **External Controller Role**: The controller manages its custom job and creates a corresponding `Workload` on the management cluster. It watches for the `Workload` to be admitted by Kueue and synced to a worker cluster. Upon seeing the synced `Workload`, the controller is then responsible for creating the actual job on that worker cluster.
+- **MultiKueue ACC Role**: The MultiKueue Admission Check Controller is job-agnostic. Its only role is to sync the `Workload` object to the appropriate worker cluster once it's admitted. It requires no knowledge of the custom job itself.
+- **Trade-offs**: This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
```

  Did you consider using AppWrapper (https://github.com/project-codeflare/appwrapper)?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2025-07-25T13:23:56Z

- **keps/2349-multikueue-external-custom-job-support/README.md:44** by [@mwielgus](https://github.com/mwielgus) — 2025-07-25T13:23:55Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
```

  Please expand GVK abbreviation.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-29T09:53:35Z

- **keps/2349-multikueue-external-custom-job-support/README.md:186** by [@khrm](https://github.com/khrm) — 2025-07-29T09:53:35Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
+
+This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization.
+- **External Controller Role**: The controller manages its custom job and creates a corresponding `Workload` on the management cluster. It watches for the `Workload` to be admitted by Kueue and synced to a worker cluster. Upon seeing the synced `Workload`, the controller is then responsible for creating the actual job on that worker cluster.
+- **MultiKueue ACC Role**: The MultiKueue Admission Check Controller is job-agnostic. Its only role is to sync the `Workload` object to the appropriate worker cluster once it's admitted. It requires no knowledge of the custom job itself.
+- **Trade-offs**: This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
```

  Yes, we did. There were some problems with that approach:
  1. No way to get status from worker to manager unless we write that ourselves outside AppWrapper. It's much more complex. You need to write a AppWrapper controller with access to worker cluster which can fetch status after Appwrapper succeeds. 
  2. We can't create PipelineRun directly, either, we need to change all things to create AppWrapper instead of PipelineRun. This adds an abstraction.
  3. Some CRD don't have PodSet in the spec. We need to do something which can solve this.
  
  
  I wrote a controller for  [AppWrapper + Tekton](https://github.com/khrm/tekton-appwrapper) which solve some of these issues but still, it's not a good design for end user especially, CI/CD.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-07-29T10:14:16Z

- **keps/2349-multikueue-external-custom-job-support/README.md:44** by [@khrm](https://github.com/khrm) — 2025-07-29T10:14:16Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
```

  Done. Changed it to GroupVersionKind (GVK)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T10:12:24Z

- **keps/2349-multikueue-external-custom-job-support/README.md:76** by [@mimowo](https://github.com/mimowo) — 2025-08-01T10:12:24Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
```

  I prefer the CRD paradigm too, but until we have it, I'm ok in the configMap.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T10:15:46Z

- **keps/2349-multikueue-external-custom-job-support/README.md:87** by [@mimowo](https://github.com/mimowo) — 2025-08-01T10:15:46Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
```

  I believe these fields could be default actually, bringing down the user-facing configuration for most framewrorks to minimum - just the enablement entry. Still, it is valueable to expose the configuration for frameworks which would have the fields in different places.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T10:20:05Z

- **keps/2349-multikueue-external-custom-job-support/README.md:78** by [@mimowo](https://github.com/mimowo) — 2025-08-01T10:20:04Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
```

  We already have the configuration dedicated to MultiKueue: https://github.com/kubernetes-sigs/kueue/blob/e2090f3422c49f5a823b2ae3e20d54b8699056df/apis/config/v1beta1/configuration_types.go#L256. I would suggest to put the configuration there, and don't change the format for the `externalFrameworks` field. So that the upgrades are easier.
  
  For example:
  ```yaml
  multikueue:
     externalFrameworks:
     - name: "taskrun.tekton.dev"
  ```
  in the minimal configuration. Implicitly (based on defaults meaning):
  
  ```yaml
  multikueue:
     externalFrameworks:
     - name: "taskrun.tekton.dev"
       managedBy: ".spec.managedBy"
       creationPatches:
        - op: replace
          path: /spec/managedBy
          value: null
       syncPatches:
       - op: replace
         path: /status
         from: /status 
  ```

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-01T10:53:18Z

- **keps/2349-multikueue-external-custom-job-support/README.md:87** by [@khrm](https://github.com/khrm) — 2025-08-01T10:53:18Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
```

  Yes. A default value makes sense.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-01T10:57:51Z

- **keps/2349-multikueue-external-custom-job-support/README.md:78** by [@khrm](https://github.com/khrm) — 2025-08-01T10:57:51Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
```

  Agreed. This will avoid breaking change.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-01T14:19:27Z

- **keps/2349-multikueue-external-custom-job-support/README.md:78** by [@khrm](https://github.com/khrm) — 2025-08-01T14:19:27Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
```

  Added this change.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-01T14:19:54Z

- **keps/2349-multikueue-external-custom-job-support/README.md:87** by [@khrm](https://github.com/khrm) — 2025-08-01T14:19:54Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
```

  I added point about default values.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-08-01T15:33:04Z

- **keps/2349-multikueue-external-custom-job-support/README.md:186** by [@kannon92](https://github.com/kannon92) — 2025-08-01T15:33:04Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
+
+This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization.
+- **External Controller Role**: The controller manages its custom job and creates a corresponding `Workload` on the management cluster. It watches for the `Workload` to be admitted by Kueue and synced to a worker cluster. Upon seeing the synced `Workload`, the controller is then responsible for creating the actual job on that worker cluster.
+- **MultiKueue ACC Role**: The MultiKueue Admission Check Controller is job-agnostic. Its only role is to sync the `Workload` object to the appropriate worker cluster once it's admitted. It requires no knowledge of the custom job itself.
+- **Trade-offs**: This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
```

  Can you add this as an alternative in this design doc?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-08-01T15:33:12Z

- **keps/2349-multikueue-external-custom-job-support/README.md:185** by [@kannon92](https://github.com/kannon92) — 2025-08-01T15:33:12Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
```

  SGTM

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-05T10:36:01Z

LGTM overall, mostly editorial comments

- **keps/2349-multikueue-external-custom-job-support/README.md:33** by [@mimowo](https://github.com/mimowo) — 2025-08-05T09:37:32Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
```

  ```suggestion
  Kueue should give a possibility to manage such custom Jobs across multiple clusters.
  ```

- **keps/2349-multikueue-external-custom-job-support/README.md:37** by [@mimowo](https://github.com/mimowo) — 2025-08-05T09:38:12Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
```

  ```suggestion
  Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
  ```

- **keps/2349-multikueue-external-custom-job-support/README.md:28** by [@mimowo](https://github.com/mimowo) — 2025-08-05T09:39:30Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
```

  ```suggestion
   This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
  ```

- **keps/2349-multikueue-external-custom-job-support/README.md:47** by [@mimowo](https://github.com/mimowo) — 2025-08-05T09:46:47Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
```

  ```suggestion
  ### User Stories 
  
  As a cluster administrator I would like to have the ability to schedule Jobs
  which aren't natively supported by Kueue, eg Tekton.
  ```

- **keps/2349-multikueue-external-custom-job-support/README.md:76** by [@mimowo](https://github.com/mimowo) — 2025-08-05T09:52:25Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
```

  ```suggestion
       multikueue:
         externalFrameworks:
           - group: "pipelinerun.tekton.dev"
             version: "v1"
             kind: "PipelineRun"
             managedBy: ".spec.managedBy"
             creationPatches:
             syncPatches:
  ```
  I don't think the key `multiKueueAdapter` is useful here, because GVK are also part of the adapter definition.
  Alternatively I would consider renaming `externalFrameworks` to `customAdapters`.

- **keps/2349-multikueue-external-custom-job-support/README.md:80** by [@mimowo](https://github.com/mimowo) — 2025-08-05T09:53:25Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
```

  Open question, why not just `name: pipelinerun.tekton.dev.v1.PipelineRun` following the format from the main "externalFrameworks" configuration?

- **keps/2349-multikueue-external-custom-job-support/README.md:121** by [@mimowo](https://github.com/mimowo) — 2025-08-05T10:16:28Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+    on the worker cluster.
+        - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
```

  ```suggestion
  For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
  - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
  - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
  - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
  ```

- **keps/2349-multikueue-external-custom-job-support/README.md:123** by [@mimowo](https://github.com/mimowo) — 2025-08-05T10:28:23Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+    on the worker cluster.
+        - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
```

  TBH, it seems this section isn't a new design, it is just emphasizing the current status quo. Unless it was requested by other reviewers I think it is safe to drop.

- **keps/2349-multikueue-external-custom-job-support/README.md:175** by [@mimowo](https://github.com/mimowo) — 2025-08-05T10:29:38Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+    on the worker cluster.
+        - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
+
+This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization.
+- **External Controller Role**: The controller manages its custom job and creates a corresponding `Workload` on the management cluster. It watches for the `Workload` to be admitted by Kueue and synced to a worker cluster. Upon seeing the synced `Workload`, the controller is then responsible for creating the actual job on that worker cluster.
+- **MultiKueue ACC Role**: The MultiKueue Admission Check Controller is job-agnostic. Its only role is to sync the `Workload` object to the appropriate worker cluster once it's admitted. It requires no knowledge of the custom job itself.
+- **Trade-offs**: This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
```

  Maybe for 
  Alpha:
  - implementation behind the feature gate
  - implement support for the default placing of the managedBy and status fields (.spec.managedBy and .status)
  
  Beta:
  - implement support for custom patches (for different placement of managedBy or status) (this might be done in Alpha, but it is not a requirement for Alpha)
  - all major reported bugs are fixed
  - feature gate enabled by default
  - Re-evaluate replacing the existing adapters for built-in integrations with the in-memory configuration of the generic adapter
  
  
  GA: 
  - all reported bugs are fixed
  - feature gate removed

- **keps/2349-multikueue-external-custom-job-support/kep.yaml:29** by [@mimowo](https://github.com/mimowo) — 2025-08-05T10:31:42Z
```diff
@@ -0,0 +1,29 @@
+title: MultiKueue external custom Job support
+kep-number: 2349
+authors:
+  - "@khrm"
+status: implementable
+creation-date: 2025-07-15
+reviewers:
+  - "@tenzen-y"
+  - "@mimowo"
+  - "@mbobrovskyi"
+approvers:
+  - "@tenzen-y"
+  - "@mimowo"
+
+see-also:
+  - "/keps/693-multikueue"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.14"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.14"
+  beta: "v0.15"
```

  Please add 
  ```
  feature-gates:
    - MultiKueueAdaptersForCustomJobs
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-05T10:55:53Z

- **keps/2349-multikueue-external-custom-job-support/README.md:199** by [@mimowo](https://github.com/mimowo) — 2025-08-05T10:51:42Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+    on the worker cluster.
+        - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
+
+This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization.
+- **External Controller Role**: The controller manages its custom job and creates a corresponding `Workload` on the management cluster. It watches for the `Workload` to be admitted by Kueue and synced to a worker cluster. Upon seeing the synced `Workload`, the controller is then responsible for creating the actual job on that worker cluster.
+- **MultiKueue ACC Role**: The MultiKueue Admission Check Controller is job-agnostic. Its only role is to sync the `Workload` object to the appropriate worker cluster once it's admitted. It requires no knowledge of the custom job itself.
+- **Trade-offs**: This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
+1. Dedicated API for MultiKueue communication between admission check controller and abstraction layer controller.
+
+2. Don't do any code changes, a custom controller can: Instead of having MultiKueue cluster associated with controller name, change the Multikueue cluster status to use a custom active condition for old controllers (controller name) that use it.
+
+3. Introduce ability to create Multikueue Admission Check controllers with custom set of adapters.
```

  ```suggestion
  3. Introduce ability to create Multikueue Admission Check controllers with custom set of adapters.
  
  Reasons to defer: 
  - it is much harder to use by users as they need to enlist multiple MultiKueue Amission Checks per CQ as initially proposed in https://github.com/kubernetes-sigs/kueue/pull/2458/files#diff-359284c3c284a904713acde8d57992e7dd5020400b955f25b990b7a35b712ae4R173-R175
  - The external admission checks would need to largely repeat the code of the MultiKueue built-in controller
  - Since the solution involves another big controller it is harder to maintain
  ```

- **keps/2349-multikueue-external-custom-job-support/README.md:194** by [@mimowo](https://github.com/mimowo) — 2025-08-05T10:53:22Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+    on the worker cluster.
+        - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
+
+This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization.
+- **External Controller Role**: The controller manages its custom job and creates a corresponding `Workload` on the management cluster. It watches for the `Workload` to be admitted by Kueue and synced to a worker cluster. Upon seeing the synced `Workload`, the controller is then responsible for creating the actual job on that worker cluster.
+- **MultiKueue ACC Role**: The MultiKueue Admission Check Controller is job-agnostic. Its only role is to sync the `Workload` object to the appropriate worker cluster once it's admitted. It requires no knowledge of the custom job itself.
+- **Trade-offs**: This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
+1. Dedicated API for MultiKueue communication between admission check controller and abstraction layer controller.
+
+2. Don't do any code changes, a custom controller can: Instead of having MultiKueue cluster associated with controller name, change the Multikueue cluster status to use a custom active condition for old controllers (controller name) that use it.
```

  ```suggestion
  2. Don't do any code changes, a custom controller can: Instead of having MultiKueue cluster associated with controller name, change the Multikueue cluster status to use a custom active condition for old controllers (controller name) that use it.
  
  Reasons to defer:
  - The complexity of the external controllers which now need to essentially do the role of the MultiKueue Admission Checks, eg. managing the AC state, loading and using secrets
  ```

- **keps/2349-multikueue-external-custom-job-support/README.md:188** by [@mimowo](https://github.com/mimowo) — 2025-08-05T10:55:41Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+    on the worker cluster.
+        - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
+
+This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization.
+- **External Controller Role**: The controller manages its custom job and creates a corresponding `Workload` on the management cluster. It watches for the `Workload` to be admitted by Kueue and synced to a worker cluster. Upon seeing the synced `Workload`, the controller is then responsible for creating the actual job on that worker cluster.
+- **MultiKueue ACC Role**: The MultiKueue Admission Check Controller is job-agnostic. Its only role is to sync the `Workload` object to the appropriate worker cluster once it's admitted. It requires no knowledge of the custom job itself.
+- **Trade-offs**: This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
+1. Dedicated API for MultiKueue communication between admission check controller and abstraction layer controller.
+
```

  ```suggestion
  1. Dedicated API for MultiKueue communication between admission check controller and abstraction layer controller.
  
  Reasons to defer:
  - overall complexity, requires a communication protocol like gRPC between MultiKueue AdmissionChecks and external controllers. 
  - more difficult to debug as multiple layers are involved.
  - requires to maintain compatibility between independently compiled binaries.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-05T14:49:03Z

- **keps/2349-multikueue-external-custom-job-support/README.md:70** by [@mimowo](https://github.com/mimowo) — 2025-08-05T14:49:03Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
```

  Please also add a note that the mechanism would allow to replace implementation of the multiple built-in adapters, by registering the in-memory Generic Adapter for their GVKs.
  
  For example I see no blocker to use it for JobSet, RayCluster, RayJob - it would be a great win to drop so much of code.
  
  It would be a great side-effect win.

### Review by [@gbenhaim](https://github.com/gbenhaim) (commented) — 2025-08-07T20:03:20Z

- **keps/2349-multikueue-external-custom-job-support/README.md:70** by [@gbenhaim](https://github.com/gbenhaim) — 2025-08-07T20:03:20Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
```

  @mimowo out of curiosity, which adapters do you think it won't able to replace and why? @khrm I think it would be useful to document what are the use cases that we know about that won't fit the proposed generic solution, so maintainers of external framework will have more info when they need to choose an implementation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-08T07:06:25Z

- **keps/2349-multikueue-external-custom-job-support/README.md:70** by [@mimowo](https://github.com/mimowo) — 2025-08-08T07:06:25Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
```

  > @mimowo out of curiosity, which adapters do you think it won't able to replace and why? 
  
  Thank you for the question. The ones which are not fitting the generalized adapter:
  1. Job
  2. Pod
  
  Reasons:
  1. short term only, because we are using MultiKueueBatchJobWithManagedBy, this env variable will probably go away in a year or so when the JobManagedBy feature in k8s reaches GA
  2. because we re using schedulingGates instead of managedBy, unless we can generalize the solution to add / remove schedulingGates, but I think we can leave it out of scope for now
  
  >  @khrm I think it would be useful to document what are the use cases that we know about that won't fit the proposed generic solution, so maintainers of external framework will have more info when they need to choose an implementation.
  
  +1

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-14T10:45:51Z

- **keps/2349-multikueue-external-custom-job-support/README.md:185** by [@khrm](https://github.com/khrm) — 2025-08-14T10:45:51Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
```

  There's PR for that here: https://github.com/tektoncd/pipeline/pull/8965

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-14T11:00:45Z

- **keps/2349-multikueue-external-custom-job-support/README.md:80** by [@khrm](https://github.com/khrm) — 2025-08-14T11:00:44Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
```

  We use this for creating client. So we need to know proper GVK. `version` and `groupname` both can accept `.` , etc.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-14T11:28:19Z

- **keps/2349-multikueue-external-custom-job-support/README.md:70** by [@khrm](https://github.com/khrm) — 2025-08-14T11:28:19Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
```

  I have added this to KEP.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-14T11:30:28Z

- **keps/2349-multikueue-external-custom-job-support/README.md:175** by [@khrm](https://github.com/khrm) — 2025-08-14T11:30:27Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+    on the worker cluster.
+        - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
+
+This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization.
+- **External Controller Role**: The controller manages its custom job and creates a corresponding `Workload` on the management cluster. It watches for the `Workload` to be admitted by Kueue and synced to a worker cluster. Upon seeing the synced `Workload`, the controller is then responsible for creating the actual job on that worker cluster.
+- **MultiKueue ACC Role**: The MultiKueue Admission Check Controller is job-agnostic. Its only role is to sync the `Workload` object to the appropriate worker cluster once it's admitted. It requires no knowledge of the custom job itself.
+- **Trade-offs**: This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
```

  I have added this to the KEP. Please verify

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-14T11:31:52Z

- **keps/2349-multikueue-external-custom-job-support/kep.yaml:29** by [@khrm](https://github.com/khrm) — 2025-08-14T11:31:52Z
```diff
@@ -0,0 +1,29 @@
+title: MultiKueue external custom Job support
+kep-number: 2349
+authors:
+  - "@khrm"
+status: implementable
+creation-date: 2025-07-15
+reviewers:
+  - "@tenzen-y"
+  - "@mimowo"
+  - "@mbobrovskyi"
+approvers:
+  - "@tenzen-y"
+  - "@mimowo"
+
+see-also:
+  - "/keps/693-multikueue"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.14"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.14"
+  beta: "v0.15"
```

  Added this. :)

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-14T11:32:16Z

- **keps/2349-multikueue-external-custom-job-support/README.md:186** by [@khrm](https://github.com/khrm) — 2025-08-14T11:32:16Z
```diff
@@ -0,0 +1,153 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+
+```
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+       on the worker cluster.
+           - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
+
+This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization.
+- **External Controller Role**: The controller manages its custom job and creates a corresponding `Workload` on the management cluster. It watches for the `Workload` to be admitted by Kueue and synced to a worker cluster. Upon seeing the synced `Workload`, the controller is then responsible for creating the actual job on that worker cluster.
+- **MultiKueue ACC Role**: The MultiKueue Admission Check Controller is job-agnostic. Its only role is to sync the `Workload` object to the appropriate worker cluster once it's admitted. It requires no knowledge of the custom job itself.
+- **Trade-offs**: This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
```

  I have added this.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-18T11:12:08Z

- **keps/2349-multikueue-external-custom-job-support/README.md:123** by [@khrm](https://github.com/khrm) — 2025-08-18T11:12:08Z
```diff
@@ -0,0 +1,172 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Workload-Only Sync for External Controllers](#workload-only-sync-for-external-controllers)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+Ability to schedule resources not yet supported by kueue like Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+       The  workload reconciler will be modified so that when it encounters a workload whose owner GVK does not match any of the built-in adapters, it will:
+        -  Look up the GVK from the configmap..
+        - If no entry is found, the workload is rejected.
+        - If an entry is found, it will use a generic adapter that performs actions based on the rules:
+        - *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+        - *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object
+    on the worker cluster.
+        - *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status
+       subresource.
+
+### Workload-Only Sync for External Controllers
```

  It's not current status quo, but I have moved this in alternative section with details on this.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-22T09:11:29Z

I still want to urge that we should take https://github.com/kubernetes-sigs/kueue/pull/2458 approach instead of this one.

- **keps/2349-multikueue-external-custom-job-support/kep.yaml:29** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T20:32:05Z
```diff
@@ -0,0 +1,31 @@
+title: MultiKueue external custom Job support
+kep-number: 2349
+authors:
+  - "@khrm"
+status: implementable
+creation-date: 2025-07-15
+reviewers:
+  - "@tenzen-y"
+  - "@mimowo"
+  - "@mbobrovskyi"
+approvers:
+  - "@tenzen-y"
+  - "@mimowo"
+
+see-also:
+  - "/keps/693-multikueue"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.14"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.14"
+  beta: "v0.15"
```

  ```suggestion
  ```
  
  We should revisit Beta graduation timing later.

- **keps/2349-multikueue-external-custom-job-support/README.md:30** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-22T08:28:12Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
```

  ```suggestion
  This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
  ```

- **keps/2349-multikueue-external-custom-job-support/README.md:118** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-22T08:58:19Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
```

  I don't think that these are improvements. The core problem is where we should impose the complexity, internal development vs API.
  
  This KEP approach imposes those to Config API. The API has highly maintainance costs since we can NOT break that w/o API version change, which means need to keep backward compatibility in the future. Even when the fixes are caused by bugs, we can not change bug.
  
  Imposing complexity on the internal mechanism allows us to avoid API breaks and lower maintenance costs since caring about backward compatibility could be avoided.

- **keps/2349-multikueue-external-custom-job-support/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-22T09:00:56Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
+
+#### Beta
+- **Milestone:** v0.15
+- All major reported bugs are fixed.
+- The feature gate is enabled by default.
+- Re-evaluate replacing the existing adapters for built-in integrations with the in-memory configuration of the generic adapter.
```

  I guess that this will never happen, since if we accept the current proposed approach, we can not remove the added APIs. This approach just rush the short-term approach that does not have ability to switch to long-term approach (exposing MulitKueue adapter interface to external).

- **keps/2349-multikueue-external-custom-job-support/README.md:118** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-22T09:11:12Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
```

  One of the motivations to support the external framework in MultiKueue is to avoid bringing the external framework specifications to the kueue.
  
  However, the proposed approach can not mitigate the risks of that. We should expose the MultiKueue adapter to external and should allow external developers flexibility by implementing the external manager.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-22T09:33:35Z

- **keps/2349-multikueue-external-custom-job-support/README.md:177** by [@khrm](https://github.com/khrm) — 2025-08-22T09:33:34Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
+
+#### Beta
+- **Milestone:** v0.15
+- All major reported bugs are fixed.
+- The feature gate is enabled by default.
+- Re-evaluate replacing the existing adapters for built-in integrations with the in-memory configuration of the generic adapter.
```

  We won't remove added APIs. We can convert the existing MultiKueue adapters use an in-memory definition during initialisation.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-22T09:36:17Z

- **keps/2349-multikueue-external-custom-job-support/README.md:118** by [@khrm](https://github.com/khrm) — 2025-08-22T09:36:16Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
```

  I agree about bugs and breaking change but shouldn't our behavior of syncing from manager to local be defined. This KEP only expose that behaviour as a configurable API.

### Review by [@ravisantoshgudimetla](https://github.com/ravisantoshgudimetla) (commented) — 2025-08-29T20:08:37Z

- **keps/2349-multikueue-external-custom-job-support/README.md:85** by [@ravisantoshgudimetla](https://github.com/ravisantoshgudimetla) — 2025-08-29T20:08:37Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
```

  Apologies for joining the party late. I feel the same - It looks like this is the responsibility of external controller. I am wondering what is wrong with external controller handling it? Is the cognitive load on plugin developer a concern. I felt like the user should be able to submit to the custom type -> have the logic to translate it a workload object -> Submit the workload to the management cluster -> Get the name of the cluster suggested by Multikueue -> Create and Sync the workload there. There are some workflow changes that can be tweaked but I'm wondering what is wrong with putting this burden on plugin developer and making it Multi-Kueue just a workload placement engine - similar to core k8s scheduler.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-08-30T20:45:17Z

- **keps/2349-multikueue-external-custom-job-support/README.md:85** by [@khrm](https://github.com/khrm) — 2025-08-30T20:45:17Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
```

  Actually, it's still the responsibility of external controller to generate workload. 
  
  The only thing we have exposed or made configurable is just ability of MultiKueue adaptor to sync resource and status between clusters.
  
  The issue isn't the code. The issue is that we will need to run a sync controller of workload in the same namespace and give it access to the same secret which MultiKueue has access to.  And there's no benefit to doing all this.  It's just a sync behaviour which is common for all CRs.
  
  
  Of course, if someone want to create an external controller, there's nothing stopping from doing that. 
  
  It made sense to have an external controller for local workload creation due to the complexity of lifecycle involved, but I don't see that in case of MultiKueue sync.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T09:26:58Z

- **keps/2349-multikueue-external-custom-job-support/README.md:118** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T09:26:58Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
```

  > shouldn't our behavior of syncing from manager to local be defined.
  
  Yes. But, the behavior should be defined as the internal API w/o CRD and Config based on the above my reason.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T09:29:22Z

- **keps/2349-multikueue-external-custom-job-support/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T09:29:22Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
+
+#### Beta
+- **Milestone:** v0.15
+- All major reported bugs are fixed.
+- The feature gate is enabled by default.
+- Re-evaluate replacing the existing adapters for built-in integrations with the in-memory configuration of the generic adapter.
```

  > We won't remove added APIs. We can convert the existing MultiKueue adapters use an in-memory definition during initialisation.
  
  That means we never revisit the in-memory configuration solution. We will keep the Config API. My proposal is that we should not expose the definition to the external API (Config / CRD).

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-09-01T09:42:16Z

- **keps/2349-multikueue-external-custom-job-support/README.md:177** by [@khrm](https://github.com/khrm) — 2025-09-01T09:42:16Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
+
+#### Beta
+- **Milestone:** v0.15
+- All major reported bugs are fixed.
+- The feature gate is enabled by default.
+- Re-evaluate replacing the existing adapters for built-in integrations with the in-memory configuration of the generic adapter.
```

  You meant exposing sync logic to external API?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T09:48:45Z

- **keps/2349-multikueue-external-custom-job-support/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T09:48:45Z
```diff
@@ -0,0 +1,214 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
+
+#### Beta
+- **Milestone:** v0.15
+- All major reported bugs are fixed.
+- The feature gate is enabled by default.
+- Re-evaluate replacing the existing adapters for built-in integrations with the in-memory configuration of the generic adapter.
```

  Which existing logic do you want to customize?
  What [interfaces](https://github.com/kubernetes-sigs/kueue/blob/68e7cc8c414871a28dee1a8a3ff4036f5b4c10e5/pkg/controller/jobframework/interface.go#L234-L264) is missing capability in your case

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T11:13:41Z

- **keps/2349-multikueue-external-custom-job-support/README.md:227** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T11:13:40Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+#### Job Flow from Manager to Worker
+   1. User: Submits the custom job (e.g., PipelineRun) with queue-name and managedBy fields to the management cluster.
+   2. External Controller creates a Workload object for the job.
+   3. MultiKueue admits it once quota is available in worker, assigning it to a worker cluster.
+   3. MultiKueue uses Generic Adapter clones the job, applies creationPatches (like removing managedBy), and creates it on the worker cluster.
+   4. MultiKueue Generic Adaptor is used to periodically fetch the job's status from the worker and applies it to the original job on the management cluster using syncPatches.
+   5. When the original job is deleted, the MultiKueue uses adaptor to remove the corresponding job from the worker cluster.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
+
+#### Beta
+- **Milestone:** v0.15
+- All major reported bugs are fixed.
+- The feature gate is enabled by default.
+- Re-evaluate replacing the existing adapters for built-in integrations with the in-memory configuration of the generic adapter.
+
+#### GA
+
+- All reported bugs are fixed.
+- The feature gate is removed.
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
+1. Dedicated API for MultiKueue communication between admission check controller and abstraction layer controller.
+
+Reasons to defer:
+- overall complexity, requires a communication protocol like gRPC between MultiKueue AdmissionChecks and external controllers. 
+- more difficult to debug as multiple layers are involved.
+- requires to maintain compatibility between independently compiled binaries.
+
+2. Don't do any code changes, a custom controller can: Instead of having MultiKueue cluster associated with controller name, change the Multikueue cluster status to use a custom active condition for old controllers (controller name) that use it.
+
+Reasons to defer:
+- The complexity of the external controllers which now need to essentially do the role of the MultiKueue Admission Checks, eg. managing the AC state, loading and using secrets
+
+3. Introduce ability to create Multikueue Admission Check controllers with custom set of adapters.
+
+Reasons to defer:
+- it is much harder to use by users as they need to enlist multiple MultiKueue Amission Checks per CQ as initially proposed in https://github.com/kubernetes-sigs/kueue/pull/2458/files#diff-359284c3c284a904713acde8d57992e7dd5020400b955f25b990b7a35b712ae4R173-R175
+- The external admission checks would need to largely repeat the code of the MultiKueue built-in controller
+- Since the solution involves another big controller it is harder to maintain
+
+4. Workload-Only Sync for External Controllers: This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization. This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+  In this model, MultiKueue's role would be strictly limited to scheduling and synchronizing only its own Workload object.
+
+   1. On the Management Cluster: A user submits a custom job (like a Tekton PipelineRun) with a queue-name label. External Controller creates a corresponding Workload object.
+   2. Scheduling: Kueue's scheduler decides when the Workload can be admitted based on available quota.
+   3. Synchronization: Upon admission, MultiKueue's only action is to copy the Workload object to the designated worker cluster. It would not create the `PipelineRun` on the worker cluster.
+   4. External Controller's Role: The external controller (e.g., Tekton's external kueue controller) would need to be "multi-cluster aware." It would be responsible for:
+       * Watching for Workload objects updates.
+       * When it sees a Workload get scheduled by MultiKueue, it would then create the original Job(let's say PipelineRun) on the worker cluster to actually run the job.
+       * It would also be solely responsible for monitoring the PipelineRun's status on the worker cluster and syncing it back to the PipelineRun object on the management cluster.
+
+External Controller can reuse the interface defined for MultiKueue Adaptor for this.
```

  @khrm Does this mean we can fully leverage the existing interfaces (https://github.com/kubernetes-sigs/kueue/blob/8f4f4e9bfdaab335fe0dead0e929b9df0afea744/pkg/controller/jobframework/interface.go#L234-L264)? Do you see any other necessary expansions in your case?

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-09-01T11:32:08Z

- **keps/2349-multikueue-external-custom-job-support/README.md:227** by [@khrm](https://github.com/khrm) — 2025-09-01T11:32:08Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+#### Job Flow from Manager to Worker
+   1. User: Submits the custom job (e.g., PipelineRun) with queue-name and managedBy fields to the management cluster.
+   2. External Controller creates a Workload object for the job.
+   3. MultiKueue admits it once quota is available in worker, assigning it to a worker cluster.
+   3. MultiKueue uses Generic Adapter clones the job, applies creationPatches (like removing managedBy), and creates it on the worker cluster.
+   4. MultiKueue Generic Adaptor is used to periodically fetch the job's status from the worker and applies it to the original job on the management cluster using syncPatches.
+   5. When the original job is deleted, the MultiKueue uses adaptor to remove the corresponding job from the worker cluster.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
+
+#### Beta
+- **Milestone:** v0.15
+- All major reported bugs are fixed.
+- The feature gate is enabled by default.
+- Re-evaluate replacing the existing adapters for built-in integrations with the in-memory configuration of the generic adapter.
+
+#### GA
+
+- All reported bugs are fixed.
+- The feature gate is removed.
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
+1. Dedicated API for MultiKueue communication between admission check controller and abstraction layer controller.
+
+Reasons to defer:
+- overall complexity, requires a communication protocol like gRPC between MultiKueue AdmissionChecks and external controllers. 
+- more difficult to debug as multiple layers are involved.
+- requires to maintain compatibility between independently compiled binaries.
+
+2. Don't do any code changes, a custom controller can: Instead of having MultiKueue cluster associated with controller name, change the Multikueue cluster status to use a custom active condition for old controllers (controller name) that use it.
+
+Reasons to defer:
+- The complexity of the external controllers which now need to essentially do the role of the MultiKueue Admission Checks, eg. managing the AC state, loading and using secrets
+
+3. Introduce ability to create Multikueue Admission Check controllers with custom set of adapters.
+
+Reasons to defer:
+- it is much harder to use by users as they need to enlist multiple MultiKueue Amission Checks per CQ as initially proposed in https://github.com/kubernetes-sigs/kueue/pull/2458/files#diff-359284c3c284a904713acde8d57992e7dd5020400b955f25b990b7a35b712ae4R173-R175
+- The external admission checks would need to largely repeat the code of the MultiKueue built-in controller
+- Since the solution involves another big controller it is harder to maintain
+
+4. Workload-Only Sync for External Controllers: This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization. This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+  In this model, MultiKueue's role would be strictly limited to scheduling and synchronizing only its own Workload object.
+
+   1. On the Management Cluster: A user submits a custom job (like a Tekton PipelineRun) with a queue-name label. External Controller creates a corresponding Workload object.
+   2. Scheduling: Kueue's scheduler decides when the Workload can be admitted based on available quota.
+   3. Synchronization: Upon admission, MultiKueue's only action is to copy the Workload object to the designated worker cluster. It would not create the `PipelineRun` on the worker cluster.
+   4. External Controller's Role: The external controller (e.g., Tekton's external kueue controller) would need to be "multi-cluster aware." It would be responsible for:
+       * Watching for Workload objects updates.
+       * When it sees a Workload get scheduled by MultiKueue, it would then create the original Job(let's say PipelineRun) on the worker cluster to actually run the job.
+       * It would also be solely responsible for monitoring the PipelineRun's status on the worker cluster and syncing it back to the PipelineRun object on the management cluster.
+
+External Controller can reuse the interface defined for MultiKueue Adaptor for this.
```

  I don't see any expansions, at least for argo workflows or Tekton.
  
  I don't see any job requiring something else apart from this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-01T11:40:17Z

- **keps/2349-multikueue-external-custom-job-support/README.md:171** by [@mimowo](https://github.com/mimowo) — 2025-09-01T11:40:17Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+#### Job Flow from Manager to Worker
+   1. User: Submits the custom job (e.g., PipelineRun) with queue-name and managedBy fields to the management cluster.
+   2. External Controller creates a Workload object for the job.
+   3. MultiKueue admits it once quota is available in worker, assigning it to a worker cluster.
+   3. MultiKueue uses Generic Adapter clones the job, applies creationPatches (like removing managedBy), and creates it on the worker cluster.
+   4. MultiKueue Generic Adaptor is used to periodically fetch the job's status from the worker and applies it to the original job on the management cluster using syncPatches.
+   5. When the original job is deleted, the MultiKueue uses adaptor to remove the corresponding job from the worker cluster.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
```

  I would suggest to move it to Beta, under "Re-evaluate the need for supporting custom patches (for different placement of `managedBy` or `status`) ". IIUC we don't yet have an indication this is needed. 
  
  Saving on API surface is a win, and I would prefer to consider making the `creationPatcher` and `syncPatches` more in the MAP style, so prefer to avoid committing to the current proposal.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-09-01T12:09:48Z

- **keps/2349-multikueue-external-custom-job-support/README.md:171** by [@khrm](https://github.com/khrm) — 2025-09-01T12:09:47Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+#### Job Flow from Manager to Worker
+   1. User: Submits the custom job (e.g., PipelineRun) with queue-name and managedBy fields to the management cluster.
+   2. External Controller creates a Workload object for the job.
+   3. MultiKueue admits it once quota is available in worker, assigning it to a worker cluster.
+   3. MultiKueue uses Generic Adapter clones the job, applies creationPatches (like removing managedBy), and creates it on the worker cluster.
+   4. MultiKueue Generic Adaptor is used to periodically fetch the job's status from the worker and applies it to the original job on the management cluster using syncPatches.
+   5. When the original job is deleted, the MultiKueue uses adaptor to remove the corresponding job from the worker cluster.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
```

  @mimowo 
  You mean instead of having JsonPatch, having a MAP like this:
  ```
  syncPatches:
     targetPath: /status
     sourcePath: /status
  ```
  Or not having these at all but only default?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T12:40:42Z

- **keps/2349-multikueue-external-custom-job-support/README.md:227** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T12:40:42Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+#### Job Flow from Manager to Worker
+   1. User: Submits the custom job (e.g., PipelineRun) with queue-name and managedBy fields to the management cluster.
+   2. External Controller creates a Workload object for the job.
+   3. MultiKueue admits it once quota is available in worker, assigning it to a worker cluster.
+   3. MultiKueue uses Generic Adapter clones the job, applies creationPatches (like removing managedBy), and creates it on the worker cluster.
+   4. MultiKueue Generic Adaptor is used to periodically fetch the job's status from the worker and applies it to the original job on the management cluster using syncPatches.
+   5. When the original job is deleted, the MultiKueue uses adaptor to remove the corresponding job from the worker cluster.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
+
+#### Beta
+- **Milestone:** v0.15
+- All major reported bugs are fixed.
+- The feature gate is enabled by default.
+- Re-evaluate replacing the existing adapters for built-in integrations with the in-memory configuration of the generic adapter.
+
+#### GA
+
+- All reported bugs are fixed.
+- The feature gate is removed.
+
+
+## Implementation History
+
+* 2024-06-20 First draft
+* 2025-07-15 Second draft
+
+
+## Drawbacks
+
+
+## Alternatives
+
+1. Dedicated API for MultiKueue communication between admission check controller and abstraction layer controller.
+
+Reasons to defer:
+- overall complexity, requires a communication protocol like gRPC between MultiKueue AdmissionChecks and external controllers. 
+- more difficult to debug as multiple layers are involved.
+- requires to maintain compatibility between independently compiled binaries.
+
+2. Don't do any code changes, a custom controller can: Instead of having MultiKueue cluster associated with controller name, change the Multikueue cluster status to use a custom active condition for old controllers (controller name) that use it.
+
+Reasons to defer:
+- The complexity of the external controllers which now need to essentially do the role of the MultiKueue Admission Checks, eg. managing the AC state, loading and using secrets
+
+3. Introduce ability to create Multikueue Admission Check controllers with custom set of adapters.
+
+Reasons to defer:
+- it is much harder to use by users as they need to enlist multiple MultiKueue Amission Checks per CQ as initially proposed in https://github.com/kubernetes-sigs/kueue/pull/2458/files#diff-359284c3c284a904713acde8d57992e7dd5020400b955f25b990b7a35b712ae4R173-R175
+- The external admission checks would need to largely repeat the code of the MultiKueue built-in controller
+- Since the solution involves another big controller it is harder to maintain
+
+4. Workload-Only Sync for External Controllers: This approach allows external controllers to retain full responsibility for their job's lifecycle, with Kueue providing the core scheduling and workload synchronization. This model offers maximum decoupling but requires the external controller author to implement their own remote job creation and status-syncing logic.
+
+  In this model, MultiKueue's role would be strictly limited to scheduling and synchronizing only its own Workload object.
+
+   1. On the Management Cluster: A user submits a custom job (like a Tekton PipelineRun) with a queue-name label. External Controller creates a corresponding Workload object.
+   2. Scheduling: Kueue's scheduler decides when the Workload can be admitted based on available quota.
+   3. Synchronization: Upon admission, MultiKueue's only action is to copy the Workload object to the designated worker cluster. It would not create the `PipelineRun` on the worker cluster.
+   4. External Controller's Role: The external controller (e.g., Tekton's external kueue controller) would need to be "multi-cluster aware." It would be responsible for:
+       * Watching for Workload objects updates.
+       * When it sees a Workload get scheduled by MultiKueue, it would then create the original Job(let's say PipelineRun) on the worker cluster to actually run the job.
+       * It would also be solely responsible for monitoring the PipelineRun's status on the worker cluster and syncing it back to the PipelineRun object on the management cluster.
+
+External Controller can reuse the interface defined for MultiKueue Adaptor for this.
```

  Alright, thank you for sharing that.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T12:44:46Z

- **keps/2349-multikueue-external-custom-job-support/README.md:77** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T12:44:46Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
```

  I might be able to approve only this `externalFrameworks` field since the kueue-controller-manager should recognize which frameworks are managed by external-controller.
  
  Is this understanding right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T12:45:18Z

- **keps/2349-multikueue-external-custom-job-support/README.md:70** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T12:45:18Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
```

  If we need to expand Config API, could you propose the Go API instead of YAML?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T13:25:38Z

- **keps/2349-multikueue-external-custom-job-support/README.md:77** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T13:25:37Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
```

  If we introduce this API, I would like to keep the same API surface like
  
  ```yaml
  multiKueue:
    externalFrameworks: ["PipelineRun.v1.pipelinerun.tekton.dev"]
  ```
  
  https://github.com/kubernetes-sigs/kueue/blob/b44cf15a52b5c97d46d172dcf2432228bd03403e/apis/config/v1beta1/configuration_types.go#L396-L398

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-09-01T13:53:30Z

- **keps/2349-multikueue-external-custom-job-support/README.md:77** by [@khrm](https://github.com/khrm) — 2025-09-01T13:53:29Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
```

  I was also thinking of going with this, but the problem is that this doesn't reliably extract gvk unless we use g/v/k format.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-01T14:30:38Z

- **keps/2349-multikueue-external-custom-job-support/README.md:171** by [@mimowo](https://github.com/mimowo) — 2025-09-01T14:30:38Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           managedBy: ".spec.managedBy"
+           creationPatches:
+           syncPatches:
+            managedBy: ".spec.managedBy"
+            creationPatches:
+            # Used to define what to patch while creating in worker. // By default, we can assume replace with null for managedBy field.
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+            # Copy the entire status block from the remote object to the local one. By default, we can assume /status. Unless some other fields also need to be copied.
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+We can have minimal configuration with default values for most of fields:
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+```
+This will translate to the former config since we will have the following default values:
+```
+            managedBy: ".spec.managedBy"
+            creationPatches:
+                - op: replace
+                  path: /spec/managedBy
+                  value: null
+            syncPatches:
+                - op: replace
+                  path: /status
+                  from: /status
+```
+
+
+For every entry in the `multiKueue.externalFrameworks` we register a MultiKueue adapter which implements the [MultiKueueAdapter interface](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/pkg/controller/jobframework/interface.go#L235C5-L235C6), in particular:
+- *~IsJobManagedByKueue~*: Reads the local job object and uses the ~managedBy~ JSONPath to verify it's managed by MultiKueue.
+- *~SyncJob~ (Create)*: Clones the local job object, applies the ~creationPatches~, adds the standard MultiKueue labels (~prebuilt-workload~, ~multikueue-origin~), and creates the resulting object on the worker cluster.
+- *~SyncJob~ (Status Sync)*: Fetches the local and remote job objects. It then iterates through the ~statusSyncPatches~, applying each one to construct a patch for the local job's status subresource.
+
+A significant benefit of this approach is the potential to replace the implementation of several built-in adapters (e.g., `JobSet`, `RayJob`, `RayCluster`) by registering the generic adapter for their GVKs. This would lead to a reduction in specialized code and improve maintainability.
+
+However, the generic adapter is not a universal solution. It cannot replace adapters for workloads that have unique lifecycle management mechanisms. This is important for maintainers of external frameworks to consider when choosing an implementation path. Notable exceptions include:
+
+- **Job**: The current adapter relies on the `MultiKueueBatchJobWithManagedBy` environment variable. This is a temporary limitation until the `JobManagedBy` feature in Kubernetes reaches General Availability.
+- **Pod**: The Pod adapter uses `schedulingGates` instead of the `managedBy` field. Extending the generic adapter to support this mechanism is out of scope for this KEP.
+
+#### Job Flow from Manager to Worker
+   1. User: Submits the custom job (e.g., PipelineRun) with queue-name and managedBy fields to the management cluster.
+   2. External Controller creates a Workload object for the job.
+   3. MultiKueue admits it once quota is available in worker, assigning it to a worker cluster.
+   3. MultiKueue uses Generic Adapter clones the job, applies creationPatches (like removing managedBy), and creates it on the worker cluster.
+   4. MultiKueue Generic Adaptor is used to periodically fetch the job's status from the worker and applies it to the original job on the management cluster using syncPatches.
+   5. When the original job is deleted, the MultiKueue uses adaptor to remove the corresponding job from the worker cluster.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+- We will test the configmap parsing and validation logic.
+- We will test the json patch logic.
+- We will test the generic adapter.
+
+##### Prerequisite testing updates
+
+#### Unit Tests
+
+Target is to keep same level or increase the coverage for all of the modified packages.
+Cover the critical part of newly created code.
+
+- `<package>`: `<date>` - `<test coverage>`
+
+#### Integration tests
+
+- A test will be added that creates a ~ConfigMap~ with rules for a mock GVK.
+- We will then create a ~Workload~ for that mock GVK and verify that the reconciler correctly applies the creation and status patches to mock remote objects.
+- Another test will verify that a ~Workload~ for an unconfigured GVK is correctly rejected.
+
+### Graduation Criteria
+
+#### Alpha
+- **Milestone:** v0.14
+- **Feature Gate:** `MultiKueueAdaptersForCustomJobs`
+- Implementation is behind a feature gate.
+- Support for the default placement of the `managedBy` and `status` fields (`.spec.managedBy` and `.status`) is implemented.
+- Support for custom patches (for different placement of `managedBy` or `status`) is implemented.
```

  I was proposing not to introduce configurable `syncPatches` in Alpha. For all known CRDs it would be the same (copy the status). This way we don't commit to API to maintain in the future. We could even drop it from the API proposal and move to the "Alternatives" section for now.
  
  We will re-evaluate the need if we encounter an API needing custom patches.
  
  If we need custom patches I would probably prefer the CEL expression as for MAP, looking at https://github.com/kubernetes/enhancements/tree/master/keps/sig-api-machinery/3962-mutating-admission-policies#api-shape and it looks more like:
  
  ```yaml
  syncPatches:
  - expression: >
          JSONPatch{op: "replace", path: "/-", value: Object{
                  status: params.status,
          }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-01T14:38:44Z

- **keps/2349-multikueue-external-custom-job-support/README.md:101** by [@mimowo](https://github.com/mimowo) — 2025-09-01T14:38:44Z
```diff
@@ -0,0 +1,247 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+    - [Job Flow for an External Controller](#job-flow-for-an-external-controller)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+This KEP proposes extending MultiKueue to support custom jobs through a configurable mechanism. Administrators will be able to define, for each custom job type (identified by its GroupVersionKind), whether it should be handled by a built-in generic adapter or by an external, multi-cluster-aware controller.
+
+This configuration will be managed via a configmap. Based on this configuration, MultiKueue will either use its generic adapter for a given GVK or expect an external controller to manage the job's lifecycle across clusters, in which case MultiKueue will only be responsible for synchronizing the `Workload` object.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Fixed Transformation: The generic adapter is limited to a fixed, non-configurable transformation logic.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap.
+
+#### Mitigations
+
+Configmaps that are not correct should be logged with errors like "Invalid GVK format". We could have a CLI command to validate the adapter logic.
+
+## Design Details
+
+### Configurable MultiKueue Adapters
+
+To support external frameworks, the MultiKueue configuration API will be extended. The following definitions will be added to the `apis/config/v1beta1` package:
+
+```go
+// MultiKueue specifies the multikueue configuration options.
+type MultiKueue struct {
+   .....
+	// ExternalFrameworks lists the external frameworks to be supported by MultiKueue.
+	// +optional
+	ExternalFrameworks []ExternalFramework `json:"externalFrameworks,omitempty"`
+}
+
+// ExternalFramework defines a framework that is not built-in.
+type ExternalFramework struct {
+	// Group is the API group of the custom job.
+	// +required
+	Group string `json:"group"`
+	// Version is the API version of the custom job.
+	// +required
+	Version string `json:"version"`
+	// Kind is the Kind of the custom job.
+	// +required
+	Kind string `json:"kind"`
+	// Controller specifies which controller should manage this custom job.
+	// Can be "generic" or "external".
+	// Defaults to "generic".
+	// +optional
+	Controller ControllerType `json:"controller,omitempty"`
```

  I would suggest dropping this for now, we can introduce when proven this is needed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T15:49:43Z

- **keps/2349-multikueue-external-custom-job-support/README.md:77** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T15:49:43Z
```diff
@@ -0,0 +1,235 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Generic Adapter via ConfigMap](#generic-adapter-via-configmap)
+    - [Job Flow from Manager to Worker](#job-flow-from-manager-to-worker)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs.  This introduces a generic, built-in MultiKueue adapter that is configurable via the Kueue Config.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on GroupVersionKind (GVK) defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+### Generic Adapter via ConfigMap
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
```

  We can: https://github.com/kubernetes-sigs/kueue/blob/b44cf15a52b5c97d46d172dcf2432228bd03403e/pkg/controller/jobframework/integrationmanager.go#L143
  The above format is standard form. Please follow the exisitng ExternalFramework format.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T15:51:52Z

- **keps/2349-multikueue-external-custom-job-support/README.md:83** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T15:51:52Z
```diff
@@ -0,0 +1,247 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+    - [Job Flow for an External Controller](#job-flow-for-an-external-controller)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+This KEP proposes extending MultiKueue to support custom jobs through a configurable mechanism. Administrators will be able to define, for each custom job type (identified by its GroupVersionKind), whether it should be handled by a built-in generic adapter or by an external, multi-cluster-aware controller.
+
+This configuration will be managed via a configmap. Based on this configuration, MultiKueue will either use its generic adapter for a given GVK or expect an external controller to manage the job's lifecycle across clusters, in which case MultiKueue will only be responsible for synchronizing the `Workload` object.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Fixed Transformation: The generic adapter is limited to a fixed, non-configurable transformation logic.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap.
+
+#### Mitigations
+
+Configmaps that are not correct should be logged with errors like "Invalid GVK format". We could have a CLI command to validate the adapter logic.
+
+## Design Details
+
+### Configurable MultiKueue Adapters
+
+To support external frameworks, the MultiKueue configuration API will be extended. The following definitions will be added to the `apis/config/v1beta1` package:
+
+```go
+// MultiKueue specifies the multikueue configuration options.
+type MultiKueue struct {
+   .....
+	// ExternalFrameworks lists the external frameworks to be supported by MultiKueue.
+	// +optional
+	ExternalFrameworks []ExternalFramework `json:"externalFrameworks,omitempty"`
```

  ```suggestion
  	ExternalFrameworks []string `json:"externalFrameworks,omitempty"`
  ```
  
  As I mentioned in https://github.com/kubernetes-sigs/kueue/pull/5981#discussion_r2314267504, we can extract gvk from `Kind.version.group.com`, which is a standardized form.

### Review by [@ravisantoshgudimetla](https://github.com/ravisantoshgudimetla) (commented) — 2025-09-01T16:25:06Z

- **keps/2349-multikueue-external-custom-job-support/README.md:85** by [@ravisantoshgudimetla](https://github.com/ravisantoshgudimetla) — 2025-09-01T16:25:06Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
```

  > Actually, it's still the responsibility of external controller to generate workload.
  
  You mean the kueue workload CR? I thought you are a registered type, you cannot?
  
  > The issue is that we will need to run a sync controller of workload in the same namespace and give it access to the same secret which MultiKueue has access to. And there's no benefit to doing all this. It's just a sync behaviour which is common for all CRs.
  
  What I am suggesting is much simpler, you can create your own controller in another namespace and have RBAC specific to CRD to do syncing for workload clusters, no need to rely on secret present in multi-kueue namespace. This way, it truly makes sure that Multi-kueue is just deciding on the cluster where the workload should run and rest of the work is done by controller giving us clear separation of clusters.(This also means we might have to change Multi-Adapter interface without SyncJob method).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-01T16:38:53Z

- **keps/2349-multikueue-external-custom-job-support/README.md:131** by [@mimowo](https://github.com/mimowo) — 2025-09-01T16:38:53Z
```diff
@@ -0,0 +1,247 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+    - [Job Flow for an External Controller](#job-flow-for-an-external-controller)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+This KEP proposes extending MultiKueue to support custom jobs through a configurable mechanism. Administrators will be able to define, for each custom job type (identified by its GroupVersionKind), whether it should be handled by a built-in generic adapter or by an external, multi-cluster-aware controller.
+
+This configuration will be managed via a configmap. Based on this configuration, MultiKueue will either use its generic adapter for a given GVK or expect an external controller to manage the job's lifecycle across clusters, in which case MultiKueue will only be responsible for synchronizing the `Workload` object.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Fixed Transformation: The generic adapter is limited to a fixed, non-configurable transformation logic.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap.
+
+#### Mitigations
+
+Configmaps that are not correct should be logged with errors like "Invalid GVK format". We could have a CLI command to validate the adapter logic.
+
+## Design Details
+
+### Configurable MultiKueue Adapters
+
+To support external frameworks, the MultiKueue configuration API will be extended. The following definitions will be added to the `apis/config/v1beta1` package:
+
+```go
+// MultiKueue specifies the multikueue configuration options.
+type MultiKueue struct {
+   .....
+	// ExternalFrameworks lists the external frameworks to be supported by MultiKueue.
+	// +optional
+	ExternalFrameworks []ExternalFramework `json:"externalFrameworks,omitempty"`
+}
+
+// ExternalFramework defines a framework that is not built-in.
+type ExternalFramework struct {
+	// Group is the API group of the custom job.
+	// +required
+	Group string `json:"group"`
+	// Version is the API version of the custom job.
+	// +required
+	Version string `json:"version"`
+	// Kind is the Kind of the custom job.
+	// +required
+	Kind string `json:"kind"`
+	// Controller specifies which controller should manage this custom job.
+	// Can be "generic" or "external".
+	// Defaults to "generic".
+	// +optional
+	Controller ControllerType `json:"controller,omitempty"`
+}
+
+// ControllerType is the type of controller for an external framework.
+type ControllerType string
+
+const (
+	// GenericController is a built-in generic MultiKueue adapter.
+	GenericController ControllerType = "generic"
+	// ExternalController means an external, multi-cluster aware controller will manage the job.
+	ExternalController ControllerType = "external"
+)
+```
+
+This change will be reflected in the `kueue-manager-config` configmap. For each framework, identified by its GVK, an administrator can specify the controller type.
+
+Example YAML configuration:
+
+```yaml
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           # controller: "generic" # this is the default
+```
+
+The `controller` field is optional and can have two values:
+- `generic` (default): Use the built-in generic MultiKueue adapter. This adapter performs a default set of operations to manage the job on a worker cluster. It assumes a `.spec.managedBy` field in the custom job and will handle removing it before creation on the worker, as well as syncing the `/status` field back from the worker to the management cluster.
+- `external`: Expect an external, multi-cluster aware controller to manage the job. In this mode, MultiKueue will only sync the `Workload` object to the worker cluster. The external controller is responsible for creating the job on the worker and syncing its status.
```

  This is not going to be implemented as Alpha for the KEP, so we can drop and move this part to the Alternatives section as a potential extension.

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-09-01T16:42:08Z

- **keps/2349-multikueue-external-custom-job-support/README.md:131** by [@khrm](https://github.com/khrm) — 2025-09-01T16:42:08Z
```diff
@@ -0,0 +1,247 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+    - [Job Flow for an External Controller](#job-flow-for-an-external-controller)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+This KEP proposes extending MultiKueue to support custom jobs through a configurable mechanism. Administrators will be able to define, for each custom job type (identified by its GroupVersionKind), whether it should be handled by a built-in generic adapter or by an external, multi-cluster-aware controller.
+
+This configuration will be managed via a configmap. Based on this configuration, MultiKueue will either use its generic adapter for a given GVK or expect an external controller to manage the job's lifecycle across clusters, in which case MultiKueue will only be responsible for synchronizing the `Workload` object.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Fixed Transformation: The generic adapter is limited to a fixed, non-configurable transformation logic.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap.
+
+#### Mitigations
+
+Configmaps that are not correct should be logged with errors like "Invalid GVK format". We could have a CLI command to validate the adapter logic.
+
+## Design Details
+
+### Configurable MultiKueue Adapters
+
+To support external frameworks, the MultiKueue configuration API will be extended. The following definitions will be added to the `apis/config/v1beta1` package:
+
+```go
+// MultiKueue specifies the multikueue configuration options.
+type MultiKueue struct {
+   .....
+	// ExternalFrameworks lists the external frameworks to be supported by MultiKueue.
+	// +optional
+	ExternalFrameworks []ExternalFramework `json:"externalFrameworks,omitempty"`
+}
+
+// ExternalFramework defines a framework that is not built-in.
+type ExternalFramework struct {
+	// Group is the API group of the custom job.
+	// +required
+	Group string `json:"group"`
+	// Version is the API version of the custom job.
+	// +required
+	Version string `json:"version"`
+	// Kind is the Kind of the custom job.
+	// +required
+	Kind string `json:"kind"`
+	// Controller specifies which controller should manage this custom job.
+	// Can be "generic" or "external".
+	// Defaults to "generic".
+	// +optional
+	Controller ControllerType `json:"controller,omitempty"`
+}
+
+// ControllerType is the type of controller for an external framework.
+type ControllerType string
+
+const (
+	// GenericController is a built-in generic MultiKueue adapter.
+	GenericController ControllerType = "generic"
+	// ExternalController means an external, multi-cluster aware controller will manage the job.
+	ExternalController ControllerType = "external"
+)
+```
+
+This change will be reflected in the `kueue-manager-config` configmap. For each framework, identified by its GVK, an administrator can specify the controller type.
+
+Example YAML configuration:
+
+```yaml
+     # In the multikueue Kueue Config
+     multikueue:
+       externalFrameworks:
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           # controller: "generic" # this is the default
+```
+
+The `controller` field is optional and can have two values:
+- `generic` (default): Use the built-in generic MultiKueue adapter. This adapter performs a default set of operations to manage the job on a worker cluster. It assumes a `.spec.managedBy` field in the custom job and will handle removing it before creation on the worker, as well as syncing the `/status` field back from the worker to the management cluster.
+- `external`: Expect an external, multi-cluster aware controller to manage the job. In this mode, MultiKueue will only sync the `Workload` object to the worker cluster. The external controller is responsible for creating the job on the worker and syncing its status.
```

  Sure. I would move this to alternative.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T16:56:53Z

- **keps/2349-multikueue-external-custom-job-support/README.md:83** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T16:56:53Z
```diff
@@ -0,0 +1,247 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+    - [Job Flow for an External Controller](#job-flow-for-an-external-controller)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+This KEP proposes extending MultiKueue to support custom jobs through a configurable mechanism. Administrators will be able to define, for each custom job type (identified by its GroupVersionKind), whether it should be handled by a built-in generic adapter or by an external, multi-cluster-aware controller.
+
+This configuration will be managed via a configmap. Based on this configuration, MultiKueue will either use its generic adapter for a given GVK or expect an external controller to manage the job's lifecycle across clusters, in which case MultiKueue will only be responsible for synchronizing the `Workload` object.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Fixed Transformation: The generic adapter is limited to a fixed, non-configurable transformation logic.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap.
+
+#### Mitigations
+
+Configmaps that are not correct should be logged with errors like "Invalid GVK format". We could have a CLI command to validate the adapter logic.
+
+## Design Details
+
+### Configurable MultiKueue Adapters
+
+To support external frameworks, the MultiKueue configuration API will be extended. The following definitions will be added to the `apis/config/v1beta1` package:
+
+```go
+// MultiKueue specifies the multikueue configuration options.
+type MultiKueue struct {
+   .....
+	// ExternalFrameworks lists the external frameworks to be supported by MultiKueue.
+	// +optional
+	ExternalFrameworks []ExternalFramework `json:"externalFrameworks,omitempty"`
```

  Based on the discussion in https://kubernetes.slack.com/archives/C032ZE66A2X/p1755181736825669, we decided to introduce `ExternalFramework` type, which has only a `name` field. In the future, we might introduce `controllerType`. But, it is not introduced in the alpha phase.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-01T16:59:10Z

- **keps/2349-multikueue-external-custom-job-support/README.md:83** by [@mimowo](https://github.com/mimowo) — 2025-09-01T16:59:10Z
```diff
@@ -0,0 +1,247 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+    - [Job Flow for an External Controller](#job-flow-for-an-external-controller)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+This KEP proposes extending MultiKueue to support custom jobs through a configurable mechanism. Administrators will be able to define, for each custom job type (identified by its GroupVersionKind), whether it should be handled by a built-in generic adapter or by an external, multi-cluster-aware controller.
+
+This configuration will be managed via a configmap. Based on this configuration, MultiKueue will either use its generic adapter for a given GVK or expect an external controller to manage the job's lifecycle across clusters, in which case MultiKueue will only be responsible for synchronizing the `Workload` object.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Fixed Transformation: The generic adapter is limited to a fixed, non-configurable transformation logic.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap.
+
+#### Mitigations
+
+Configmaps that are not correct should be logged with errors like "Invalid GVK format". We could have a CLI command to validate the adapter logic.
+
+## Design Details
+
+### Configurable MultiKueue Adapters
+
+To support external frameworks, the MultiKueue configuration API will be extended. The following definitions will be added to the `apis/config/v1beta1` package:
+
+```go
+// MultiKueue specifies the multikueue configuration options.
+type MultiKueue struct {
+   .....
+	// ExternalFrameworks lists the external frameworks to be supported by MultiKueue.
+	// +optional
+	ExternalFrameworks []ExternalFramework `json:"externalFrameworks,omitempty"`
```

  As we discussed on slack, we are conservative about the API, but some degree of extensibility might be needed in the future. Some ideas:
  - allows to specifiy controllerType: Generic / External to discriminate between the mechanisms
  - position of the managedBy (but we should be careful about it and not plan for Alpha)
  - syncPatches to customize the synchronization (but we should be careful about it and not plan for Alpha)
  
  So, we propose to expose just 
  
  ```golang
  type MultiKueueExternalFramework struct {
  	Name string `json:"name"`
  }
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T17:46:44Z

Thank you for update.
Otherwise LGTM.

- **keps/2349-multikueue-external-custom-job-support/README.md:44** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T17:45:36Z
```diff
@@ -0,0 +1,253 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
```

  ```suggestion
  
  This KEP does not aim to generalize the Workload synchronization mechanism for kueue-external-controller.
  Even after this feature is supported, the Workload resource synchronization is handled by kueue-controller-manager (MultiKueue).
  ```

- **keps/2349-multikueue-external-custom-job-support/README.md:102** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T17:45:57Z
```diff
@@ -0,0 +1,253 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+This KEP proposes extending MultiKueue to support custom jobs through a configurable mechanism. Administrators will be able to define, for each custom job type (identified by its GroupVersionKind), whether it should be handled by a built-in generic adapter or by an external, multi-cluster-aware controller.
+
+This configuration will be managed via a configmap. Based on this configuration, MultiKueue will either use its generic adapter for a given GVK or expect an external controller to manage the job's lifecycle across clusters, in which case MultiKueue will only be responsible for synchronizing the `Workload` object.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Fixed Transformation: The generic adapter is limited to a fixed, non-configurable transformation logic.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap.
+
+#### Mitigations
+
+Configmaps that are not correct should be logged with errors like "Invalid GVK format". We could have a CLI command to validate the adapter logic.
+
+## Design Details
+
+### Configurable MultiKueue Adapters
+
+To support external frameworks, the MultiKueue configuration API will be extended. Administrators will be able to list custom job GVKs that should be handled by a built-in generic adapter.
+
+The following definitions will be added to the `apis/config/v1beta1` package:
+
+```go
+// MultiKueue specifies the multikueue configuration options.
+type MultiKueue struct {
+   .....
+	// ExternalFrameworks is the list of the external frameworks to be supported by the generic MultiKueue adapter.
+	// +optional
+	ExternalFrameworks []MultiKueueExternalFramework `json:"externalFrameworks,omitempty"`
+}
+
+// MultiKueueExternalFramework defines a framework that is not built-in.
+type MultiKueueExternalFramework struct {
+	// Name is the GVK of the resource that are
+    // is managed by external controllers
+    // the expected format is `Kind.version.group.com`.
+	Name string `json:"name"`
+}
+```
+
+This change will be reflected in the `kueue-manager-config` configmap.
+
+Example YAML configuration:
+
+```yaml
+     # In the multikueue Kueue Config
+     multikueue:
```

  ```suggestion
       # In the multiKueue Kueue Config
       multiKueue:
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T17:50:01Z

- **keps/2349-multikueue-external-custom-job-support/README.md:44** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T17:50:00Z
```diff
@@ -0,0 +1,253 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
```

  If @khrm feels that this is incorrect or miscontext, I would appreciate a new proposal from you.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T17:50:38Z

- **keps/2349-multikueue-external-custom-job-support/README.md:102** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T17:50:38Z
```diff
@@ -0,0 +1,253 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+This KEP proposes extending MultiKueue to support custom jobs through a configurable mechanism. Administrators will be able to define, for each custom job type (identified by its GroupVersionKind), whether it should be handled by a built-in generic adapter or by an external, multi-cluster-aware controller.
+
+This configuration will be managed via a configmap. Based on this configuration, MultiKueue will either use its generic adapter for a given GVK or expect an external controller to manage the job's lifecycle across clusters, in which case MultiKueue will only be responsible for synchronizing the `Workload` object.
+
+### User Stories
+
+As a cluster administrator I would like to have the ability to schedule Jobs
+which aren't natively supported by Kueue, eg Tekton.
+
+### Constraints
+
+- Fixed Transformation: The generic adapter is limited to a fixed, non-configurable transformation logic.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap.
+
+#### Mitigations
+
+Configmaps that are not correct should be logged with errors like "Invalid GVK format". We could have a CLI command to validate the adapter logic.
+
+## Design Details
+
+### Configurable MultiKueue Adapters
+
+To support external frameworks, the MultiKueue configuration API will be extended. Administrators will be able to list custom job GVKs that should be handled by a built-in generic adapter.
+
+The following definitions will be added to the `apis/config/v1beta1` package:
+
+```go
+// MultiKueue specifies the multikueue configuration options.
+type MultiKueue struct {
+   .....
+	// ExternalFrameworks is the list of the external frameworks to be supported by the generic MultiKueue adapter.
+	// +optional
+	ExternalFrameworks []MultiKueueExternalFramework `json:"externalFrameworks,omitempty"`
+}
+
+// MultiKueueExternalFramework defines a framework that is not built-in.
+type MultiKueueExternalFramework struct {
+	// Name is the GVK of the resource that are
+    // is managed by external controllers
+    // the expected format is `Kind.version.group.com`.
+	Name string `json:"name"`
+}
+```
+
+This change will be reflected in the `kueue-manager-config` configmap.
+
+Example YAML configuration:
+
+```yaml
+     # In the multikueue Kueue Config
+     multikueue:
```

  https://github.com/kubernetes-sigs/kueue/blob/5eabdcbdcaaaeef6b054db05107dd6ba06dfcffc/apis/config/v1beta1/configuration_types.go#L93

### Review by [@khrm](https://github.com/khrm) (commented) — 2025-09-01T17:53:53Z

- **keps/2349-multikueue-external-custom-job-support/README.md:44** by [@khrm](https://github.com/khrm) — 2025-09-01T17:53:53Z
```diff
@@ -0,0 +1,253 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Configurable MultiKueue Adapters](#configurable-multikueue-adapters)
+    - [Job Flow for the Generic Adapter](#job-flow-for-the-generic-adapter)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Alpha](#alpha)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+This KEP introduces a mechanism to support MultiKueue for custom Jobs. This is achieved by introducing a configurable system that allows administrators to choose between a built-in generic MultiKueue adapter or an external controller for managing custom job types.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such custom Jobs across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage custom Jobs in a MultiKueue environment.
+
+
+### Non-Goals
+
```

  This is correct. Thanks. :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T18:01:14Z

- **keps/2349-multikueue-external-custom-job-support/README.md:85** by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T18:01:14Z
```diff
@@ -0,0 +1,142 @@
+# KEP-2349: MultiKueue external custom Job support
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories ( Optional )](#user-stories--optional-)
+  - [Constraints](#constraints)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Risks](#risks)
+    - [Mitigations](#mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+ This KEP introduces a mechanism to support MultiKueue for custom job frome external controllers without requiring modifications changes in external job controllers.  This introduces a generic, built-in MultiKueue adapter that is configurable via a configmap.
+
+## Motivation
+
+Many Kueue adopters have specific CustomResources which might not be public.
+Kueue should give a possibility to manage such CustomResources across multiple clusters.
+
+### Goals
+
+Give a possibility for external controllers to manage private CustomResources in a MultiKueue environment.
+
+
+### Non-Goals
+
+## Proposal
+
+MultiKueue controller will be configurable via configmap. Based on this configmap, a generic MultiKueueAdapter will be implemented within Kueue. When processing a workload without supported built-in adapter, generic MultiKueueAdapter will be called based on gvk defined in configmap. It will use configuration defined to manage lifycycle's on the worker clusters.
+
+### User Stories ( Optional )
+
+
+### Constraints
+
+- Limited Transformation: Generic Adaptor are limited to simple JsonPatch transformation configurable.
+
+### Risks and Mitigations
+
+#### Risks
+
+ Configmaps are unstructured by default. They don't provide validation by default unlike CRD. User could make typo mistakes or give invalid syntax in configmap. Or user could give invalid syntax.
+
+#### Mitigations
+
+Configmap which aren't correct should logged like Invalid GVK format, Invalid JsonPatch ops. We could have cli command to validate adapter logic.
+
+## Design Details
+
+The `kueue-manager-config` configmap will be expanded to configure the generic adapter.
+
+```
+     # In the main Kueue ConfigMap
+     integrations:
+       externalFrameworks:
+         # OLD FORMAT
+         - "taskrun.tekton.dev"
+         # NEW, EXPANDED FORMAT (for MultiKueue)
+         - group: "pipelinerun.tekton.dev"
+           version: "v1"
+           kind: "PipelineRun"
+           multiKueueAdapter:
+            managedBy: ".spec.managedBy"
+            creationPatches:
```

  @ravisantoshgudimetla As I described in https://kubernetes.slack.com/archives/C032ZE66A2X/p1755181736825669, this KEP aims to delegate only Workload resource creation capability. In other words, the kueue-controller-manager is still responsible to synchronize Workload between manager and worker clusters.
  
  But, as I mentioned in https://github.com/kubernetes-sigs/kueue/pull/5981#issuecomment-3242837163, I would like to make an effort to generalize the Workload synchronization, but it should be handled as a separate KEP (enhancement). So, if you are interested in that, I'm happy to review your proposal.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-09-01T18:01:52Z

@khrm LGTM, Thank you!
/approve
/lgtm

/hold for @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-02T09:52:30Z

/lgtm
/approve
/unhold
Thank you! Looking forward to the implementation
