# PR #600: [workload] Get default resource values from LimitRanges

**Summary**: [workload] Get default resource values from LimitRanges

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/600

**Last updated**: 2023-04-20T06:00:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `workload_default_from_namespace_limits` → `main`
- **Created**: 2023-03-01T13:44:32Z
- **Updated**: 2023-04-20T06:00:49Z
- **Merged**: 2023-03-17T21:27:16Z
- **Closed**: 2023-03-17T21:27:16Z
- **Labels**: `kind/feature`, `lgtm`, `tide/merge-method-squash`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: _none_
- **Changed files**: 11   **+778 / -133**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add the possibility to use default workload resource values based on [LimitRanges](https://kubernetes.io/docs/concepts/policy/limit-range/#constraints-on-resource-limits-and-requests) if present in the target namespace.

The order by which Requested resource values are set becomes:
1. Job spec
3. LimitRanges in the target namespace
2. Limits  set for the same container in the Job Spec

In addition, this provides a more generic alternative approach to fix #590, by moving the limits to request default in the workload controller without an impact to the workload's spec .

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #541, #590

#### Special notes for your reviewer:

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-01T13:44:37Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 453f4f28a5a21ed9d3cb3b0f1e4da0275136a528 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6414d603257dd80008059d63 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-600--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-600--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTYwMC0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.niDa661MPDdDHWr15ZZ7p4LRDuXHdI8dAjoWLi4fvKo)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site settings](https://app.netlify.com/sites/kubernetes-sigs-kueue/settings/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-01T13:44:40Z

Welcome @trasc! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-01T13:44:42Z

Hi @trasc. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-03-01T14:33:57Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-01T14:38:27Z

@kerthcet since this is related to #590, can you take a look?

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-03-01T14:40:43Z

> @kerthcet since this is related to #590, can you take a look?

Sure, but this will defer to tomorrow.
/assign

### Comment by [@trasc](https://github.com/trasc) — 2023-03-02T12:53:47Z

> 



> Just take an overview of this PR, I think we have something to come to an agreement at first:
> 
> * About the defaulting orders:
>   
>   * Respect container resources first
>   * Respect LimitRange, we should not defaulting the requests to limits as we want before applying the policy, it's a hard constraint required by the administrators.
>   * Defaulting the requests to limits if not set
> * About the validation
>   
>   * We should also reject the creation if violating the LimitRange, in validation I think.
>   * If no request set in final, reject the creation also.
> 
> cc @alculquicondor for advices also.

For the defaulting order, in my opinion, the value set in the limits of the job has better chances to be closer to the actual need of the job that a value set for the entire namespace. The same happens for pods. 

Regarding the validation, sure, it could be added.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-03-03T04:30:49Z

> For the defaulting order, in my opinion, the value set in the limits of the job has better chances to be closer to the actual need of the job that a value set for the entire namespace. The same happens for pods.

Defaulting Limits to Requests is not transparent to users, if we have LimitRange with defaultRequest set, workload can be constructed successfully, why we get involved with the `limits -> requests`? We used to do this just because workloads requires the requests.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T13:32:17Z

Instead of the hash, we might be better off storing the original requests as suggested in option 1 of https://github.com/kubernetes-sigs/kueue/issues/611#issuecomment-1458295397

### Comment by [@trasc](https://github.com/trasc) — 2023-03-08T14:40:50Z

> Instead of the hash, we might be better off storing the original requests as suggested in option 1 of [#611 (comment)](https://github.com/kubernetes-sigs/kueue/issues/611#issuecomment-1458295397)

Sure, but do we want to change the API in  this PR?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T14:54:24Z

Yes, because if we merge this PR, we will pretty much undo everything when we store the original requests.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T15:21:30Z

We can start simpler:

- Drop the hash
- Add a LimitRange rencociler that deletes every Workload in the namespace that is not admitted.

We don't expect that changes to LimitRange happen often and we can improve on this later.

### Comment by [@trasc](https://github.com/trasc) — 2023-03-08T16:06:08Z

For the api change should we go with something like 
```
type WorkloadSpec struct {
	// .....
	PodSets []PodSet `json:"podSets"`
	// this one will be set by the default webhook and used in scheduling
	SchedPodSets []PodSet `json:"schedPodSets"`

```

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T16:08:12Z

Let's start with this https://github.com/kubernetes-sigs/kueue/pull/600#issuecomment-1460323882, which should be good enough for this release.

### Comment by [@trasc](https://github.com/trasc) — 2023-03-09T07:14:24Z





> Let's start with this [#600 (comment)](https://github.com/kubernetes-sigs/kueue/pull/600#issuecomment-1460323882), which should be good enough for this release.

If we drop the hash and not have a way to "remember" the original resources, in order to verify the equality of a job with it's workload we'll need to apply the default setting scheme every time we need to check the equality, otherwise we'll have a similar issue as #590 related to LimitRange defaulting.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-09T15:26:48Z

Right, we would have to add this somewhat complex verification in each job (batch, mpi, ray, etc) controller.

Then please proceed with option 1 of https://github.com/kubernetes-sigs/kueue/issues/611#issuecomment-1458295397

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-09T15:40:15Z

> For the api change should we go with something like
> 
> ```
> type WorkloadSpec struct {
> 	// .....
> 	PodSets []PodSet `json:"podSets"`
> 	// this one will be set by the default webhook and used in scheduling
> 	SchedPodSets []PodSet `json:"schedPodSets"`
> ```

There are two alternatives:

```golang
type WorkloadStatus struct {
  PodSetsRequests []PodSetRequests
}

type PodSetRequests struct {
  Name string
  Requests corev1.ResourceList
}
```

If the creation mutating webhook allows you to update the status. Otherwise:

```golang
type PodSet struct {
  TotalRequests corev1.ResourceList
}
```

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-03-10T04:32:16Z

Left a comment here https://github.com/kubernetes-sigs/kueue/issues/611#issuecomment-1463236862

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-10T13:41:18Z

> If the creation mutating webhook allows you to update the status. 

It doesn't. Then we need to put the calculated requests in the Spec.

### Comment by [@trasc](https://github.com/trasc) — 2023-03-14T04:45:13Z

/retest

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-17T21:11:59Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/600#pullrequestreview-1346741245" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/600#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-03-02T10:57:07Z

Just take an overview of this PR, I think we have something to come to an agreement at first:
- About the defaulting orders:
   - Respect container resources first
   - Respect LimitRange, we should not defaulting the requests to limits as we want before applying the policy, it's a hard constraint required by the administrators.
   - Defaulting the requests to limits if not set
- About the validation
  - We should also reject the creation if violating the LimitRange, in validation I think.
  - If no request set in final, reject the creation also.
  
cc @alculquicondor for advices also.

- **apis/kueue/webhooks/workload_webhook.go:75** by [@kerthcet](https://github.com/kerthcet) — 2023-03-02T09:56:28Z
```diff
@@ -21,29 +21,73 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
+				ret = append(ret, lri)
+			}
+		}
+	}
+	return ret, nil
+}
+
+type WorkloadWebhook struct {
+	getter LimitRangeProvider
+}
 
 func setupWebhookForWorkload(mgr ctrl.Manager) error {
+
```

  Remove this blank line.

- **apis/kueue/webhooks/workload_webhook.go:60** by [@kerthcet](https://github.com/kerthcet) — 2023-03-02T10:23:04Z
```diff
@@ -21,29 +21,73 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
```

  I think we should only consider `defaults` here,  if no defaults, then we'll set defaults to limits.

- **pkg/controller/workload/job/job_controller.go:654** by [@kerthcet](https://github.com/kerthcet) — 2023-03-02T10:40:04Z
```diff
@@ -619,14 +660,12 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 		return false
 	}
 
-	// nodeSelector may change, hence we are not checking for
-	// equality of the whole job.Spec.Template.Spec.
-	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+	specHash, err := jobSpecHash(job)
```

  I think this is not what we want, before we validate that the job is semantically equal with workload, for workload might be created randomly, but now we just validate the job is not updated, if I understood correctly. Then, we can compare the UID and ResourceVersion instead.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-02T12:32:30Z

- **pkg/controller/workload/job/job_controller.go:654** by [@trasc](https://github.com/trasc) — 2023-03-02T12:32:29Z
```diff
@@ -619,14 +660,12 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 		return false
 	}
 
-	// nodeSelector may change, hence we are not checking for
-	// equality of the whole job.Spec.Template.Spec.
-	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+	specHash, err := jobSpecHash(job)
```

  The difference between  `jobSpecHash` and UID+Resourece is that the result of `jobSpecHash` will be the same ti the [Init]Containers specs are not changed.
  
  From the equality point of view, indeed, now we check that the current version of the job is semantically equal to the job version at the creation of the workload, however , starting from the semantically equal job and applying the same default logic will create the same workload (the only exception for this being if LimitRanges change ).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-02T13:57:17Z

- **apis/kueue/webhooks/workload_webhook.go:60** by [@trasc](https://github.com/trasc) — 2023-03-02T13:57:16Z
```diff
@@ -21,29 +21,73 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
```

  I'm afraid I don't understand.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-02T13:58:04Z

- **apis/kueue/webhooks/workload_webhook.go:75** by [@trasc](https://github.com/trasc) — 2023-03-02T13:58:04Z
```diff
@@ -21,29 +21,73 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
+				ret = append(ret, lri)
+			}
+		}
+	}
+	return ret, nil
+}
+
+type WorkloadWebhook struct {
+	getter LimitRangeProvider
+}
 
 func setupWebhookForWorkload(mgr ctrl.Manager) error {
+
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-02T17:24:12Z

- **apis/kueue/webhooks/workload_webhook.go:39** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:04:58Z
```diff
@@ -21,29 +21,71 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
```

  We shouldn't need this interface.
  The fake client already gives you the ability to populate objects in tests.

- **apis/kueue/webhooks/workload_webhook.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:08:53Z
```diff
@@ -21,29 +21,73 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
```

  We need to consider all the fields. `min` and `max` would also affect the containers requests.

- **apis/kueue/webhooks/workload_webhook.go:61** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:10:30Z
```diff
@@ -21,29 +21,71 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
```

  the controller-runtime client has indexing capabilities. Use them to avoid filtering at this level.
  For example https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/indexer/indexer.go

- **apis/kueue/webhooks/workload_webhook.go:74** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:11:10Z
```diff
@@ -21,29 +21,71 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
+				ret = append(ret, lri)
+			}
+		}
+	}
+	return ret, nil
+}
+
+type WorkloadWebhook struct {
+	getter LimitRangeProvider
+}
 
 func setupWebhookForWorkload(mgr ctrl.Manager) error {
+	client, err := cgcorev1.NewForConfig(mgr.GetConfig())
```

  use the controller-runtime client instead: `mgr.GetClient`

- **apis/kueue/webhooks/workload_webhook.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:13:29Z
```diff
@@ -21,29 +21,71 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
+				ret = append(ret, lri)
+			}
+		}
+	}
+	return ret, nil
+}
+
+type WorkloadWebhook struct {
+	getter LimitRangeProvider
+}
 
 func setupWebhookForWorkload(mgr ctrl.Manager) error {
+	client, err := cgcorev1.NewForConfig(mgr.GetConfig())
+	if err != nil {
+		return err
+	}
+	wh := &WorkloadWebhook{getter: &limitRangeProvider{getter: client}}
+
 	return ctrl.NewWebhookManagedBy(mgr).
 		For(&kueue.Workload{}).
-		WithDefaulter(&WorkloadWebhook{}).
-		WithValidator(&WorkloadWebhook{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
 		Complete()
 }
 
 // +kubebuilder:webhook:path=/mutate-kueue-x-k8s-io-v1alpha2-workload,mutating=true,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=workloads,verbs=create;update,versions=v1alpha2,name=mworkload.kb.io,admissionReviewVersions=v1
+//+kubebuilder:rbac:groups="",resources=limitranges,verbs=get;list
```

  ```suggestion
  //+kubebuilder:rbac:groups="",resources=limitranges,verbs=get;list;watch
  ```
  
  This allows the controller-runtime client to cache the LimitRanges instead of querying every time.

- **apis/kueue/webhooks/workload_webhook.go:117** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:14:50Z
```diff
@@ -65,6 +112,9 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 		podSet := &wl.Spec.PodSets[i]
 		setContainersDefaults(podSet.Spec.InitContainers)
 		setContainersDefaults(podSet.Spec.Containers)
+
+		setContainersDefaultsFromLimitRanges(podSet.Spec.InitContainers, nsLimitRanges)
+		setContainersDefaultsFromLimitRanges(podSet.Spec.Containers, nsLimitRanges)
```

  should this run before or after setContainersDefaults?
  
  Can you experiment in a live cluster and see what are the effects of not having requests but limits, along with LimitRanges?

- **apis/kueue/webhooks/workload_webhook.go:135** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:15:47Z
```diff
@@ -76,13 +126,31 @@ func setContainersDefaults(containers []corev1.Container) {
 			if c.Resources.Requests == nil {
 				c.Resources.Requests = make(corev1.ResourceList)
 			}
-			for k, v := range c.Resources.Limits {
-				if _, exists := c.Resources.Requests[k]; !exists {
-					c.Resources.Requests[k] = v.DeepCopy()
-				}
-			}
+			c.Resources.Requests = mergeResourceList(c.Resources.Requests, c.Resources.Limits)
+		}
+	}
+}
+
+func setContainersDefaultsFromLimitRanges(containers []corev1.Container, limits []corev1.LimitRangeItem) {
+	for _, ri := range limits {
```

  can there be more than one Container LimitRange per namespace?

- **apis/kueue/webhooks/workload_webhook.go:139** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:16:18Z
```diff
@@ -76,13 +126,31 @@ func setContainersDefaults(containers []corev1.Container) {
 			if c.Resources.Requests == nil {
 				c.Resources.Requests = make(corev1.ResourceList)
 			}
-			for k, v := range c.Resources.Limits {
-				if _, exists := c.Resources.Requests[k]; !exists {
-					c.Resources.Requests[k] = v.DeepCopy()
-				}
-			}
+			c.Resources.Requests = mergeResourceList(c.Resources.Requests, c.Resources.Limits)
+		}
+	}
+}
+
+func setContainersDefaultsFromLimitRanges(containers []corev1.Container, limits []corev1.LimitRangeItem) {
+	for _, ri := range limits {
+		for i := range containers {
+			cr := &containers[i].Resources
+			cr.Limits = mergeResourceList(cr.Limits, ri.Default)
+			cr.Requests = mergeResourceList(cr.Requests, ri.DefaultRequest)
```

  what about Min and Max?

- **apis/kueue/webhooks/workload_webhook_test.go:539** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:17:08Z
```diff
@@ -398,3 +535,153 @@ func TestValidateWorkloadUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestListRangeGetter(t *testing.T) {
```

  We shouldn't need this test once we use the controller-runtime client.

- **pkg/controller/workload/job/job_controller.go:654** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-02T17:24:07Z
```diff
@@ -619,14 +660,12 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 		return false
 	}
 
-	// nodeSelector may change, hence we are not checking for
-	// equality of the whole job.Spec.Template.Spec.
-	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+	specHash, err := jobSpecHash(job)
```

  what if the LimitRanges change, but not the Job specs?
  
  The safest thing to do would be to look at the LimitRanges again. Although only before the Job is running. Because once it is running, the Pods wouldn't change when the LimitRanges change.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-03-03T05:35:25Z

- **apis/kueue/webhooks/workload_webhook.go:60** by [@kerthcet](https://github.com/kerthcet) — 2023-03-03T04:13:50Z
```diff
@@ -21,29 +21,73 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
```

  Yes, what @alculquicondor said is what I mean. Sorry for the confusion. 
  In my option, the approach should be consider the `default` and `defaultRequest` in webhook defaulting, consider the `min` and `max` in webhook validating. 
  
  When comparing `jobAndWorkloadEqual`:
   - if job is suspend, we'll query the limitRange again
   - if job is unsuspend, we can ignore the resources in comparation because pod will not be affected by LimitRange after creation.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-03T07:18:08Z

- **apis/kueue/webhooks/workload_webhook.go:74** by [@trasc](https://github.com/trasc) — 2023-03-03T07:18:08Z
```diff
@@ -21,29 +21,71 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
+				ret = append(ret, lri)
+			}
+		}
+	}
+	return ret, nil
+}
+
+type WorkloadWebhook struct {
+	getter LimitRangeProvider
+}
 
 func setupWebhookForWorkload(mgr ctrl.Manager) error {
+	client, err := cgcorev1.NewForConfig(mgr.GetConfig())
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-03T07:18:20Z

- **apis/kueue/webhooks/workload_webhook.go:88** by [@trasc](https://github.com/trasc) — 2023-03-03T07:18:20Z
```diff
@@ -21,29 +21,71 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
+				ret = append(ret, lri)
+			}
+		}
+	}
+	return ret, nil
+}
+
+type WorkloadWebhook struct {
+	getter LimitRangeProvider
+}
 
 func setupWebhookForWorkload(mgr ctrl.Manager) error {
+	client, err := cgcorev1.NewForConfig(mgr.GetConfig())
+	if err != nil {
+		return err
+	}
+	wh := &WorkloadWebhook{getter: &limitRangeProvider{getter: client}}
+
 	return ctrl.NewWebhookManagedBy(mgr).
 		For(&kueue.Workload{}).
-		WithDefaulter(&WorkloadWebhook{}).
-		WithValidator(&WorkloadWebhook{}).
+		WithDefaulter(wh).
+		WithValidator(wh).
 		Complete()
 }
 
 // +kubebuilder:webhook:path=/mutate-kueue-x-k8s-io-v1alpha2-workload,mutating=true,failurePolicy=fail,sideEffects=None,groups=kueue.x-k8s.io,resources=workloads,verbs=create;update,versions=v1alpha2,name=mworkload.kb.io,admissionReviewVersions=v1
+//+kubebuilder:rbac:groups="",resources=limitranges,verbs=get;list
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-03T07:20:09Z

- **apis/kueue/webhooks/workload_webhook.go:117** by [@trasc](https://github.com/trasc) — 2023-03-03T07:20:08Z
```diff
@@ -65,6 +112,9 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 		podSet := &wl.Spec.PodSets[i]
 		setContainersDefaults(podSet.Spec.InitContainers)
 		setContainersDefaults(podSet.Spec.Containers)
+
+		setContainersDefaultsFromLimitRanges(podSet.Spec.InitContainers, nsLimitRanges)
+		setContainersDefaultsFromLimitRanges(podSet.Spec.Containers, nsLimitRanges)
```

  This is how it works for pods.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-03T07:24:23Z

- **apis/kueue/webhooks/workload_webhook.go:135** by [@trasc](https://github.com/trasc) — 2023-03-03T07:24:22Z
```diff
@@ -76,13 +126,31 @@ func setContainersDefaults(containers []corev1.Container) {
 			if c.Resources.Requests == nil {
 				c.Resources.Requests = make(corev1.ResourceList)
 			}
-			for k, v := range c.Resources.Limits {
-				if _, exists := c.Resources.Requests[k]; !exists {
-					c.Resources.Requests[k] = v.DeepCopy()
-				}
-			}
+			c.Resources.Requests = mergeResourceList(c.Resources.Requests, c.Resources.Limits)
+		}
+	}
+}
+
+func setContainersDefaultsFromLimitRanges(containers []corev1.Container, limits []corev1.LimitRangeItem) {
+	for _, ri := range limits {
```

  Yes, and our "merge" strategy is the same as the one used by  https://github.com/kubernetes/kubernetes/blob/master/plugin/pkg/admission/limitranger/admission.go

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-03T07:24:45Z

- **apis/kueue/webhooks/workload_webhook_test.go:539** by [@trasc](https://github.com/trasc) — 2023-03-03T07:24:44Z
```diff
@@ -398,3 +535,153 @@ func TestValidateWorkloadUpdate(t *testing.T) {
 		})
 	}
 }
+
+func TestListRangeGetter(t *testing.T) {
```

  removed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-03T14:58:46Z

- **apis/kueue/webhooks/workload_webhook.go:135** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-03T14:58:46Z
```diff
@@ -76,13 +126,31 @@ func setContainersDefaults(containers []corev1.Container) {
 			if c.Resources.Requests == nil {
 				c.Resources.Requests = make(corev1.ResourceList)
 			}
-			for k, v := range c.Resources.Limits {
-				if _, exists := c.Resources.Requests[k]; !exists {
-					c.Resources.Requests[k] = v.DeepCopy()
-				}
-			}
+			c.Resources.Requests = mergeResourceList(c.Resources.Requests, c.Resources.Limits)
+		}
+	}
+}
+
+func setContainersDefaultsFromLimitRanges(containers []corev1.Container, limits []corev1.LimitRangeItem) {
+	for _, ri := range limits {
```

  Please reference the relevant code from kubernetes/kubernetes in a comment.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-03T15:15:11Z

- **apis/kueue/webhooks/workload_webhook.go:80** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-03T14:59:36Z
```diff
@@ -65,24 +75,70 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 		podSet := &wl.Spec.PodSets[i]
 		setContainersDefaults(podSet.Spec.InitContainers)
 		setContainersDefaults(podSet.Spec.Containers)
+
+		if nsLimitRangesDefaults != nil {
+			setContainersDefaultsFromRR(podSet.Spec.InitContainers, nsLimitRangesDefaults)
```

  ```suggestion
  			setContainersDefaultsFromLR(podSet.Spec.InitContainers, nsLimitRangesDefaults)
  ```

- **apis/kueue/webhooks/workload_webhook.go:117** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-03T15:07:17Z
```diff
@@ -65,24 +75,70 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 		podSet := &wl.Spec.PodSets[i]
 		setContainersDefaults(podSet.Spec.InitContainers)
 		setContainersDefaults(podSet.Spec.Containers)
+
+		if nsLimitRangesDefaults != nil {
+			setContainersDefaultsFromRR(podSet.Spec.InitContainers, nsLimitRangesDefaults)
+			setContainersDefaultsFromRR(podSet.Spec.Containers, nsLimitRangesDefaults)
+		}
 	}
 	return nil
 }
 
+func (w *WorkloadWebhook) getDefaultsFromLimitRanges(ctx context.Context, namespace string) (*corev1.ResourceRequirements, error) {
+	list := corev1.LimitRangeList{}
+	err := w.client.List(ctx, &list, &client.ListOptions{Namespace: namespace})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := &corev1.ResourceRequirements{
+		Limits:   corev1.ResourceList{},
+		Requests: corev1.ResourceList{},
+	}
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
+				ret.Requests = mergeResourceList(ret.Requests, lri.DefaultRequest)
```

  IIUC, `min` and `max` do not alter the values, they are just used for validation.

- **apis/kueue/webhooks/workload_webhook_test.go:341** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-03T15:11:15Z
```diff
@@ -173,17 +176,169 @@ func TestWorkloadWebhookDefault(t *testing.T) {
 				},
 			},
 		},
+		"get default limits from LimitsRange": {
+			wl: kueue.Workload{
+				Spec: kueue.WorkloadSpec{
+					PodSets: []kueue.PodSet{
+						{
+							Name: "foo",
+							Spec: corev1.PodSpec{
+								InitContainers: []corev1.Container{
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu": resource.MustParse("1"),
+											},
+											Limits: corev1.ResourceList{
+												"cpu": resource.MustParse("1.5"),
+											},
+										},
+									},
+								},
+							},
+						},
+						{
+							Name: "bar",
+							Spec: corev1.PodSpec{
+								Containers: []corev1.Container{
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu": resource.MustParse("1.5"),
+											},
+										},
+									},
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"memory": resource.MustParse("100Mi"),
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			},
+			wantWl: kueue.Workload{
+				Spec: kueue.WorkloadSpec{
+					PodSets: []kueue.PodSet{
+						{
+							Name: "foo",
+							Spec: corev1.PodSpec{
+								InitContainers: []corev1.Container{
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu":    resource.MustParse("1"),
+												"memory": resource.MustParse("300Mi"),
+											},
+											Limits: corev1.ResourceList{
+												"cpu":    resource.MustParse("1.5"),
+												"memory": resource.MustParse("400Mi"),
+											},
+										},
+									},
+								},
+							},
+						},
+						{
+							Name: "bar",
+							Spec: corev1.PodSpec{
+								Containers: []corev1.Container{
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu":    resource.MustParse("1.5"),
+												"memory": resource.MustParse("300Mi"),
+											},
+											Limits: corev1.ResourceList{
+												"cpu":    resource.MustParse("4"),
+												"memory": resource.MustParse("400Mi"),
+											},
+										},
+									},
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu":    resource.MustParse("3"),
+												"memory": resource.MustParse("100Mi"),
+											},
+											Limits: corev1.ResourceList{
+												"cpu":    resource.MustParse("4"),
+												"memory": resource.MustParse("400Mi"),
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			},
+			limitRanges: []client.Object{
+				&corev1.LimitRange{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "limits1",
+						Namespace: "default",
+					},
+					Spec: corev1.LimitRangeSpec{
+						Limits: []corev1.LimitRangeItem{
+							{
+								Type: corev1.LimitTypeContainer,
+								Default: corev1.ResourceList{
+									"cpu": resource.MustParse("4"),
+								},
+								DefaultRequest: corev1.ResourceList{
+									"cpu": resource.MustParse("3"),
+								},
+							},
+						},
+					},
+				},
+				&corev1.LimitRange{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "limits2",
+						Namespace: "default",
+					},
+					Spec: corev1.LimitRangeSpec{
+						Limits: []corev1.LimitRangeItem{
+							{
+								Type: corev1.LimitTypeContainer,
+								Default: corev1.ResourceList{
+									"cpu": resource.MustParse("4"),
+								},
+								DefaultRequest: corev1.ResourceList{
+									"cpu": resource.MustParse("3"),
+								},
+							},
+							{
+								Type: corev1.LimitTypeContainer,
+								Default: corev1.ResourceList{
+									"memory": resource.MustParse("400Mi"),
+								},
+								DefaultRequest: corev1.ResourceList{
+									"memory": resource.MustParse("300Mi"),
+								},
+							},
+						},
+					},
+				},
+			},
+		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
-			wh := &WorkloadWebhook{}
+			wh := &WorkloadWebhook{client: testingutil.NewFakeClient(tc.limitRanges...)}
 			wlCopy := tc.wl.DeepCopy()
 			if err := wh.Default(context.Background(), wlCopy); err != nil {
 				t.Fatalf("Could not apply defaults: %v", err)
 			}
 			if diff := cmp.Diff(tc.wantWl, *wlCopy); diff != "" {
 				t.Errorf("Obtained wrong defaults (-want,+got):\n%s", diff)
 			}
+			t.Log(wlCopy)
```

  remove this line

- **pkg/controller/workload/job/job_controller.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-03T15:14:05Z
```diff
@@ -50,6 +53,10 @@ var (
 	parentWorkloadKey = ".metadata.parentWorkload"
 )
 
+const (
+	sourceSpecHashKey = "kueue.x-k8s.io/source-hash"
```

  worth putting this in a more visible place, like `pkg/constants`

- **pkg/controller/workload/job/job_controller.go:654** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-03T15:15:05Z
```diff
@@ -619,14 +660,12 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 		return false
 	}
 
-	// nodeSelector may change, hence we are not checking for
-	// equality of the whole job.Spec.Template.Spec.
-	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+	specHash, err := jobSpecHash(job)
```

  this question is still open

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-06T09:46:02Z

- **apis/kueue/webhooks/workload_webhook.go:60** by [@trasc](https://github.com/trasc) — 2023-03-06T09:46:01Z
```diff
@@ -21,29 +21,73 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
```

  Indeed we could also validate the boundaries, however I propose to do this in another Issue/PR otherwise it will get to hard to track. Also we should decide what should happen if the validation fails, as of now, the job controller will just endlessly to to create the workload and fail.
  
  In my opinion,  tracking the defaults used in order to recreate the workload add too much coupling between  the job-controllers and the workloads, with very little added value (in practice I don't expect the `LimitRanges` to change very often).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-06T10:05:23Z

- **pkg/controller/workload/job/job_controller.go:654** by [@trasc](https://github.com/trasc) — 2023-03-06T10:05:22Z
```diff
@@ -619,14 +660,12 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 		return false
 	}
 
-	// nodeSelector may change, hence we are not checking for
-	// equality of the whole job.Spec.Template.Spec.
-	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+	specHash, err := jobSpecHash(job)
```

  The only scenario in which could change the  flow is when a workload is not scheduled because the an old default value douse not fit and it will fit with the new one.
  What we could do is to extend the workload controller to watch the changes in the  LimitsRanges and mark the workloads using outdated values.  Then the job controllers can decide if they want to delete and recreate the workload. Do we want something like that?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-06T10:13:24Z

- **apis/kueue/webhooks/workload_webhook.go:139** by [@trasc](https://github.com/trasc) — 2023-03-06T10:13:24Z
```diff
@@ -76,13 +126,31 @@ func setContainersDefaults(containers []corev1.Container) {
 			if c.Resources.Requests == nil {
 				c.Resources.Requests = make(corev1.ResourceList)
 			}
-			for k, v := range c.Resources.Limits {
-				if _, exists := c.Resources.Requests[k]; !exists {
-					c.Resources.Requests[k] = v.DeepCopy()
-				}
-			}
+			c.Resources.Requests = mergeResourceList(c.Resources.Requests, c.Resources.Limits)
+		}
+	}
+}
+
+func setContainersDefaultsFromLimitRanges(containers []corev1.Container, limits []corev1.LimitRangeItem) {
+	for _, ri := range limits {
+		for i := range containers {
+			cr := &containers[i].Resources
+			cr.Limits = mergeResourceList(cr.Limits, ri.Default)
+			cr.Requests = mergeResourceList(cr.Requests, ri.DefaultRequest)
```

  In my opinion Min and Max  should only be used in validation.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-06T10:14:07Z

- **apis/kueue/webhooks/workload_webhook.go:117** by [@trasc](https://github.com/trasc) — 2023-03-06T10:14:07Z
```diff
@@ -65,24 +75,70 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 		podSet := &wl.Spec.PodSets[i]
 		setContainersDefaults(podSet.Spec.InitContainers)
 		setContainersDefaults(podSet.Spec.Containers)
+
+		if nsLimitRangesDefaults != nil {
+			setContainersDefaultsFromRR(podSet.Spec.InitContainers, nsLimitRangesDefaults)
+			setContainersDefaultsFromRR(podSet.Spec.Containers, nsLimitRangesDefaults)
+		}
 	}
 	return nil
 }
 
+func (w *WorkloadWebhook) getDefaultsFromLimitRanges(ctx context.Context, namespace string) (*corev1.ResourceRequirements, error) {
+	list := corev1.LimitRangeList{}
+	err := w.client.List(ctx, &list, &client.ListOptions{Namespace: namespace})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := &corev1.ResourceRequirements{
+		Limits:   corev1.ResourceList{},
+		Requests: corev1.ResourceList{},
+	}
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
+				ret.Requests = mergeResourceList(ret.Requests, lri.DefaultRequest)
```

  yes

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-06T10:28:51Z

- **apis/kueue/webhooks/workload_webhook_test.go:341** by [@trasc](https://github.com/trasc) — 2023-03-06T10:28:51Z
```diff
@@ -173,17 +176,169 @@ func TestWorkloadWebhookDefault(t *testing.T) {
 				},
 			},
 		},
+		"get default limits from LimitsRange": {
+			wl: kueue.Workload{
+				Spec: kueue.WorkloadSpec{
+					PodSets: []kueue.PodSet{
+						{
+							Name: "foo",
+							Spec: corev1.PodSpec{
+								InitContainers: []corev1.Container{
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu": resource.MustParse("1"),
+											},
+											Limits: corev1.ResourceList{
+												"cpu": resource.MustParse("1.5"),
+											},
+										},
+									},
+								},
+							},
+						},
+						{
+							Name: "bar",
+							Spec: corev1.PodSpec{
+								Containers: []corev1.Container{
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu": resource.MustParse("1.5"),
+											},
+										},
+									},
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"memory": resource.MustParse("100Mi"),
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			},
+			wantWl: kueue.Workload{
+				Spec: kueue.WorkloadSpec{
+					PodSets: []kueue.PodSet{
+						{
+							Name: "foo",
+							Spec: corev1.PodSpec{
+								InitContainers: []corev1.Container{
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu":    resource.MustParse("1"),
+												"memory": resource.MustParse("300Mi"),
+											},
+											Limits: corev1.ResourceList{
+												"cpu":    resource.MustParse("1.5"),
+												"memory": resource.MustParse("400Mi"),
+											},
+										},
+									},
+								},
+							},
+						},
+						{
+							Name: "bar",
+							Spec: corev1.PodSpec{
+								Containers: []corev1.Container{
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu":    resource.MustParse("1.5"),
+												"memory": resource.MustParse("300Mi"),
+											},
+											Limits: corev1.ResourceList{
+												"cpu":    resource.MustParse("4"),
+												"memory": resource.MustParse("400Mi"),
+											},
+										},
+									},
+									{
+										Resources: corev1.ResourceRequirements{
+											Requests: corev1.ResourceList{
+												"cpu":    resource.MustParse("3"),
+												"memory": resource.MustParse("100Mi"),
+											},
+											Limits: corev1.ResourceList{
+												"cpu":    resource.MustParse("4"),
+												"memory": resource.MustParse("400Mi"),
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			},
+			limitRanges: []client.Object{
+				&corev1.LimitRange{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "limits1",
+						Namespace: "default",
+					},
+					Spec: corev1.LimitRangeSpec{
+						Limits: []corev1.LimitRangeItem{
+							{
+								Type: corev1.LimitTypeContainer,
+								Default: corev1.ResourceList{
+									"cpu": resource.MustParse("4"),
+								},
+								DefaultRequest: corev1.ResourceList{
+									"cpu": resource.MustParse("3"),
+								},
+							},
+						},
+					},
+				},
+				&corev1.LimitRange{
+					ObjectMeta: metav1.ObjectMeta{
+						Name:      "limits2",
+						Namespace: "default",
+					},
+					Spec: corev1.LimitRangeSpec{
+						Limits: []corev1.LimitRangeItem{
+							{
+								Type: corev1.LimitTypeContainer,
+								Default: corev1.ResourceList{
+									"cpu": resource.MustParse("4"),
+								},
+								DefaultRequest: corev1.ResourceList{
+									"cpu": resource.MustParse("3"),
+								},
+							},
+							{
+								Type: corev1.LimitTypeContainer,
+								Default: corev1.ResourceList{
+									"memory": resource.MustParse("400Mi"),
+								},
+								DefaultRequest: corev1.ResourceList{
+									"memory": resource.MustParse("300Mi"),
+								},
+							},
+						},
+					},
+				},
+			},
+		},
 	}
 	for name, tc := range cases {
 		t.Run(name, func(t *testing.T) {
-			wh := &WorkloadWebhook{}
+			wh := &WorkloadWebhook{client: testingutil.NewFakeClient(tc.limitRanges...)}
 			wlCopy := tc.wl.DeepCopy()
 			if err := wh.Default(context.Background(), wlCopy); err != nil {
 				t.Fatalf("Could not apply defaults: %v", err)
 			}
 			if diff := cmp.Diff(tc.wantWl, *wlCopy); diff != "" {
 				t.Errorf("Obtained wrong defaults (-want,+got):\n%s", diff)
 			}
+			t.Log(wlCopy)
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-06T10:36:29Z

- **pkg/controller/workload/job/job_controller.go:57** by [@trasc](https://github.com/trasc) — 2023-03-06T10:36:28Z
```diff
@@ -50,6 +53,10 @@ var (
 	parentWorkloadKey = ".metadata.parentWorkload"
 )
 
+const (
+	sourceSpecHashKey = "kueue.x-k8s.io/source-hash"
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-06T10:49:28Z

- **apis/kueue/webhooks/workload_webhook.go:61** by [@trasc](https://github.com/trasc) — 2023-03-06T10:49:28Z
```diff
@@ -21,29 +21,71 @@ import (
 
 	corev1 "k8s.io/api/core/v1"
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	metav1validation "k8s.io/apimachinery/pkg/apis/meta/v1/validation"
 	"k8s.io/apimachinery/pkg/runtime"
 	"k8s.io/apimachinery/pkg/util/sets"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
+	cgcorev1 "k8s.io/client-go/kubernetes/typed/core/v1"
 	"k8s.io/klog/v2"
 	ctrl "sigs.k8s.io/controller-runtime"
 	"sigs.k8s.io/controller-runtime/pkg/webhook"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1alpha2"
 )
 
-type WorkloadWebhook struct{}
+// LimitRangeProvider - hepler interface able to privide LimitRanges
+type LimitRangeProvider interface {
+	// GetContainersLimitRanges - retrive all the container relevant limit range item in the provided
+	// namespace
+	GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error)
+}
+
+type limitRangeProvider struct {
+	getter cgcorev1.LimitRangesGetter
+}
+
+func (w *limitRangeProvider) GetContainersLimitRanges(ctx context.Context, namespace string) ([]corev1.LimitRangeItem, error) {
+	list, err := w.getter.LimitRanges(namespace).List(ctx, metav1.ListOptions{})
+	if err != nil {
+		return nil, err
+	}
+	if len(list.Items) == 0 {
+		return nil, nil
+	}
+
+	ret := make([]corev1.LimitRangeItem, 0, len(list.Items))
+	for _, lr := range list.Items {
+		for _, lri := range lr.Spec.Limits {
+			if lri.Type == corev1.LimitTypeContainer {
```

  Indexing could help, but only for the first loop, and in practice , is the same namespace, is unlikely to have multiple LimitRanges.
  
  With the change in  `getDefaultsFromLimitRanges` these loops are only executed once for every web-hook call.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-06T21:36:42Z

- **pkg/controller/workload/job/job_controller.go:654** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-06T21:36:41Z
```diff
@@ -619,14 +660,12 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 		return false
 	}
 
-	// nodeSelector may change, hence we are not checking for
-	// equality of the whole job.Spec.Template.Spec.
-	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+	specHash, err := jobSpecHash(job)
```

  Let's assume LimitRanges never change. In that case, this is a fine solution and I would be ok merging it.
  
  Realistically, LimitRanges can change, the next question is whether it's acceptable to be out-of-sync for a first iteration of LimitRange support. I think it's fine.
  
  However, I'm afraid that once we add support for changing LimitRanges, we would just throw away this code based on hashes. Are we ok with that?
  
  I think it's just more useful to directly write the code that handles changing LimitRanges. We don't need to add code to react to changes in LimitRange. It suffices to do the checks whenever there are changes in the Workload or Job object. If there are changes, we can just keep the current logic that deletes the Workload object. Once we decide to have a better support for this, we can change the code to update the Workload object instead.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-07T07:42:18Z

- **pkg/controller/workload/job/job_controller.go:654** by [@trasc](https://github.com/trasc) — 2023-03-07T07:42:18Z
```diff
@@ -619,14 +660,12 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 		return false
 	}
 
-	// nodeSelector may change, hence we are not checking for
-	// equality of the whole job.Spec.Template.Spec.
-	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+	specHash, err := jobSpecHash(job)
```

  This part just keeps track of the sync between the job spec and the original (before defaulting is applied ) workload content and should not be impacted by the the limits changes.
  
  Also #611 was opened to work on that resync.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-14T15:24:17Z

- **apis/kueue/v1beta1/workload_types.go:102** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:02:34Z
```diff
@@ -95,6 +95,13 @@ type PodSet struct {
 	// count is the number of pods for the spec.
 	// +kubebuilder:validation:Minimum=1
 	Count int32 `json:"count"`
+
+	// Total Request holds the pod's resource requirements, the values being
+	// maintained by the Kueue operator.
+	//
+	// The computation can take into account the cluster's LimitRange.
```

  ```suggestion
  	// The computation takes into account defaults, RuntimeClass and LimitRange.
  ```

- **apis/kueue/webhooks/workload_webhook.go:80** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:04:49Z
```diff
@@ -63,26 +74,84 @@ func (w *WorkloadWebhook) Default(ctx context.Context, obj runtime.Object) error
 	}
 	for i := range wl.Spec.PodSets {
 		podSet := &wl.Spec.PodSets[i]
-		setContainersDefaults(podSet.Template.Spec.InitContainers)
-		setContainersDefaults(podSet.Template.Spec.Containers)
+
+		// we apply the default request logic in a pod spec copy in order to compute the TotalResources
+		specCopy := podSet.Template.Spec.DeepCopy()
+		updatePodDefaultResources(specCopy, nsLimitRangesDefaults)
```

  let's apply these defaults into the totals only, instead of modifying the pod template

- **pkg/cache/cache_test.go:718** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:11:37Z
```diff
@@ -715,9 +715,11 @@ func TestCacheWorkloadOperations(t *testing.T) {
 		*utiltesting.MakePodSet("driver", 1).
 			Request(corev1.ResourceCPU, "10m").
 			Request(corev1.ResourceMemory, "512Ki").
+			TotalRequestFunc(workload.PodTotalRequests).
```

  Is there an scenario where we pass a different function?
  
  If this is about avoiding a circular dependency, it might be better to call `workload.PodSetsTotalRequests` once in the test body function, instead of doing it in the wrappers.

- **pkg/controller/core/workload_controller.go:454** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:18:00Z
```diff
@@ -425,3 +443,51 @@ func handlePodOverhead(log logr.Logger, wl *kueue.Workload, c client.Client) {
 		}
 	}
 }
+
+type limitRangeHandler struct {
+	client client.Client
+}
+
+func (h *limitRangeHandler) Create(e event.CreateEvent, _ workqueue.RateLimitingInterface) {
+	h.deleteWorkloads(e.Object)
+}
+
+func (h *limitRangeHandler) Update(e event.UpdateEvent, _ workqueue.RateLimitingInterface) {
+	h.deleteWorkloads(e.ObjectNew)
+}
+
+func (h *limitRangeHandler) Delete(e event.DeleteEvent, _ workqueue.RateLimitingInterface) {
+	h.deleteWorkloads(e.Object)
+}
+
+func (h *limitRangeHandler) Generic(_ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+}
+
+func (h *limitRangeHandler) deleteWorkloads(obj client.Object) {
+	lr, ok := obj.(*corev1.LimitRange)
+	if !ok {
+		return
+	}
```

  also check that the LimitRange has LimitTypeContainer

- **pkg/controller/core/workload_controller.go:486** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:19:01Z
```diff
@@ -425,3 +443,51 @@ func handlePodOverhead(log logr.Logger, wl *kueue.Workload, c client.Client) {
 		}
 	}
 }
+
+type limitRangeHandler struct {
+	client client.Client
+}
+
+func (h *limitRangeHandler) Create(e event.CreateEvent, _ workqueue.RateLimitingInterface) {
+	h.deleteWorkloads(e.Object)
+}
+
+func (h *limitRangeHandler) Update(e event.UpdateEvent, _ workqueue.RateLimitingInterface) {
+	h.deleteWorkloads(e.ObjectNew)
+}
+
+func (h *limitRangeHandler) Delete(e event.DeleteEvent, _ workqueue.RateLimitingInterface) {
+	h.deleteWorkloads(e.Object)
+}
+
+func (h *limitRangeHandler) Generic(_ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+}
+
+func (h *limitRangeHandler) deleteWorkloads(obj client.Object) {
+	lr, ok := obj.(*corev1.LimitRange)
+	if !ok {
+		return
+	}
+
+	//TODO: the eventHandler should get a context soon, and this could be dropped
+	// https://github.com/kubernetes-sigs/controller-runtime/blob/master/pkg/handler/eventhandler.go
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("limitRange", klog.KObj(lr))
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	lst := kueue.WorkloadList{}
+	err := h.client.List(ctx, &lst, client.InNamespace(lr.Namespace), client.MatchingFields{indexer.WorkloadAdmittedKey: string(metav1.ConditionFalse)})
+	if err != nil {
+		log.Error(err, "Could not list the unatmitted workloads")
+	}
+	log.V(5).Info("deleting workloads", "count", len(lst.Items))
+
+	for _, w := range lst.Items {
+		log.V(5).Info("deleting", "workload", klog.KObj(&w))
```

  Better move it after the Delete call
  
  ```suggestion
  		log.V(2).Info("Deleted Workload due to changing LimitRange", "workload", klog.KObj(&w))
  ```

- **pkg/controller/core/workload_controller.go:481** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:19:18Z
```diff
@@ -425,3 +443,51 @@ func handlePodOverhead(log logr.Logger, wl *kueue.Workload, c client.Client) {
 		}
 	}
 }
+
+type limitRangeHandler struct {
+	client client.Client
+}
+
+func (h *limitRangeHandler) Create(e event.CreateEvent, _ workqueue.RateLimitingInterface) {
+	h.deleteWorkloads(e.Object)
+}
+
+func (h *limitRangeHandler) Update(e event.UpdateEvent, _ workqueue.RateLimitingInterface) {
+	h.deleteWorkloads(e.ObjectNew)
+}
+
+func (h *limitRangeHandler) Delete(e event.DeleteEvent, _ workqueue.RateLimitingInterface) {
+	h.deleteWorkloads(e.Object)
+}
+
+func (h *limitRangeHandler) Generic(_ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+}
+
+func (h *limitRangeHandler) deleteWorkloads(obj client.Object) {
+	lr, ok := obj.(*corev1.LimitRange)
+	if !ok {
+		return
+	}
+
+	//TODO: the eventHandler should get a context soon, and this could be dropped
+	// https://github.com/kubernetes-sigs/controller-runtime/blob/master/pkg/handler/eventhandler.go
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("limitRange", klog.KObj(lr))
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	lst := kueue.WorkloadList{}
+	err := h.client.List(ctx, &lst, client.InNamespace(lr.Namespace), client.MatchingFields{indexer.WorkloadAdmittedKey: string(metav1.ConditionFalse)})
+	if err != nil {
+		log.Error(err, "Could not list the unatmitted workloads")
```

  ```suggestion
  		log.Error(err, "Could not list the unadmitted workloads")
  ```

- **pkg/controller/core/workload_controller.go:447** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:19:47Z
```diff
@@ -425,3 +443,51 @@ func handlePodOverhead(log logr.Logger, wl *kueue.Workload, c client.Client) {
 		}
 	}
 }
+
+type limitRangeHandler struct {
```

  I'm hesitant about doing this in a handler. Handlers should be fast, and thus avoid API calls. Could you add a separate controller just for LimitRange? That way we can do the deletions asynchronously in a worker.
  
  Although if we move the calculation to admission, we won't need this, right?

- **apis/kueue/v1beta1/workload_types.go:104** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:24:05Z
```diff
@@ -95,6 +95,13 @@ type PodSet struct {
 	// count is the number of pods for the spec.
 	// +kubebuilder:validation:Minimum=1
 	Count int32 `json:"count"`
+
+	// Total Request holds the pod's resource requirements, the values being
+	// maintained by the Kueue operator.
+	//
+	// The computation can take into account the cluster's LimitRange.
+	// +optional
+	TotalRequests corev1.ResourceList `json:"totalRequests,omitempty"`
```

  If we move this field into status, we can do the calculations at the time of admission.
  This means that we don't need to recreate Workloads. And after admission they will have the information in Status, which the cache can use.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-15T06:16:22Z

- **pkg/cache/cache_test.go:718** by [@trasc](https://github.com/trasc) — 2023-03-15T06:16:22Z
```diff
@@ -715,9 +715,11 @@ func TestCacheWorkloadOperations(t *testing.T) {
 		*utiltesting.MakePodSet("driver", 1).
 			Request(corev1.ResourceCPU, "10m").
 			Request(corev1.ResourceMemory, "512Ki").
+			TotalRequestFunc(workload.PodTotalRequests).
```

  no, but workload package is also using the wrappers and I'm doing this to avoid circular dependencies

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-15T13:25:21Z

- **pkg/cache/cache_test.go:718** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T13:25:20Z
```diff
@@ -715,9 +715,11 @@ func TestCacheWorkloadOperations(t *testing.T) {
 		*utiltesting.MakePodSet("driver", 1).
 			Request(corev1.ResourceCPU, "10m").
 			Request(corev1.ResourceMemory, "512Ki").
+			TotalRequestFunc(workload.PodTotalRequests).
```

  Right. Then there are two alternatives:
  1. Avoid making use of the wrappers in `workload_test.go`. These tests are small and intend to test multiple PodSpec fields that might not be worth to add in the wrappers.
  2. Call some form of `workload.PodSetsTotalRequest` from the test body instead of in the wrappers.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-16T19:05:09Z

With this PR, if a LimitRange changes, then the usage of admitted Workloads would change.

I think this is fine as an intermediate step given that we do have the same problem with RuntimeClass.

- **pkg/util/limitrange/limitrange.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T18:58:15Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  given this, I think having a cache for the limitranges is an overkill.
  
  You can just use `client.List` in the Workload controller. Note that this doesn't involve an API call, as it would use the informer cache.

- **pkg/controller/core/limitrange_controller.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T18:59:33Z
  If we get rid of the cache, this controller is not needed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-16T19:07:54Z

- **pkg/util/resource/resource.go:73** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T19:07:44Z
```diff
@@ -0,0 +1,85 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resource
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	apiresource "k8s.io/apimachinery/pkg/api/resource"
+)
+
+type resolveConflict func(a, b apiresource.Quantity) apiresource.Quantity
+
+func mergeResourceList(a, b corev1.ResourceList, f resolveConflict) corev1.ResourceList {
+	if a == nil {
+		return b.DeepCopy()
+	}
+	ret := a.DeepCopy()
+
+	for k, vb := range b {
+		if va, exists := ret[k]; !exists {
+			ret[k] = vb.DeepCopy()
+		} else {
+			if f != nil {
+				ret[k] = f(va, vb)
+			}
+		}
+	}
+	return ret
+}
+
+// MergeResourceListKeepFirst creates a new ResourceList holding all resource values from dst
+// and any new values from src
+func MergeResourceListKeepFirst(dst, src corev1.ResourceList) corev1.ResourceList {
+	return mergeResourceList(dst, src, nil)
+}
+
+func maxQ(a, b apiresource.Quantity) apiresource.Quantity {
+	if a.Cmp(b) < 0 {
+		return b
+	}
+	return a
+}
+
+// MergeResourceListKeepMax creates a new ResourceList holding all the values from a and b
+// and resolve potential conflicts by keeping the highest value.
+func MergeResourceListKeepMax(a, b corev1.ResourceList) corev1.ResourceList {
+	return mergeResourceList(a, b, maxQ)
+}
+
+func minQ(a, b apiresource.Quantity) apiresource.Quantity {
+	if a.Cmp(b) > 0 {
+		return b
+	}
+	return a
+}
+
+// MergeResourceListKeepMin creates a new ResourceList holding all the values from a and b
+// and resolve potential conflicts by keeping the lowest value.
+func MergeResourceListKeepMin(a, b corev1.ResourceList) corev1.ResourceList {
+	return mergeResourceList(a, b, minQ)
```

  I would just inline the function here, because `minQ` is not useful on its own

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-16T19:59:00Z

- **pkg/util/limitrange/limitrange.go:60** by [@trasc](https://github.com/trasc) — 2023-03-16T19:59:00Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  It could look as overkill now, but going forward I think we sould use the cache to compute live resource usage for the workloads, in the scheduling process, and record those values at the admission time (somewhere in status.admission) to be used as while the workload is in the cache.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-16T20:41:30Z

- **pkg/util/limitrange/limitrange.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T20:41:30Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  Just storing in status should be enough.
  Usage from admitted workloads would just be calculated from the status.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T05:32:17Z

- **pkg/util/limitrange/limitrange.go:60** by [@trasc](https://github.com/trasc) — 2023-03-17T05:32:17Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  Even so, when a change is done to the limit ranges, we'd want to trigger a readjustment of the total for all the unadmitted workloads within a namespace, without a cache for all of them we need to list the same set of limitRanges and compute the summary.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T06:20:54Z

- **pkg/util/resource/resource.go:73** by [@trasc](https://github.com/trasc) — 2023-03-17T06:20:54Z
```diff
@@ -0,0 +1,85 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resource
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	apiresource "k8s.io/apimachinery/pkg/api/resource"
+)
+
+type resolveConflict func(a, b apiresource.Quantity) apiresource.Quantity
+
+func mergeResourceList(a, b corev1.ResourceList, f resolveConflict) corev1.ResourceList {
+	if a == nil {
+		return b.DeepCopy()
+	}
+	ret := a.DeepCopy()
+
+	for k, vb := range b {
+		if va, exists := ret[k]; !exists {
+			ret[k] = vb.DeepCopy()
+		} else {
+			if f != nil {
+				ret[k] = f(va, vb)
+			}
+		}
+	}
+	return ret
+}
+
+// MergeResourceListKeepFirst creates a new ResourceList holding all resource values from dst
+// and any new values from src
+func MergeResourceListKeepFirst(dst, src corev1.ResourceList) corev1.ResourceList {
+	return mergeResourceList(dst, src, nil)
+}
+
+func maxQ(a, b apiresource.Quantity) apiresource.Quantity {
+	if a.Cmp(b) < 0 {
+		return b
+	}
+	return a
+}
+
+// MergeResourceListKeepMax creates a new ResourceList holding all the values from a and b
+// and resolve potential conflicts by keeping the highest value.
+func MergeResourceListKeepMax(a, b corev1.ResourceList) corev1.ResourceList {
+	return mergeResourceList(a, b, maxQ)
+}
+
+func minQ(a, b apiresource.Quantity) apiresource.Quantity {
+	if a.Cmp(b) > 0 {
+		return b
+	}
+	return a
+}
+
+// MergeResourceListKeepMin creates a new ResourceList holding all the values from a and b
+// and resolve potential conflicts by keeping the lowest value.
+func MergeResourceListKeepMin(a, b corev1.ResourceList) corev1.ResourceList {
+	return mergeResourceList(a, b, minQ)
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T13:12:58Z

- **pkg/util/limitrange/limitrange.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T13:12:57Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  yes, and that is fine, because:
  1. Usually there is just one.
  2. The client already has a cache, so there are no API calls.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T13:20:26Z

- **pkg/util/limitrange/limitrange.go:60** by [@trasc](https://github.com/trasc) — 2023-03-17T13:20:25Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  One other use-case for this validation could be to use it in the scheduler to validate that the resources do not fit the limits. (some ware in `nominate()`)

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T13:32:33Z

- **pkg/util/limitrange/limitrange.go:60** by [@trasc](https://github.com/trasc) — 2023-03-17T13:32:33Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  However, if you insist, we can drop it for now and deal with the validation when the time comes.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T14:06:19Z

- **pkg/util/limitrange/limitrange.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T14:06:18Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  Validation can also list from the client.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T14:19:22Z

- **pkg/util/limitrange/limitrange.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T14:19:21Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  Maintaining a cache in addition to the informer's cache adds several complications that has to payoff with a significant performance boost. It's not worth optimizing for limit ranges when they are usually just one.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T16:21:31Z

- **pkg/util/limitrange/limitrange.go:60** by [@trasc](https://github.com/trasc) — 2023-03-17T16:21:31Z
```diff
@@ -0,0 +1,104 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package limitrange
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/equality"
+
+	"sigs.k8s.io/kueue/pkg/util/resource"
+)
+
+type Summary map[corev1.LimitType]corev1.LimitRangeItem
+
+// Summarize summarizes the provides ranges by:
+// 1. keeping the lowest values for Max and MaxLimitReqestRatio limits
+// 2. keeping the highest values for Min limits
+// 3. keeping the first encountered values for Default and DefaultRequests
+func Summarize(ranges ...corev1.LimitRange) Summary {
+	ret := make(Summary, 3)
+	for i := range ranges {
+		for _, item := range ranges[i].Spec.Limits {
+			ret[item.Type] = addToSummary(ret[item.Type], item)
+		}
+	}
+	return ret
+}
+
+func addToSummary(summary, item corev1.LimitRangeItem) corev1.LimitRangeItem {
+	summary.Max = resource.MergeResourceListKeepMin(summary.Max, item.Max)
+	summary.Min = resource.MergeResourceListKeepMax(summary.Min, item.Min)
+
+	summary.Default = resource.MergeResourceListKeepFirst(summary.Default, item.Default)
+	summary.DefaultRequest = resource.MergeResourceListKeepFirst(summary.DefaultRequest, item.DefaultRequest)
+
+	summary.MaxLimitRequestRatio = resource.MergeResourceListKeepMin(summary.MaxLimitRequestRatio, item.MaxLimitRequestRatio)
+	return summary
+}
+
+// SummaryCache provides a thread-safe cache mapping a string to a Summary
+// Like the standard map, reading from it is safe and will return a zero value
+type SummaryCache struct {
+	m sync.RWMutex
+
+	// in practice we'll have one for each namespace
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T16:23:44Z

- **pkg/cache/cache_test.go:718** by [@trasc](https://github.com/trasc) — 2023-03-17T16:23:43Z
```diff
@@ -715,9 +715,11 @@ func TestCacheWorkloadOperations(t *testing.T) {
 		*utiltesting.MakePodSet("driver", 1).
 			Request(corev1.ResourceCPU, "10m").
 			Request(corev1.ResourceMemory, "512Ki").
+			TotalRequestFunc(workload.PodTotalRequests).
```

  no longer needed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T18:26:37Z

- **pkg/controller/core/workload_controller.go:438** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T18:07:24Z
```diff
@@ -425,3 +433,50 @@ func handlePodOverhead(log logr.Logger, wl *kueue.Workload, c client.Client) {
 		}
 	}
 }
+
+func (r *WorkloadReconciler) handlePodLimitRange(log logr.Logger, wl *kueue.Workload) {
+	ctx := context.Background()
```

  ```suggestion
  	ctx := context.TODO()
  ```
  
  given that controller-runtime will add contexts to event handlers soon

- **pkg/controller/core/workload_controller.go:441** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T18:11:47Z
```diff
@@ -425,3 +433,50 @@ func handlePodOverhead(log logr.Logger, wl *kueue.Workload, c client.Client) {
 		}
 	}
 }
+
+func (r *WorkloadReconciler) handlePodLimitRange(log logr.Logger, wl *kueue.Workload) {
+	ctx := context.Background()
+	// get the list of limit ranges
+	var list corev1.LimitRangeList
+	if err := r.client.List(ctx, &list, &client.ListOptions{Namespace: wl.Namespace}); err != nil {
```

  maybe for follow up: we can use an index to just grab the just the ones that define a Container type.

- **pkg/util/resource/resource_test.go:47** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T18:16:43Z
```diff
@@ -0,0 +1,198 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resource
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	apiresource "k8s.io/apimachinery/pkg/api/resource"
+)
+
+func TestMerge(t *testing.T) {
+	rl_500mcpu_2GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("500m"),
+		corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+	}
+	rl_1cpu := corev1.ResourceList{
+		corev1.ResourceCPU: apiresource.MustParse("1"),
+	}
+	rl_1cpu_1GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("1"),
+		corev1.ResourceMemory: apiresource.MustParse("1Gi"),
+	}
+
+	type oper_result struct {
+		oper   func(a, b corev1.ResourceList) corev1.ResourceList
+		result corev1.ResourceList
+	}
+	cases := map[string]struct {
+		a        corev1.ResourceList
+		b        corev1.ResourceList
+		expected map[string]oper_result
```

  nit
  
  ```suggestion
  		want map[string]oper_result
  ```

- **pkg/util/resource/resource_test.go:83** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T18:17:30Z
```diff
@@ -0,0 +1,198 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resource
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	apiresource "k8s.io/apimachinery/pkg/api/resource"
+)
+
+func TestMerge(t *testing.T) {
+	rl_500mcpu_2GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("500m"),
+		corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+	}
+	rl_1cpu := corev1.ResourceList{
+		corev1.ResourceCPU: apiresource.MustParse("1"),
+	}
+	rl_1cpu_1GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("1"),
+		corev1.ResourceMemory: apiresource.MustParse("1Gi"),
+	}
+
+	type oper_result struct {
+		oper   func(a, b corev1.ResourceList) corev1.ResourceList
+		result corev1.ResourceList
+	}
+	cases := map[string]struct {
+		a        corev1.ResourceList
+		b        corev1.ResourceList
+		expected map[string]oper_result
+	}{
+		"asymetric": {
+			a: rl_1cpu,
+			b: rl_500mcpu_2GiMem,
+			expected: map[string]oper_result{
+				"merge": {
+					oper: MergeResourceListKeepFirst,
+					result: corev1.ResourceList{
+						corev1.ResourceCPU:    apiresource.MustParse("1"),
+						corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+					},
+				},
+				"min": {
+					oper: MergeResourceListKeepMin,
+					result: corev1.ResourceList{
+						corev1.ResourceCPU:    apiresource.MustParse("500m"),
+						corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+					},
+				},
+				"max": {
+					oper: MergeResourceListKeepMax,
+					result: corev1.ResourceList{
+						corev1.ResourceCPU:    apiresource.MustParse("1"),
+						corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+					},
+				},
+				"sum": {
+					oper: MergeResourceListKeepSum,
+					result: corev1.ResourceList{
+						corev1.ResourceCPU:    apiresource.MustParse("1500m"),
+						corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+					},
+				},
+			},
+		},
+		"symetric": {
```

  ```suggestion
  		"symmetric": {
  ```

- **pkg/util/resource/resource_test.go:49** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T18:17:36Z
```diff
@@ -0,0 +1,198 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resource
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	apiresource "k8s.io/apimachinery/pkg/api/resource"
+)
+
+func TestMerge(t *testing.T) {
+	rl_500mcpu_2GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("500m"),
+		corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+	}
+	rl_1cpu := corev1.ResourceList{
+		corev1.ResourceCPU: apiresource.MustParse("1"),
+	}
+	rl_1cpu_1GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("1"),
+		corev1.ResourceMemory: apiresource.MustParse("1Gi"),
+	}
+
+	type oper_result struct {
+		oper   func(a, b corev1.ResourceList) corev1.ResourceList
+		result corev1.ResourceList
+	}
+	cases := map[string]struct {
+		a        corev1.ResourceList
+		b        corev1.ResourceList
+		expected map[string]oper_result
+	}{
+		"asymetric": {
```

  ```suggestion
  		"asymmetric": {
  ```

- **pkg/util/testing/wrappers.go:515** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T18:20:09Z
```diff
@@ -499,3 +511,66 @@ func (rc *RuntimeClassWrapper) PodOverhead(resources corev1.ResourceList) *Runti
 func (rc *RuntimeClassWrapper) Obj() *nodev1.RuntimeClass {
 	return &rc.RuntimeClass
 }
+
+type limitRangeBuider struct{ corev1.LimitRange }
```

  ```suggestion
  type LimitRangeWrapper struct{ corev1.LimitRange }
  ```
  
  to stay consistent

- **pkg/util/testing/wrappers.go:543** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T18:21:19Z
```diff
@@ -499,3 +511,66 @@ func (rc *RuntimeClassWrapper) PodOverhead(resources corev1.ResourceList) *Runti
 func (rc *RuntimeClassWrapper) Obj() *nodev1.RuntimeClass {
 	return &rc.RuntimeClass
 }
+
+type limitRangeBuider struct{ corev1.LimitRange }
+
+func MakeLimitRange(name, namespace string) *limitRangeBuider {
+	return &limitRangeBuider{
+		LimitRange: corev1.LimitRange{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      name,
+				Namespace: namespace,
+			},
+			Spec: corev1.LimitRangeSpec{
+				Limits: []corev1.LimitRangeItem{
+					{
+						Type: corev1.LimitTypeContainer,
+						Min: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("2"),
+							corev1.ResourceMemory: resource.MustParse("200M"),
+						},
+						DefaultRequest: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("3"),
+							corev1.ResourceMemory: resource.MustParse("300M"),
+						},
+						Default: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("4"),
+							corev1.ResourceMemory: resource.MustParse("400M"),
+						},
+						Max: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("5"),
+							corev1.ResourceMemory: resource.MustParse("500M"),
+						},
```

  Remove these lines. It's too many constants hidden in just `MakeLimitRange`.
  
  When we put some constants in other wrappers, they correspond to defaults or things that almost every test should need.

- **pkg/util/testing/wrappers.go:568** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T18:23:05Z
```diff
@@ -499,3 +511,66 @@ func (rc *RuntimeClassWrapper) PodOverhead(resources corev1.ResourceList) *Runti
 func (rc *RuntimeClassWrapper) Obj() *nodev1.RuntimeClass {
 	return &rc.RuntimeClass
 }
+
+type limitRangeBuider struct{ corev1.LimitRange }
+
+func MakeLimitRange(name, namespace string) *limitRangeBuider {
+	return &limitRangeBuider{
+		LimitRange: corev1.LimitRange{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      name,
+				Namespace: namespace,
+			},
+			Spec: corev1.LimitRangeSpec{
+				Limits: []corev1.LimitRangeItem{
+					{
+						Type: corev1.LimitTypeContainer,
+						Min: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("2"),
+							corev1.ResourceMemory: resource.MustParse("200M"),
+						},
+						DefaultRequest: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("3"),
+							corev1.ResourceMemory: resource.MustParse("300M"),
+						},
+						Default: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("4"),
+							corev1.ResourceMemory: resource.MustParse("400M"),
+						},
+						Max: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("5"),
+							corev1.ResourceMemory: resource.MustParse("500M"),
+						},
+					},
+				},
+			},
+		},
+	}
+}
+
+func (lr *limitRangeBuider) WithType(t corev1.LimitType) *limitRangeBuider {
+	lr.Spec.Limits[0].Type = t
+	return lr
+}
+
+func (lr *limitRangeBuider) WithValue(member string, t corev1.ResourceName, q string) *limitRangeBuider {
+	target := lr.Spec.Limits[0].Max
+	switch member {
+	case "Min":
+		target = lr.Spec.Limits[0].Min
+	case "DefaultRequest":
+		target = lr.Spec.Limits[0].DefaultRequest
+	case "Default":
+		target = lr.Spec.Limits[0].Default
+	case "Max":
+	//nothing
+	default:
+		//just let it update the Max
```

  panic

- **test/integration/controller/core/workload_controller_test.go:274** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T18:25:52Z
```diff
@@ -249,4 +252,144 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			}, ignoreCqCondition))
 		})
 	})
+
+	ginkgo.When("When LimitRanges are defined", func() {
+		ginkgo.BeforeEach(func() {
+			limitRange := testing.MakeLimitRange("limits", ns.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, limitRange)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(corev1.ResourceCPU, "5", "5").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		})
+
+		ginkgo.It("Should use the range defined requests, if not provided", func() {
```

  ```suggestion
  		ginkgo.It("Should use the range defined default requests, if provided", func() {
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:05:23Z

- **pkg/controller/core/workload_controller.go:438** by [@trasc](https://github.com/trasc) — 2023-03-17T21:05:23Z
```diff
@@ -425,3 +433,50 @@ func handlePodOverhead(log logr.Logger, wl *kueue.Workload, c client.Client) {
 		}
 	}
 }
+
+func (r *WorkloadReconciler) handlePodLimitRange(log logr.Logger, wl *kueue.Workload) {
+	ctx := context.Background()
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:05:39Z

- **pkg/controller/core/workload_controller.go:441** by [@trasc](https://github.com/trasc) — 2023-03-17T21:05:39Z
```diff
@@ -425,3 +433,50 @@ func handlePodOverhead(log logr.Logger, wl *kueue.Workload, c client.Client) {
 		}
 	}
 }
+
+func (r *WorkloadReconciler) handlePodLimitRange(log logr.Logger, wl *kueue.Workload) {
+	ctx := context.Background()
+	// get the list of limit ranges
+	var list corev1.LimitRangeList
+	if err := r.client.List(ctx, &list, &client.ListOptions{Namespace: wl.Namespace}); err != nil {
```

  added

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:05:51Z

- **pkg/util/resource/resource_test.go:47** by [@trasc](https://github.com/trasc) — 2023-03-17T21:05:51Z
```diff
@@ -0,0 +1,198 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resource
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	apiresource "k8s.io/apimachinery/pkg/api/resource"
+)
+
+func TestMerge(t *testing.T) {
+	rl_500mcpu_2GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("500m"),
+		corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+	}
+	rl_1cpu := corev1.ResourceList{
+		corev1.ResourceCPU: apiresource.MustParse("1"),
+	}
+	rl_1cpu_1GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("1"),
+		corev1.ResourceMemory: apiresource.MustParse("1Gi"),
+	}
+
+	type oper_result struct {
+		oper   func(a, b corev1.ResourceList) corev1.ResourceList
+		result corev1.ResourceList
+	}
+	cases := map[string]struct {
+		a        corev1.ResourceList
+		b        corev1.ResourceList
+		expected map[string]oper_result
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:06:16Z

- **pkg/util/resource/resource_test.go:83** by [@trasc](https://github.com/trasc) — 2023-03-17T21:06:16Z
```diff
@@ -0,0 +1,198 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resource
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	apiresource "k8s.io/apimachinery/pkg/api/resource"
+)
+
+func TestMerge(t *testing.T) {
+	rl_500mcpu_2GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("500m"),
+		corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+	}
+	rl_1cpu := corev1.ResourceList{
+		corev1.ResourceCPU: apiresource.MustParse("1"),
+	}
+	rl_1cpu_1GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("1"),
+		corev1.ResourceMemory: apiresource.MustParse("1Gi"),
+	}
+
+	type oper_result struct {
+		oper   func(a, b corev1.ResourceList) corev1.ResourceList
+		result corev1.ResourceList
+	}
+	cases := map[string]struct {
+		a        corev1.ResourceList
+		b        corev1.ResourceList
+		expected map[string]oper_result
+	}{
+		"asymetric": {
+			a: rl_1cpu,
+			b: rl_500mcpu_2GiMem,
+			expected: map[string]oper_result{
+				"merge": {
+					oper: MergeResourceListKeepFirst,
+					result: corev1.ResourceList{
+						corev1.ResourceCPU:    apiresource.MustParse("1"),
+						corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+					},
+				},
+				"min": {
+					oper: MergeResourceListKeepMin,
+					result: corev1.ResourceList{
+						corev1.ResourceCPU:    apiresource.MustParse("500m"),
+						corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+					},
+				},
+				"max": {
+					oper: MergeResourceListKeepMax,
+					result: corev1.ResourceList{
+						corev1.ResourceCPU:    apiresource.MustParse("1"),
+						corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+					},
+				},
+				"sum": {
+					oper: MergeResourceListKeepSum,
+					result: corev1.ResourceList{
+						corev1.ResourceCPU:    apiresource.MustParse("1500m"),
+						corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+					},
+				},
+			},
+		},
+		"symetric": {
```

  done, thanks

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:06:35Z

- **pkg/util/testing/wrappers.go:515** by [@trasc](https://github.com/trasc) — 2023-03-17T21:06:34Z
```diff
@@ -499,3 +511,66 @@ func (rc *RuntimeClassWrapper) PodOverhead(resources corev1.ResourceList) *Runti
 func (rc *RuntimeClassWrapper) Obj() *nodev1.RuntimeClass {
 	return &rc.RuntimeClass
 }
+
+type limitRangeBuider struct{ corev1.LimitRange }
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:06:45Z

- **pkg/util/resource/resource_test.go:49** by [@trasc](https://github.com/trasc) — 2023-03-17T21:06:44Z
```diff
@@ -0,0 +1,198 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resource
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	apiresource "k8s.io/apimachinery/pkg/api/resource"
+)
+
+func TestMerge(t *testing.T) {
+	rl_500mcpu_2GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("500m"),
+		corev1.ResourceMemory: apiresource.MustParse("2Gi"),
+	}
+	rl_1cpu := corev1.ResourceList{
+		corev1.ResourceCPU: apiresource.MustParse("1"),
+	}
+	rl_1cpu_1GiMem := corev1.ResourceList{
+		corev1.ResourceCPU:    apiresource.MustParse("1"),
+		corev1.ResourceMemory: apiresource.MustParse("1Gi"),
+	}
+
+	type oper_result struct {
+		oper   func(a, b corev1.ResourceList) corev1.ResourceList
+		result corev1.ResourceList
+	}
+	cases := map[string]struct {
+		a        corev1.ResourceList
+		b        corev1.ResourceList
+		expected map[string]oper_result
+	}{
+		"asymetric": {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:06:59Z

- **pkg/util/testing/wrappers.go:543** by [@trasc](https://github.com/trasc) — 2023-03-17T21:06:59Z
```diff
@@ -499,3 +511,66 @@ func (rc *RuntimeClassWrapper) PodOverhead(resources corev1.ResourceList) *Runti
 func (rc *RuntimeClassWrapper) Obj() *nodev1.RuntimeClass {
 	return &rc.RuntimeClass
 }
+
+type limitRangeBuider struct{ corev1.LimitRange }
+
+func MakeLimitRange(name, namespace string) *limitRangeBuider {
+	return &limitRangeBuider{
+		LimitRange: corev1.LimitRange{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      name,
+				Namespace: namespace,
+			},
+			Spec: corev1.LimitRangeSpec{
+				Limits: []corev1.LimitRangeItem{
+					{
+						Type: corev1.LimitTypeContainer,
+						Min: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("2"),
+							corev1.ResourceMemory: resource.MustParse("200M"),
+						},
+						DefaultRequest: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("3"),
+							corev1.ResourceMemory: resource.MustParse("300M"),
+						},
+						Default: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("4"),
+							corev1.ResourceMemory: resource.MustParse("400M"),
+						},
+						Max: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("5"),
+							corev1.ResourceMemory: resource.MustParse("500M"),
+						},
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:08:29Z

- **test/integration/controller/core/workload_controller_test.go:274** by [@trasc](https://github.com/trasc) — 2023-03-17T21:08:28Z
```diff
@@ -249,4 +252,144 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			}, ignoreCqCondition))
 		})
 	})
+
+	ginkgo.When("When LimitRanges are defined", func() {
+		ginkgo.BeforeEach(func() {
+			limitRange := testing.MakeLimitRange("limits", ns.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, limitRange)).To(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("clusterqueue").
+				ResourceGroup(*testing.MakeFlavorQuotas(flavorOnDemand).
+					Resource(corev1.ResourceCPU, "5", "5").Obj()).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(util.DeleteRuntimeClass(ctx, k8sClient, runtimeClass)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+		})
+
+		ginkgo.It("Should use the range defined requests, if not provided", func() {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:08:39Z

- **pkg/util/testing/wrappers.go:568** by [@trasc](https://github.com/trasc) — 2023-03-17T21:08:38Z
```diff
@@ -499,3 +511,66 @@ func (rc *RuntimeClassWrapper) PodOverhead(resources corev1.ResourceList) *Runti
 func (rc *RuntimeClassWrapper) Obj() *nodev1.RuntimeClass {
 	return &rc.RuntimeClass
 }
+
+type limitRangeBuider struct{ corev1.LimitRange }
+
+func MakeLimitRange(name, namespace string) *limitRangeBuider {
+	return &limitRangeBuider{
+		LimitRange: corev1.LimitRange{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      name,
+				Namespace: namespace,
+			},
+			Spec: corev1.LimitRangeSpec{
+				Limits: []corev1.LimitRangeItem{
+					{
+						Type: corev1.LimitTypeContainer,
+						Min: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("2"),
+							corev1.ResourceMemory: resource.MustParse("200M"),
+						},
+						DefaultRequest: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("3"),
+							corev1.ResourceMemory: resource.MustParse("300M"),
+						},
+						Default: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("4"),
+							corev1.ResourceMemory: resource.MustParse("400M"),
+						},
+						Max: corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("5"),
+							corev1.ResourceMemory: resource.MustParse("500M"),
+						},
+					},
+				},
+			},
+		},
+	}
+}
+
+func (lr *limitRangeBuider) WithType(t corev1.LimitType) *limitRangeBuider {
+	lr.Spec.Limits[0].Type = t
+	return lr
+}
+
+func (lr *limitRangeBuider) WithValue(member string, t corev1.ResourceName, q string) *limitRangeBuider {
+	target := lr.Spec.Limits[0].Max
+	switch member {
+	case "Min":
+		target = lr.Spec.Limits[0].Min
+	case "DefaultRequest":
+		target = lr.Spec.Limits[0].DefaultRequest
+	case "Default":
+		target = lr.Spec.Limits[0].Default
+	case "Max":
+	//nothing
+	default:
+		//just let it update the Max
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T21:11:30Z

Comments that can be done in follow ups

/lgtm
/approve

/label tide/merge-method-squash

- **pkg/controller/core/indexer/indexer.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T21:09:22Z
```diff
@@ -58,6 +60,20 @@ func IndexWorkloadClusterQueue(obj client.Object) []string {
 	return []string{string(wl.Status.Admission.ClusterQueue)}
 }
 
+func IndexLimitRangeHasContainerType(obj client.Object) []string {
```

  We might need to also check type Pod when we do validation (for min/max).
  
  So an alternative implementation would be `IndexLimitRangeByType`. Then you can return all the types that the LimitRange has.
  
  But we can leave this for later.

- **pkg/util/resource/resource_test.go:24** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T21:10:47Z
```diff
@@ -0,0 +1,198 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package resource
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	apiresource "k8s.io/apimachinery/pkg/api/resource"
```

  we avoid aliases when there are no conflicts.

- **pkg/util/testing/wrappers.go:532** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T21:11:13Z
```diff
@@ -499,3 +511,55 @@ func (rc *RuntimeClassWrapper) PodOverhead(resources corev1.ResourceList) *Runti
 func (rc *RuntimeClassWrapper) Obj() *nodev1.RuntimeClass {
 	return &rc.RuntimeClass
 }
+
+type LimitRangeWrapper struct{ corev1.LimitRange }
+
+func MakeLimitRange(name, namespace string) *LimitRangeWrapper {
+	return &LimitRangeWrapper{
+		LimitRange: corev1.LimitRange{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      name,
+				Namespace: namespace,
+			},
+			Spec: corev1.LimitRangeSpec{
+				Limits: []corev1.LimitRangeItem{
+					{
+						Type:                 corev1.LimitTypeContainer,
+						Max:                  corev1.ResourceList{},
+						Min:                  corev1.ResourceList{},
+						Default:              corev1.ResourceList{},
+						DefaultRequest:       corev1.ResourceList{},
+						MaxLimitRequestRatio: corev1.ResourceList{},
```

  you could remove these lines.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-17T21:17:38Z

- **pkg/util/testing/wrappers.go:532** by [@trasc](https://github.com/trasc) — 2023-03-17T21:17:37Z
```diff
@@ -499,3 +511,55 @@ func (rc *RuntimeClassWrapper) PodOverhead(resources corev1.ResourceList) *Runti
 func (rc *RuntimeClassWrapper) Obj() *nodev1.RuntimeClass {
 	return &rc.RuntimeClass
 }
+
+type LimitRangeWrapper struct{ corev1.LimitRange }
+
+func MakeLimitRange(name, namespace string) *LimitRangeWrapper {
+	return &LimitRangeWrapper{
+		LimitRange: corev1.LimitRange{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      name,
+				Namespace: namespace,
+			},
+			Spec: corev1.LimitRangeSpec{
+				Limits: []corev1.LimitRangeItem{
+					{
+						Type:                 corev1.LimitTypeContainer,
+						Max:                  corev1.ResourceList{},
+						Min:                  corev1.ResourceList{},
+						Default:              corev1.ResourceList{},
+						DefaultRequest:       corev1.ResourceList{},
+						MaxLimitRequestRatio: corev1.ResourceList{},
```

  yes, but then you need to check / alocate the empty map for every value set

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T21:18:27Z

- **pkg/util/testing/wrappers.go:532** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T21:18:27Z
```diff
@@ -499,3 +511,55 @@ func (rc *RuntimeClassWrapper) PodOverhead(resources corev1.ResourceList) *Runti
 func (rc *RuntimeClassWrapper) Obj() *nodev1.RuntimeClass {
 	return &rc.RuntimeClass
 }
+
+type LimitRangeWrapper struct{ corev1.LimitRange }
+
+func MakeLimitRange(name, namespace string) *LimitRangeWrapper {
+	return &LimitRangeWrapper{
+		LimitRange: corev1.LimitRange{
+			ObjectMeta: metav1.ObjectMeta{
+				Name:      name,
+				Namespace: namespace,
+			},
+			Spec: corev1.LimitRangeSpec{
+				Limits: []corev1.LimitRangeItem{
+					{
+						Type:                 corev1.LimitTypeContainer,
+						Max:                  corev1.ResourceList{},
+						Min:                  corev1.ResourceList{},
+						Default:              corev1.ResourceList{},
+						DefaultRequest:       corev1.ResourceList{},
+						MaxLimitRequestRatio: corev1.ResourceList{},
```

  ahhh, sorry, I forgot these were maps :)
