# PR #604: Migrate controllers to v1beta1

**Summary**: Migrate controllers to v1beta1

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/604

**Last updated**: 2023-03-08T16:01:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `beta1-controllers` → `main`
- **Created**: 2023-03-03T17:05:43Z
- **Updated**: 2023-03-08T16:01:14Z
- **Merged**: 2023-03-08T16:01:13Z
- **Closed**: 2023-03-08T16:01:13Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 81   **+13020 / -3399**

## Description

#### What type of PR is this?

/kind feature
/kind api-change

#### What this PR does / why we need it:

Uses the v1beta1 API throughout the code base and changes the storage version.
The code is functionally the same, but there are some differences:
- Use of `client.Status()` when admitting or evicting workloads.
- The concept of "codependent resources" disappears in favor of the API's resourceGroups.
- The queue name in a Job can be specified as a label or annotation.

Makes use of APIs introduced in #532 (contained in the first two commits).

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #23

#### Special notes for your reviewer:

I heavily changed the testing wrappers in a attempt to make the test cases more slim.
The wrappers also add `coveredResources` automatically and panic if you define flavors with different resources.

To be done if follow ups:
- #603
- Documentation updates.
- Remove v1alpha2

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-03T17:05:48Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cfaf110d56eac916d93e7e93a466f26a26ac9957 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6408adbbb8915b00089939d4 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-03T17:05:49Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/604#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-03T17:06:06Z

/hold
for all the eyes that want to have a look :)

cc @ahg-g

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-03T20:53:57Z

/test pull-kueue-test-integration-main

### Comment by [@ahg-g](https://github.com/ahg-g) — 2023-03-08T06:49:44Z

should we delete v1alpha2? it is orphaned now, right?

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T13:35:39Z

> I think this could have been made easier to review if we introduced the changes to v1beta1 over different PRs (for example, first add v1beta1 as an exact copy of v1alpha2, then do the Workload Admission move, then the ResourceFlavor changes, then the ClusterQueue etc.), the wrappers refactoring could have also been introduced in a separate PR.

Yeah, I was too far ahead with changes once I realized the PR was too big :(

The wrapping changes was an attempt to make it easier for me to re-write the unit tests.

> As for the logic itself, I wonder if we actually needed to make all those changes at once? maybe by copying the external type to the existing internal types we could have avoided doing all those changes in one go?

There are no internal types in CRDs. Only a "storage version", which is v1beta1 now.

> should we delete v1alpha2? it is orphaned now, right?

Next PR

### Comment by [@ahg-g](https://github.com/ahg-g) — 2023-03-08T15:02:49Z

> There are no internal types in CRDs. Only a "storage version", which is v1beta1 now.

we have internal types in the code itself, not sure how much we use the api types directly though in the core logic.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2023-03-08T15:03:01Z

/hold
/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T15:45:13Z

/hold cancel

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T15:45:32Z

/hold
for squash

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T15:46:14Z

squashed in two commits
/hold cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-08T15:49:05Z

/lgtm

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-08T15:50:18Z

/lgtm

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2023-03-08T06:44:49Z

Unbelievable effort, but hard to review :) 

I think this could have been made easier to review if we introduced the changes to v1beta1 over different PRs (for example, first add v1beta1 as an exact copy of v1alpha2, then do the Workload Admission move, then the ResourceFlavor changes, then the ClusterQueue etc.), the wrappers refactoring could have also been introduced in a separate PR.

As for the logic itself, I wonder if we actually needed to make all those changes at once? maybe by copying the external type to the existing internal types we could have avoided doing all those changes in one go?

Regardless, since this doesn't intend to introduce new behavior, our only hope is that we have enough test coverage for validation :)

- **pkg/cache/cache.go:399** by [@ahg-g](https://github.com/ahg-g) — 2023-03-08T05:01:51Z
```diff
@@ -381,22 +387,20 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 }
 
 func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
-	updateUsage(wi, c.UsedResources, m)
+	updateUsage(wi, c.Usage, m)
 	qKey := workload.QueueKey(wi.Obj)
 	if _, ok := c.admittedWorkloadsPerQueue[qKey]; ok {
 		c.admittedWorkloadsPerQueue[qKey] += int(m)
 	}
 }
 
-func updateUsage(wi *workload.Info, usedResources ResourceQuantities, m int64) {
+func updateUsage(wi *workload.Info, cqUsage FlavorResourceQuantities, m int64) {
```

  make this a member function of ClusterQueue since it updates the Usage of a CQ?

- **pkg/cache/cache.go:403** by [@ahg-g](https://github.com/ahg-g) — 2023-03-08T05:08:33Z
```diff
@@ -381,22 +387,20 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 }
 
 func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
-	updateUsage(wi, c.UsedResources, m)
+	updateUsage(wi, c.Usage, m)
 	qKey := workload.QueueKey(wi.Obj)
 	if _, ok := c.admittedWorkloadsPerQueue[qKey]; ok {
 		c.admittedWorkloadsPerQueue[qKey] += int(m)
 	}
 }
 
-func updateUsage(wi *workload.Info, usedResources ResourceQuantities, m int64) {
+func updateUsage(wi *workload.Info, cqUsage FlavorResourceQuantities, m int64) {
 	for _, ps := range wi.TotalRequests {
 		for wlRes, wlResFlv := range ps.Flavors {
 			v, wlResExist := ps.Requests[wlRes]
-			cqResFlv, cqResExist := usedResources[wlRes]
-			if cqResExist && wlResExist {
-				if _, cqFlvExist := cqResFlv[wlResFlv]; cqFlvExist {
-					cqResFlv[wlResFlv] += v * m
-				}
+			cqFlv, cqFlvExist := cqUsage[wlResFlv]
+			if cqFlvExist && wlResExist {
+				cqFlv[wlRes] += v * m
```

  a similar check to the previous one is still applicable: 
  
  `if _, exist := cqFlv[wlRes]; exist {`

- **pkg/controller/workload/job/job_controller.go:629** by [@ahg-g](https://github.com/ahg-g) — 2023-03-08T06:21:35Z
```diff
@@ -617,14 +617,18 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 	// nodeSelector may change, hence we are not checking for
 	// equality of the whole job.Spec.Template.Spec.
 	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+		wl.Spec.PodSets[0].Template.Spec.InitContainers) {
 		return false
 	}
 	return equality.Semantic.DeepEqual(job.Spec.Template.Spec.Containers,
-		wl.Spec.PodSets[0].Spec.Containers)
+		wl.Spec.PodSets[0].Template.Spec.Containers)
 }
 
 func queueName(job *batchv1.Job) string {
+	v, ok := job.Labels[constants.QueueLabel]
+	if ok {
```

  ```suggestion
  	if v, ok := job.Labels[constants.QueueLabel]; ok {
  ```

- **test/integration/webhook/clusterqueue_test.go:14** by [@ahg-g](https://github.com/ahg-g) — 2023-03-08T06:35:09Z
```diff
@@ -11,7 +11,7 @@ See the License for the specific language governing permissions and
 limitations under the License.
 */
 
-package v1alpha2
+package webhook
```

  why did we move them?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-08T14:28:19Z

- **pkg/cache/cache.go:399** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T14:28:19Z
```diff
@@ -381,22 +387,20 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 }
 
 func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
-	updateUsage(wi, c.UsedResources, m)
+	updateUsage(wi, c.Usage, m)
 	qKey := workload.QueueKey(wi.Obj)
 	if _, ok := c.admittedWorkloadsPerQueue[qKey]; ok {
 		c.admittedWorkloadsPerQueue[qKey] += int(m)
 	}
 }
 
-func updateUsage(wi *workload.Info, usedResources ResourceQuantities, m int64) {
+func updateUsage(wi *workload.Info, cqUsage FlavorResourceQuantities, m int64) {
```

  No, because the snapshot also uses this function to update the usage of the cohort, not just the clusterQueue.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-08T14:38:14Z

- **pkg/cache/cache.go:403** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T14:34:52Z
```diff
@@ -381,22 +387,20 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 }
 
 func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
-	updateUsage(wi, c.UsedResources, m)
+	updateUsage(wi, c.Usage, m)
 	qKey := workload.QueueKey(wi.Obj)
 	if _, ok := c.admittedWorkloadsPerQueue[qKey]; ok {
 		c.admittedWorkloadsPerQueue[qKey] += int(m)
 	}
 }
 
-func updateUsage(wi *workload.Info, usedResources ResourceQuantities, m int64) {
+func updateUsage(wi *workload.Info, cqUsage FlavorResourceQuantities, m int64) {
 	for _, ps := range wi.TotalRequests {
 		for wlRes, wlResFlv := range ps.Flavors {
 			v, wlResExist := ps.Requests[wlRes]
-			cqResFlv, cqResExist := usedResources[wlRes]
-			if cqResExist && wlResExist {
-				if _, cqFlvExist := cqResFlv[wlResFlv]; cqFlvExist {
-					cqResFlv[wlResFlv] += v * m
-				}
+			cqFlv, cqFlvExist := cqUsage[wlResFlv]
+			if cqFlvExist && wlResExist {
+				cqFlv[wlRes] += v * m
```

  reverted

- **test/integration/webhook/clusterqueue_test.go:14** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T14:37:19Z
```diff
@@ -11,7 +11,7 @@ See the License for the specific language governing permissions and
 limitations under the License.
 */
 
-package v1alpha2
+package webhook
```

  Maybe when there are conversion webhooks it'll make sense to the tests to the version, but for now it doesn't make much sense.

- **pkg/controller/workload/job/job_controller.go:629** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T14:37:41Z
```diff
@@ -617,14 +617,18 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 	// nodeSelector may change, hence we are not checking for
 	// equality of the whole job.Spec.Template.Spec.
 	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+		wl.Spec.PodSets[0].Template.Spec.InitContainers) {
 		return false
 	}
 	return equality.Semantic.DeepEqual(job.Spec.Template.Spec.Containers,
-		wl.Spec.PodSets[0].Spec.Containers)
+		wl.Spec.PodSets[0].Template.Spec.Containers)
 }
 
 func queueName(job *batchv1.Job) string {
+	v, ok := job.Labels[constants.QueueLabel]
+	if ok {
```

  Done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-08T15:42:44Z

Thank you for the great work!

Maybe for a follow up, we must update the samples:

https://github.com/kubernetes-sigs/kueue/tree/main/config/samples

- **Makefile:213** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-08T01:05:11Z
```diff
@@ -210,7 +210,7 @@ clean-manifests = (cd config/components/manager && $(KUSTOMIZE) edit set image c
 
 .PHONY: install
 install: manifests kustomize ## Install CRDs into the K8s cluster specified in ~/.kube/config.
-	$(KUSTOMIZE) build config/components/crd | kubectl apply -f -
+	$(KUSTOMIZE) build config/components/crd | kubectl apply --server-side -f -
```

  Maybe for a follow-up, we need to update docs as well.
  
  https://github.com/kubernetes-sigs/kueue/blob/cf76bfe02ae58e5005403821689592c6b967ce5a/site/content/en/docs/installation/_index.md#install-the-latest-development-version

- **pkg/controller/workload/job/job_controller.go:631** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-08T15:18:04Z
```diff
@@ -617,14 +617,17 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 	// nodeSelector may change, hence we are not checking for
 	// equality of the whole job.Spec.Template.Spec.
 	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+		wl.Spec.PodSets[0].Template.Spec.InitContainers) {
 		return false
 	}
 	return equality.Semantic.DeepEqual(job.Spec.Template.Spec.Containers,
-		wl.Spec.PodSets[0].Spec.Containers)
+		wl.Spec.PodSets[0].Template.Spec.Containers)
 }
 
 func queueName(job *batchv1.Job) string {
+	if v, ok := job.Labels[constants.QueueLabel]; ok {
+		return v
+	}
 	return job.Annotations[constants.QueueAnnotation]
```

  Why take the queue name from annotations?  For backward compatible?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-08T15:45:00Z

- **Makefile:213** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T15:45:00Z
```diff
@@ -210,7 +210,7 @@ clean-manifests = (cd config/components/manager && $(KUSTOMIZE) edit set image c
 
 .PHONY: install
 install: manifests kustomize ## Install CRDs into the K8s cluster specified in ~/.kube/config.
-	$(KUSTOMIZE) build config/components/crd | kubectl apply -f -
+	$(KUSTOMIZE) build config/components/crd | kubectl apply --server-side -f -
```

  Yes, that's next step :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-08T15:45:06Z

- **pkg/controller/workload/job/job_controller.go:631** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T15:45:06Z
```diff
@@ -617,14 +617,17 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 	// nodeSelector may change, hence we are not checking for
 	// equality of the whole job.Spec.Template.Spec.
 	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+		wl.Spec.PodSets[0].Template.Spec.InitContainers) {
 		return false
 	}
 	return equality.Semantic.DeepEqual(job.Spec.Template.Spec.Containers,
-		wl.Spec.PodSets[0].Spec.Containers)
+		wl.Spec.PodSets[0].Template.Spec.Containers)
 }
 
 func queueName(job *batchv1.Job) string {
+	if v, ok := job.Labels[constants.QueueLabel]; ok {
+		return v
+	}
 	return job.Annotations[constants.QueueAnnotation]
```

  Yes

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-08T15:46:29Z

- **pkg/controller/workload/job/job_controller.go:631** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-08T15:46:28Z
```diff
@@ -617,14 +617,17 @@ func jobAndWorkloadEqual(job *batchv1.Job, wl *kueue.Workload) bool {
 	// nodeSelector may change, hence we are not checking for
 	// equality of the whole job.Spec.Template.Spec.
 	if !equality.Semantic.DeepEqual(job.Spec.Template.Spec.InitContainers,
-		wl.Spec.PodSets[0].Spec.InitContainers) {
+		wl.Spec.PodSets[0].Template.Spec.InitContainers) {
 		return false
 	}
 	return equality.Semantic.DeepEqual(job.Spec.Template.Spec.Containers,
-		wl.Spec.PodSets[0].Spec.Containers)
+		wl.Spec.PodSets[0].Template.Spec.Containers)
 }
 
 func queueName(job *batchv1.Job) string {
+	if v, ok := job.Labels[constants.QueueLabel]; ok {
+		return v
+	}
 	return job.Annotations[constants.QueueAnnotation]
```

  Great :)
