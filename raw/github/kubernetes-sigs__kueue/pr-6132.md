# PR #6132: Flavor Fungibility Prefers Fit over NoBorrow

**Summary**: Flavor Fungibility Prefers Fit over NoBorrow

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6132

**Last updated**: 2025-07-25T06:19:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `fungibility_order` → `main`
- **Created**: 2025-07-22T09:20:46Z
- **Updated**: 2025-07-25T06:19:24Z
- **Merged**: 2025-07-24T14:44:28Z
- **Closed**: 2025-07-24T14:44:28Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@gabesaba](https://github.com/gabesaba)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 5   **+399 / -51**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Some of the users would like to define a different preference when selecting a flavor in flavor fungibility. Currently we always try to minimize preemptions. Sometimes it means that an incoming high-priority workload would choose a flavor in which it has to borrow (because its nominal quota is in use or borrowed). A workload that is borrowing is at risk of being preempted which may be undesirable for high-priority workloads so it might be better for it to preempt and be scheduled in a flavor that does not require borrowing.

This PR adds a feature gate `FlavorFungibilityImplicitPreferenceDefault`. When the feature gate is enabled, it infers the preference (borrowing or preemption) based on the FavorFungibilityStrategy. More precisely in two cases:
1. `WhenCanPreempt = Preempt` and `WhenCanBorrow = TryNextFlavor`
2.  `WhenCanPreempt = TryNextFlavor` and `WhenCanBorrow = TryNextFlavor`

flavor assigner will prioritize flavors that require preemption over ones that require borrowing. In all other cases, borrowing will be prioritized.

KEP changes will be added in a separate PR.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5424

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Flavor Fungibility: Introduces a new mode which allows to prefer preemption over borrowing when choosing a flavor. 
In this mode the preference is decided based on FavorFungibilityStrategy. This behavior is behind the 
FlavorFungibilityImplicitPreferenceDefault Alpha feature gate (disabled by default).
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-22T09:20:48Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-22T09:21:57Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1ca30567a2d9cba18523223013a52d80c500916c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6882307a48ecf50008c9b6fb |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-22T09:35:01Z

@pajakd @gabesaba please prepare also a KEP update for this extension.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-22T10:15:58Z

/hold
I have some higher level concerns as it does not match my understanding of the solution from initial discussions. If we want this to be included still in 0.13 please prepare KEP update with the story and justification for the changes, in particular addressing:
- https://github.com/kubernetes-sigs/kueue/pull/6132/files#r2222027953
- https://github.com/kubernetes-sigs/kueue/pull/6132#discussion_r2221988502

### Comment by [@pajakd](https://github.com/pajakd) — 2025-07-22T11:11:57Z

> @pajakd @gabesaba please prepare also a KEP update for this extension.

Proposed KEP changes in #6133

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-23T15:34:32Z

/kind-remove api-change
New feature gates don't need to be marked as API change

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-23T15:39:29Z

> /hold
> I have some higher level concerns as it does not match my understanding of the solution from initial discussions. If we want this to be included still in 0.13 please prepare KEP update with the story and justification for the changes, in particular addressing:

Dropping API changes and borrowingDistance from the PR makes my concerns addressed. I think both may some merit, but it is very late in the release process so I prefer this change behind a feature gate, and scoped to the issue we have. 

I'm open to introduce API (next releases) and support borrowingDistance (potentially even this release in separate PR with dedicated release note and tests).
/hold cancel

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-23T15:55:57Z

LGTM overall, left some nit-ish comments like to commonize the feature gate check, and making sure the new core is covered with tests.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-24T09:14:08Z

@pajakd please mark all addressed comments as resolved and update the release note

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-07-24T13:36:09Z

/lgtm

thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-24T13:36:17Z

LGTM label has been added.  <details>Git tree hash: a68c4aa2a68c2fd06ffe2058e2b15a0a81d171ad</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-24T14:12:08Z

/remove-kind api-change

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-24T14:20:03Z

/approve
Please follow up on the two non-blocking comments:
- https://github.com/kubernetes-sigs/kueue/pull/6132#discussion_r2228362196
- https://github.com/kubernetes-sigs/kueue/pull/6132#discussion_r2228643843

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-24T14:20:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6132#issuecomment-3113664737" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6132#" title="Author self-approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T09:36:53Z

- **apis/kueue/v1beta1/clusterqueue_types.go:392** by [@mimowo](https://github.com/mimowo) — 2025-07-22T09:36:53Z
```diff
@@ -385,6 +385,13 @@ const (
 	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+	AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+	AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  Probably just a question to better understand the subtle issue: what is the reason for negative "Avoid" rather than "Prefer"?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T09:52:04Z

- **apis/kueue/v1beta1/clusterqueue_types.go:392** by [@pajakd](https://github.com/pajakd) — 2025-07-22T09:52:04Z
```diff
@@ -385,6 +385,13 @@ const (
 	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+	AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+	AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  We never prefer anything here. Whenever we find a flavor that does not need either preemption or borrowing, we immediately return it as perfect fit. 
  
  I would like to make sure that it is clear that we will not prefer to preempt but rather prioritize to avoid borrowing. Don't know if there is better wording for this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T10:04:28Z

- **apis/kueue/v1beta1/clusterqueue_types.go:392** by [@mimowo](https://github.com/mimowo) — 2025-07-22T10:04:28Z
```diff
@@ -385,6 +385,13 @@ const (
 	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+	AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+	AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  I'm ok with avoidance, but let me ask more , because I remember some earlier discussions on this problem which assumed preference rather than avoidance.
  
   Is there any reason to change the plans? Are both approaches satisfying the business requirements, or only avoidance?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T10:05:47Z

- **apis/kueue/v1beta1/clusterqueue_types.go:392** by [@mimowo](https://github.com/mimowo) — 2025-07-22T10:05:47Z
```diff
@@ -385,6 +385,13 @@ const (
 	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+	AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+	AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  Also, in the KEP update please add a new Story, so that we can think of the API which best fits the business use case. Otherwise it is difficult to compare alternatives

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-07-22T10:09:51Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:339** by [@gabesaba](https://github.com/gabesaba) — 2025-07-22T09:50:06Z
```diff
@@ -333,7 +338,25 @@ const (
 	fit
 )
 
-func fromPreemptionPossibility(preemptionPossibility preemptioncommon.PreemptionPossibility) granularMode {
+// isPreferred returns true if mode a is better than b according to the selected strategy
+func isPreferred(a, b granularMode, strategy kueue.FlavorSelectionStrategy) bool {
+	if a.preemptionMode == noFit {
+		return false
+	}
```

  Should we have this? that way, we are sure that borrowing distance is well-defined (as we return 0, when no-fit)
  ```suggestion
  	}
          if b.preemptionMode == noFit {
  		return true
  	}
  ```

- **pkg/scheduler/flavorassigner/flavorassigner.go:645** by [@gabesaba](https://github.com/gabesaba) — 2025-07-22T09:52:20Z
```diff
@@ -619,7 +642,7 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 	}
 
 	var bestAssignment ResourceAssignment
-	bestAssignmentMode := noFit
+	bestAssignmentMode := granularMode{preemptionMode: noFit, borrowingDistance: 1}
```

  should distance be maxInt?

- **pkg/scheduler/flavorassigner/flavorassigner.go:836** by [@gabesaba](https://github.com/gabesaba) — 2025-07-22T10:05:09Z
```diff
@@ -812,24 +833,25 @@ func (a *FlavorAssigner) fitsResourceQuota(log logr.Logger, fr resources.FlavorR
 	if val > maxCapacity {
 		status.appendf("insufficient quota for %s in flavor %s, request > maximum capacity (%s > %s)",
 			fr.Resource, fr.Flavor, resources.ResourceQuantityString(fr.Resource, val), resources.ResourceQuantityString(fr.Resource, maxCapacity))
-		return noFit, 0, &status
+		return granularMode{preemptionMode: noFit, borrowingDistance: 0}, &status
```

  maxInt?

- **pkg/scheduler/preemption/preemption_oracle.go:53** by [@gabesaba](https://github.com/gabesaba) — 2025-07-22T10:07:03Z
```diff
@@ -46,13 +47,18 @@ func (p *PreemptionOracle) SimulatePreemption(log logr.Logger, cq *cache.Cluster
 		frsNeedPreemption: sets.New(fr),
 		workloadUsage:     workload.Usage{Quota: resources.FlavorResourceQuantities{fr: quantity}},
 	})
+	for _, candidate := range candidates {
+		revertRemoval := cq.SimulateUsageRemoval(candidate.WorkloadInfo.Usage())
+		defer revertRemoval()
+	}
```

  can we use SimulateWorklaodRemoval?

- **pkg/scheduler/preemption/preemption_oracle.go:62** by [@gabesaba](https://github.com/gabesaba) — 2025-07-22T10:07:43Z
```diff
@@ -46,13 +47,18 @@ func (p *PreemptionOracle) SimulatePreemption(log logr.Logger, cq *cache.Cluster
 		frsNeedPreemption: sets.New(fr),
 		workloadUsage:     workload.Usage{Quota: resources.FlavorResourceQuantities{fr: quantity}},
 	})
+	for _, candidate := range candidates {
+		revertRemoval := cq.SimulateUsageRemoval(candidate.WorkloadInfo.Usage())
+		defer revertRemoval()
+	}
+	borrowAfterPreemptions, _ := classical.FindHeightOfLowestSubtreeThatFits(cq, fr, quantity)
 	if len(candidates) == 0 {
-		return preemptioncommon.NoCandidates
+		return preemptioncommon.NoCandidates, borrowAfterPreemptions
 	}
```

  can this clause come before usage removal? on line 50?

- **pkg/webhooks/clusterqueue_webhook.go:245** by [@gabesaba](https://github.com/gabesaba) — 2025-07-22T10:09:11Z
```diff
@@ -224,3 +227,21 @@ func validateLendingLimit(lend, nominal resource.Quantity, config validationConf
 	}
 	return allErrs
 }
+
+// validateFlavorFungibility enforces that FlavorSelectionStrategy is consistent with WhenCanBorrow and WhenCanPreempt
+func validateFlavorFungibility(flavorFungibility *kueue.FlavorFungibility, fldPath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	if features.Enabled(features.FlavorFungibility) && flavorFungibility != nil {
+		if flavorFungibility.WhenCanBorrow == kueue.Borrow &&
+			flavorFungibility.WhenCanPreempt == kueue.TryNextFlavor &&
+			flavorFungibility.WhenCanPreemptAndBorrow == kueue.AvoidBorrowing {
+			allErrs = append(allErrs, field.Invalid(fldPath, flavorFungibility, flavorFungibilityErrorMsg))
+		}
+		if flavorFungibility.WhenCanBorrow == kueue.TryNextFlavor &&
+			flavorFungibility.WhenCanPreempt == kueue.Preempt &&
+			flavorFungibility.WhenCanPreemptAndBorrow == kueue.AvoidPreemption {
+			allErrs = append(allErrs, field.Invalid(fldPath, flavorFungibility, flavorFungibilityErrorMsg))
+		}
+	}
```

  how about when Preempt && Borrow? It doesn't make sense to be set here either.

- **apis/kueue/v1beta1/clusterqueue_types.go:423** by [@gabesaba](https://github.com/gabesaba) — 2025-07-22T10:09:47Z
```diff
@@ -409,6 +416,17 @@ type FlavorFungibility struct {
 	// +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
 	// +kubebuilder:default="TryNextFlavor"
 	WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt,omitempty"`
+	// whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+	// multiple options. If either borrowing or preemption is neccessary and multiple options
+	// are available, then this field defines the selection strategy. The possible values are:
+	//
+	// - `AvoidPreemption` (default): prefer to allocate in a flavor that does not preempt
```

  Should we specify that this is the default, only when both options are set to `TryNextFlavor`?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T10:12:55Z

- **apis/kueue/v1beta1/clusterqueue_types.go:392** by [@pajakd](https://github.com/pajakd) — 2025-07-22T10:12:55Z
```diff
@@ -385,6 +385,13 @@ const (
 	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+	AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+	AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  It is only naming -- both approaches satisfy the requirements. 
  
  But in my opinion 'avoid' reflects more precisely what we want to do. Everyone wants to have the most stable fit that does not require preemption or borrowing. I think that with `PreferPreemption` someone might get an impression that we would prefer (`Preempt`, `NoBorrow`) over (`Fit`, `NoBorrow`).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T10:14:14Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-22T10:14:14Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  Is this necessary for the MVP of the business in question? Minimizing the scope of changes to what is really needed will make it easier to approve.
  
  if needed, please clarify in the KEP update why.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-07-22T10:18:13Z

- **apis/kueue/v1beta1/clusterqueue_types.go:392** by [@gabesaba](https://github.com/gabesaba) — 2025-07-22T10:18:13Z
```diff
@@ -385,6 +385,13 @@ const (
 	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+	AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+	AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  I'm happy with either. If it is prefer, something like `PreferFit` or `PreferNoPreemption`, and `PreferNoBorrow`. At that point, though, avoid sounds more concise

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T10:23:06Z

- **apis/kueue/v1beta1/clusterqueue_types.go:392** by [@mimowo](https://github.com/mimowo) — 2025-07-22T10:23:06Z
```diff
@@ -385,6 +385,13 @@ const (
 	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+	AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+	AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  > I think that with PreferPreemption someone might get an impression that we would prefer (Preempt, NoBorrow) over (Fit, NoBorrow).
  
  I see, OTOH `AvoidPreemption` sounds more definitive than prefer. One may get impression (Preempt, NoBorrow), will not happen when `AvoidPreemption` is used, but IIUC it may happen if all flavors are (Preempt, NoBorrow).

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-07-22T10:29:43Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@gabesaba](https://github.com/gabesaba) — 2025-07-22T10:29:43Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  This is just an implementation detail. I think that it is better to group this information together, as we are passing around (PreemptionMode, BorrowDistance) everywhere

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T10:34:26Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-22T10:34:26Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  It seems like used to prioritize preference within the Borrow mode, rather than borrow vs preempt. So, I think this is a different enhancement.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T10:35:51Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-22T10:35:51Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  If my understanding is correct, I would suggest a separate PR for this, for ease of reviewing and separation of concerns. Also, we could consider cherrypicking that part, as no API changes are involved.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T11:20:34Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:645** by [@pajakd](https://github.com/pajakd) — 2025-07-22T11:20:33Z
```diff
@@ -619,7 +642,7 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 	}
 
 	var bestAssignment ResourceAssignment
-	bestAssignmentMode := noFit
+	bestAssignmentMode := granularMode{preemptionMode: noFit, borrowingDistance: 1}
```

  Good idea. Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T11:20:39Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:836** by [@pajakd](https://github.com/pajakd) — 2025-07-22T11:20:39Z
```diff
@@ -812,24 +833,25 @@ func (a *FlavorAssigner) fitsResourceQuota(log logr.Logger, fr resources.FlavorR
 	if val > maxCapacity {
 		status.appendf("insufficient quota for %s in flavor %s, request > maximum capacity (%s > %s)",
 			fr.Resource, fr.Flavor, resources.ResourceQuantityString(fr.Resource, val), resources.ResourceQuantityString(fr.Resource, maxCapacity))
-		return noFit, 0, &status
+		return granularMode{preemptionMode: noFit, borrowingDistance: 0}, &status
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T11:41:23Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@pajakd](https://github.com/pajakd) — 2025-07-22T11:41:23Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  Currently we have a single "dimension" that we optimize when choosing a flavor (`preemption`). 
  
  With this change we want to allow the users to prioritize optimization along a different dimension (`borrowing`).
  
  We need a struct with both values because we have to remember what is the mode of currently best flavor.
  
  So, I believe this is necessary for this enhancement and is useless outside of this enhancement.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T11:56:27Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-22T11:56:27Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  I might be missing something obvious, I was assuming:
  ```yaml
  flavorFungability:
    whenCanBorrow: TryNextFlavor
  ```
  Now, we identify 3 flavors with (Borrow, NoFit), but with different distances, say (Borrow, NoFit, 1), (Borrow, NoFit, 3), (Borrow, NoFit, 5), so we choose the the first flavor, because it means borrowing over the shortest distance (1).
  
  Let me know if/what I'm missing here.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T12:08:44Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@pajakd](https://github.com/pajakd) — 2025-07-22T12:08:43Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  In general this is the desired behavior (although `NoFit` is something that we never choose so perhaps lets think of `(Borrow, Preempt, 1), (Borrow, Preempt, 3), (Borrow, Preempt, 5))`.
  
  But now lets say we extend the sequence with one more element:
  `(Borrow, Preempt, 1), (Borrow, Preempt, 3), (Borrow, Preempt, 5), (Borrow, Reclaim, 1)`
  then we want to update to the last one. 
  
  So, for this to work we have to remember what is up to now the best preemption mode and borrowing mode. And this struct is simply an internal representation of the score some flavor gets and this score must be a pair.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T12:28:26Z

- **pkg/scheduler/preemption/preemption_oracle.go:53** by [@pajakd](https://github.com/pajakd) — 2025-07-22T12:28:26Z
```diff
@@ -46,13 +47,18 @@ func (p *PreemptionOracle) SimulatePreemption(log logr.Logger, cq *cache.Cluster
 		frsNeedPreemption: sets.New(fr),
 		workloadUsage:     workload.Usage{Quota: resources.FlavorResourceQuantities{fr: quantity}},
 	})
+	for _, candidate := range candidates {
+		revertRemoval := cq.SimulateUsageRemoval(candidate.WorkloadInfo.Usage())
+		defer revertRemoval()
+	}
```

  Yes. But we have to iterate over the candidates anyway because they are of type `Target` and we need `WorkloadInfo`. With `SimulateWorkloadRemoval` we don't have to get the `WorkloadInfo.Usage()` so it should be better.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T12:30:17Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-22T12:30:17Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  > In general this is the desired behavior (although NoFit
  
  Sorry, I meant `(Borrow, Fit, 1), (Borrow, Fit, 3), (Borrow, Fit, 5)`

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T12:31:10Z

- **pkg/scheduler/preemption/preemption_oracle.go:62** by [@pajakd](https://github.com/pajakd) — 2025-07-22T12:31:10Z
```diff
@@ -46,13 +47,18 @@ func (p *PreemptionOracle) SimulatePreemption(log logr.Logger, cq *cache.Cluster
 		frsNeedPreemption: sets.New(fr),
 		workloadUsage:     workload.Usage{Quota: resources.FlavorResourceQuantities{fr: quantity}},
 	})
+	for _, candidate := range candidates {
+		revertRemoval := cq.SimulateUsageRemoval(candidate.WorkloadInfo.Usage())
+		defer revertRemoval()
+	}
+	borrowAfterPreemptions, _ := classical.FindHeightOfLowestSubtreeThatFits(cq, fr, quantity)
 	if len(candidates) == 0 {
-		return preemptioncommon.NoCandidates
+		return preemptioncommon.NoCandidates, borrowAfterPreemptions
 	}
```

  We need to calculate the borrowing level before this (even if there are no candidates) because we return it. And before that calculation we have to remove the usage of the candidates (if there are any). So, this is why its here.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-23T09:42:21Z

- **pkg/webhooks/clusterqueue_webhook.go:245** by [@pajakd](https://github.com/pajakd) — 2025-07-23T09:42:21Z
```diff
@@ -224,3 +227,21 @@ func validateLendingLimit(lend, nominal resource.Quantity, config validationConf
 	}
 	return allErrs
 }
+
+// validateFlavorFungibility enforces that FlavorSelectionStrategy is consistent with WhenCanBorrow and WhenCanPreempt
+func validateFlavorFungibility(flavorFungibility *kueue.FlavorFungibility, fldPath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	if features.Enabled(features.FlavorFungibility) && flavorFungibility != nil {
+		if flavorFungibility.WhenCanBorrow == kueue.Borrow &&
+			flavorFungibility.WhenCanPreempt == kueue.TryNextFlavor &&
+			flavorFungibility.WhenCanPreemptAndBorrow == kueue.AvoidBorrowing {
+			allErrs = append(allErrs, field.Invalid(fldPath, flavorFungibility, flavorFungibilityErrorMsg))
+		}
+		if flavorFungibility.WhenCanBorrow == kueue.TryNextFlavor &&
+			flavorFungibility.WhenCanPreempt == kueue.Preempt &&
+			flavorFungibility.WhenCanPreemptAndBorrow == kueue.AvoidPreemption {
+			allErrs = append(allErrs, field.Invalid(fldPath, flavorFungibility, flavorFungibilityErrorMsg))
+		}
+	}
```

  Done. The new field can be nonempty only if `WhenCanPreempt = TryNextFlavor` and `WhenCanBorrow = TryNextFlavor`. So now there are exactly 5 legal combinations and each of them is meaningful.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-23T09:42:33Z

- **apis/kueue/v1beta1/clusterqueue_types.go:423** by [@pajakd](https://github.com/pajakd) — 2025-07-23T09:42:33Z
```diff
@@ -409,6 +416,17 @@ type FlavorFungibility struct {
 	// +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
 	// +kubebuilder:default="TryNextFlavor"
 	WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt,omitempty"`
+	// whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+	// multiple options. If either borrowing or preemption is neccessary and multiple options
+	// are available, then this field defines the selection strategy. The possible values are:
+	//
+	// - `AvoidPreemption` (default): prefer to allocate in a flavor that does not preempt
```

  Updated the comment.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-07-23T10:09:40Z

- **pkg/scheduler/preemption/preemption_oracle.go:62** by [@gabesaba](https://github.com/gabesaba) — 2025-07-23T09:57:50Z
```diff
@@ -46,13 +47,18 @@ func (p *PreemptionOracle) SimulatePreemption(log logr.Logger, cq *cache.Cluster
 		frsNeedPreemption: sets.New(fr),
 		workloadUsage:     workload.Usage{Quota: resources.FlavorResourceQuantities{fr: quantity}},
 	})
+	for _, candidate := range candidates {
+		revertRemoval := cq.SimulateUsageRemoval(candidate.WorkloadInfo.Usage())
+		defer revertRemoval()
+	}
+	borrowAfterPreemptions, _ := classical.FindHeightOfLowestSubtreeThatFits(cq, fr, quantity)
 	if len(candidates) == 0 {
-		return preemptioncommon.NoCandidates
+		return preemptioncommon.NoCandidates, borrowAfterPreemptions
 	}
```

  Does borrowing level matter if it is NoFit (which is implied by 0 candidates, right?)

- **pkg/webhooks/clusterqueue_webhook.go:42** by [@gabesaba](https://github.com/gabesaba) — 2025-07-23T10:06:26Z
```diff
@@ -39,6 +39,7 @@ import (
 const (
 	limitIsEmptyErrorMsgTemplate string = `must be nil when %s is empty`
 	lendingLimitErrorMsg         string = `must be less than or equal to the nominalQuota`
+	flavorFungibilityErrorMsg    string = `message`
```

  ^

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T10:37:01Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-23T10:37:01Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  As I synced with @pajakd I think the introduction dimention of the borrowing is desired. 
  
  Still, I think this is conceptually a separate enhancement which could be extracted to a new PR. However, given this PR would be unlikely to be cherry-picked, and given the release is soon, I'm ok to include it into this PR.
  
  Still, since this change impacts users who don't even use preemption or the new API fields, it warrants a release note, and dedicated test for the scenario. 
  
  Something like "This change also introduces prioritization of flavors which borrow over shorter distances, even if whenCanPreemptAndBorrow is not used.". Please also add such a test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T10:37:23Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-23T10:37:22Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  cc @tenzen-y to check if he is ok with the coupling

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T10:50:51Z

- **apis/kueue/v1beta1/clusterqueue_types.go:432** by [@mimowo](https://github.com/mimowo) — 2025-07-23T10:50:51Z
```diff
@@ -409,6 +416,20 @@ type FlavorFungibility struct {
 	// +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
 	// +kubebuilder:default="TryNextFlavor"
 	WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt,omitempty"`
+	// whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+	// multiple options. If either borrowing or preemption is necessary and multiple options
+	// are available, then this field defines the selection strategy. Can be set only if
+	// `WhenCanBorrow = TryNextFlavor` and `WhenCanPreempt = TryNextFlavor`.
+	// The possible values are:
+	//
+	// - `PreferBorrowing`: prefer to allocate in a flavor that does not preempt
+	//    other workloads. This is the default flavor selection policy if
+	//    `WhenCanBorrow = TryNextFlavor` and `WhenCanPreempt = TryNextFlavor`.
+	// - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+	//    even if it means preempting other workloads.
+	// +optional
+	// +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+	WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"whenCanPreemptAndBorrow,omitempty"`
```

  ```suggestion
  	WhenCanPreemptAndBorrow *FlavorSelectionPolicy `json:"whenCanPreemptAndBorrow,omitempty"`
  ```
  It is generally good practice in k8s API to add new fields to pre-existing structures using pointer fields.
  Otherwise the clients generated using old schema will be failing to get / patch. Also clients generated with the new code will have issues with old schema as they would always expect the field.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T10:53:05Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-23T10:53:05Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  Still, to offload the PR, I'm also ok to split it out, and just try to include both PRs (while I'm ok with coupling this is my actual preference in ideal scenario).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T11:18:33Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-23T11:18:33Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  Actually, sorry, I take it back, I'm leaning to drop the enhancement from this PR, and focus on the API only, as it is still has open questions. We can work on the borrowingDistance improvement post release. I see this has complications in a couple of places in the code which make distraction from the main issue in the enhancement, which actually creates additional unnecessary risk.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-23T12:11:28Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@pajakd](https://github.com/pajakd) — 2025-07-23T12:11:28Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  I removed the borrowingDistance. The distinction is now made only based on binary signal Borrow/NoBorrow.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-23T15:38:09Z

- **pkg/webhooks/clusterqueue_webhook.go:42** by [@pajakd](https://github.com/pajakd) — 2025-07-23T15:38:09Z
```diff
@@ -39,6 +39,7 @@ import (
 const (
 	limitIsEmptyErrorMsgTemplate string = `must be nil when %s is empty`
 	lendingLimitErrorMsg         string = `must be less than or equal to the nominalQuota`
+	flavorFungibilityErrorMsg    string = `message`
```

  Fixed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T15:41:32Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:351** by [@mimowo](https://github.com/mimowo) — 2025-07-23T15:41:32Z
```diff
@@ -333,7 +338,30 @@ const (
 	fit
 )
 
-func fromPreemptionPossibility(preemptionPossibility preemptioncommon.PreemptionPossibility) granularMode {
+// isPreferred returns true if mode a is better than b according to the selected strategy
+func isPreferred(a, b granularMode, fungiblityConfig kueue.FlavorFungibility, fairSharingEnabled bool) bool {
+	if a.preemptionMode == noFit {
+		return false
+	}
+	if b.preemptionMode == noFit {
+		return true
+	}
+
+	if (fungiblityConfig.WhenCanBorrow == kueue.TryNextFlavor && fungiblityConfig.WhenCanPreempt == kueue.Preempt) ||
+		(fungiblityConfig.WhenCanBorrow == kueue.TryNextFlavor && fungiblityConfig.WhenCanPreempt == kueue.TryNextFlavor && fairSharingEnabled) {
```

  Please wrap this in a function which has a suggestive name, probably "preferPreemptionOverBorrowing"?
  
  EDIT: or maybe "preferToAvoidBorrowing"?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T15:54:36Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:738** by [@mimowo](https://github.com/mimowo) — 2025-07-23T15:54:36Z
```diff
@@ -703,40 +730,55 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			resQuota := a.cq.QuotaFor(resources.FlavorResource{Flavor: fName, Resource: rName})
 			// Check considering the flavor usage by previous pod sets.
 			fr := resources.FlavorResource{Flavor: fName, Resource: rName}
-			mode, borrow, s := a.fitsResourceQuota(log, fr, val+assignmentUsage[fr], resQuota)
+			preemptionMode, borrow, s := a.fitsResourceQuota(log, fr, val+assignmentUsage[fr], resQuota)
 			if s != nil {
 				status.reasons = append(status.reasons, s.reasons...)
 			}
-			if mode < representativeMode {
-				representativeMode = mode
+			mode := granularMode{preemptionMode, borrow > 0}
+			if features.Enabled(features.FlavorFungibilityImplicitPreferenceDefault) {
```

  This "if" looks virtually identical as https://github.com/kubernetes-sigs/kueue/pull/6132/files#diff-0870ec2c0ea330a7d087a6ef599905bf9b7164a0dd889d1b00ba17c31a1bfd05R767-R776.
  
  Could we move the feature gate check for `features.Enabled(features.FlavorFungibilityImplicitPreferenceDefault)` and `representativeMode.preemptionMode > mode.preemptionMode` under the `isPreferred` function?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T15:54:54Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:342** by [@mimowo](https://github.com/mimowo) — 2025-07-23T15:54:54Z
```diff
@@ -333,7 +338,30 @@ const (
 	fit
 )
 
-func fromPreemptionPossibility(preemptionPossibility preemptioncommon.PreemptionPossibility) granularMode {
+// isPreferred returns true if mode a is better than b according to the selected strategy
+func isPreferred(a, b granularMode, fungiblityConfig kueue.FlavorFungibility, fairSharingEnabled bool) bool {
```

  Please check the go coverage tool to check if all branches here are covered.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T07:14:28Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:738** by [@pajakd](https://github.com/pajakd) — 2025-07-24T07:14:27Z
```diff
@@ -703,40 +730,55 @@ func (a *FlavorAssigner) findFlavorForPodSetResource(
 			resQuota := a.cq.QuotaFor(resources.FlavorResource{Flavor: fName, Resource: rName})
 			// Check considering the flavor usage by previous pod sets.
 			fr := resources.FlavorResource{Flavor: fName, Resource: rName}
-			mode, borrow, s := a.fitsResourceQuota(log, fr, val+assignmentUsage[fr], resQuota)
+			preemptionMode, borrow, s := a.fitsResourceQuota(log, fr, val+assignmentUsage[fr], resQuota)
 			if s != nil {
 				status.reasons = append(status.reasons, s.reasons...)
 			}
-			if mode < representativeMode {
-				representativeMode = mode
+			mode := granularMode{preemptionMode, borrow > 0}
+			if features.Enabled(features.FlavorFungibilityImplicitPreferenceDefault) {
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T07:14:33Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:351** by [@pajakd](https://github.com/pajakd) — 2025-07-24T07:14:33Z
```diff
@@ -333,7 +338,30 @@ const (
 	fit
 )
 
-func fromPreemptionPossibility(preemptionPossibility preemptioncommon.PreemptionPossibility) granularMode {
+// isPreferred returns true if mode a is better than b according to the selected strategy
+func isPreferred(a, b granularMode, fungiblityConfig kueue.FlavorFungibility, fairSharingEnabled bool) bool {
+	if a.preemptionMode == noFit {
+		return false
+	}
+	if b.preemptionMode == noFit {
+		return true
+	}
+
+	if (fungiblityConfig.WhenCanBorrow == kueue.TryNextFlavor && fungiblityConfig.WhenCanPreempt == kueue.Preempt) ||
+		(fungiblityConfig.WhenCanBorrow == kueue.TryNextFlavor && fungiblityConfig.WhenCanPreempt == kueue.TryNextFlavor && fairSharingEnabled) {
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T07:16:49Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:342** by [@pajakd](https://github.com/pajakd) — 2025-07-24T07:16:48Z
```diff
@@ -333,7 +338,30 @@ const (
 	fit
 )
 
-func fromPreemptionPossibility(preemptionPossibility preemptioncommon.PreemptionPossibility) granularMode {
+// isPreferred returns true if mode a is better than b according to the selected strategy
+func isPreferred(a, b granularMode, fungiblityConfig kueue.FlavorFungibility, fairSharingEnabled bool) bool {
```

  Had to add a test for the case -- featuregate enabled, borrowing preferred. Now all branches of the isPreferred are covered.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T09:12:32Z

- **pkg/scheduler/preemption/preemption_oracle.go:62** by [@pajakd](https://github.com/pajakd) — 2025-07-24T09:12:32Z
```diff
@@ -46,13 +47,18 @@ func (p *PreemptionOracle) SimulatePreemption(log logr.Logger, cq *cache.Cluster
 		frsNeedPreemption: sets.New(fr),
 		workloadUsage:     workload.Usage{Quota: resources.FlavorResourceQuantities{fr: quantity}},
 	})
+	for _, candidate := range candidates {
+		revertRemoval := cq.SimulateUsageRemoval(candidate.WorkloadInfo.Usage())
+		defer revertRemoval()
+	}
+	borrowAfterPreemptions, _ := classical.FindHeightOfLowestSubtreeThatFits(cq, fr, quantity)
 	if len(candidates) == 0 {
-		return preemptioncommon.NoCandidates
+		return preemptioncommon.NoCandidates, borrowAfterPreemptions
 	}
```

  It does not matter. If the workload does not fit, the `borrowAfterPreemptions` will be the maximum distance. Do you suggest it makes more sense to return something else?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-24T09:13:27Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:326** by [@mimowo](https://github.com/mimowo) — 2025-07-24T09:13:27Z
```diff
@@ -320,11 +320,16 @@ func (m FlavorAssignmentMode) String() string {
 
 // granularMode is the FlavorAssignmentMode internal to
 // FlavorAssigner, which lets us distinguish priority based preemption,
-// and reclamation within Cohort.
-type granularMode int
+// reclamation within Cohort and borrowing.
+type granularMode struct {
+	preemptionMode    preemptionMode
+	borrowingDistance int
```

  feel free to resolve

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-24T09:15:26Z

- **pkg/features/kube_features.go:187** by [@mimowo](https://github.com/mimowo) — 2025-07-24T09:15:26Z
```diff
@@ -180,6 +180,12 @@ const (
 	// Enforces that even Jobs with a queue-name label are only reconciled if their namespace
 	// matches managedJobsNamespaceSelector.
 	ManagedJobsNamespaceSelectorAlwaysRespected featuregate.Feature = "ManagedJobsNamespaceSelectorAlwaysRespected"
+
+	// owner: @pajakd
+	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/582-preempt-based-on-flavor-order
+	//
+	// In flavor fungiblity, the preference whether to preempt or borrow is inferred from flavor fungiblity policy
```

  ```suggestion
  	// In flavor fungiblity, the preference whether to preempt or borrow is inferred from flavor fungiblity policy
  	// This feature gate is going to be replaced by an API before graduation or deprecation.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-24T09:23:48Z

- **apis/kueue/v1beta1/clusterqueue_types.go:392** by [@mimowo](https://github.com/mimowo) — 2025-07-24T09:23:48Z
```diff
@@ -385,6 +385,13 @@ const (
 	TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+	AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+	AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  Since we decide for 0.13 to go with feature gate I think we can park the discussion, and propose new API for 0.14.
  
  Feel free to resolve the comment.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T11:55:13Z

- **pkg/scheduler/preemption/preemption_oracle.go:62** by [@pajakd](https://github.com/pajakd) — 2025-07-24T11:55:13Z
```diff
@@ -46,13 +47,18 @@ func (p *PreemptionOracle) SimulatePreemption(log logr.Logger, cq *cache.Cluster
 		frsNeedPreemption: sets.New(fr),
 		workloadUsage:     workload.Usage{Quota: resources.FlavorResourceQuantities{fr: quantity}},
 	})
+	for _, candidate := range candidates {
+		revertRemoval := cq.SimulateUsageRemoval(candidate.WorkloadInfo.Usage())
+		defer revertRemoval()
+	}
+	borrowAfterPreemptions, _ := classical.FindHeightOfLowestSubtreeThatFits(cq, fr, quantity)
 	if len(candidates) == 0 {
-		return preemptioncommon.NoCandidates
+		return preemptioncommon.NoCandidates, borrowAfterPreemptions
 	}
```

  I think I finally understood what you mean. Updated the code.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T11:55:55Z

- **apis/kueue/v1beta1/clusterqueue_types.go:423** by [@pajakd](https://github.com/pajakd) — 2025-07-24T11:55:55Z
```diff
@@ -409,6 +416,17 @@ type FlavorFungibility struct {
 	// +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
 	// +kubebuilder:default="TryNextFlavor"
 	WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt,omitempty"`
+	// whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+	// multiple options. If either borrowing or preemption is neccessary and multiple options
+	// are available, then this field defines the selection strategy. The possible values are:
+	//
+	// - `AvoidPreemption` (default): prefer to allocate in a flavor that does not preempt
```

  Obsolete as API changes were removed from this PR.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T11:56:18Z

- **pkg/webhooks/clusterqueue_webhook.go:245** by [@pajakd](https://github.com/pajakd) — 2025-07-24T11:56:18Z
```diff
@@ -224,3 +227,21 @@ func validateLendingLimit(lend, nominal resource.Quantity, config validationConf
 	}
 	return allErrs
 }
+
+// validateFlavorFungibility enforces that FlavorSelectionStrategy is consistent with WhenCanBorrow and WhenCanPreempt
+func validateFlavorFungibility(flavorFungibility *kueue.FlavorFungibility, fldPath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	if features.Enabled(features.FlavorFungibility) && flavorFungibility != nil {
+		if flavorFungibility.WhenCanBorrow == kueue.Borrow &&
+			flavorFungibility.WhenCanPreempt == kueue.TryNextFlavor &&
+			flavorFungibility.WhenCanPreemptAndBorrow == kueue.AvoidBorrowing {
+			allErrs = append(allErrs, field.Invalid(fldPath, flavorFungibility, flavorFungibilityErrorMsg))
+		}
+		if flavorFungibility.WhenCanBorrow == kueue.TryNextFlavor &&
+			flavorFungibility.WhenCanPreempt == kueue.Preempt &&
+			flavorFungibility.WhenCanPreemptAndBorrow == kueue.AvoidPreemption {
+			allErrs = append(allErrs, field.Invalid(fldPath, flavorFungibility, flavorFungibilityErrorMsg))
+		}
+	}
```

  Obsolete as API changes were removed.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T12:21:08Z

- **pkg/scheduler/preemption/preemption_oracle.go:62** by [@pajakd](https://github.com/pajakd) — 2025-07-24T12:21:08Z
```diff
@@ -46,13 +47,18 @@ func (p *PreemptionOracle) SimulatePreemption(log logr.Logger, cq *cache.Cluster
 		frsNeedPreemption: sets.New(fr),
 		workloadUsage:     workload.Usage{Quota: resources.FlavorResourceQuantities{fr: quantity}},
 	})
+	for _, candidate := range candidates {
+		revertRemoval := cq.SimulateUsageRemoval(candidate.WorkloadInfo.Usage())
+		defer revertRemoval()
+	}
+	borrowAfterPreemptions, _ := classical.FindHeightOfLowestSubtreeThatFits(cq, fr, quantity)
 	if len(candidates) == 0 {
-		return preemptioncommon.NoCandidates
+		return preemptioncommon.NoCandidates, borrowAfterPreemptions
 	}
```

  I take it back. Changing the borrowing for NoCandiates makes some integrations tests to fail. I reverted the change. I'll investigate why is that but in the meantime can it stay as it is?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-24T14:11:29Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:333** by [@mimowo](https://github.com/mimowo) — 2025-07-24T14:11:29Z
```diff
@@ -322,7 +327,42 @@ const (
 	fit
 )
 
-func fromPreemptionPossibility(preemptionPossibility preemptioncommon.PreemptionPossibility) granularMode {
+func preferToAvoidBorrowing(fungiblityConfig kueue.FlavorFungibility) bool {
+	return (fungiblityConfig.WhenCanBorrow == kueue.TryNextFlavor && fungiblityConfig.WhenCanPreempt == kueue.Preempt) ||
+		(fungiblityConfig.WhenCanBorrow == kueue.TryNextFlavor && fungiblityConfig.WhenCanPreempt == kueue.TryNextFlavor)
+}
```

  non-blocking nit: Since now the function simplifies to `fungiblityConfig.WhenCanBorrow == kueue.TryNextFlavor` can you inline?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-24T14:17:56Z

- **pkg/scheduler/preemption/preemption_oracle.go:62** by [@mimowo](https://github.com/mimowo) — 2025-07-24T14:17:55Z
```diff
@@ -46,13 +47,18 @@ func (p *PreemptionOracle) SimulatePreemption(log logr.Logger, cq *cache.Cluster
 		frsNeedPreemption: sets.New(fr),
 		workloadUsage:     workload.Usage{Quota: resources.FlavorResourceQuantities{fr: quantity}},
 	})
+	for _, candidate := range candidates {
+		revertRemoval := cq.SimulateUsageRemoval(candidate.WorkloadInfo.Usage())
+		defer revertRemoval()
+	}
+	borrowAfterPreemptions, _ := classical.FindHeightOfLowestSubtreeThatFits(cq, fr, quantity)
 	if len(candidates) == 0 {
-		return preemptioncommon.NoCandidates
+		return preemptioncommon.NoCandidates, borrowAfterPreemptions
 	}
```

  Once investigated please also add a unit test which demonstrates why this needs to be the wait it is. So that it is much easier to avoid the trap in the future.
