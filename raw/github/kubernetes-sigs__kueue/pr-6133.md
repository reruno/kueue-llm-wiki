# PR #6133: [KEP] Update KEP of the flavor fungibility feature

**Summary**: [KEP] Update KEP of the flavor fungibility feature

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6133

**Last updated**: 2025-07-24T14:12:30Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `kep_fungibility` → `main`
- **Created**: 2025-07-22T11:10:12Z
- **Updated**: 2025-07-24T14:12:30Z
- **Merged**: 2025-07-24T14:12:29Z
- **Closed**: 2025-07-24T14:12:29Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 2   **+58 / -9**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #5424 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-22T11:10:15Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-22T11:10:26Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0f2d41d2ed67db9893da638f8612c23805d1c7f9 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68822ee39d20050008c4bae5 |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-23T15:33:16Z

xref as KEP  for https://github.com/kubernetes-sigs/kueue/pull/6132

@pajakd please update the descption to say "KEP for #..." or "Part of #..." , then GH would x-ref automatically

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-24T14:11:10Z

/lgtm
/approve
I believe all comments are addressed. We can always follow up if needed.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-24T14:11:18Z

LGTM label has been added.  <details>Git tree hash: 27b2ffca085aa18641a1e08976ac648dd27d09ca</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-24T14:11:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6133#issuecomment-3113630598" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6133#" title="Author self-approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-22T12:53:52Z

First questions.

- **keps/582-preempt-based-on-flavor-order/README.md:244** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-22T12:52:24Z
```diff
@@ -183,15 +220,30 @@ type ClusterQueueSpec struct {
 }
 ```
 
-If flavorFungibility is nil in configuration, we will set the `WhenCanBorrow` to `Borrow` and set `WhenCanPreempt` to `TryNextFlavor` to maintain consistency with the current behavior.
+If flavorFungibility is nil in configuration, we will set the `WhenCanBorrow` to `Borrow`, set `WhenCanPreempt` to `TryNextFlavor` and `WhenCanPreemptAndBorrow` to `AvoidPreemption` to maintain consistency with the current behavior.
 
 ### Behavior Changes
 
-We will not change the behavior to judge whether a podset can get enough resource in certain resource flavor. Preemption and admission will not be influenced also. We only change the order these flavors were considered.
+We will not change the behavior to judge whether a podset can get enough resource in certain resource flavor. Preemption and admission will not be influenced also. We don't change the order these flavors are considered, but `WhenCanBorrow` and `WhenCanPreempt` control how many flavors are considered.
+
+We try to schedule a podset in succesive resource flavors in a loop and we decide whether to traverse to the next flavor or break from the loop based on the `flavorFungibility`. The behavior depending on the simulated scheduling result and the configuration is as follows:
+
+| Simulation result  |Configuration  | Behavior |
+|------ | -------- | ------- |
+| `NoFit` | any | continue |
+| `Preempt`, `NoBorrow` | `WhenCanPreempt = TryNextFlavor` | continue |
+| `Preempt`, `NoBorrow` | `WhenCanPreempt = Preempt` | break |
+| `Fit`, `Borrow` | `WhenCanBorrow = TryNextFlavor` | continue |
+| `Fit`, `Borrow` | `WhenCanBorrow = Borrow` | break |
+| `Fit`, `NoBorrow`| any| break| 
+
+By `Borrow`/`NoBorrow` we mean whether borrowing is required or not to fit the considered podset in the sumulation result. After we complete the loop, either by trying all the flavors or by breaking out of it, we end up with a list of possible flavors containing all the considered flavors that yielded a result different than `NoFit`. We choose the flavor to assign based on the following rules:
 
-After we try to schedule a podset in a resource flavor, we decide whether to traverse to the next flavor base on the `flavorFungibility`. If the assignment mode is `NoFit`, we will always try the next flavor until the last one. When the assignment mode is `Preempt`, we can return the current assignment if `WhenCanPreempt` is `Preempt`. Otherwise if the assignment mode is `Fit`, we try the next flavor only when we need borrowing in the current flavor and `WhenCanBorrow` is `TryNextFlavor`.
+- (`Fit`, `NoBorrow`) is the most prefered
+- (`Fit`, `Borrow`) is prefered over (`Preempt`, `NoBorrow`) if `WhenCanPreemptAndBorrow = AvoidPreemption`
+- (`Preempt`, `NoBorrow`) is prefered over (`Fit`, `Borrow`) if `WhenCanPreemptAndBorrow = AvoidBorrowing`
```

  > (`Preempt`, `NoBorrow`) is prefered over (`Fit`, `Borrow`) if `WhenCanPreemptAndBorrow = AvoidBorrowing`
  
  What does this mean? Does the `whenCanPreemptAndBorrow: AvoidBorrowing` respect Preempt and NoBorrow over the Fit? Or does this strategy avoid only Fit with borrow?
  
  IIUC, Fit contains 2 patterns: (1 the workload fits nominalQuota (2 the workload fits borrowed quotas.

- **keps/582-preempt-based-on-flavor-order/README.md:173** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-22T12:53:34Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
```

  Do you have any reason to choose `strategy` suffix? 
  Could we select FlavorSelectionPolicy to align with FlavorFungibilityPolicy?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T13:01:23Z

- **keps/582-preempt-based-on-flavor-order/README.md:177** by [@mimowo](https://github.com/mimowo) — 2025-07-22T13:01:23Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+   AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+   AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  As also asked in the PR impl: https://github.com/kubernetes-sigs/kueue/pull/6132#discussion_r2221907413, I'm not convinced to the naming, the "Avoid" seems stronger than prefer. So, it is likely users would interpret like "do nothing" when all flavors are  (Preempt, NoBorrow). 
  
  So I would be leaning to "PreferPreemption", "PreferBorrowing", or an enum field "preference: Preemption/Borrowing", wdyt?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-22T13:04:55Z

- **keps/582-preempt-based-on-flavor-order/README.md:177** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-22T13:04:55Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+   AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+   AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  I also think that new strategies should not be "Avoid" since it would be better to mention what will happen rather than what will NOT happen.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-22T13:15:38Z

- **keps/582-preempt-based-on-flavor-order/README.md:125** by [@mimowo](https://github.com/mimowo) — 2025-07-22T13:15:38Z
```diff
@@ -122,7 +122,8 @@ in my cluster. In this case I prefer my high priority jobs not running on spot
 instances. If high priority jobs can preempt jobs in standard instances before trying spot instances,
 stability can be achieved.
 
-My use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreempt` to `Preempt`  in the ClusterQueue's spec.
+This use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreemptAndBorrow` to
```

  Can you maybe start a new Story 2? I'm still not clear how this business case relates to `borrowingDistance` I see in the implementation.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T17:59:05Z

- **keps/582-preempt-based-on-flavor-order/README.md:177** by [@pajakd](https://github.com/pajakd) — 2025-07-22T17:59:05Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
+
+const (
+   AvoidPreemption FlavorSelectionStrategy = "AvoidPreemption"
+   AvoidBorrowing  FlavorSelectionStrategy = "AvoidBorrowing"
```

  OK. So I guess more people prefer `prefer` :) Updated.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T18:04:38Z

- **keps/582-preempt-based-on-flavor-order/README.md:173** by [@pajakd](https://github.com/pajakd) — 2025-07-22T18:04:38Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionStrategy string
```

  Oh, of course. Changed to `FlavorSelectionStrategy`

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-22T18:20:50Z

- **keps/582-preempt-based-on-flavor-order/README.md:244** by [@pajakd](https://github.com/pajakd) — 2025-07-22T18:20:50Z
```diff
@@ -183,15 +220,30 @@ type ClusterQueueSpec struct {
 }
 ```
 
-If flavorFungibility is nil in configuration, we will set the `WhenCanBorrow` to `Borrow` and set `WhenCanPreempt` to `TryNextFlavor` to maintain consistency with the current behavior.
+If flavorFungibility is nil in configuration, we will set the `WhenCanBorrow` to `Borrow`, set `WhenCanPreempt` to `TryNextFlavor` and `WhenCanPreemptAndBorrow` to `AvoidPreemption` to maintain consistency with the current behavior.
 
 ### Behavior Changes
 
-We will not change the behavior to judge whether a podset can get enough resource in certain resource flavor. Preemption and admission will not be influenced also. We only change the order these flavors were considered.
+We will not change the behavior to judge whether a podset can get enough resource in certain resource flavor. Preemption and admission will not be influenced also. We don't change the order these flavors are considered, but `WhenCanBorrow` and `WhenCanPreempt` control how many flavors are considered.
+
+We try to schedule a podset in succesive resource flavors in a loop and we decide whether to traverse to the next flavor or break from the loop based on the `flavorFungibility`. The behavior depending on the simulated scheduling result and the configuration is as follows:
+
+| Simulation result  |Configuration  | Behavior |
+|------ | -------- | ------- |
+| `NoFit` | any | continue |
+| `Preempt`, `NoBorrow` | `WhenCanPreempt = TryNextFlavor` | continue |
+| `Preempt`, `NoBorrow` | `WhenCanPreempt = Preempt` | break |
+| `Fit`, `Borrow` | `WhenCanBorrow = TryNextFlavor` | continue |
+| `Fit`, `Borrow` | `WhenCanBorrow = Borrow` | break |
+| `Fit`, `NoBorrow`| any| break| 
+
+By `Borrow`/`NoBorrow` we mean whether borrowing is required or not to fit the considered podset in the sumulation result. After we complete the loop, either by trying all the flavors or by breaking out of it, we end up with a list of possible flavors containing all the considered flavors that yielded a result different than `NoFit`. We choose the flavor to assign based on the following rules:
 
-After we try to schedule a podset in a resource flavor, we decide whether to traverse to the next flavor base on the `flavorFungibility`. If the assignment mode is `NoFit`, we will always try the next flavor until the last one. When the assignment mode is `Preempt`, we can return the current assignment if `WhenCanPreempt` is `Preempt`. Otherwise if the assignment mode is `Fit`, we try the next flavor only when we need borrowing in the current flavor and `WhenCanBorrow` is `TryNextFlavor`.
+- (`Fit`, `NoBorrow`) is the most prefered
+- (`Fit`, `Borrow`) is prefered over (`Preempt`, `NoBorrow`) if `WhenCanPreemptAndBorrow = AvoidPreemption`
+- (`Preempt`, `NoBorrow`) is prefered over (`Fit`, `Borrow`) if `WhenCanPreemptAndBorrow = AvoidBorrowing`
```

  Right, so in my notation we have 4 interesting cases (`Fit`, `NoBorrow`), (`Fit`, `Borrow`), (`Preempt`, `NoBorrow`), (`Preempt`, `Borrow`).
  
  1. `WhenCanPreemptAndBorrow = PreferBorrowing` (current) the order is  (`Fit`, `NoBorrow`), (`Fit`, `Borrow`), (`Preempt`, `NoBorrow`), (`Preempt`, `Borrow`)
  2. `WhenCanPreemptAndBorrow = PreferPreemption` (new) the order is  (`Fit`, `NoBorrow`), (`Preempt`, `NoBorrow`), (`Fit`, `Borrow`),  (`Preempt`, `Borrow`)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T10:59:28Z

- **keps/582-preempt-based-on-flavor-order/README.md:125** by [@mimowo](https://github.com/mimowo) — 2025-07-23T10:59:27Z
```diff
@@ -122,7 +122,8 @@ in my cluster. In this case I prefer my high priority jobs not running on spot
 instances. If high priority jobs can preempt jobs in standard instances before trying spot instances,
 stability can be achieved.
 
-My use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreempt` to `Preempt`  in the ClusterQueue's spec.
+This use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreemptAndBorrow` to
```

  I think the new story should be along the lines:
  
  As an admin of system managed by Kueue with FairSharing enabled I would like to minimize the risk that admitted workloads get preempted soon after admission. Since every borrowing workload is a preemption(reclaim) candidate, to minimize the risk, I would like to prioritize selecting flavors which are preempting rather than borrowing.
  
  My use case can be supported by setting `spec.flavorFungability.whenCanPreemptAndBorrow: Preempt`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T10:59:40Z

- **keps/582-preempt-based-on-flavor-order/README.md:126** by [@mimowo](https://github.com/mimowo) — 2025-07-23T10:59:39Z
```diff
@@ -122,7 +122,8 @@ in my cluster. In this case I prefer my high priority jobs not running on spot
 instances. If high priority jobs can preempt jobs in standard instances before trying spot instances,
 stability can be achieved.
 
-My use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreempt` to `Preempt`  in the ClusterQueue's spec.
+This use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreemptAndBorrow` to
+`PreferPreemption` in the ClusterQueue's spec.
```

  Why not just "Preempt"?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T11:00:13Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T11:00:13Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  ```suggestion
    WhenCanPreemptAndBorrow *FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
  ```
  As explained in the impl PR https://github.com/kubernetes-sigs/kueue/pull/6132#discussion_r2225145536

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T11:01:21Z

- **keps/582-preempt-based-on-flavor-order/README.md:211** by [@mimowo](https://github.com/mimowo) — 2025-07-23T11:01:21Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
```

  Why not just `Borrow`,`Preempt`? Since the field is already `whenCanPreemptAndBorrow` it is already clear we have the two options.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T11:41:52Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T11:41:52Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  Actually, I find this API still confusing a lot. Its form is similar to whenCanPreempt and whenCanBorrow, but the semantics is very differenent. whenCanPreempt and whenCanBorrow indicate actually: stop looking for new flavors. They mean when flavor which preempt -> do something, when flavor which can borrow -> do something.
  
  The naming convention suggests the new field has the same nature, when a flavor which can preempt and borrow is encountered -> do something. 
  
  However, the nature of the field is very different, it means "once the set of flavors is determined, choose the one fitting the preference: Borrow or Preempt".
  
  cc @tenzen-y @pajakd 
  
  Let me propose something.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T11:55:56Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T11:55:56Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  The API I'm thinking towards is 
  ```yaml
  flavorFungability:
    whenCanBorrow: Stop / TryNextFlavor
    whenCanPreempt: Stop / TryNextFlavor
    preferenceOrder: ["Preempt",  "PreemptWhileBorrowing", "Borrow"]
  ```
  Now, the field is `preferenceOrder` because some users may want to position "PreemptWhileBorrowing" differently. So, by saying just "Preempt / Borrow" we are too constraining for the future. 
  
  I think what happens here is some **implicit ordering** currently "Borrow", "Preempt", "PreemptWhileBorrowing". 
  
  As mentioned in https://github.com/kubernetes-sigs/kueue/pull/6133/files#r2225166242 the story is really for a user which is using FairSharing, and we got feedback due to the very nature of fair sharing they want "Preempt", "PreemptWhileBorrowing", "Borrow".
  
  However, I can imagine some users will want ["Preempt", "PreemptWhileBorrowing", "Borrow"] (say outside of FairSharing), so long term something like `preferenceOrder` might be helpful.
  
  So, I propose to do not introduce new API for 0.13, just a beta level feature gate (for now), `FairSharingWithFlavorFungabilityPrefersPreemption`. I think this is a sensible default when FairSharing is used for the reasons explained in the Story. The feature gate will give us bailout option if some users are unhappy with the change (I think this is unlikely), yet will give us time to think of better API. I'm also ok to make the feature gate alpha for now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T12:45:21Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T12:45:21Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  Ok, let me expand on the dependency on FairSharing as it was, rightfully, questioned when discussing with @pajakd @gabesaba . 
  
  What we want with this feature is to avoid borrowing when reclaim is enabled. Because if a workload borrows it is likely a preemption target of the workloads in CQs which are entitles to reclaim. 
  
  Initially I thought, and was mistaken, that FairSharing enables all CQs in the system to reclaim (which is the common configuration), but in fact a CQ may still disabled preemption.reclaimWithinCohort.
  
  Let me rework the feature gate idea then. I think what makes sense it still to use *implicit defaults* until we have a better idea of what is needed.
  
  So my proposal is:
  1. `whenCanBorrow: Borrow` & `whenCanPreempt: TryNextFlavor` -> implicit default to for preference to Borrow
  2. `whenCanBorrow: TryNextFlavor` & `whenCanPreempt: Preempt` -> implicit default to for preference to Preempt
  3. `whenCanBorrow: Borrow` & `whenCanPreempt: Preempt`  -> implicit default does not matter because we just choose first
  4. `whenCanBorrow: TryNextFlavor` & `whenCanPreempt: TryNextFlavor` -> make implicit default depending on the enablement of FairSharing. This is enough to capture the idea of FairSharing to mix borrowing with reclaim, yet will only be scoped to CQs which have  `whenCanBorrow: TryNextFlavor` & `whenCanPreempt: TryNextFlavor`. 
  
  EDIT: The intention for tying the default to FairSharing (in 4.) is the guesstimate of user preferences for "Preempt". The reason is that FairSharing with reclamation disabled is rather corner case usage, so I think it is reasonable to assume users who use FairSharing will prefer "Preempt" over "Borrowing".
  
  All of this I would hide behind feature gate `FlavorFungabilityImplicitPreferenceDefault`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T12:47:55Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T12:47:54Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  Once we hear from users no complains for a couple of releases (most likely scenario) we can just graduate `FlavorFungabilityImplicitPreferenceDefault`. If some users report issues we can think of `preferenceOrder` idea to override the implicit default. However, for now I think we can improve the default behavior using feature gate without expanding the API.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-23T16:03:52Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-23T16:03:52Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  I don't think that we should graduate FlavorFungabilityImplicitPreferenceDefault FG to default enablement ones (beta, ga) since IIRC, pattern 2 (`whenCanBorrow: TryNextFlavor` & `whenCanPreempt: Preempt`) breaks the current behavior.
  
  If we want to ship this behavior as a default, we should extend the API.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T16:09:58Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T16:09:58Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  Well it changes the behavior, but I think in a preferrable way. 
  
  Rather than extending the API I prefer to keep the API surface minimal. When we graduate the feature to Beta, we will wait for a couple of releases to get feedback if no one complains I think it means the semantic change is desired(as I expect).
  
  We did similar approach in the past with MultiplePreemptions feature gate for example.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-23T16:22:20Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-23T16:22:20Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  The flavorFungibility is supported since v0.5. And pattern 2 is kept as a `Borrow`.
  However, if we change the existing behavior (pattern 2) to `Preempt`, which breaks production.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-23T16:23:58Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-23T16:23:58Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  The minimized API is better. But that could not be a reason to break long-running specification.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T16:35:19Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T16:35:19Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  FlavorFungability is still a Beta feature itself, so I think we can still change the behavior (deprecating the old one), the feature gate gives us time to collect user feedback.
  
  I think when user says `whenCanBorrow: TryNextFlavor & whenCanPreempt: Preempt` it expresses preference for "preemption", and so it makes sense to me to change the behavior at the level of "defaulting" between preferring borrowing and preferring preemption.
  
  I mostly want to avoid maintaining the entire network of options which no-one would use (say `whenCanBorrow: TryNextFlavor & whenCanPreempt: Preempt`, but `preference: Borrow`).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-23T16:43:03Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-23T16:43:03Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  If we want to break the Beta feature, we should introduce new API field or an alternative mode as opposed to Preempt and TryNextFlavor. The flavor fungibility is Beta feature, that is no alpha feature.
  
  > I mostly want to avoid maintaining the entire network of options which no-one would use 
  
  We could not proof anyone does not use that. It is evidence of absence. And obviously the current users depend on this feature and behavior since this is enabled by default.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T16:55:55Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T16:55:55Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  > But that could not be a reason to break long-running specification.
  
  Could you support this with some reference? I looked up the KEP, but couldn't find the behavior specified, it seems more of an implementation detail, whatever was simpler.  I'm not convinced we need to treat every implementation decision as "specification".
  
  > If we want to break the Beta feature, we should introduce new API field or an alternative mode as opposed to Preempt and TryNextFlavor. The flavor fungibility is Beta feature, that is no alpha feature.
  
  We have a history of changing semantrics even in the core k8s of GA features, for example here is one where I'm familiar with [Pod phase changed when container exits with 0 in 1.27](https://github.com/kubernetes/kubernetes/issues/118310), so I'm doubtful if there is any policy on that.
  
  > However, if we change the existing behavior (pattern 2) to Preempt, which breaks production.
  
  We are not sure about that. I would argue this is a corner case which is unlikely to break production, rather fix. Do you foresee some users expecting to borrow if they declare "whenCanPreempt: Preempt"?
  
  > We could not proof anyone does not use that. It is evidence of absence. And obviously the current users depend on this feature and behavior since this is enabled by default.
  
  Sure, that is why we don't change it immediately. I'm proposing a feature gate. An affected user can always disable the gate and report an issue.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T17:25:30Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T17:25:30Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  To base the discussion on the k8s guidelines, I think this would be it: [Deprecating a feature or behavior](https://kubernetes.io/docs/reference/using-api/deprecation-policy/#deprecating-a-feature-or-behavior). 
  
  In our case we are not deprecating the feature, but "behavior". I think the relevant rule would be "Rule #7: Deprecated behaviors must function for no less than 1 year after their announced deprecation.", which I'm reading: if we graduate `FlavorFungabilityImplicitPreferenceDefault` to Beta, then we need to keep it for 1 year in Beta to allow users to return to the old behavior. So, I think the path exists. wdyt?
  
  Still, if you feel strongly about it, I'm ok with API, but I wouldn't like to rush with new APIs. So far all the API proposals I've seen have some drawbacks. So, I'm ok to make the feature gate as "Alpha", but with a note that we intend to replace it with an API. wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-23T18:09:43Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@mimowo](https://github.com/mimowo) — 2025-07-23T18:09:43Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  fyi: we synced on slack with @tenzen-y, and we agreed to introduce an alpha feature gate as a temporary measure to collect feedback from users. 
  
  However, going forward instead of graduating the feature gate to Beta, we will introduce an API. This will give us more time to design and discuss the API without rushing. We can add a comment to the feature gate that it is not meant for graduation and its role should be replaced with API.
  
  Unless someone proposes an API which everyone loves, but it seems challenging. So far all proposals have some drawbacks IMO.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-24T09:19:45Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-24T09:19:44Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  @mimowo Thank you for summarizing our discussion.
  As Michal mentioned in the above comment, we introduce a temporary FG (`FlavorFungabilityImplicitPreferenceDefault `) for early feedback users for v0,13. However, to avoid the significant specification changes (Borrow -> Preemption), we do not change the behavior as a default. So, we will revist the API consideration for the v0.14. After we define the APIs for new behavior for v0.14, we will remove the FG (`FlavorFungabilityImplicitPreferenceDefault`).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-24T09:20:03Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-24T09:20:03Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  @gabesaba @pajakd Could you address those decisions?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T09:56:34Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@pajakd](https://github.com/pajakd) — 2025-07-24T09:56:34Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  Updated the implementation PR (#6132) accordingly. Now I'll update this PR to include the decisions.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T10:59:57Z

- **keps/582-preempt-based-on-flavor-order/README.md:213** by [@pajakd](https://github.com/pajakd) — 2025-07-24T10:59:57Z
```diff
@@ -164,16 +165,52 @@ For each type of resource in each podSet, Kueue will traverse all resource group
 
 ```
 const (
-	Borrow FlavorFungibilityPolicy = "Borrow"
-	Preempt  FlavorFungibilityPolicy = "Preempt"
+  Borrow FlavorFungibilityPolicy = "Borrow"
+  Preempt  FlavorFungibilityPolicy = "Preempt"
   TryNextFlavor FlavorFungibilityPolicy = "TryNextFlavor"
 )
 
+type FlavorSelectionPolicy string
+
+const (
+   PreferBorrowing FlavorSelectionPolicy = "PreferBorrowing"
+   PreferPreemption  FlavorSelectionPolicy = "PreferPreemption"
+)
+
 type FlavorFungibility struct {
-  // +kubebuilder:validation:Enum="Borrow,TryNextFlavor"
+  // whenCanBorrow determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Borrow` (default): stop searching and use the best flavor found so far
+  //   according to the selection strategy
+  // - `TryNextFlavor`: try next flavor even if the current
+  //   flavor has enough resources to borrow.
+  //
+  // +kubebuilder:validation:Enum={Borrow,TryNextFlavor}
+  // +kubebuilder:default="Borrow"
   WhenCanBorrow FlavorFungibilityPolicy  `json:"whenCanBorrow"`
-  // +kubebuilder:validation:Enum="Preempt,TryNextFlavor"
+  // whenCanPreempt determines whether a workload should try the next flavor
+  // or stop the search. The possible values are:
+  //
+  // - `Preempt`: stop searching and use the best flavor found so far 
+  //   according to the selection strategy
+  // - `TryNextFlavor` (default): try next flavor even if there are enough
+  //   candidates for preemption in the current flavor.
+  //
+  // +kubebuilder:validation:Enum={Preempt,TryNextFlavor}
+  // +kubebuilder:default="TryNextFlavor"
   WhenCanPreempt FlavorFungibilityPolicy `json:"whenCanPreempt"`
+  // whenCanPreemptAndBorrow defines how should a workload chose the flavor in case of
+  // multiple options. If either borrowing or preemption is necessary and multiple options
+  // are available, then this field defines the selection strategy. The possible values are:
+  //
+  // - `PreferBorrowing` (default): prefer to allocate in a flavor that does not preempt
+  //    other workloads.
+  // - `PreferPreemption`: prefer to allocate in a flavor that uses only the nominal quota
+  //    even if it means preempting other workloads.
+  // +kubebuilder:validation:Enum={PreferBorrowing,PreferPreemption}
+  // +kubebuilder:default="PreferBorrowing"
+  WhenCanPreemptAndBorrow FlavorSelectionPolicy `json:"preferredStrategy,omitempty"`
```

  This is obsolete as there are no API changes for now.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T11:24:51Z

- **keps/582-preempt-based-on-flavor-order/README.md:125** by [@pajakd](https://github.com/pajakd) — 2025-07-24T11:24:51Z
```diff
@@ -122,7 +122,8 @@ in my cluster. In this case I prefer my high priority jobs not running on spot
 instances. If high priority jobs can preempt jobs in standard instances before trying spot instances,
 stability can be achieved.
 
-My use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreempt` to `Preempt`  in the ClusterQueue's spec.
+This use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreemptAndBorrow` to
```

  Updated as recommended.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-07-24T11:26:27Z

- **keps/582-preempt-based-on-flavor-order/README.md:126** by [@pajakd](https://github.com/pajakd) — 2025-07-24T11:26:27Z
```diff
@@ -122,7 +122,8 @@ in my cluster. In this case I prefer my high priority jobs not running on spot
 instances. If high priority jobs can preempt jobs in standard instances before trying spot instances,
 stability can be achieved.
 
-My use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreempt` to `Preempt`  in the ClusterQueue's spec.
+This use case can be supported by setting `.Spec.FlavorFungibility.WhenCanPreemptAndBorrow` to
+`PreferPreemption` in the ClusterQueue's spec.
```

  This is obsolete as there is no API changes (for now).
