# PR #6141: [kep-4400] Add preemptionPolicy to WorkloadPriorityClass

**Summary**: [kep-4400] Add preemptionPolicy to WorkloadPriorityClass

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6141

**Last updated**: 2026-02-27T16:39:46Z

---

## Metadata

- **State**: open
- **Author**: [@jenny-yang-ai](https://github.com/jenny-yang-ai)
- **Branch**: `jennyy/kep-4400` → `main`
- **Created**: 2025-07-22T21:45:01Z
- **Updated**: 2026-02-27T16:39:46Z
- **Merged**: —
- **Closed**: —
- **Labels**: `size/L`, `release-note-none`, `do-not-merge/hold`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+163 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind documentation

#### What this PR does / why we need it:
Updates KEP-973 based on https://github.com/kubernetes-sigs/kueue/discussions/6123

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-22T21:45:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 4598e17e7c3c243a16fe095fae755c536ae595e6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6881e20eb6e74700084167af |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-22T21:45:11Z

Hi @jenny-yang-ai. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-23T06:40:55Z

/ok-to-test

### Comment by [@kannon92](https://github.com/kannon92) — 2025-07-23T21:38:47Z

KEP is clear to me. 

I have one minor comment but its more of an implementation detail.

/lgtm

/assign @mimowo @tenzen-y

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-23T21:38:53Z

LGTM label has been added.  <details>Git tree hash: f05205af714a7a6917b7904ddf93b3031251c44e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-24T07:34:40Z

New changes are detected. LGTM label has been removed.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-24T07:34:42Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6141#" title="Author self-approved">jenny-yang-ai</a>*
**Once this PR has been reviewed and has the lgtm label**, please ask for approval from [mimowo](https://github.com/mimowo). For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mimowo"]} -->

### Comment by [@jenny-yang-ai](https://github.com/jenny-yang-ai) — 2025-07-28T17:09:51Z

bump @tenzen-y @mimowo anything blocking this to merge?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-28T17:11:26Z

> bump @tenzen-y @mimowo anything blocking this to merge?

We briefly reviewed this KEP, but have not done a detailed review.
In the next release cycle, we will revisit here.

### Comment by [@jenny-yang-ai](https://github.com/jenny-yang-ai) — 2025-07-30T23:00:47Z

@tenzen-y Thanks for letting me know! When's the next release cycle?

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-08-05T10:03:28Z

> @tenzen-y Thanks for letting me know! When's the next release cycle?

I believe it will be mid-September.

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-08-05T10:03:42Z

/assign

I'll take a review pass.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-05T11:18:47Z

I will check this one after I come back from OOO in the next week.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-05T14:42:42Z

I missed the KEP, I will be reviewing it this week, thanks!

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-09-08T07:46:28Z

Hi @jenny-yang-ai, will you return to this PR?

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-11-21T16:56:09Z

I'm happy to drive this further if @jenny-yang-ai is ok with it.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-05T17:10:12Z

Landing here, adding to the goals here: https://github.com/kubernetes-sigs/kueue/issues/8019

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:05:27Z

@jenny-yang-ai are you still up to continue the work?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-10T12:29:27Z

Actually, IIUC the main use case for this is to have "uber" workloads for running AI training which have a special treatment. 

I discussed this with @mwielgus and he is considering an alternative design. 

The weak aspects of this proposal in the current form:
1. it allows for "priority" inversion when a lower-priority workload (numerically) may be "non-preemptibe" while the higher priority workload is preemptible
2. it reverts the meaning of the `preemptionPolicy` compared to the k8s core. There the `preemptionPolicy: Never` means "I can never preempt other workloads", here it means "I can never be preempted by other workloads".

So since this does not seem an urgent task I think we can park for a little while to have a bit more discussion.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-12-10T12:34:06Z

/hold
The topic of "uber" workloads is quite important and we are looking forward for some solution in Kueue, but I would prefer not to rush with this design. It will be great to put this design (or alternative) for discussion during wg-batch meeting.

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2025-12-18T19:28:00Z

@mimowo @mwielgus Are we still considering this for 0.16? If not, we can remove it from the 0.16 tracker.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-01-27T16:33:40Z

I want to re-evaluate the design in the light of the KEP coming for the per-CQ approach in https://github.com/kubernetes-sigs/kueue/issues/8826

### Comment by [@mimowo](https://github.com/mimowo) — 2026-02-27T16:39:43Z

/kind kep
Adding new label for the ease of filtering PRs which are KEPs  / designs are require special handling

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-23T21:27:50Z

- **keps/4400-workload-priority-preemption-policy/README.md:65** by [@kannon92](https://github.com/kannon92) — 2025-07-23T21:27:50Z
```diff
@@ -0,0 +1,141 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
+// apis/kueue/v1beta1/workloadpriorityclass_types.go
+type WorkloadPriorityClass struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Value int32 `json:"value"`
+    Description string `json:"description,omitempty"`
+    
+    // preemptionPolicy controls whether workloads using this priority class can be preempted.
+    // Valid values are "Always" (default) and "Never".
+    // "Always" means workloads can be preempted by higher priority workloads.
+    // "Never" means workloads cannot be preempted, but are restricted to nominal quota only.
+    // +kubebuilder:default=Always
```

  I don't think we can default an optional field.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-23T21:49:48Z

- **keps/4400-workload-priority-preemption-policy/README.md:65** by [@kannon92](https://github.com/kannon92) — 2025-07-23T21:49:48Z
```diff
@@ -0,0 +1,141 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
+// apis/kueue/v1beta1/workloadpriorityclass_types.go
+type WorkloadPriorityClass struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Value int32 `json:"value"`
+    Description string `json:"description,omitempty"`
+    
+    // preemptionPolicy controls whether workloads using this priority class can be preempted.
+    // Valid values are "Always" (default) and "Never".
+    // "Always" means workloads can be preempted by higher priority workloads.
+    // "Never" means workloads cannot be preempted, but are restricted to nominal quota only.
+    // +kubebuilder:default=Always
```

  I seem to be mistaken.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-24T03:24:08Z

I left a few very initial comments.

- **keps/4400-workload-priority-preemption-policy/README.md:56** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-24T03:18:26Z
```diff
@@ -0,0 +1,141 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
```

  Could you remove an existing fields from this code snippet, then leave only proposed fields?

- **keps/4400-workload-priority-preemption-policy/README.md:72** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-24T03:23:51Z
```diff
@@ -0,0 +1,141 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
+// apis/kueue/v1beta1/workloadpriorityclass_types.go
+type WorkloadPriorityClass struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Value int32 `json:"value"`
+    Description string `json:"description,omitempty"`
+    
+    // preemptionPolicy controls whether workloads using this priority class can be preempted.
+    // Valid values are "Always" (default) and "Never".
+    // "Always" means workloads can be preempted by higher priority workloads.
+    // "Never" means workloads cannot be preempted, but are restricted to nominal quota only.
+    // +kubebuilder:default=Always
+    // +kubebuilder:validation:Enum=Always;Never
+    // +optional
+    PreemptionPolicy *PreemptionPolicy `json:"preemptionPolicy,omitempty"`
```

  I would use *WorkloadPrioritClassPreemptionPolicy as this typed instead of *PreemptionPolicy so that we can avoid typed conflicts across resources.
  
  cc @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-24T06:04:13Z

- **keps/4400-workload-priority-preemption-policy/README.md:72** by [@mimowo](https://github.com/mimowo) — 2025-07-24T06:04:13Z
```diff
@@ -0,0 +1,141 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
+// apis/kueue/v1beta1/workloadpriorityclass_types.go
+type WorkloadPriorityClass struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Value int32 `json:"value"`
+    Description string `json:"description,omitempty"`
+    
+    // preemptionPolicy controls whether workloads using this priority class can be preempted.
+    // Valid values are "Always" (default) and "Never".
+    // "Always" means workloads can be preempted by higher priority workloads.
+    // "Never" means workloads cannot be preempted, but are restricted to nominal quota only.
+    // +kubebuilder:default=Always
+    // +kubebuilder:validation:Enum=Always;Never
+    // +optional
+    PreemptionPolicy *PreemptionPolicy `json:"preemptionPolicy,omitempty"`
```

  Same here, We often add the name of the struct as prefix in kubernetes when defining APIs

### Review by [@jenny-yang-ai](https://github.com/jenny-yang-ai) (commented) — 2025-07-24T07:35:02Z

- **keps/4400-workload-priority-preemption-policy/README.md:72** by [@jenny-yang-ai](https://github.com/jenny-yang-ai) — 2025-07-24T07:35:02Z
```diff
@@ -0,0 +1,141 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
+// apis/kueue/v1beta1/workloadpriorityclass_types.go
+type WorkloadPriorityClass struct {
+    metav1.TypeMeta   `json:",inline"`
+    metav1.ObjectMeta `json:"metadata,omitempty"`
+
+    Value int32 `json:"value"`
+    Description string `json:"description,omitempty"`
+    
+    // preemptionPolicy controls whether workloads using this priority class can be preempted.
+    // Valid values are "Always" (default) and "Never".
+    // "Always" means workloads can be preempted by higher priority workloads.
+    // "Never" means workloads cannot be preempted, but are restricted to nominal quota only.
+    // +kubebuilder:default=Always
+    // +kubebuilder:validation:Enum=Always;Never
+    // +optional
+    PreemptionPolicy *PreemptionPolicy `json:"preemptionPolicy,omitempty"`
```

  sounds good, made the change.

### Review by [@jenny-yang-ai](https://github.com/jenny-yang-ai) (commented) — 2025-07-24T07:35:17Z

- **keps/4400-workload-priority-preemption-policy/README.md:56** by [@jenny-yang-ai](https://github.com/jenny-yang-ai) — 2025-07-24T07:35:17Z
```diff
@@ -0,0 +1,141 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
```

  I have made the change, thanks

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-07-25T16:00:21Z

- **keps/4400-workload-priority-preemption-policy/kep.yaml:15** by [@kannon92](https://github.com/kannon92) — 2025-07-25T16:00:20Z
```diff
@@ -0,0 +1,26 @@
+title: WorkloadPriorityClass Preemption Policy
+kep-number: 4400
+authors:
+  - "@jenny-yang-ai"
+status: provisional
+creation-date: 2025-07-22
+reviewers:
+  - "@kannon92"
+  - "@tenzen-y"
+approvers:
+  - "@kannon92"
+  - "@tenzen-y"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
```

  @tenzen-y @mimowo 
  
  This seems to not be too involved. Do we still want alpha for it?
  
  I'm not sure if we follow the same pattern as k8s that new APIs require an alpha.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-08-05T11:14:29Z

This looks generally good :)

- **keps/4400-workload-priority-preemption-policy/README.md:46** by [@gabesaba](https://github.com/gabesaba) — 2025-08-05T10:58:26Z
```diff
@@ -0,0 +1,137 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
```

  What if there are some shared resources defined _at the [Cohort level](https://kueue.sigs.k8s.io/docs/concepts/cohort/#configuring-quotas)_, should the workload be able to use these resources?

- **keps/4400-workload-priority-preemption-policy/README.md:102** by [@gabesaba](https://github.com/gabesaba) — 2025-08-05T11:04:27Z
```diff
@@ -0,0 +1,137 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
+// apis/kueue/v1beta1/workloadpriorityclass_types.go
+type WorkloadPriorityClass struct {
+    // ... existing fields ...
+    
+    // preemptionPolicy controls whether workloads using this priority class can be preempted.
+    // Valid values are "Always" (default) and "Never".
+    // "Always" means workloads can be preempted by higher priority workloads.
+    // "Never" means workloads cannot be preempted, but are restricted to nominal quota only.
+    // +kubebuilder:default=Always
+    // +kubebuilder:validation:Enum=Always;Never
+    // +optional
+    PreemptionPolicy *WorkloadPriorityClassPreemptionPolicy `json:"preemptionPolicy,omitempty"`
+}
+
+// WorkloadPriorityClassPreemptionPolicy describes a policy for if/when to preempt a workload.
+type WorkloadPriorityClassPreemptionPolicy string
+
+const (
+    // WorkloadPriorityClassPreemptionPolicyAlways means that workloads can always be preempted by
+    // other workloads with higher priority.
+    WorkloadPriorityClassPreemptionPolicyAlways WorkloadPriorityClassPreemptionPolicy = "Always"
+    
+    // WorkloadPriorityClassPreemptionPolicyNever means that workloads should never be preempted.
+    // Workloads with this policy are restricted to nominal quota and cannot
+    // borrow resources from other ClusterQueues.
+    WorkloadPriorityClassPreemptionPolicyNever WorkloadPriorityClassPreemptionPolicy = "Never"
+)
+```
+
+### Implementation Details
+
+1. **Add PreemptionPolicy field to WorkloadPriorityClass** - Add a field that can be `Always` or `Never` (extensible for future values)
+
+2. **Track NonPreemptibleUsage in clusterqueue_snapshot.go** - Calculate current usage by non-preemptible workloads for each FlavorResource
+
+3. **Update flavorassigner.go/fitsResourceQuota** - If a workload is non-preemptible, check that it doesn't exceed the non-preemptible quota constraint
+
+4. **Update preemption.go/findCandidates** - Skip non-preemptible workloads when selecting preemption candidates
+
+5. **Define IsNonPreemptible helper** - `getWorkloadPriorityClass(w).preemptionPolicy == Never`
+
+### Interaction with ClusterQueue Preemption Policies
+
+The preemption policy hierarchy works as follows:
+
+1. **ClusterQueue policy is checked first** - If the ClusterQueue disables preemption, no workloads in that queue can be preempted regardless of their WorkloadPriorityClass. However, the quota constraints for non-preemptible workloads still apply.
```

  This ClusterQueue configuration defines which workloads the ClusterQueue can preempt (including its own workloads, via [WithinClusterQueue](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/apis/kueue/v1beta1/clusterqueue_types.go#L470-L472)). We do not currently have a configuration to prevent other ClusterQueues from preempting workloads in the ClusterQueue defining the policy, except configuration the BorrowingLimit to 0 (since nominal quota can't ever be preempted)

- **keps/4400-workload-priority-preemption-policy/README.md:45** by [@gabesaba](https://github.com/gabesaba) — 2025-08-05T11:07:55Z
```diff
@@ -0,0 +1,137 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
```

  May consider requiring [StrictFIFO](https://github.com/kubernetes-sigs/kueue/blob/22e4502335b0116300b3dcc63d237cf8b7ac759a/apis/kueue/v1beta1/clusterqueue_types.go#L166-L169), otherwise these workloads may starve

- **keps/4400-workload-priority-preemption-policy/README.md:88** by [@gabesaba](https://github.com/gabesaba) — 2025-08-05T11:09:31Z
```diff
@@ -0,0 +1,137 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
+// apis/kueue/v1beta1/workloadpriorityclass_types.go
+type WorkloadPriorityClass struct {
+    // ... existing fields ...
+    
+    // preemptionPolicy controls whether workloads using this priority class can be preempted.
+    // Valid values are "Always" (default) and "Never".
+    // "Always" means workloads can be preempted by higher priority workloads.
+    // "Never" means workloads cannot be preempted, but are restricted to nominal quota only.
+    // +kubebuilder:default=Always
+    // +kubebuilder:validation:Enum=Always;Never
+    // +optional
+    PreemptionPolicy *WorkloadPriorityClassPreemptionPolicy `json:"preemptionPolicy,omitempty"`
+}
+
+// WorkloadPriorityClassPreemptionPolicy describes a policy for if/when to preempt a workload.
+type WorkloadPriorityClassPreemptionPolicy string
+
+const (
+    // WorkloadPriorityClassPreemptionPolicyAlways means that workloads can always be preempted by
+    // other workloads with higher priority.
+    WorkloadPriorityClassPreemptionPolicyAlways WorkloadPriorityClassPreemptionPolicy = "Always"
+    
+    // WorkloadPriorityClassPreemptionPolicyNever means that workloads should never be preempted.
+    // Workloads with this policy are restricted to nominal quota and cannot
+    // borrow resources from other ClusterQueues.
+    WorkloadPriorityClassPreemptionPolicyNever WorkloadPriorityClassPreemptionPolicy = "Never"
+)
+```
+
+### Implementation Details
+
+1. **Add PreemptionPolicy field to WorkloadPriorityClass** - Add a field that can be `Always` or `Never` (extensible for future values)
```

  `Always` feels a little ambiguous/imprecise to me. It sounds like the workload will be unconditionally preempted.

- **keps/4400-workload-priority-preemption-policy/README.md:118** by [@gabesaba](https://github.com/gabesaba) — 2025-08-05T11:12:49Z
```diff
@@ -0,0 +1,137 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Quota Constraints for Non-Preemptible Workloads
+
+Workloads using a `WorkloadPriorityClass` with `preemptionPolicy: Never` are subject to the additional quota constraint:
+
+- **Nominal Quota Limit**: They can only consume resources up to the ClusterQueue's nominal quota
+- **No Borrowing**: They cannot borrow resources from other ClusterQueues in the cohort  
+
+This prevents scenarios where non-preemptible workloads consume borrowed quota that cannot be reclaimed
+
+## Design Details
+
+### API Changes
+
+Extend the `WorkloadPriorityClass` type with a `preemptionPolicy` field:
+
+```golang
+// apis/kueue/v1beta1/workloadpriorityclass_types.go
+type WorkloadPriorityClass struct {
+    // ... existing fields ...
+    
+    // preemptionPolicy controls whether workloads using this priority class can be preempted.
+    // Valid values are "Always" (default) and "Never".
+    // "Always" means workloads can be preempted by higher priority workloads.
+    // "Never" means workloads cannot be preempted, but are restricted to nominal quota only.
+    // +kubebuilder:default=Always
+    // +kubebuilder:validation:Enum=Always;Never
+    // +optional
+    PreemptionPolicy *WorkloadPriorityClassPreemptionPolicy `json:"preemptionPolicy,omitempty"`
+}
+
+// WorkloadPriorityClassPreemptionPolicy describes a policy for if/when to preempt a workload.
+type WorkloadPriorityClassPreemptionPolicy string
+
+const (
+    // WorkloadPriorityClassPreemptionPolicyAlways means that workloads can always be preempted by
+    // other workloads with higher priority.
+    WorkloadPriorityClassPreemptionPolicyAlways WorkloadPriorityClassPreemptionPolicy = "Always"
+    
+    // WorkloadPriorityClassPreemptionPolicyNever means that workloads should never be preempted.
+    // Workloads with this policy are restricted to nominal quota and cannot
+    // borrow resources from other ClusterQueues.
+    WorkloadPriorityClassPreemptionPolicyNever WorkloadPriorityClassPreemptionPolicy = "Never"
+)
+```
+
+### Implementation Details
+
+1. **Add PreemptionPolicy field to WorkloadPriorityClass** - Add a field that can be `Always` or `Never` (extensible for future values)
+
+2. **Track NonPreemptibleUsage in clusterqueue_snapshot.go** - Calculate current usage by non-preemptible workloads for each FlavorResource
+
+3. **Update flavorassigner.go/fitsResourceQuota** - If a workload is non-preemptible, check that it doesn't exceed the non-preemptible quota constraint
+
+4. **Update preemption.go/findCandidates** - Skip non-preemptible workloads when selecting preemption candidates
+
+5. **Define IsNonPreemptible helper** - `getWorkloadPriorityClass(w).preemptionPolicy == Never`
+
+### Interaction with ClusterQueue Preemption Policies
+
+The preemption policy hierarchy works as follows:
+
+1. **ClusterQueue policy is checked first** - If the ClusterQueue disables preemption, no workloads in that queue can be preempted regardless of their WorkloadPriorityClass. However, the quota constraints for non-preemptible workloads still apply.
+2. **WorkloadPriorityClass policy is checked second** - If the ClusterQueue allows preemption, then WorkloadPriorityClass `preemptionPolicy` takes effect
+
+This ensures ClusterQueue administrators maintain top-level control over preemption behavior.
+
+### Test Plan
+
+#### Unit Tests
+
+- `WorkloadPriorityClass` API validation
+- Preemption candidate selection with different policies
+- Quota constraint enforcement for non-preemptible workloads
+- Priority resolution with preemption policy
+
+#### Integration tests
+
+- Workloads with `preemptionPolicy: Never` cannot be preempted within ClusterQueue
```

  Also a test for outside clusterqueue would be good. Perhaps a scenario like this:
  
  Non-preemptible low-priority workload, WL1, admits into ClusterQueueA. A preemptible, but higher priority workload, WL2, admits into ClusterQueueA, while borrowing from ClusterQueueB. New workload for ClusterQueueB cannot preempt WL1.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-06T09:21:01Z

- **keps/4400-workload-priority-preemption-policy/README.md:37** by [@mimowo](https://github.com/mimowo) — 2025-08-06T09:21:00Z
```diff
@@ -0,0 +1,137 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
```

  Do you actually have a case which requires mixing preemptible and non-preemptible workloads in the same LQ and CQ? If so, it would be great to expand the story explaining why this is not an option.
  
  Otherwise, maybe the KEP is not necessary, because you could split the workloads into two streams (preemptible and non-preemptible) targeting different CQs, then the CQ for non-preemptible workloads could achieve the same using:
  - preemption.withinClusterQueue=Never - preventing preemption within the non-preemptible CQ
  - borrowingLimit=0 (ensuring we never have workloads which are reclamation targets)
  
  wdyt?
  
  My main hesitation comes from the realization of the side-effects of the setting mentioned in the "Quota Constraints for Non-Preemptible Workloads" which are quite tricky, especially in the context of cohorts.
  
  EDIT: if we need the mixing of the preemptible and non-preemtible workloads, then I'm wondering if the "side effects" could be better expressed by an ortoghonal Workload API annotation , like `kueue.x-k8s.io/avoid-borrowing`, which could also be usable outside of the new WorkloadPriorityClass policy.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-06T09:26:00Z

- **keps/4400-workload-priority-preemption-policy/README.md:37** by [@mimowo](https://github.com/mimowo) — 2025-08-06T09:26:00Z
```diff
@@ -0,0 +1,137 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
```

  cc @gabesaba

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-08-24T16:37:47Z

- **keps/4400-workload-priority-preemption-policy/README.md:37** by [@kannon92](https://github.com/kannon92) — 2025-08-24T16:37:47Z
```diff
@@ -0,0 +1,137 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
```

  https://github.com/NVIDIA/KAI-Scheduler/issues/68#issuecomment-2801010125
  
  @enoodle mentioned that this is a feature in KAI (allows Jobs to be marked non preeemptible).
  
  Generally, I think this feature is useful as is.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:05:43Z

- **keps/4400-workload-priority-preemption-policy/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:05:43Z
```diff
@@ -0,0 +1,26 @@
+title: WorkloadPriorityClass Preemption Policy
+kep-number: 4400
+authors:
+  - "@jenny-yang-ai"
+status: provisional
+creation-date: 2025-07-22
+reviewers:
+  - "@kannon92"
+  - "@tenzen-y"
+approvers:
+  - "@kannon92"
+  - "@tenzen-y"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.13"
```

  ```suggestion
  latest-milestone: "v0.16"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:05:58Z

- **keps/4400-workload-priority-preemption-policy/kep.yaml:26** by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:05:58Z
```diff
@@ -0,0 +1,26 @@
+title: WorkloadPriorityClass Preemption Policy
+kep-number: 4400
+authors:
+  - "@jenny-yang-ai"
+status: provisional
+creation-date: 2025-07-22
+reviewers:
+  - "@kannon92"
+  - "@tenzen-y"
+approvers:
+  - "@kannon92"
+  - "@tenzen-y"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.13"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.14"
+  beta: "v0.15"
+  stable: "v0.16" 
```

  ```suggestion
    alpha: "v0.16"
    beta: 
    stable: 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:12:51Z

- **keps/4400-workload-priority-preemption-policy/README.md:37** by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:12:51Z
```diff
@@ -0,0 +1,137 @@
+# KEP-4400: WorkloadPriorityClass Preemption Policy
+
+<!-- toc -->
+- [Summary](#summary)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Quota Constraints for Non-Preemptible Workloads](#quota-constraints-for-non-preemptible-workloads)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Implementation Details](#implementation-details)
+  - [Interaction with ClusterQueue Preemption Policies](#interaction-with-clusterqueue-preemption-policies)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes extending the `WorkloadPriorityClass` API (introduced in KEP-973) with a `preemptionPolicy` field to provide administrators with fine-grained control over which workloads can be preempted. This allows multiple workloads within the same ClusterQueue to have different preemption behaviors irrespective of priority level.
+
+## Proposal
+
+Extend the `WorkloadPriorityClass` API with a `preemptionPolicy` field that controls whether workloads using that priority class can be preempted. This follows the same pattern as Kubernetes core `PriorityClass.preemptionPolicy`.
+
+The preemption policy will be enforced at scheduling time, with additional quota constraints for non-preemptible workloads to prevent resource deadlocks.
+
+### User Stories
+
+#### Story 1
+
+As a Kueue user, I want to make specific workloads non-preemptible because they cannot be resumed easily, and I'm willing to wait longer for these workloads to run. My administrator will use the PreemptionPolicy field in WorkloadPriorityClass to provide a lever for this behavior.
```

  +1, sorry I didn't return to the KEP earlier, but was pretty busy with other stuff.
  
  > Do you actually have a case which requires mixing preemptible and non-preemptible workloads in the same LQ and CQ?
  
  I guess the comment linked by @kannon92 is addressing that question as people consider it a gap in Kueue relative to KAI
  
  > My main hesitation comes from the realization of the side-effects of the setting mentioned in the "Quota Constraints for Non-Preemptible Workloads" which are quite tricky, especially in the context of cohorts.
  
  It remains a hesitation / doubt, but again WorkloadPriorityClasses are created by admins, so the assumption is that users cannot abuse it unless the admin creates the class. If some admin is concered about the side effects they can further restrict the use of such priority classes by extra validation.
  
  Overall I don't consider it a blocker

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:13:33Z

- **keps/4400-workload-priority-preemption-policy/kep.yaml:8** by [@mimowo](https://github.com/mimowo) — 2025-12-05T18:13:33Z
```diff
@@ -0,0 +1,26 @@
+title: WorkloadPriorityClass Preemption Policy
+kep-number: 4400
+authors:
+  - "@jenny-yang-ai"
+status: provisional
+creation-date: 2025-07-22
+reviewers:
+  - "@kannon92"
```

  Add me here

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-12-05T18:16:16Z

LGTM, I don't find any particular gaps. What I like about the proposal is that using non-preemptible workloads requires an admin to create the WorkloadPriorityClass first to open this up as an option.

It would be great to see additional pass from @tenzen-y or @mwielgus but other than that I'm happy with it.

cc @sohankunkerkar who is looking into prototyping the feature already
