# PR #6159: Cleanup patch handling by commonizing the code

**Summary**: Cleanup patch handling by commonizing the code

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6159

**Last updated**: 2026-02-21T15:46:35Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `commonize-patch-handling` → `main`
- **Created**: 2025-07-24T08:16:35Z
- **Updated**: 2026-02-21T15:46:35Z
- **Merged**: 2025-07-24T09:04:28Z
- **Closed**: 2025-07-24T09:04:28Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+12 / -22**

## Description

#### What type of PR is this?

/kind cleanup

#### What this PR does / why we need it:

To avoid code duplication

#### Which issue(s) this PR fixes:

Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-24T08:16:40Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 549ee8f22b887860474fd1c2c07b5c2bd8247255 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6881ec037f6f730008d321ea |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-24T08:16:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6159#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-24T08:33:57Z

/lgtm
Thank you!

/hold to resolve @gabesaba comment.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-24T08:34:04Z

LGTM label has been added.  <details>Git tree hash: 96a90e69f865bd653652165df48be5a7748f30ed</details>

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-07-24T09:03:39Z

/unhold

discussed with Michał over  chat. I will try writing the change described in https://github.com/kubernetes-sigs/kueue/pull/6159#discussion_r2227889989. If it is as simple as I think, I will send out a follow-up PR

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-07-24T08:27:24Z

- **pkg/util/client/client.go:48** by [@gabesaba](https://github.com/gabesaba) — 2025-07-24T08:27:23Z
```diff
@@ -35,32 +35,22 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 // If strict is true, the resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
-	}
-	updated, err := update()
-	if err != nil || !updated {
-		return err
-	}
-	patch, err := CreatePatch(objOriginal, obj)
-	if err != nil {
-		return err
-	}
-	if err = c.Patch(ctx, obj, patch); err != nil {
-		return err
-	}
-	return nil
+	return patch(ctx, c, obj, strict, false, update)
 }
 
 // PatchStatus applies the merge patch of client.Object status.
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
+	return patch(ctx, c, obj, true, true, update)
+}
+
+func patch(ctx context.Context, c client.Client, obj client.Object, strict bool, status bool, update func() (bool, error)) error {
```

  are there any other callers besides PatchStatus?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-24T08:44:14Z

- **pkg/util/client/client.go:48** by [@mimowo](https://github.com/mimowo) — 2025-07-24T08:44:14Z
```diff
@@ -35,32 +35,22 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 // If strict is true, the resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
-	}
-	updated, err := update()
-	if err != nil || !updated {
-		return err
-	}
-	patch, err := CreatePatch(objOriginal, obj)
-	if err != nil {
-		return err
-	}
-	if err = c.Patch(ctx, obj, patch); err != nil {
-		return err
-	}
-	return nil
+	return patch(ctx, c, obj, strict, false, update)
 }
 
 // PatchStatus applies the merge patch of client.Object status.
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
+	return patch(ctx, c, obj, true, true, update)
+}
+
+func patch(ctx context.Context, c client.Client, obj client.Object, strict bool, status bool, update func() (bool, error)) error {
```

  Yes, "Patch".

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-07-24T08:57:19Z

- **pkg/util/client/client.go:48** by [@gabesaba](https://github.com/gabesaba) — 2025-07-24T08:54:41Z
```diff
@@ -35,32 +35,22 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 // If strict is true, the resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
-	}
-	updated, err := update()
-	if err != nil || !updated {
-		return err
-	}
-	patch, err := CreatePatch(objOriginal, obj)
-	if err != nil {
-		return err
-	}
-	if err = c.Patch(ctx, obj, patch); err != nil {
-		return err
-	}
-	return nil
+	return patch(ctx, c, obj, strict, false, update)
 }
 
 // PatchStatus applies the merge patch of client.Object status.
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
+	return patch(ctx, c, obj, true, true, update)
+}
+
+func patch(ctx context.Context, c client.Client, obj client.Object, strict bool, status bool, update func() (bool, error)) error {
```

  What about decomposition like this?
  
  1) getOriginalObject(strict) => client.Object
  2) updateAndCreatePatch(original, obj, update) => (patch, err)
  
  Then, PatchStatus, Patch would both use these functions, and diverge at the very end. Rather than having status bool branch behavior? I'm not very happy about these double booleans in signature 
  
  ```
  Patch{
    original := getOriginalObject(strict)
    patch, err := updateAndCreatePatch(original, obj, update)
    // handle err
    c.patch(ctx, obj, patch)
  }
  ```
  
  ```
  PatchStatus {
    original := getOriginalObject(true)
    ...
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-24T09:04:35Z

- **pkg/util/client/client.go:48** by [@mimowo](https://github.com/mimowo) — 2025-07-24T09:04:35Z
```diff
@@ -35,32 +35,22 @@ func CreatePatch(before, after client.Object) (client.Patch, error) {
 // If strict is true, the resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool, update func() (bool, error)) error {
-	objOriginal := obj.DeepCopyObject().(client.Object)
-	if strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
-	}
-	updated, err := update()
-	if err != nil || !updated {
-		return err
-	}
-	patch, err := CreatePatch(objOriginal, obj)
-	if err != nil {
-		return err
-	}
-	if err = c.Patch(ctx, obj, patch); err != nil {
-		return err
-	}
-	return nil
+	return patch(ctx, c, obj, strict, false, update)
 }
 
 // PatchStatus applies the merge patch of client.Object status.
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
+	return patch(ctx, c, obj, true, true, update)
+}
+
+func patch(ctx context.Context, c client.Client, obj client.Object, strict bool, status bool, update func() (bool, error)) error {
```

  > What about decomposition like this?
  
  It is an alternative but seems like more code.
  
  >  I'm not very happy about these double booleans in signature
  
  This is internal non-exported function, so I'm not too worried about it, but we can rework, for sure.
  A common pattern in golang is to introduce a variadic list of "options" like for example cmp.Diff or client.List does, but it feels like overkill here.
