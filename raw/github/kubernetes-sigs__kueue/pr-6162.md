# PR #6162: Use Patch Merge instead of Patch Apply for admission and eviction

**Summary**: Use Patch Merge instead of Patch Apply for admission and eviction

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6162

**Last updated**: 2025-09-19T13:58:49Z

---

## Metadata

- **State**: closed
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `cleanup/use-patch-status` → `main`
- **Created**: 2025-07-24T09:49:01Z
- **Updated**: 2025-09-19T13:58:49Z
- **Merged**: —
- **Closed**: 2025-09-19T13:58:49Z
- **Labels**: `size/XXL`, `release-note-none`, `needs-rebase`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 37   **+1586 / -387**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6158 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-24T09:49:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8bebee9e5a187fc59be7ab027df9194481f75cb3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68bedc9c9afabc000838e8ef |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-07-24T10:44:18Z

/release-note-edit
```release-note
NONE
```

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-05T10:45:43Z

@mszadkow I expect this PR to be much smaller and simpler as we now have the workload.Evict single point of entry. PTAL.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-08-05T11:19:34Z

> @mszadkow I expect this PR to be much smaller and simpler as we now have the workload.Evict single point of entry. PTAL.

Yes, definitively, most of the work is done.
 Thank you!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-26T15:51:21Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6162#" title="Author self-approved">mszadkow</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [mimowo](https://github.com/mimowo) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mimowo"]} -->

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-09-08T14:12:47Z

@mbobrovskyi @mimowo @mwysokin 
It's ready for review, I wonder how to approach the testing side of things as this feature flag touches so many places.
Maybe we could have an extra testing job with the flag enabled?
For now I left the flag enabled to see the CI output

### Comment by [@mwysokin](https://github.com/mwysokin) — 2025-09-08T14:51:51Z

+1 to what @mimowo said

It's more work but this piece of code is absolutely critical. The time spent here will definitely pay off with fewer issues afterwards.

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-09-08T15:03:22Z

> I think this PR is very hard to review as it mixes important changes with refactoring.
> 
> Could you instead please send a preparatory PR where you prepare the function in the workload.go to be used both for patch as apply? For example I like PatchAdmissionStatus which accepts a function. Similarly, we could make the Evict, iiuc?

Yes, make sense I agree wholeheartedly :)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-10T20:21:26Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-24T09:55:49Z

- **pkg/controller/core/workload_controller.go:239** by [@mimowo](https://github.com/mimowo) — 2025-07-24T09:55:49Z
```diff
@@ -231,35 +232,42 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
 		}
 	} else {
-		var updated, evicted bool
+		var updated, evicted, reportWorkloadEvictedOnce bool
 		reason := kueue.WorkloadDeactivated
 		message := "The workload is deactivated"
-		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
-		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+
+		err := clientutil.PatchStatus(ctx, r.client, &wl, func() (bool, error) {
```

  We should probably set the same field manager for backwards-compatibility.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-07-24T10:01:16Z

- **pkg/controller/core/workload_controller.go:239** by [@mszadkow](https://github.com/mszadkow) — 2025-07-24T10:01:16Z
```diff
@@ -231,35 +232,42 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
 		}
 	} else {
-		var updated, evicted bool
+		var updated, evicted, reportWorkloadEvictedOnce bool
 		reason := kueue.WorkloadDeactivated
 		message := "The workload is deactivated"
-		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
-		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+
+		err := clientutil.PatchStatus(ctx, r.client, &wl, func() (bool, error) {
```

  done in line 262

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-07-24T10:19:17Z

- **pkg/controller/core/workload_controller.go:239** by [@mszadkow](https://github.com/mszadkow) — 2025-07-24T10:19:17Z
```diff
@@ -231,35 +232,42 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{}, client.IgnoreNotFound(workload.ApplyAdmissionStatus(ctx, r.client, &wl, true, r.clock))
 		}
 	} else {
-		var updated, evicted bool
+		var updated, evicted, reportWorkloadEvictedOnce bool
 		reason := kueue.WorkloadDeactivated
 		message := "The workload is deactivated"
-		dtCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadDeactivationTarget)
-		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+
+		err := clientutil.PatchStatus(ctx, r.client, &wl, func() (bool, error) {
```

  the others didn't I will keep it consistent

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-07-28T11:58:24Z

- **pkg/controller/admissionchecks/multikueue/workload.go:509** by [@mszadkow](https://github.com/mszadkow) — 2025-07-28T11:58:24Z
```diff
@@ -500,13 +506,13 @@ func (w *wlReconciler) nominateAndSynchronizeWorkers(ctx context.Context, group
 			if remoteWl == nil {
 				clone := cloneForCreate(group.local, group.remoteClients[rem].origin)
 				if err := group.remoteClients[rem].client.Create(ctx, clone); err != nil {
-					log.V(2).Error(err, "creating remote object", "remote", rem)
+					log.Error(err, "creating remote object", "remote", rem)
```

  I can move this to separate PR, cuz log levelling shouldn't matter for Error which has fixed level.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-05T10:26:05Z

- **pkg/scheduler/scheduler.go:785** by [@mszadkow](https://github.com/mszadkow) — 2025-09-05T10:26:04Z
```diff
@@ -769,13 +782,15 @@ func (s *Scheduler) requeueAndUpdate(ctx context.Context, e entry) {
 	log.V(2).Info("Workload re-queued", "workload", klog.KObj(e.Obj), "clusterQueue", klog.KRef("", string(e.ClusterQueue)), "queue", klog.KRef(e.Obj.Namespace, string(e.Obj.Spec.QueueName)), "requeueReason", e.requeueReason, "added", added, "status", e.status)
 
 	if e.status == notNominated || e.status == skipped {
-		patch := workload.PrepareWorkloadPatch(e.Obj, true, s.clock)
-		reservationIsChanged := workload.UnsetQuotaReservationWithCondition(patch, "Pending", e.inadmissibleMsg, s.clock.Now())
-		resourceRequestsIsChanged := workload.PropagateResourceRequests(patch, &e.Info)
-		if reservationIsChanged || resourceRequestsIsChanged {
-			if err := workload.ApplyAdmissionStatusPatch(ctx, s.client, patch); err != nil {
-				log.Error(err, "Could not update Workload status")
-			}
+		wl := e.Obj
```

  DeepCopy?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-08T14:18:34Z

- **test/integration/singlecluster/tas/tas_test.go:51** by [@mszadkow](https://github.com/mszadkow) — 2025-09-08T14:18:34Z
```diff
@@ -48,9 +48,7 @@ import (
 )
 
 var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
-	var (
-		ns *corev1.Namespace
-	)
+	var ns *corev1.Namespace
```

  to be reverted

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-08T14:18:48Z

- **test/integration/singlecluster/scheduler/scheduler_test.go:2233** by [@mszadkow](https://github.com/mszadkow) — 2025-09-08T14:18:48Z
```diff
@@ -2230,7 +2230,7 @@ var _ = ginkgo.Describe("Scheduler", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(clusterQueue), clusterQueue)).To(gomega.Succeed())
 					g.Expect(clusterQueue.Status.PendingWorkloads).Should(gomega.Equal(int32(2)))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  probably be reverted

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-08T14:19:12Z

- **test/util/util.go:357** by [@mszadkow](https://github.com/mszadkow) — 2025-09-08T14:19:11Z
```diff
@@ -354,7 +354,7 @@ func expectWorkloadsToBeAdmittedCountWithOffset(ctx context.Context, offset int,
 				admitted++
 			}
 		}
-		g.Expect(admitted).Should(gomega.Equal(count), "Not enough workloads are admitted")
+		g.Expect(admitted).Should(gomega.Equal(count), fmt.Sprintf("Not enough workloads are admitted, %#v", wls))
```

  to be reverted

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-08T14:20:22Z

- **pkg/controller/core/workload_controller_test.go:670** by [@mszadkow](https://github.com/mszadkow) — 2025-09-08T14:20:22Z
```diff
@@ -667,7 +668,6 @@ func TestReconcile(t *testing.T) {
 						Reason:  "DeactivatedDueToAdmissionCheck",
 						Message: "The workload is deactivated due to Admission check(s): check-1, were rejected",
 					},
-					// In a real cluster this condition would be removed but it cant be in the fake cluster
```

  to be reverted

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-08T14:21:28Z

- **pkg/features/kube_features.go:289** by [@mszadkow](https://github.com/mszadkow) — 2025-09-08T14:21:27Z
```diff
@@ -281,6 +286,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FlavorFungibilityImplicitPreferenceDefault: {
 		{Version: version.MustParse("0.13"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	WorkloadRequestUseMergePatch: {
```

  Like I said in the comment, this was enabled to see how it behaves in CI, as it affects many places and many tests

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-08T14:40:59Z

I think this PR is very hard to review as it mixes important changes with refactoring.

Could you instead please send a preparatory PR where you prepare the function in the workload.go to be used both for patch as apply? For example I like PatchAdmissionStatus which accepts a function. Similarly, we could make the Evict, iiuc?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T07:30:33Z

- **pkg/util/client/client.go:52** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T07:30:33Z
```diff
@@ -44,27 +45,33 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool,
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
-	return updateAndPatch(obj, true, update, func(patch client.Patch) error {
+	return PatchStatusWithStrict(ctx, c, true, nil, obj, update)
+}
+
+func PatchStatusWithStrict(ctx context.Context, c client.Client, strict bool, objOrig, obj client.Object, update func() (bool, error)) error {
+	return updateAndPatch(objOrig, obj, true, update, func(patch client.Patch) error {
```

  ```suggestion
  	return updateAndPatch(objOrig, obj, strict, update, func(patch client.Patch) error {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T07:34:34Z

- **pkg/features/kube_features.go:290** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T07:34:34Z
```diff
@@ -281,6 +286,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FlavorFungibilityImplicitPreferenceDefault: {
 		{Version: version.MustParse("0.13"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	WorkloadRequestUseMergePatch: {
+		{Version: version.MustParse("0.14"), Default: true, PreRelease: featuregate.Alpha},
```

  ```suggestion
  		{Version: version.MustParse("0.14"), Default: false, PreRelease: featuregate.Alpha},
  ```
  If it’s an Alpha feature, it should be disabled by default.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T07:40:05Z

- **pkg/util/client/client.go:55** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T07:40:05Z
```diff
@@ -44,27 +45,33 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool,
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
-	return updateAndPatch(obj, true, update, func(patch client.Patch) error {
+	return PatchStatusWithStrict(ctx, c, true, nil, obj, update)
+}
+
+func PatchStatusWithStrict(ctx context.Context, c client.Client, strict bool, objOrig, obj client.Object, update func() (bool, error)) error {
+	return updateAndPatch(objOrig, obj, true, update, func(patch client.Patch) error {
 		return c.Status().Patch(ctx, obj, patch)
 	})
 }
 
-func getOriginalObject(obj client.Object, strict bool) client.Object {
-	objOriginal := obj.DeepCopyObject().(client.Object)
+func updateAndPatch(objOrig, obj client.Object, strict bool, update func() (bool, error), patchFn func(client.Patch) error) error {
+	if obj == nil {
+		return errors.New("obj can not be nil")
+	}
+
+	if objOrig == nil {
+		objOrig = obj.DeepCopyObject().(client.Object)
+	}
+
 	if strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
```

  Please keep this comment.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T07:41:19Z

- **pkg/util/client/client.go:74** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T07:41:18Z
```diff
@@ -44,27 +45,33 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool,
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
-	return updateAndPatch(obj, true, update, func(patch client.Patch) error {
+	return PatchStatusWithStrict(ctx, c, true, nil, obj, update)
+}
+
+func PatchStatusWithStrict(ctx context.Context, c client.Client, strict bool, objOrig, obj client.Object, update func() (bool, error)) error {
+	return updateAndPatch(objOrig, obj, true, update, func(patch client.Patch) error {
 		return c.Status().Patch(ctx, obj, patch)
 	})
 }
 
-func getOriginalObject(obj client.Object, strict bool) client.Object {
-	objOriginal := obj.DeepCopyObject().(client.Object)
+func updateAndPatch(objOrig, obj client.Object, strict bool, update func() (bool, error), patchFn func(client.Patch) error) error {
+	if obj == nil {
+		return errors.New("obj can not be nil")
+	}
+
+	if objOrig == nil {
+		objOrig = obj.DeepCopyObject().(client.Object)
+	}
+
 	if strict {
-		// Clearing ResourceVersion from the original object to make sure it is included in the generated patch.
-		objOriginal.SetResourceVersion("")
+		objOrig.SetResourceVersion("")
 	}
-	return objOriginal
-}
 
-func updateAndPatch(obj client.Object, strict bool, update func() (bool, error), patchFn func(client.Patch) error) error {
-	objOriginal := getOriginalObject(obj, strict)
 	updated, err := update()
 	if err != nil || !updated {
 		return err
 	}
-	patch, err := createPatch(objOriginal, obj)
+	patch, err := createPatch(objOrig, obj)
```

  Can we avoid renaming to reduce changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T07:43:16Z

- **pkg/util/client/client.go:51** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T07:43:16Z
```diff
@@ -44,27 +45,33 @@ func Patch(ctx context.Context, c client.Client, obj client.Object, strict bool,
 // The resourceVersion will be part of the patch, make this call fail if
 // client.Object was changed.
 func PatchStatus(ctx context.Context, c client.Client, obj client.Object, update func() (bool, error)) error {
-	return updateAndPatch(obj, true, update, func(patch client.Patch) error {
+	return PatchStatusWithStrict(ctx, c, true, nil, obj, update)
+}
+
+func PatchStatusWithStrict(ctx context.Context, c client.Client, strict bool, objOrig, obj client.Object, update func() (bool, error)) error {
```

  ```suggestion
  func PatchStatusWithOriginalObj(ctx context.Context, c client.Client, strict bool, objOrig, obj client.Object, update func() (bool, error)) error {
  ```
  Maybe like this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T07:56:15Z

- **pkg/scheduler/scheduler.go:637** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T07:56:15Z
```diff
@@ -621,26 +626,27 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 		s.updateEntryPenalty(log, e, add)
 
 		// Trigger LocalQueue reconciler to apply any pending penalties
-		s.queues.NotifyWorkloadUpdateWatchers(e.Obj, newWorkload)
+		s.queues.NotifyWorkloadUpdateWatchers(origWorkload, newWorkload)
 	}
 
 	if features.Enabled(features.TopologyAwareScheduling) && len(e.Obj.Status.NodesToReplace) > 0 {
 		log.V(5).Info("Clearing the topology assignment recovery field from the workload status after successful recovery")
 		newWorkload.Status.NodesToReplace = nil
 	}
 
+	newWorkloadCopy := newWorkload.DeepCopy()
```

  This is to prevent a race condition, correct?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T08:13:47Z

- **pkg/controller/core/workload_controller_test.go:405** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T08:13:47Z
```diff
@@ -398,14 +398,15 @@ func TestReconcile(t *testing.T) {
 	cases := map[string]struct {
 		enableObjectRetentionPolicies bool
 
-		workload       *kueue.Workload
-		cq             *kueue.ClusterQueue
-		lq             *kueue.LocalQueue
-		wantWorkload   *kueue.Workload
-		wantError      error
-		wantEvents     []utiltesting.EventRecord
-		wantResult     reconcile.Result
-		reconcilerOpts []Option
+		workload               *kueue.Workload
+		cq                     *kueue.ClusterQueue
+		lq                     *kueue.LocalQueue
+		wantWorkload           *kueue.Workload
+		wantWorkloadPatchMerge *kueue.Workload
```

  Not sure I got why this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T08:15:15Z

- **pkg/controller/core/workload_controller_test.go:2252** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T08:15:15Z
```diff
@@ -2059,8 +2248,15 @@ func TestReconcile(t *testing.T) {
 				}
 				gotWorkload = nil
 			}
-			if diff := cmp.Diff(tc.wantWorkload, gotWorkload, workloadCmpOpts...); diff != "" {
-				t.Errorf("Workloads after reconcile (-want,+got):\n%s", diff)
+
+			if features.Enabled(features.WorkloadRequestUseMergePatch) && tc.wantWorkloadPatchMerge != nil {
```

  Shouldn't we add some test case parameter instead? Like enabledWorkloadRequestUseMergePatch.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T08:16:40Z

- **pkg/controller/core/workload_controller_test.go:2252** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T08:16:40Z
```diff
@@ -2059,8 +2248,15 @@ func TestReconcile(t *testing.T) {
 				}
 				gotWorkload = nil
 			}
-			if diff := cmp.Diff(tc.wantWorkload, gotWorkload, workloadCmpOpts...); diff != "" {
-				t.Errorf("Workloads after reconcile (-want,+got):\n%s", diff)
+
+			if features.Enabled(features.WorkloadRequestUseMergePatch) && tc.wantWorkloadPatchMerge != nil {
```

  Or maybe it is much better to run all tests with and without this feature. I think result should be the same.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-09T08:17:53Z

- **pkg/scheduler/scheduler.go:637** by [@mszadkow](https://github.com/mszadkow) — 2025-09-09T08:17:53Z
```diff
@@ -621,26 +626,27 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 		s.updateEntryPenalty(log, e, add)
 
 		// Trigger LocalQueue reconciler to apply any pending penalties
-		s.queues.NotifyWorkloadUpdateWatchers(e.Obj, newWorkload)
+		s.queues.NotifyWorkloadUpdateWatchers(origWorkload, newWorkload)
 	}
 
 	if features.Enabled(features.TopologyAwareScheduling) && len(e.Obj.Status.NodesToReplace) > 0 {
 		log.V(5).Info("Clearing the topology assignment recovery field from the workload status after successful recovery")
 		newWorkload.Status.NodesToReplace = nil
 	}
 
+	newWorkloadCopy := newWorkload.DeepCopy()
```

  Yes, exactly

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-09-09T08:18:02Z

- **pkg/controller/jobs/pod/pod_controller_test.go:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-09-09T08:18:02Z
```diff
@@ -264,15 +264,16 @@ func TestReconciler(t *testing.T) {
 	testCases := map[string]struct {
 		enableObjectRetentionPolicies bool
 
-		reconcileKey           *types.NamespacedName
-		initObjects            []client.Object
-		pods                   []corev1.Pod
-		wantPods               []corev1.Pod
-		workloads              []kueue.Workload
-		wantWorkloads          []kueue.Workload
-		wantErr                error
-		workloadCmpOpts        cmp.Options
-		excessPodsExpectations []keyUIDs
+		reconcileKey                      *types.NamespacedName
+		initObjects                       []client.Object
+		pods                              []corev1.Pod
+		wantPods                          []corev1.Pod
+		workloads                         []kueue.Workload
+		wantWorkloads                     []kueue.Workload
+		wantWorkloadsWithPatchStatusMerge []kueue.Workload
```

  The same here and other places.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-09T08:27:52Z

- **pkg/features/kube_features.go:290** by [@mszadkow](https://github.com/mszadkow) — 2025-09-09T08:27:52Z
```diff
@@ -281,6 +286,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FlavorFungibilityImplicitPreferenceDefault: {
 		{Version: version.MustParse("0.13"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	WorkloadRequestUseMergePatch: {
+		{Version: version.MustParse("0.14"), Default: true, PreRelease: featuregate.Alpha},
```

  Yes, I know, plz refer to my comment - https://github.com/kubernetes-sigs/kueue/pull/6162#discussion_r2330414017

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-09-11T15:58:54Z

- **pkg/controller/core/workload_controller_test.go:2252** by [@mszadkow](https://github.com/mszadkow) — 2025-09-11T15:58:53Z
```diff
@@ -2059,8 +2248,15 @@ func TestReconcile(t *testing.T) {
 				}
 				gotWorkload = nil
 			}
-			if diff := cmp.Diff(tc.wantWorkload, gotWorkload, workloadCmpOpts...); diff != "" {
-				t.Errorf("Workloads after reconcile (-want,+got):\n%s", diff)
+
+			if features.Enabled(features.WorkloadRequestUseMergePatch) && tc.wantWorkloadPatchMerge != nil {
```

  I double checked and it's actually not that simple.
  Bc, e.g. if reconciliation loop clears the admission and SSA was intercepted (original version) the expected object is not updated as well.
  With Merge Patch without interception or anything the Admission gets cleared in the expected object.
  Thus we need 2 sets wantWorkloads, but in certain situations
