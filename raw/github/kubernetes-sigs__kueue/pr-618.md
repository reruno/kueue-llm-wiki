# PR #618: Add documentation for preemption

**Summary**: Add documentation for preemption

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/618

**Last updated**: 2023-03-09T15:12:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `preemption-doc` → `main`
- **Created**: 2023-03-08T19:23:27Z
- **Updated**: 2023-03-09T15:12:03Z
- **Merged**: 2023-03-09T15:12:03Z
- **Closed**: 2023-03-09T15:12:03Z
- **Labels**: `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 3   **+83 / -35**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Follow up to #514 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/576

#### Special notes for your reviewer:

I'm using v1beta1 in this PR, but updates to the rest of the documentation will come in a separate PR.

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-08T19:23:32Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5db1f31272c167090cbbada952029fbbcd58683a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6408ea332eeffe0007f72467 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-618--kubernetes-sigs-kueue.netlify.app/docs/concepts/cluster_queue](https://deploy-preview-618--kubernetes-sigs-kueue.netlify.app/docs/concepts/cluster_queue) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTYxOC0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.3RbXIsPZUShuNMlvICFnU71DG_8oQBrFhS60mHfT-V4)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site settings](https://app.netlify.com/sites/kubernetes-sigs-kueue/settings/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-08T19:23:33Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/618#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-08T19:28:26Z

Fixes #576

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-03-09T04:14:35Z

/lgtm
/hold
In case of other advices.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-09T14:54:29Z

/hold cancel

## Reviews

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2023-03-08T19:41:15Z

Just adding some suggestions.

- **config/components/crd/bases/kueue.x-k8s.io_clusterqueues.yaml:528** by [@cortespao](https://github.com/cortespao) — 2023-03-08T19:33:27Z
```diff
@@ -509,27 +509,27 @@ spec:
               preemption:
                 description: "preemption describes policies to preempt Workloads from
                   this ClusterQueue or the ClusterQueue's cohort. \n Preemption can
-                  happen in two scenarios: \n - When a Workload fits within the min
+                  happen in two scenarios: \n - When a Workload fits within the nominal
                   quota of the ClusterQueue, but the quota is currently borrowed by
                   other ClusterQueues in the cohort. Preempting Workloads in other
-                  ClusterQueues allows this ClusterQueue to reclaim its min quota.
-                  - When a Workload doesn't fit within the min quota of the ClusterQueue
-                  and there are active Workloads with lower priority. \n The preemption
-                  algorithm tries to find a minimal set of Workloads to preempt to
-                  accomomdate the pending Workload, preempting Workloads with lower
-                  priority first."
+                  ClusterQueues allows this ClusterQueue to reclaim its nominal quota.
+                  - When a Workload doesn't fit within the nominal quota of the ClusterQueue
+                  and there are admitted Workloads in the ClusterQueue with lower
+                  priority. \n The preemption algorithm tries to find a minimal set
+                  of Workloads to preempt to accomomdate the pending Workload, preempting
+                  Workloads with lower priority first."
                 properties:
                   reclaimWithinCohort:
                     default: Never
                     description: "reclaimWithinCohort determines whether a pending
                       Workload can preempt Workloads from other ClusterQueues in the
-                      cohort that are using more than their min quota. Possible values
-                      are: \n - `Never` (default): do not preempt workloads in the
-                      cohort. - `LowerPriority`: if the pending workload fits within
-                      the min quota of its ClusterQueue, only preempt workloads in
-                      the cohort that have lower priority than the pending Workload.
-                      - `Any`: if the pending workload fits within the min quota of
-                      its ClusterQueue, preempt any workload in the cohort, irrespective
+                      cohort that are using more than their nominal quota. Possible
+                      values are: \n - `Never` (default): do not preempt workloads
+                      in the cohort. - `LowerPriority`: if the pending workload fits
```

  ```suggestion
                        in the cohort. - `LowerPriority`: if the pending Workload fits
  ```

- **site/content/en/docs/concepts/cluster_queue.md:285** by [@cortespao](https://github.com/cortespao) — 2023-03-08T19:36:39Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
+
+	  - `Never` (default): do not preempt workloads in the cohort.
```

  Not sure all this indentation space is needed

- **site/content/en/docs/concepts/cluster_queue.md:283** by [@cortespao](https://github.com/cortespao) — 2023-03-08T19:38:21Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
```

  ```suggestion
    Workloads from other ClusterQueues that are using more than
    their nominal quota in the cohort. The possible values are:
  ```

- **site/content/en/docs/concepts/cluster_queue.md:286** by [@cortespao](https://github.com/cortespao) — 2023-03-08T19:38:49Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
+
+	  - `Never` (default): do not preempt workloads in the cohort.
+	  - `LowerPriority`: if the pending workload fits within the nominal
```

  ```suggestion
  	  - `LowerPriority`: if the pending Workload fits within the nominal
  ```

- **site/content/en/docs/concepts/cluster_queue.md:289** by [@cortespao](https://github.com/cortespao) — 2023-03-08T19:39:15Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
+
+	  - `Never` (default): do not preempt workloads in the cohort.
+	  - `LowerPriority`: if the pending workload fits within the nominal
+	    quota of its ClusterQueue, only preempt workloads in the cohort that have
+	    lower priority than the pending Workload.
+	  - `Any`: if the pending workload fits within the nominal quota of its
```

  ```suggestion
  	  - `Any`: if the pending Workload fits within the nominal quota of its
  ```

- **site/content/en/docs/concepts/cluster_queue.md:303** by [@cortespao](https://github.com/cortespao) — 2023-03-08T19:40:55Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
+
+	  - `Never` (default): do not preempt workloads in the cohort.
+	  - `LowerPriority`: if the pending workload fits within the nominal
+	    quota of its ClusterQueue, only preempt workloads in the cohort that have
+	    lower priority than the pending Workload.
+	  - `Any`: if the pending workload fits within the nominal quota of its
+	    ClusterQueue, preempt any Workload in the cohort, irrespective of
+	    priority.
+
+- `withinClusterQueue` determines whether a pending Workload that doesn't fit
+  within the nominal quota for its ClusterQueue, can preempt active Workloads in
+  the ClusterQueue. Possible values are:
+ 
+  - `Never` (default): do not preempt workloads in the ClusterQueue.
+  - `LowerPriority`: only preempt workloads in the ClusterQueue that have
+    lower priority than the pending Workload.
+
+Note that an incoming Workload can preempt Workloads both within the
+ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
+Workloads as possible, prefering Workloads with these characteristics:
```

  ```suggestion
  Workloads as possible, prioritizing Workloads with these characteristics:
  ```
  
  If you decide to keep  _preferring_, fix the typo :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-08T20:04:13Z

/label tide/merge-method-squash

- **config/components/crd/bases/kueue.x-k8s.io_clusterqueues.yaml:528** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T19:58:30Z
```diff
@@ -509,27 +509,27 @@ spec:
               preemption:
                 description: "preemption describes policies to preempt Workloads from
                   this ClusterQueue or the ClusterQueue's cohort. \n Preemption can
-                  happen in two scenarios: \n - When a Workload fits within the min
+                  happen in two scenarios: \n - When a Workload fits within the nominal
                   quota of the ClusterQueue, but the quota is currently borrowed by
                   other ClusterQueues in the cohort. Preempting Workloads in other
-                  ClusterQueues allows this ClusterQueue to reclaim its min quota.
-                  - When a Workload doesn't fit within the min quota of the ClusterQueue
-                  and there are active Workloads with lower priority. \n The preemption
-                  algorithm tries to find a minimal set of Workloads to preempt to
-                  accomomdate the pending Workload, preempting Workloads with lower
-                  priority first."
+                  ClusterQueues allows this ClusterQueue to reclaim its nominal quota.
+                  - When a Workload doesn't fit within the nominal quota of the ClusterQueue
+                  and there are admitted Workloads in the ClusterQueue with lower
+                  priority. \n The preemption algorithm tries to find a minimal set
+                  of Workloads to preempt to accomomdate the pending Workload, preempting
+                  Workloads with lower priority first."
                 properties:
                   reclaimWithinCohort:
                     default: Never
                     description: "reclaimWithinCohort determines whether a pending
                       Workload can preempt Workloads from other ClusterQueues in the
-                      cohort that are using more than their min quota. Possible values
-                      are: \n - `Never` (default): do not preempt workloads in the
-                      cohort. - `LowerPriority`: if the pending workload fits within
-                      the min quota of its ClusterQueue, only preempt workloads in
-                      the cohort that have lower priority than the pending Workload.
-                      - `Any`: if the pending workload fits within the min quota of
-                      its ClusterQueue, preempt any workload in the cohort, irrespective
+                      cohort that are using more than their nominal quota. Possible
+                      values are: \n - `Never` (default): do not preempt workloads
+                      in the cohort. - `LowerPriority`: if the pending workload fits
```

  Done

- **site/content/en/docs/concepts/cluster_queue.md:283** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T19:59:44Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
```

  Why move "in the cohort"? The ClusterQueues belong to the cohort.
  
  Changed the other part.

- **site/content/en/docs/concepts/cluster_queue.md:285** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T20:01:11Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
+
+	  - `Never` (default): do not preempt workloads in the cohort.
```

  removed

- **site/content/en/docs/concepts/cluster_queue.md:286** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T20:01:16Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
+
+	  - `Never` (default): do not preempt workloads in the cohort.
+	  - `LowerPriority`: if the pending workload fits within the nominal
```

  Done

- **site/content/en/docs/concepts/cluster_queue.md:289** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T20:01:20Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
+
+	  - `Never` (default): do not preempt workloads in the cohort.
+	  - `LowerPriority`: if the pending workload fits within the nominal
+	    quota of its ClusterQueue, only preempt workloads in the cohort that have
+	    lower priority than the pending Workload.
+	  - `Any`: if the pending workload fits within the nominal quota of its
```

  Done

- **site/content/en/docs/concepts/cluster_queue.md:303** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-08T20:02:26Z
```diff
@@ -256,6 +256,55 @@ you can set the `.spec.resources[*].flavors[*].quota.max`
 If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
 borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
 
+## Preemption
+
+When there is not enough quota left in a ClusterQueue or its cohort, an incoming
+Workload can trigger preemption of previously admitted Workloads, based on
+policies for the ClusterQueue.
+
+A configuration for a ClusterQueue that enables preemption looks like the
+following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: team-a-cq
+spec:
+  preemption:
+    reclaimWithinCohort: Any
+    withinClusterQueue: LowerPriority
+```
+
+The fields above have the following semantics:
+
+- `reclaimWithinCohort` determines whether a pending Workload can preempt
+  Workloads from other ClusterQueues in the cohort that are using more than
+  their nominal quota. Possible values are:
+
+	  - `Never` (default): do not preempt workloads in the cohort.
+	  - `LowerPriority`: if the pending workload fits within the nominal
+	    quota of its ClusterQueue, only preempt workloads in the cohort that have
+	    lower priority than the pending Workload.
+	  - `Any`: if the pending workload fits within the nominal quota of its
+	    ClusterQueue, preempt any Workload in the cohort, irrespective of
+	    priority.
+
+- `withinClusterQueue` determines whether a pending Workload that doesn't fit
+  within the nominal quota for its ClusterQueue, can preempt active Workloads in
+  the ClusterQueue. Possible values are:
+ 
+  - `Never` (default): do not preempt workloads in the ClusterQueue.
+  - `LowerPriority`: only preempt workloads in the ClusterQueue that have
+    lower priority than the pending Workload.
+
+Note that an incoming Workload can preempt Workloads both within the
+ClusterQueue and the cohort. Kueue implements heuristics to preempt as few
+Workloads as possible, prefering Workloads with these characteristics:
```

  :joy: thanks
  I kept the word "preferring". I think "prioritizing" can be confusing given that we talk about priority below.
