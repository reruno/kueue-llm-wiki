# PR #6210: [KEP-3258]: Create delayed admission check retries KEP

**Summary**: [KEP-3258]: Create delayed admission check retries KEP

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6210

**Last updated**: 2025-10-31T12:22:07Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@dhenkel92](https://github.com/dhenkel92)
- **Branch**: `dhenkel/06-02-docs_kep_create_delayed_admission_check_retries_kep` → `main`
- **Created**: 2025-07-28T14:07:45Z
- **Updated**: 2025-10-31T12:22:07Z
- **Merged**: 2025-10-31T12:22:06Z
- **Closed**: 2025-10-31T12:22:06Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-none`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+599 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Add option to delay admission check retries to kueue.

#### Which issue(s) this PR fixes:

KEP for https://github.com/kubernetes-sigs/kueue/issues/3258

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-28T14:07:51Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cbcf0af85c172825eb360cb73ecef68d1fba52c3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6904797d10b0ba00088cb809 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-28T14:07:55Z

Hi @dhenkel92. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-28T14:20:33Z

/ok-to-test

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-07-28T15:17:26Z

/retest

### Comment by [@PBundyra](https://github.com/PBundyra) — 2025-07-30T10:15:46Z

/assign

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-05T09:25:40Z

@dhenkel92 thank you for working on this important feature.

I'm adding this to my queue, and going to work on reviewing this together with @PBundyra.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-05T14:32:30Z

High level comment: it would be better for the ease of use, and long term maintainability of the project to extend the API of admission check to contain the Retry configuration analogous to ProvisioningRequestsConfig:  https://github.com/kubernetes-sigs/kueue/blob/main/apis/kueue/v1beta1/provisioningrequestconfig_types.go#L120-L151. Then, we could track just the count of retries per AdmissionCheck.

In particular:
- it would eliminate the need to expose the functions for computing the RequeueAfter as we want to unexport Kueuehttps://github.com/kubernetes-sigs/kueue/issues/4615
0 
- simplifies the code of external AdmissionCheck would be just to declare the AdmissionCheck as "Retry" (or we could introduce "Failed"). Then, the workload controller would decide whether to retry to handle as Rejection. 
- it is better to delegate computation of `RequeueAt` to one controller also due to possible clock skew
- it would make the API mode readable, the `RetryAfter` can be misleading, because in fact it is retried at the "max" of all times. Technically it is after, but it might be also 1 year later :) 

I think if we had such a feature we would not need the specific ProvisioningRequestConfig.spec.retryStrategy at all.

Now, regarding compatibility with ProvisioningRequestConfig:
- for the first iteration of the feature I would just request AdmissionCheck.spec.retryStrategy can only be declared for other (we can just prohibit setting `AdmissionCheck.spec.retryStrategy` when `ProvisioningRequestConfig` is referenced
- for future iterations of the feature we should enable using `AdmissionCheck.spec.retryStrategy`  and deprecate ProvisioningRequestConfig .spec.retryStrategy, but for a while support both in code
- for v1beta2 we can drop `ProvisioningRequestConfig .spec.retryStrategy,` entirely

wdyt  @dhenkel92 @PBundyra  @tenzen-y ?

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-08-06T14:41:59Z

> it would be better for the ease of use, and long term maintainability of the project to extend the API of admission check to contain the Retry configuration analogous to ProvisioningRequestsConfig: [main/apis/kueue/v1beta1/provisioningrequestconfig_types.go#L120-L151](https://github.com/kubernetes-sigs/kueue/blob/main/apis/kueue/v1beta1/provisioningrequestconfig_types.go#L120-L151). Then, we could track just the count of retries per AdmissionCheck.

I like your idea in general. It would make the API less complex and push more logic to a central place.
We considered that option too, but having the logic in the controller was actually a feature for us. It allows us to control the retry behavior based on the workload and the current cluster state, rather than relying on a static configuration that’s the same for all workloads.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-07T10:06:41Z

> I like your idea in general. It would make the API less complex and push more logic to a central place.

+1

> We considered that option too, but having the logic in the controller was actually a feature for us. It allows us to control the retry behavior based on the workload and the current cluster state, rather than relying on a static configuration that’s the same for all workloads.

I see, in that case would it work for you if the `RequeueAfterSeconds` is a duration rather than specific datetime ? The advantages:
- eliminates the need to expose the helper `UpdateRequeueState`
- eliminates the potential issues with clock skew between external controller and Kueue
- it makes the calculations for external controllers demanding exponential backoff slightly simpler

I think `RequeueAfterSeconds` could be optional field. When not specified we delegate the decision about the delay to Kueue (it might be 0 or exponential). This would still allow us to migrate ProvReq to the new mechanism (we could add it as a point to re-evaluate for the feature while in Beta). 

Note to clarify: As the AC .spec.retryStrategy would be still compatible with the proposal in the future we could defer it for later when needed.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-07T10:51:51Z

@dhenkel92 it would be also good to maybe add / restructure the user stories to make it clear which could rely on exponential backoff and which require more control.

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-08-08T11:42:43Z

> I think RequeueAfterSeconds could be optional field. When not specified we delegate the decision about the delay to Kueue (it might be 0 or exponential). 

I really like the idea of combining the two 👍
To summarise it in my own words:
* We add a retry strategy field to AdmissionCheck.Spec similar to the [ProvisioningRequestRetryStrategy](https://github.com/kubernetes-sigs/kueue/blob/main/apis/kueue/v1beta1/provisioningrequestconfig_types.go#L120-L151)
* We introduce an optional RequeueAfterSeconds field in the AdmissionCheckState.
* When an admission check sets its state to "Retry", Kueue evicts the workload as usual and increments an internal retry counter. Based on this counter, it calculates the delay and sets the RequeueAfterSeconds field in the check state.
* To determine if a workload should be retried, Kueue looks for the longest delay and checks whether it's still in the future.
* If a controller wants to set or override the delay, it can update the RequeueAfterSeconds field in the status.

I have two questions:

1) Should RequeueAfterSeconds be relative to LastTransitionTime?
I'm wondering if intermediate updates could modify LastTransitionTime. For example, if my admission check sets LastTransitionTime = now and RequeueAfterSeconds = 120, I'd expect Kueue to also update LastTransitionTime when evicting the workload, which would delay the dealy.

2) Should Kueue maintain a single retry counter or multiple?
I can see two possible approaches:

Option A: Single retry counter
As soon as any admission check sets its state to "Retry", Kueue evicts the workload and increments a single internal counter. It then uses this counter to compute delays for all admission checks, even those that didn’t explicitly request a retry.
The advantage here is simplicity: we only need one retry signal, and we’ll wait accordingly. The downside is that we might wait longer than necessary if only one check actually needed a longer delay.

Option B: One counter per admission check
Each admission check maintains its own retry counter. This allows backoff to be more granular and tailored. However, coordinating and syncing multiple counters becomes more complex.

> This would still allow us to migrate ProvReq to the new mechanism (we could add it as a point to re-evaluate for the feature while in Beta).

+1

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-08T13:15:43Z

Oh actually Im surprised the admission check controller needs to set the LastTransitionTime=now. I think we could populate it in webhook provided that it was not supplied by user, but that is a detail.

I think requeueAfterSecondd will be counted formally from Evicted=True which is just the following request after state=Retry, so for practical purposes it sounds ok / the same.

I like the idea of separate counters because it allows to have different exponential backoff configuration per admission check. Then Kueue would be taking max of the requeueAfterSeconds and populate the requeueState.requeueAt. I think we don't need to touch requeueState.count, it would be good to leave it only for WaitForPodsReady, as originally designed. We decided to overload the field when introducing retries for ProvReq, but I think this decision was unfortunate. This new mechanism will allow us to untangle in the future, so Im happy about the KEP being worked on.

Let me know in what you think or if Im missing something.

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-08-08T13:39:09Z

> Let me know in what you think or if Im missing something.

I think what you said makes sense. I'll put the API changes into the KEP next week and then we can continue the discussion from there 🙂

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-01T12:56:52Z

> Now, regarding compatibility with ProvisioningRequestConfig:
> 
> - for the first iteration of the feature I would just request AdmissionCheck.spec.retryStrategy can only be declared for other (we can just prohibit setting AdmissionCheck.spec.retryStrategy when ProvisioningRequestConfig is referenced
> - for future iterations of the feature we should enable using AdmissionCheck.spec.retryStrategy and deprecate ProvisioningRequestConfig .spec.retryStrategy, but for a while support both in code
> - for v1beta2 we can drop ProvisioningRequestConfig.spec.retryStrategy, entirely

These migration step looks good to me.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-08T16:39:58Z

/lgtm
/approve
Leaving on hold to give a chance to comment to @tenzen-y . However, I think you can already start coding / prototyping
/hold

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-08T16:40:05Z

LGTM label has been added.  <details>Git tree hash: bd539898281daa2ef0bc8042f0b27a6aaab77c2d</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-11T14:42:15Z

> /lgtm /approve Leaving on hold to give a chance to comment to @tenzen-y . However, I think you can already start coding / prototyping /hold

Landing here now. Let me see this in this weekend.

### Comment by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-22T17:43:40Z

> > /lgtm /approve Leaving on hold to give a chance to comment to @tenzen-y . However, I think you can already start coding / prototyping /hold
> 
> Landing here now. Let me see this in this weekend.

Did you get a chance to take a look @tenzen-y ?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-24T07:14:46Z

Friendly ping @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-24T14:07:38Z

Sorry for delaying my checks.
I am keeping observing notifications

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-30T15:47:56Z

Please fix the TOC

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-31T11:57:27Z

LGTM label has been added.  <details>Git tree hash: 6d0ea7f02337cdbdf3a285812e7920719b3caf52</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-31T11:57:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6210#" title="Author self-approved">dhenkel92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6210#issuecomment-3267112532" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6210#pullrequestreview-3403936609" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-31T11:57:43Z

/hold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-31T11:57:47Z

/hold cancel

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-05T09:17:05Z

- **keps/3258-delayed-admission-check-retries/README.md:210** by [@PBundyra](https://github.com/PBundyra) — 2025-08-05T09:17:04Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
```

  IIUC there's nothing preventing the second ac from increasing `requeueState.requeueAt` value

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-05T09:26:21Z

- **keps/3258-delayed-admission-check-retries/README.md:225** by [@PBundyra](https://github.com/PBundyra) — 2025-08-05T09:26:21Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
```

  Those are not exactly the same condition like in 3. because of the Workload having now `Evicted=true` condition, and admission check controller could behave differently when processing a Workload with this condition

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-05T09:35:05Z

- **keps/3258-delayed-admission-check-retries/README.md:222** by [@PBundyra](https://github.com/PBundyra) — 2025-08-05T09:35:05Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
```

  Please note that the `requeueState` field in Workload's status in not used exclusively by Admission Checks but also WaitForPodsReady feature. This means while migrating AdmissionCheck related logic to per-admission-check retry handling, we should keep the remaining part related to WaitForPodsReady. Could we add it to the Notes section?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-05T09:37:27Z

- **keps/3258-delayed-admission-check-retries/README.md:234** by [@PBundyra](https://github.com/PBundyra) — 2025-08-05T09:37:27Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
```

  Why are those changes necessary for the proposal? Can't scheduler rely on the existing mechanism?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-05T09:40:40Z

- **keps/3258-delayed-admission-check-retries/README.md:374** by [@PBundyra](https://github.com/PBundyra) — 2025-08-05T09:40:39Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+#### Solution: Delayed Processing and State Reconciliation
+
+To address these issues, the implementation will be changed to allow admission check updates after a workload has been evicted. When an admission check state is set to `Retry` or `Ready`, after eviction, Kueue should reset the state to `Pending` and defer any immediate action. Instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12 + 13). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. The rest of the flow remains the same as before. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue --> kueue: Don't retry as retry after is not done (in 1m)
+  ac1 ->> kube: Set AC to retry with 2m delay
```

  When will this AdmissionCheck reset to Pending?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-05T09:59:08Z

- **keps/3258-delayed-admission-check-retries/README.md:374** by [@PBundyra](https://github.com/PBundyra) — 2025-08-05T09:59:08Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+#### Solution: Delayed Processing and State Reconciliation
+
+To address these issues, the implementation will be changed to allow admission check updates after a workload has been evicted. When an admission check state is set to `Retry` or `Ready`, after eviction, Kueue should reset the state to `Pending` and defer any immediate action. Instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12 + 13). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. The rest of the flow remains the same as before. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue --> kueue: Don't retry as retry after is not done (in 1m)
+  ac1 ->> kube: Set AC to retry with 2m delay
```

  Maybe Kueue could reset all AdmissionChecks when assigning quota instead of eviction, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-06T08:53:18Z

- **keps/3258-delayed-admission-check-retries/README.md:157** by [@mimowo](https://github.com/mimowo) — 2025-08-06T08:53:18Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
```

  This is not necessary if we take this more declarative approach: https://github.com/kubernetes-sigs/kueue/pull/6210#issuecomment-3155482372

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-02T11:12:04Z

- **keps/3258-delayed-admission-check-retries/README.md:210** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-02T11:12:04Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
```

  I think this is correct in theory, but it might cause issues due to how it's implemented.
  When an admission controller sets the state to retry in combination with `requeueState.requeueAt`, the controller will:
  1) Evict the workload
  2) [Schedule a timer ](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/workload_controller.go#L763-L773)to internally requeue the workload
  3) After the timer fires, [perform some checks and finally re-add the workload to the heap](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/queue/cluster_queue.go#L151-L180)
  
  [One of the checks](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/queue/cluster_queue.go#L175-L178) in step 3 is whether the requeueAt timestamp is expired. So, if Admission Controller 1 sets requeueAt to `n + 1h`, the [queues.AddOrUpdateWorkload](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/workload_controller.go#L767) function will be triggered in one hour. But if another check later updates that timestamp in between, the workload will end up stuck in an inadmissible state until Kueue is restarted.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-02T11:13:34Z

- **keps/3258-delayed-admission-check-retries/README.md:222** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-02T11:13:34Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
```

  That's a good point and I wasn't aware of that. Thank you.
  I'll take it into account in the updated version.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-02T11:20:59Z

- **keps/3258-delayed-admission-check-retries/README.md:234** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-02T11:20:59Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
```

  So the main idea is to get rid of [the timer](https://github.com/kubernetes-sigs/kueue/blob/3658e05baed9fe424763c1c24ff2147b56019f0d/pkg/controller/core/workload_controller.go#L763) that currently triggers the future re-adding of workloads to the internal heap ([here are more details why](https://github.com/kubernetes-sigs/kueue/pull/6210#discussion_r2315761025)). Right now, these workloads only exist in the timer and will eventually be added back to the heap structure.
  
  Unfortunately, removing this mechanism would mean that all future retry workloads would immediately become part of the heap and, in the worst case, might go through the scheduler prematurely.
  
  My best idea at the moment is either to build a separate heap for future retries or to make the scheduler aware of this new mechanism.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-02T11:24:26Z

- **keps/3258-delayed-admission-check-retries/README.md:374** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-02T11:24:26Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+#### Solution: Delayed Processing and State Reconciliation
+
+To address these issues, the implementation will be changed to allow admission check updates after a workload has been evicted. When an admission check state is set to `Retry` or `Ready`, after eviction, Kueue should reset the state to `Pending` and defer any immediate action. Instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12 + 13). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. The rest of the flow remains the same as before. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue --> kueue: Don't retry as retry after is not done (in 1m)
+  ac1 ->> kube: Set AC to retry with 2m delay
```

  This sounds like a great idea 🙂 
  I'll think more about the potential implications and add it to the doc.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-02T14:40:04Z

- **keps/3258-delayed-admission-check-retries/kep.yaml:8** by [@mimowo](https://github.com/mimowo) — 2025-09-02T14:23:13Z
```diff
@@ -0,0 +1,37 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-02-06
+reviewers:
+  - TBD
```

  Add me @mimowo and @PBundyra

- **keps/3258-delayed-admission-check-retries/kep.yaml:10** by [@mimowo](https://github.com/mimowo) — 2025-09-02T14:23:19Z
```diff
@@ -0,0 +1,37 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-02-06
+reviewers:
+  - TBD
+approvers:
+  - TBD
```

  Add me

- **keps/3258-delayed-admission-check-retries/kep.yaml:20** by [@mimowo](https://github.com/mimowo) — 2025-09-02T14:23:49Z
```diff
@@ -0,0 +1,37 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-02-06
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: TBD
```

  ```suggestion
  latest-milestone: "v0.14"
  ```

- **keps/3258-delayed-admission-check-retries/kep.yaml:23** by [@mimowo](https://github.com/mimowo) — 2025-09-02T14:24:00Z
```diff
@@ -0,0 +1,37 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-02-06
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: TBD
+
+milestone:
+  alpha: TBD
```

  ```suggestion
    alpha: "v0.14"
  ```

- **keps/3258-delayed-admission-check-retries/README.md:25** by [@mimowo](https://github.com/mimowo) — 2025-09-02T14:27:40Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
```

  Please add back the Graduation Criteria section.

- **keps/3258-delayed-admission-check-retries/README.md:157** by [@mimowo](https://github.com/mimowo) — 2025-09-02T14:30:10Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
```

  Let's use `RequeueAfterSeconds` as per the discussion. This will be time counted from the eviction time.

- **keps/3258-delayed-admission-check-retries/kep.yaml:37** by [@mimowo](https://github.com/mimowo) — 2025-09-02T14:30:43Z
```diff
@@ -0,0 +1,37 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-02-06
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: TBD
+
+milestone:
+  alpha: TBD
+  beta: TBD
+  stable: TBD
+
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+# No associated feature gates. The feature can be disabled through the Configuration API.
+disable-supported: true
+
+metrics:
+  - admission_retry_delay_seconds
+  - admission_retry_total
+  - workload_admission_retry_duration_seconds
+  - workloads_pending_admission_retry
+  - admission_late_updates_total
```

  I would suggest to defer metrics for beta. Let's focus on MVP first. 
  
  Moreover it seems the metrics are independent on the feature itself which is mostly meant for the ease of adding the retry mechanism. So, I think we could have an independent GH issue for the metrics.

- **keps/3258-delayed-admission-check-retries/README.md:478** by [@mimowo](https://github.com/mimowo) — 2025-09-02T14:35:21Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+#### Solution: Delayed Processing and State Reconciliation
+
+To address these issues, the implementation will be changed to allow admission check updates after a workload has been evicted. When an admission check state is set to `Retry` or `Ready`, after eviction, Kueue should reset the state to `Pending` and defer any immediate action. Instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12 + 13). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. The rest of the flow remains the same as before. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue --> kueue: Don't retry as retry after is not done (in 1m)
+  ac1 ->> kube: Set AC to retry with 2m delay
+  kueue --> kueue: Don't retry as retry after is not done (in 2m)
+  deactivate ac1
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+##### Caveats
+
+In the new case, there's still a race condition if one admission check is fast and doesn't set any delay, which means that the workload will be retries immediately.
+However, this shouldn't be a problem, since the update can be posted at any time. This means the second admission check will eventually set the retry with a delay, ensuring it's not reworked immediately again.
+That said, it still means we go through a couple of unnecessary cycles, which should be okay.
+
+#### Alternative: Wait for All Admission Checks
+
+One alternative approach would be to have Kueue wait for all admission checks to complete before taking any eviction action. This would ensure that all admission checks have the opportunity to set their retry delays, and Kueue could then use the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue -> kueue: Ignore as not all ACs are done
+  ac1 ->> kube: Set AC to retry with 2m delay
+  deactivate ac1
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue -> kueue: Wait for 2m
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This diagram shows the alternative flow where admission check one sets the state to retry with delay, and Kueue ignores it initially. Only after admission check two returns a result does Kueue take action and start to evict the workload. However, this approach has significant disadvantages:
+
+**Breaking Backwards Compatibility**: The behavior fundamentally changes from the current implementation. Where Kueue would evict or reject a workload immediately upon the first admission check failure, it would now wait for all admission checks to respond. This could affect existing workload behaviors and expectations.
+
+**Risk of Stuck Workloads**: If one of the admission check controllers becomes unresponsive or is unable to set the status, a workload would be stuck indefinitely in the admission phase. This also means that the quota reservation would be held forever, potentially starving other workloads in the queue. This creates a new failure mode that doesn't exist in the current implementation.
+
+Due to these significant drawbacks, this alternative was rejected in favor of the solution described before.
+
+### Documentation Requirements
+
+To ensure proper implementation of admission check controllers, the following documentation should be added:
+
+- **Admission Check Flow Guide**: Detailed documentation on the expected flow and behavior of admission checks, including:
+
+  - Proper handling of state transitions
+  - Best practices for setting retry delays
+  - Guidelines on when and how to update admission check states
+  - Examples of correct implementation patterns
+
+- **Migration Guide**: Step-by-step instructions for migrating from the old `requeueState` mechanism to the new per-admission-check retry system
+
+### Test Plan
+
+#### Observability
+
+The following metrics will be added:
+
+- admission_retry_delay_seconds
+- admission_retry_total
+- workload_admission_retry_duration_seconds
+- workloads_pending_admission_retry
+- admission_late_updates_total
```

  As mentioned in another comment, I would defer it for Beta, or handle independently.

- **keps/3258-delayed-admission-check-retries/README.md:416** by [@mimowo](https://github.com/mimowo) — 2025-09-02T14:39:58Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+#### Solution: Delayed Processing and State Reconciliation
+
+To address these issues, the implementation will be changed to allow admission check updates after a workload has been evicted. When an admission check state is set to `Retry` or `Ready`, after eviction, Kueue should reset the state to `Pending` and defer any immediate action. Instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12 + 13). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. The rest of the flow remains the same as before. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue --> kueue: Don't retry as retry after is not done (in 1m)
+  ac1 ->> kube: Set AC to retry with 2m delay
+  kueue --> kueue: Don't retry as retry after is not done (in 2m)
+  deactivate ac1
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+##### Caveats
+
+In the new case, there's still a race condition if one admission check is fast and doesn't set any delay, which means that the workload will be retries immediately.
+However, this shouldn't be a problem, since the update can be posted at any time. This means the second admission check will eventually set the retry with a delay, ensuring it's not reworked immediately again.
+That said, it still means we go through a couple of unnecessary cycles, which should be okay.
+
+#### Alternative: Wait for All Admission Checks
+
+One alternative approach would be to have Kueue wait for all admission checks to complete before taking any eviction action. This would ensure that all admission checks have the opportunity to set their retry delays, and Kueue could then use the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue -> kueue: Ignore as not all ACs are done
+  ac1 ->> kube: Set AC to retry with 2m delay
+  deactivate ac1
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue -> kueue: Wait for 2m
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This diagram shows the alternative flow where admission check one sets the state to retry with delay, and Kueue ignores it initially. Only after admission check two returns a result does Kueue take action and start to evict the workload. However, this approach has significant disadvantages:
+
+**Breaking Backwards Compatibility**: The behavior fundamentally changes from the current implementation. Where Kueue would evict or reject a workload immediately upon the first admission check failure, it would now wait for all admission checks to respond. This could affect existing workload behaviors and expectations.
+
+**Risk of Stuck Workloads**: If one of the admission check controllers becomes unresponsive or is unable to set the status, a workload would be stuck indefinitely in the admission phase. This also means that the quota reservation would be held forever, potentially starving other workloads in the queue. This creates a new failure mode that doesn't exist in the current implementation.
+
+Due to these significant drawbacks, this alternative was rejected in favor of the solution described before.
+
+### Documentation Requirements
+
+To ensure proper implementation of admission check controllers, the following documentation should be added:
+
+- **Admission Check Flow Guide**: Detailed documentation on the expected flow and behavior of admission checks, including:
+
+  - Proper handling of state transitions
+  - Best practices for setting retry delays
+  - Guidelines on when and how to update admission check states
+  - Examples of correct implementation patterns
+
+- **Migration Guide**: Step-by-step instructions for migrating from the old `requeueState` mechanism to the new per-admission-check retry system
+
+### Test Plan
+
+#### Observability
+
+The following metrics will be added:
+
+- admission_retry_delay_seconds
+- admission_retry_total
+- workload_admission_retry_duration_seconds
+- workloads_pending_admission_retry
+- admission_late_updates_total
+
+#### Unit Tests
+
+- Test retry delay calculation with single and multiple admission checks
+- Test backward compatibility with existing retry behavior
+
+#### Integration tests
+
+- End-to-end test with external admission check setting retry delays
+- Test interaction between multiple admission checks with different delays
+- Test workload flow through retry cycles with delays
+- Performance tests to ensure no regression in scheduling throughput
+- Test that the workload state remains consistent when admission checks post updates after workload eviction
+- Verify that late admission check updates are properly handled without corrupting the state machine
+
```

  The Graduation Criteria section should be added back. The initial proposal (might require more iterations):
  
  Alpha:
  - ACRetryAfter feature gate in alpha (disabled by default)
  - implement support for handling the RetryAfterSeconds and RetryCount fields
  - initial documentation page
  
  Beta:
  - ACRetryAfter feature gate in beta (enabled by default)
  - fix all reported bugs
  - re-evaluate the migration of the ProvisioningRequest to the new API
  
  GA:
  - ACRetryAfter feature gate in stable (locked and enabled by default)
  - fix all reported bugs

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-04T12:29:49Z

- **keps/3258-delayed-admission-check-retries/README.md:225** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-04T12:29:49Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
```

  Yes, you're right. I think kueue should handle such cases in a better and more predictive way.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-04T15:20:17Z

- **keps/3258-delayed-admission-check-retries/README.md:374** by [@mimowo](https://github.com/mimowo) — 2025-09-04T15:20:17Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+#### Solution: Delayed Processing and State Reconciliation
+
+To address these issues, the implementation will be changed to allow admission check updates after a workload has been evicted. When an admission check state is set to `Retry` or `Ready`, after eviction, Kueue should reset the state to `Pending` and defer any immediate action. Instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12 + 13). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. The rest of the flow remains the same as before. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue --> kueue: Don't retry as retry after is not done (in 1m)
+  ac1 ->> kube: Set AC to retry with 2m delay
```

  > Maybe Kueue could reset all AdmissionChecks when assigning quota instead of eviction, wdyt?
  > This sounds like a great idea 🙂 
  
  Wait, what is the problem this is solving?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-04T15:57:06Z

- **keps/3258-delayed-admission-check-retries/README.md:155** by [@mimowo](https://github.com/mimowo) — 2025-09-04T15:57:06Z
```diff
@@ -0,0 +1,566 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RetryAfterSeconds
+    //
+    // +optional
+    RetryAfterSeconds *metav1.Duration `json:"retryAfterSeconds,omitempty"`
```

  Maybe `RequeueAfterSeconds` better captures the intention. Because it corresponds to the period between `Eviction=true` and `Requeued=true`, is my understanding correct?
  
  EDIT: but I don't have a strong view here, both can be justified, just exploring what is your view here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-04T16:07:44Z

- **keps/3258-delayed-admission-check-retries/README.md:219** by [@mimowo](https://github.com/mimowo) — 2025-09-04T16:07:44Z
```diff
@@ -0,0 +1,566 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RetryAfterSeconds
+    //
+    // +optional
+    RetryAfterSeconds *metav1.Duration `json:"retryAfterSeconds,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: Evicted
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:10:00Z"
+
+  requeueState:
+    requeueAt: "2024-02-07T00:10:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfterSeconds: 50400 // in 14h, which is approx midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfterSeconds: 480 # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfterSeconds set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfterSeconds` across all checks
+   - Set `.status.requeueState.requeueAt` based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+2. The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+   - Respect retry delays when processing workloads
+   - Ensure workloads are not scheduled before their retry time has elapsed
```

  We don't need to detail really. Or it could go maybe other "Implementation details" subsection or "Design details", to make it clear this is low-level stuff.
  
  Also, I'm not sure what is behind this statement "Respect retry delays when processing workloads". I believe we could just rely on the Requeued=True being the guard to scheduling the workload, without changes directly to scheduler.go

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-04T16:33:15Z

- **keps/3258-delayed-admission-check-retries/README.md:374** by [@mimowo](https://github.com/mimowo) — 2025-09-04T16:33:15Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+#### Solution: Delayed Processing and State Reconciliation
+
+To address these issues, the implementation will be changed to allow admission check updates after a workload has been evicted. When an admission check state is set to `Retry` or `Ready`, after eviction, Kueue should reset the state to `Pending` and defer any immediate action. Instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12 + 13). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. The rest of the flow remains the same as before. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue --> kueue: Don't retry as retry after is not done (in 1m)
+  ac1 ->> kube: Set AC to retry with 2m delay
```

  Oh, I see, is this attempt to solve this: https://github.com/kubernetes-sigs/kueue/pull/6210/files#diff-feb8cf5e3c07ee341c0be57b132a5a85193b9d1e2c8a9db5f320dbc707fe3db4R221-R224? 
  
  Ah, I didn't know this is important to wait for multiple ACs to report the time. Indeed, currently "first one wins".
  
  Ok, so, let's see:
  - we don't want to delay setting `Evicted=true`, because we should release quota ASAP
  - however, if we reset the state of the admissionChecks on setting Evicted=True (as currently), then we lose the information 
  - we would use the `Requeued=true` condition as the gate for "retry" to admit (requeue)
  
  So, I think we should actually defer resetting the admission checks (if RequeueAfterSeconds is supplied) until setting Requeued=true, because once we have Requeued=True, then we need to ignore bumping the time any longer. We could even reject such requests by validation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-04T17:07:37Z

- **keps/3258-delayed-admission-check-retries/README.md:223** by [@mimowo](https://github.com/mimowo) — 2025-09-04T17:07:37Z
```diff
@@ -0,0 +1,566 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RetryAfterSeconds
+    //
+    // +optional
+    RetryAfterSeconds *metav1.Duration `json:"retryAfterSeconds,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: Evicted
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:10:00Z"
+
+  requeueState:
+    requeueAt: "2024-02-07T00:10:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfterSeconds: 50400 // in 14h, which is approx midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfterSeconds: 480 # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfterSeconds set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfterSeconds` across all checks
+   - Set `.status.requeueState.requeueAt` based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+2. The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+   - Respect retry delays when processing workloads
+   - Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
```

  ```suggestion
  The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
  ```
  The fact that it evicts is ok, and is here to stay, so that we can release quota ASAP. We just need to ensure the information **when to retry** is not reset at the moment of eviction.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-04T17:16:31Z

- **keps/3258-delayed-admission-check-retries/README.md:345** by [@mimowo](https://github.com/mimowo) — 2025-09-04T17:16:31Z
```diff
@@ -0,0 +1,566 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RetryAfterSeconds
+    //
+    // +optional
+    RetryAfterSeconds *metav1.Duration `json:"retryAfterSeconds,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: Evicted
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:10:00Z"
+
+  requeueState:
+    requeueAt: "2024-02-07T00:10:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfterSeconds: 50400 // in 14h, which is approx midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfterSeconds: 480 # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfterSeconds set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfterSeconds` across all checks
+   - Set `.status.requeueState.requeueAt` based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+2. The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+   - Respect retry delays when processing workloads
+   - Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+#### Solution: Delayed Processing and State Reconciliation
+
+To address these issues, the implementation will be changed to allow admission check updates after a workload has been evicted. When an admission check state is set to `Retry` or `Ready`, after eviction, Kueue should reset the state to `Pending` and defer any immediate action. Instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12 + 13). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. The rest of the flow remains the same as before. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
```

  I would suggest to modify the approach as proposed in  https://github.com/kubernetes-sigs/kueue/pull/6210/files#r2322751787 (and move this one to alternatives potentially).
  
  The main points:
  - we set Evicted=True as soon as there is any AC status with state=Retry
  - if the AC1 has `RequeueAfterSeconds` specified, then we defer resetting the AC states to Pending, and keep them as retry. Kueue uses the value to compute the requeueAt
  
  Now, consider scenario when AC1 wants to retry after 60s, but AC2 after 120s, then we have two scenarios:
  1. requeueAt (60s) elapses before AC2 reports, Kueue sets Requeued=True, and rejects attempts to set Retry for ACs (while QuotaReserved=False)
  2. AC2 reports `RequeueAfterSeconds: 120` while Requeued=False, then Kueue updates the RequeuedAt to the new value and waits. Once requeueAt elapses it resets the admission checks states.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-04T17:18:48Z

- **keps/3258-delayed-admission-check-retries/README.md:122** by [@mimowo](https://github.com/mimowo) — 2025-09-04T17:18:48Z
```diff
@@ -0,0 +1,566 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RetryAfterSeconds
+    //
+    // +optional
+    RetryAfterSeconds *metav1.Duration `json:"retryAfterSeconds,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: Evicted
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:10:00Z"
+
+  requeueState:
+    requeueAt: "2024-02-07T00:10:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfterSeconds: 50400 // in 14h, which is approx midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfterSeconds: 480 # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfterSeconds set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfterSeconds` across all checks
+   - Set `.status.requeueState.requeueAt` based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+2. The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+   - Respect retry delays when processing workloads
+   - Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
```

  The content from here up to `#### Solution: Delayed Processing and State Reconciliation` in my opinion is not "Design Details" , or even "Implementation details" - this is more of the status quo analysis, and potential pitfalls when designing. I think it should go to another section, just linked from Design Details, probably Notes or Caveats.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-05T08:39:07Z

- **keps/3258-delayed-admission-check-retries/README.md:155** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-05T08:39:07Z
```diff
@@ -0,0 +1,566 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RetryAfterSeconds
+    //
+    // +optional
+    RetryAfterSeconds *metav1.Duration `json:"retryAfterSeconds,omitempty"`
```

  I like the idea. It would also make the feature’s guarantees clearer. Right now, it sounds like the workload will be retried at this point, but there’s no guarantee. It’s just requeued.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-05T08:53:52Z

- **keps/3258-delayed-admission-check-retries/README.md:219** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-05T08:53:52Z
```diff
@@ -0,0 +1,566 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RetryAfterSeconds
+    //
+    // +optional
+    RetryAfterSeconds *metav1.Duration `json:"retryAfterSeconds,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: Evicted
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:10:00Z"
+
+  requeueState:
+    requeueAt: "2024-02-07T00:10:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfterSeconds: 50400 // in 14h, which is approx midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfterSeconds: 480 # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfterSeconds set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfterSeconds` across all checks
+   - Set `.status.requeueState.requeueAt` based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+2. The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+   - Respect retry delays when processing workloads
+   - Ensure workloads are not scheduled before their retry time has elapsed
```

  I've removed this section. It does not provide any value here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-05T12:01:14Z

- **keps/3258-delayed-admission-check-retries/README.md:234** by [@mimowo](https://github.com/mimowo) — 2025-09-05T12:01:14Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
```

  The point is **correct** on its own, but since it does not require extra work it is a bit misleading (imo).
  
  At least I hope that with the recent changes to the KEP, like using the Requeued=true condition, we don't need the changes, and we can rely on the scheduling being gated by the condition. 
  
  Daniel, let us know if some changes are still needed. Even if needed, I'm not sure the technical details need to necessarily surface the KEP level.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-05T12:08:35Z

- **keps/3258-delayed-admission-check-retries/README.md:374** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-05T12:08:35Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
+
+### Problem: Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+##### What happens if the second AC updates it's state at the wrong time?
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+#### Solution: Delayed Processing and State Reconciliation
+
+To address these issues, the implementation will be changed to allow admission check updates after a workload has been evicted. When an admission check state is set to `Retry` or `Ready`, after eviction, Kueue should reset the state to `Pending` and defer any immediate action. Instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12 + 13). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. The rest of the flow remains the same as before. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue --> kueue: Don't retry as retry after is not done (in 1m)
+  ac1 ->> kube: Set AC to retry with 2m delay
```

  The idea makes sense to me. It would ensure we have a clear contract and make it easier to tell when an admission check controller is allowed to update the state and when it isn’t.
  
  I’ve updated the #delayed-processing-and-state-reconciliation section of the doc to reflect it.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-05T12:12:26Z

- **keps/3258-delayed-admission-check-retries/README.md:234** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-05T12:12:26Z
```diff
@@ -0,0 +1,545 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Example: Workload Status with Multiple Admission Checks](#example-workload-status-with-multiple-admission-checks)
+  - [Controller Changes](#controller-changes)
+  - [Scheduler Changes](#scheduler-changes)
+  - [Problem: Race Condition Between Admission Check Updates and Workload Eviction](#problem-race-condition-between-admission-check-updates-and-workload-eviction)
+      - [What happens if the second AC updates it's state at the wrong time?](#what-happens-if-the-second-ac-updates-its-state-at-the-wrong-time)
+    - [Solution: Delayed Processing and State Reconciliation](#solution-delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+    - [Alternative: Wait for All Admission Checks](#alternative-wait-for-all-admission-checks)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Observability](#observability)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there is a shared `.status.requeueState` field that was introduced specifically for the provisioning request admission check. However, as this field is shared between all admission checks, it will lead to concurrency issues if used more widely. Additionally, the current approach does not provide a way to configure different retry delays for different admission checks.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Migrate the existing `requeueState` mechanism to the new per-admission-check system
+- Provide helper libraries for common retry strategies (fixed delay, exponential backoff)
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `retryAfter`: A timestamp indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `retryAfter` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: The new fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RetryAfter indicates when this check should be retried.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will not re-evaluate until this time has passed.
+    //
+    // Example: To retry in 5 minutes:
+    //   retryAfter = &metav1.Time{Time: time.Now().Add(5*time.Minute)}
+    //
+    // +optional
+    RetryAfter *metav1.Time `json:"retryAfter,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // Controllers can use this for retry strategies:
+    // - Exponential backoff: delay = baseDelay * (2^retryCount)
+    // - Linear backoff: delay = baseDelay * retryCount
+    // - Max retries: if retryCount > limit, set State=Rejected
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+#### Example: Workload Status with Multiple Admission Checks
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  conditions:
+    - type: QuotaReserved
+      status: "True"
+      lastTransitionTime: "2024-02-06T10:00:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      retryAfter: "2024-02-07T00:00:00Z" # Retry at midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      retryAfter: "2024-02-06T10:38:00Z" # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:30:00Z"
+      message: "License server temporarily unavailable"
+      # No retryAfter set - will retry immediately
+# In this example, Kueue will wait until 00:00:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+### Controller Changes
+
+1. **Workload Controller** (`pkg/controller/core/workload_controller.go`):
+
+   - When processing admission checks in `Retry` state, calculate the maximum `retryAfter` across all checks
+   - Set workload requeue time based on the calculated delay
+   - Increment `retryCount` when transitioning back to `Pending`
+   - Remove the existing `requeueState` logic and migrate to per-admission-check retry handling
+
+2. **Helper Functions**:
+
+   - Add utilities for calculating backoff delays (similar to existing `UpdateRequeueState` in `pkg/workload/workload.go`)
+   - Provide standard implementations for fixed delay and exponential backoff strategies
+
+### Scheduler Changes
+
+The scheduler (`pkg/scheduler/scheduler.go`) will need updates to:
+
+- Respect retry delays when processing workloads
+- Ensure workloads are not scheduled before their retry time has elapsed
```

  I’ve removed the whole section since I agree it was very confusing. I’m also not sure it’s even still required, so it’s definitely just an implementation detail.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-09-05T13:47:56Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@PBundyra](https://github.com/PBundyra) — 2025-09-05T13:47:56Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  Do we update the eviction time every time a different admission check fails? If not I think this will be a bit inaccurate
  
  Imagine we have two admission checks:
  1. One fails and sets RequeueAfter: 1h and the other is still running
  2. 30mins passes by and the other admission check fails and sets RequeueAfter for 1h
  3. Ideally the workload should not be retried for the next 1h and I wonder if we support it with the current proposal

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-05T14:03:07Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@mimowo](https://github.com/mimowo) — 2025-09-05T14:03:06Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  @PBundyra do you have an alternative proposal?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-09-05T14:12:18Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@PBundyra](https://github.com/PBundyra) — 2025-09-05T14:12:18Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  One way would be to introduce a timestamp as a part of `AdmissionCheckState` and then Workload controller could iterate over sum timestamp+requeueAfter. The field could be set by webhook
  
  EDIT: we already have this field as a part of `AdmissionCheckState` so just rely on that and sum Retry Timestamp + RequeueAfterSeconds instead of Eviction timestamp + RequeueAfterSeconds

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-05T14:28:15Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@mimowo](https://github.com/mimowo) — 2025-09-05T14:28:15Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  yeah, so basically `max(Retry LastTransitionTime + RequeueAfterSeconds)` instead of `max(Eviction time + `RequeueAfterSeconds)`. 
  
  This looks like a good idea. In case of a single AC it does not matter, but in case of multiple ACs it seems easier to use. 
  
  wdyt @dhenkel92 ? If no objections, I would suggest to update as it seems simpler to use.
  
  Related side note: currently the LastTransitionTime is set by the controller, I think we should revisit that and set it by Kueue if not supplied by the controller. Could you add it as a Graduation point:
  "Re-evaluate setting the LastTranstionTime by Kueue in webhook if not supplied by external controller". This would eliminate any potential issues with clock skew.

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-05T15:55:53Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-05T15:55:53Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  I think it would make sense if we update the LastTransitionTime in a admission webhook. Otherwise, my initial proposal of using `RequeueAfter` instead of `RequeueAfterSeconds` makes more sense.
  
  I'm wondering if there are other cases where the transition time would be updated, but I can't think of any good right now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-05T16:01:42Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@mimowo](https://github.com/mimowo) — 2025-09-05T16:01:42Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  In both proposals a new extra field is needed. And they have 1:1 mapping. I still think it is more readable to use deltas than two timestamps. Plus we may use the webhook in near future, but I wouldn't block the first iteration on that
  
  So I like 
  
  Last transition time: now
  RequeueAfterSecondd

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-05T16:32:50Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-05T16:32:49Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  >  I still think it is more readable to use deltas than two timestamps.
  
  I wouldn't agree with that. IMO a timestamp, like `RequeueAter: <timestamp>` is way more straightforward and doesn't rely on user knowledge to understand what's happening.
  
  However, I see the point of synchronized clocks. Having Kueue to maintain the LastTransitionTime and the calculation is way more reliable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-08T09:02:08Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@mimowo](https://github.com/mimowo) — 2025-09-08T09:02:08Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  I still prefer delta- based time because:
  1. Timeouts aren't really that natural as people will operate in different time-zones, and re-computing is a mental exercise when debugging
  2. Also, computing deltas between "now" and "requeueAfter" is a mental exercise when debugging
  3. The standard approach for configuring requeueAfter in k8s (both controller runtime, and core k8s)
  4. Once we set LastTransitionTime: now via webhook we completely eliminate the clock skew issues, otherwise we are locked in them

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-09-08T12:32:24Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@dhenkel92](https://github.com/dhenkel92) — 2025-09-08T12:32:23Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  In general, I think we can continue using delta durations, especially when we’re setting the last transition time in an admission webhook.
  
  For 1) and 2), it probably depends on what you’re debugging. If you’re interested in the actual delay calculation, like the final offset, you’re right. However, if you’re more interested in the chain of events, you now have to do the computation in your head again.
  
  For 3) and 4), do you have an example for me? I’m just interested in general. With my very quick search, I only found Leases, where we actually have two timestamps:
  
  ```
  apiVersion: coordination.k8s.io/v1
  kind: Lease
  metadata:
    ...
  spec:
    acquireTime: "2025-07-03T08:58:16.272738Z"
    holderIdentity:xxx
    leaseDurationSeconds: 15
    leaseTransitions: 154
    renewTime: "2025-07-03T13:27:59.600258Z"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-08T16:28:04Z

- **keps/3258-delayed-admission-check-retries/README.md:267** by [@mimowo](https://github.com/mimowo) — 2025-09-08T16:28:04Z
```diff
@@ -0,0 +1,556 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+<!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   Eviction time + RequeueAfterSeconds
```

  > For 3) and 4), do you have an example for me? 
  
  here is defined: https://github.com/kubernetes-sigs/controller-runtime/blob/b473d3c1891e589b004c68239b30cdf7215473d2/pkg/reconcile/reconcile.go#L44-L46
  
  used in Kueue: https://github.com/kubernetes-sigs/kueue/blob/307b5fc662827f5d18f3b710a5f724cd922cc600/pkg/controller/core/leader_aware_reconciler.go#L87
  
  In k8s usage example https://github.com/kubernetes/kubernetes/blob/447ca5ff024a0ba7511577f1afe16130d997a9b2/pkg/controller/job/job_controller.go#L966
  
  but yeah, these are not API-facing, just in-memory. 
  
  The HTTP "Retry-After" supports both time and delta: https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Retry-After
  
  >  I only found Leases, where we actually have two timestamps:
  
  interesting case, didn't know seems like a mix of both delta and time. Actually, note that in our case the `requeueAt` would also be determining the actual time still.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-30T16:17:38Z

@dhenkel92 Sorry for the delayed review comments.
I left 2 comments, otherwise LGTM.

Thank you!

- **keps/3258-delayed-admission-check-retries/README.md:275** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T16:12:12Z
```diff
@@ -0,0 +1,588 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  * [Goals](#goals)
+  * [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  * [User Stories](#user-stories)
+    + [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    + [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    + [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  * [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    + [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  * [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  * [API Changes](#api-changes)
+    + [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  * [Documentation Requirements](#documentation-requirements)
+  * [Test Plan](#test-plan)
+    + [Unit Tests](#unit-tests)
+    + [Integration tests](#integration-tests)
+  * [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  * [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+
+<!-- tocstop -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+  <!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // lastTransitionTime is the last time the condition transitioned from one status to another.
+    // This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
+    // +required
+    LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   lastTransitionTime + RequeueAfterSeconds
+    //
+    // +optional
+    RequeueAfterSeconds *metav1.Duration `json:"requeueAfterSeconds,omitempty"`
```

  Will this be reset once Worklaod is admitted or check succeeds the same as retryCount?

- **keps/3258-delayed-admission-check-retries/README.md:338** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T16:14:45Z
```diff
@@ -0,0 +1,588 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  * [Goals](#goals)
+  * [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  * [User Stories](#user-stories)
+    + [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    + [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    + [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  * [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    + [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  * [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  * [API Changes](#api-changes)
+    + [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  * [Documentation Requirements](#documentation-requirements)
+  * [Test Plan](#test-plan)
+    + [Unit Tests](#unit-tests)
+    + [Integration tests](#integration-tests)
+  * [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  * [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+
+<!-- tocstop -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+  <!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // lastTransitionTime is the last time the condition transitioned from one status to another.
+    // This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
+    // +required
+    LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   lastTransitionTime + RequeueAfterSeconds
+    //
+    // +optional
+    RequeueAfterSeconds *metav1.Duration `json:"requeueAfterSeconds,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+To get the final timestamp, Kueue adds the relative RequeueAfterSeconds duration to the LastTransitionTime field from the spec.
+
+**Example: Workload Status with Multiple Admission Checks**
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  requeueState:
+    requeueAt: "2024-02-07T00:10:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:10:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      requeueAfterSeconds: 50400 // in 14h, which is approx midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:11:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      requeueAfterSeconds: 480 # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:20:00Z"
+      message: "License server temporarily unavailable"
+      # No requeueAfterSeconds set - will retry immediately
+# In this example, Kueue will wait until 00:10:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+#### Delayed Processing and State Reconciliation
+
+To address the issues mentioned in [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction), the implementation will be changed to allow admission check updates until all available delays have expired. The workload should still be evicted immediately to release all reserved quota. However, Kueue will no longer reset the admission check states right away. Instead, it will wait until `max(AdmissionCheckState.lastTransitionTime + AdmissionCheckState.RequeueAfterSeconds)` is reached. Only then will the admission checks be reset to pending, and the workload marked as `Requeued=true`.
```

  > Instead, it will wait until `max(AdmissionCheckState.lastTransitionTime + AdmissionCheckState.RequeueAfterSeconds)` is reached.
  
  We may want to select other algorightms like `MIN` / `MID` or something else. But, for sure, we can consider those later once we face the concreate use cases.

- **keps/3258-delayed-admission-check-retries/README.md:500** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T16:15:46Z
```diff
@@ -0,0 +1,588 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  * [Goals](#goals)
+  * [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  * [User Stories](#user-stories)
+    + [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    + [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    + [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  * [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    + [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  * [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  * [API Changes](#api-changes)
+    + [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  * [Documentation Requirements](#documentation-requirements)
+  * [Test Plan](#test-plan)
+    + [Unit Tests](#unit-tests)
+    + [Integration tests](#integration-tests)
+  * [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  * [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+
+<!-- tocstop -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+  <!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // lastTransitionTime is the last time the condition transitioned from one status to another.
+    // This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
+    // +required
+    LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   lastTransitionTime + RequeueAfterSeconds
+    //
+    // +optional
+    RequeueAfterSeconds *metav1.Duration `json:"requeueAfterSeconds,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+To get the final timestamp, Kueue adds the relative RequeueAfterSeconds duration to the LastTransitionTime field from the spec.
+
+**Example: Workload Status with Multiple Admission Checks**
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  requeueState:
+    requeueAt: "2024-02-07T00:10:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:10:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      requeueAfterSeconds: 50400 // in 14h, which is approx midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:11:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      requeueAfterSeconds: 480 # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:20:00Z"
+      message: "License server temporarily unavailable"
+      # No requeueAfterSeconds set - will retry immediately
+# In this example, Kueue will wait until 00:10:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+#### Delayed Processing and State Reconciliation
+
+To address the issues mentioned in [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction), the implementation will be changed to allow admission check updates until all available delays have expired. The workload should still be evicted immediately to release all reserved quota. However, Kueue will no longer reset the admission check states right away. Instead, it will wait until `max(AdmissionCheckState.lastTransitionTime + AdmissionCheckState.RequeueAfterSeconds)` is reached. Only then will the admission checks be reset to pending, and the workload marked as `Requeued=true`.
+
+Furthermore, instead of scheduling a timer in `pkg/controller/core/workload_controller.go:807` that sets the requeuing in the future, the controller should rely on the Reconcile function for managing delays. Whenever the workload gets re-submitted to be reconciled, it should search for the latest delay across all admission checks. If the calculated retry time is in the future, the reconciler should reprocess the workload with a delay. If it's in the past, the reconciler should add the workload back into the scheduling rotation, by setting the `Requeued=true` condition.
+
+![This is how the reconciler would work high level](reconciler-flow.png "Reconciler flow")
+
+This diagram shows the updated approach where admission check one sets the state to Retry with a delay of one minute (step 10), and Kueue evicts the workload (step 12). Crucially, admission check two is able to push its update even after the workload has been evicted, and Kueue will take this new delay into account. Only after all available retry delays have expired Kueue will set the `Requeued=true` condition, which then triggers the actual requeuing of the workload. This approach ensures that all admission checks have the opportunity to set their retry delays, and Kueue will respect the maximum delay across all checks.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Evict Workload
+  activate kueue
+  kueue --> kueue: Don't retry as retry after is not done (in 1m)
+  ac1 ->> kube: Set AC to retry with 2m delay
+  kueue --> kueue: Don't retry as retry after is not done (in 2m)
+  deactivate ac1
+  kueue -> kueue: Set Requeued=true condition on Workload
+  kueue -> kueue: Reset admission check status
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+##### Caveats
+
+In the new case, there's still a race condition if one admission check is fast and doesn't set any delay, which means that the workload will be retries immediately.
+However, this shouldn't be a problem, since the update can be posted at any time. This means the second admission check will eventually set the retry with a delay, ensuring it's not reworked immediately again.
+That said, it still means we go through a couple of unnecessary cycles, which should be okay.
+
+### Documentation Requirements
+
+To ensure proper implementation of admission check controllers, the following documentation should be added:
+
+- **Admission Check Flow Guide**: Detailed documentation on the expected flow and behavior of admission checks, including:
+  - Proper handling of state transitions
+  - Best practices for setting retry delays
+  - Guidelines on when and how to update admission check states
+  - Examples of correct implementation patterns
+
+- **Migration Guide**: Step-by-step instructions for migrating from the old `requeueState` mechanism to the new per-admission-check retry system
+
+### Test Plan
+
+#### Unit Tests
+
+- Test retry delay calculation with single and multiple admission checks
+- Test backward compatibility with existing retry behavior
+
+#### Integration tests
+
+- End-to-end test with external admission check setting retry delays
+- Test interaction between multiple admission checks with different delays
+- Test workload flow through retry cycles with delays
+- Test that the workload state remains consistent when admission checks post updates after workload eviction
+- Verify that late admission check updates are properly handled without corrupting the state machine
+
+### Graduation Criteria
+
+**Alpha:**
+
+- `ACRequeueAfter` feature gate (disabled by default in alpha)
+- rework delaying mechanism to support multiple updates to `requeueState.requeueAt`
+- add new fields (`RequeueAfterSeconds` and `RetryCount`) to admission check state
+- update kueue logic to take new admission check state filed into account
+- first iteration of admission check documentation
+
+**Beta:**
+
+- `ACRequeueAfter` feature gate in beta (enabled by default)
+- fix all reported bugs
+- enhance admission check documentation based on community feedback
+- re-evaluate the migration of the `ProvisioningRequest` to the new API
+- add metrics to track retry behavior
+
+**GA:**
+
+- `ACRequeueAfter` feature gate in stable (locked and enabled by default)
+- fix all reported bugs
+
+## Implementation History
+
+- 2025-07-21: Initial KEP proposal
+
+## Drawbacks
+
+- Adds complexity to the admission check state machine
+- Potential for workloads to be delayed for extended periods
+- Will break strict FIFO ordering guarantees in some scenarios
+
+## Future outlook
+
+With this KEP, each admission check controller must implement its own delay logic, leading to redundant effort and inconsistent behavior.
+
+To reduce this friction, a future extension could add automatic retry delay handling directly in Kueue. This would involve enhancing the `AdmissionCheck` CRD with a configurable retry delay, such as:
+
+```
+type AdmissionCheckSpec struct {
+  Retry *AdmissionCheckRetry
+}
+
+type AdmissionCheckRetryStrategy string
+
+const (
+  AdmissionCheckRetryStrategyStatic  AdmissionCheckRetryStrategy = "static"
+  AdmissionCheckRetryStrategyBackoff AdmissionCheckRetryStrategy = "backoff"
+)
+
+type AdmissionCheckRetry struct {
+  Strategy  AdmissionCheckRetryStrategy
+  BaseDelay time.Duration
+  Faktor    *float64
+  Jitter    *float64
+}
+```
+
+When an admission check includes this configuration and sets its state to Retry, Kueue would compute the delay and populate the `requeueAfterSeconds`` field defined in this KEP.
+
+## Alternatives
+
+1. **Global retry configuration per AdmissionCheck**: Reactivate and enhance the deprecated `retryDelayMinutes` field in the AdmissionCheck spec. This was rejected because:
+   - It doesn't allow for dynamic retry strategies
+   - All workloads would use the same fixed delay
+
+2. **Shared retry configuration**: Continue using the existing `requeueState` mechanism. This was rejected because:
+   - It creates race conditions between multiple admission checks
+   - Only one admission check can effectively control the retry delay
+
+3. **Separate AdmissionCheckRetryState status field**: Add a new top-level status field to the Workload that contains all retry-related information for admission checks:
+
+   ```go
+   type WorkloadStatus struct {
+       // Existing fields...
+
+       // New field for retry state
+       AdmissionCheckRetryState *AdmissionCheckRetryState `json:"admissionCheckRetryState,omitempty"`
+   }
+
+   type AdmissionCheckRetryState struct {
+       // Map of admission check name to retry information
+       RetryInfo map[string]RetryInfo `json:"retryInfo"`
```

  This is just a comment for the future.
  Basically, the map fields are not strongly recommended. So, if we want to store arbitrary parameters, we should consider other forms.

- **keps/3258-delayed-admission-check-retries/kep.yaml:29** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T16:17:01Z
```diff
@@ -0,0 +1,33 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-09-04
+reviewers:
+  - "@mimowo"
+  - "@PBundyra"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: "v0.14"
+
+milestone:
+  alpha: "v0.14"
+  beta: TBD
+  stable: TBD
+
+# List the feature gate name and the components for which it must be enabled
+feature-gates: {}
```

  @mimowo Shouldn't we introduce FG so that we can immediately disable this feature once we observe critical bugs?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-30T16:32:52Z

- **keps/3258-delayed-admission-check-retries/kep.yaml:29** by [@mimowo](https://github.com/mimowo) — 2025-10-30T16:32:51Z
```diff
@@ -0,0 +1,33 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-09-04
+reviewers:
+  - "@mimowo"
+  - "@PBundyra"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: "v0.14"
+
+milestone:
+  alpha: "v0.14"
+  beta: TBD
+  stable: TBD
+
+# List the feature gate name and the components for which it must be enabled
+feature-gates: {}
```

  We could introduce DelayedAdmissionCheckRetry, but I would go with that directly to Beta. 
  
  The reason is that the feature would impact requeue only if an external controller sets RequeueAfterSeconds, so users could just choose not to set that.
  
  I think it is more important to guard the refactoring of ProvisioniongRequest to migrate to the new mechanism. I'm not sure if we want separate gate or reuse. Probably we could reuse.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-30T16:36:53Z

- **keps/3258-delayed-admission-check-retries/kep.yaml:29** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T16:36:53Z
```diff
@@ -0,0 +1,33 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-09-04
+reviewers:
+  - "@mimowo"
+  - "@PBundyra"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: "v0.14"
+
+milestone:
+  alpha: "v0.14"
+  beta: TBD
+  stable: TBD
+
+# List the feature gate name and the components for which it must be enabled
+feature-gates: {}
```

  OK, here I don't have a strong opinion for the dedicated FG, DelayedAdmissionCheckRetry.
  For ProvisioningRequest retry mechanism migration, let's consider that afterwards.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-30T16:38:51Z

- **keps/3258-delayed-admission-check-retries/kep.yaml:25** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T16:38:52Z
```diff
@@ -0,0 +1,33 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-09-04
+reviewers:
+  - "@mimowo"
+  - "@PBundyra"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: "v0.14"
+
+milestone:
+  alpha: "v0.14"
+  beta: TBD
```

  ```suggestion
    alpha: ""
    beta: "v0.14"
  ```
  
  As we discussed in https://github.com/kubernetes-sigs/kueue/pull/6210#discussion_r2478721513, we would like to go directly with Beta.
  
  Indeed, this introduces a new API field, and we do not have any way to disable the API.
  
  cc @mimowo

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-30T16:54:44Z

- **keps/3258-delayed-admission-check-retries/kep.yaml:25** by [@mimowo](https://github.com/mimowo) — 2025-10-30T16:54:44Z
```diff
@@ -0,0 +1,33 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-09-04
+reviewers:
+  - "@mimowo"
+  - "@PBundyra"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: "v0.14"
+
+milestone:
+  alpha: "v0.14"
+  beta: TBD
```

  Yes, but `beta: "v0.15"` now

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-30T17:12:19Z

- **keps/3258-delayed-admission-check-retries/kep.yaml:25** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T17:12:19Z
```diff
@@ -0,0 +1,33 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-09-04
+reviewers:
+  - "@mimowo"
+  - "@PBundyra"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: "v0.14"
+
+milestone:
+  alpha: "v0.14"
+  beta: TBD
```

  Ah, good point. Thanks.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-10-30T18:03:49Z

- **keps/3258-delayed-admission-check-retries/kep.yaml:29** by [@mimowo](https://github.com/mimowo) — 2025-10-30T18:03:49Z
```diff
@@ -0,0 +1,33 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-09-04
+reviewers:
+  - "@mimowo"
+  - "@PBundyra"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: "v0.14"
+
+milestone:
+  alpha: "v0.14"
+  beta: TBD
+  stable: TBD
+
+# List the feature gate name and the components for which it must be enabled
+feature-gates: {}
```

  ok so I think we can skip for now and make the decision based on the impl. I can also add that retrospectively when Daniel is ooo if we conclude we need it

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-30T18:08:49Z

- **keps/3258-delayed-admission-check-retries/kep.yaml:29** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T18:08:49Z
```diff
@@ -0,0 +1,33 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-09-04
+reviewers:
+  - "@mimowo"
+  - "@PBundyra"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: "v0.14"
+
+milestone:
+  alpha: "v0.14"
+  beta: TBD
+  stable: TBD
+
+# List the feature gate name and the components for which it must be enabled
+feature-gates: {}
```

  SGTM

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-30T18:13:33Z

- **keps/3258-delayed-admission-check-retries/README.md:338** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-30T18:13:33Z
```diff
@@ -0,0 +1,588 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  * [Goals](#goals)
+  * [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  * [User Stories](#user-stories)
+    + [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    + [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    + [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  * [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    + [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  * [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  * [API Changes](#api-changes)
+    + [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  * [Documentation Requirements](#documentation-requirements)
+  * [Test Plan](#test-plan)
+    + [Unit Tests](#unit-tests)
+    + [Integration tests](#integration-tests)
+  * [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  * [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+
+<!-- tocstop -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+  <!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // lastTransitionTime is the last time the condition transitioned from one status to another.
+    // This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
+    // +required
+    LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   lastTransitionTime + RequeueAfterSeconds
+    //
+    // +optional
+    RequeueAfterSeconds *metav1.Duration `json:"requeueAfterSeconds,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+To get the final timestamp, Kueue adds the relative RequeueAfterSeconds duration to the LastTransitionTime field from the spec.
+
+**Example: Workload Status with Multiple Admission Checks**
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  requeueState:
+    requeueAt: "2024-02-07T00:10:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:10:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      requeueAfterSeconds: 50400 // in 14h, which is approx midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:11:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      requeueAfterSeconds: 480 # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:20:00Z"
+      message: "License server temporarily unavailable"
+      # No requeueAfterSeconds set - will retry immediately
+# In this example, Kueue will wait until 00:10:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+#### Delayed Processing and State Reconciliation
+
+To address the issues mentioned in [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction), the implementation will be changed to allow admission check updates until all available delays have expired. The workload should still be evicted immediately to release all reserved quota. However, Kueue will no longer reset the admission check states right away. Instead, it will wait until `max(AdmissionCheckState.lastTransitionTime + AdmissionCheckState.RequeueAfterSeconds)` is reached. Only then will the admission checks be reset to pending, and the workload marked as `Requeued=true`.
```

  That's a good idea 👍 
  We should consider it in the follow up KEPs

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-30T18:19:21Z

- **keps/3258-delayed-admission-check-retries/README.md:275** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-30T18:19:21Z
```diff
@@ -0,0 +1,588 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  * [Goals](#goals)
+  * [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  * [User Stories](#user-stories)
+    + [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    + [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    + [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  * [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    + [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  * [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  * [API Changes](#api-changes)
+    + [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  * [Documentation Requirements](#documentation-requirements)
+  * [Test Plan](#test-plan)
+    + [Unit Tests](#unit-tests)
+    + [Integration tests](#integration-tests)
+  * [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  * [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+
+<!-- tocstop -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+  <!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // lastTransitionTime is the last time the condition transitioned from one status to another.
+    // This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
+    // +required
+    LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   lastTransitionTime + RequeueAfterSeconds
+    //
+    // +optional
+    RequeueAfterSeconds *metav1.Duration `json:"requeueAfterSeconds,omitempty"`
```

  That's a good point. I haven't mentioned it in the KEP yet.
  In the current implementation the `RequeueAfterSeconds` would be reset to nil as soon as the workload got requeued.
  Besides that, both new fields will be reset during workload deactivation.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-30T19:15:51Z

- **keps/3258-delayed-admission-check-retries/README.md:275** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T19:15:51Z
```diff
@@ -0,0 +1,588 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  * [Goals](#goals)
+  * [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  * [User Stories](#user-stories)
+    + [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    + [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    + [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  * [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    + [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  * [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  * [API Changes](#api-changes)
+    + [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  * [Documentation Requirements](#documentation-requirements)
+  * [Test Plan](#test-plan)
+    + [Unit Tests](#unit-tests)
+    + [Integration tests](#integration-tests)
+  * [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  * [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+
+<!-- tocstop -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+  <!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // lastTransitionTime is the last time the condition transitioned from one status to another.
+    // This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
+    // +required
+    LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   lastTransitionTime + RequeueAfterSeconds
+    //
+    // +optional
+    RequeueAfterSeconds *metav1.Duration `json:"requeueAfterSeconds,omitempty"`
```

  Thank you for describing that.
  That sounds good.
  
  Please let me know once you update this KEP to align with actual behavior.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-30T19:16:22Z

- **keps/3258-delayed-admission-check-retries/README.md:338** by [@tenzen-y](https://github.com/tenzen-y) — 2025-10-30T19:16:22Z
```diff
@@ -0,0 +1,588 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  * [Goals](#goals)
+  * [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  * [User Stories](#user-stories)
+    + [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    + [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    + [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  * [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    + [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  * [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  * [API Changes](#api-changes)
+    + [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  * [Documentation Requirements](#documentation-requirements)
+  * [Test Plan](#test-plan)
+    + [Unit Tests](#unit-tests)
+    + [Integration tests](#integration-tests)
+  * [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  * [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+
+<!-- tocstop -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+  <!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // lastTransitionTime is the last time the condition transitioned from one status to another.
+    // This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
+    // +required
+    LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   lastTransitionTime + RequeueAfterSeconds
+    //
+    // +optional
+    RequeueAfterSeconds *metav1.Duration `json:"requeueAfterSeconds,omitempty"`
+
+    // RetryCount tracks retry attempts for this admission check.
+    // Kueue automatically increments this when transitioning from
+    // Retry→Pending. Reset to 0 when check succeeds or workload
+    // is admitted.
+    //
+    // +optional
+    RetryCount *int32 `json:"retryCount,omitempty"`
+}
+```
+
+To get the final timestamp, Kueue adds the relative RequeueAfterSeconds duration to the LastTransitionTime field from the spec.
+
+**Example: Workload Status with Multiple Admission Checks**
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: ml-training-job
+  namespace: research
+status:
+  requeueState:
+    requeueAt: "2024-02-07T00:10:00Z"
+
+  admissionChecks:
+    # Check 1: Budget check with daily reset
+    - name: budget-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:10:00Z"
+      message: "Daily budget exhausted. Will retry at midnight."
+      requeueAfterSeconds: 50400 // in 14h, which is approx midnight
+      retryCount: 1
+
+    # Check 2: GPU availability with exponential backoff
+    - name: gpu-availability
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:11:00Z"
+      message: "No A100 GPUs available. Retrying in 8m (attempt 4)"
+      requeueAfterSeconds: 480 # 8 minutes from now
+      retryCount: 4
+
+    # Check 3: License server check - immediate retry
+    - name: license-check
+      state: Retry
+      lastTransitionTime: "2024-02-06T10:20:00Z"
+      message: "License server temporarily unavailable"
+      # No requeueAfterSeconds set - will retry immediately
+# In this example, Kueue will wait until 00:10:00Z (the longest delay)
+# before re-evaluating all admission checks
+```
+
+#### Delayed Processing and State Reconciliation
+
+To address the issues mentioned in [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction), the implementation will be changed to allow admission check updates until all available delays have expired. The workload should still be evicted immediately to release all reserved quota. However, Kueue will no longer reset the admission check states right away. Instead, it will wait until `max(AdmissionCheckState.lastTransitionTime + AdmissionCheckState.RequeueAfterSeconds)` is reached. Only then will the admission checks be reset to pending, and the workload marked as `Requeued=true`.
```

  Absolutely, right 🥇

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-31T08:55:54Z

- **keps/3258-delayed-admission-check-retries/kep.yaml:25** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-31T08:55:54Z
```diff
@@ -0,0 +1,33 @@
+title: Delayed Admission Check Retries
+kep-number: 3258
+authors:
+  - "@dhenkel92"
+status: provisional
+creation-date: 2025-09-04
+reviewers:
+  - "@mimowo"
+  - "@PBundyra"
+approvers:
+  - "@mimowo"
+
+# The target maturity stage. Valid values are:
+# - "alpha"
+# - "beta"
+# - "stable"
+# To determine the maturity level for the feature please see:
+# https://github.com/kubernetes-sigs/kueue/blob/main/docs/concepts/features.md#feature-gates-and-maturity
+stage: alpha
+
+latest-milestone: "v0.14"
+
+milestone:
+  alpha: "v0.14"
+  beta: TBD
```

  Updated

### Review by [@dhenkel92](https://github.com/dhenkel92) (commented) — 2025-10-31T08:57:03Z

- **keps/3258-delayed-admission-check-retries/README.md:275** by [@dhenkel92](https://github.com/dhenkel92) — 2025-10-31T08:57:03Z
```diff
@@ -0,0 +1,588 @@
+# KEP-3258: Delayed Admission Check Retries
+
+<!-- toc -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  * [Goals](#goals)
+  * [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  * [User Stories](#user-stories)
+    + [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    + [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    + [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  * [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    + [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  * [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  * [API Changes](#api-changes)
+    + [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  * [Documentation Requirements](#documentation-requirements)
+  * [Test Plan](#test-plan)
+    + [Unit Tests](#unit-tests)
+    + [Integration tests](#integration-tests)
+  * [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  * [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+
+<!-- tocstop -->
+
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1: External Admission Check with Backoff](#story-1-external-admission-check-with-backoff)
+    - [Story 2: Control Plane Load Management](#story-2-control-plane-load-management)
+    - [Story 3: Multi-tenancy with Different Retry Strategies](#story-3-multi-tenancy-with-different-retry-strategies)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Race Condition Between Admission Check Updates and Workload Eviction](#race-condition-between-admission-check-updates-and-workload-eviction)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+    - [Delayed Processing and State Reconciliation](#delayed-processing-and-state-reconciliation)
+      - [Caveats](#caveats)
+  - [Documentation Requirements](#documentation-requirements)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Future outlook](#future-outlook)
+- [Alternatives](#alternatives)
+  - [Alternative to sync AC results: Wait for All Admission Checks](#alternative-to-sync-ac-results-wait-for-all-admission-checks)
+  <!-- /toc -->
+
+## Summary
+
+This KEP proposes a mechanism to allow external admission check controllers to specify retry delays when marking a workload's admission check state as `Retry`. Currently, when an admission check is set to `Retry`, Kueue immediately evicts the workload and requeues it with minimal delay, which can cause excessive load on external controllers and the control plane.
+
+## Motivation
+
+Kueue supports custom admission checks that allow external controllers to influence workload admission decisions. When an external controller sets an admission check state to `Retry`, Kueue currently:
+
+1. Evicts the workload
+2. Releases the quota reservation
+3. Immediately requeues the workload with minimal delay
+
+This immediate retry behavior can cause several issues:
+
+- Excessive load on external admission check controllers
+- Unnecessary churn in the control plane
+- Inefficient resource utilization when the retry condition is expected to persist for a known duration
+- Artificial reduction of available quotas
+
+Currently, there's a shared `.status.requeueState` field used by both the [provisioning request admission check](https://kueue.sigs.k8s.io/docs/admission-check-controllers/provisioning/#provisioningrequestconfig) and the [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/) feature. It already serves these purposes well but will need some adjustments to support the new use case.
+
+### Goals
+
+- Provide a mechanism for external admission check controllers to specify retry delays
+- Allow each admission check to independently manage its retry timing
+- Support proper coordination of multiple admission checks with different retry delays
+
+### Non-Goals
+
+- Change the behavior of other admission check states (`Ready`, `Rejected`, `Pending`)
+- Reactivate the deprecated `AdmissionCheck.Spec.RetryDelayMinutes` field
+- Modify the FIFO ordering guarantees for workloads without retry delays
+- Change the retry behavior for admission checks that don't specify a delay
+
+## Proposal
+
+Add new fields to the `AdmissionCheckState` status to support per-admission-check retry delays:
+
+- `requeueAfterSeconds`: A duration, based on eviction time, indicating when this admission check should be retried
+- `retryCount`: A counter tracking how many times this admission check has been retried so far
+
+When multiple admission checks are in the `Retry` state with different delays, Kueue will use the maximum delay across all checks and save it into `requeueState.requeueAt`.
+Besides that, the old behavior remains unchanged. When all admission checks are in the `Ready` state, the workload will be admitted; if any of them are in the `Rejected` state, the workload will be rejected.
+
+### User Stories
+
+#### Story 1: External Admission Check with Backoff
+
+As a developer of an external admission check controller that validates cloud resource availability, I want to implement exponential backoff when resources are temporarily unavailable, so that I don't overwhelm the cloud API with requests.
+
+When my controller detects that required cloud resources are not available, it can:
+
+1. Set the admission check state to `Retry`
+2. Calculate an appropriate backoff delay (e.g., 30s, 1m, 2m, 4m)
+3. Set `requeueAfterSeconds` to the calculated future timestamp
+4. Track retry attempts using `retryCount`
+
+#### Story 2: Control Plane Load Management
+
+As a cluster administrator, I have an admission check that validates whether the Kubernetes control plane is healthy before admitting large workloads. When the control plane is under stress, I want workloads to be delayed with longer intervals to allow recovery.
+
+The admission check controller can:
+
+1. Detect control plane stress (high API server latency, etcd performance issues)
+2. Set workloads to `Retry` with delays proportional to the stress level
+3. Prevent thundering herd effects by spreading retry times
+
+#### Story 3: Multi-tenancy with Different Retry Strategies
+
+As a platform engineer supporting multiple teams, I want different admission checks to have independent retry strategies based on their specific requirements without interfering with each other.
+
+For example:
+
+- Security scanning admission check: retry after 5 minutes if scanner is busy
+- Resource provisioning check: retry after 1 minute if capacity is being added
+- Compliance validation check: retry after 10 minutes if external audit service is down
+
+### Notes/Constraints/Caveats
+
+- The retry delay is advisory; the actual delay might be slightly longer due to reconciliation timing
+- Workloads with very long retry delays might appear to be "stuck" to users
+- When multiple admission checks specify different retry delays, the workload will not be retried until all delays have expired
+- The retry mechanism does not guarantee strict FIFO ordering when delays are involved
+- There is a potential race condition where Kueue might reset admission check states before all controllers have set their retry delays
+- The `requeueState.requeueAt` field is used not only by admission checks but also by features like [WaitForPodsReady](https://kueue.sigs.k8s.io/docs/tasks/manage/setup_wait_for_pods_ready/#requeuing-strategy)
+
+#### Race Condition Between Admission Check Updates and Workload Eviction
+
+The current implementation has critical issues with how admission checks are processed. The system uses a small state machine that reacts immediately to changes in the admission check state. When one admission check sets the status to `Retry`, Kueue evicts the workload (and resets the AC state) right away. This immediate action prevents other admission checks from updating their status afterward. If another check sets the status to `Retry` after eviction, it can break Kueue’s internal state. Therefore, the order of events must be strictly followed. The challenge here is that to wait for the maximum time, we need to know all potential delays in advance.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry
+  deactivate ac1
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+The diagram above shows the current flow of a workload with two admission checks:
+
+- Both admission checks will set the state to `Retry`
+- Kueue will take actions after the first one applies its result (steps 10-13)
+- The second admission check should not update its status after the workload got evicted (step 14)
+  - Otherwise, it will cause issues in Kueue's internal state machine
+
+This is a fundamental problem for delayed retries as we can never know the maximum delay if Kueue reacts to the first response.
+
+```mermaid
+sequenceDiagram
+  autonumber
+  actor user as User
+  participant kueue as Kueue
+  participant kube as Kube API Server
+  participant ac1 as Admission Check 1
+  participant ac2 as Admission Check 2
+
+  user ->> kube: Create Workload
+  kube -->> kueue: New WL event
+  activate kueue
+    kueue -> kueue: Reserve Quota
+    kueue -> kueue: Attach admission check information
+    kueue ->> kube: Push WL update
+  deactivate kueue
+  kube -->> ac1: WL update event
+  activate ac1
+  kube -->> ac2: WL update event
+  activate ac2
+  ac1 ->> ac1: Perform checks
+  ac2 ->> ac2: Perform checks
+  ac2 ->> kube: Set AC to retry with 1m delay
+  deactivate ac2
+  kube -->> kueue: WL update event
+  kueue ->> kube: Reset admission check states
+  kueue ->> kube: Evict Workload
+  activate kueue
+  ac1 -x kube: Try set AC to retry with 2m delay
+  deactivate ac1
+  kueue -> kueue: Wait for 1m
+  kueue -> kueue: Requeue Workload
+  kueue -> kueue: Reserve Quota
+  kueue ->> kube: Push WL update
+  deactivate kueue
+```
+
+This updated version of the diagram includes potential delays:
+
+- Admission check 1 sets the status to retry with a delay of one minute (step 10)
+- Kueue will immediately take action and evict the workload (step 12 + 13)
+- Shortly after, admission check 2 wants to set its state to retry too, but can't as the workload was evicted already (step 14)
+  - However, it would have set a higher retry delay than admission check one
+  - This means that we'll always only use the delay of the fastest responding check
+
+**What happens if the second AC updates it's state at the wrong time?**
+
+When the admission check controller is too fast and doesn't implement proper safeguards, it can lead to invalid states where a workload won't be reconsidered by Kueue anymore.
+The following sequence of events illustrates how this issue can eventually break a workload:
+
+1. User creates a workload.
+2. Kueue reserves quota and applies changes in two steps:
+   2.1 Applies a patch to set resource flavour information and sets `QuotaReserved=true`.
+   2.2 Applies an update to `add admission checks to the status in a pending state`.
+3. The admission check controller runs because `QuotaReserved=true` and `AdmissionCheckState=Pending`:
+   3.1 It applies a patch to `set the admission check state to retry`.
+4. Kueue `resets the admission check state to Pending` and sets `Evicted=true` in a single patch.
+5. The admission check controller runs again under the same conditions:
+   5.1 It `sets the admission check state back to retry`.
+6. Kueue does not notice the change and sets `QuotaReserved=false` and `Requeue=false` in a single patch
+   6.1 Evicted remains true, and QuotaReserved is false.
+   6.2 The admission check state stays in Retry.
+7. Kueue sets `Requeued=true`.
+8. Kueue can't reserve quota again due to the invalid state. It tries to schedule the workload but always sees the admission check in a Retry state.
+   8.1 The message on the QuotaReserved status: The workload has failed admission checks.
+
+At this point, the workload should theoretically be picked up by the scheduler again. However, it's ignored because the admission check remains in a Retry state that is never cleared.
+We mitigated this issue by being more cautious with updates in our custom admission checks. Specifically, by avoiding state changes at the wrong time. However, this behavior was not immediately obvious.
+
+### Risks and Mitigations
+
+**Risk**: Breaking backward compatibility with existing admission check controllers\
+**Mitigation**: All fields are optional; controllers that don't set them will maintain current behavior
+
+**Risk**: Workloads appearing stuck due to long retry delays \
+**Mitigation**: Add clear status messages that indicate the retry time, expose metrics to monitor retry delays, and document the recommended maximum delay values.
+
+**Risk**: Resource leakage if workloads are forgotten during long delays \
+**Mitigation**: Ensure proper cleanup mechanisms and consider maximum delay limits
+
+**Risk**: Race conditions when multiple admission checks set delays \
+**Mitigation**: Allow and support admission checks to update the state after a workload has been evicted.
+
+## Design Details
+
+### API Changes
+
+Add new fields to `AdmissionCheckState` in `workload_types.go`:
+
+```go
+type AdmissionCheckState struct {
+    // lastTransitionTime is the last time the condition transitioned from one status to another.
+    // This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
+    // +required
+    LastTransitionTime metav1.Time `json:"lastTransitionTime"`
+
+    // Existing fields remain unchanged...
+
+    // RequeueAfterSeconds indicates how long to wait at least before
+    // retrying to admit the workload.
+    // When State=Retry, admission check controllers should set this
+    // to implement delays between retry attempts.
+    //
+    // If nil when State=Retry, Kueue will retry immediately.
+    // If set, Kueue will add the workload back to the queue after
+    //   lastTransitionTime + RequeueAfterSeconds
+    //
+    // +optional
+    RequeueAfterSeconds *metav1.Duration `json:"requeueAfterSeconds,omitempty"`
```

  I've added [a section](https://github.com/kubernetes-sigs/kueue/pull/6210/files#diff-feb8cf5e3c07ee341c0be57b132a5a85193b9d1e2c8a9db5f320dbc707fe3db4R289-R295) to the API changes chapter.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-10-31T11:57:20Z

Thank you!
/lgtm
/approve
