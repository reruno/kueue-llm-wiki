# PR #623: Add a conditions field to LocalQueueStatus

**Summary**: Add a conditions field to LocalQueueStatus

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/623

**Last updated**: 2023-03-17T17:50:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `add-condition-to-lq` → `main`
- **Created**: 2023-03-13T04:22:29Z
- **Updated**: 2023-03-17T17:50:39Z
- **Merged**: 2023-03-17T17:47:18Z
- **Closed**: 2023-03-17T17:47:18Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `kind/api-change`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 8   **+355 / -36**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
/kind api-change

#### What this PR does / why we need it:
I added a conditions field to LocalQueue to inform users if the clusterQueues that backs localQueues exists.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #545 

#### Special notes for your reviewer:

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-13T04:22:36Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a68e3713a660d1567c18a9648428ccfeb2904df3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6414924d6856820008e1fb17 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-13T05:32:56Z

I'm not sure the reason why prow CI could not start jobs :(

> description: 'Pod can not be created: create pod test-pods/72019b33-c157-11ed-97cb-9a5bca66f65b
    in cluster default: Unauthorized'

https://prow.k8s.io/prowjob?prowjob=72019b33-c157-11ed-97cb-9a5bca66f65b

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-13T09:08:35Z

There seems to be a problem with the prow.

https://github.com/kubernetes/test-infra/issues/29003

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T11:00:23Z

@alculquicondor @denkensk I addressed all comments. PTAL.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T11:00:45Z

/assign @alculquicondor @denkensk

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-15T21:36:59Z

@alculquicondor I modified this PR so that the queue controller watches all types of events about clusterQueue.
PTAL

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:51:24Z

/test pull-kueue-test-integration-main

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T15:52:45Z

> /test pull-kueue-test-integration-main

The error looked legit, and I think it's specific to this PR

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T15:54:22Z

The error message is different:

```
{Expected success, but got an error:
    <*errors.StatusError | 0xc000cb06e0>: 
    Operation cannot be fulfilled on localqueues.kueue.x-k8s.io "queue-test": the object has been modified; please apply your changes to the latest version and try again
    {
        ErrStatus: {
            TypeMeta: {Kind: "", APIVersion: ""},
            ListMeta: {
                SelfLink: "",
                ResourceVersion: "",
                Continue: "",
                RemainingItemCount: nil,
            },
            Status: "Failure",
            Message: "Operation cannot be fulfilled on localqueues.kueue.x-k8s.io \"queue-test\": the object has been modified; please apply your changes to the latest version and try again",
            Reason: "Conflict",
            Details: {
                Name: "queue-test",
                Group: "kueue.x-k8s.io",
                Kind: "localqueues",
                UID: "",
                Causes: nil,
                RetryAfterSeconds: 0,
            },
            Code: 409,
        },
    }
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:55:29Z

> The error message is different:
> 
> ```
> {Expected success, but got an error:
>     <*errors.StatusError | 0xc000cb06e0>: 
>     Operation cannot be fulfilled on localqueues.kueue.x-k8s.io "queue-test": the object has been modified; please apply your changes to the latest version and try again
>     {
>         ErrStatus: {
>             TypeMeta: {Kind: "", APIVersion: ""},
>             ListMeta: {
>                 SelfLink: "",
>                 ResourceVersion: "",
>                 Continue: "",
>                 RemainingItemCount: nil,
>             },
>             Status: "Failure",
>             Message: "Operation cannot be fulfilled on localqueues.kueue.x-k8s.io \"queue-test\": the object has been modified; please apply your changes to the latest version and try again",
>             Reason: "Conflict",
>             Details: {
>                 Name: "queue-test",
>                 Group: "kueue.x-k8s.io",
>                 Kind: "localqueues",
>                 UID: "",
>                 Causes: nil,
>                 RetryAfterSeconds: 0,
>             },
>             Code: 409,
>         },
>     }
> ```

Ah, you're right. That is a typically reconciling error. I will update the test.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-17T15:57:13Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/623#pullrequestreview-1346225431" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/623#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T16:19:41Z

@alculquicondor I fixed an error in integration tests and squashed commits into one.

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-13T06:53:13Z

- **test/integration/webhook/localqueue_test.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-13T06:53:13Z
```diff
@@ -46,10 +46,12 @@ var _ = ginkgo.Describe("Queue validating webhook", func() {
 			gomega.Expect(k8sClient.Create(ctx, obj)).Should(gomega.Succeed())
 
 			ginkgo.By("Updating the Queue")
-			obj.Spec.ClusterQueue = "bar"
-			err := k8sClient.Update(ctx, obj)
-			gomega.Expect(err).Should(gomega.HaveOccurred())
-			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
+			gomega.Eventually(func() error {
+				var updateQ kueue.LocalQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(obj), &updateQ)).Should(gomega.Succeed())
+				updateQ.Spec.ClusterQueue = "bar"
+				return k8sClient.Update(ctx, &updateQ)
+			}, util.Timeout, util.Interval).Should(testing.BeForbiddenError())
```

  To avoid the following errors:
  
  ```shell
  Queue validating webhook when Updating a Queue [It] Should reject the change of spec.clusterQueue
  /Users/tenzen-y/go/src/github.com/kubernetes-sigs/kueue/test/integration/webhook/localqueue_test.go:43
  
    [FAILED] error: Operation cannot be fulfilled on localqueues.kueue.x-k8s.io "queue-test": the object has been modified; please apply your changes to the latest version and try again
    Expected
        <bool>: false
    to be true
    In [It] at: /Users/tenzen-y/go/src/github.com/kubernetes-sigs/kueue/test/integration/webhook/localqueue_test.go:52 @ 03/13/23 14:51:19.95
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-13T18:41:13Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T18:28:06Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  Maybe the condition should just be `"Active"`, similar to what we have in ClusterQueue. And then when Active=false, the `reason` could be "ClusterQueueDoesNotExist"

- **pkg/cache/cache.go:871** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T18:29:21Z
```diff
@@ -868,6 +868,13 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+func (c *Cache) ClusterQueueExists(cqName string) bool {
```

  It would be more accurate to obtain this from the `queue.Manager` instead, as that's what's involved in scheduling.

- **pkg/controller/core/clusterqueue_controller.go:163** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T18:33:23Z
```diff
@@ -160,6 +160,8 @@ func (r *ClusterQueueReconciler) Create(e event.CreateEvent) bool {
 		// No need to interact with the cache for other objects.
 		return true
 	}
+	defer r.notifyWatchers(nil, cq)
```

  make sure existing watchers ignore create events if they didn't need them before

- **pkg/controller/core/localqueue_controller.go:68** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T18:36:07Z
```diff
@@ -52,13 +56,29 @@ func NewLocalQueueReconciler(client client.Client, queues *queue.Manager, cache
 		cache:      cache,
 		client:     client,
 		wlUpdateCh: make(chan event.GenericEvent, updateChBuffer),
+		cqUpdateCh: make(chan event.GenericEvent, updateChBuffer),
 	}
 }
 
 func (r *LocalQueueReconciler) NotifyWorkloadUpdate(w *kueue.Workload) {
 	r.wlUpdateCh <- event.GenericEvent{Object: w}
 }
 
+// NotifyClusterQueueUpdate will listen for the create/delete events of clusterQueues to help
+// verifying whether clusterQueues that backs localQueues exists in the cluster.
```

  Also say that we ignore updates.

- **pkg/controller/core/localqueue_controller.go:232** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T18:39:26Z
```diff
@@ -174,11 +207,50 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+// Since the events come from a channel Source, only the Generic handler will
+// receive events.
+type qCQHandler struct {
+	qManager *queue.Manager
+}
+
+func (h *qCQHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	qKeys := h.qManager.LocalQueuesReferenceClusterQueue(cq.Name)
```

  better list from the client, which has an index

- **test/integration/controller/core/localqueue_controller_test.go:64** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T18:40:15Z
```diff
@@ -63,6 +62,38 @@ var _ = ginkgo.Describe("Queue controller", func() {
 	})
 
 	ginkgo.It("Should update status when workloads are created", func() {
```

  create a separate `It`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-13T18:44:35Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-13T18:44:35Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  Sounds good.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-13T18:45:07Z

- **pkg/cache/cache.go:871** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-13T18:45:06Z
```diff
@@ -868,6 +868,13 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+func (c *Cache) ClusterQueueExists(cqName string) bool {
```

  That makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-13T18:50:16Z

- **pkg/controller/core/clusterqueue_controller.go:163** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-13T18:50:16Z
```diff
@@ -160,6 +160,8 @@ func (r *ClusterQueueReconciler) Create(e event.CreateEvent) bool {
 		// No need to interact with the cache for other objects.
 		return true
 	}
+	defer r.notifyWatchers(nil, cq)
```

  I confirmed that the existing watcher is only for resourceFlavors, and it skips to send a message when it receives create events.
  
  https://github.com/kubernetes-sigs/kueue/blob/f3267deaa2ae65f37675878dfeb679a97cb47d80/pkg/controller/core/resourceflavor_controller.go#L182-L203

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-13T18:51:54Z

- **pkg/controller/core/localqueue_controller.go:68** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-13T18:51:53Z
```diff
@@ -52,13 +56,29 @@ func NewLocalQueueReconciler(client client.Client, queues *queue.Manager, cache
 		cache:      cache,
 		client:     client,
 		wlUpdateCh: make(chan event.GenericEvent, updateChBuffer),
+		cqUpdateCh: make(chan event.GenericEvent, updateChBuffer),
 	}
 }
 
 func (r *LocalQueueReconciler) NotifyWorkloadUpdate(w *kueue.Workload) {
 	r.wlUpdateCh <- event.GenericEvent{Object: w}
 }
 
+// NotifyClusterQueueUpdate will listen for the create/delete events of clusterQueues to help
+// verifying whether clusterQueues that backs localQueues exists in the cluster.
```

  Sure.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T06:44:09Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T06:44:09Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T06:52:09Z

- **pkg/cache/cache.go:871** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T06:52:08Z
```diff
@@ -868,6 +868,13 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+func (c *Cache) ClusterQueueExists(cqName string) bool {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T06:56:35Z

- **pkg/controller/core/localqueue_controller.go:68** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T06:56:35Z
```diff
@@ -52,13 +56,29 @@ func NewLocalQueueReconciler(client client.Client, queues *queue.Manager, cache
 		cache:      cache,
 		client:     client,
 		wlUpdateCh: make(chan event.GenericEvent, updateChBuffer),
+		cqUpdateCh: make(chan event.GenericEvent, updateChBuffer),
 	}
 }
 
 func (r *LocalQueueReconciler) NotifyWorkloadUpdate(w *kueue.Workload) {
 	r.wlUpdateCh <- event.GenericEvent{Object: w}
 }
 
+// NotifyClusterQueueUpdate will listen for the create/delete events of clusterQueues to help
+// verifying whether clusterQueues that backs localQueues exists in the cluster.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T10:04:46Z

- **pkg/controller/core/localqueue_controller.go:232** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T10:04:45Z
```diff
@@ -174,11 +207,50 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+// Since the events come from a channel Source, only the Generic handler will
+// receive events.
+type qCQHandler struct {
+	qManager *queue.Manager
+}
+
+func (h *qCQHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Generic(e event.GenericEvent, q workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	qKeys := h.qManager.LocalQueuesReferenceClusterQueue(cq.Name)
```

  Done.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2023-03-14T10:47:03Z

- **pkg/controller/core/localqueue_controller.go:235** by [@denkensk](https://github.com/denkensk) — 2023-03-14T10:41:53Z
```diff
@@ -174,11 +208,56 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+// Since the events come from a channel Source, only the Generic handler will
+// receive events.
+type qCQHandler struct {
+	client client.Client
+	log    logr.Logger
+}
+
+func (h *qCQHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Generic(e event.GenericEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+
+	var queues kueue.LocalQueueList
+	err := h.client.List(context.Background(), &queues, client.MatchingFields{utilindexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		h.log.Error(err, "listing queues that match the clusterQueue")
+		return
+	} else if len(queues.Items) == 0 {
+		return
+	}
```

  Maybe you can remove these lines because we can check them by the loop below.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T10:51:34Z

- **test/integration/controller/core/localqueue_controller_test.go:64** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T10:51:33Z
```diff
@@ -63,6 +62,38 @@ var _ = ginkgo.Describe("Queue controller", func() {
 	})
 
 	ginkgo.It("Should update status when workloads are created", func() {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T10:52:34Z

- **pkg/controller/core/localqueue_controller.go:235** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T10:52:33Z
```diff
@@ -174,11 +208,56 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+// Since the events come from a channel Source, only the Generic handler will
+// receive events.
+type qCQHandler struct {
+	client client.Client
+	log    logr.Logger
+}
+
+func (h *qCQHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Generic(e event.GenericEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+
+	var queues kueue.LocalQueueList
+	err := h.client.List(context.Background(), &queues, client.MatchingFields{utilindexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		h.log.Error(err, "listing queues that match the clusterQueue")
+		return
+	} else if len(queues.Items) == 0 {
+		return
+	}
```

  It makes sense :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T10:54:31Z

- **pkg/controller/core/localqueue_controller.go:235** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T10:54:31Z
```diff
@@ -174,11 +208,56 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+// Since the events come from a channel Source, only the Generic handler will
+// receive events.
+type qCQHandler struct {
+	client client.Client
+	log    logr.Logger
+}
+
+func (h *qCQHandler) Create(event.CreateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Update(event.UpdateEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Delete(event.DeleteEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) Generic(e event.GenericEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+
+	var queues kueue.LocalQueueList
+	err := h.client.List(context.Background(), &queues, client.MatchingFields{utilindexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		h.log.Error(err, "listing queues that match the clusterQueue")
+		return
+	} else if len(queues.Items) == 0 {
+		return
+	}
```

  Done.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2023-03-14T10:58:26Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@denkensk](https://github.com/denkensk) — 2023-03-14T10:58:25Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  If the related ClusterQueue exists but is not active, the LocalQueue also can't submit new workloads to its related ClusterQueue, right? In this situation, is the LocalQueue active or not? @alculquicondor

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T12:29:55Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T12:29:54Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  IIUC, in that case, the LocalQueue can submit new workloads to related ClusterQueues, although the ClusterQueue can not admit submitted workloads from the LocalQueue.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T12:31:09Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T12:31:08Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  > In this situation, is the LocalQueue active or not?
  
  In my implementation, the controller marks the LocalQueue active.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2023-03-14T13:41:56Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@denkensk](https://github.com/denkensk) — 2023-03-14T13:41:55Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  > IIUC, in that case, the LocalQueue can submit new workloads to related ClusterQueues, although the ClusterQueue can not admit submitted workloads from the LocalQueue.
  
  OK. Thanks. It's better to be documented in the user guide in the future. The same keyword `active` in different objects represents different meanings.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-14T13:52:30Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T13:52:30Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  That's a good point. I think we better not make one depend on the other to avoid dependencies between the controllers.
  
  But yes, it should go in the documentation. Maybe we need a page for "Troubleshooting"

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T14:21:58Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T14:21:58Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  SGTM.
  
  I created a new issue to keep tracking this: #630

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-14T16:04:36Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T16:04:35Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  > I think we better not make one depend on the other to avoid dependencies between the controllers.
  
  Wait, we don't need to add another notifier. The ClusterQueue notifier is enough. We can just check that the ClusterQueue also has the Active condition.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T16:11:35Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T16:11:34Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  > > I think we better not make one depend on the other to avoid dependencies between the controllers.
  > 
  > Wait, we don't need to add another notifier. The ClusterQueue notifier is enough. We can just check that the ClusterQueue also has the Active condition.
  
  Does that mean we copy the Active condition of CQ and paste it into the Active condition of Q?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T16:23:27Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T16:23:26Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  If so, if any resourceFlavors are missing, both Active conditions of the CQ and the Q are marked false.
  IMO, it sounds a bit weird. 
  wdyt?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-14T16:45:45Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T16:45:45Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  > Does that mean we copy the Active condition of CQ and paste it into the Active condition of Q?
  
  Yeah, that should be enough.
  
  > If so, if any resourceFlavors are missing, both Active conditions of the CQ and the Q are marked false.
  IMO, it sounds a bit weird.
  
  How so? Any pending workload in the LocalQueue would remain unschedulable. It's actually better because it's possible that due to RBAC end-users can only see the LocalQueue but not the ClusterQueue.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T17:19:59Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T17:19:59Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  If those active conditions are synchronized, it is difficult for the end user to realize whether the batch admins made a mistake or they (end-users) did.
  
  For example, the controller marks LocalQueue active=false with "ClusterQueueDoesNotExist" in both cases of the end-users pointing to nonexistent ClusterQueue in LocalQueue and batch Admins forgetting to deploy some resourceFlavors.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T17:37:49Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T17:37:48Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  Also, it might be better to have ClusterQueueActive=[true|false] conditions in LocalQueueStstus to inform the end users that related CQ is active.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-14T17:50:33Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T17:50:32Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  You can tell from the Reason. If the ClusterQueue doesn't exist, it will have a different reason than if the ClusterQueue is Inactive.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T18:04:04Z

- **apis/kueue/v1beta1/localqueue_types.go:54** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T18:04:04Z
```diff
@@ -39,8 +39,21 @@ type LocalQueueStatus struct {
 	// admitted to a ClusterQueue and that haven't finished yet.
 	// +optional
 	AdmittedWorkloads int32 `json:"admittedWorkloads"`
+
+	// Conditions hold the latest available observations of the LocalQueue
+	// current state.
+	// +optional
+	// +listType=map
+	// +listMapKey=type
+	Conditions []metav1.Condition `json:"conditions:omitempty"`
 }
 
+const (
+	// ClusterQueueExists indicates clusterQueue that backs this localQueue exists in the cluster and
+	// this localQueue can submit new workloads to its clusterQueue.
+	ClusterQueueExists string = "ClusterQueueExists"
```

  You're right. Sounds good.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T21:34:20Z

- **pkg/controller/core/localqueue_controller.go:112** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T21:34:19Z
```diff
@@ -74,17 +77,49 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
+	oldStatus := queueObj.Status.DeepCopy()
 
 	pending, err := r.queues.PendingWorkloads(&queueObj)
 	if err != nil {
 		r.log.Error(err, "Failed to retrieve localQueue status")
 		return ctrl.Result{}, err
 	}
-
 	queueObj.Status.PendingWorkloads = pending
 	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
+
+	err = r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &kueue.ClusterQueue{})
+	if client.IgnoreNotFound(err) != nil {
+		return ctrl.Result{}, nil
+	}
+	if r.cache.ClusterQueueActive(string(queueObj.Spec.ClusterQueue)) {
+		meta.SetStatusCondition(&queueObj.Status.Conditions, metav1.Condition{
+			Type:    kueue.LocalQueueActive,
+			Status:  metav1.ConditionTrue,
+			Reason:  "Ready",
+			Message: "Can submit new workloads to clusterQueue",
+		})
+	} else {
+		var reason string
+		if errors.IsNotFound(err) {
+			reason = "ClusterQueueDoesNotExist"
+		} else {
+			reason = "ClusterQueueIsInactive"
+		}
+		//if r.queues.ClusterQueueExists(string(queueObj.Spec.ClusterQueue)) {
+		//	reason = "ClusterQueueIsInactive"
+		//} else {
+		//	reason = "ClusterQueueDoesNotExist"
+		//}
```

  @alculquicondor ~~It seems we can not get the clusterQueue when we watch all events for clusterQueue instead of a notifier.~~
  
  https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/623/pull-kueue-test-integration-main/1635751835224510464
  
  ~~So we might need a notifier. WDYT?~~

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T21:58:33Z

- **pkg/controller/core/localqueue_controller.go:112** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T21:58:32Z
```diff
@@ -74,17 +77,49 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
+	oldStatus := queueObj.Status.DeepCopy()
 
 	pending, err := r.queues.PendingWorkloads(&queueObj)
 	if err != nil {
 		r.log.Error(err, "Failed to retrieve localQueue status")
 		return ctrl.Result{}, err
 	}
-
 	queueObj.Status.PendingWorkloads = pending
 	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
+
+	err = r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &kueue.ClusterQueue{})
+	if client.IgnoreNotFound(err) != nil {
+		return ctrl.Result{}, nil
+	}
+	if r.cache.ClusterQueueActive(string(queueObj.Spec.ClusterQueue)) {
+		meta.SetStatusCondition(&queueObj.Status.Conditions, metav1.Condition{
+			Type:    kueue.LocalQueueActive,
+			Status:  metav1.ConditionTrue,
+			Reason:  "Ready",
+			Message: "Can submit new workloads to clusterQueue",
+		})
+	} else {
+		var reason string
+		if errors.IsNotFound(err) {
+			reason = "ClusterQueueDoesNotExist"
+		} else {
+			reason = "ClusterQueueIsInactive"
+		}
+		//if r.queues.ClusterQueueExists(string(queueObj.Spec.ClusterQueue)) {
+		//	reason = "ClusterQueueIsInactive"
+		//} else {
+		//	reason = "ClusterQueueDoesNotExist"
+		//}
```

  ~~The controller could not get a clusterQueue referenced by the queue from either the client or the qManager.~~

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T22:24:12Z

- **pkg/controller/core/localqueue_controller.go:112** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T22:24:12Z
```diff
@@ -74,17 +77,49 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
+	oldStatus := queueObj.Status.DeepCopy()
 
 	pending, err := r.queues.PendingWorkloads(&queueObj)
 	if err != nil {
 		r.log.Error(err, "Failed to retrieve localQueue status")
 		return ctrl.Result{}, err
 	}
-
 	queueObj.Status.PendingWorkloads = pending
 	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
+
+	err = r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &kueue.ClusterQueue{})
+	if client.IgnoreNotFound(err) != nil {
+		return ctrl.Result{}, nil
+	}
+	if r.cache.ClusterQueueActive(string(queueObj.Spec.ClusterQueue)) {
+		meta.SetStatusCondition(&queueObj.Status.Conditions, metav1.Condition{
+			Type:    kueue.LocalQueueActive,
+			Status:  metav1.ConditionTrue,
+			Reason:  "Ready",
+			Message: "Can submit new workloads to clusterQueue",
+		})
+	} else {
+		var reason string
+		if errors.IsNotFound(err) {
+			reason = "ClusterQueueDoesNotExist"
+		} else {
+			reason = "ClusterQueueIsInactive"
+		}
+		//if r.queues.ClusterQueueExists(string(queueObj.Spec.ClusterQueue)) {
+		//	reason = "ClusterQueueIsInactive"
+		//} else {
+		//	reason = "ClusterQueueDoesNotExist"
+		//}
```

  Sorry, this is my bad.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-14T22:27:01Z

- **pkg/controller/core/localqueue_controller.go:112** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-14T22:27:01Z
```diff
@@ -74,17 +77,49 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
+	oldStatus := queueObj.Status.DeepCopy()
 
 	pending, err := r.queues.PendingWorkloads(&queueObj)
 	if err != nil {
 		r.log.Error(err, "Failed to retrieve localQueue status")
 		return ctrl.Result{}, err
 	}
-
 	queueObj.Status.PendingWorkloads = pending
 	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
+
+	err = r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &kueue.ClusterQueue{})
+	if client.IgnoreNotFound(err) != nil {
+		return ctrl.Result{}, nil
+	}
+	if r.cache.ClusterQueueActive(string(queueObj.Spec.ClusterQueue)) {
+		meta.SetStatusCondition(&queueObj.Status.Conditions, metav1.Condition{
+			Type:    kueue.LocalQueueActive,
+			Status:  metav1.ConditionTrue,
+			Reason:  "Ready",
+			Message: "Can submit new workloads to clusterQueue",
+		})
+	} else {
+		var reason string
+		if errors.IsNotFound(err) {
+			reason = "ClusterQueueDoesNotExist"
+		} else {
+			reason = "ClusterQueueIsInactive"
+		}
+		//if r.queues.ClusterQueueExists(string(queueObj.Spec.ClusterQueue)) {
+		//	reason = "ClusterQueueIsInactive"
+		//} else {
+		//	reason = "ClusterQueueDoesNotExist"
+		//}
```

  I just remembered that I need to implement each event handler.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-16T15:52:31Z

- **pkg/controller/core/localqueue_controller.go:38** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:46:54Z
```diff
@@ -33,6 +35,7 @@ import (
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/constants"
+	utilindexer "sigs.k8s.io/kueue/pkg/controller/core/indexer"
```

  no alias if there are no collisions

- **pkg/controller/core/localqueue_controller.go:249** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:50:49Z
```diff
@@ -174,11 +196,75 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{utilindexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.AddAfter(req, constants.UpdatesBatchPeriod)
+	}
+}
+
 // SetupWithManager sets up the controller with the Manager.
 func (r *LocalQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
+	queueCQHandler := qCQHandler{
+		client: r.client,
+	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.LocalQueue{}).
 		Watches(&source.Channel{Source: r.wlUpdateCh}, &qWorkloadHandler{}).
+		Watches(&source.Kind{Type: &kueue.ClusterQueue{}}, &queueCQHandler).
```

  If you are using a Kind source, you should get the information about existence of CQ and status from the client.
  
  If you use a Channel source, you should get all the information from the caches.
  
  This is because a Channel source is based on cache updates, whereas a Kind source is based on client updates.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-16T16:02:18Z

- **pkg/controller/core/localqueue_controller.go:249** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-16T16:02:18Z
```diff
@@ -174,11 +196,75 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{utilindexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.AddAfter(req, constants.UpdatesBatchPeriod)
+	}
+}
+
 // SetupWithManager sets up the controller with the Manager.
 func (r *LocalQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
+	queueCQHandler := qCQHandler{
+		client: r.client,
+	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.LocalQueue{}).
 		Watches(&source.Channel{Source: r.wlUpdateCh}, &qWorkloadHandler{}).
+		Watches(&source.Kind{Type: &kueue.ClusterQueue{}}, &queueCQHandler).
```

  It makes sense. Thank you for clarifying :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-16T16:43:53Z

- **pkg/controller/core/localqueue_controller.go:249** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-16T16:43:53Z
```diff
@@ -174,11 +196,75 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{utilindexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.AddAfter(req, constants.UpdatesBatchPeriod)
+	}
+}
+
 // SetupWithManager sets up the controller with the Manager.
 func (r *LocalQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
+	queueCQHandler := qCQHandler{
+		client: r.client,
+	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.LocalQueue{}).
 		Watches(&source.Channel{Source: r.wlUpdateCh}, &qWorkloadHandler{}).
+		Watches(&source.Kind{Type: &kueue.ClusterQueue{}}, &queueCQHandler).
```

  @alculquicondor Let me make sure my understanding is correct.
  
  In my understanding of your comment below, I should watch all events for clusterQueue in the localQueue controller instead of sending a generic event from the clusterQueue controller to the localQueue controller using the function `NotifyClusterQueueUpdate` function. 
  
  > Wait, we don't need to add another notifier. The ClusterQueue notifier is enough. We can just check that the ClusterQueue also has the Active condition.
  
  https://github.com/kubernetes-sigs/kueue/pull/623#discussion_r1135797721
  
  https://github.com/kubernetes-sigs/kueue/blob/c2bd78b0ce7a3cf6880faaf01f079309ad7324fa/pkg/controller/core/localqueue_controller.go#L67-L80
  
  Perhaps, were you meant just synchronizing active status conditions in localQueue with clusterQueue, not watching all events using kind Source?
  
  This means "another notifier" is not `NotifyClusterQueueUpdate`, right?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-16T18:42:34Z

- **pkg/controller/core/localqueue_controller.go:249** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T18:42:34Z
```diff
@@ -174,11 +196,75 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{utilindexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.AddAfter(req, constants.UpdatesBatchPeriod)
+	}
+}
+
 // SetupWithManager sets up the controller with the Manager.
 func (r *LocalQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
+	queueCQHandler := qCQHandler{
+		client: r.client,
+	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.LocalQueue{}).
 		Watches(&source.Channel{Source: r.wlUpdateCh}, &qWorkloadHandler{}).
+		Watches(&source.Kind{Type: &kueue.ClusterQueue{}}, &queueCQHandler).
```

  > In my understanding of your comment below, I should watch all events for clusterQueue in the localQueue controller instead of sending a generic event from the clusterQueue controller 
  
  Yes, I think that's the simplest solution.
  
  The notifiers are useful when you need to guarantee that the kueue cache is updated before updating the other object. If these cache updates are not reflected in the objects' status somehow, then we need to use notifiers.
  
  But whether a ClusterQueue exists or has an active condition is visible in the status, so we can just use that.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-16T18:53:57Z

- **pkg/controller/core/localqueue_controller.go:249** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-16T18:53:57Z
```diff
@@ -174,11 +196,75 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{utilindexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.AddAfter(req, constants.UpdatesBatchPeriod)
+	}
+}
+
 // SetupWithManager sets up the controller with the Manager.
 func (r *LocalQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
+	queueCQHandler := qCQHandler{
+		client: r.client,
+	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.LocalQueue{}).
 		Watches(&source.Channel{Source: r.wlUpdateCh}, &qWorkloadHandler{}).
+		Watches(&source.Kind{Type: &kueue.ClusterQueue{}}, &queueCQHandler).
```

  Thank you for the response.
  My understanding looks like be correct.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-16T20:31:06Z

- **pkg/controller/core/localqueue_controller.go:249** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-16T20:31:06Z
```diff
@@ -174,11 +196,75 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{utilindexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.AddAfter(req, constants.UpdatesBatchPeriod)
+	}
+}
+
 // SetupWithManager sets up the controller with the Manager.
 func (r *LocalQueueReconciler) SetupWithManager(mgr ctrl.Manager) error {
+	queueCQHandler := qCQHandler{
+		client: r.client,
+	}
 	return ctrl.NewControllerManagedBy(mgr).
 		For(&kueue.LocalQueue{}).
 		Watches(&source.Channel{Source: r.wlUpdateCh}, &qWorkloadHandler{}).
+		Watches(&source.Kind{Type: &kueue.ClusterQueue{}}, &queueCQHandler).
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-16T20:31:12Z

- **pkg/controller/core/localqueue_controller.go:38** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-16T20:31:11Z
```diff
@@ -33,6 +35,7 @@ import (
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/cache"
 	"sigs.k8s.io/kueue/pkg/constants"
+	utilindexer "sigs.k8s.io/kueue/pkg/controller/core/indexer"
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-16T20:51:59Z

- **pkg/controller/core/localqueue_controller.go:89** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T20:47:19Z
```diff
@@ -74,20 +78,36 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
-	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
```

  ```suggestion
  			return ctrl.Result{}, client.IgnoreNotFound(err)
  ```

- **pkg/controller/core/localqueue_controller.go:94** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T20:48:04Z
```diff
@@ -74,20 +78,36 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
-	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
+				"clusterQueue", klog.KRef("", string(queueObj.Spec.ClusterQueue)))
+			// Update status only.
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, "", "", "")
```

  Do not update status in this case, let the reconciler retry later.

- **pkg/controller/core/localqueue_controller.go:99** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T20:48:28Z
```diff
@@ -74,20 +78,36 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
-	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
+				"clusterQueue", klog.KRef("", string(queueObj.Spec.ClusterQueue)))
+			// Update status only.
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, "", "", "")
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	} else {
```

  avoid else by returning above when there is nothing else to do.

- **pkg/controller/core/localqueue_controller.go:104** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T20:48:57Z
```diff
@@ -74,20 +78,36 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
-	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
+				"clusterQueue", klog.KRef("", string(queueObj.Spec.ClusterQueue)))
+			// Update status only.
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, "", "", "")
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	} else {
+		if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to clusterQueue")
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
```

  return directly

- **pkg/controller/core/localqueue_controller.go:254** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T20:50:32Z
```diff
@@ -174,11 +194,104 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{indexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.AddAfter(req, constants.UpdatesBatchPeriod)
```

  I don't expect these updates to happen often, so no need for batching, but also ok to leave.

- **pkg/queue/manager.go:555** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T20:51:37Z
```diff
@@ -551,3 +551,10 @@ func (m *Manager) reportPendingWorkloads(cqName string, cq ClusterQueue) {
 	}
 	metrics.ReportPendingWorkloads(cqName, active, inadmissible)
 }
+
+func (m *Manager) ClusterQueueExists(cqName string) bool {
```

  can revert now

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-16T20:56:17Z

- **pkg/queue/manager.go:555** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-16T20:56:17Z
```diff
@@ -551,3 +551,10 @@ func (m *Manager) reportPendingWorkloads(cqName string, cq ClusterQueue) {
 	}
 	metrics.ReportPendingWorkloads(cqName, active, inadmissible)
 }
+
+func (m *Manager) ClusterQueueExists(cqName string) bool {
```

  Sorry, my mistake.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-16T20:57:57Z

- **pkg/queue/manager.go:555** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-16T20:57:57Z
```diff
@@ -551,3 +551,10 @@ func (m *Manager) reportPendingWorkloads(cqName string, cq ClusterQueue) {
 	}
 	metrics.ReportPendingWorkloads(cqName, active, inadmissible)
 }
+
+func (m *Manager) ClusterQueueExists(cqName string) bool {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-16T21:00:49Z

- **pkg/controller/core/localqueue_controller.go:254** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-16T21:00:48Z
```diff
@@ -174,11 +194,104 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{indexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.AddAfter(req, constants.UpdatesBatchPeriod)
```

  > I don't expect these updates to happen often, so no need for batching
  
  Makes sense. I will modify it so that a queue is added to the workqueue, immediately.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T12:01:50Z

- **pkg/controller/core/localqueue_controller.go:254** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T12:01:50Z
```diff
@@ -174,11 +194,104 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{indexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.AddAfter(req, constants.UpdatesBatchPeriod)
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T12:13:13Z

- **pkg/controller/core/localqueue_controller.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T12:13:13Z
```diff
@@ -74,20 +78,36 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
-	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T12:13:19Z

- **pkg/controller/core/localqueue_controller.go:94** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T12:13:19Z
```diff
@@ -74,20 +78,36 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
-	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
+				"clusterQueue", klog.KRef("", string(queueObj.Spec.ClusterQueue)))
+			// Update status only.
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, "", "", "")
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T12:13:23Z

- **pkg/controller/core/localqueue_controller.go:104** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T12:13:23Z
```diff
@@ -74,20 +78,36 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
-	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
+				"clusterQueue", klog.KRef("", string(queueObj.Spec.ClusterQueue)))
+			// Update status only.
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, "", "", "")
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	} else {
+		if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to clusterQueue")
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T12:13:25Z

- **pkg/controller/core/localqueue_controller.go:99** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T12:13:25Z
```diff
@@ -74,20 +78,36 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
-	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
-		return ctrl.Result{}, client.IgnoreNotFound(err)
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
+				"clusterQueue", klog.KRef("", string(queueObj.Spec.ClusterQueue)))
+			// Update status only.
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, "", "", "")
+			if err != nil {
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	} else {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T12:29:14Z

- **Makefile:148** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T12:29:13Z
```diff
@@ -145,7 +145,7 @@ test: generate fmt vet gotestsum ## Run tests.
 
 .PHONY: test-integration
 test-integration: manifests generate fmt vet envtest ginkgo mpi-operator-crd ## Run tests.
-	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) --arch=amd64 use $(ENVTEST_K8S_VERSION) -p path)" \
+	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" \
```

  Since k8s v1.24, envtest has started supporting arm64 architecture.
  
  ref: https://github.com/kubernetes-sigs/kubebuilder/issues/2664

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T14:31:45Z

- **pkg/controller/core/localqueue_controller.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T14:24:03Z
```diff
@@ -74,22 +78,26 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		} else {
```

  no else needed

- **pkg/controller/core/localqueue_controller.go:82** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T14:24:48Z
```diff
@@ -74,22 +78,26 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
```

  move to a package const

- **pkg/controller/core/localqueue_controller.go:89** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T14:25:32Z
```diff
@@ -74,22 +78,26 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
```

  Doesn't the manager log the returned error? If so, maybe wrapping the error with `%w` could be useful.

- **pkg/controller/core/localqueue_controller.go:97** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T14:26:57Z
```diff
@@ -74,22 +78,26 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
+				"clusterQueue", klog.KRef("", string(queueObj.Spec.ClusterQueue)))
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
+	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
+		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to clusterQueue")
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	} else {
```

  no else needed

- **pkg/controller/core/localqueue_controller.go:152** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T14:27:17Z
```diff
@@ -141,7 +149,7 @@ func (r *LocalQueueReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *LocalQueueReconciler) Generic(e event.GenericEvent) bool {
-	r.log.V(3).Info("Got Workload event", "workload", klog.KObj(e.Object))
+	r.log.V(2).Info("Got generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
```

  Revert

- **pkg/controller/core/localqueue_controller.go:240** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T14:29:02Z
```diff
@@ -174,11 +182,104 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{indexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.Add(req)
```

  nit: inline here

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T14:32:02Z

- **Makefile:148** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T14:32:02Z
```diff
@@ -145,7 +145,7 @@ test: generate fmt vet gotestsum ## Run tests.
 
 .PHONY: test-integration
 test-integration: manifests generate fmt vet envtest ginkgo mpi-operator-crd ## Run tests.
-	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) --arch=amd64 use $(ENVTEST_K8S_VERSION) -p path)" \
+	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" \
```

  Could be a separate PR for better tracking

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T15:08:37Z

- **Makefile:148** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:08:36Z
```diff
@@ -145,7 +145,7 @@ test: generate fmt vet gotestsum ## Run tests.
 
 .PHONY: test-integration
 test-integration: manifests generate fmt vet envtest ginkgo mpi-operator-crd ## Run tests.
-	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) --arch=amd64 use $(ENVTEST_K8S_VERSION) -p path)" \
+	KUBEBUILDER_ASSETS="$(shell $(ENVTEST) use $(ENVTEST_K8S_VERSION) -p path)" \
```

  Makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T15:10:03Z

- **pkg/controller/core/localqueue_controller.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:10:03Z
```diff
@@ -74,22 +78,26 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
```

  You're right.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T15:12:04Z

- **pkg/controller/core/localqueue_controller.go:88** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:12:04Z
```diff
@@ -74,22 +78,26 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		} else {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T15:12:15Z

- **pkg/controller/core/localqueue_controller.go:97** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:12:15Z
```diff
@@ -74,22 +78,26 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
+				"clusterQueue", klog.KRef("", string(queueObj.Spec.ClusterQueue)))
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
 	}
-
-	queueObj.Status.PendingWorkloads = pending
-	queueObj.Status.AdmittedWorkloads = r.cache.AdmittedWorkloadsInLocalQueue(&queueObj)
-	if !equality.Semantic.DeepEqual(oldStatus, queueObj.Status) {
-		err := r.client.Status().Update(ctx, &queueObj)
+	if meta.IsStatusConditionTrue(cq.Status.Conditions, kueue.ClusterQueueActive) {
+		err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionTrue, "Ready", "Can submit new workloads to clusterQueue")
+		return ctrl.Result{}, client.IgnoreNotFound(err)
+	} else {
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T15:12:44Z

- **pkg/controller/core/localqueue_controller.go:152** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:12:44Z
```diff
@@ -141,7 +149,7 @@ func (r *LocalQueueReconciler) Update(e event.UpdateEvent) bool {
 }
 
 func (r *LocalQueueReconciler) Generic(e event.GenericEvent) bool {
-	r.log.V(3).Info("Got Workload event", "workload", klog.KObj(e.Object))
+	r.log.V(2).Info("Got generic event", "obj", klog.KObj(e.Object), "kind", e.Object.GetObjectKind().GroupVersionKind())
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T15:25:05Z

- **pkg/controller/core/localqueue_controller.go:82** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:25:05Z
```diff
@@ -74,22 +78,26 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T15:25:50Z

- **pkg/controller/core/localqueue_controller.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:25:49Z
```diff
@@ -74,22 +78,26 @@ func (r *LocalQueueReconciler) Reconcile(ctx context.Context, req ctrl.Request)
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling LocalQueue")
 
-	// Shallow copy enough for now.
-	oldStatus := queueObj.Status
-
-	pending, err := r.queues.PendingWorkloads(&queueObj)
+	var cq kueue.ClusterQueue
+	queueIsInactiveMsg := "Can't submit new workloads to clusterQueue"
+	err := r.client.Get(ctx, client.ObjectKey{Name: string(queueObj.Spec.ClusterQueue)}, &cq)
 	if err != nil {
-		r.log.Error(err, "Failed to retrieve localQueue status")
-		return ctrl.Result{}, err
+		if apierrors.IsNotFound(err) {
+			err = r.UpdateStatusIfChanged(ctx, &queueObj, metav1.ConditionFalse, "ClusterQueueDoesNotExist", queueIsInactiveMsg)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		} else {
+			r.log.Error(err, "Failed to get a clusterQueue that localQueue references",
```

  I removed the log since the manager log reports a similar message.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T15:31:14Z

- **pkg/controller/core/localqueue_controller.go:240** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T15:31:13Z
```diff
@@ -174,11 +182,104 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{indexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
+		return
+	}
+	for _, q := range queues.Items {
+		req := reconcile.Request{
+			NamespacedName: client.ObjectKeyFromObject(&q),
+		}
+		wq.Add(req)
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T15:57:01Z

/approve

Please look into the flaky test

- **pkg/controller/core/localqueue_controller.go:233** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T15:56:25Z
```diff
@@ -174,11 +180,101 @@ func (h *qWorkloadHandler) Generic(e event.GenericEvent, q workqueue.RateLimitin
 	q.AddAfter(req, constants.UpdatesBatchPeriod)
 }
 
+// qCQHandler signals the controller to reconcile the Queue associated
+// to the workload in the event.
+type qCQHandler struct {
+	client client.Client
+}
+
+func (h *qCQHandler) Create(e event.CreateEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Update(e event.UpdateEvent, wq workqueue.RateLimitingInterface) {
+	newCq, ok := e.ObjectNew.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	oldCq, ok := e.ObjectOld.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	// Iff .status.conditions of the clusterQueue is updated,
+	// this handler sends all queues related to the clusterQueue to workqueue.
+	if equality.Semantic.DeepEqual(oldCq.Status.Conditions, newCq.Status.Conditions) {
+		return
+	}
+	h.addLocalQueueToWorkQueue(newCq, wq)
+}
+
+func (h *qCQHandler) Delete(e event.DeleteEvent, wq workqueue.RateLimitingInterface) {
+	cq, ok := e.Object.(*kueue.ClusterQueue)
+	if !ok {
+		return
+	}
+	h.addLocalQueueToWorkQueue(cq, wq)
+}
+
+func (h *qCQHandler) Generic(event.GenericEvent, workqueue.RateLimitingInterface) {
+}
+
+func (h *qCQHandler) addLocalQueueToWorkQueue(cq *kueue.ClusterQueue, wq workqueue.RateLimitingInterface) {
+	ctx := context.TODO()
+	log := ctrl.LoggerFrom(ctx).WithValues("clusterQueue", klog.KObj(cq))
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	var queues kueue.LocalQueueList
+	err := h.client.List(ctx, &queues, client.MatchingFields{indexer.QueueClusterQueueKey: cq.Name})
+	if err != nil {
+		log.Error(err, "listing queues that match the clusterQueue")
```

  ```suggestion
  		log.Error(err, "Could not list queues that match the clusterQueue")
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-17T16:18:02Z

- **test/integration/webhook/localqueue_test.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-17T16:18:02Z
```diff
@@ -33,8 +33,12 @@ var _ = ginkgo.Describe("Queue validating webhook", func() {
 			gomega.Expect(k8sClient.Create(ctx, obj)).Should(gomega.Succeed())
 
 			ginkgo.By("Updating the Queue status")
-			obj.Status.PendingWorkloads = 3
-			gomega.Expect(k8sClient.Status().Update(ctx, obj)).Should(gomega.Succeed())
+			gomega.Eventually(func() error {
+				var updatedQ kueue.LocalQueue
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(obj), &updatedQ)).Should(gomega.Succeed())
+				updatedQ.Status.PendingWorkloads = 3
+				return k8sClient.Status().Update(ctx, &updatedQ)
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  I updated this test to avoid a typical updating error in reconciliation.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T17:46:06Z

/lgtm

Thanks!
