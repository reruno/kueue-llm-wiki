# PR #627: Commonize reconciler code by generic Job reconciler

**Summary**: Commonize reconciler code by generic Job reconciler

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/627

**Last updated**: 2023-03-18T18:47:26Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `commonize-reconciler2` → `main`
- **Created**: 2023-03-13T11:21:40Z
- **Updated**: 2023-03-18T18:47:26Z
- **Merged**: 2023-03-14T17:10:18Z
- **Closed**: 2023-03-14T17:10:18Z
- **Labels**: `kind/feature`, `lgtm`, `size/XXL`, `tide/merge-method-squash`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@kerthcet](https://github.com/kerthcet)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 15   **+641 / -801**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

1. It cleans up the code by unifying the reconciler code for Job and MPIJob
2. It creates a better API for use of other job frameworks

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #369

#### Special notes for your reviewer:

There will be follow ups:
- rename the directories and files in jobframework (see https://github.com/kubernetes-sigs/kueue/pull/578#discussion_r1124225607)
- commonize indexing and webhooks

I still don't use type aliasing for Job. The reason is that I need the client.Object for event recorders. Alternatively, we could pass the client.Object to the Reconcile function, but it doesn't seem that clean.

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-13T11:21:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1ec37a1d013f8d0979a01d0c90088a4f0468a86f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64109dc137504900081039c6 |

### Comment by [@mimowo](https://github.com/mimowo) — 2023-03-13T11:22:33Z

/retest

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-13T11:24:40Z

@mimowo There seems to be a problem with prow.

https://github.com/kubernetes/test-infra/issues/29003

### Comment by [@mimowo](https://github.com/mimowo) — 2023-03-13T11:26:49Z

/assign @alculquicondor @kerthcet

### Comment by [@mimowo](https://github.com/mimowo) — 2023-03-13T13:29:17Z

cc @mwielgus

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-14T17:09:40Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/627#discussion_r1135924746" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/627#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-13T19:30:36Z

- **pkg/controller/workload/job/job_controller.go:45** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T19:15:24Z
```diff
@@ -40,21 +38,17 @@ import (
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/constants"
 	"sigs.k8s.io/kueue/pkg/controller/workload/jobframework"
-	"sigs.k8s.io/kueue/pkg/workload"
 )
 
 var (
-	ownerKey          = ".metadata.ownerReferences[batch.Job]"
 	parentWorkloadKey = ".metadata.parentWorkload"
+	gvk               = metav1.GroupVersionKind{Group: batchv1.SchemeGroupVersion.Group, Version: batchv1.SchemeGroupVersion.Version, Kind: "Job"}
```

  ```suggestion
  	gvk               = batchv1.SchemeGroupVersion.WithKind("Job")
  ```

- **pkg/controller/workload/job/job_controller.go:155** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T19:21:19Z
```diff
@@ -151,45 +148,41 @@ func (h *parentWorkloadHandler) queueReconcileForChildJob(object client.Object,
 	}
 }
 
-type BatchJob struct {
+type Job struct {
 	batchv1.Job
 }
 
-func (b *BatchJob) Object() client.Object {
+func (b *Job) Object() client.Object {
```

  `j` instead of `b`

- **pkg/controller/workload/job/job_controller.go:90** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T19:22:54Z
```diff
@@ -93,13 +87,16 @@ func NewReconciler(
 	for _, opt := range opts {
 		opt(&options)
 	}
+	genericJobReconciler := jobframework.NewReconciler(scheme,
```

  if you make `Job` an alias, you might not need to embed the reconciler.
  
  you could do something like:
  
  ```golang
  return jobframework.NewReconciler[Job](...)
  ```

- **pkg/controller/workload/job/job_controller.go:345** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T19:24:33Z
```diff
@@ -355,162 +341,14 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
+	var job batchv1.Job
+	if err := r.client.Get(ctx, req.NamespacedName, &job); err != nil {
```

  This line could move into Reconcile with generics

- **pkg/controller/workload/jobframework/helpers.go:38** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T19:25:24Z
```diff
@@ -28,14 +29,180 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/constants"
 	utilpriority "sigs.k8s.io/kueue/pkg/util/priority"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// GenericJobReconciler reconciles a GenericJob object
+type GenericJobReconciler struct {
```

  just Reconciler.
  Also change the name of the file from helpers into `reconciler.go`

- **pkg/controller/workload/jobframework/helpers.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T19:27:11Z
```diff
@@ -28,14 +29,180 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/constants"
 	utilpriority "sigs.k8s.io/kueue/pkg/util/priority"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// GenericJobReconciler reconciles a GenericJob object
+type GenericJobReconciler struct {
+	client                     client.Client
+	scheme                     *runtime.Scheme
+	record                     record.EventRecorder
+	manageJobsWithoutQueueName bool
+	waitForPodsReady           bool
+}
+
+type options struct {
+	manageJobsWithoutQueueName bool
+	waitForPodsReady           bool
+}
+
+// Option configures the reconciler.
+type Option func(*options)
+
+// WithManageJobsWithoutQueueName indicates if the controller should reconcile
+// jobs that don't set the queue name annotation.
+func WithManageJobsWithoutQueueName(f bool) Option {
+	return func(o *options) {
+		o.manageJobsWithoutQueueName = f
+	}
+}
+
+// WithWaitForPodsReady indicates if the controller should add the PodsReady
+// condition to the workload when the corresponding job has all pods ready
+// or succeeded.
+func WithWaitForPodsReady(f bool) Option {
+	return func(o *options) {
+		o.waitForPodsReady = f
+	}
+}
+
+var defaultOptions = options{}
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...Option) *GenericJobReconciler {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &GenericJobReconciler{
+		scheme:                     scheme,
+		client:                     client,
+		record:                     record,
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		waitForPodsReady:           options.waitForPodsReady,
+	}
+}
+
+func (r *GenericJobReconciler) Reconcile(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
+	namespacedName := types.NamespacedName{Name: job.Object().GetName(), Namespace: job.Object().GetNamespace()}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("job", namespacedName.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	isStandaloneJob := job.ParentWorkloadName() == ""
+
+	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
+	// queue-name or the parent-workload annotation set.
+	if !r.manageJobsWithoutQueueName && job.QueueName() == "" && isStandaloneJob {
+		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
+		return ctrl.Result{}, nil
+	}
+
+	log.V(2).Info("Reconciling Job")
```

  Please share how these log lines look. In particular, I would like to know if it's clear when the log refers to a job or an mpijob. I think the log name should cover it.

- **pkg/controller/workload/jobframework/interface.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-13T19:27:55Z
```diff
@@ -53,8 +53,6 @@ type GenericJob interface {
 	IsActive() bool
 	// PodsReady instructs whether job derived pods are all ready now.
 	PodsReady() bool
-	// GetWorkloadName returns the name of the workload for the job.
-	GetWorkloadName() string
-	// GetOwnerKey returns a unique key that identifies Workloads owned by the job implementation.
-	GetOwnerKey() string
+	// GetGVK returns GVK (Group Version Kind) for the job.
+	GetGVK() *metav1.GroupVersionKind
```

  ```suggestion
  	GetGVK() metav1.GroupVersionKind
  ```

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-03-14T07:26:15Z

- **pkg/controller/workload/jobframework/helpers.go:296** by [@kerthcet](https://github.com/kerthcet) — 2023-03-14T07:16:42Z
```diff
@@ -121,9 +288,20 @@ func EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request,
 	return match, nil
 }
 
-// StartJob will unsuspend the job, and also inject the node affinity.
-func StartJob(ctx context.Context, client client.Client, record record.EventRecorder, job GenericJob, wl *kueue.Workload) error {
-	nodeSelectors, err := GetNodeSelectors(ctx, client, wl)
+// equivalentToWorkload checks if the job corresponds to the workload
+func (r *GenericJobReconciler) equivalentToWorkload(job GenericJob, wl *kueue.Workload) bool {
+	owner := metav1.GetControllerOf(wl)
+	// Indexes don't work in unit tests, so we explicitly check for the
+	// owner here.
+	if owner.Name != job.Object().GetName() {
```

  This is duplicated in `EnsureOneWorkload`, I think we can avoid this like https://github.com/kubernetes-sigs/kueue/blob/48156ad74e2cb4598076fdab747789cacdca23ed/pkg/util/testing/client.go#L42-L43

- **pkg/controller/workload/job/job_controller.go:112** by [@kerthcet](https://github.com/kerthcet) — 2023-03-14T07:26:03Z
```diff
@@ -151,45 +148,41 @@ func (h *parentWorkloadHandler) queueReconcileForChildJob(object client.Object,
 	}
 }
 
-type BatchJob struct {
+type Job struct {
```

  Should we change this to a general name `Job`? Considering we may want to change the folder `workload` to `job` in the future.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T08:57:59Z

- **pkg/controller/workload/job/job_controller.go:45** by [@mimowo](https://github.com/mimowo) — 2023-03-14T08:57:59Z
```diff
@@ -40,21 +38,17 @@ import (
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
 	"sigs.k8s.io/kueue/pkg/constants"
 	"sigs.k8s.io/kueue/pkg/controller/workload/jobframework"
-	"sigs.k8s.io/kueue/pkg/workload"
 )
 
 var (
-	ownerKey          = ".metadata.ownerReferences[batch.Job]"
 	parentWorkloadKey = ".metadata.parentWorkload"
+	gvk               = metav1.GroupVersionKind{Group: batchv1.SchemeGroupVersion.Group, Version: batchv1.SchemeGroupVersion.Version, Kind: "Job"}
```

  Done, for that I had to change the package for GVK from metav1 to schema.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T08:58:20Z

- **pkg/controller/workload/job/job_controller.go:155** by [@mimowo](https://github.com/mimowo) — 2023-03-14T08:58:20Z
```diff
@@ -151,45 +148,41 @@ func (h *parentWorkloadHandler) queueReconcileForChildJob(object client.Object,
 	}
 }
 
-type BatchJob struct {
+type Job struct {
 	batchv1.Job
 }
 
-func (b *BatchJob) Object() client.Object {
+func (b *Job) Object() client.Object {
```

  Renamed to `job` actually.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T09:20:20Z

- **pkg/controller/workload/job/job_controller.go:90** by [@mimowo](https://github.com/mimowo) — 2023-03-14T09:20:20Z
```diff
@@ -93,13 +87,16 @@ func NewReconciler(
 	for _, opt := range opts {
 		opt(&options)
 	}
+	genericJobReconciler := jobframework.NewReconciler(scheme,
```

  Well, I've tried to make the alias for `type Job batchv1.Job`, but then I couldn't use the types interchangeably. 
  In particular, I cannot use the alias as `client.Client` needed for the requests and event recording. 
  
  I'm happy to use the alias, but I will need more input to progress, as I hit issues.
  
  As for now, it turns out I don't need embedding of the reconciler without generics.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T09:21:28Z

- **pkg/controller/workload/job/job_controller.go:345** by [@mimowo](https://github.com/mimowo) — 2023-03-14T09:21:28Z
```diff
@@ -355,162 +341,14 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
+	var job batchv1.Job
+	if err := r.client.Get(ctx, req.NamespacedName, &job); err != nil {
```

  Moved it into ReconcileGenericJob. I'm happy to do the reconciler using generics, but I will need more input.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T09:22:50Z

- **pkg/controller/workload/jobframework/helpers.go:38** by [@mimowo](https://github.com/mimowo) — 2023-03-14T09:22:50Z
```diff
@@ -28,14 +29,180 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/constants"
 	utilpriority "sigs.k8s.io/kueue/pkg/util/priority"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// GenericJobReconciler reconciles a GenericJob object
+type GenericJobReconciler struct {
```

  Renamed the file.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T09:24:50Z

- **pkg/controller/workload/jobframework/helpers.go:112** by [@mimowo](https://github.com/mimowo) — 2023-03-14T09:24:49Z
```diff
@@ -28,14 +29,180 @@ import (
 	"sigs.k8s.io/controller-runtime/pkg/client"
 
 	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/constants"
 	utilpriority "sigs.k8s.io/kueue/pkg/util/priority"
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
-// EnsureOneWorkload will query for the single matched workload corresponding to job and return it.
+// GenericJobReconciler reconciles a GenericJob object
+type GenericJobReconciler struct {
+	client                     client.Client
+	scheme                     *runtime.Scheme
+	record                     record.EventRecorder
+	manageJobsWithoutQueueName bool
+	waitForPodsReady           bool
+}
+
+type options struct {
+	manageJobsWithoutQueueName bool
+	waitForPodsReady           bool
+}
+
+// Option configures the reconciler.
+type Option func(*options)
+
+// WithManageJobsWithoutQueueName indicates if the controller should reconcile
+// jobs that don't set the queue name annotation.
+func WithManageJobsWithoutQueueName(f bool) Option {
+	return func(o *options) {
+		o.manageJobsWithoutQueueName = f
+	}
+}
+
+// WithWaitForPodsReady indicates if the controller should add the PodsReady
+// condition to the workload when the corresponding job has all pods ready
+// or succeeded.
+func WithWaitForPodsReady(f bool) Option {
+	return func(o *options) {
+		o.waitForPodsReady = f
+	}
+}
+
+var defaultOptions = options{}
+
+func NewReconciler(
+	scheme *runtime.Scheme,
+	client client.Client,
+	record record.EventRecorder,
+	opts ...Option) *GenericJobReconciler {
+	options := defaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+
+	return &GenericJobReconciler{
+		scheme:                     scheme,
+		client:                     client,
+		record:                     record,
+		manageJobsWithoutQueueName: options.manageJobsWithoutQueueName,
+		waitForPodsReady:           options.waitForPodsReady,
+	}
+}
+
+func (r *GenericJobReconciler) Reconcile(ctx context.Context, req ctrl.Request, job GenericJob) (ctrl.Result, error) {
+	namespacedName := types.NamespacedName{Name: job.Object().GetName(), Namespace: job.Object().GetNamespace()}
+
+	log := ctrl.LoggerFrom(ctx).WithValues("job", namespacedName.String())
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	isStandaloneJob := job.ParentWorkloadName() == ""
+
+	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
+	// queue-name or the parent-workload annotation set.
+	if !r.manageJobsWithoutQueueName && job.QueueName() == "" && isStandaloneJob {
+		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
+		return ctrl.Result{}, nil
+	}
+
+	log.V(2).Info("Reconciling Job")
```

  This is an example log line for MPI Job: 
  ```
  2023-03-14T10:03:29.470733346+01:00   LEVEL(-2)       jobframework/helpers.go:112     Reconciling Job {"controller": "mpijob", "controllerGroup": "kubeflow.org", "controllerKind": "MPIJob", "MPIJob": {"name":"test-job","namespace":"default"}, "namespace": "default", "name": "test-job", "reconcileID": "8dbde845-2ccf-4341-8fd7-749225243656", "job": "default/test-job"}
  ```
  and for Batch Job:
  ```
    2023-03-14T09:43:40.691102798+01:00   LEVEL(-2)       jobframework/helpers.go:112     Reconciling Job {"controller": "job", "controllerGroup": "batch", "controllerKind": "Job", "Job": {"name":"test-job","namespace":"default"}, "namespace": "default", "name": "test-job", "reconcileID": "414137e7-60b0-4edc-9ff1-753b81ab2186", "job": "default/test-job"}
    ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T09:25:15Z

- **pkg/controller/workload/jobframework/interface.go:57** by [@mimowo](https://github.com/mimowo) — 2023-03-14T09:25:15Z
```diff
@@ -53,8 +53,6 @@ type GenericJob interface {
 	IsActive() bool
 	// PodsReady instructs whether job derived pods are all ready now.
 	PodsReady() bool
-	// GetWorkloadName returns the name of the workload for the job.
-	GetWorkloadName() string
-	// GetOwnerKey returns a unique key that identifies Workloads owned by the job implementation.
-	GetOwnerKey() string
+	// GetGVK returns GVK (Group Version Kind) for the job.
+	GetGVK() *metav1.GroupVersionKind
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T09:35:08Z

- **pkg/controller/workload/job/job_controller.go:112** by [@mimowo](https://github.com/mimowo) — 2023-03-14T09:35:07Z
```diff
@@ -151,45 +148,41 @@ func (h *parentWorkloadHandler) queueReconcileForChildJob(object client.Object,
 	}
 }
 
-type BatchJob struct {
+type Job struct {
```

  I'm ok with either Job or BatchJob. It was @alculquicondor suggestion previously to rename just to Job. This makes sense, from the consistency perspective (we also have MPIJob, not KubeflowMPIJob). 
  As for the folder names, maybe we could have something like:
  ```
  controller/
  ├── jobframework
  ├── jobs/
     ├── mpijob
     ├── job
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T09:42:17Z

- **pkg/controller/workload/job/job_controller.go:90** by [@mimowo](https://github.com/mimowo) — 2023-03-14T09:42:16Z
```diff
@@ -93,13 +87,16 @@ func NewReconciler(
 	for _, opt := range opts {
 		opt(&options)
 	}
+	genericJobReconciler := jobframework.NewReconciler(scheme,
```

  IIUC, say, I create an alias in the `mpijob` package:
  `type MPIJob kubeflow.MPIJob`. Once I pass the type MPIJob to the `jobframwork` package I get compilation errors when trying to use it in methods requiring `client.Object`. I think this is because the `jobframework` package does not depend on the `mpijob` package, so it no longer knows that MPIJob extends `client.Object`. I might be missing something, but not sure how to make this work ATM.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-14T15:54:21Z

just nits because I think we can simplify a tiny bit using generics

- **pkg/controller/workload/job/job_controller.go:112** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:46:58Z
```diff
@@ -151,45 +148,41 @@ func (h *parentWorkloadHandler) queueReconcileForChildJob(object client.Object,
 	}
 }
 
-type BatchJob struct {
+type Job struct {
```

  It should be fine either way.

- **pkg/controller/workload/job/job_controller.go:116** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:47:20Z
```diff
@@ -151,100 +109,96 @@ func (h *parentWorkloadHandler) queueReconcileForChildJob(object client.Object,
 	}
 }
 
-type BatchJob struct {
+type Job struct {
 	batchv1.Job
 }
 
-func (b *BatchJob) Object() client.Object {
-	return &b.Job
+func (job *Job) Object() client.Object {
```

  just `j`? one letter is common for receivers.

- **pkg/controller/workload/job/job_controller.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:51:08Z
```diff
@@ -355,162 +302,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
-	}
-
-	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&batchJob.Job))
-	ctx = ctrl.LoggerInto(ctx, log)
-
-	isStandaloneJob := batchJob.ParentWorkloadName() == ""
-
-	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
-	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && batchJob.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
-	}
-
-	log.V(2).Info("Reconciling Job")
-
-	// 1. make sure there is only a single existing instance of the workload.
-	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := jobframework.EnsureOneWorkload(ctx, r.client, req, r.record, &batchJob)
-	if err != nil {
-		log.Error(err, "Getting existing workloads")
-		return ctrl.Result{}, err
-	}
-
-	// 2. handle job is finished.
-	if condition, finished := batchJob.Finished(); finished {
-		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-			return ctrl.Result{}, nil
-		}
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
-		}
-		return ctrl.Result{}, nil
-	}
-
-	// 3. handle workload is nil.
-	if wl == nil {
-		if !isStandaloneJob {
-			return ctrl.Result{}, nil
-		}
-		err := r.handleJobWithNoWorkload(ctx, &batchJob)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// 4. handle WaitForPodsReady only for a standalone job.
-	if isStandaloneJob {
-		// handle a job when waitForPodsReady is enabled, and it is the main job
-		if r.waitForPodsReady {
-			log.V(5).Info("Handling a job when waitForPodsReady is enabled")
-			condition := generatePodsReadyCondition(&batchJob, wl)
-			// optimization to avoid sending the update request if the status didn't change
-			if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-				log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
-				apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-				err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-				if err != nil {
-					log.Error(err, "Updating workload status")
-				}
-			}
-		}
-	}
-
-	// 5. handle job is suspended.
-	if batchJob.IsSuspend() {
-		// start the job if the workload has been admitted, and the job is still suspended
-		if wl.Status.Admission != nil {
-			log.V(2).Info("Job admitted, unsuspending")
-			err := jobframework.StartJob(ctx, r.client, r.record, &batchJob, wl)
-			if err != nil {
-				log.Error(err, "Unsuspending job")
-			}
-			return ctrl.Result{}, err
-		}
-
-		// update queue name if changed.
-		q := batchJob.QueueName()
-		if wl.Spec.QueueName != q {
-			log.V(2).Info("Job changed queues, updating workload")
-			wl.Spec.QueueName = q
-			err := r.client.Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload queue")
-			}
-			return ctrl.Result{}, err
-		}
-		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
-		return ctrl.Result{}, nil
-	}
-
-	// 6. handle job is unsuspended.
-	if wl.Status.Admission == nil {
-		// the job must be suspended if the workload is not yet admitted.
-		log.V(2).Info("Running job is not admitted by a cluster queue, suspending")
-		err := jobframework.StopJob(ctx, r.client, r.record, &batchJob, wl, "Not admitted by cluster queue")
-		if err != nil {
-			log.Error(err, "Suspending job with non admitted workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// workload is admitted and job is running, nothing to do.
-	log.V(3).Info("Job running with admitted workload, nothing to do")
-	return ctrl.Result{}, nil
-}
-
-func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, batchJob *BatchJob) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	// Wait until there are no active pods.
-	if batchJob.IsActive() {
-		log.V(2).Info("Job is suspended but still has active pods, waiting")
-		return nil
-	}
-
-	// Create the corresponding workload.
-	wl, err := jobframework.ConstructWorkload(ctx, r.client, r.scheme, batchJob)
-	if err != nil {
-		return err
-	}
-	if err = r.client.Create(ctx, wl); err != nil {
-		return err
-	}
-	r.record.Eventf(batchJob.Object(), corev1.EventTypeNormal, "CreatedWorkload",
-		"Created Workload: %v", workload.Key(wl))
-	return nil
-}
-
-func generatePodsReadyCondition(batchJob *BatchJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the the condition when the pods transition
-	// Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if wl.Status.Admission != nil && (batchJob.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-	}
+	fjr := (*jobframework.JobReconciler)(r)
```

  this casting shouldn't be needed

- **pkg/controller/workload/job/job_controller.go:306** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:51:26Z
```diff
@@ -355,162 +302,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
-	}
-
-	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&batchJob.Job))
-	ctx = ctrl.LoggerInto(ctx, log)
-
-	isStandaloneJob := batchJob.ParentWorkloadName() == ""
-
-	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
-	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && batchJob.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
-	}
-
-	log.V(2).Info("Reconciling Job")
-
-	// 1. make sure there is only a single existing instance of the workload.
-	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := jobframework.EnsureOneWorkload(ctx, r.client, req, r.record, &batchJob)
-	if err != nil {
-		log.Error(err, "Getting existing workloads")
-		return ctrl.Result{}, err
-	}
-
-	// 2. handle job is finished.
-	if condition, finished := batchJob.Finished(); finished {
-		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-			return ctrl.Result{}, nil
-		}
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
-		}
-		return ctrl.Result{}, nil
-	}
-
-	// 3. handle workload is nil.
-	if wl == nil {
-		if !isStandaloneJob {
-			return ctrl.Result{}, nil
-		}
-		err := r.handleJobWithNoWorkload(ctx, &batchJob)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// 4. handle WaitForPodsReady only for a standalone job.
-	if isStandaloneJob {
-		// handle a job when waitForPodsReady is enabled, and it is the main job
-		if r.waitForPodsReady {
-			log.V(5).Info("Handling a job when waitForPodsReady is enabled")
-			condition := generatePodsReadyCondition(&batchJob, wl)
-			// optimization to avoid sending the update request if the status didn't change
-			if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-				log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
-				apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-				err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-				if err != nil {
-					log.Error(err, "Updating workload status")
-				}
-			}
-		}
-	}
-
-	// 5. handle job is suspended.
-	if batchJob.IsSuspend() {
-		// start the job if the workload has been admitted, and the job is still suspended
-		if wl.Status.Admission != nil {
-			log.V(2).Info("Job admitted, unsuspending")
-			err := jobframework.StartJob(ctx, r.client, r.record, &batchJob, wl)
-			if err != nil {
-				log.Error(err, "Unsuspending job")
-			}
-			return ctrl.Result{}, err
-		}
-
-		// update queue name if changed.
-		q := batchJob.QueueName()
-		if wl.Spec.QueueName != q {
-			log.V(2).Info("Job changed queues, updating workload")
-			wl.Spec.QueueName = q
-			err := r.client.Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload queue")
-			}
-			return ctrl.Result{}, err
-		}
-		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
-		return ctrl.Result{}, nil
-	}
-
-	// 6. handle job is unsuspended.
-	if wl.Status.Admission == nil {
-		// the job must be suspended if the workload is not yet admitted.
-		log.V(2).Info("Running job is not admitted by a cluster queue, suspending")
-		err := jobframework.StopJob(ctx, r.client, r.record, &batchJob, wl, "Not admitted by cluster queue")
-		if err != nil {
-			log.Error(err, "Suspending job with non admitted workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// workload is admitted and job is running, nothing to do.
-	log.V(3).Info("Job running with admitted workload, nothing to do")
-	return ctrl.Result{}, nil
-}
-
-func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, batchJob *BatchJob) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	// Wait until there are no active pods.
-	if batchJob.IsActive() {
-		log.V(2).Info("Job is suspended but still has active pods, waiting")
-		return nil
-	}
-
-	// Create the corresponding workload.
-	wl, err := jobframework.ConstructWorkload(ctx, r.client, r.scheme, batchJob)
-	if err != nil {
-		return err
-	}
-	if err = r.client.Create(ctx, wl); err != nil {
-		return err
-	}
-	r.record.Eventf(batchJob.Object(), corev1.EventTypeNormal, "CreatedWorkload",
-		"Created Workload: %v", workload.Key(wl))
-	return nil
-}
-
-func generatePodsReadyCondition(batchJob *BatchJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the the condition when the pods transition
-	// Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if wl.Status.Admission != nil && (batchJob.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-	}
+	fjr := (*jobframework.JobReconciler)(r)
+	return fjr.ReconcileGenericJob(ctx, req, &Job{})
```

  did you consider using generics instead?

- **pkg/controller/workload/jobframework/workload_names.go:40** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T15:52:57Z
```diff
@@ -36,19 +37,19 @@ func GetWorkloadNameForOwnerRef(owner *metav1.OwnerReference) (string, error) {
 	if err != nil {
 		return "", err
 	}
-	gvk := metav1.GroupVersionKind{Group: gv.Group, Version: gv.Version, Kind: owner.Kind}
-	return GetWorkloadNameForOwnerWithGVK(owner.Name, &gvk), nil
+	gvk := schema.GroupVersionKind{Group: gv.Group, Version: gv.Version, Kind: owner.Kind}
```

  `gv.WithKind`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T16:06:16Z

- **pkg/controller/workload/job/job_controller.go:306** by [@mimowo](https://github.com/mimowo) — 2023-03-14T16:06:16Z
```diff
@@ -355,162 +302,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
-	}
-
-	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&batchJob.Job))
-	ctx = ctrl.LoggerInto(ctx, log)
-
-	isStandaloneJob := batchJob.ParentWorkloadName() == ""
-
-	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
-	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && batchJob.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
-	}
-
-	log.V(2).Info("Reconciling Job")
-
-	// 1. make sure there is only a single existing instance of the workload.
-	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := jobframework.EnsureOneWorkload(ctx, r.client, req, r.record, &batchJob)
-	if err != nil {
-		log.Error(err, "Getting existing workloads")
-		return ctrl.Result{}, err
-	}
-
-	// 2. handle job is finished.
-	if condition, finished := batchJob.Finished(); finished {
-		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-			return ctrl.Result{}, nil
-		}
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
-		}
-		return ctrl.Result{}, nil
-	}
-
-	// 3. handle workload is nil.
-	if wl == nil {
-		if !isStandaloneJob {
-			return ctrl.Result{}, nil
-		}
-		err := r.handleJobWithNoWorkload(ctx, &batchJob)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// 4. handle WaitForPodsReady only for a standalone job.
-	if isStandaloneJob {
-		// handle a job when waitForPodsReady is enabled, and it is the main job
-		if r.waitForPodsReady {
-			log.V(5).Info("Handling a job when waitForPodsReady is enabled")
-			condition := generatePodsReadyCondition(&batchJob, wl)
-			// optimization to avoid sending the update request if the status didn't change
-			if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-				log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
-				apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-				err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-				if err != nil {
-					log.Error(err, "Updating workload status")
-				}
-			}
-		}
-	}
-
-	// 5. handle job is suspended.
-	if batchJob.IsSuspend() {
-		// start the job if the workload has been admitted, and the job is still suspended
-		if wl.Status.Admission != nil {
-			log.V(2).Info("Job admitted, unsuspending")
-			err := jobframework.StartJob(ctx, r.client, r.record, &batchJob, wl)
-			if err != nil {
-				log.Error(err, "Unsuspending job")
-			}
-			return ctrl.Result{}, err
-		}
-
-		// update queue name if changed.
-		q := batchJob.QueueName()
-		if wl.Spec.QueueName != q {
-			log.V(2).Info("Job changed queues, updating workload")
-			wl.Spec.QueueName = q
-			err := r.client.Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload queue")
-			}
-			return ctrl.Result{}, err
-		}
-		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
-		return ctrl.Result{}, nil
-	}
-
-	// 6. handle job is unsuspended.
-	if wl.Status.Admission == nil {
-		// the job must be suspended if the workload is not yet admitted.
-		log.V(2).Info("Running job is not admitted by a cluster queue, suspending")
-		err := jobframework.StopJob(ctx, r.client, r.record, &batchJob, wl, "Not admitted by cluster queue")
-		if err != nil {
-			log.Error(err, "Suspending job with non admitted workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// workload is admitted and job is running, nothing to do.
-	log.V(3).Info("Job running with admitted workload, nothing to do")
-	return ctrl.Result{}, nil
-}
-
-func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, batchJob *BatchJob) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	// Wait until there are no active pods.
-	if batchJob.IsActive() {
-		log.V(2).Info("Job is suspended but still has active pods, waiting")
-		return nil
-	}
-
-	// Create the corresponding workload.
-	wl, err := jobframework.ConstructWorkload(ctx, r.client, r.scheme, batchJob)
-	if err != nil {
-		return err
-	}
-	if err = r.client.Create(ctx, wl); err != nil {
-		return err
-	}
-	r.record.Eventf(batchJob.Object(), corev1.EventTypeNormal, "CreatedWorkload",
-		"Created Workload: %v", workload.Key(wl))
-	return nil
-}
-
-func generatePodsReadyCondition(batchJob *BatchJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the the condition when the pods transition
-	// Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if wl.Status.Admission != nil && (batchJob.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-	}
+	fjr := (*jobframework.JobReconciler)(r)
+	return fjr.ReconcileGenericJob(ctx, req, &Job{})
```

  Yes, I tried, but could not make this work. I would be happy to leave it as a follow up, if we decide this is the right approach.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T16:08:35Z

- **pkg/controller/workload/job/job_controller.go:305** by [@mimowo](https://github.com/mimowo) — 2023-03-14T16:08:34Z
```diff
@@ -355,162 +302,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
-	}
-
-	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&batchJob.Job))
-	ctx = ctrl.LoggerInto(ctx, log)
-
-	isStandaloneJob := batchJob.ParentWorkloadName() == ""
-
-	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
-	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && batchJob.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
-	}
-
-	log.V(2).Info("Reconciling Job")
-
-	// 1. make sure there is only a single existing instance of the workload.
-	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := jobframework.EnsureOneWorkload(ctx, r.client, req, r.record, &batchJob)
-	if err != nil {
-		log.Error(err, "Getting existing workloads")
-		return ctrl.Result{}, err
-	}
-
-	// 2. handle job is finished.
-	if condition, finished := batchJob.Finished(); finished {
-		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-			return ctrl.Result{}, nil
-		}
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
-		}
-		return ctrl.Result{}, nil
-	}
-
-	// 3. handle workload is nil.
-	if wl == nil {
-		if !isStandaloneJob {
-			return ctrl.Result{}, nil
-		}
-		err := r.handleJobWithNoWorkload(ctx, &batchJob)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// 4. handle WaitForPodsReady only for a standalone job.
-	if isStandaloneJob {
-		// handle a job when waitForPodsReady is enabled, and it is the main job
-		if r.waitForPodsReady {
-			log.V(5).Info("Handling a job when waitForPodsReady is enabled")
-			condition := generatePodsReadyCondition(&batchJob, wl)
-			// optimization to avoid sending the update request if the status didn't change
-			if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-				log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
-				apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-				err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-				if err != nil {
-					log.Error(err, "Updating workload status")
-				}
-			}
-		}
-	}
-
-	// 5. handle job is suspended.
-	if batchJob.IsSuspend() {
-		// start the job if the workload has been admitted, and the job is still suspended
-		if wl.Status.Admission != nil {
-			log.V(2).Info("Job admitted, unsuspending")
-			err := jobframework.StartJob(ctx, r.client, r.record, &batchJob, wl)
-			if err != nil {
-				log.Error(err, "Unsuspending job")
-			}
-			return ctrl.Result{}, err
-		}
-
-		// update queue name if changed.
-		q := batchJob.QueueName()
-		if wl.Spec.QueueName != q {
-			log.V(2).Info("Job changed queues, updating workload")
-			wl.Spec.QueueName = q
-			err := r.client.Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload queue")
-			}
-			return ctrl.Result{}, err
-		}
-		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
-		return ctrl.Result{}, nil
-	}
-
-	// 6. handle job is unsuspended.
-	if wl.Status.Admission == nil {
-		// the job must be suspended if the workload is not yet admitted.
-		log.V(2).Info("Running job is not admitted by a cluster queue, suspending")
-		err := jobframework.StopJob(ctx, r.client, r.record, &batchJob, wl, "Not admitted by cluster queue")
-		if err != nil {
-			log.Error(err, "Suspending job with non admitted workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// workload is admitted and job is running, nothing to do.
-	log.V(3).Info("Job running with admitted workload, nothing to do")
-	return ctrl.Result{}, nil
-}
-
-func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, batchJob *BatchJob) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	// Wait until there are no active pods.
-	if batchJob.IsActive() {
-		log.V(2).Info("Job is suspended but still has active pods, waiting")
-		return nil
-	}
-
-	// Create the corresponding workload.
-	wl, err := jobframework.ConstructWorkload(ctx, r.client, r.scheme, batchJob)
-	if err != nil {
-		return err
-	}
-	if err = r.client.Create(ctx, wl); err != nil {
-		return err
-	}
-	r.record.Eventf(batchJob.Object(), corev1.EventTypeNormal, "CreatedWorkload",
-		"Created Workload: %v", workload.Key(wl))
-	return nil
-}
-
-func generatePodsReadyCondition(batchJob *BatchJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the the condition when the pods transition
-	// Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if wl.Status.Admission != nil && (batchJob.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-	}
+	fjr := (*jobframework.JobReconciler)(r)
```

  If I do this: `r.ReconcileGenericJob(ctx, req, &Job{})` then it does not compile, I get `r.ReconcileGenericJob undefined (type *JobReconciler has no field or method ReconcileGenericJob)`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T16:10:12Z

- **pkg/controller/workload/jobframework/workload_names.go:40** by [@mimowo](https://github.com/mimowo) — 2023-03-14T16:10:11Z
```diff
@@ -36,19 +37,19 @@ func GetWorkloadNameForOwnerRef(owner *metav1.OwnerReference) (string, error) {
 	if err != nil {
 		return "", err
 	}
-	gvk := metav1.GroupVersionKind{Group: gv.Group, Version: gv.Version, Kind: owner.Kind}
-	return GetWorkloadNameForOwnerWithGVK(owner.Name, &gvk), nil
+	gvk := schema.GroupVersionKind{Group: gv.Group, Version: gv.Version, Kind: owner.Kind}
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T16:12:10Z

- **pkg/controller/workload/job/job_controller.go:116** by [@mimowo](https://github.com/mimowo) — 2023-03-14T16:12:09Z
```diff
@@ -151,100 +109,96 @@ func (h *parentWorkloadHandler) queueReconcileForChildJob(object client.Object,
 	}
 }
 
-type BatchJob struct {
+type Job struct {
 	batchv1.Job
 }
 
-func (b *BatchJob) Object() client.Object {
-	return &b.Job
+func (job *Job) Object() client.Object {
```

  Renamed here and in MPIJob

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T16:17:02Z

- **pkg/controller/workload/jobframework/helpers.go:296** by [@mimowo](https://github.com/mimowo) — 2023-03-14T16:17:02Z
```diff
@@ -121,9 +288,20 @@ func EnsureOneWorkload(ctx context.Context, cli client.Client, req ctrl.Request,
 	return match, nil
 }
 
-// StartJob will unsuspend the job, and also inject the node affinity.
-func StartJob(ctx context.Context, client client.Client, record record.EventRecorder, job GenericJob, wl *kueue.Workload) error {
-	nodeSelectors, err := GetNodeSelectors(ctx, client, wl)
+// equivalentToWorkload checks if the job corresponds to the workload
+func (r *GenericJobReconciler) equivalentToWorkload(job GenericJob, wl *kueue.Workload) bool {
+	owner := metav1.GetControllerOf(wl)
+	// Indexes don't work in unit tests, so we explicitly check for the
+	// owner here.
+	if owner.Name != job.Object().GetName() {
```

  Removed the duplication by deleting the code in front. We could probably replace it by using unit test `WithIndex` as you point out, but this would be out of scope for this PR, IMO.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-14T16:43:42Z

- **pkg/controller/workload/job/job_controller.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T16:43:41Z
```diff
@@ -355,162 +302,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
-	}
-
-	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&batchJob.Job))
-	ctx = ctrl.LoggerInto(ctx, log)
-
-	isStandaloneJob := batchJob.ParentWorkloadName() == ""
-
-	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
-	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && batchJob.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
-	}
-
-	log.V(2).Info("Reconciling Job")
-
-	// 1. make sure there is only a single existing instance of the workload.
-	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := jobframework.EnsureOneWorkload(ctx, r.client, req, r.record, &batchJob)
-	if err != nil {
-		log.Error(err, "Getting existing workloads")
-		return ctrl.Result{}, err
-	}
-
-	// 2. handle job is finished.
-	if condition, finished := batchJob.Finished(); finished {
-		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-			return ctrl.Result{}, nil
-		}
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
-		}
-		return ctrl.Result{}, nil
-	}
-
-	// 3. handle workload is nil.
-	if wl == nil {
-		if !isStandaloneJob {
-			return ctrl.Result{}, nil
-		}
-		err := r.handleJobWithNoWorkload(ctx, &batchJob)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// 4. handle WaitForPodsReady only for a standalone job.
-	if isStandaloneJob {
-		// handle a job when waitForPodsReady is enabled, and it is the main job
-		if r.waitForPodsReady {
-			log.V(5).Info("Handling a job when waitForPodsReady is enabled")
-			condition := generatePodsReadyCondition(&batchJob, wl)
-			// optimization to avoid sending the update request if the status didn't change
-			if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-				log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
-				apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-				err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-				if err != nil {
-					log.Error(err, "Updating workload status")
-				}
-			}
-		}
-	}
-
-	// 5. handle job is suspended.
-	if batchJob.IsSuspend() {
-		// start the job if the workload has been admitted, and the job is still suspended
-		if wl.Status.Admission != nil {
-			log.V(2).Info("Job admitted, unsuspending")
-			err := jobframework.StartJob(ctx, r.client, r.record, &batchJob, wl)
-			if err != nil {
-				log.Error(err, "Unsuspending job")
-			}
-			return ctrl.Result{}, err
-		}
-
-		// update queue name if changed.
-		q := batchJob.QueueName()
-		if wl.Spec.QueueName != q {
-			log.V(2).Info("Job changed queues, updating workload")
-			wl.Spec.QueueName = q
-			err := r.client.Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload queue")
-			}
-			return ctrl.Result{}, err
-		}
-		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
-		return ctrl.Result{}, nil
-	}
-
-	// 6. handle job is unsuspended.
-	if wl.Status.Admission == nil {
-		// the job must be suspended if the workload is not yet admitted.
-		log.V(2).Info("Running job is not admitted by a cluster queue, suspending")
-		err := jobframework.StopJob(ctx, r.client, r.record, &batchJob, wl, "Not admitted by cluster queue")
-		if err != nil {
-			log.Error(err, "Suspending job with non admitted workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// workload is admitted and job is running, nothing to do.
-	log.V(3).Info("Job running with admitted workload, nothing to do")
-	return ctrl.Result{}, nil
-}
-
-func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, batchJob *BatchJob) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	// Wait until there are no active pods.
-	if batchJob.IsActive() {
-		log.V(2).Info("Job is suspended but still has active pods, waiting")
-		return nil
-	}
-
-	// Create the corresponding workload.
-	wl, err := jobframework.ConstructWorkload(ctx, r.client, r.scheme, batchJob)
-	if err != nil {
-		return err
-	}
-	if err = r.client.Create(ctx, wl); err != nil {
-		return err
-	}
-	r.record.Eventf(batchJob.Object(), corev1.EventTypeNormal, "CreatedWorkload",
-		"Created Workload: %v", workload.Key(wl))
-	return nil
-}
-
-func generatePodsReadyCondition(batchJob *BatchJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the the condition when the pods transition
-	// Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if wl.Status.Admission != nil && (batchJob.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-	}
+	fjr := (*jobframework.JobReconciler)(r)
```

  That's very odd. Then maybe it's worth just doing Reconciler a struct of its own until we figure if we can replace the entire thing with generics.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T17:00:51Z

- **pkg/controller/workload/job/job_controller.go:305** by [@mimowo](https://github.com/mimowo) — 2023-03-14T17:00:51Z
```diff
@@ -355,162 +302,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
-	}
-
-	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&batchJob.Job))
-	ctx = ctrl.LoggerInto(ctx, log)
-
-	isStandaloneJob := batchJob.ParentWorkloadName() == ""
-
-	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
-	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && batchJob.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
-	}
-
-	log.V(2).Info("Reconciling Job")
-
-	// 1. make sure there is only a single existing instance of the workload.
-	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := jobframework.EnsureOneWorkload(ctx, r.client, req, r.record, &batchJob)
-	if err != nil {
-		log.Error(err, "Getting existing workloads")
-		return ctrl.Result{}, err
-	}
-
-	// 2. handle job is finished.
-	if condition, finished := batchJob.Finished(); finished {
-		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-			return ctrl.Result{}, nil
-		}
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
-		}
-		return ctrl.Result{}, nil
-	}
-
-	// 3. handle workload is nil.
-	if wl == nil {
-		if !isStandaloneJob {
-			return ctrl.Result{}, nil
-		}
-		err := r.handleJobWithNoWorkload(ctx, &batchJob)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// 4. handle WaitForPodsReady only for a standalone job.
-	if isStandaloneJob {
-		// handle a job when waitForPodsReady is enabled, and it is the main job
-		if r.waitForPodsReady {
-			log.V(5).Info("Handling a job when waitForPodsReady is enabled")
-			condition := generatePodsReadyCondition(&batchJob, wl)
-			// optimization to avoid sending the update request if the status didn't change
-			if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-				log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
-				apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-				err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-				if err != nil {
-					log.Error(err, "Updating workload status")
-				}
-			}
-		}
-	}
-
-	// 5. handle job is suspended.
-	if batchJob.IsSuspend() {
-		// start the job if the workload has been admitted, and the job is still suspended
-		if wl.Status.Admission != nil {
-			log.V(2).Info("Job admitted, unsuspending")
-			err := jobframework.StartJob(ctx, r.client, r.record, &batchJob, wl)
-			if err != nil {
-				log.Error(err, "Unsuspending job")
-			}
-			return ctrl.Result{}, err
-		}
-
-		// update queue name if changed.
-		q := batchJob.QueueName()
-		if wl.Spec.QueueName != q {
-			log.V(2).Info("Job changed queues, updating workload")
-			wl.Spec.QueueName = q
-			err := r.client.Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload queue")
-			}
-			return ctrl.Result{}, err
-		}
-		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
-		return ctrl.Result{}, nil
-	}
-
-	// 6. handle job is unsuspended.
-	if wl.Status.Admission == nil {
-		// the job must be suspended if the workload is not yet admitted.
-		log.V(2).Info("Running job is not admitted by a cluster queue, suspending")
-		err := jobframework.StopJob(ctx, r.client, r.record, &batchJob, wl, "Not admitted by cluster queue")
-		if err != nil {
-			log.Error(err, "Suspending job with non admitted workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// workload is admitted and job is running, nothing to do.
-	log.V(3).Info("Job running with admitted workload, nothing to do")
-	return ctrl.Result{}, nil
-}
-
-func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, batchJob *BatchJob) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	// Wait until there are no active pods.
-	if batchJob.IsActive() {
-		log.V(2).Info("Job is suspended but still has active pods, waiting")
-		return nil
-	}
-
-	// Create the corresponding workload.
-	wl, err := jobframework.ConstructWorkload(ctx, r.client, r.scheme, batchJob)
-	if err != nil {
-		return err
-	}
-	if err = r.client.Create(ctx, wl); err != nil {
-		return err
-	}
-	r.record.Eventf(batchJob.Object(), corev1.EventTypeNormal, "CreatedWorkload",
-		"Created Workload: %v", workload.Key(wl))
-	return nil
-}
-
-func generatePodsReadyCondition(batchJob *BatchJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the the condition when the pods transition
-	// Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if wl.Status.Admission != nil && (batchJob.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-	}
+	fjr := (*jobframework.JobReconciler)(r)
```

  I think it is acceptable as is, but let me know. What do you mean a struct on its own (as before, or another proposal)?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T17:02:05Z

- **pkg/controller/workload/job/job_controller.go:305** by [@mimowo](https://github.com/mimowo) — 2023-03-14T17:02:04Z
```diff
@@ -355,162 +302,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
-	}
-
-	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&batchJob.Job))
-	ctx = ctrl.LoggerInto(ctx, log)
-
-	isStandaloneJob := batchJob.ParentWorkloadName() == ""
-
-	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
-	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && batchJob.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
-	}
-
-	log.V(2).Info("Reconciling Job")
-
-	// 1. make sure there is only a single existing instance of the workload.
-	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := jobframework.EnsureOneWorkload(ctx, r.client, req, r.record, &batchJob)
-	if err != nil {
-		log.Error(err, "Getting existing workloads")
-		return ctrl.Result{}, err
-	}
-
-	// 2. handle job is finished.
-	if condition, finished := batchJob.Finished(); finished {
-		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-			return ctrl.Result{}, nil
-		}
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
-		}
-		return ctrl.Result{}, nil
-	}
-
-	// 3. handle workload is nil.
-	if wl == nil {
-		if !isStandaloneJob {
-			return ctrl.Result{}, nil
-		}
-		err := r.handleJobWithNoWorkload(ctx, &batchJob)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// 4. handle WaitForPodsReady only for a standalone job.
-	if isStandaloneJob {
-		// handle a job when waitForPodsReady is enabled, and it is the main job
-		if r.waitForPodsReady {
-			log.V(5).Info("Handling a job when waitForPodsReady is enabled")
-			condition := generatePodsReadyCondition(&batchJob, wl)
-			// optimization to avoid sending the update request if the status didn't change
-			if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-				log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
-				apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-				err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-				if err != nil {
-					log.Error(err, "Updating workload status")
-				}
-			}
-		}
-	}
-
-	// 5. handle job is suspended.
-	if batchJob.IsSuspend() {
-		// start the job if the workload has been admitted, and the job is still suspended
-		if wl.Status.Admission != nil {
-			log.V(2).Info("Job admitted, unsuspending")
-			err := jobframework.StartJob(ctx, r.client, r.record, &batchJob, wl)
-			if err != nil {
-				log.Error(err, "Unsuspending job")
-			}
-			return ctrl.Result{}, err
-		}
-
-		// update queue name if changed.
-		q := batchJob.QueueName()
-		if wl.Spec.QueueName != q {
-			log.V(2).Info("Job changed queues, updating workload")
-			wl.Spec.QueueName = q
-			err := r.client.Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload queue")
-			}
-			return ctrl.Result{}, err
-		}
-		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
-		return ctrl.Result{}, nil
-	}
-
-	// 6. handle job is unsuspended.
-	if wl.Status.Admission == nil {
-		// the job must be suspended if the workload is not yet admitted.
-		log.V(2).Info("Running job is not admitted by a cluster queue, suspending")
-		err := jobframework.StopJob(ctx, r.client, r.record, &batchJob, wl, "Not admitted by cluster queue")
-		if err != nil {
-			log.Error(err, "Suspending job with non admitted workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// workload is admitted and job is running, nothing to do.
-	log.V(3).Info("Job running with admitted workload, nothing to do")
-	return ctrl.Result{}, nil
-}
-
-func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, batchJob *BatchJob) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	// Wait until there are no active pods.
-	if batchJob.IsActive() {
-		log.V(2).Info("Job is suspended but still has active pods, waiting")
-		return nil
-	}
-
-	// Create the corresponding workload.
-	wl, err := jobframework.ConstructWorkload(ctx, r.client, r.scheme, batchJob)
-	if err != nil {
-		return err
-	}
-	if err = r.client.Create(ctx, wl); err != nil {
-		return err
-	}
-	r.record.Eventf(batchJob.Object(), corev1.EventTypeNormal, "CreatedWorkload",
-		"Created Workload: %v", workload.Key(wl))
-	return nil
-}
-
-func generatePodsReadyCondition(batchJob *BatchJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the the condition when the pods transition
-	// Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if wl.Status.Admission != nil && (batchJob.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-	}
+	fjr := (*jobframework.JobReconciler)(r)
```

  I'm in general a little bit worried that this is already a sizable PR and prolonging will result in unnecesary conflicts, so prefer to delay the cleanups for later, unless really needed, let me know.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-14T17:09:30Z

- **pkg/controller/workload/job/job_controller.go:305** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-14T17:09:30Z
```diff
@@ -355,162 +302,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
-	}
-
-	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&batchJob.Job))
-	ctx = ctrl.LoggerInto(ctx, log)
-
-	isStandaloneJob := batchJob.ParentWorkloadName() == ""
-
-	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
-	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && batchJob.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
-	}
-
-	log.V(2).Info("Reconciling Job")
-
-	// 1. make sure there is only a single existing instance of the workload.
-	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := jobframework.EnsureOneWorkload(ctx, r.client, req, r.record, &batchJob)
-	if err != nil {
-		log.Error(err, "Getting existing workloads")
-		return ctrl.Result{}, err
-	}
-
-	// 2. handle job is finished.
-	if condition, finished := batchJob.Finished(); finished {
-		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-			return ctrl.Result{}, nil
-		}
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
-		}
-		return ctrl.Result{}, nil
-	}
-
-	// 3. handle workload is nil.
-	if wl == nil {
-		if !isStandaloneJob {
-			return ctrl.Result{}, nil
-		}
-		err := r.handleJobWithNoWorkload(ctx, &batchJob)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// 4. handle WaitForPodsReady only for a standalone job.
-	if isStandaloneJob {
-		// handle a job when waitForPodsReady is enabled, and it is the main job
-		if r.waitForPodsReady {
-			log.V(5).Info("Handling a job when waitForPodsReady is enabled")
-			condition := generatePodsReadyCondition(&batchJob, wl)
-			// optimization to avoid sending the update request if the status didn't change
-			if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-				log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
-				apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-				err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-				if err != nil {
-					log.Error(err, "Updating workload status")
-				}
-			}
-		}
-	}
-
-	// 5. handle job is suspended.
-	if batchJob.IsSuspend() {
-		// start the job if the workload has been admitted, and the job is still suspended
-		if wl.Status.Admission != nil {
-			log.V(2).Info("Job admitted, unsuspending")
-			err := jobframework.StartJob(ctx, r.client, r.record, &batchJob, wl)
-			if err != nil {
-				log.Error(err, "Unsuspending job")
-			}
-			return ctrl.Result{}, err
-		}
-
-		// update queue name if changed.
-		q := batchJob.QueueName()
-		if wl.Spec.QueueName != q {
-			log.V(2).Info("Job changed queues, updating workload")
-			wl.Spec.QueueName = q
-			err := r.client.Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload queue")
-			}
-			return ctrl.Result{}, err
-		}
-		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
-		return ctrl.Result{}, nil
-	}
-
-	// 6. handle job is unsuspended.
-	if wl.Status.Admission == nil {
-		// the job must be suspended if the workload is not yet admitted.
-		log.V(2).Info("Running job is not admitted by a cluster queue, suspending")
-		err := jobframework.StopJob(ctx, r.client, r.record, &batchJob, wl, "Not admitted by cluster queue")
-		if err != nil {
-			log.Error(err, "Suspending job with non admitted workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// workload is admitted and job is running, nothing to do.
-	log.V(3).Info("Job running with admitted workload, nothing to do")
-	return ctrl.Result{}, nil
-}
-
-func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, batchJob *BatchJob) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	// Wait until there are no active pods.
-	if batchJob.IsActive() {
-		log.V(2).Info("Job is suspended but still has active pods, waiting")
-		return nil
-	}
-
-	// Create the corresponding workload.
-	wl, err := jobframework.ConstructWorkload(ctx, r.client, r.scheme, batchJob)
-	if err != nil {
-		return err
-	}
-	if err = r.client.Create(ctx, wl); err != nil {
-		return err
-	}
-	r.record.Eventf(batchJob.Object(), corev1.EventTypeNormal, "CreatedWorkload",
-		"Created Workload: %v", workload.Key(wl))
-	return nil
-}
-
-func generatePodsReadyCondition(batchJob *BatchJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the the condition when the pods transition
-	// Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if wl.Status.Admission != nil && (batchJob.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-	}
+	fjr := (*jobframework.JobReconciler)(r)
```

  Yeah, I meant like it was before.
  
  But let's get this merged.
  
  /lgtm
  /approve
  /label tide/merge-method-squash

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-14T17:19:09Z

- **pkg/controller/workload/job/job_controller.go:305** by [@mimowo](https://github.com/mimowo) — 2023-03-14T17:19:08Z
```diff
@@ -355,162 +302,10 @@ func SetupIndexes(ctx context.Context, indexer client.FieldIndexer) error {
 //+kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
 
 func (r *JobReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
-	var batchJob BatchJob
-	if err := r.client.Get(ctx, req.NamespacedName, &batchJob.Job); err != nil {
-		// we'll ignore not-found errors, since there is nothing to do.
-		return ctrl.Result{}, client.IgnoreNotFound(err)
-	}
-
-	log := ctrl.LoggerFrom(ctx).WithValues("job", klog.KObj(&batchJob.Job))
-	ctx = ctrl.LoggerInto(ctx, log)
-
-	isStandaloneJob := batchJob.ParentWorkloadName() == ""
-
-	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
-	// queue-name or the parent-workload annotation set.
-	if !r.manageJobsWithoutQueueName && batchJob.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
-		return ctrl.Result{}, nil
-	}
-
-	log.V(2).Info("Reconciling Job")
-
-	// 1. make sure there is only a single existing instance of the workload.
-	// If there's no workload exists and job is unsuspended, we'll stop it immediately.
-	wl, err := jobframework.EnsureOneWorkload(ctx, r.client, req, r.record, &batchJob)
-	if err != nil {
-		log.Error(err, "Getting existing workloads")
-		return ctrl.Result{}, err
-	}
-
-	// 2. handle job is finished.
-	if condition, finished := batchJob.Finished(); finished {
-		if wl == nil || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
-			return ctrl.Result{}, nil
-		}
-		err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-		if err != nil {
-			log.Error(err, "Updating workload status")
-		}
-		return ctrl.Result{}, nil
-	}
-
-	// 3. handle workload is nil.
-	if wl == nil {
-		if !isStandaloneJob {
-			return ctrl.Result{}, nil
-		}
-		err := r.handleJobWithNoWorkload(ctx, &batchJob)
-		if err != nil {
-			log.Error(err, "Handling job with no workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// 4. handle WaitForPodsReady only for a standalone job.
-	if isStandaloneJob {
-		// handle a job when waitForPodsReady is enabled, and it is the main job
-		if r.waitForPodsReady {
-			log.V(5).Info("Handling a job when waitForPodsReady is enabled")
-			condition := generatePodsReadyCondition(&batchJob, wl)
-			// optimization to avoid sending the update request if the status didn't change
-			if !apimeta.IsStatusConditionPresentAndEqual(wl.Status.Conditions, condition.Type, condition.Status) {
-				log.V(3).Info(fmt.Sprintf("Updating the PodsReady condition with status: %v", condition.Status))
-				apimeta.SetStatusCondition(&wl.Status.Conditions, condition)
-				err := workload.UpdateStatus(ctx, r.client, wl, condition.Type, condition.Status, condition.Reason, condition.Message, constants.JobControllerName)
-				if err != nil {
-					log.Error(err, "Updating workload status")
-				}
-			}
-		}
-	}
-
-	// 5. handle job is suspended.
-	if batchJob.IsSuspend() {
-		// start the job if the workload has been admitted, and the job is still suspended
-		if wl.Status.Admission != nil {
-			log.V(2).Info("Job admitted, unsuspending")
-			err := jobframework.StartJob(ctx, r.client, r.record, &batchJob, wl)
-			if err != nil {
-				log.Error(err, "Unsuspending job")
-			}
-			return ctrl.Result{}, err
-		}
-
-		// update queue name if changed.
-		q := batchJob.QueueName()
-		if wl.Spec.QueueName != q {
-			log.V(2).Info("Job changed queues, updating workload")
-			wl.Spec.QueueName = q
-			err := r.client.Update(ctx, wl)
-			if err != nil {
-				log.Error(err, "Updating workload queue")
-			}
-			return ctrl.Result{}, err
-		}
-		log.V(3).Info("Job is suspended and workload not yet admitted by a clusterQueue, nothing to do")
-		return ctrl.Result{}, nil
-	}
-
-	// 6. handle job is unsuspended.
-	if wl.Status.Admission == nil {
-		// the job must be suspended if the workload is not yet admitted.
-		log.V(2).Info("Running job is not admitted by a cluster queue, suspending")
-		err := jobframework.StopJob(ctx, r.client, r.record, &batchJob, wl, "Not admitted by cluster queue")
-		if err != nil {
-			log.Error(err, "Suspending job with non admitted workload")
-		}
-		return ctrl.Result{}, err
-	}
-
-	// workload is admitted and job is running, nothing to do.
-	log.V(3).Info("Job running with admitted workload, nothing to do")
-	return ctrl.Result{}, nil
-}
-
-func (r *JobReconciler) handleJobWithNoWorkload(ctx context.Context, batchJob *BatchJob) error {
-	log := ctrl.LoggerFrom(ctx)
-
-	// Wait until there are no active pods.
-	if batchJob.IsActive() {
-		log.V(2).Info("Job is suspended but still has active pods, waiting")
-		return nil
-	}
-
-	// Create the corresponding workload.
-	wl, err := jobframework.ConstructWorkload(ctx, r.client, r.scheme, batchJob)
-	if err != nil {
-		return err
-	}
-	if err = r.client.Create(ctx, wl); err != nil {
-		return err
-	}
-	r.record.Eventf(batchJob.Object(), corev1.EventTypeNormal, "CreatedWorkload",
-		"Created Workload: %v", workload.Key(wl))
-	return nil
-}
-
-func generatePodsReadyCondition(batchJob *BatchJob, wl *kueue.Workload) metav1.Condition {
-	conditionStatus := metav1.ConditionFalse
-	message := "Not all pods are ready or succeeded"
-	// Once PodsReady=True it stays as long as the workload remains admitted to
-	// avoid unnecessary flickering the the condition when the pods transition
-	// Ready to Completed. As pods finish, they transition first into the
-	// uncountedTerminatedPods staging area, before passing to the
-	// succeeded/failed counters.
-	if wl.Status.Admission != nil && (batchJob.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
-		conditionStatus = metav1.ConditionTrue
-		message = "All pods were ready or succeeded since the workload admission"
-	}
-	return metav1.Condition{
-		Type:    kueue.WorkloadPodsReady,
-		Status:  conditionStatus,
-		Reason:  "PodsReady",
-		Message: message,
-	}
+	fjr := (*jobframework.JobReconciler)(r)
```

  Thanks! I will probably create an issue for this, like "Refactor generic reconciler to avoid unnecessary casts".
