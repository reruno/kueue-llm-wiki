# PR #6270: FlavorFungibility: update documentation for FlavorFungibilityImplicitPreferenceDefault

**Summary**: FlavorFungibility: update documentation for FlavorFungibilityImplicitPreferenceDefault

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6270

**Last updated**: 2025-08-01T15:06:19Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `fungibility_docu` → `main`
- **Created**: 2025-07-30T08:03:05Z
- **Updated**: 2025-08-01T15:06:19Z
- **Merged**: 2025-08-01T15:05:39Z
- **Closed**: 2025-08-01T15:05:39Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@gabesaba](https://github.com/gabesaba), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 1   **+40 / -15**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6201

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-30T08:03:11Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6a2cee3e64b86343af789179e021ce631f08f88b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/688cc723920c3800089ccf30 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6270--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6270--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTYyNzAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.Flu7BurzQaai1DGLfn0nR-vAHMDA2KBi9t3Btd2eiFA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6270--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@gabesaba](https://github.com/gabesaba) — 2025-08-01T14:15:43Z

/lgtm

thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-01T14:15:50Z

LGTM label has been added.  <details>Git tree hash: 3b878ee7f4c9edd13478edaacff88e85553037ec</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-01T14:18:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6270#" title="Author self-approved">pajakd</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6270#pullrequestreview-3079427270" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T14:18:27Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T14:18:30Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6270#issuecomment-3144751712):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T15:06:19Z

@tenzen-y: new pull request created: #6364

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6270#issuecomment-3144751712):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-07-31T13:09:46Z

- **site/content/en/docs/concepts/cluster_queue.md:480** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:00:42Z
```diff
@@ -475,12 +475,14 @@ the heuristics that Kueue implements to preempt as few Workloads as possible.
 
 ## FlavorFungibility
 
-When there is not enough nominal quota of resources in a ResourceFlavor, the incoming Workload can borrow
-quota or preempt running Workloads in the ClusterQueue or Cohort.
-
-Kueue evaluates the flavors in a ClusterQueue in order. You can influence whether to prioritize
-preemptions or borrowing in a flavor before trying to accommodate the Workload in the next flavor, by
-setting the `flavorFungibility` field.
+When there is not enough nominal quota of resources in a ResourceFlavor, the
+incoming Workload can borrow quota or preempt running Workloads in the
+ClusterQueue or Cohort. In the ClusterQueue there can be multiple ResourceFlavors
```

  ```suggestion
  ClusterQueue or Cohort. In the ClusterQueue there can be multiple Flavors
  ```

- **site/content/en/docs/concepts/cluster_queue.md:1** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:01:00Z
  consistently capitalize Flavor

- **site/content/en/docs/concepts/cluster_queue.md:481** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:01:38Z
```diff
@@ -475,12 +475,14 @@ the heuristics that Kueue implements to preempt as few Workloads as possible.
 
 ## FlavorFungibility
 
-When there is not enough nominal quota of resources in a ResourceFlavor, the incoming Workload can borrow
-quota or preempt running Workloads in the ClusterQueue or Cohort.
-
-Kueue evaluates the flavors in a ClusterQueue in order. You can influence whether to prioritize
-preemptions or borrowing in a flavor before trying to accommodate the Workload in the next flavor, by
-setting the `flavorFungibility` field.
+When there is not enough nominal quota of resources in a ResourceFlavor, the
+incoming Workload can borrow quota or preempt running Workloads in the
+ClusterQueue or Cohort. In the ClusterQueue there can be multiple ResourceFlavors
+for each resource, and there might be different amounts of available quota in each
```

  ```suggestion
  for each resource, with potentially different amounts of quota available in each
  ```

- **site/content/en/docs/concepts/cluster_queue.md:482** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:02:02Z
```diff
@@ -475,12 +475,14 @@ the heuristics that Kueue implements to preempt as few Workloads as possible.
 
 ## FlavorFungibility
 
-When there is not enough nominal quota of resources in a ResourceFlavor, the incoming Workload can borrow
-quota or preempt running Workloads in the ClusterQueue or Cohort.
-
-Kueue evaluates the flavors in a ClusterQueue in order. You can influence whether to prioritize
-preemptions or borrowing in a flavor before trying to accommodate the Workload in the next flavor, by
-setting the `flavorFungibility` field.
+When there is not enough nominal quota of resources in a ResourceFlavor, the
+incoming Workload can borrow quota or preempt running Workloads in the
+ClusterQueue or Cohort. In the ClusterQueue there can be multiple ResourceFlavors
+for each resource, and there might be different amounts of available quota in each
+of them. Kueue evaluates the flavors in a ClusterQueue in order and for each it
```

  ```suggestion
  Kueue evaluates the flavors in a ClusterQueue in order and for each it
  ```

- **site/content/en/docs/concepts/cluster_queue.md:482** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:02:46Z
```diff
@@ -475,12 +475,14 @@ the heuristics that Kueue implements to preempt as few Workloads as possible.
 
 ## FlavorFungibility
 
-When there is not enough nominal quota of resources in a ResourceFlavor, the incoming Workload can borrow
-quota or preempt running Workloads in the ClusterQueue or Cohort.
-
-Kueue evaluates the flavors in a ClusterQueue in order. You can influence whether to prioritize
-preemptions or borrowing in a flavor before trying to accommodate the Workload in the next flavor, by
-setting the `flavorFungibility` field.
+When there is not enough nominal quota of resources in a ResourceFlavor, the
+incoming Workload can borrow quota or preempt running Workloads in the
+ClusterQueue or Cohort. In the ClusterQueue there can be multiple ResourceFlavors
+for each resource, and there might be different amounts of available quota in each
+of them. Kueue evaluates the flavors in a ClusterQueue in order and for each it
```

  Is this accurate that we check each? We check sequentially until we find (Fit, NoBorrow), or satisfy the configuration of FlavorFungibility.

- **site/content/en/docs/concepts/cluster_queue.md:485** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:03:27Z
```diff
@@ -475,12 +475,14 @@ the heuristics that Kueue implements to preempt as few Workloads as possible.
 
 ## FlavorFungibility
 
-When there is not enough nominal quota of resources in a ResourceFlavor, the incoming Workload can borrow
-quota or preempt running Workloads in the ClusterQueue or Cohort.
-
-Kueue evaluates the flavors in a ClusterQueue in order. You can influence whether to prioritize
-preemptions or borrowing in a flavor before trying to accommodate the Workload in the next flavor, by
-setting the `flavorFungibility` field.
+When there is not enough nominal quota of resources in a ResourceFlavor, the
+incoming Workload can borrow quota or preempt running Workloads in the
+ClusterQueue or Cohort. In the ClusterQueue there can be multiple ResourceFlavors
+for each resource, and there might be different amounts of available quota in each
+of them. Kueue evaluates the flavors in a ClusterQueue in order and for each it
+checks if the required resource of the workload fits in this ResourceFlavor and
+if borrowing or preemption is required. You can influence how many flavors to
+consider by setting the `flavorFungibility` field.
```

  ```suggestion
  if borrowing or preemption is required. You can influence the Flavor selection process by configuring the `flavorFungibility` field.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:502** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:03:38Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` what should happen if a workload can get enough resource by
```

  ```suggestion
  - `whenCanBorrow` defines what should happen if a workload can get enough resource by
  ```

- **site/content/en/docs/concepts/cluster_queue.md:506** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:03:54Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` what should happen if a workload can get enough resource by
```

  ```suggestion
  - `whenCanPreempt` defines what should happen if a workload can get enough resource by
  ```

- **site/content/en/docs/concepts/cluster_queue.md:513** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:04:36Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
+
+If during the search, the workload finds some ResourceFlavor in which it can fit
+without preemption or borrowing, such ResourceFlavor is immediately selected
+(regardless of the above configuration). Otherwise, out of the considered
```

  ```suggestion
  without preemption or borrowing, such ResourceFlavor is immediately selected,
  regardless of the above configuration. Otherwise, out of the considered
  ```

- **site/content/en/docs/concepts/cluster_queue.md:516** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:04:59Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
+
+If during the search, the workload finds some ResourceFlavor in which it can fit
+without preemption or borrowing, such ResourceFlavor is immediately selected
+(regardless of the above configuration). Otherwise, out of the considered
+ResourceFlavors, Kueue selects a one that fits the workload using borrowing
+(without preemptions). If there is no such ResourceFlavor, Kueue selects a flavor
+that uses preemption and is preferably not using borrowing.
```

  ```suggestion
  that uses preemption and is preferably not borrowing.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:523** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:05:39Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
+
+If during the search, the workload finds some ResourceFlavor in which it can fit
+without preemption or borrowing, such ResourceFlavor is immediately selected
+(regardless of the above configuration). Otherwise, out of the considered
+ResourceFlavors, Kueue selects a one that fits the workload using borrowing
+(without preemptions). If there is no such ResourceFlavor, Kueue selects a flavor
+that uses preemption and is preferably not using borrowing.
+
+As explained above, when assigning flavors, by default Kueue avoids preemptions
+and prefers to borrow. Borrowing is not disruptive to other workloads but a
+workload that borrows risks being prempted (since it is using nominal quota
+from some other Cluster Queue). If you prefer to sometimes preempt rather than borrow,
+you can enable feature gate `FlavorFungibilityImplicitPreferenceDefault`, which
+changes the default preference as follows. If `whenCanBorrow = TryNextFlavor`
```

  ```suggestion
  changes the default preference as follows: If `whenCanBorrow = TryNextFlavor`
  ```

- **site/content/en/docs/concepts/cluster_queue.md:519** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:08:41Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
+
+If during the search, the workload finds some ResourceFlavor in which it can fit
+without preemption or borrowing, such ResourceFlavor is immediately selected
+(regardless of the above configuration). Otherwise, out of the considered
+ResourceFlavors, Kueue selects a one that fits the workload using borrowing
+(without preemptions). If there is no such ResourceFlavor, Kueue selects a flavor
+that uses preemption and is preferably not using borrowing.
+
+As explained above, when assigning flavors, by default Kueue avoids preemptions
+and prefers to borrow. Borrowing is not disruptive to other workloads but a
```

  ```suggestion
  By default Kueue avoids preemptions and prefers borrowing when assigning flavors. Borrowing is not disruptive to other workloads but a
  ```

- **site/content/en/docs/concepts/cluster_queue.md:521** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:09:22Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
+
+If during the search, the workload finds some ResourceFlavor in which it can fit
+without preemption or borrowing, such ResourceFlavor is immediately selected
+(regardless of the above configuration). Otherwise, out of the considered
+ResourceFlavors, Kueue selects a one that fits the workload using borrowing
+(without preemptions). If there is no such ResourceFlavor, Kueue selects a flavor
+that uses preemption and is preferably not using borrowing.
+
+As explained above, when assigning flavors, by default Kueue avoids preemptions
+and prefers to borrow. Borrowing is not disruptive to other workloads but a
+workload that borrows risks being prempted (since it is using nominal quota
+from some other Cluster Queue). If you prefer to sometimes preempt rather than borrow,
```

  ```suggestion
  from some other Cluster Queue). If you prefer to preempt rather than borrow when possible,
  ```

- **site/content/en/docs/concepts/cluster_queue.md:522** by [@gabesaba](https://github.com/gabesaba) — 2025-07-31T13:09:35Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
+
+If during the search, the workload finds some ResourceFlavor in which it can fit
+without preemption or borrowing, such ResourceFlavor is immediately selected
+(regardless of the above configuration). Otherwise, out of the considered
+ResourceFlavors, Kueue selects a one that fits the workload using borrowing
+(without preemptions). If there is no such ResourceFlavor, Kueue selects a flavor
+that uses preemption and is preferably not using borrowing.
+
+As explained above, when assigning flavors, by default Kueue avoids preemptions
+and prefers to borrow. Borrowing is not disruptive to other workloads but a
+workload that borrows risks being prempted (since it is using nominal quota
+from some other Cluster Queue). If you prefer to sometimes preempt rather than borrow,
+you can enable feature gate `FlavorFungibilityImplicitPreferenceDefault`, which
```

  ```suggestion
  you can enable the feature gate `FlavorFungibilityImplicitPreferenceDefault`, which
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T13:23:26Z

- **site/content/en/docs/concepts/cluster_queue.md:504** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T13:09:59Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` defines what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
```

  ```suggestion
    - `Borrow` (default): Kueue stops looking for a better assignment.
  ```
  
  IIUC, the workload does not try to find the flavor assignment by itself.
  The flavor assigner will do it.

- **site/content/en/docs/concepts/cluster_queue.md:509** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T13:10:48Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` defines what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` defines what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
```

  ```suggestion
    - `Preempt`: Kueue stops looking for a better assignment.
    - `TryNextFlavor` (default): Kueue tries the next ResourceFlavor.
  ```
  
  Same as Borrow.

- **site/content/en/docs/concepts/cluster_queue.md:523** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T13:13:28Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` defines what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` defines what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
+
+If during the search, the workload finds some ResourceFlavor in which it can fit
+without preemption or borrowing, such ResourceFlavor is immediately selected,
+regardless of the above configuration. Otherwise, out of the considered
+ResourceFlavors, Kueue selects a one that fits the workload using borrowing
+(without preemptions). If there is no such ResourceFlavor, Kueue selects a Flavor
+that uses preemption and is preferably not borrowing.
+
+By default Kueue avoids preemptions and prefers borrowing when assigning Flavors.
+Borrowing is not disruptive to other workloads but a
+workload that borrows risks being prempted (since it is using nominal quota
+from some other Cluster Queue). If you prefer to preempt rather than borrow when possible,
+you can enable the feature gate `FlavorFungibilityImplicitPreferenceDefault`, which
+changes the default preference as follows: If `whenCanBorrow = TryNextFlavor`
```

  ```suggestion
  changes the default preference as follows: If `.spec.flavorFungibility.whenCanBorrow` is `TryNextFlavor`,
  ```

- **site/content/en/docs/concepts/cluster_queue.md:525** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T13:14:56Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` defines what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` defines what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
+
+If during the search, the workload finds some ResourceFlavor in which it can fit
+without preemption or borrowing, such ResourceFlavor is immediately selected,
+regardless of the above configuration. Otherwise, out of the considered
+ResourceFlavors, Kueue selects a one that fits the workload using borrowing
+(without preemptions). If there is no such ResourceFlavor, Kueue selects a Flavor
+that uses preemption and is preferably not borrowing.
+
+By default Kueue avoids preemptions and prefers borrowing when assigning Flavors.
+Borrowing is not disruptive to other workloads but a
+workload that borrows risks being prempted (since it is using nominal quota
+from some other Cluster Queue). If you prefer to preempt rather than borrow when possible,
+you can enable the feature gate `FlavorFungibilityImplicitPreferenceDefault`, which
+changes the default preference as follows: If `whenCanBorrow = TryNextFlavor`
+it assumes that preemption is preferred over borrowing and otherwise it assumes
+that borrowing is preferred over preemption.
```

  Additionally, could you mention which Kueue version supports this FG like the following Note card?
  
  ```
  AdmissionFairSharing is currently an alpha feature and is not enabled by default.
  
  You can enable it by editing the AdmissionFairSharing feature gate. Check the [Installation](https://kueue.sigs.k8s.io/docs/installation/#change-the-feature-gates-configuration) guide for details on feature gate configuration.
  ```
  
  https://kueue.sigs.k8s.io/docs/concepts/admission_fair_sharing/

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-08-01T13:32:39Z

- **site/content/en/docs/concepts/cluster_queue.md:1** by [@pajakd](https://github.com/pajakd) — 2025-08-01T13:32:39Z
  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-08-01T13:39:01Z

- **site/content/en/docs/concepts/cluster_queue.md:482** by [@pajakd](https://github.com/pajakd) — 2025-08-01T13:39:01Z
```diff
@@ -475,12 +475,14 @@ the heuristics that Kueue implements to preempt as few Workloads as possible.
 
 ## FlavorFungibility
 
-When there is not enough nominal quota of resources in a ResourceFlavor, the incoming Workload can borrow
-quota or preempt running Workloads in the ClusterQueue or Cohort.
-
-Kueue evaluates the flavors in a ClusterQueue in order. You can influence whether to prioritize
-preemptions or borrowing in a flavor before trying to accommodate the Workload in the next flavor, by
-setting the `flavorFungibility` field.
+When there is not enough nominal quota of resources in a ResourceFlavor, the
+incoming Workload can borrow quota or preempt running Workloads in the
+ClusterQueue or Cohort. In the ClusterQueue there can be multiple ResourceFlavors
+for each resource, and there might be different amounts of available quota in each
+of them. Kueue evaluates the flavors in a ClusterQueue in order and for each it
```

  I ment that for each checked flavor it does something. I did not mean that it checks all the flavors. Do you have a suggestion how make it clearer?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2025-08-01T13:58:41Z

- **site/content/en/docs/concepts/cluster_queue.md:525** by [@pajakd](https://github.com/pajakd) — 2025-08-01T13:58:41Z
```diff
@@ -497,17 +499,30 @@ spec:
 
 The fields above do the following:
 
-- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
-  - `Borrow` (default): ClusterQueue stops finding a better assignment.
-  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
-- `whenCanPreempt` determines whether a workload should try preemption in current ResourceFlavor before try the next one. The possible values are:
-  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
-  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
-
-By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources.
-And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
-
-Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing.
+- `whenCanBorrow` defines what should happen if a workload can get enough resource by
+borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): workload stops looking for a better assignment.
+  - `TryNextFlavor`: workload tries the next ResourceFlavor.
+- `whenCanPreempt` defines what should happen if a workload can get enough resource by
+preempting in current ResourceFlavor. The possible values are:
+  - `Preempt`: workload stops looking for a better assignment.
+  - `TryNextFlavor` (default): workload tries the next ResourceFlavor.
+
+If during the search, the workload finds some ResourceFlavor in which it can fit
+without preemption or borrowing, such ResourceFlavor is immediately selected,
+regardless of the above configuration. Otherwise, out of the considered
+ResourceFlavors, Kueue selects a one that fits the workload using borrowing
+(without preemptions). If there is no such ResourceFlavor, Kueue selects a Flavor
+that uses preemption and is preferably not borrowing.
+
+By default Kueue avoids preemptions and prefers borrowing when assigning Flavors.
+Borrowing is not disruptive to other workloads but a
+workload that borrows risks being prempted (since it is using nominal quota
+from some other Cluster Queue). If you prefer to preempt rather than borrow when possible,
+you can enable the feature gate `FlavorFungibilityImplicitPreferenceDefault`, which
+changes the default preference as follows: If `whenCanBorrow = TryNextFlavor`
+it assumes that preemption is preferred over borrowing and otherwise it assumes
+that borrowing is preferred over preemption.
```

  Done.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-08-01T14:15:52Z

- **site/content/en/docs/concepts/cluster_queue.md:482** by [@gabesaba](https://github.com/gabesaba) — 2025-08-01T14:15:52Z
```diff
@@ -475,12 +475,14 @@ the heuristics that Kueue implements to preempt as few Workloads as possible.
 
 ## FlavorFungibility
 
-When there is not enough nominal quota of resources in a ResourceFlavor, the incoming Workload can borrow
-quota or preempt running Workloads in the ClusterQueue or Cohort.
-
-Kueue evaluates the flavors in a ClusterQueue in order. You can influence whether to prioritize
-preemptions or borrowing in a flavor before trying to accommodate the Workload in the next flavor, by
-setting the `flavorFungibility` field.
+When there is not enough nominal quota of resources in a ResourceFlavor, the
+incoming Workload can borrow quota or preempt running Workloads in the
+ClusterQueue or Cohort. In the ClusterQueue there can be multiple ResourceFlavors
+for each resource, and there might be different amounts of available quota in each
+of them. Kueue evaluates the flavors in a ClusterQueue in order and for each it
```

  I read it again and it sounds clear to me :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T14:18:11Z

Thank you!
/lgtm
/approve
