# PR #6271: [website] Update MultiKueue Dispatcher API site

**Summary**: [website] Update MultiKueue Dispatcher API site

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6271

**Last updated**: 2025-08-01T12:15:52Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `docs/mk-dispatcher-api` → `main`
- **Created**: 2025-07-30T08:51:17Z
- **Updated**: 2025-08-01T12:15:52Z
- **Merged**: 2025-08-01T11:11:37Z
- **Closed**: 2025-08-01T11:11:37Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+45 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Update documentation related to the MultiKueue Dispatcher.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6202

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-30T08:51:25Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | db20d5f2c502e5d9ef398c5f69a2797135813b60 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/688c9c089d90ae0008ae27a6 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6271--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6271--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTYyNzEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9._BYMiz0CAC1WVXYvXqP032GZ_1TnAthaqLwZs7tIza4)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6271--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-31T15:40:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6271#" title="Author self-approved">mszadkow</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6271#pullrequestreview-3075958738" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-31T15:40:54Z

LGTM label has been added.  <details>Git tree hash: 419b9813cd4f1e205e613c0710e155be56fd379f</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T10:52:29Z

/unhold
/lgtm
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-01T10:52:37Z

LGTM label has been added.  <details>Git tree hash: de9caa95620f347795ce8ada3d3a82cfa08ef2a2</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:15:15Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T12:15:52Z

@tenzen-y: new pull request created: #6350

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6271#issuecomment-3144381214):

>/cherry-pick website
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-31T08:37:32Z

- **site/content/en/docs/concepts/multikueue.md:69** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-31T08:31:47Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
```

  could you make this as Note?

- **site/content/en/docs/concepts/multikueue.md:77** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-31T08:33:05Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+It provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted,
+once the field is set it becomes immutable and it's no longer possible to set `status.nominatedClusterNames`.
```

  IIUC, the `.status.nominatedClusterNames` is reset after the clustrName is set, right?

- **site/content/en/docs/concepts/multikueue.md:95** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-31T08:36:30Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+It provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted,
+once the field is set it becomes immutable and it's no longer possible to set `status.nominatedClusterNames`.
+
+### AllAtOnce (Default Mode):
+In this mode, the Workload is copied to all available worker clusters as soon as it obtains a QuotaReservation in the manager cluster.
+This approach ensures the fastest possible admission by allowing all clusters to compete for the Workload simultaneously.
+
+### Incremental:
+This mode introduces a gradual dispatching strategy where clusters are nominated in rounds.
+Initially, a subset of up to 3 clusters is selected, and the Workload is copied only to these clusters.
+If none of the nominated clusters admit the Workload within a fixed duration (5 minutes),
+additional 3 clusters are incrementally added in subsequent rounds.
+
+### External (Custom implementation):
```

  Could you mention about needing `kueue-admission` field manager to patch .status.nominatedClusterNames as a known limitation?

- **site/content/en/docs/concepts/multikueue.md:88** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-31T08:37:29Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+It provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted,
+once the field is set it becomes immutable and it's no longer possible to set `status.nominatedClusterNames`.
+
+### AllAtOnce (Default Mode):
+In this mode, the Workload is copied to all available worker clusters as soon as it obtains a QuotaReservation in the manager cluster.
+This approach ensures the fastest possible admission by allowing all clusters to compete for the Workload simultaneously.
+
+### Incremental:
```

  Could you mention about the all worker clusters are sorted with dictionary order, and then every 3 clusters are nominated from the sorted workers?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-07-31T11:13:31Z

- **site/content/en/docs/concepts/multikueue.md:77** by [@mszadkow](https://github.com/mszadkow) — 2025-07-31T11:13:30Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+It provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted,
+once the field is set it becomes immutable and it's no longer possible to set `status.nominatedClusterNames`.
```

  Correct, I will mention it.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-07-31T11:14:21Z

- **site/content/en/docs/concepts/multikueue.md:95** by [@mszadkow](https://github.com/mszadkow) — 2025-07-31T11:14:21Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+It provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted,
+once the field is set it becomes immutable and it's no longer possible to set `status.nominatedClusterNames`.
+
+### AllAtOnce (Default Mode):
+In this mode, the Workload is copied to all available worker clusters as soon as it obtains a QuotaReservation in the manager cluster.
+This approach ensures the fastest possible admission by allowing all clusters to compete for the Workload simultaneously.
+
+### Incremental:
+This mode introduces a gradual dispatching strategy where clusters are nominated in rounds.
+Initially, a subset of up to 3 clusters is selected, and the Workload is copied only to these clusters.
+If none of the nominated clusters admit the Workload within a fixed duration (5 minutes),
+additional 3 clusters are incrementally added in subsequent rounds.
+
+### External (Custom implementation):
```

  Yes, I believe it must be mentioned, wdyt @mimowo ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-31T11:27:39Z

- **site/content/en/docs/concepts/multikueue.md:95** by [@mimowo](https://github.com/mimowo) — 2025-07-31T11:27:38Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+It provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted,
+once the field is set it becomes immutable and it's no longer possible to set `status.nominatedClusterNames`.
+
+### AllAtOnce (Default Mode):
+In this mode, the Workload is copied to all available worker clusters as soon as it obtains a QuotaReservation in the manager cluster.
+This approach ensures the fastest possible admission by allowing all clusters to compete for the Workload simultaneously.
+
+### Incremental:
+This mode introduces a gradual dispatching strategy where clusters are nominated in rounds.
+Initially, a subset of up to 3 clusters is selected, and the Workload is copied only to these clusters.
+If none of the nominated clusters admit the Workload within a fixed duration (5 minutes),
+additional 3 clusters are incrementally added in subsequent rounds.
+
+### External (Custom implementation):
```

  Yes, for sure. Thank you for pointing this limitation out.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-07-31T11:28:54Z

- **site/content/en/docs/concepts/multikueue.md:69** by [@mszadkow](https://github.com/mszadkow) — 2025-07-31T11:28:54Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
```

  Done.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-07-31T11:29:00Z

- **site/content/en/docs/concepts/multikueue.md:88** by [@mszadkow](https://github.com/mszadkow) — 2025-07-31T11:29:00Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+It provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted,
+once the field is set it becomes immutable and it's no longer possible to set `status.nominatedClusterNames`.
+
+### AllAtOnce (Default Mode):
+In this mode, the Workload is copied to all available worker clusters as soon as it obtains a QuotaReservation in the manager cluster.
+This approach ensures the fastest possible admission by allowing all clusters to compete for the Workload simultaneously.
+
+### Incremental:
```

  Done.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-07-31T11:29:04Z

- **site/content/en/docs/concepts/multikueue.md:95** by [@mszadkow](https://github.com/mszadkow) — 2025-07-31T11:29:04Z
```diff
@@ -61,6 +63,35 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+It provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted,
+once the field is set it becomes immutable and it's no longer possible to set `status.nominatedClusterNames`.
+
+### AllAtOnce (Default Mode):
+In this mode, the Workload is copied to all available worker clusters as soon as it obtains a QuotaReservation in the manager cluster.
+This approach ensures the fastest possible admission by allowing all clusters to compete for the Workload simultaneously.
+
+### Incremental:
+This mode introduces a gradual dispatching strategy where clusters are nominated in rounds.
+Initially, a subset of up to 3 clusters is selected, and the Workload is copied only to these clusters.
+If none of the nominated clusters admit the Workload within a fixed duration (5 minutes),
+additional 3 clusters are incrementally added in subsequent rounds.
+
+### External (Custom implementation):
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-31T15:40:48Z

LGTM, thanks
/lgtm
/approve

/hold 
in case of @mimowo want to leave commen.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-31T15:51:04Z

- **site/content/en/docs/concepts/multikueue.md:105** by [@mimowo](https://github.com/mimowo) — 2025-07-31T15:51:04Z
```diff
@@ -61,6 +63,48 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+{{% alert title="Note" color="primary" %}}
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+{{% /alert %}}
+Provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted.
+Once the field is set, it becomes immutable and the `status.nominatedClusterNames` field is reset,
+and it is no longer possible to set it. 
+
+This ensures that the Workload's cluster assignment is finalized and prevents further nomination of clusters.
+
+### AllAtOnce (Default Mode):
+In this mode, the Workload is copied to all available worker clusters as soon as it obtains a QuotaReservation in the manager cluster.
+This approach ensures the fastest possible admission by allowing all clusters to compete for the Workload simultaneously.
+
+### Incremental:
+This mode introduces a gradual dispatching strategy where clusters are nominated in rounds.
+Initially, all worker clusters are sorted in dictionary order, and a subset of up to 3 clusters is selected from the sorted list.
+The Workload is copied only to these nominated clusters.
+If none of the nominated clusters admit the Workload within a fixed duration (5 minutes),
+an additional up to 3 clusters are incrementally added in subsequent rounds, following the same dictionary order.
+
+### External (Custom implementation):
+In this mode, the selection of worker clusters is delegated to an external controller.
+The external controller is responsible for setting the `.status.nominatedClusterNames` field in the Workload to specify the clusters where it should be copied.
+
+The MultiKueue Workload Controller synchronizes the Workload with the nominated clusters.
+
+Known Limitation:
+{{% alert title="Warning" color="primary" %}}
+For the external controller to patch the `.status.nominatedClusterNames` field, it must use the kueue-admission field manager.
+This is required because the kueue-admission field manager is responsible for managing updates to the `.status.nominatedClusterNames` field.
+Without this, the external controller may encounter conflicts or be unable to update the field.
```

  ```suggestion
  This is required because the kueue-admission field manager is responsible for managing updates to the `.status.nominatedClusterNames` field.
  Without this, the Kueue is not able to admit the MultiKueue workloads.
  ```
  The previous proposal seems missing the point, unless I'm missing something, let me know @mszadkow 
  
  My understanding the fact that external controller may conflict is irrelevant. The issue is that when it takes the field ownership to say "my-field", then the core Kueue cannot admit the workload, because it  cannot reset `.status.nominatedClusterNames`, as a result it cannot set `status.clusterName` (which is done in the same request, because they are mutually exclusive). As a result it cannot set AC Ready which is needed to admit.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-08-01T10:50:18Z

- **site/content/en/docs/concepts/multikueue.md:105** by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T10:50:18Z
```diff
@@ -61,6 +63,48 @@ The dispatching flow works as follows:
    - The manager performs a final status sync.
    - It then deletes the corresponding objects from the worker cluster.
 
+## Workload Dispatching
+
+{{% alert title="Note" color="primary" %}}
+The MultiKueue Dispatcher mechanism is available since Kueue v0.13.
+{{% /alert %}}
+Provides a flexible way to control how workloads are distributed across worker clusters
+by allowing users to choose between built-in dispatching algorithms or implement custom ones.
+This mechanism ensures efficient workload placement while minimizing resource contention and preemptions across clusters.
+
+The `status.nominatedClusterNames` field lists the worker clusters currently being considered for scheduling the Workload,
+as determined by the dispatching algorithm, and is updated while the Workload is pending admission.
+
+The `status.clusterName` field specifies the worker cluster where the Workload has been successfully admitted.
+Once the field is set, it becomes immutable and the `status.nominatedClusterNames` field is reset,
+and it is no longer possible to set it. 
+
+This ensures that the Workload's cluster assignment is finalized and prevents further nomination of clusters.
+
+### AllAtOnce (Default Mode):
+In this mode, the Workload is copied to all available worker clusters as soon as it obtains a QuotaReservation in the manager cluster.
+This approach ensures the fastest possible admission by allowing all clusters to compete for the Workload simultaneously.
+
+### Incremental:
+This mode introduces a gradual dispatching strategy where clusters are nominated in rounds.
+Initially, all worker clusters are sorted in dictionary order, and a subset of up to 3 clusters is selected from the sorted list.
+The Workload is copied only to these nominated clusters.
+If none of the nominated clusters admit the Workload within a fixed duration (5 minutes),
+an additional up to 3 clusters are incrementally added in subsequent rounds, following the same dictionary order.
+
+### External (Custom implementation):
+In this mode, the selection of worker clusters is delegated to an external controller.
+The external controller is responsible for setting the `.status.nominatedClusterNames` field in the Workload to specify the clusters where it should be copied.
+
+The MultiKueue Workload Controller synchronizes the Workload with the nominated clusters.
+
+Known Limitation:
+{{% alert title="Warning" color="primary" %}}
+For the external controller to patch the `.status.nominatedClusterNames` field, it must use the kueue-admission field manager.
+This is required because the kueue-admission field manager is responsible for managing updates to the `.status.nominatedClusterNames` field.
+Without this, the external controller may encounter conflicts or be unable to update the field.
```

  Yes correct,  the suggestion is more accurate explanation.
