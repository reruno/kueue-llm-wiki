# PR #6283: Fix evicting workloads due to AdmissionCheck retry

**Summary**: Fix evicting workloads due to AdmissionCheck retry

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6283

**Last updated**: 2026-02-21T15:46:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-provisioning-retries-new` → `main`
- **Created**: 2025-07-30T12:37:33Z
- **Updated**: 2026-02-21T15:46:48Z
- **Merged**: 2025-07-30T16:52:27Z
- **Closed**: 2025-07-30T16:52:27Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@pajakd](https://github.com/pajakd), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 8   **+68 / -20**

## Description

#### What type of PR is this?

/kind bug


<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #5129
Fixes #5573
Fixes #5480

#### Special notes for your reviewer:

Since the issue has a racy nature I opened also experiment PRs:
- https://github.com/kubernetes-sigs/kueue/pull/6285 - aims to show the repro using 300 repeats on existing code
- https://github.com/kubernetes-sigs/kueue/pull/6284 - aims to show on CI the fix works

To minimize the scope of the fix I only use the strict mode for requests which operate on the status.admissionChecks.state. For other requests I leave non-strict (but we may revist that later).

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix the bug which could occasionally cause workloads evicted by the built-in AdmissionChecks
(ProvisioningRequest and MultiKueue) to get stuck in the evicted state which didn't allow re-scheduling.
This could happen when the AdmissionCheck controller would trigger eviction by setting the
Admission check state to "Retry".
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-30T12:37:43Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a8d5130e1d3bff2391449dad0708d13bc252d137 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/688a215c1e0513000866c51f |

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T12:46:09Z

/cc @tenzen-y @PBundyra @pajakd ptal

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-30T12:46:15Z

@mimowo: GitHub didn't allow me to request PR reviews from the following users: ptal.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6283#issuecomment-3136170548):

>/cc @tenzen-y @PBundyra @pajakd ptal
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T12:46:58Z

/hold 
to avoid premature merging (we may prefer to wait for the experiment PRs).

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:45:35Z

@mimowo Could we make release-note as more generic? In other workds, could we say that AdmissionCheck mechanism occurs the rollback problem in case of Eviction, and the effective ranges are MultiKueue and ProvisioningRequest and custom AdmissionCheck controller?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-30T13:48:18Z

LGTM label has been added.  <details>Git tree hash: 00464bd6eb4e00679418a7d061b86be1737f480a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-30T13:48:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6283#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6283#pullrequestreview-3071643864" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T13:48:37Z

Sure, but this change does not affect implementations of the custom AC controllers directly.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:50:46Z

> Sure, but this change does not affect implementations of the custom AC controllers directly.

Ah, right. If they use BaseSSAWorkload function, they could technically face the same problem.
But, we don't want to care all custom cases.

So, as you said, we can focus on ProvReq and MultiKueue, but we want to mention about AdmissionCheck problem so that the external AdmissionCheck controller devel could check their code whether their controller has the same problem.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T13:52:37Z

/release-note-edit
```release-note
Fix the bug which could occasionally cause workloads evicted, by the built-in AdmissionChecks
(ProvisioningRequest and MultiKueue), to get stuck in the evicted state which didn't allow re-scheduling.
This could happen when the AdmissionCheck controller would trigger eviction by setting the
Admission check state to "Retry".
```
wdyt?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T13:53:00Z

/release-note-edit
```release-note
Fix the bug which could occasionally cause workloads evicted by the built-in AdmissionChecks
(ProvisioningRequest and MultiKueue) to get stuck in the evicted state which didn't allow re-scheduling.
This could happen when the AdmissionCheck controller would trigger eviction by setting the
Admission check state to "Retry".
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:57:33Z

> /release-note-edit
> 
> ```
> Fix the bug which could occasionally cause workloads evicted, by the built-in AdmissionChecks
> (ProvisioningRequest and MultiKueue), to get stuck in the evicted state which didn't allow re-scheduling.
> This could happen when the AdmissionCheck controller would trigger eviction by setting the
> Admission check state to "Retry".
> ```
> 
> wdyt?

SGTM, thanks.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T14:08:56Z

/test pull-kueue-test-integration-baseline-main
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-multikueue-main
/test pull-kueue-test-e2e-multikueue-main
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-31

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T14:12:52Z

Just trying

/cherry-pick release-0.12
/cherry-pick release-0.13

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-07-30T14:12:54Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of `release-0.12`, `release-0.13` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6283#issuecomment-3136543061):

>Just trying
>
>/cherry-pick release-0.12
>/cherry-pick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T14:25:17Z

/test pull-kueue-test-integration-baseline-main
https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6283/pull-kueue-test-integration-baseline-main/1950559248463171584
this is a known new flake https://github.com/kubernetes-sigs/kueue/issues/6227. Hopefully we can address in the coming days.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T14:57:31Z

/test pull-kueue-test-integration-baseline-main
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-multikueue-main
/test pull-kueue-test-e2e-multikueue-main
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-31

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T15:00:22Z

> Just trying
> 
> /cherry-pick release-0.12 /cherry-pick release-0.13

I tested locally and it merges without conflicts to the branches

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T15:09:42Z

> > Just trying
> > /cherry-pick release-0.12 /cherry-pick release-0.13
> 
> I tested locally and it merges without conflicts to the branches

great to hear that

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T15:26:50Z

/test pull-kueue-test-integration-baseline-main
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-multikueue-main
/test pull-kueue-test-e2e-multikueue-main
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-31

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T16:02:50Z

/test pull-kueue-test-integration-baseline-main
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-multikueue-main
/test pull-kueue-test-e2e-multikueue-main
/test pull-kueue-test-e2e-main-1-33
/test pull-kueue-test-e2e-main-1-32
/test pull-kueue-test-e2e-main-1-31

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T16:08:16Z

Unfortunately running the test on CI despite nearly 1000 runs didn't bring failures (the experiment PRs), so I think we need to rely on the presence / absence of the rare flakes + manual testing locally, but reproducing the failure is tricky.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-30T16:30:57Z

> Let us try performing tests 5 times.
> /hold

Done for integration and e2e tests, no flakes other than already known ones.
/unhold

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-07-30T16:53:06Z

@tenzen-y: new pull request created: #6298

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6283#issuecomment-3136543061):

>Just trying
>
>/cherry-pick release-0.12
>/cherry-pick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-07-30T16:53:42Z

@tenzen-y: new pull request created: #6299

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6283#issuecomment-3136543061):

>Just trying
>
>/cherry-pick release-0.12
>/cherry-pick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-30T13:22:52Z

Basically, lgtm.
Awesome fixing!

- **test/e2e/singlecluster/e2e_test.go:657** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:16:41Z
```diff
@@ -654,7 +654,7 @@ var _ = ginkgo.Describe("Kueue", func() {
 			ginkgo.By("setting the check as successful", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					patch := workload.BaseSSAWorkload(createdWorkload)
+					patch := workload.BaseSSAWorkload(createdWorkload, true)
```

  I think that it would be better to create a testing function, `BaseSSAWorkload`, to enforce strict mode in E2E tests.
  If we allow them to disable strict mode, we will make the regressions.
  
  However, the `BaseSSAWorkload` testing function could block the misconfigurations in E2E tests.

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:953** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:19:07Z
```diff
@@ -917,6 +917,52 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			util.ExpectObjectToBeDeleted(ctx, k8sClient, prc, true)
 		})
 
+		ginkgo.It("Admission checks for an evicted workload are Pending", func() {
+			// Repro for https://github.com/kubernetes-sigs/kueue/issues/5129
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the provision request-1 as Failed", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+				}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.Failed,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.Failed,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the Workload is Evicted", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
+					g.Expect(evicted).To(gomega.BeTrue())
+				}, util.Timeout, time.Millisecond).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the AdmissionChecks are reset to Pending and remain this way", func() {
```

  Just checking, is this the core verification for the observed bugs?

- **pkg/controller/admissionchecks/provisioning/controller.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:21:29Z
  IIUC, these changes are key to preventing rolling back to the outdated Admission.

- **pkg/controller/admissionchecks/multikueue/workload.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:22:40Z
  Just checking, this is not reported from your customers. But, MultiKueue Admission could technically occur the same problem as ProvReq since both relies on the AdmissionCheck mechanism, right?
  
  If yes, that makes sense to contain this fix in this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-30T13:28:00Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:953** by [@mimowo](https://github.com/mimowo) — 2025-07-30T13:27:59Z
```diff
@@ -917,6 +917,52 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			util.ExpectObjectToBeDeleted(ctx, k8sClient, prc, true)
 		})
 
+		ginkgo.It("Admission checks for an evicted workload are Pending", func() {
+			// Repro for https://github.com/kubernetes-sigs/kueue/issues/5129
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the provision request-1 as Failed", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+				}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.Failed,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.Failed,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the Workload is Evicted", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
+					g.Expect(evicted).To(gomega.BeTrue())
+				}, util.Timeout, time.Millisecond).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the AdmissionChecks are reset to Pending and remain this way", func() {
```

  Yes, exactly. To me more precise the first assert is enough:
  
  ```
  check := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
  g.Expect(check).NotTo(gomega.BeNil())
  g.Expect(check.State).To(gomega.Equal(kueue.CheckStatePending), fmt.Sprintf("workload status: %v, conditions: %v", updatedWl.Status, updatedWl.Status.Conditions))
  ```
  Let me just cleanup the message

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-30T13:28:31Z

- **pkg/controller/admissionchecks/provisioning/controller.go:1** by [@mimowo](https://github.com/mimowo) — 2025-07-30T13:28:31Z
  Yes, these are the main changes for the fix (to use strict in provisioning controller).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-30T13:34:22Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1** by [@mimowo](https://github.com/mimowo) — 2025-07-30T13:34:21Z
  Yes, I think exactly the same issue could occur in MultiKueue in case of retry admission Check.
  
  However, the conditions required to repro require to trigger retry, similar to "The connection to a worker cluster is unreliable", and then await for re-admission. IIRC we don't have such test currently, but it would be tricky to add, and even then it would probably only fail 1/100 (similar as for ProvReq).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-30T13:39:06Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:953** by [@mimowo](https://github.com/mimowo) — 2025-07-30T13:39:06Z
```diff
@@ -917,6 +917,52 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			util.ExpectObjectToBeDeleted(ctx, k8sClient, prc, true)
 		})
 
+		ginkgo.It("Admission checks for an evicted workload are Pending", func() {
+			// Repro for https://github.com/kubernetes-sigs/kueue/issues/5129
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the provision request-1 as Failed", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+				}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.Failed,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.Failed,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the Workload is Evicted", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
+					g.Expect(evicted).To(gomega.BeTrue())
+				}, util.Timeout, time.Millisecond).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the AdmissionChecks are reset to Pending and remain this way", func() {
```

  cleaned up, ptal

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-30T13:40:43Z

- **test/e2e/singlecluster/e2e_test.go:657** by [@mimowo](https://github.com/mimowo) — 2025-07-30T13:40:43Z
```diff
@@ -654,7 +654,7 @@ var _ = ginkgo.Describe("Kueue", func() {
 			ginkgo.By("setting the check as successful", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					patch := workload.BaseSSAWorkload(createdWorkload)
+					patch := workload.BaseSSAWorkload(createdWorkload, true)
```

  Yes, I think this is better to use `BaseSSAWorkload(w, true)` in tests consistently. 
  
  I was thinking to introduce function like `testing/util -> BaseSSAWorkloadForTests(w)`, wdyt?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-30T13:42:30Z

- **test/e2e/singlecluster/e2e_test.go:657** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:42:30Z
```diff
@@ -654,7 +654,7 @@ var _ = ginkgo.Describe("Kueue", func() {
 			ginkgo.By("setting the check as successful", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					patch := workload.BaseSSAWorkload(createdWorkload)
+					patch := workload.BaseSSAWorkload(createdWorkload, true)
```

  SGTM, thank you.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-30T13:43:18Z

- **test/e2e/singlecluster/e2e_test.go:657** by [@mimowo](https://github.com/mimowo) — 2025-07-30T13:43:18Z
```diff
@@ -654,7 +654,7 @@ var _ = ginkgo.Describe("Kueue", func() {
 			ginkgo.By("setting the check as successful", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					patch := workload.BaseSSAWorkload(createdWorkload)
+					patch := workload.BaseSSAWorkload(createdWorkload, true)
```

  Updated tentatively per suggestion - naming the function as util.BaseSSAWorkload, ptal

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-30T13:43:46Z

- **pkg/controller/admissionchecks/multikueue/workload.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:43:46Z
  I don't request to add such a tricky test for now.
  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-30T13:46:33Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:953** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:46:32Z
```diff
@@ -917,6 +917,52 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 			util.ExpectObjectToBeDeleted(ctx, k8sClient, prc, true)
 		})
 
+		ginkgo.It("Admission checks for an evicted workload are Pending", func() {
+			// Repro for https://github.com/kubernetes-sigs/kueue/issues/5129
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
+					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the provision request-1 as Failed", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 1),
+				}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   autoscaling.Failed,
+						Status: metav1.ConditionTrue,
+						Reason: autoscaling.Failed,
+					})
+					g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the Workload is Evicted", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
+					_, evicted := workload.IsEvictedByAdmissionCheck(&updatedWl)
+					g.Expect(evicted).To(gomega.BeTrue())
+				}, util.Timeout, time.Millisecond).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the AdmissionChecks are reset to Pending and remain this way", func() {
```

  SGTM, thank you.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-30T13:46:54Z

- **test/e2e/singlecluster/e2e_test.go:657** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T13:46:53Z
```diff
@@ -654,7 +654,7 @@ var _ = ginkgo.Describe("Kueue", func() {
 			ginkgo.By("setting the check as successful", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
 					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					patch := workload.BaseSSAWorkload(createdWorkload)
+					patch := workload.BaseSSAWorkload(createdWorkload, true)
```

  Both sounds good to me.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-30T13:48:12Z

/lgtm
/approve

Let us try performing tests 5 times.

/hold
