# PR #6306: Automated cherry pick of #6300: Support disabling the client-side rate limiting

**Summary**: Automated cherry pick of #6300: Support disabling the client-side rate limiting

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6306

**Last updated**: 2025-07-31T07:06:45Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `automated-cherry-pick-of-#6300-upstream-release-0.12` → `release-0.12`
- **Created**: 2025-07-30T18:12:20Z
- **Updated**: 2025-07-31T07:06:45Z
- **Merged**: 2025-07-31T06:59:16Z
- **Closed**: 2025-07-31T06:59:16Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `cncf-cla: yes`, `size/S`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo)
- **Changed files**: 3   **+10 / -1**

## Description

Cherry pick of #6300 on release-0.12.

#6300: Support disabling the client-side rate limiting

For details on the cherry pick process, see the [cherry pick requests](https://git.k8s.io/community/contributors/devel/sig-release/cherry-picks.md) page.

```release-note
Support disabling client-side ratelimiting in Config API clientConnection.qps with a negative value (e.g., -1)
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-30T18:12:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 08b0ab6743c0fcede86211ea1589406aa06761eb |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/688a608704340700081eb429 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6306--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6306--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTYzMDYtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.3V99PL7fGHsf3jDTWOffMGsmXxBOer7CCThxE-qudhQ)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6306--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-30T18:12:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6306#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/release-0.12/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T18:12:38Z

/kind bug

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T18:14:31Z

/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-31T06:57:40Z

/lgtm
Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-31T06:57:45Z

LGTM label has been added.  <details>Git tree hash: a0790f4adacee6978fe1c2ebe245a87c9c11a25c</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-30T18:13:18Z

- **cmd/kueue/main.go:196** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-30T18:13:18Z
```diff
@@ -187,7 +187,13 @@ func main() {
 
 	// Set the RateLimiter here, otherwise the controller-runtime's typedClient will use a different RateLimiter
 	// for each API type.
-	kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	// When the controller-runtime <= 0.20, the client-side ratelimiting will be disabled only when a QPS has a negative value.
+	// The following QPS negative value chack allows us to disable the client-side ratelimiting.
+	if *cfg.ClientConnection.QPS >= 0.0 {
+		kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	} else {
+		kubeConfig.QPS = *cfg.ClientConnection.QPS
+	}
```

  This `else` condition is for controller-runtime <= v0.20.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-31T06:37:13Z

- **cmd/kueue/main.go:196** by [@mimowo](https://github.com/mimowo) — 2025-07-31T06:37:13Z
```diff
@@ -187,7 +187,13 @@ func main() {
 
 	// Set the RateLimiter here, otherwise the controller-runtime's typedClient will use a different RateLimiter
 	// for each API type.
-	kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	// When the controller-runtime <= 0.20, the client-side ratelimiting will be disabled only when a QPS has a negative value.
+	// The following QPS negative value chack allows us to disable the client-side ratelimiting.
+	if *cfg.ClientConnection.QPS >= 0.0 {
+		kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	} else {
+		kubeConfig.QPS = *cfg.ClientConnection.QPS
+	}
```

  Just a double check, does, on controller-runtime <= v0.20 passing negative QPS results in disabling the rate limiter? Can you provide some code reference?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-31T06:46:03Z

- **cmd/kueue/main.go:196** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-31T06:46:03Z
```diff
@@ -187,7 +187,13 @@ func main() {
 
 	// Set the RateLimiter here, otherwise the controller-runtime's typedClient will use a different RateLimiter
 	// for each API type.
-	kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	// When the controller-runtime <= 0.20, the client-side ratelimiting will be disabled only when a QPS has a negative value.
+	// The following QPS negative value chack allows us to disable the client-side ratelimiting.
+	if *cfg.ClientConnection.QPS >= 0.0 {
+		kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	} else {
+		kubeConfig.QPS = *cfg.ClientConnection.QPS
+	}
```

  The controller-runtime mutates default value only when it is 0.0: https://github.com/kubernetes-sigs/controller-runtime/blob/v0.20.2/pkg/client/config/config.go#L95-L120
  
  If it is a negative value, the controller-runtime replies to the client-go specifications. https://github.com/kubernetes/client-go/blob/d99dd130a2fc7519c0bc2bd7271447b2a16c04a2/rest/config.go#L117-L122

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-07-31T06:47:09Z

- **cmd/kueue/main.go:196** by [@tenzen-y](https://github.com/tenzen-y) — 2025-07-31T06:47:09Z
```diff
@@ -187,7 +187,13 @@ func main() {
 
 	// Set the RateLimiter here, otherwise the controller-runtime's typedClient will use a different RateLimiter
 	// for each API type.
-	kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	// When the controller-runtime <= 0.20, the client-side ratelimiting will be disabled only when a QPS has a negative value.
+	// The following QPS negative value chack allows us to disable the client-side ratelimiting.
+	if *cfg.ClientConnection.QPS >= 0.0 {
+		kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	} else {
+		kubeConfig.QPS = *cfg.ClientConnection.QPS
+	}
```

  If we specify the RateLimiter like `if` statement, the negative value is not considered.
  That's why we need to use `QPS` instead of RateLimiter.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-07-31T06:57:30Z

- **cmd/kueue/main.go:196** by [@mimowo](https://github.com/mimowo) — 2025-07-31T06:57:30Z
```diff
@@ -187,7 +187,13 @@ func main() {
 
 	// Set the RateLimiter here, otherwise the controller-runtime's typedClient will use a different RateLimiter
 	// for each API type.
-	kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	// When the controller-runtime <= 0.20, the client-side ratelimiting will be disabled only when a QPS has a negative value.
+	// The following QPS negative value chack allows us to disable the client-side ratelimiting.
+	if *cfg.ClientConnection.QPS >= 0.0 {
+		kubeConfig.RateLimiter = flowcontrol.NewTokenBucketRateLimiter(*cfg.ClientConnection.QPS, int(*cfg.ClientConnection.Burst))
+	} else {
+		kubeConfig.QPS = *cfg.ClientConnection.QPS
+	}
```

  sgtm, thanks for explaining. We can keep it open for posterity reference.
