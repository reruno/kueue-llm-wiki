# PR #632: Documentation update for v1beta1 api

**Summary**: Documentation update for v1beta1 api

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/632

**Last updated**: 2023-03-16T20:43:20Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mwielgus](https://github.com/mwielgus)
- **Branch**: `docs-v1beta1` → `main`
- **Created**: 2023-03-14T23:51:47Z
- **Updated**: 2023-03-16T20:43:20Z
- **Merged**: 2023-03-16T20:43:20Z
- **Closed**: 2023-03-16T20:43:20Z
- **Labels**: `lgtm`, `size/XL`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 7   **+269 / -277**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Documentation update necessary for v1beta1 api graduation.

#### Which issue(s) this PR fixes:

#### Special notes for your reviewer:

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-14T23:51:52Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 66d2df189a37c35f5c22ce98e2168eecc2ed84b6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64137cc33e8c450008025ed5 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-632--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-632--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTYzMi0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.iVWCPjKymyL_xbNhru9K2X2lyvg0kyHN80Nwt990ExA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site settings](https://app.netlify.com/sites/kubernetes-sigs-kueue/settings/deploys#deploy-notifications)._

### Comment by [@mwielgus](https://github.com/mwielgus) — 2023-03-14T23:52:06Z

/assign @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T14:54:03Z

Also please update `config/samples`

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T14:54:15Z

cc @cortespao

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T20:39:20Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-16T20:39:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/632#issuecomment-1472708781" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/632#" title="Author self-approved">mwielgus</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-15T16:29:40Z

- **site/content/en/docs/concepts/cluster_queue.md:24** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:19:34Z
```diff
@@ -18,23 +18,21 @@ Only [cluster administrators](/docs/tasks#batch-administrator) should create `Cl
 A sample ClusterQueue looks like the following:
 
 ```yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ClusterQueue
 metadata:
-  name: cluster-queue
+  name: "cluster-queue"
```

  any particular reason to add the quotes? In general it's not needed and I prefer to keep things simple.

- **site/content/en/docs/concepts/cluster_queue.md:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:20:27Z
```diff
@@ -56,16 +54,16 @@ models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/
 In a process called [admission](/docs/concepts#admission), Kueue assigns to the
 [Workload pod sets](/docs/concepts/workload#pod-sets) a flavor for each resource the pod set
 requests.
-Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
-list that has enough unused `min` quota in the ClusterQueue or the
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroup.flavors`
```

  ```suggestion
  Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroups[*].flavors`
  ```

- **site/content/en/docs/concepts/cluster_queue.md:65** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:22:20Z
```diff
@@ -56,16 +54,16 @@ models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/
 In a process called [admission](/docs/concepts#admission), Kueue assigns to the
 [Workload pod sets](/docs/concepts/workload#pod-sets) a flavor for each resource the pod set
 requests.
-Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
-list that has enough unused `min` quota in the ClusterQueue or the
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroup.flavors`
+list that has enough unused `nominalQuota` quota in the ClusterQueue or the
 ClusterQueue's [cohort](#cohort).
 
-### Codependent resources
+### Resource Groups
 
 It is possible that multiple resources in a ClusterQueue have the same flavors.
 This is typical for `cpu` and `memory`, where the flavors are generally tied to
 a machine family or VM availability policies. When two or more resources in a
```

  To tie two or more resources to the same set of flavors, you can list them in the same resource group.

- **site/content/en/docs/concepts/cluster_queue.md:68** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:22:37Z
```diff
@@ -56,16 +54,16 @@ models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/
 In a process called [admission](/docs/concepts#admission), Kueue assigns to the
 [Workload pod sets](/docs/concepts/workload#pod-sets) a flavor for each resource the pod set
 requests.
-Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
-list that has enough unused `min` quota in the ClusterQueue or the
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroup.flavors`
+list that has enough unused `nominalQuota` quota in the ClusterQueue or the
 ClusterQueue's [cohort](#cohort).
 
-### Codependent resources
+### Resource Groups
 
 It is possible that multiple resources in a ClusterQueue have the same flavors.
 This is typical for `cpu` and `memory`, where the flavors are generally tied to
 a machine family or VM availability policies. When two or more resources in a
-ClusterQueue match their flavors, they are said to be codependent resources.
+ClusterQueue match their flavors, they are said to be a resourceGroup.
 
 To manage codependent resources, you should list the flavors in the ClusterQueue
```

  Update this paragraph.

- **site/content/en/docs/concepts/cluster_queue.md:72** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:23:17Z
```diff
@@ -74,37 +72,37 @@ Kueue assigns the same flavor to the codependent resources that the pod set requ
 An example of a ClusterQueue with codependent resources looks like the following:
```

  ```suggestion
  An example of a ClusterQueue with multiple resource groups looks like the following:
  ```

- **site/content/en/docs/concepts/cluster_queue.md:104** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:23:50Z
```diff
@@ -74,37 +72,37 @@ Kueue assigns the same flavor to the codependent resources that the pod set requ
 An example of a ClusterQueue with codependent resources looks like the following:
 
 ```yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ClusterQueue
 metadata:
-  name: cluster-queue
+  name: "cluster-queue"
 spec:
   namespaceSelector: {}
-  resources:
-  - name: "cpu"
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
     flavors:
-    - name: spot
-      quota:
-        min: 18
-    - name: on_demand
-      quota:
-        min: 9
-  - name: "memory"
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: 36Gi
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+  - coveredResources: ["gpu"]
     flavors:
-    - name: spot
-      quota:
-        min: 72Gi
-    - name: on_demand
-      quota:
-        min: 36Gi
-  - name: "gpu"
-    flavors:
-    - name: vendor1
-      quota:
-        min: 10
-    - name: vendor2
-      quota:
-        min: 10
+    - name: "vendor1"
+      resources:
+      - name: "gpu"
+        nominalQuota: 10
+    - name: "vendor2"
+      resources:
+      - name: "gpu"
+        nominalQuota: 10
 ```
 
 In the example above, `cpu` and `memory` are codependent resources, while `gpu`
```

  ```suggestion
  In the example above, `cpu` and `memory` belong to the same resource group and have the same flavors, while `gpu`
  ```

- **site/content/en/docs/concepts/cluster_queue.md:170** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:24:09Z
```diff
@@ -168,18 +166,19 @@ When a ClusterQueue is part of a cohort, Kueue satisfies the following admission
 semantics:
 
 - When assigning flavors, Kueue goes through the list of flavors in the
-  ClusterQueue's `.spec.resources[*].flavors`. For each flavor, Kueue attempts
+  relevant ResourceGroup inside ClusterQueue's 
+  (`.spec.resourcesGroup.flavors`). For each flavor, Kueue attempts
```

  ```suggestion
    (`.spec.resourcesGroups[*].flavors`). For each flavor, Kueue attempts
  ```

- **site/content/en/docs/concepts/cluster_queue.md:171** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:24:54Z
```diff
@@ -168,18 +166,19 @@ When a ClusterQueue is part of a cohort, Kueue satisfies the following admission
 semantics:
 
 - When assigning flavors, Kueue goes through the list of flavors in the
-  ClusterQueue's `.spec.resources[*].flavors`. For each flavor, Kueue attempts
+  relevant ResourceGroup inside ClusterQueue's 
+  (`.spec.resourcesGroup.flavors`). For each flavor, Kueue attempts
   to fit a Workload's pod set according to the quota defined in the
   ClusterQueue for the flavor and the unused quota in the cohort.
   If the Workload doesn't fit, Kueue evaluates the next flavor in the list.
 - A Workload's pod set resource fits in a flavor defined for a ClusterQueue
-  resource if the sum of requests for the resource:
-  1. Is less than or equal to the unused `.quota.min` for the flavor in the
+  resourceGroup if the sum of requests for the resource:
```

  I think just resource worked here better.

- **site/content/en/docs/concepts/cluster_queue.md:239** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:25:50Z
```diff
@@ -239,22 +235,23 @@ scenarios:
   `team-a-cq` can admit Workloads with resources adding up to `12+9=21` CPUs and
   `48+36=84Gi` of memory.
 - If ClusterQueue `team-b-cq` has pending Workloads and the ClusterQueue
-  `team-a-cq` has all its `min` quota used, Kueue will admit Workloads in
+  `team-a-cq` has all its `nominalQuota` quota used, Kueue will admit Workloads in
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
-  Therefore, Kueue ensures the `min` quota for `team-b-cq` is met.
+  Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
-**Note**: Kueue [does not support preemption](https://github.com/kubernetes-sigs/kueue/issues/83).
-Workloads already admitted by Kueue will _not_ be stopped to make space for new Workloads.
+**Note**:
+Kueue supports preemption and Workloads already admitted by Kueue may be 
```

  ```suggestion
  Kueue supports [preemption](#preemption) and Workloads already admitted by Kueue may be 
  ```

- **site/content/en/docs/concepts/cluster_queue.md:249** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:26:22Z
```diff
@@ -239,22 +235,23 @@ scenarios:
   `team-a-cq` can admit Workloads with resources adding up to `12+9=21` CPUs and
   `48+36=84Gi` of memory.
 - If ClusterQueue `team-b-cq` has pending Workloads and the ClusterQueue
-  `team-a-cq` has all its `min` quota used, Kueue will admit Workloads in
+  `team-a-cq` has all its `nominalQuota` quota used, Kueue will admit Workloads in
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
-  Therefore, Kueue ensures the `min` quota for `team-b-cq` is met.
+  Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
-**Note**: Kueue [does not support preemption](https://github.com/kubernetes-sigs/kueue/issues/83).
-Workloads already admitted by Kueue will _not_ be stopped to make space for new Workloads.
+**Note**:
+Kueue supports preemption and Workloads already admitted by Kueue may be 
+stopped to make space for new Workloads.
 
-### Max quotas
+### BorrowingLimit
 
 To limit the amount of resources that a ClusterQueue can borrow from others,
-you can set the `.spec.resources[*].flavors[*].quota.max`
+you can set the `.spec.resourcesGroup[*].flavors[*].resource.borrowingLimit`
```

  ```suggestion
  you can set the `.spec.resourcesGroups[*].flavors[*].resources[*].borrowingLimit`
  ```

- **site/content/en/docs/concepts/cluster_queue.md:253** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:27:00Z
```diff
@@ -239,22 +235,23 @@ scenarios:
   `team-a-cq` can admit Workloads with resources adding up to `12+9=21` CPUs and
   `48+36=84Gi` of memory.
 - If ClusterQueue `team-b-cq` has pending Workloads and the ClusterQueue
-  `team-a-cq` has all its `min` quota used, Kueue will admit Workloads in
+  `team-a-cq` has all its `nominalQuota` quota used, Kueue will admit Workloads in
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
-  Therefore, Kueue ensures the `min` quota for `team-b-cq` is met.
+  Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
-**Note**: Kueue [does not support preemption](https://github.com/kubernetes-sigs/kueue/issues/83).
-Workloads already admitted by Kueue will _not_ be stopped to make space for new Workloads.
+**Note**:
+Kueue supports preemption and Workloads already admitted by Kueue may be 
+stopped to make space for new Workloads.
 
-### Max quotas
+### BorrowingLimit
 
 To limit the amount of resources that a ClusterQueue can borrow from others,
-you can set the `.spec.resources[*].flavors[*].quota.max`
+you can set the `.spec.resourcesGroup[*].flavors[*].resource.borrowingLimit`
 [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
-`max` must be greater than or equal to `min`.
 
-If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
-borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
+If, for a given flavor/resource, the `borrowingLimit` field is empty or null, 
+a ClusterQueue can borrow up to the sum of min quotas from all the 
```

  ```suggestion
  a ClusterQueue can borrow up to the sum of nominal quotas from all the 
  ```

- **site/content/en/docs/concepts/resource_flavor.md:46** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:27:23Z
```diff
@@ -22,27 +22,28 @@ an [empty ResourceFlavor](#empty-resourceflavor) instead of adding labels to cus
 A sample ResourceFlavor looks like the following:
 
 ```yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ResourceFlavor
 metadata:
   name: spot
-nodeSelector:
-  instance-type: spot
-taints:
-- effect: NoSchedule
-  key: spot
-  value: "true"
+spec:
+  nodeLabels:
+    instance-type: spot
+  nodeTaints:
+  - effect: NoSchedule
+    key: spot
+    value: "true"
 ```
 
 You can use the `.metadata.name` field to reference a ResourceFlavor from a
-[ClusterQueue](/docs/concepts/cluster_queue) in the `.spec.resources[*].flavors[*].name` field.
+[ClusterQueue](/docs/concepts/cluster_queue) in the `.spec.resourceGroups[*].flavors[*].name` field.
 
 ## ResourceFlavor labels
 
 **Requires Kubernetes 1.23 or newer**
 
 To associate a ResourceFlavor with a subset of nodes of your cluster, you can
-configure the `.nodeSelector` field with matching node labels that uniquely identify
+configure the `.nodeLabels` field with matching node labels that uniquely identify
```

  ```suggestion
  configure the `.spec.nodeLabels` field with matching node labels that uniquely identify
  ```

- **site/content/en/docs/concepts/resource_flavor.md:74** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:27:35Z
```diff
@@ -70,7 +71,7 @@ that Kueue selected, Kueue performs the following steps:
 
 ## ResourceFlavor taints
 
-To restrict the usage of a ResourceFlavor, you can configure the `.taints` field.
+To restrict the usage of a ResourceFlavor, you can configure the `.nodeTaints` field.
```

  ```suggestion
  To restrict the usage of a ResourceFlavor, you can configure the `.spec.nodeTaints` field.
  ```

- **site/content/en/docs/tasks/administer_cluster_quotas.md:88** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-15T16:28:07Z
```diff
@@ -84,10 +82,10 @@ Write the manifest for the ResourceFlavor. It should look similar to the followi
 
 ```yaml
 # default-flavor.yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ResourceFlavor
 metadata:
-  name: default
+  name: default-falvor
```

  ```suggestion
    name: default-flavor
  ```

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T16:41:05Z

- **site/content/en/docs/concepts/cluster_queue.md:24** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T16:41:04Z
```diff
@@ -18,23 +18,21 @@ Only [cluster administrators](/docs/tasks#batch-administrator) should create `Cl
 A sample ClusterQueue looks like the following:
 
 ```yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ClusterQueue
 metadata:
-  name: cluster-queue
+  name: "cluster-queue"
```

  We are inconsistent where we put quotes. Somewhere they are placed, somewhere not. Lets for with "" for all names and strings.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T16:43:42Z

- **site/content/en/docs/concepts/cluster_queue.md:57** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T16:43:42Z
```diff
@@ -56,16 +54,16 @@ models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/
 In a process called [admission](/docs/concepts#admission), Kueue assigns to the
 [Workload pod sets](/docs/concepts/workload#pod-sets) a flavor for each resource the pod set
 requests.
-Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
-list that has enough unused `min` quota in the ClusterQueue or the
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroup.flavors`
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T16:43:51Z

- **site/content/en/docs/concepts/cluster_queue.md:72** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T16:43:51Z
```diff
@@ -74,37 +72,37 @@ Kueue assigns the same flavor to the codependent resources that the pod set requ
 An example of a ClusterQueue with codependent resources looks like the following:
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T16:43:57Z

- **site/content/en/docs/concepts/cluster_queue.md:249** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T16:43:57Z
```diff
@@ -239,22 +235,23 @@ scenarios:
   `team-a-cq` can admit Workloads with resources adding up to `12+9=21` CPUs and
   `48+36=84Gi` of memory.
 - If ClusterQueue `team-b-cq` has pending Workloads and the ClusterQueue
-  `team-a-cq` has all its `min` quota used, Kueue will admit Workloads in
+  `team-a-cq` has all its `nominalQuota` quota used, Kueue will admit Workloads in
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
-  Therefore, Kueue ensures the `min` quota for `team-b-cq` is met.
+  Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
-**Note**: Kueue [does not support preemption](https://github.com/kubernetes-sigs/kueue/issues/83).
-Workloads already admitted by Kueue will _not_ be stopped to make space for new Workloads.
+**Note**:
+Kueue supports preemption and Workloads already admitted by Kueue may be 
+stopped to make space for new Workloads.
 
-### Max quotas
+### BorrowingLimit
 
 To limit the amount of resources that a ClusterQueue can borrow from others,
-you can set the `.spec.resources[*].flavors[*].quota.max`
+you can set the `.spec.resourcesGroup[*].flavors[*].resource.borrowingLimit`
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T16:44:04Z

- **site/content/en/docs/concepts/cluster_queue.md:253** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T16:44:03Z
```diff
@@ -239,22 +235,23 @@ scenarios:
   `team-a-cq` can admit Workloads with resources adding up to `12+9=21` CPUs and
   `48+36=84Gi` of memory.
 - If ClusterQueue `team-b-cq` has pending Workloads and the ClusterQueue
-  `team-a-cq` has all its `min` quota used, Kueue will admit Workloads in
+  `team-a-cq` has all its `nominalQuota` quota used, Kueue will admit Workloads in
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
-  Therefore, Kueue ensures the `min` quota for `team-b-cq` is met.
+  Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
-**Note**: Kueue [does not support preemption](https://github.com/kubernetes-sigs/kueue/issues/83).
-Workloads already admitted by Kueue will _not_ be stopped to make space for new Workloads.
+**Note**:
+Kueue supports preemption and Workloads already admitted by Kueue may be 
+stopped to make space for new Workloads.
 
-### Max quotas
+### BorrowingLimit
 
 To limit the amount of resources that a ClusterQueue can borrow from others,
-you can set the `.spec.resources[*].flavors[*].quota.max`
+you can set the `.spec.resourcesGroup[*].flavors[*].resource.borrowingLimit`
 [quantity](https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/) field.
-`max` must be greater than or equal to `min`.
 
-If, for a given flavor, the `max` field is empty or null, a ClusterQueue can
-borrow up to the sum of min quotas from all the ClusterQueues in the cohort.
+If, for a given flavor/resource, the `borrowingLimit` field is empty or null, 
+a ClusterQueue can borrow up to the sum of min quotas from all the 
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T16:44:23Z

- **site/content/en/docs/concepts/resource_flavor.md:74** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T16:44:22Z
```diff
@@ -70,7 +71,7 @@ that Kueue selected, Kueue performs the following steps:
 
 ## ResourceFlavor taints
 
-To restrict the usage of a ResourceFlavor, you can configure the `.taints` field.
+To restrict the usage of a ResourceFlavor, you can configure the `.nodeTaints` field.
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T16:44:48Z

- **site/content/en/docs/concepts/resource_flavor.md:74** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T16:44:47Z
```diff
@@ -70,7 +71,7 @@ that Kueue selected, Kueue performs the following steps:
 
 ## ResourceFlavor taints
 
-To restrict the usage of a ResourceFlavor, you can configure the `.taints` field.
+To restrict the usage of a ResourceFlavor, you can configure the `.nodeTaints` field.
```

  Done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-15T16:53:25Z

- **site/content/en/docs/tasks/administer_cluster_quotas.md:48** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-15T16:53:25Z
```diff
@@ -40,23 +40,21 @@ Write the manifest for the ClusterQueue. It should look similar to the following
 
 ```yaml
 # cluster-queue.yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ClusterQueue
 metadata:
-  name: cluster-queue
+  name: "cluster-queue"
 spec:
-  namespaceSelector: {} # match all.
-  resources:
-  - name: "cpu"
-    flavors:
-    - name: default-flavor
-      quota:
-        min: 9
-  - name: "memory"
+  namespaceSelector: {}
```

  ```suggestion
    namespaceSelector: {} # match all.
  ```
  
  Can we keep using a comment?

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2023-03-15T18:15:43Z

- **site/content/en/docs/concepts/cluster_queue.md:24** by [@cortespao](https://github.com/cortespao) — 2023-03-15T18:15:43Z
```diff
@@ -18,23 +18,21 @@ Only [cluster administrators](/docs/tasks#batch-administrator) should create `Cl
 A sample ClusterQueue looks like the following:
 
 ```yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ClusterQueue
 metadata:
-  name: cluster-queue
+  name: "cluster-queue"
```

  My two cents:
  I lean towards not  putting quotation marks around code unless the quotation marks are mandatory part of the code. I suggest not using quotation consistently.
  
  If the K8s SG says otherwise, please disregard my comment.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T22:13:12Z

- **site/content/en/docs/concepts/cluster_queue.md:68** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T22:13:12Z
```diff
@@ -56,16 +54,16 @@ models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/
 In a process called [admission](/docs/concepts#admission), Kueue assigns to the
 [Workload pod sets](/docs/concepts/workload#pod-sets) a flavor for each resource the pod set
 requests.
-Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
-list that has enough unused `min` quota in the ClusterQueue or the
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroup.flavors`
+list that has enough unused `nominalQuota` quota in the ClusterQueue or the
 ClusterQueue's [cohort](#cohort).
 
-### Codependent resources
+### Resource Groups
 
 It is possible that multiple resources in a ClusterQueue have the same flavors.
 This is typical for `cpu` and `memory`, where the flavors are generally tied to
 a machine family or VM availability policies. When two or more resources in a
-ClusterQueue match their flavors, they are said to be codependent resources.
+ClusterQueue match their flavors, they are said to be a resourceGroup.
 
 To manage codependent resources, you should list the flavors in the ClusterQueue
```

  I wanted to remove it altogether. Must have missed it. Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T22:13:19Z

- **site/content/en/docs/concepts/cluster_queue.md:65** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T22:13:18Z
```diff
@@ -56,16 +54,16 @@ models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/
 In a process called [admission](/docs/concepts#admission), Kueue assigns to the
 [Workload pod sets](/docs/concepts/workload#pod-sets) a flavor for each resource the pod set
 requests.
-Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
-list that has enough unused `min` quota in the ClusterQueue or the
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroup.flavors`
+list that has enough unused `nominalQuota` quota in the ClusterQueue or the
 ClusterQueue's [cohort](#cohort).
 
-### Codependent resources
+### Resource Groups
 
 It is possible that multiple resources in a ClusterQueue have the same flavors.
 This is typical for `cpu` and `memory`, where the flavors are generally tied to
 a machine family or VM availability policies. When two or more resources in a
```

  Dome.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T22:17:12Z

- **site/content/en/docs/concepts/cluster_queue.md:24** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T22:17:12Z
```diff
@@ -18,23 +18,21 @@ Only [cluster administrators](/docs/tasks#batch-administrator) should create `Cl
 A sample ClusterQueue looks like the following:
 
 ```yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ClusterQueue
 metadata:
-  name: cluster-queue
+  name: "cluster-queue"
```

  I couldn't find an official style guide for K8S yamls. I can pass all yamls through some formatter, but would prefer to do it as a separate PR.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-15T22:17:38Z

- **site/content/en/docs/tasks/administer_cluster_quotas.md:88** by [@mwielgus](https://github.com/mwielgus) — 2023-03-15T22:17:37Z
```diff
@@ -84,10 +82,10 @@ Write the manifest for the ResourceFlavor. It should look similar to the followi
 
 ```yaml
 # default-flavor.yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ResourceFlavor
 metadata:
-  name: default
+  name: default-falvor
```

  Done.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-16T15:19:41Z

- **config/samples/job.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:16:01Z
  why a new file instead of the existing one?

- **config/samples/job.yaml:5** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:16:06Z
```diff
@@ -0,0 +1,20 @@
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  annotations:
```

  labels

- **config/samples/other.yaml:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:16:35Z
  What's this for?

- **site/content/en/docs/concepts/cluster_queue.md:24** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:16:59Z
```diff
@@ -18,23 +18,21 @@ Only [cluster administrators](/docs/tasks#batch-administrator) should create `Cl
 A sample ClusterQueue looks like the following:
 
 ```yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ClusterQueue
 metadata:
-  name: cluster-queue
+  name: "cluster-queue"
```

  I also couldn't find a rule about it, but it looks like most samples don't have quotes.
  
  But doing it in a separate PR sounds good

- **site/content/en/docs/concepts/cluster_queue.md:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:17:54Z
```diff
@@ -56,16 +54,16 @@ models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/
 In a process called [admission](/docs/concepts#admission), Kueue assigns to the
 [Workload pod sets](/docs/concepts/workload#pod-sets) a flavor for each resource the pod set
 requests.
-Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
-list that has enough unused `min` quota in the ClusterQueue or the
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroup.flavors`
```

  Still unresolved. Forgot to push?

- **site/content/en/docs/concepts/cluster_queue.md:104** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:18:33Z
```diff
@@ -74,37 +72,37 @@ Kueue assigns the same flavor to the codependent resources that the pod set requ
 An example of a ClusterQueue with codependent resources looks like the following:
 
 ```yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ClusterQueue
 metadata:
-  name: cluster-queue
+  name: "cluster-queue"
 spec:
   namespaceSelector: {}
-  resources:
-  - name: "cpu"
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
     flavors:
-    - name: spot
-      quota:
-        min: 18
-    - name: on_demand
-      quota:
-        min: 9
-  - name: "memory"
+    - name: "spot"
+      resources:
+      - name: "cpu"
+        nominalQuota: 9
+      - name: "memory"
+        nominalQuota: 36Gi
+    - name: "on-demand"
+      resources:
+      - name: "cpu"
+        nominalQuota: 18
+      - name: "memory"
+        nominalQuota: 72Gi
+  - coveredResources: ["gpu"]
     flavors:
-    - name: spot
-      quota:
-        min: 72Gi
-    - name: on_demand
-      quota:
-        min: 36Gi
-  - name: "gpu"
-    flavors:
-    - name: vendor1
-      quota:
-        min: 10
-    - name: vendor2
-      quota:
-        min: 10
+    - name: "vendor1"
+      resources:
+      - name: "gpu"
+        nominalQuota: 10
+    - name: "vendor2"
+      resources:
+      - name: "gpu"
+        nominalQuota: 10
 ```
 
 In the example above, `cpu` and `memory` are codependent resources, while `gpu`
```

  Still pending

- **site/content/en/docs/concepts/cluster_queue.md:171** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:18:40Z
```diff
@@ -168,18 +166,19 @@ When a ClusterQueue is part of a cohort, Kueue satisfies the following admission
 semantics:
 
 - When assigning flavors, Kueue goes through the list of flavors in the
-  ClusterQueue's `.spec.resources[*].flavors`. For each flavor, Kueue attempts
+  relevant ResourceGroup inside ClusterQueue's 
+  (`.spec.resourcesGroup.flavors`). For each flavor, Kueue attempts
   to fit a Workload's pod set according to the quota defined in the
   ClusterQueue for the flavor and the unused quota in the cohort.
   If the Workload doesn't fit, Kueue evaluates the next flavor in the list.
 - A Workload's pod set resource fits in a flavor defined for a ClusterQueue
-  resource if the sum of requests for the resource:
-  1. Is less than or equal to the unused `.quota.min` for the flavor in the
+  resourceGroup if the sum of requests for the resource:
```

  Still pending

- **site/content/en/docs/concepts/cluster_queue.md:239** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T15:19:19Z
```diff
@@ -239,22 +235,23 @@ scenarios:
   `team-a-cq` can admit Workloads with resources adding up to `12+9=21` CPUs and
   `48+36=84Gi` of memory.
 - If ClusterQueue `team-b-cq` has pending Workloads and the ClusterQueue
-  `team-a-cq` has all its `min` quota used, Kueue will admit Workloads in
+  `team-a-cq` has all its `nominalQuota` quota used, Kueue will admit Workloads in
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
-  Therefore, Kueue ensures the `min` quota for `team-b-cq` is met.
+  Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
-**Note**: Kueue [does not support preemption](https://github.com/kubernetes-sigs/kueue/issues/83).
-Workloads already admitted by Kueue will _not_ be stopped to make space for new Workloads.
+**Note**:
+Kueue supports preemption and Workloads already admitted by Kueue may be 
```

  Either add the link or just remove the section given that preemption is described below.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-16T16:00:36Z

- **config/samples/job.yaml:1** by [@mwielgus](https://github.com/mwielgus) — 2023-03-16T16:00:36Z
  Had some garbage in the folder. Added by mistake. Removed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-16T16:01:05Z

- **config/samples/other.yaml:1** by [@mwielgus](https://github.com/mwielgus) — 2023-03-16T16:01:04Z
  Removed. Some old garbage added by accident.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-16T19:21:48Z

You also need to update sample-job to use labels instead of annotations for the queue name.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-16T20:26:27Z

- **site/content/en/docs/concepts/cluster_queue.md:57** by [@mwielgus](https://github.com/mwielgus) — 2023-03-16T20:26:27Z
```diff
@@ -56,16 +54,16 @@ models). You can define a flavor using a [ResourceFlavor object](/docs/concepts/
 In a process called [admission](/docs/concepts#admission), Kueue assigns to the
 [Workload pod sets](/docs/concepts/workload#pod-sets) a flavor for each resource the pod set
 requests.
-Kueue assigns the first flavor in the ClusterQueue's `.spec.resources[*].flavors`
-list that has enough unused `min` quota in the ClusterQueue or the
+Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroup.flavors`
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-16T20:27:33Z

- **site/content/en/docs/concepts/cluster_queue.md:171** by [@mwielgus](https://github.com/mwielgus) — 2023-03-16T20:27:33Z
```diff
@@ -168,18 +166,19 @@ When a ClusterQueue is part of a cohort, Kueue satisfies the following admission
 semantics:
 
 - When assigning flavors, Kueue goes through the list of flavors in the
-  ClusterQueue's `.spec.resources[*].flavors`. For each flavor, Kueue attempts
+  relevant ResourceGroup inside ClusterQueue's 
+  (`.spec.resourcesGroup.flavors`). For each flavor, Kueue attempts
   to fit a Workload's pod set according to the quota defined in the
   ClusterQueue for the flavor and the unused quota in the cohort.
   If the Workload doesn't fit, Kueue evaluates the next flavor in the list.
 - A Workload's pod set resource fits in a flavor defined for a ClusterQueue
-  resource if the sum of requests for the resource:
-  1. Is less than or equal to the unused `.quota.min` for the flavor in the
+  resourceGroup if the sum of requests for the resource:
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-16T20:27:44Z

- **site/content/en/docs/concepts/cluster_queue.md:239** by [@mwielgus](https://github.com/mwielgus) — 2023-03-16T20:27:43Z
```diff
@@ -239,22 +235,23 @@ scenarios:
   `team-a-cq` can admit Workloads with resources adding up to `12+9=21` CPUs and
   `48+36=84Gi` of memory.
 - If ClusterQueue `team-b-cq` has pending Workloads and the ClusterQueue
-  `team-a-cq` has all its `min` quota used, Kueue will admit Workloads in
+  `team-a-cq` has all its `nominalQuota` quota used, Kueue will admit Workloads in
   ClusterQueue `team-b-cq` before admitting any new Workloads in `team-a-cq`.
-  Therefore, Kueue ensures the `min` quota for `team-b-cq` is met.
+  Therefore, Kueue ensures the `nominalQuota` quota for `team-b-cq` is met.
 
-**Note**: Kueue [does not support preemption](https://github.com/kubernetes-sigs/kueue/issues/83).
-Workloads already admitted by Kueue will _not_ be stopped to make space for new Workloads.
+**Note**:
+Kueue supports preemption and Workloads already admitted by Kueue may be 
```

  Removed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-03-16T20:29:30Z

- **site/content/en/docs/tasks/administer_cluster_quotas.md:48** by [@mwielgus](https://github.com/mwielgus) — 2023-03-16T20:29:29Z
```diff
@@ -40,23 +40,21 @@ Write the manifest for the ClusterQueue. It should look similar to the following
 
 ```yaml
 # cluster-queue.yaml
-apiVersion: kueue.x-k8s.io/v1alpha2
+apiVersion: kueue.x-k8s.io/v1beta1
 kind: ClusterQueue
 metadata:
-  name: cluster-queue
+  name: "cluster-queue"
 spec:
-  namespaceSelector: {} # match all.
-  resources:
-  - name: "cpu"
-    flavors:
-    - name: default-flavor
-      quota:
-        min: 9
-  - name: "memory"
+  namespaceSelector: {}
```

  Done.
