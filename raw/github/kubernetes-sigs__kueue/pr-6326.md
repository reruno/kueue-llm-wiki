# PR #6326: [Flaky] Eliminate test flakiness - MK Dispatcher Incremental mode

**Summary**: [Flaky] Eliminate test flakiness - MK Dispatcher Incremental mode

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6326

**Last updated**: 2025-08-01T12:39:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `flaky/6233-mk-cluster-reconnect-fix` → `main`
- **Created**: 2025-07-31T12:57:07Z
- **Updated**: 2025-08-01T12:39:39Z
- **Merged**: 2025-08-01T12:37:38Z
- **Closed**: 2025-08-01T12:37:38Z
- **Labels**: `lgtm`, `kind/flake`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 2   **+17 / -21**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind flake
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6233

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-31T12:57:13Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ea71d7da0a5bd924a4f50de5182f6547aa647734 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/688caeb184e4090008ab1a5a |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T10:43:19Z

retest-1
/test pull-kueue-test-e2e-multikueue-main

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T11:11:16Z

retest-2
/test pull-kueue-test-e2e-multikueue-main

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:11:34Z

@mszadkow going forward I would like to extract the tests which change MK configuration to a separate file, like `multikueue/customconfigs_test.go`. It will make reasoning about the configurations much easier. 

It can be a follow up if we want to minimize the fix here.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:12:21Z

/release-note-edit
```release-note
NONE
```

### Comment by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T12:24:39Z

> @mszadkow going forward I would like to extract the tests which change MK configuration to a separate file, like `multikueue/customconfigs_test.go`. It will make reasoning about the configurations much easier.
> 
> It can be a follow up if we want to minimize the fix here.

Yes, let's make it a follow up.

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:35:51Z

/lgtm
/approve
Thank you for working on this. Let's see if this eliminates the flake completely.
/cherrypick release-0.13
/cherrypick release-0.12

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T12:35:53Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.12`, `release-0.13` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6326#issuecomment-3144438676):

>/lgtm
>/approve
>Thank you for working on this. Let's see if this eliminates the flake completely.
>/cherrypick release-0.13
>/cherrypick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-01T12:35:57Z

LGTM label has been added.  <details>Git tree hash: baea21f06f4341eb80da1b370a64f99f8a2358e1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-01T12:35:57Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6326#issuecomment-3144438676" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6326#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T12:38:16Z

@mimowo: new pull request created: #6351

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6326#issuecomment-3144438676):

>/lgtm
>/approve
>Thank you for working on this. Let's see if this eliminates the flake completely.
>/cherrypick release-0.13
>/cherrypick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T12:38:50Z

@mimowo: #6326 failed to apply on top of branch "release-0.12":
```
Applying: Eliminate test flakiness
Using index info to reconstruct a base tree...
M	test/e2e/multikueue/e2e_test.go
M	test/e2e/multikueue/suite_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/e2e/multikueue/suite_test.go
CONFLICT (content): Merge conflict in test/e2e/multikueue/suite_test.go
Auto-merging test/e2e/multikueue/e2e_test.go
CONFLICT (content): Merge conflict in test/e2e/multikueue/e2e_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Eliminate test flakiness

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6326#issuecomment-3144438676):

>/lgtm
>/approve
>Thank you for working on this. Let's see if this eliminates the flake completely.
>/cherrypick release-0.13
>/cherrypick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:39:39Z

> @mimowo: https://github.com/kubernetes-sigs/kueue/pull/6326 failed to apply on top of branch "release-0.12":

@mszadkow please also prepare for 0.12 the extension of timeouts

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T11:16:56Z

- **test/e2e/multikueue/e2e_test.go:67** by [@mimowo](https://github.com/mimowo) — 2025-08-01T11:16:56Z
```diff
@@ -64,7 +64,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("MultiKueue", func() {
+var _ = ginkgo.Describe("MultiKueue", ginkgo.Ordered, func() {
```

  This PR is changing a bunch of things, what is really relevant for fixing the flake? It would be good to only change one thing at the time

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T11:19:25Z

- **test/e2e/multikueue/e2e_test.go:1031** by [@mimowo](https://github.com/mimowo) — 2025-08-01T11:19:25Z
```diff
@@ -1028,19 +1034,6 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
-				gomega.Eventually(func(g gomega.Gomega) {
```

  Changes to "Should run a job on worker if admitted" don't seem related, because in this failure run https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6345/pull-kueue-test-e2e-multikueue-main/1951172197842161664 the "MultiKueue with Incremental mode" was run first and failed first.
  
  So, the failure in "Should run a job on worker if admitted" was just a result of some resources left.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T11:22:52Z

- **test/e2e/multikueue/e2e_test.go:892** by [@mimowo](https://github.com/mimowo) — 2025-08-01T11:22:51Z
```diff
@@ -888,21 +888,27 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		})
 	})
 
-	ginkgo.When("MultiKueue with Incremental mode", ginkgo.Ordered, func() {
-		ginkgo.BeforeAll(func() {
+	ginkgo.When("Incremental mode", func() {
+		ginkgo.BeforeEach(func() {
```

  IIUC BeforeAll / BeforeEach don't matter here, and ginkgo.Ordered, because we have only one "It" test inside the When?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T11:23:55Z

- **test/e2e/multikueue/e2e_test.go:67** by [@mimowo](https://github.com/mimowo) — 2025-08-01T11:23:55Z
```diff
@@ -64,7 +64,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("MultiKueue", func() {
+var _ = ginkgo.Describe("MultiKueue", ginkgo.Ordered, func() {
```

  I looked at the PR and it seems the only changes potentially releveant is exending timeout or calling WaitForKueueAvailability. Not sure which one is important

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-08-01T11:30:04Z

- **test/e2e/multikueue/e2e_test.go:67** by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T11:30:04Z
```diff
@@ -64,7 +64,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("MultiKueue", func() {
+var _ = ginkgo.Describe("MultiKueue", ginkgo.Ordered, func() {
```

  I know and fully agree that some changes might not be relevant, please consider I marked the PR as `WIP`.
  I will narrow down amount of changes, however each run "cost" a lot of time and I actually think some changes must come in batches, like extending timeout and add extra check for worker cluster after reconnection.
  Some of the changes helps me better reason about potential issues, like this one keeps the order, I will rather remove it later.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T11:37:08Z

- **test/e2e/multikueue/e2e_test.go:909** by [@mimowo](https://github.com/mimowo) — 2025-08-01T11:37:08Z
```diff
@@ -888,21 +888,27 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		})
 	})
 
-	ginkgo.When("MultiKueue with Incremental mode", ginkgo.Ordered, func() {
+	ginkgo.When("Incremental mode", ginkgo.Ordered, func() {
 		ginkgo.BeforeAll(func() {
 			ginkgo.By("setting MultiKueue Dispatcher to Incremental", func() {
 				util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
-					cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					if cfg.MultiKueue == nil {
+						cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					}
 					cfg.MultiKueue.DispatcherName = ptr.To(kueueconfig.MultiKueueDispatcherModeIncremental)
 				})
+				util.WaitForKueueAvailability(ctx, k8sManagerClient)
 			})
 		})
 		ginkgo.AfterAll(func() {
 			ginkgo.By("setting MultiKueue Dispatcher back to AllAtOnce", func() {
 				util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
-					cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					if cfg.MultiKueue == nil {
+						cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					}
 					cfg.MultiKueue.DispatcherName = ptr.To(string(kueueconfig.MultiKueueDispatcherModeAllAtOnce))
```

  Actually this is probably not the  root cause for the flake but this looks wrong.
  We should not set the DispatcherName to AllAtOnce but to revert the entire configuration to original.
  
  The test code should memorize the orignal in the Before routine, and "After should set it. Sorry for missing during review, it can be a follow up,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T11:40:19Z

- **test/e2e/multikueue/e2e_test.go:179** by [@mimowo](https://github.com/mimowo) — 2025-08-01T11:40:18Z
```diff
@@ -176,12 +176,12 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		util.ExpectObjectToBeDeleted(ctx, k8sWorker2Client, worker2Cq, true)
 		util.ExpectObjectToBeDeleted(ctx, k8sWorker2Client, worker2Flavor, true)
 
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, managerCq, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, managerFlavor, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, multiKueueAc, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, multiKueueConfig, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, workerCluster1, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, workerCluster2, true)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sManagerClient, managerCq, true, util.LongTimeout)
```

  Looking at this PR, this is likely the fix, not WaitForKueueAvailability, because the default timeout is 10s. On a loaded environment like MK I can imagine this is often not enough.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T11:42:46Z

- **test/e2e/multikueue/e2e_test.go:901** by [@mimowo](https://github.com/mimowo) — 2025-08-01T11:42:46Z
```diff
@@ -888,21 +888,27 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		})
 	})
 
-	ginkgo.When("MultiKueue with Incremental mode", ginkgo.Ordered, func() {
+	ginkgo.When("Incremental mode", ginkgo.Ordered, func() {
 		ginkgo.BeforeAll(func() {
 			ginkgo.By("setting MultiKueue Dispatcher to Incremental", func() {
 				util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
-					cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					if cfg.MultiKueue == nil {
+						cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					}
```

  Yeah, this is likely not a reason for the flake, but clearly a bug in the previous test code, because we would nil all other settings

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-08-01T11:44:05Z

- **test/e2e/multikueue/e2e_test.go:909** by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T11:44:05Z
```diff
@@ -888,21 +888,27 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		})
 	})
 
-	ginkgo.When("MultiKueue with Incremental mode", ginkgo.Ordered, func() {
+	ginkgo.When("Incremental mode", ginkgo.Ordered, func() {
 		ginkgo.BeforeAll(func() {
 			ginkgo.By("setting MultiKueue Dispatcher to Incremental", func() {
 				util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
-					cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					if cfg.MultiKueue == nil {
+						cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					}
 					cfg.MultiKueue.DispatcherName = ptr.To(kueueconfig.MultiKueueDispatcherModeIncremental)
 				})
+				util.WaitForKueueAvailability(ctx, k8sManagerClient)
 			})
 		})
 		ginkgo.AfterAll(func() {
 			ginkgo.By("setting MultiKueue Dispatcher back to AllAtOnce", func() {
 				util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
-					cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					if cfg.MultiKueue == nil {
+						cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					}
 					cfg.MultiKueue.DispatcherName = ptr.To(string(kueueconfig.MultiKueueDispatcherModeAllAtOnce))
```

  Yes, the mistake was to keep the default configuration in the suite_test.go.
  I will remove it from there as to global.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-08-01T11:45:36Z

- **test/e2e/multikueue/e2e_test.go:901** by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T11:45:36Z
```diff
@@ -888,21 +888,27 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		})
 	})
 
-	ginkgo.When("MultiKueue with Incremental mode", ginkgo.Ordered, func() {
+	ginkgo.When("Incremental mode", ginkgo.Ordered, func() {
 		ginkgo.BeforeAll(func() {
 			ginkgo.By("setting MultiKueue Dispatcher to Incremental", func() {
 				util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
-					cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					if cfg.MultiKueue == nil {
+						cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					}
```

  Yes, I also consider this a bug fix

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-08-01T11:48:12Z

- **test/e2e/multikueue/e2e_test.go:1031** by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T11:48:12Z
```diff
@@ -1028,19 +1034,6 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 				cmd := exec.Command("docker", "network", "connect", "kind", worker1Container)
 				output, err := cmd.CombinedOutput()
 				gomega.Expect(err).NotTo(gomega.HaveOccurred(), "%s: %s", err, output)
-				gomega.Eventually(func(g gomega.Gomega) {
```

  Removed.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-08-01T11:49:16Z

- **test/e2e/multikueue/e2e_test.go:179** by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T11:49:16Z
```diff
@@ -176,12 +176,12 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		util.ExpectObjectToBeDeleted(ctx, k8sWorker2Client, worker2Cq, true)
 		util.ExpectObjectToBeDeleted(ctx, k8sWorker2Client, worker2Flavor, true)
 
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, managerCq, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, managerFlavor, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, multiKueueAc, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, multiKueueConfig, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, workerCluster1, true)
-		util.ExpectObjectToBeDeleted(ctx, k8sManagerClient, workerCluster2, true)
+		util.ExpectObjectToBeDeletedWithTimeout(ctx, k8sManagerClient, managerCq, true, util.LongTimeout)
```

  Yes I agree it's main thing, there is no restart of Kueue failure that could explain the flakiness

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-08-01T11:49:30Z

- **test/e2e/multikueue/e2e_test.go:67** by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T11:49:30Z
```diff
@@ -64,7 +64,7 @@ import (
 	"sigs.k8s.io/kueue/test/util"
 )
 
-var _ = ginkgo.Describe("MultiKueue", func() {
+var _ = ginkgo.Describe("MultiKueue", ginkgo.Ordered, func() {
```

  Reverted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T11:49:47Z

- **test/e2e/multikueue/e2e_test.go:911** by [@mimowo](https://github.com/mimowo) — 2025-08-01T11:49:47Z
```diff
@@ -888,21 +888,27 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		})
 	})
 
-	ginkgo.When("MultiKueue with Incremental mode", ginkgo.Ordered, func() {
+	ginkgo.When("Incremental mode", ginkgo.Ordered, func() {
+		var defaultManagerKueueCfg *kueueconfig.Configuration
+
 		ginkgo.BeforeAll(func() {
 			ginkgo.By("setting MultiKueue Dispatcher to Incremental", func() {
-				util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
-					cfg.MultiKueue = &kueueconfig.MultiKueue{}
+				defaultManagerKueueCfg = util.GetKueueConfiguration(ctx, k8sManagerClient)
+				newCfg := defaultManagerKueueCfg.DeepCopy()
+				util.UpdateKueueConfiguration(ctx, k8sManagerClient, newCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
+					if cfg.MultiKueue == nil {
+						cfg.MultiKueue = &kueueconfig.MultiKueue{}
+					}
 					cfg.MultiKueue.DispatcherName = ptr.To(kueueconfig.MultiKueueDispatcherModeIncremental)
 				})
+				util.WaitForKueueAvailability(ctx, k8sManagerClient)
 			})
 		})
 		ginkgo.AfterAll(func() {
 			ginkgo.By("setting MultiKueue Dispatcher back to AllAtOnce", func() {
-				util.UpdateKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg, managerClusterName, func(cfg *kueueconfig.Configuration) {
-					cfg.MultiKueue = &kueueconfig.MultiKueue{}
-					cfg.MultiKueue.DispatcherName = ptr.To(string(kueueconfig.MultiKueueDispatcherModeAllAtOnce))
-				})
+				util.ApplyKueueConfiguration(ctx, k8sManagerClient, defaultManagerKueueCfg)
+				util.RestartKueueController(ctx, k8sManagerClient, managerClusterName)
+				util.WaitForKueueAvailability(ctx, k8sManagerClient)
```

  Reading the code of RestartKueueController it seems that `WaitForKueueAvailability` is unnecessary, as redundant.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2025-08-01T11:50:06Z

- **test/e2e/multikueue/e2e_test.go:892** by [@mszadkow](https://github.com/mszadkow) — 2025-08-01T11:50:06Z
```diff
@@ -888,21 +888,27 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 		})
 	})
 
-	ginkgo.When("MultiKueue with Incremental mode", ginkgo.Ordered, func() {
-		ginkgo.BeforeAll(func() {
+	ginkgo.When("Incremental mode", func() {
+		ginkgo.BeforeEach(func() {
```

  Reverted.
  However I changed the When string bc it duplicated the work MultiKueue
