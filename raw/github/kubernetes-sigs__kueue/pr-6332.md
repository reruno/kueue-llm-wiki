# PR #6332: Fix accounting for the evicted_workloads_once_total metric

**Summary**: Fix accounting for the evicted_workloads_once_total metric

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6332

**Last updated**: 2026-02-21T15:46:40Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-evicted-once-metrics` → `main`
- **Created**: 2025-07-31T15:33:07Z
- **Updated**: 2026-02-21T15:46:40Z
- **Merged**: 2025-08-01T14:39:38Z
- **Closed**: 2025-08-01T14:39:38Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 7   **+55 / -26**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of  #6296

#### Special notes for your reviewer:

This is the preparatory PR for https://github.com/kubernetes-sigs/kueue/pull/6297 to first align the user-observable behavior before commonizing the code.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Fix accounting for the `evicted_workloads_once_total` metric:
- the metric wasn't incremented for workloads evicted due to stopped LocalQueue (LocalQueueStopped reason)
- the reason used for the metric was "Deactivated" for workloads deactivated by users and Kueue, now the reason label can have the following values: Deactivated, DeactivatedDueToAdmissionCheck, DeactivatedDueToMaximumExecutionTimeExceeded, DeactivatedDueToRequeuingLimitExceeded. This approach aligns the metric with `evicted_workloads_total`.
- the metric was incremented during preemption before the preemption request was issued. Thus, it could be incorrectly over-counted in case of the preemption request failure.
- the metric was not incremented for workload evicted due to NodeFailures (TAS)

The existing and introduced DeactivatedDueToXYZ reason label values will be replaced by the single "Deactivated" reason label value and underlying_cause in the future release.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-07-31T15:33:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9b40c05a9aba9671459f203a30e907b01b63c4c1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/688cb87a1d024d0008d776ae |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6332--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6332--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTYzMzItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.flz1y9r3FghOaWp3V-7aS8AhYgtCPPqz7vY0L9KGup8)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6332--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2025-07-31T16:23:52Z

cc @tenzen-y ptal

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-01T14:07:12Z

LGTM label has been added.  <details>Git tree hash: 7f512d4feb703df56f531de9e5226b7834d82c8f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-01T14:07:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6332#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6332#pullrequestreview-3079381663" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T14:10:38Z

```release-note
Fix accounting for the `evicted_workloads_once_total` metric:
- the metric wasn't incremented for workloads evicted due to stopped LocalQueue (LocalQueueStopped reason)
- the reason used for the metric was "Deactivated" for workloads deactivated by users and Kueue, now the reason label can have the following values: Deactivated, DeactivatedDueToAdmissionCheck, DeactivatedDueToMaximumExecutionTimeExceeded, DeactivatedDueToRequeuingLimitExceeded. This approach aligns the metric with `evicted_workloads_total`.
- the metric was incremented during preemption before the preemption request was issued. Thus, it could be incorrectly over-counted in case of the preemption request failure.
- the metric was not incremented for workload evicted due to NodeFailures (TAS)

The existing and introduced EvictedDueToXYZ reason label will be removed, and the single reason label will be decoupled to Eviction reason and underlying_cause in the future release.
```

@mimowo What about new release note?

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T14:12:59Z

sgtm

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T14:15:10Z

/cherrypick release-0.13
/cherrypick release-0.12

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T14:15:12Z

/release-note-edit

```release-note
Fix accounting for the `evicted_workloads_once_total` metric:
- the metric wasn't incremented for workloads evicted due to stopped LocalQueue (LocalQueueStopped reason)
- the reason used for the metric was "Deactivated" for workloads deactivated by users and Kueue, now the reason label can have the following values: Deactivated, DeactivatedDueToAdmissionCheck, DeactivatedDueToMaximumExecutionTimeExceeded, DeactivatedDueToRequeuingLimitExceeded. This approach aligns the metric with `evicted_workloads_total`.
- the metric was incremented during preemption before the preemption request was issued. Thus, it could be incorrectly over-counted in case of the preemption request failure.
- the metric was not incremented for workload evicted due to NodeFailures (TAS)

The existing and introduced EvictedDueToXYZ reason label will be removed, and the single reason label will be decoupled to Eviction reason and underlying_cause in the future release.
```

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T14:15:13Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.12`, `release-0.13` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6332#issuecomment-3144740909):

>/cherrypick release-0.13
>/cherrypick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-01T14:19:15Z

/release-note-edit
```release-note
Fix accounting for the `evicted_workloads_once_total` metric:
- the metric wasn't incremented for workloads evicted due to stopped LocalQueue (LocalQueueStopped reason)
- the reason used for the metric was "Deactivated" for workloads deactivated by users and Kueue, now the reason label can have the following values: Deactivated, DeactivatedDueToAdmissionCheck, DeactivatedDueToMaximumExecutionTimeExceeded, DeactivatedDueToRequeuingLimitExceeded. This approach aligns the metric with `evicted_workloads_total`.
- the metric was incremented during preemption before the preemption request was issued. Thus, it could be incorrectly over-counted in case of the preemption request failure.
- the metric was not incremented for workload evicted due to NodeFailures (TAS)

The existing and introduced DeactivatedDueToXYZ reason label values will be replaced by the single "Deactivated" reason label value and underlying_cause in the future release.
```

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T14:40:17Z

@mimowo: new pull request created: #6360

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6332#issuecomment-3144740909):

>/cherrypick release-0.13
>/cherrypick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-01T14:40:51Z

@mimowo: #6332 failed to apply on top of branch "release-0.12":
```
Applying: Fix the evicted_workloads_once_total metric
Using index info to reconstruct a base tree...
M	pkg/controller/core/workload_controller.go
M	pkg/controller/tas/node_failure_controller.go
M	pkg/metrics/metrics.go
M	pkg/scheduler/preemption/preemption.go
M	test/integration/singlecluster/scheduler/scheduler_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/scheduler/scheduler_test.go
Auto-merging pkg/scheduler/preemption/preemption.go
Auto-merging pkg/metrics/metrics.go
CONFLICT (content): Merge conflict in pkg/metrics/metrics.go
Auto-merging pkg/controller/tas/node_failure_controller.go
CONFLICT (content): Merge conflict in pkg/controller/tas/node_failure_controller.go
Auto-merging pkg/controller/core/workload_controller.go
CONFLICT (content): Merge conflict in pkg/controller/core/workload_controller.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0001 Fix the evicted_workloads_once_total metric

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6332#issuecomment-3144740909):

>/cherrypick release-0.13
>/cherrypick release-0.12


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T12:40:53Z

This looks like fine-grained deactivation metrics. I like that this idea, but I think we need to refine a bit as I mentioned in https://github.com/kubernetes-sigs/kueue/pull/6332#discussion_r2247877771

- **pkg/metrics/metrics.go:168** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:29:54Z
```diff
@@ -164,7 +164,12 @@ The label 'reason' can have the following values:
 - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
 - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
-- "Deactivated" means that the workload was evicted because spec.active is set to false`,
+- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
+- "NodeFailures" means that the workload was evicted due to node failures when using TAS.
```

  ```suggestion
  - "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.
  ```
  
  I would propose using the complete name in case of a user-facing one.
  We don't say AC, ProvReq, and MK. Instead of that, we want to say AdmissionCheck, ProvisioningRequest, and MultiKueue.

- **pkg/metrics/metrics.go:285** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:30:13Z
```diff
@@ -276,7 +281,12 @@ The label 'reason' can have the following values:
 - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
 - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
-- "Deactivated" means that the workload was evicted because spec.active is set to false`,
+- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
+- "NodeFailures" means that the workload was evicted due to node failures when using TAS.
```

  ```suggestion
  - "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.
  ```

- **pkg/metrics/metrics.go:312** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:30:28Z
```diff
@@ -298,7 +308,12 @@ The label 'reason' can have the following values:
 - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
 - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
-- "Deactivated" means that the workload was evicted because spec.active is set to false`,
+- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
+- "NodeFailures" means that the workload was evicted due to node failures when using TAS.
```

  ```suggestion
  - "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.
  ```

- **pkg/metrics/metrics.go:331** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:30:41Z
```diff
@@ -312,7 +327,12 @@ The label 'reason' can have the following values:
 - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
 - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
-- "Deactivated" means that the workload was evicted because spec.active is set to false`,
+- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
+- "NodeFailures" means that the workload was evicted due to node failures when using TAS.
```

  ```suggestion
  - "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.
  ```

- **pkg/controller/tas/node_failure_controller.go:304** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:34:06Z
```diff
@@ -302,12 +302,7 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 }
 
 func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload, evictionMessage string) error {
-	workload.PrepareForEviction(wl, r.clock.Now(), kueue.WorkloadEvictedDueToNodeFailures, evictionMessage)
-	if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock); err != nil {
-		return err
-	}
-	workload.ReportEvictedWorkload(r.recorder, wl, wl.Status.Admission.ClusterQueue, kueue.WorkloadEvictedDueToNodeFailures, evictionMessage)
-	return nil
+	return workload.EvictWorkload(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, evictionMessage, r.clock)
 }
```

  Couldn't we remove this `startEviction` function, and directly use `workload.EvictWorkload(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, evictionMessage, r.clock)` everywhere?

- **pkg/metrics/metrics.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:39:56Z
  Instead of concatenating the "Deactivated" and the deactivating reason, should we prepare the `underlying_cause`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T12:46:06Z

- **pkg/metrics/metrics.go:1** by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:46:05Z
  Yes I think so, but I would leave it for future (we can open issue), because:
  - this is substantially more work to do right, we probably should have a separate condition like "Deactivated" to define the underlyingCause, similarly as we have the "Preempted" and "WaitForPodsReady" conditions
  - it would be a breaking change for the "evicted_worklaoads_total" metric which is existing for much longer, so we should be more careful adjusting, probably using a feature gate.
  
  So, as a follow up I imagine we can introduce Deactivated condition as a source of the underllyingCause, and then use "Deactivated" as "reason". However, I would like to do it only for 0.14+

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T12:49:35Z

- **pkg/controller/tas/node_failure_controller.go:304** by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:49:35Z
```diff
@@ -302,12 +302,7 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 }
 
 func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload, evictionMessage string) error {
-	workload.PrepareForEviction(wl, r.clock.Now(), kueue.WorkloadEvictedDueToNodeFailures, evictionMessage)
-	if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock); err != nil {
-		return err
-	}
-	workload.ReportEvictedWorkload(r.recorder, wl, wl.Status.Admission.ClusterQueue, kueue.WorkloadEvictedDueToNodeFailures, evictionMessage)
-	return nil
+	return workload.EvictWorkload(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, evictionMessage, r.clock)
 }
```

  Yes, sure we can, just wanted to minimize the diff for ease of reviewing. Maybe I can do it in the follow up where I commonize the code, and focus this PR just on the semantic changes?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T12:52:14Z

- **pkg/metrics/metrics.go:331** by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:52:14Z
```diff
@@ -312,7 +327,12 @@ The label 'reason' can have the following values:
 - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
 - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
-- "Deactivated" means that the workload was evicted because spec.active is set to false`,
+- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
+- "NodeFailures" means that the workload was evicted due to node failures when using TAS.
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T12:52:24Z

- **pkg/metrics/metrics.go:312** by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:52:24Z
```diff
@@ -298,7 +308,12 @@ The label 'reason' can have the following values:
 - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
 - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
-- "Deactivated" means that the workload was evicted because spec.active is set to false`,
+- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
+- "NodeFailures" means that the workload was evicted due to node failures when using TAS.
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T12:52:34Z

- **pkg/metrics/metrics.go:285** by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:52:34Z
```diff
@@ -276,7 +281,12 @@ The label 'reason' can have the following values:
 - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
 - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
-- "Deactivated" means that the workload was evicted because spec.active is set to false`,
+- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
+- "NodeFailures" means that the workload was evicted due to node failures when using TAS.
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T12:52:44Z

- **pkg/metrics/metrics.go:168** by [@mimowo](https://github.com/mimowo) — 2025-08-01T12:52:44Z
```diff
@@ -164,7 +164,12 @@ The label 'reason' can have the following values:
 - "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.
 - "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
-- "Deactivated" means that the workload was evicted because spec.active is set to false`,
+- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
+- "NodeFailures" means that the workload was evicted due to node failures when using TAS.
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T12:54:07Z

- **pkg/metrics/metrics.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:54:06Z
  In that case, we should not distinguish between Deactivation based on reason.
  After this PR, the deactivated semantic in metric is changed, which means the counting value will decrease and delegate some counts to each DueToXXX metric.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T12:56:05Z

- **pkg/metrics/metrics.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:56:05Z
  So, my recomendation is introducing only `LocalQueueStopped` and `NodeFailures `.
  And keeping `Deactivated` the same as before, then removing `DeactivatedDueToXXX` reasons.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T12:57:53Z

- **pkg/controller/tas/node_failure_controller.go:304** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T12:57:53Z
```diff
@@ -302,12 +302,7 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 }
 
 func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload, evictionMessage string) error {
-	workload.PrepareForEviction(wl, r.clock.Now(), kueue.WorkloadEvictedDueToNodeFailures, evictionMessage)
-	if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock); err != nil {
-		return err
-	}
-	workload.ReportEvictedWorkload(r.recorder, wl, wl.Status.Admission.ClusterQueue, kueue.WorkloadEvictedDueToNodeFailures, evictionMessage)
-	return nil
+	return workload.EvictWorkload(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, evictionMessage, r.clock)
 }
```

  Are you able to work on https://github.com/kubernetes-sigs/kueue/pull/6297?
  If so, I'm ok with that.
  
  Because removing this function could introduce conflicts between main and release branches.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T13:00:04Z

- **pkg/metrics/metrics.go:1** by [@mimowo](https://github.com/mimowo) — 2025-08-01T13:00:04Z
  > In that case, we should not distinguish between Deactivation based on reason.
  
  yes, for now users of graphana would need to aggregate the values. OTOH this way they can distinguish "deactivated" manually and deactivate by Kueue.
  
  > After this PR, the deactivated semantic in metric is changed
  
  Yes, but I think it is a bugfix to align with the `evicted_workloads_total`. Note that this PR is not changing `evicted_workloads_total`, just fixes the metric comment.  I think one of the two metrics is broken which makes it hard to untagle. Since evicted_workloads_once_total is very new I think it is fair to align it with `evicted_workloads_total`. This way of aligning gives users more granularity. 
  
  We can add a comment that if they used the metric to track "Deactivated", they now need to aggregate over the four different label values.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T13:02:55Z

- **pkg/metrics/metrics.go:1** by [@mimowo](https://github.com/mimowo) — 2025-08-01T13:02:55Z
  yes, the eviction metrics currently are mess, and I'm trying to untagle. Since `evicted_workloads_once_total` is the most recently added I think it fair to align the reasons by changing this metric. Note that the changes in the comments for all other metrics are just fixes to the commentary,, not to the logic.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T13:04:59Z

- **pkg/controller/tas/node_failure_controller.go:304** by [@mimowo](https://github.com/mimowo) — 2025-08-01T13:04:58Z
```diff
@@ -302,12 +302,7 @@ func (r *nodeFailureReconciler) patchWorkloadsForNodeToReplace(ctx context.Conte
 }
 
 func (r *nodeFailureReconciler) startEviction(ctx context.Context, wl *kueue.Workload, evictionMessage string) error {
-	workload.PrepareForEviction(wl, r.clock.Now(), kueue.WorkloadEvictedDueToNodeFailures, evictionMessage)
-	if err := workload.ApplyAdmissionStatus(ctx, r.client, wl, true, r.clock); err != nil {
-		return err
-	}
-	workload.ReportEvictedWorkload(r.recorder, wl, wl.Status.Admission.ClusterQueue, kueue.WorkloadEvictedDueToNodeFailures, evictionMessage)
-	return nil
+	return workload.EvictWorkload(ctx, r.client, r.recorder, wl, kueue.WorkloadEvictedDueToNodeFailures, evictionMessage, r.clock)
 }
```

  Sure, I started from working on that PR, I extracted this one just to make reviewing easier as requested. I'm going to follow up.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T13:07:34Z

- **pkg/metrics/metrics.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T13:07:33Z
  I understand the problem, but this approach tries to resolve the problem in a temporary way. Ideally, we should prepare `underlying_cause`. OTOH, introducing `underlying_cause` could be considered as a new feature as you mentioned.
  
  We should fix the problem with fewer changes. Even if the sum of counts is not changed, dividing metrics based on reason could be handled by "break".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T13:16:53Z

- **pkg/metrics/metrics.go:1** by [@mimowo](https://github.com/mimowo) — 2025-08-01T13:16:53Z
  Yes, this is a "temporary" state just to fix evicted_workloads_once_total by aligning with the older preexisting metrics.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T13:17:44Z

- **pkg/metrics/metrics.go:1** by [@mimowo](https://github.com/mimowo) — 2025-08-01T13:17:44Z
  Ultimaltely I agree the old metrics are bad, but fixing them is a much bigger task. It is not my priority at the moment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-01T13:22:35Z

- **pkg/metrics/metrics.go:1** by [@mimowo](https://github.com/mimowo) — 2025-08-01T13:22:35Z
  > I understand the problem, but this approach tries to resolve the problem in a temporary way.
  
  Yes, this is temporary to align and commonize how we handle metrics. 
  
  I'm afraid I may not have time to work on the ideal way. and I'm afraid that if we postopone the commonizing PR people will introduce even more bugs and inconsistencies over time.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T14:05:52Z

- **pkg/metrics/metrics.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T14:05:52Z
  I synced with Michal offline.
  
  As @mimowo proved in https://github.com/kubernetes-sigs/kueue/pull/6355, the current Deactivation metrics reason is very inconsistent. Some metrics have DeactivatedDueToXXX, but others do not have.
  
  So, we decided to introduce DeactivatedDueToXXX reasons to others to keep consistency across all metrics for the short term solution.
  But, in the long-term solution, we will introduce the underlying_cause label, and remove DeactivatedDueToXXX reasons.
  The removal of reasons are acceptable based on Kubernetes compatibility policy. We remember that the long-term supported reason was removed (https://github.com/kubernetes/kubernetes/blob/7bbcfda63af2feff9ccf685a72a45bc66df0cecd/pkg/controller/job/metrics/metrics.go#L59) in the Job controller.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T14:07:05Z

Thank you
/lgtm
/approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-01T14:07:26Z

- **pkg/metrics/metrics.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-01T14:07:25Z
  We want to keep opening this thread.
