# PR #640: Move jobframework constants into the directory

**Summary**: Move jobframework constants into the directory

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/640

**Last updated**: 2023-03-18T18:44:33Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `move-jobframework-constants` → `main`
- **Created**: 2023-03-16T14:53:19Z
- **Updated**: 2023-03-18T18:44:33Z
- **Merged**: 2023-03-17T16:09:17Z
- **Closed**: 2023-03-17T16:09:17Z
- **Labels**: `lgtm`, `tide/merge-method-squash`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 18   **+294 / -209**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

To make the jobframework stuff contained within the package. See related comment: https://github.com/kubernetes-sigs/kueue/pull/633#discussion_r1137383502

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #369

#### Special notes for your reviewer:

The solution is to extract from the monolitic `testing` package the packages for job framework testing (batchv1.Job and MPIJob). Only these packages will depend on the `jobframwork` package.

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-16T14:53:25Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 82390b4de606867b4e4c2032f3f5bbe48b7de4a8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64148dff9df2770008cf1ef9 |

### Comment by [@mimowo](https://github.com/mimowo) — 2023-03-16T15:09:41Z

/assign @alculquicondor

### Comment by [@mimowo](https://github.com/mimowo) — 2023-03-17T14:57:54Z

@alculquicondor I think I have addressed the comments and simplified the interface so that the handling of labels and annotations for queue name is commonized. If we think MPIJob should support the spec.runPolicy.schedulingPolicy.Queue I will add this under a dedicated PR. This didn't work anyway, so let's progress the interface as needed. PTAL.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T16:03:43Z

/lgtm
/approve
/label tide/merge-method-squash

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-17T16:03:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/640#issuecomment-1474062036" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/640#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-16T19:27:19Z

- **pkg/controller/jobframework/constants.go:2** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T19:14:30Z
```diff
@@ -0,0 +1,35 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  2023

- **pkg/controller/jobframework/reconciler.go:108** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T19:16:18Z
```diff
@@ -105,7 +105,7 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 	// when manageJobsWithoutQueueName is disabled we only reconcile jobs that have either
 	// queue-name or the parent-workload annotation set.
 	if !r.manageJobsWithoutQueueName && job.QueueName() == "" && isStandaloneJob {
-		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", constants.QueueAnnotation, constants.ParentWorkloadAnnotation))
+		log.V(3).Info(fmt.Sprintf("Neither %s, nor %s annotation is set, ignoring the job", QueueAnnotation, ParentWorkloadAnnotation))
```

  ```suggestion
  		log.V(3).Info(fmt.Sprintf("Neither %s label, nor %s annotation is set, ignoring the job", QueueLabel, ParentWorkloadAnnotation))
  ```

- **pkg/controller/jobframework/validation.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T19:17:28Z
```diff
@@ -19,14 +19,12 @@ import (
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
-
-	"sigs.k8s.io/kueue/pkg/constants"
 )
 
 var (
 	annotationsPath       = field.NewPath("metadata", "annotations")
-	ParentWorkloadKeyPath = annotationsPath.Key(constants.ParentWorkloadAnnotation)
-	QueueNamePath         = annotationsPath.Key(constants.QueueAnnotation)
+	ParentWorkloadKeyPath = annotationsPath.Key(ParentWorkloadAnnotation)
+	QueueNamePath         = annotationsPath.Key(QueueAnnotation)
```

  oh, we should validate the Label as well. I forgot to do this as part of the migration to v1beta1.

- **pkg/controller/jobs/job/job_controller.go:125** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T19:20:28Z
```diff
@@ -118,11 +117,11 @@ func (j *Job) Object() client.Object {
 }
 
 func (j *Job) ParentWorkloadName() string {
-	return j.Annotations[constants.ParentWorkloadAnnotation]
+	return j.Annotations[jobframework.ParentWorkloadAnnotation]
 }
 
 func (j *Job) QueueName() string {
-	return j.Annotations[constants.QueueAnnotation]
```

  I think this was lost over the rebases.... We should check first in Labels, and, if not found, as Annotation.

- **pkg/controller/jobs/mpijob/mpijob_controller.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T19:24:14Z
```diff
@@ -64,11 +63,11 @@ func (j *MPIJob) Object() client.Object {
 }
 
 func (j *MPIJob) ParentWorkloadName() string {
-	return j.Annotations[constants.ParentWorkloadAnnotation]
+	return j.Annotations[jobframework.ParentWorkloadAnnotation]
 }
 
 func (j *MPIJob) QueueName() string {
-	return j.Annotations[constants.QueueAnnotation]
+	return j.Annotations[jobframework.QueueAnnotation]
```

  same change here

- **pkg/util/testingjobs/job/wrappers.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-16T19:26:21Z
```diff
@@ -0,0 +1,122 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package testing
+
+import (
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// JobWrapper wraps a Job.
+type JobWrapper struct{ batchv1.Job }
+
+// MakeJob creates a wrapper for a suspended job with a single container and parallelism=1.
+func MakeJob(name, ns string) *JobWrapper {
+	return &JobWrapper{batchv1.Job{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:        name,
+			Namespace:   ns,
+			Annotations: make(map[string]string, 1),
+		},
+		Spec: batchv1.JobSpec{
+			Parallelism: pointer.Int32(1),
+			Suspend:     pointer.Bool(true),
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					RestartPolicy: "Never",
+					Containers: []corev1.Container{
+						{
+							Name:      "c",
+							Image:     "pause",
+							Command:   []string{},
+							Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
+						},
+					},
+					NodeSelector: map[string]string{},
+				},
+			},
+		},
+	}}
+}
+
+// Obj returns the inner Job.
+func (j *JobWrapper) Obj() *batchv1.Job {
+	return &j.Job
+}
+
+// Suspend updates the suspend status of the job
+func (j *JobWrapper) Suspend(s bool) *JobWrapper {
+	j.Spec.Suspend = pointer.Bool(s)
+	return j
+}
+
+// Parallelism updates job parallelism.
+func (j *JobWrapper) Parallelism(p int32) *JobWrapper {
+	j.Spec.Parallelism = pointer.Int32(p)
+	return j
+}
+
+// PriorityClass updates job priorityclass.
+func (j *JobWrapper) PriorityClass(pc string) *JobWrapper {
+	j.Spec.Template.Spec.PriorityClassName = pc
+	return j
+}
+
+// Queue updates the queue name of the job
+func (j *JobWrapper) Queue(queue string) *JobWrapper {
+	j.Annotations[jobframework.QueueAnnotation] = queue
```

  label

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-17T13:34:28Z

- **pkg/controller/jobframework/constants.go:2** by [@mimowo](https://github.com/mimowo) — 2023-03-17T13:34:28Z
```diff
@@ -0,0 +1,35 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-17T13:38:29Z

- **pkg/controller/jobframework/validation.go:27** by [@mimowo](https://github.com/mimowo) — 2023-03-17T13:38:28Z
```diff
@@ -19,14 +19,12 @@ import (
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
-
-	"sigs.k8s.io/kueue/pkg/constants"
 )
 
 var (
 	annotationsPath       = field.NewPath("metadata", "annotations")
-	ParentWorkloadKeyPath = annotationsPath.Key(constants.ParentWorkloadAnnotation)
-	QueueNamePath         = annotationsPath.Key(constants.QueueAnnotation)
+	ParentWorkloadKeyPath = annotationsPath.Key(ParentWorkloadAnnotation)
+	QueueNamePath         = annotationsPath.Key(QueueAnnotation)
```

  I suggest that we move out the QueueName function from the interface, because the logic of validation for `queueName` needs to agree with the QueueName() implementation. The best way to enforce it is to have it implemented on our side. If MPI want to support its spec field, we will have a separate PR to do it, and to support a dedicated function for this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-17T13:39:42Z

- **pkg/controller/jobs/job/job_controller.go:125** by [@mimowo](https://github.com/mimowo) — 2023-03-17T13:39:42Z
```diff
@@ -118,11 +117,11 @@ func (j *Job) Object() client.Object {
 }
 
 func (j *Job) ParentWorkloadName() string {
-	return j.Annotations[constants.ParentWorkloadAnnotation]
+	return j.Annotations[jobframework.ParentWorkloadAnnotation]
 }
 
 func (j *Job) QueueName() string {
-	return j.Annotations[constants.QueueAnnotation]
```

  Possibly, but if so, there wasn't a test for it, I'm going to add the support and the test, but I would like to move the function out from the interface as discussed here: https://github.com/kubernetes-sigs/kueue/pull/640#discussion_r1140250234.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-17T13:50:53Z

- **pkg/util/testingjobs/job/wrappers.go:95** by [@mimowo](https://github.com/mimowo) — 2023-03-17T13:50:53Z
```diff
@@ -0,0 +1,122 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package testing
+
+import (
+	batchv1 "k8s.io/api/batch/v1"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/pointer"
+)
+
+// JobWrapper wraps a Job.
+type JobWrapper struct{ batchv1.Job }
+
+// MakeJob creates a wrapper for a suspended job with a single container and parallelism=1.
+func MakeJob(name, ns string) *JobWrapper {
+	return &JobWrapper{batchv1.Job{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:        name,
+			Namespace:   ns,
+			Annotations: make(map[string]string, 1),
+		},
+		Spec: batchv1.JobSpec{
+			Parallelism: pointer.Int32(1),
+			Suspend:     pointer.Bool(true),
+			Template: corev1.PodTemplateSpec{
+				Spec: corev1.PodSpec{
+					RestartPolicy: "Never",
+					Containers: []corev1.Container{
+						{
+							Name:      "c",
+							Image:     "pause",
+							Command:   []string{},
+							Resources: corev1.ResourceRequirements{Requests: corev1.ResourceList{}},
+						},
+					},
+					NodeSelector: map[string]string{},
+				},
+			},
+		},
+	}}
+}
+
+// Obj returns the inner Job.
+func (j *JobWrapper) Obj() *batchv1.Job {
+	return &j.Job
+}
+
+// Suspend updates the suspend status of the job
+func (j *JobWrapper) Suspend(s bool) *JobWrapper {
+	j.Spec.Suspend = pointer.Bool(s)
+	return j
+}
+
+// Parallelism updates job parallelism.
+func (j *JobWrapper) Parallelism(p int32) *JobWrapper {
+	j.Spec.Parallelism = pointer.Int32(p)
+	return j
+}
+
+// PriorityClass updates job priorityclass.
+func (j *JobWrapper) PriorityClass(pc string) *JobWrapper {
+	j.Spec.Template.Spec.PriorityClassName = pc
+	return j
+}
+
+// Queue updates the queue name of the job
+func (j *JobWrapper) Queue(queue string) *JobWrapper {
+	j.Annotations[jobframework.QueueAnnotation] = queue
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-17T14:21:16Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:70** by [@mimowo](https://github.com/mimowo) — 2023-03-17T14:21:16Z
```diff
@@ -64,11 +63,11 @@ func (j *MPIJob) Object() client.Object {
 }
 
 func (j *MPIJob) ParentWorkloadName() string {
-	return j.Annotations[constants.ParentWorkloadAnnotation]
+	return j.Annotations[jobframework.ParentWorkloadAnnotation]
 }
 
 func (j *MPIJob) QueueName() string {
-	return j.Annotations[constants.QueueAnnotation]
+	return j.Annotations[jobframework.QueueAnnotation]
```

  Fixed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-17T15:50:16Z

- **pkg/controller/jobframework/validation.go:38** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T15:46:01Z
```diff
@@ -19,16 +19,22 @@ import (
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
-
-	"sigs.k8s.io/kueue/pkg/constants"
 )
 
 var (
 	annotationsPath       = field.NewPath("metadata", "annotations")
-	ParentWorkloadKeyPath = annotationsPath.Key(constants.ParentWorkloadAnnotation)
-	QueueNamePath         = annotationsPath.Key(constants.QueueAnnotation)
+	labelsPath            = field.NewPath("metadata", "labels")
+	parentWorkloadKeyPath = annotationsPath.Key(ParentWorkloadAnnotation)
+	queueNameLabelPath    = labelsPath.Key(QueueLabel)
 )
 
+func ValidateCreateForQueueName(job GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	allErrs = append(allErrs, ValidateLabelAsCRDName(job, QueueLabel)...)
+	allErrs = append(allErrs, ValidateAnnotationAsCRDName(job, QueueAnnotation)...)
+	return allErrs
+}
+
 func ValidateAnnotationAsCRDName(job GenericJob, crdNameAnnotation string) field.ErrorList {
```

  can be unexported now?

- **pkg/controller/jobframework/validation.go:48** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T15:46:08Z
```diff
@@ -39,19 +45,29 @@ func ValidateAnnotationAsCRDName(job GenericJob, crdNameAnnotation string) field
 	return allErrs
 }
 
+func ValidateLabelAsCRDName(job GenericJob, crdNameLabel string) field.ErrorList {
```

  unexported?

- **test/integration/controller/job/job_controller_test.go:247** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T15:49:56Z
```diff
@@ -243,6 +243,17 @@ var _ = ginkgo.Describe("Job controller", func() {
 				createdWorkload.Status.Conditions[0].Status == metav1.ConditionTrue
 		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
 	})
+	ginkgo.It("Should reconcile job when queueName set by annotation (deprecated)", func() {
```

  Thanks!
  
  Add an empty line before `It`

- **test/integration/controller/job/job_controller_test.go:259** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-17T15:50:09Z
```diff
@@ -243,6 +243,17 @@ var _ = ginkgo.Describe("Job controller", func() {
 				createdWorkload.Status.Conditions[0].Status == metav1.ConditionTrue
 		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
 	})
+	ginkgo.It("Should reconcile job when queueName set by annotation (deprecated)", func() {
+		ginkgo.By("checking the workload is created with correct queue name assigned")
+		jobQueueName := "test-queue"
+		job := testingjob.MakeJob(jobName, jobNamespace).QueueNameAnnotation("test-queue").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(jobQueueName))
+	})
 	ginkgo.When("The parent-workload annotation is used", func() {
```

  Empty line before `When`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-17T15:57:03Z

- **pkg/controller/jobframework/validation.go:38** by [@mimowo](https://github.com/mimowo) — 2023-03-17T15:57:02Z
```diff
@@ -19,16 +19,22 @@ import (
 	apivalidation "k8s.io/apimachinery/pkg/api/validation"
 	"k8s.io/apimachinery/pkg/util/validation"
 	"k8s.io/apimachinery/pkg/util/validation/field"
-
-	"sigs.k8s.io/kueue/pkg/constants"
 )
 
 var (
 	annotationsPath       = field.NewPath("metadata", "annotations")
-	ParentWorkloadKeyPath = annotationsPath.Key(constants.ParentWorkloadAnnotation)
-	QueueNamePath         = annotationsPath.Key(constants.QueueAnnotation)
+	labelsPath            = field.NewPath("metadata", "labels")
+	parentWorkloadKeyPath = annotationsPath.Key(ParentWorkloadAnnotation)
+	queueNameLabelPath    = labelsPath.Key(QueueLabel)
 )
 
+func ValidateCreateForQueueName(job GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	allErrs = append(allErrs, ValidateLabelAsCRDName(job, QueueLabel)...)
+	allErrs = append(allErrs, ValidateAnnotationAsCRDName(job, QueueAnnotation)...)
+	return allErrs
+}
+
 func ValidateAnnotationAsCRDName(job GenericJob, crdNameAnnotation string) field.ErrorList {
```

  Still is used by parent-workload, could follow up as for Queuename

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-17T15:57:12Z

- **test/integration/controller/job/job_controller_test.go:247** by [@mimowo](https://github.com/mimowo) — 2023-03-17T15:57:12Z
```diff
@@ -243,6 +243,17 @@ var _ = ginkgo.Describe("Job controller", func() {
 				createdWorkload.Status.Conditions[0].Status == metav1.ConditionTrue
 		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
 	})
+	ginkgo.It("Should reconcile job when queueName set by annotation (deprecated)", func() {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-17T15:57:59Z

- **test/integration/controller/job/job_controller_test.go:259** by [@mimowo](https://github.com/mimowo) — 2023-03-17T15:57:59Z
```diff
@@ -243,6 +243,17 @@ var _ = ginkgo.Describe("Job controller", func() {
 				createdWorkload.Status.Conditions[0].Status == metav1.ConditionTrue
 		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
 	})
+	ginkgo.It("Should reconcile job when queueName set by annotation (deprecated)", func() {
+		ginkgo.By("checking the workload is created with correct queue name assigned")
+		jobQueueName := "test-queue"
+		job := testingjob.MakeJob(jobName, jobNamespace).QueueNameAnnotation("test-queue").Obj()
+		gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		createdWorkload := &kueue.Workload{}
+		gomega.Eventually(func() error {
+			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(jobQueueName))
+	})
 	ginkgo.When("The parent-workload annotation is used", func() {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-17T15:58:14Z

- **pkg/controller/jobframework/validation.go:48** by [@mimowo](https://github.com/mimowo) — 2023-03-17T15:58:13Z
```diff
@@ -39,19 +45,29 @@ func ValidateAnnotationAsCRDName(job GenericJob, crdNameAnnotation string) field
 	return allErrs
 }
 
+func ValidateLabelAsCRDName(job GenericJob, crdNameLabel string) field.ErrorList {
```

  Yes, lowercased
