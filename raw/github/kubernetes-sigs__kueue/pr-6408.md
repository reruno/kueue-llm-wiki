# PR #6408: [ProvReq] Clean up integration tests

**Summary**: [ProvReq] Clean up integration tests

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6408

**Last updated**: 2025-10-10T10:50:59Z

---

## Metadata

- **State**: closed
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `provreq-test-cleanup` → `main`
- **Created**: 2025-08-04T09:06:09Z
- **Updated**: 2025-10-10T10:50:59Z
- **Merged**: —
- **Closed**: 2025-10-10T10:50:58Z
- **Labels**: `release-note-none`, `size/XL`, `needs-rebase`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 1   **+264 / -688**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Make tests more maintainable

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:
There is no semantic change in tests, just refactor

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-08-04T09:06:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8b54a600ddaed2856ac7d6a92922ac72174f2f33 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6890a1a75bacd60008f78165 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-04T09:06:17Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6408#" title="Author self-approved">PBundyra</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [gabesaba](https://github.com/gabesaba) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-07T01:23:25Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-29T07:48:09Z

@PBundyra: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-main-1-34 | 8b54a600ddaed2856ac7d6a92922ac72174f2f33 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/6408/pull-kueue-test-e2e-main-1-34/1961334755274264576) | true | `/test pull-kueue-test-e2e-main-1-34`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=6408). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3APBundyra). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-10-10T10:50:53Z

/close
To avoid distraction. We've discussed this with @PBundyra and the idea is to introduce horizontal helpers first to follow the DAMP principle, rather than vertical inside the test. For that we have these two issues:
https://github.com/kubernetes-sigs/kueue/issues/7021
https://github.com/kubernetes-sigs/kueue/issues/7023

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-10-10T10:50:59Z

@mimowo: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6408#issuecomment-3389341357):

>/close
>To avoid distraction. We've discussed this with @PBundyra and the idea is to introduce horizontal helpers first to follow the DAMP principle, rather than vertical inside the test. For that we have these two issues:
>https://github.com/kubernetes-sigs/kueue/issues/7021
>https://github.com/kubernetes-sigs/kueue/issues/7023
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-04T10:15:09Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-04T10:15:05Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  I don't think we should introduce many programming functions for TEST.
  In production, we aligh with DRY, but in Test, we should implement those as DUMP which means redundant codes in Test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-04T10:30:08Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@mimowo](https://github.com/mimowo) — 2025-08-04T10:30:08Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  Yeah, it is a tradeoff, it is a detail between duplication and declarative language. 
  
  Ideally tests are declarative and short, reconciling these two objectives is an art on its own :) 
  
  We sometimes introduce helper functions for testing when they have a good chance of being reusable, but these are rather not reusable, so I agree with @tenzen-y.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-04T10:31:29Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@mimowo](https://github.com/mimowo) — 2025-08-04T10:31:29Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  btw, rather than code duplication I think this is a bigger issue with the test: https://github.com/kubernetes-sigs/kueue/issues/6311

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-04T11:37:07Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@PBundyra](https://github.com/PBundyra) — 2025-08-04T11:37:07Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  I agree with both of you but at the same time functions like `updateProvisioningRequestStatus`, `expectAdmissionCheckState` reduced a lot of boiler plate which resulted in over 400 of deleted loc which improves maintainability a lot.
  
  We do use helper functions anyway, they're just in `util` package but I don't really see a big difference between defining them in `util` package or in the tests

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-04T11:41:41Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-04T11:41:41Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  In Test, we respect how to maintain the Test case instead of "code." In Test, when the expected results are changed, we typically fix the boiler plates everywhere. In that case, if we forgot to fix boiler plates somewhere, the Test will fail.
  
  This failure is important to maintain Test cases.

### Review by [@gabesaba](https://github.com/gabesaba) (commented) — 2025-08-04T14:11:46Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:48** by [@gabesaba](https://github.com/gabesaba) — 2025-08-04T13:53:04Z
```diff
@@ -43,12 +45,12 @@ const (
 	customResourceOne = "example.org/res1"
 )
 
-var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
-	var (
-		resourceGPU    corev1.ResourceName = "example.com/gpu"
-		flavorOnDemand                     = "on-demand"
-	)
+var (
```

  nit: const

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:889** by [@gabesaba](https://github.com/gabesaba) — 2025-08-04T14:01:30Z
```diff
@@ -881,27 +616,10 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 
 		ginkgo.It("Admission checks for an evicted workload are Pending", func() {
 			// Repro for https://github.com/kubernetes-sigs/kueue/issues/5129
-			ginkgo.By("Setting the quota reservation to the workload", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
-					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
```

  how come this can be deleted?

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:931** by [@gabesaba](https://github.com/gabesaba) — 2025-08-04T14:01:43Z
```diff
@@ -923,27 +641,10 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 
 		ginkgo.It("Should retry if a ProvisioningRequest fails, then succeed if the second Provisioning request succeeds", func() {
-			ginkgo.By("Setting the quota reservation to the workload", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
-					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
```

  same as previous

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1037** by [@gabesaba](https://github.com/gabesaba) — 2025-08-04T14:02:37Z
```diff
@@ -984,72 +681,28 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			ginkgo.By("Setting the quota reservation to the workload", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
-					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
+			setQuotaReservation(ctx, k8sClient, wlKey, admission)
 
-			ginkgo.By("Checking the provision request-2 exists", func() {
-				gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-				provReqKey := types.NamespacedName{
-					Namespace: wlKey.Namespace,
-					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 2),
-				}
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
+			provReqKey2 := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 2),
+			}
+			expectProvisioningRequest(ctx, k8sClient, provReqKey2, &createdRequest)
 
-			ginkgo.By("Setting the provision request-2 as Provisioned", func() {
-				provReqKey := types.NamespacedName{
-					Namespace: wlKey.Namespace,
-					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 2),
-				}
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
-					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
-						Type:   autoscaling.Provisioned,
-						Status: metav1.ConditionTrue,
-						Reason: autoscaling.Provisioned,
-					})
-					g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			updateProvisioningRequestStatus(ctx, k8sClient, provReqKey2, metav1.Condition{
+				Type:   autoscaling.Provisioned,
+				Status: metav1.ConditionTrue,
+				Reason: autoscaling.Provisioned,
 			})
 
-			ginkgo.By("Checking the admission check is ready", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(state).NotTo(gomega.BeNil())
-					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
+			expectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, "")
 		})
 
 		ginkgo.It("Should retry if a ProvisioningRequest fails, then reject AdmissionCheck if the second ProvisioningRequest fails", func() {
-			ginkgo.By("Setting the quota reservation to the workload", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
-					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
```

  ^

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@gabesaba](https://github.com/gabesaba) — 2025-08-04T14:11:42Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  I support this PR. I think it is a good cleanup and reduces the risk of bugs. 
  
  > In Test, we respect how to maintain the Test case instead of "code." In Test, when the expected results are changed, we typically fix the boiler plates everywhere. In that case, if we forgot to fix boiler plates somewhere, the Test will fail.
  
  Still, another function of tests is to serve as documentation of behavior. If tests are hard to read/understand because of their verbosity they lose this functionality
  
  > I don't think we should introduce many programming functions for TEST.
  In production, we aligh with DRY, but in Test, we should implement those as [DAMP] which means redundant codes in Test.
  
  I think as long as the test data used for assertions is very explicit and can be read where the test is defined (as it is in this PR, where the data we wish to assert is visible from each individual test, and passed to the function), DRY is good. It will make new tests easier to review, and could even help spot bugs in existing tests, since they are so hard to read given the extra boilerplate
  
  > We sometimes introduce helper functions for testing when they have a good chance of being reusable, but these are rather not reusable, so I agree with @tenzen-y.
  
  Several of these functions are used >10 times, and the scope they are defined is just to this file (not to some utility package)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-04T14:22:21Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@mimowo](https://github.com/mimowo) — 2025-08-04T14:20:36Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  Ok, I looked through the code again, and I have mixed feelings. Some aspects I like and some I don't, it is hard for me to update in comments, so let me summarize here:
  - PR1: function ExpectAdmissionCheck(wl, AdmissionCheck), without ginkgo.By, when it fails it logs the entire workload and workload status. Use this function also in tas_test.go and potentially others. I would inspire it based on [this](https://github.com/kubernetes-sigs/kueue/pull/6408/files#diff-b524dd7e94e695c9007bf094b1ff9cd5a94952214c0db7de004c7ce5bea7ed1bL1131-L1137)
  - PR2: function util.HasEventAppeared which is a wrapper around testing.HasEventAppeared which asserts not error, and result is true. Use the new function accross the code
  - PR3: better use of ginkgo.By steps
    * revert expectWorkloadDeactivated but split its parts into seprate ginkgo.By. Move ExpectEvictedWorkloadsTotalMetric out of Eventualy as it is already using eventually
    * revert checkPodTemplate but split what it wrapps into By like "Verify ProvisioningRequest Pod template for PodSet1"
    
  wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-04T14:38:53Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@mimowo](https://github.com/mimowo) — 2025-08-04T14:38:53Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  > I support this PR. I think it is a good cleanup and reduces the risk of bugs.
  
  Commonizing is nice, but I would suggest first to look for functions which have a good potential for reusability, and move them into testing utils so that also other test suites can benefit.
  
  The two most outstanding places to resusability to me seem `ExpectAdmissionCheck` and `HasEventAppeared` so I would start here. 
  
  I actually hope people will not be using `expectWorkloadDeactivated` or `checkPodTemplate` functions as they are defined here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-04T14:44:39Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-04T14:44:39Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  >> In Test, we respect how to maintain the Test case instead of "code." In Test, when the expected results are changed, we typically fix the boiler plates everywhere. In that case, if we forgot to fix boiler plates somewhere, the Test will fail.
  >>
  > Still, another function of tests is to serve as documentation of behavior. If tests are hard to read/understand because of their verbosity they lose this functionality
  
  Yes, each test case also has a documentation role. So, such utility functions wrap what is criteria for the story.
  For example, the `checkPodTemplate` checks if 
  
  1. Pod can be got from cluster
  2. createdTemplate has expected containers
  3. createdTemplate has expected nodeSelectors
  4. createdTemplate has expected labels.
  
  The above utilities hide those verifications. In other words, each test case lacks those steps about the expected story. For sure, we jump to the utily function, then we can check what is expected story. But, in Test, I think that we should represent the expected story at that place as much as possible. That's why we should respect DUMP rather than DRY.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-04T14:49:16Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@mimowo](https://github.com/mimowo) — 2025-08-04T14:49:16Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  > That's why we should respect DUMP rather than DRY.
  
  Yes, but acknowledging both are important. 
  
  BTW, I bet you mean DAMP "Descriptive and Meaningful Phrases"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-04T15:28:47Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1153** by [@mimowo](https://github.com/mimowo) — 2025-08-04T15:28:47Z
```diff
@@ -1575,3 +1070,85 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 	})
 })
+
+func setQuotaReservation(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, admission *kueue.Admission) {
+	ginkgo.By("Setting the quota reservation to the workload", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).Should(gomega.Succeed())
+			g.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectProvisioningRequest(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, createdRequest *autoscaling.ProvisioningRequest) {
+	ginkgo.By("Checking that the provision request exists", func() {
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func updateProvisioningRequestStatus(ctx context.Context, k8sClient client.Client, provReqKey types.NamespacedName, condition metav1.Condition) {
+	ginkgo.By(fmt.Sprintf("Setting the provision request condition %s to %s", condition.Type, condition.Status), func() {
+		createdRequest := &autoscaling.ProvisioningRequest{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, provReqKey, createdRequest)).Should(gomega.Succeed())
+			apimeta.SetStatusCondition(&createdRequest.Status.Conditions, condition)
+			g.Expect(k8sClient.Status().Update(ctx, createdRequest)).Should(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectWorkloadDeactivated(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName, cqName string) {
+	ginkgo.By("Checking if workload is deactivated, Rejected status was once in the status.admissionCheck[*] field, an event is emitted and a metric is increased", func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsActive(updatedWl)).To(gomega.BeFalse())
+
+			ok, err := testing.HasEventAppeared(ctx, k8sClient, corev1.Event{
+				Reason:  "AdmissionCheckRejected",
+				Type:    corev1.EventTypeWarning,
+				Message: fmt.Sprintf("Deactivating workload because AdmissionCheck for %v was Rejected: ", acName),
+			})
+			g.Expect(err).NotTo(gomega.HaveOccurred())
+			g.Expect(ok).To(gomega.BeTrue())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			g.Expect(workload.IsEvictedByDeactivation(updatedWl)).To(gomega.BeTrue())
+			util.ExpectEvictedWorkloadsTotalMetric(cqName, "DeactivatedDueToAdmissionCheck", 1)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func expectAdmissionCheckState(ctx context.Context, k8sClient client.Client, wlKey types.NamespacedName, acName string, expectedState kueue.CheckState, expectedMessage string) {
+	ginkgo.By(fmt.Sprintf("Checking admission check %q state is %s", acName, expectedState), func() {
+		updatedWl := &kueue.Workload{}
+		gomega.Eventually(func(g gomega.Gomega) {
+			g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+			acState := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(acName))
+			g.Expect(acState).NotTo(gomega.BeNil())
+			g.Expect(acState.State).To(gomega.Equal(expectedState))
+			if expectedMessage != "" {
+				g.Expect(acState.Message).To(gomega.Equal(expectedMessage))
+			}
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	})
+}
+
+func checkPodTemplate(ctx context.Context, k8sClient client.Client, namespace, name string, expectedContainers []corev1.Container, expectedNodeSelector map[string]string, ignoreCmpOpts ...cmp.Option) {
+	createdTemplate := &corev1.PodTemplate{}
+	templateKey := types.NamespacedName{
+		Namespace: namespace,
+		Name:      name,
+	}
+	gomega.Eventually(func(g gomega.Gomega) {
+		g.Expect(k8sClient.Get(ctx, templateKey, createdTemplate)).Should(gomega.Succeed())
+	}, util.Timeout, util.Interval).Should(gomega.Succeed())
+	gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(expectedContainers, ignoreCmpOpts...))
+	gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(expectedNodeSelector))
+	gomega.Expect(createdTemplate.ObjectMeta.GetLabels()).To(gomega.BeComparableTo(map[string]string{constants.ManagedByKueueLabelKey: constants.ManagedByKueueLabelValue}))
+}
```

  > I actually hope people will not be using expectWorkloadDeactivated or checkPodTemplate functions as they are defined here.
  
  And just to be clear here. I think this function expect workload deactivated makes it really unclear what is the intention by so many internal checks. I think we can check if workload is deactivated in one line. All the other checks are important but I don't see a need to multiply them by test case, because it also makes tests unnecessarily longer. I would like to have all the checks only once, and all other tests would just check if the workload has active=false.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-06T10:16:53Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:889** by [@PBundyra](https://github.com/PBundyra) — 2025-08-06T10:16:52Z
```diff
@@ -881,27 +616,10 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 
 		ginkgo.It("Admission checks for an evicted workload are Pending", func() {
 			// Repro for https://github.com/kubernetes-sigs/kueue/issues/5129
-			ginkgo.By("Setting the quota reservation to the workload", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
-					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
```

  Because I moved setting quota to the `JustBeforeEach` section

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-06T10:16:59Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:931** by [@PBundyra](https://github.com/PBundyra) — 2025-08-06T10:16:59Z
```diff
@@ -923,27 +641,10 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 		})
 
 		ginkgo.It("Should retry if a ProvisioningRequest fails, then succeed if the second Provisioning request succeeds", func() {
-			ginkgo.By("Setting the quota reservation to the workload", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
-					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
```

  ditto

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-06T10:17:05Z

- **test/integration/singlecluster/controller/admissionchecks/provisioning/provisioning_test.go:1037** by [@PBundyra](https://github.com/PBundyra) — 2025-08-06T10:17:05Z
```diff
@@ -984,72 +681,28 @@ var _ = ginkgo.Describe("Provisioning", ginkgo.Ordered, ginkgo.ContinueOnFailure
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			ginkgo.By("Setting the quota reservation to the workload", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
-					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
+			setQuotaReservation(ctx, k8sClient, wlKey, admission)
 
-			ginkgo.By("Checking the provision request-2 exists", func() {
-				gomega.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-				provReqKey := types.NamespacedName{
-					Namespace: wlKey.Namespace,
-					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 2),
-				}
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
+			provReqKey2 := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 2),
+			}
+			expectProvisioningRequest(ctx, k8sClient, provReqKey2, &createdRequest)
 
-			ginkgo.By("Setting the provision request-2 as Provisioned", func() {
-				provReqKey := types.NamespacedName{
-					Namespace: wlKey.Namespace,
-					Name:      provisioning.ProvisioningRequestName(wlKey.Name, kueue.AdmissionCheckReference(ac.Name), 2),
-				}
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, provReqKey, &createdRequest)).Should(gomega.Succeed())
-					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
-						Type:   autoscaling.Provisioned,
-						Status: metav1.ConditionTrue,
-						Reason: autoscaling.Provisioned,
-					})
-					g.Expect(k8sClient.Status().Update(ctx, &createdRequest)).Should(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			updateProvisioningRequestStatus(ctx, k8sClient, provReqKey2, metav1.Condition{
+				Type:   autoscaling.Provisioned,
+				Status: metav1.ConditionTrue,
+				Reason: autoscaling.Provisioned,
 			})
 
-			ginkgo.By("Checking the admission check is ready", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).To(gomega.Succeed())
-					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, kueue.AdmissionCheckReference(ac.Name))
-					g.Expect(state).NotTo(gomega.BeNil())
-					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
+			expectAdmissionCheckState(ctx, k8sClient, wlKey, ac.Name, kueue.CheckStateReady, "")
 		})
 
 		ginkgo.It("Should retry if a ProvisioningRequest fails, then reject AdmissionCheck if the second ProvisioningRequest fails", func() {
-			ginkgo.By("Setting the quota reservation to the workload", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlKey, &updatedWl)).Should(gomega.Succeed())
-					g.Expect(util.SetQuotaReservation(ctx, k8sClient, &updatedWl, admission)).To(gomega.Succeed())
-				}, util.Timeout, util.Interval).Should(gomega.Succeed())
-			})
```

  ditto
