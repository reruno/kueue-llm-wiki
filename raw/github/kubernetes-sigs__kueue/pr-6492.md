# PR #6492: Add initial documentation for ElastiJobs - batchv1/Job

**Summary**: Add initial documentation for ElastiJobs - batchv1/Job

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6492

**Last updated**: 2025-08-12T15:26:05Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ichekrygin](https://github.com/ichekrygin)
- **Branch**: `elastic-job-docs` → `main`
- **Created**: 2025-08-07T05:48:34Z
- **Updated**: 2025-08-12T15:26:05Z
- **Merged**: 2025-08-12T06:55:07Z
- **Closed**: 2025-08-12T06:55:08Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: _none_
- **Changed files**: 4   **+201 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

/kind documentation

#### What this PR does / why we need it:

This PR updates Kueue documentation to include ElasticJobs Concept and elastic job support in `batchv1/Job`

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6349

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-08-07T05:48:41Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 02f22d4dd33fb2f0afafee42ce8271f053eae5ee |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/689a3cdb161d9a000874e709 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6492--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6492--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTY0OTItLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.X6G8GYXXxFozYOjL3dDnmtMsRk3TreX1NqeWb2PsfaE)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6492--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-12T06:53:39Z

LGTM label has been added.  <details>Git tree hash: df69da270764cc0993c1491349fab79872f60109</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-12T06:53:42Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6492#" title="Author self-approved">ichekrygin</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6492#pullrequestreview-3108815420" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-12T07:07:55Z

/cherrypick website

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-12T07:08:20Z

/cherry-pick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-12T07:08:33Z

@mimowo: new pull request created: #6552

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6492#issuecomment-3177985077):

>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-12T07:09:09Z

@mimowo: new pull request could not be created: failed to create pull request against kubernetes-sigs/kueue#website from head k8s-infra-cherrypick-robot:cherry-pick-6492-to-website: status code 422 not one of [201], body: {"message":"Validation Failed","errors":[{"resource":"PullRequest","code":"custom","message":"A pull request already exists for k8s-infra-cherrypick-robot:cherry-pick-6492-to-website."}],"documentation_url":"https://docs.github.com/rest/pulls/pulls#create-a-pull-request","status":"422"}

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6492#issuecomment-3177986160):

>/cherry-pick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-08-07T21:12:02Z

- **site/static/examples/jobs/sample-scalable-job.yaml:11** by [@kannon92](https://github.com/kannon92) — 2025-08-07T21:12:01Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
```

  Don't we need to set completionMode?
  
  I think NonIndexedJobs are immutable and you would have to have an IndexedJob to actually allow for scaling of a Job.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-07T21:34:42Z

- **site/static/examples/jobs/sample-scalable-job.yaml:11** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-07T21:34:42Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
```

  > I think NonIndexedJobs are immutable and you would have to have an IndexedJob to actually allow for scaling of a Job.
  
  Hi @kannon92, thank you for taking a look.
  
  I believe `parallelism` is always mutable, regardless of whether `completionMode` is set to `NonIndexed` (default) or `Indexed`.
  
  Perhaps the confusion is with the `completions` field, which *is* immutable once the Job starts running.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-08-07T23:43:20Z

- **site/static/examples/jobs/sample-scalable-job.yaml:11** by [@kannon92](https://github.com/kannon92) — 2025-08-07T23:43:20Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
```

  https://github.com/kubernetes/kubernetes/blob/master/pkg/apis/batch/validation/validation.go#L860

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-08-07T23:46:17Z

- **site/content/en/docs/concepts/elastic_workload.md:71** by [@kannon92](https://github.com/kannon92) — 2025-08-07T23:46:17Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+---
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **delta** of the additional replicas. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
+3. **Scaling Down**: If the job reduces its parallelism, the updated pod count is recorded directly into the existing workload.
+4. **Preemption**: Follows the existing workload preemption mechanism.
+5. **Completion**: Follows the existing workload completion behavior.
+
+---
+
+## Example
+
+{{< include "examples/jobs/sample-scalable-job.yaml" "yaml" >}}
+
+The example above will result in an admitted workload and 3 running pods.
+The parallelism can be adjusted (increased or decreased) as long as the job remains in an "Active" state (i.e., not yet completed).
+
+---
+
+## Feature Gate
+
+Elastic Workloads via Workload Slices are gated by the following feature flag:
+
+```yaml
+ElasticJobsViaWorkloadSlices: true
+```
+
+Additionally, Elastic Job behavior must be explicitly enabled on a per-job basis via annotation:
+
+```yaml
+metadata:
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+```
+
+---
+
+## Limitations
+
+* Currently available only for `batch/v1.Job` workloads
+* Slice reconciliation is in **Alpha** and may evolve in future releases
```

  ```suggestion
  * Slice reconciliation is in **Alpha** and may evolve in future releases
  ```
  
  I would probably not include this as you have a section at the top of this doc where you state that this is alpha.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2025-08-07T23:46:36Z

- **site/content/en/docs/concepts/elastic_workload.md:71** by [@kannon92](https://github.com/kannon92) — 2025-08-07T23:46:36Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+---
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **delta** of the additional replicas. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
+3. **Scaling Down**: If the job reduces its parallelism, the updated pod count is recorded directly into the existing workload.
+4. **Preemption**: Follows the existing workload preemption mechanism.
+5. **Completion**: Follows the existing workload completion behavior.
+
+---
+
+## Example
+
+{{< include "examples/jobs/sample-scalable-job.yaml" "yaml" >}}
+
+The example above will result in an admitted workload and 3 running pods.
+The parallelism can be adjusted (increased or decreased) as long as the job remains in an "Active" state (i.e., not yet completed).
+
+---
+
+## Feature Gate
+
+Elastic Workloads via Workload Slices are gated by the following feature flag:
+
+```yaml
+ElasticJobsViaWorkloadSlices: true
+```
+
+Additionally, Elastic Job behavior must be explicitly enabled on a per-job basis via annotation:
+
+```yaml
+metadata:
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+```
+
+---
+
+## Limitations
+
+* Currently available only for `batch/v1.Job` workloads
+* Slice reconciliation is in **Alpha** and may evolve in future releases
```

  Also the entire feature is alpha and not just "slice reconciliation"?

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-08T00:32:02Z

- **site/static/examples/jobs/sample-scalable-job.yaml:11** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-08T00:32:02Z
```diff
@@ -0,0 +1,25 @@
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  namespace: default
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  parallelism: 3
```

  Right, for scaling `batchv1/Job` we don't change "completions", we change "parallelism" :)

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-08T00:34:08Z

- **site/content/en/docs/concepts/elastic_workload.md:71** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-08T00:34:08Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+---
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **delta** of the additional replicas. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
+3. **Scaling Down**: If the job reduces its parallelism, the updated pod count is recorded directly into the existing workload.
+4. **Preemption**: Follows the existing workload preemption mechanism.
+5. **Completion**: Follows the existing workload completion behavior.
+
+---
+
+## Example
+
+{{< include "examples/jobs/sample-scalable-job.yaml" "yaml" >}}
+
+The example above will result in an admitted workload and 3 running pods.
+The parallelism can be adjusted (increased or decreased) as long as the job remains in an "Active" state (i.e., not yet completed).
+
+---
+
+## Feature Gate
+
+Elastic Workloads via Workload Slices are gated by the following feature flag:
+
+```yaml
+ElasticJobsViaWorkloadSlices: true
+```
+
+Additionally, Elastic Job behavior must be explicitly enabled on a per-job basis via annotation:
+
+```yaml
+metadata:
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+```
+
+---
+
+## Limitations
+
+* Currently available only for `batch/v1.Job` workloads
+* Slice reconciliation is in **Alpha** and may evolve in future releases
```

  Sounds good. I will remove it from the "Limitations" section.

### Review by [@amy](https://github.com/amy) (commented) — 2025-08-09T22:08:10Z

- **site/content/en/docs/concepts/elastic_workload.md:34** by [@amy](https://github.com/amy) — 2025-08-09T22:08:10Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+---
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **delta** of the additional replicas. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
```

  Clarifying question about `the new slice replaces the original workload`. Is the new slice this amount of quota: Old + delta? Asking because its a scale up.

### Review by [@amy](https://github.com/amy) (commented) — 2025-08-09T22:16:51Z

- **site/content/en/docs/concepts/_index.md:94** by [@amy](https://github.com/amy) — 2025-08-09T22:16:51Z
```diff
@@ -89,4 +89,9 @@ resources that are now required by the owning ClusterQueue.
 
 ### [Fair Sharing](/docs/concepts/fair_sharing)
 
-Mechanisms in Kueue to share quota between tenants fairly.
\ No newline at end of file
+Mechanisms in Kueue to share quota between tenants fairly.
+
+### [Elastic Workloads](/docs/concepts/elastic_workload)
```

  It would probably be good to somehow correlate the docs for `partial admission and dynamic reclaim`. And differentiate these features so people don't get confused which stage of scheduling dynamic scaling happens.
  
  If it is possible to use these features together, let's also provide samples of how to do that and what we should see when running a watch on the workloads based off cohort conditions.

### Review by [@amy](https://github.com/amy) (commented) — 2025-08-09T22:22:40Z

- **site/content/en/docs/tasks/run/jobs.md:239** by [@amy](https://github.com/amy) — 2025-08-09T22:22:40Z
```diff
@@ -215,3 +215,26 @@ For example, a Job defined by the following manifest:
 {{< include "examples/jobs/sample-job-partial-admission.yaml" "yaml" >}}
 
 When queued in a ClusterQueue with only 9 CPUs available, it will be admitted with `parallelism=9`. Note that the number of completions doesn't change.
+
+## ElasticJob
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+Kueue supports the ability for batch users to scale a `Job`'s parallelism **without recreating, restarting, or suspending** the Job.
+
+To enable this feature, ensure the following:
+
+* The `ElasticJobsViaWorkloadSlices` feature gate is set to `true`
+* The Job is annotated with `kueue.x-k8s.io/elastic-job: "true"`
+
+For example, a Job defined by the following manifest:
+
+{{< include "examples/jobs/sample-scalable-job.yaml" "yaml" >}}
+
+Once the Job is created, you can scale it by updating the `parallelism` field and applying the change.
+
+When scaling **up**, you should observe:
+
+* A **new workload** created and admitted for the updated parallelism
+* The **previous workload** being marked as `Finished`
```

  It would be good to provide kubectl commands and what people should see when they run a `watch` on events for the workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-11T15:46:40Z

- **site/content/en/docs/concepts/elastic_workload.md:29** by [@mimowo](https://github.com/mimowo) — 2025-08-11T15:46:39Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+---
```

  Let's don't add the `---`. I have nothing against the line, but just for the sake of visual consistency across the documentation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-11T15:56:27Z

- **site/content/en/docs/concepts/elastic_workload.md:27** by [@mimowo](https://github.com/mimowo) — 2025-08-11T15:56:27Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
```

  In the current form this feels redundant as already mentioned in the intro "abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.". 
  
  I would be ok with higher level "use-cases" section, like:
  - like dynamically adjusting thoughput for embarrassingly parallel jobs, or
  - using AI/ML frameworks which support elasticity, like [Distributed Torch Elastic](https://docs.pytorch.org/docs/stable/distributed.elastic.html)

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T16:17:53Z

- **site/content/en/docs/concepts/elastic_workload.md:34** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T16:17:53Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+---
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **delta** of the additional replicas. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
```

  The new slice contains the total desired replica count (not delta).

### Review by [@amy](https://github.com/amy) (commented) — 2025-08-11T16:23:34Z

- **site/content/en/docs/concepts/elastic_workload.md:34** by [@amy](https://github.com/amy) — 2025-08-11T16:23:34Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+---
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **delta** of the additional replicas. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
```

  Makes sense! In that case, can you change the text to this:
  ```
  2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the total desired replica count, which includes the **delta** of the additional replicas. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
  ```
  
  Or something to that effect. Thank you!

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T16:53:27Z

- **site/content/en/docs/concepts/elastic_workload.md:34** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T16:53:27Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+---
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **delta** of the additional replicas. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
```

  Updated, PTAL.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T16:55:32Z

- **site/content/en/docs/concepts/_index.md:94** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T16:55:32Z
```diff
@@ -89,4 +89,9 @@ resources that are now required by the owning ClusterQueue.
 
 ### [Fair Sharing](/docs/concepts/fair_sharing)
 
-Mechanisms in Kueue to share quota between tenants fairly.
\ No newline at end of file
+Mechanisms in Kueue to share quota between tenants fairly.
+
+### [Elastic Workloads](/docs/concepts/elastic_workload)
```

  Excellent callout @amy. 
  
  I added a "partial admission" limitation to the section below. 
  
  As for the 'dynamic reclaim` - I may need a little guidance. Elastic Jobs are not relying on or intersecting with the `dynamic reclaim` feature/functionality. What would you recommend?

### Review by [@amy](https://github.com/amy) (commented) — 2025-08-11T17:04:56Z

- **site/content/en/docs/concepts/elastic_workload.md:34** by [@amy](https://github.com/amy) — 2025-08-11T17:04:56Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+---
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **delta** of the additional replicas. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
```

  LGTM

### Review by [@amy](https://github.com/amy) (commented) — 2025-08-11T17:08:46Z

- **site/content/en/docs/concepts/_index.md:94** by [@amy](https://github.com/amy) — 2025-08-11T17:08:46Z
```diff
@@ -89,4 +89,9 @@ resources that are now required by the owning ClusterQueue.
 
 ### [Fair Sharing](/docs/concepts/fair_sharing)
 
-Mechanisms in Kueue to share quota between tenants fairly.
\ No newline at end of file
+Mechanisms in Kueue to share quota between tenants fairly.
+
+### [Elastic Workloads](/docs/concepts/elastic_workload)
```

  Solid. Maybe a specific callout that this feature doesn't intersect with dynamic reclaim would suffice? Mainly, its a similar domain. So any added clarification on which features can or cannot be combined would be good.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-11T17:19:51Z

- **site/content/en/docs/concepts/elastic_workload.md:23** by [@mimowo](https://github.com/mimowo) — 2025-08-11T17:19:50Z
```diff
@@ -0,0 +1,71 @@
+
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
```

  I think somewhere in this section we should add a note that the scaling operation assumes workload to remain in the same ResourceFlavor.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T18:11:22Z

- **site/content/en/docs/tasks/run/jobs.md:239** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T18:11:22Z
```diff
@@ -215,3 +215,26 @@ For example, a Job defined by the following manifest:
 {{< include "examples/jobs/sample-job-partial-admission.yaml" "yaml" >}}
 
 When queued in a ClusterQueue with only 9 CPUs available, it will be admitted with `parallelism=9`. Note that the number of completions doesn't change.
+
+## ElasticJob
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+Kueue supports the ability for batch users to scale a `Job`'s parallelism **without recreating, restarting, or suspending** the Job.
+
+To enable this feature, ensure the following:
+
+* The `ElasticJobsViaWorkloadSlices` feature gate is set to `true`
+* The Job is annotated with `kueue.x-k8s.io/elastic-job: "true"`
+
+For example, a Job defined by the following manifest:
+
+{{< include "examples/jobs/sample-scalable-job.yaml" "yaml" >}}
+
+Once the Job is created, you can scale it by updating the `parallelism` field and applying the change.
+
+When scaling **up**, you should observe:
+
+* A **new workload** created and admitted for the updated parallelism
+* The **previous workload** being marked as `Finished`
```

  Updated with Example Walk-through section. PTAL

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T18:14:31Z

- **site/content/en/docs/concepts/elastic_workload.md:23** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T18:14:31Z
```diff
@@ -0,0 +1,71 @@
+
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
```

  Do you think adding this to the "Limitations" section would be a good choice?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-11T18:15:02Z

- **site/content/en/docs/concepts/elastic_workload.md:64** by [@mimowo](https://github.com/mimowo) — 2025-08-11T18:15:02Z
```diff
@@ -0,0 +1,71 @@
+
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **increased** replicas count. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
+3. **Scaling Down**: If the job reduces its parallelism, the updated pod count is recorded directly into the existing workload.
+4. **Preemption**: Follows the existing workload preemption mechanism.
+5. **Completion**: Follows the existing workload completion behavior.
+
+## Example
+
+{{< include "examples/jobs/sample-scalable-job.yaml" "yaml" >}}
+
+The example above will result in an admitted workload and 3 running pods.
+The parallelism can be adjusted (increased or decreased) as long as the job remains in an "Active" state (i.e., not yet completed).
+
+## Feature Gate
+
+Elastic Workloads via Workload Slices are gated by the following feature flag:
+
+```yaml
+ElasticJobsViaWorkloadSlices: true
+```
+
+Additionally, Elastic Job behavior must be explicitly enabled on a per-job basis via annotation:
+
+```yaml
+metadata:
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+```
+
+## Limitations
+
+* Currently available only for `batch/v1.Job` workloads.
+* Slice reconciliation is in **Alpha** and may change in future releases.
```

  I would drop this point, sounds too technical. Any feature in "Alpha" can change pretty drastically, or be even dropped, so I wouldn't mention that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-11T18:15:46Z

- **site/content/en/docs/concepts/elastic_workload.md:63** by [@mimowo](https://github.com/mimowo) — 2025-08-11T18:15:46Z
```diff
@@ -0,0 +1,71 @@
+
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **increased** replicas count. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
+3. **Scaling Down**: If the job reduces its parallelism, the updated pod count is recorded directly into the existing workload.
+4. **Preemption**: Follows the existing workload preemption mechanism.
+5. **Completion**: Follows the existing workload completion behavior.
+
+## Example
+
+{{< include "examples/jobs/sample-scalable-job.yaml" "yaml" >}}
+
+The example above will result in an admitted workload and 3 running pods.
+The parallelism can be adjusted (increased or decreased) as long as the job remains in an "Active" state (i.e., not yet completed).
+
+## Feature Gate
+
+Elastic Workloads via Workload Slices are gated by the following feature flag:
+
+```yaml
+ElasticJobsViaWorkloadSlices: true
+```
+
+Additionally, Elastic Job behavior must be explicitly enabled on a per-job basis via annotation:
+
+```yaml
+metadata:
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+```
+
+## Limitations
```

  Please also add no support for Topology-Aware Scheduling and MultiKueue. I know you are working on MultiKueue, but we can then drop the point. For now it remains a limitation.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T18:19:36Z

- **site/content/en/docs/concepts/elastic_workload.md:23** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T18:19:36Z
```diff
@@ -0,0 +1,71 @@
+
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
```

  Updated "Limitations" - PTAL.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T18:43:22Z

- **site/content/en/docs/concepts/elastic_workload.md:63** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T18:43:22Z
```diff
@@ -0,0 +1,71 @@
+
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **increased** replicas count. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
+3. **Scaling Down**: If the job reduces its parallelism, the updated pod count is recorded directly into the existing workload.
+4. **Preemption**: Follows the existing workload preemption mechanism.
+5. **Completion**: Follows the existing workload completion behavior.
+
+## Example
+
+{{< include "examples/jobs/sample-scalable-job.yaml" "yaml" >}}
+
+The example above will result in an admitted workload and 3 running pods.
+The parallelism can be adjusted (increased or decreased) as long as the job remains in an "Active" state (i.e., not yet completed).
+
+## Feature Gate
+
+Elastic Workloads via Workload Slices are gated by the following feature flag:
+
+```yaml
+ElasticJobsViaWorkloadSlices: true
+```
+
+Additionally, Elastic Job behavior must be explicitly enabled on a per-job basis via annotation:
+
+```yaml
+metadata:
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+```
+
+## Limitations
```

  Added.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T18:44:43Z

- **site/content/en/docs/concepts/elastic_workload.md:64** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T18:44:43Z
```diff
@@ -0,0 +1,71 @@
+
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
+
+## Lifecycle
+
+1. **Initial Admission**: A job is submitted and its first `Workload` is created and admitted.
+2. **Scaling Up**: If the job requests more parallelism, a new slice is created with the **increased** replicas count. Once admitted, the new slice replaces the original workload by marking the old one as `Finished`.
+3. **Scaling Down**: If the job reduces its parallelism, the updated pod count is recorded directly into the existing workload.
+4. **Preemption**: Follows the existing workload preemption mechanism.
+5. **Completion**: Follows the existing workload completion behavior.
+
+## Example
+
+{{< include "examples/jobs/sample-scalable-job.yaml" "yaml" >}}
+
+The example above will result in an admitted workload and 3 running pods.
+The parallelism can be adjusted (increased or decreased) as long as the job remains in an "Active" state (i.e., not yet completed).
+
+## Feature Gate
+
+Elastic Workloads via Workload Slices are gated by the following feature flag:
+
+```yaml
+ElasticJobsViaWorkloadSlices: true
+```
+
+Additionally, Elastic Job behavior must be explicitly enabled on a per-job basis via annotation:
+
+```yaml
+metadata:
+  annotations:
+    kueue.x-k8s.io/elastic-job: "true"
+```
+
+## Limitations
+
+* Currently available only for `batch/v1.Job` workloads.
+* Slice reconciliation is in **Alpha** and may change in future releases.
```

  Removed.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T18:44:56Z

- **site/content/en/docs/concepts/elastic_workload.md:27** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T18:44:56Z
```diff
@@ -0,0 +1,72 @@
+---
+title: "Elastic Workloads"
+date: 2025-04-16
+weight: 4
+description: >
+  Workload types that support dynamic scaling.
+---
+
+{{< feature-state state="alpha" for_version="v0.13" >}}
+
+## Elastic Workloads (Workload Slices)
+
+Elastic Workloads extend the core `Workload` abstraction in Kueue to support **dynamic scaling** of admitted jobs, without requiring suspension or requeueing.
+This is achieved through the use of **Workload Slices**, which track partial allocations of a parent job's scale-up and scale-down operations.
+
+This feature enables more responsive and efficient scheduling for jobs that can adapt to changing cluster capacity, particularly in environments with fluctuating workloads or constrained resources.
+
+## Dynamic Scaling
+
+Traditionally, a `Workload` in Kueue represents a single atomic unit of admission.
+Once admitted, it reflects a fixed set of pod replicas and consumes a defined amount of quota. If a job needs to scale up or down, the `Workload` must be suspended, removed, or replaced entirely.
+
+While scaling **down** a workload is relatively straightforward and does not require additional capacity or a new workload slice, scaling **up** is more involved. It requires *additional capacity* that must be explicitly requested and *admitted* by Kueue through a new `Workload Slice`.
+
+## Use Cases
+
+* **Horizontal scaling of batch jobs**, without restarting or requeueing the entire job
```

  Updated.

### Review by [@ichekrygin](https://github.com/ichekrygin) (commented) — 2025-08-11T18:51:20Z

- **site/content/en/docs/concepts/_index.md:94** by [@ichekrygin](https://github.com/ichekrygin) — 2025-08-11T18:51:20Z
```diff
@@ -89,4 +89,9 @@ resources that are now required by the owning ClusterQueue.
 
 ### [Fair Sharing](/docs/concepts/fair_sharing)
 
-Mechanisms in Kueue to share quota between tenants fairly.
\ No newline at end of file
+Mechanisms in Kueue to share quota between tenants fairly.
+
+### [Elastic Workloads](/docs/concepts/elastic_workload)
```

  Added a special note. PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-08-12T06:53:32Z

/lgtm
/approve
/cherrypick website
Thank you 👍
