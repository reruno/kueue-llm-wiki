# PR #6553: [Doc]: Restructures Documentation

**Summary**: [Doc]: Restructures Documentation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6553

**Last updated**: 2025-09-16T03:10:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@JasminPradhan](https://github.com/JasminPradhan)
- **Branch**: `restructure` → `main`
- **Created**: 2025-08-12T07:18:37Z
- **Updated**: 2025-09-16T03:10:47Z
- **Merged**: 2025-09-15T07:22:10Z
- **Closed**: 2025-09-15T07:22:10Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@gabesaba](https://github.com/gabesaba), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 5   **+57 / -47**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation


#### What this PR does / why we need it:
- It restructures the documentation.
- Modifies the content following the above changes.
- Fixes the broken links after changes.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Restructures doc for #6394

#### Does this PR introduce a user-facing change?

<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-08-12T07:18:44Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b95e70371c3dbffd6185066237a8dc9fcf59839c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68c4f775f16b300008461c3e |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-6553--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-6553--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTY1NTMtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.xBL2bK7z_S5OaWDYlx4rEogxsahUTJlBYJDXTDA5BqQ)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-6553--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-12T07:18:48Z

Hi @JasminPradhan. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-08-12T07:39:12Z

cc @PBundyra would you like to give it a pass?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-12T23:13:50Z

/ok-to-test

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-27T21:19:23Z

@PBundyra Can you re-check the updated commits?

### Comment by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-09-11T15:08:33Z

@mimowo @PBundyra @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-15T07:00:44Z

/lgtm
/approve
Thank you, it looks much better!
/cherrypick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-09-15T07:00:47Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6553#issuecomment-3290749393):

>/lgtm
>/approve
>Thank you, it looks much better!
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-15T07:00:52Z

LGTM label has been added.  <details>Git tree hash: 0d17ebeedfce8ee3d9aa6369db482b12566ee6f9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-09-15T07:00:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6553#" title="Author self-approved">JasminPradhan</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6553#issuecomment-3290749393" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-09-15T07:22:48Z

@mimowo: #6553 failed to apply on top of branch "website":
```
Applying: Update: Restructure the Admission and AdmissionChecks
Applying: Remove admission-check-controller
Using index info to reconstruct a base tree...
M	site/content/en/docs/admission-check-controllers/provisioning.md
Falling back to patching base and 3-way merge...
CONFLICT (modify/delete): site/content/en/docs/admission-check-controllers/provisioning.md deleted in Remove admission-check-controller and modified in HEAD. Version HEAD of site/content/en/docs/admission-check-controllers/provisioning.md left in tree.
Removing site/content/en/docs/admission-check-controllers/_index.md
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config advice.mergeConflict false"
Patch failed at 0002 Remove admission-check-controller

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6553#issuecomment-3290749393):

>/lgtm
>/approve
>Thank you, it looks much better!
>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2025-09-15T07:26:20Z

@JasminPradhan could you please open a PR against the website branch which resolves the conflicts?

### Comment by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-09-16T03:10:47Z

@mimowo I have opened the [PR](https://github.com/kubernetes-sigs/kueue/pull/6847)

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-12T09:47:54Z

- **site/content/en/docs/concepts/admission.md:21** by [@PBundyra](https://github.com/PBundyra) — 2025-08-12T09:47:54Z
```diff
@@ -18,53 +18,31 @@ Kueue implements this through a two-phase admission cycle:
 1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
     Topology-Aware Scheduling is enabled.
 
-2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/admission-check-controllers/provisioning/), or are the pluggable
+2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or are the pluggable
```

  ```suggestion
  2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. Checks can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or plugins created by users.```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-08-12T09:48:14Z

- **site/content/en/docs/concepts/admission.md:45** by [@PBundyra](https://github.com/PBundyra) — 2025-08-12T09:48:14Z
```diff
@@ -18,53 +18,31 @@ Kueue implements this through a two-phase admission cycle:
 1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
     Topology-Aware Scheduling is enabled.
 
-2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/admission-check-controllers/provisioning/), or are the pluggable
+2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or are the pluggable
 controllers that can perform validations such as policy checks, compliance, etc.
 The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
 
-## Provisioning AdmissionCheck 
+## [Provisioning AdmissionCheck ](docs/concepts/admission_check/provisioning_request)
 
-When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cluster-autoscaler environments.
+When neither AdmissionChecks nor [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were configured, Admissions were mainly based on quota checks. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig) addresses this in cluster-autoscaler environments through the following sequential checks:
+- First reserving ClusterQue resources (**Quota Reservation**),
+- Then confirming the physical capacity via ProvisioningRequest and Cluster Autoscaler(CA) (**Capacity Guarantee**)
 
-Kueue's enhanced admission requires two sequential checks:
-
-1. **Quota Reservation:** Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other Workloads from claiming it. This step verifies logical resource availability.
-2. **Capacity Guarantee:** This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) (CA) to verify physical resource availability. 
-    - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
-    - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
-      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
-      - `Provisioned=false`: Provisioning in progress
-      - `Failed=true`:  CA couldn't provision the capacity
-      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it  
-      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
-  
-    These conditions only affect non-admitted Workloads. Once admitted, they are ignored.
-
-
-Let's understand this with a real-world usage - GPU Workload:
-
-Scenario: *AI training job requiring 16 GPUs :*
-
-- **Step 1** *(Quota Reservation)*: ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
-
-- **Step 2** *(Admission Check)*: Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
-  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
-
-  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload.
-
-Outcome:
-*Job starts immediately with all 16 GPUs available.*
 
 ## Failure Handling:
 
 - For temporary issues (e.g., cloud capacity shortages):
   - The system **releases** the reserved quota immediately.
   - It **requeues** the workload.
-  - It triggers exponential backoff retries via [`retryStrategy`](docs/admission-check-controllers/provisioning/#retry-strategy) in ProvisioningRequestConfig.
+  - It triggers exponential backoff retries via [`retryStrategy`](/docs/concepts/admission_check/provisioning_request/#retry-strategy) in ProvisioningRequestConfig.
   - Kueue creates new `ProvisioningRequest` with `-attempt<N>` suffix each retry.
 
 - For permanent failures:
   - The system marks the AdmissionCheck as `Rejected`
   - It evicts the Workload.
   - It releases the reserved quota. 
   - It deactivates the Workload and to requeue it, the user needs to set the `.status.active` field to `true`.
+
```

  I believe this should also be moved to the ProvisioningRequest page

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-08-29T14:19:43Z

- **site/content/en/docs/concepts/admission.md:45** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-08-29T14:19:43Z
```diff
@@ -18,53 +18,31 @@ Kueue implements this through a two-phase admission cycle:
 1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
     Topology-Aware Scheduling is enabled.
 
-2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/admission-check-controllers/provisioning/), or are the pluggable
+2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or are the pluggable
 controllers that can perform validations such as policy checks, compliance, etc.
 The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
 
-## Provisioning AdmissionCheck 
+## [Provisioning AdmissionCheck ](docs/concepts/admission_check/provisioning_request)
 
-When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cluster-autoscaler environments.
+When neither AdmissionChecks nor [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were configured, Admissions were mainly based on quota checks. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig) addresses this in cluster-autoscaler environments through the following sequential checks:
+- First reserving ClusterQue resources (**Quota Reservation**),
+- Then confirming the physical capacity via ProvisioningRequest and Cluster Autoscaler(CA) (**Capacity Guarantee**)
 
-Kueue's enhanced admission requires two sequential checks:
-
-1. **Quota Reservation:** Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other Workloads from claiming it. This step verifies logical resource availability.
-2. **Capacity Guarantee:** This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) (CA) to verify physical resource availability. 
-    - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
-    - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
-      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
-      - `Provisioned=false`: Provisioning in progress
-      - `Failed=true`:  CA couldn't provision the capacity
-      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it  
-      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
-  
-    These conditions only affect non-admitted Workloads. Once admitted, they are ignored.
-
-
-Let's understand this with a real-world usage - GPU Workload:
-
-Scenario: *AI training job requiring 16 GPUs :*
-
-- **Step 1** *(Quota Reservation)*: ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
-
-- **Step 2** *(Admission Check)*: Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
-  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
-
-  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload.
-
-Outcome:
-*Job starts immediately with all 16 GPUs available.*
 
 ## Failure Handling:
 
 - For temporary issues (e.g., cloud capacity shortages):
   - The system **releases** the reserved quota immediately.
   - It **requeues** the workload.
-  - It triggers exponential backoff retries via [`retryStrategy`](docs/admission-check-controllers/provisioning/#retry-strategy) in ProvisioningRequestConfig.
+  - It triggers exponential backoff retries via [`retryStrategy`](/docs/concepts/admission_check/provisioning_request/#retry-strategy) in ProvisioningRequestConfig.
   - Kueue creates new `ProvisioningRequest` with `-attempt<N>` suffix each retry.
 
 - For permanent failures:
   - The system marks the AdmissionCheck as `Rejected`
   - It evicts the Workload.
   - It releases the reserved quota. 
   - It deactivates the Workload and to requeue it, the user needs to set the `.status.active` field to `true`.
+
```

  @PBundyra, I have updated the link and have added reference for the ProvisioningRequestConfig. [Retry strategy and exponential backoff](https://deploy-preview-6553--kubernetes-sigs-kueue.netlify.app/docs/concepts/admission_check/provisioning_request/#retry-strategy) is already present in the Provisioning Request page. This line is, I think, is important here to understand Failure handling.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2025-09-02T12:31:56Z

- **site/content/en/docs/concepts/admission.md:28** by [@PBundyra](https://github.com/PBundyra) — 2025-09-02T12:23:29Z
```diff
@@ -18,53 +18,31 @@ Kueue implements this through a two-phase admission cycle:
 1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
     Topology-Aware Scheduling is enabled.
 
-2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/admission-check-controllers/provisioning/), or are the pluggable
-controllers that can perform validations such as policy checks, compliance, etc.
-The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
-
-## Provisioning AdmissionCheck 
-
-When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cluster-autoscaler environments.
-
-Kueue's enhanced admission requires two sequential checks:
-
-1. **Quota Reservation:** Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other Workloads from claiming it. This step verifies logical resource availability.
-2. **Capacity Guarantee:** This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) (CA) to verify physical resource availability. 
-    - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
-    - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
-      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
-      - `Provisioned=false`: Provisioning in progress
-      - `Failed=true`:  CA couldn't provision the capacity
-      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it  
-      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
-  
-    These conditions only affect non-admitted Workloads. Once admitted, they are ignored.
-
+2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. Checks can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or plugins created by users.
 
-Let's understand this with a real-world usage - GPU Workload:
-
-Scenario: *AI training job requiring 16 GPUs :*
-
-- **Step 1** *(Quota Reservation)*: ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
 
-- **Step 2** *(Admission Check)*: Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
-  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+## [Provisioning AdmissionCheck ](docs/concepts/admission_check/provisioning_request)
 
-  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload.
+When neither AdmissionChecks nor [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were configured, Admissions were mainly based on quota checks. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig) addresses this in cluster-autoscaler environments through the following sequential checks:
+- First reserving ClusterQue resources (**Quota Reservation**),
```

  ```suggestion
  - First reserving ClusterQueue resources (**Quota Reservation**),
  ```

- **site/content/en/docs/concepts/admission_check/provisioning_request.md:9** by [@PBundyra](https://github.com/PBundyra) — 2025-09-02T12:24:57Z
```diff
@@ -1,11 +1,46 @@
 ---
-title: "Provisioning Admission Check Controller"
-date: 2023-10-23
-weight: 1
+title: "Provisioning AdmissionCheck"
+date: 
+weight: 
 description: >
   An admission check controller providing kueue integration with cluster autoscaler.
 ---
 
+When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioning-admissioncheck-controller) addresses this in cluster-autoscaler environments.
```

  ```suggestion
  When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it didn't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioning-admissioncheck-controller) addresses this in cluster-autoscaler environments.
  ```

- **site/content/en/docs/concepts/admission.md:21** by [@PBundyra](https://github.com/PBundyra) — 2025-09-02T12:25:56Z
```diff
@@ -18,53 +18,31 @@ Kueue implements this through a two-phase admission cycle:
 1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
     Topology-Aware Scheduling is enabled.
 
-2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/admission-check-controllers/provisioning/), or are the pluggable
-controllers that can perform validations such as policy checks, compliance, etc.
-The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
-
-## Provisioning AdmissionCheck 
-
-When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cluster-autoscaler environments.
-
-Kueue's enhanced admission requires two sequential checks:
-
-1. **Quota Reservation:** Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other Workloads from claiming it. This step verifies logical resource availability.
-2. **Capacity Guarantee:** This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) (CA) to verify physical resource availability. 
-    - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
-    - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
-      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
-      - `Provisioned=false`: Provisioning in progress
-      - `Failed=true`:  CA couldn't provision the capacity
-      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it  
-      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
-  
-    These conditions only affect non-admitted Workloads. Once admitted, they are ignored.
-
+2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. Checks can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or plugins created by users.
```

  ```suggestion
  2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. Checks can be either built-in such as [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or user-created plugins.
  ```

- **site/content/en/docs/concepts/admission.md:27** by [@PBundyra](https://github.com/PBundyra) — 2025-09-02T12:26:40Z
```diff
@@ -18,53 +18,31 @@ Kueue implements this through a two-phase admission cycle:
 1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
     Topology-Aware Scheduling is enabled.
 
-2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/admission-check-controllers/provisioning/), or are the pluggable
-controllers that can perform validations such as policy checks, compliance, etc.
-The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
-
-## Provisioning AdmissionCheck 
-
-When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cluster-autoscaler environments.
-
-Kueue's enhanced admission requires two sequential checks:
-
-1. **Quota Reservation:** Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other Workloads from claiming it. This step verifies logical resource availability.
-2. **Capacity Guarantee:** This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) (CA) to verify physical resource availability. 
-    - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
-    - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
-      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
-      - `Provisioned=false`: Provisioning in progress
-      - `Failed=true`:  CA couldn't provision the capacity
-      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it  
-      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
-  
-    These conditions only affect non-admitted Workloads. Once admitted, they are ignored.
-
+2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. Checks can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or plugins created by users.
 
-Let's understand this with a real-world usage - GPU Workload:
-
-Scenario: *AI training job requiring 16 GPUs :*
-
-- **Step 1** *(Quota Reservation)*: ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
 
-- **Step 2** *(Admission Check)*: Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
-  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+## [Provisioning AdmissionCheck ](docs/concepts/admission_check/provisioning_request)
 
-  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload.
+When neither AdmissionChecks nor [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were configured, Admissions were mainly based on quota checks. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig) addresses this in cluster-autoscaler environments through the following sequential checks:
```

  ```suggestion
  When neither AdmissionChecks nor [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were configured, Admissions were mainly based on quota checks. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig) addresses this in cluster-autoscaler environments through the following sequence of checks:
  ```

- **site/content/en/docs/concepts/admission_check/provisioning_request.md:35** by [@PBundyra](https://github.com/PBundyra) — 2025-09-02T12:27:43Z
```diff
@@ -1,11 +1,46 @@
 ---
-title: "Provisioning Admission Check Controller"
-date: 2023-10-23
-weight: 1
+title: "Provisioning AdmissionCheck"
+date: 
+weight: 
 description: >
   An admission check controller providing kueue integration with cluster autoscaler.
 ---
 
+When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioning-admissioncheck-controller) addresses this in cluster-autoscaler environments.
+
+Kueue's enhanced admission requires two sequential checks:
+
+1. **Quota Reservation:** Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other Workloads from claiming it. This step verifies logical resource availability.
+2. **Capacity Guarantee:** This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) (CA) to verify physical resource availability. 
+    - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/concepts/admission_check/provisioning_request/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig) settings, and setting owner reference to Workload.
+    - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
+      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
+      - `Provisioned=false`: Provisioning in progress
+      - `Failed=true`:  CA couldn't provision the capacity
+      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it  
+      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
+  
+    These conditions only affect non-admitted Workloads. Once admitted, they are ignored.
+
+
+Let's understand this with a real-world usage - GPU Workload:
+
+Scenario: *AI training job requiring 16 GPUs :*
+
+- **Step 1** *(Quota Reservation)*: ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+
+- **Step 2** *(Admission Check)*: Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
+  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+
+  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload.
```

  ```suggestion
    - Kueue sees the `Provisioned=true` marks the AdmissionCheck `Ready` and admits workload.
  ```

- **site/content/en/docs/concepts/admission_check/_index.md:4** by [@PBundyra](https://github.com/PBundyra) — 2025-09-02T12:31:52Z
```diff
@@ -1,9 +1,10 @@
 ---
 title: "Admission Check"
-date: 2024-06-13
-weight: 6
```

  Why did we delete the date and weight?

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-09-02T21:04:21Z

- **site/content/en/docs/concepts/admission_check/_index.md:4** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-09-02T21:04:21Z
```diff
@@ -1,9 +1,10 @@
 ---
 title: "Admission Check"
-date: 2024-06-13
-weight: 6
```

  let me check...maybe by mistake

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T16:36:39Z

- **site/content/en/docs/concepts/admission.md:46** by [@mimowo](https://github.com/mimowo) — 2025-09-11T16:36:39Z
```diff
@@ -18,53 +18,31 @@ Kueue implements this through a two-phase admission cycle:
 1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
     Topology-Aware Scheduling is enabled.
 
-2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/admission-check-controllers/provisioning/), or are the pluggable
-controllers that can perform validations such as policy checks, compliance, etc.
-The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
-
-## Provisioning AdmissionCheck 
-
-When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cluster-autoscaler environments.
-
-Kueue's enhanced admission requires two sequential checks:
-
-1. **Quota Reservation:** Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other Workloads from claiming it. This step verifies logical resource availability.
-2. **Capacity Guarantee:** This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) (CA) to verify physical resource availability. 
-    - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
-    - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
-      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
-      - `Provisioned=false`: Provisioning in progress
-      - `Failed=true`:  CA couldn't provision the capacity
-      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it  
-      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
-  
-    These conditions only affect non-admitted Workloads. Once admitted, they are ignored.
-
+2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. Checks can be either built-in such as [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or user-created plugins.
 
-Let's understand this with a real-world usage - GPU Workload:
-
-Scenario: *AI training job requiring 16 GPUs :*
-
-- **Step 1** *(Quota Reservation)*: ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
 
-- **Step 2** *(Admission Check)*: Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
-  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+## [Provisioning AdmissionCheck ](docs/concepts/admission_check/provisioning_request)
 
-  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload.
+When neither AdmissionChecks nor [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were configured, Admissions were mainly based on quota checks. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig) addresses this in cluster-autoscaler environments through the following sequence of checks:
+- First reserving ClusterQueue resources (**Quota Reservation**),
+- Then confirming the physical capacity via ProvisioningRequest and Cluster Autoscaler(CA) (**Capacity Guarantee**)
 
-Outcome:
-*Job starts immediately with all 16 GPUs available.*
 
 ## Failure Handling:
 
 - For temporary issues (e.g., cloud capacity shortages):
   - The system **releases** the reserved quota immediately.
   - It **requeues** the workload.
-  - It triggers exponential backoff retries via [`retryStrategy`](docs/admission-check-controllers/provisioning/#retry-strategy) in ProvisioningRequestConfig.
+  - It triggers exponential backoff retries via [`retryStrategy`](/docs/concepts/admission_check/provisioning_request/#retry-strategy) in [ProvisioningRequestConfig](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig).
   - Kueue creates new `ProvisioningRequest` with `-attempt<N>` suffix each retry.
 
 - For permanent failures:
   - The system marks the AdmissionCheck as `Rejected`
   - It evicts the Workload.
   - It releases the reserved quota. 
   - It deactivates the Workload and to requeue it, the user needs to set the `.status.active` field to `true`.
+
+  ## What's Next?
```

  ```suggestion
  ## What's Next?
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T16:37:16Z

- **site/content/en/docs/concepts/admission_check/provisioning_request.md:2** by [@mimowo](https://github.com/mimowo) — 2025-09-11T16:37:16Z
```diff
@@ -1,11 +1,46 @@
 ---
-title: "Provisioning Admission Check Controller"
-date: 2023-10-23
-weight: 1
+title: "Provisioning AdmissionCheck"
```

  ```suggestion
  title: "ProvisioningRequest"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T16:39:46Z

- **site/content/en/docs/concepts/admission.md:29** by [@mimowo](https://github.com/mimowo) — 2025-09-11T16:39:46Z
```diff
@@ -18,53 +18,31 @@ Kueue implements this through a two-phase admission cycle:
 1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
     Topology-Aware Scheduling is enabled.
 
-2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/admission-check-controllers/provisioning/), or are the pluggable
-controllers that can perform validations such as policy checks, compliance, etc.
-The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
-
-## Provisioning AdmissionCheck 
-
-When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cluster-autoscaler environments.
-
-Kueue's enhanced admission requires two sequential checks:
-
-1. **Quota Reservation:** Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other Workloads from claiming it. This step verifies logical resource availability.
-2. **Capacity Guarantee:** This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) (CA) to verify physical resource availability. 
-    - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
-    - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
-      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
-      - `Provisioned=false`: Provisioning in progress
-      - `Failed=true`:  CA couldn't provision the capacity
-      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it  
-      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
-  
-    These conditions only affect non-admitted Workloads. Once admitted, they are ignored.
-
+2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. Checks can be either built-in such as [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or user-created plugins.
 
-Let's understand this with a real-world usage - GPU Workload:
-
-Scenario: *AI training job requiring 16 GPUs :*
-
-- **Step 1** *(Quota Reservation)*: ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
 
-- **Step 2** *(Admission Check)*: Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
-  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+## [Provisioning AdmissionCheck ](docs/concepts/admission_check/provisioning_request)
 
-  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload.
+When neither AdmissionChecks nor [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were configured, Admissions were mainly based on quota checks. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig) addresses this in cluster-autoscaler environments through the following sequence of checks:
+- First reserving ClusterQueue resources (**Quota Reservation**),
+- Then confirming the physical capacity via ProvisioningRequest and Cluster Autoscaler(CA) (**Capacity Guarantee**)
```

  I suggest to drop this section entirely now. The built-in admission checks are already referenced in the point (2.) just above, so it does not seem to bring much value.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T16:40:59Z

- **site/content/en/docs/concepts/admission_check/multikueue.md:6** by [@mimowo](https://github.com/mimowo) — 2025-09-11T16:40:59Z
```diff
@@ -0,0 +1,9 @@
+---
+title: "Multikueue"
+date: 
+weight: 
+description: >
+  Multi-cluster, quota-governed job scheduling.
```

  ```suggestion
    A built-in admission check providing Kueue multi-cluster Job dispatching.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T16:41:30Z

- **site/content/en/docs/concepts/admission_check/provisioning_request.md:6** by [@mimowo](https://github.com/mimowo) — 2025-09-11T16:41:30Z
```diff
@@ -1,11 +1,46 @@
 ---
-title: "Provisioning Admission Check Controller"
-date: 2023-10-23
-weight: 1
+title: "Provisioning AdmissionCheck"
+date: 
+weight: 
 description: >
   An admission check controller providing kueue integration with cluster autoscaler.
```

  ```suggestion
    A built-in admission check providing Kueue integration with cluster-autoscaler.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T16:48:00Z

- **site/content/en/docs/concepts/admission_check/multikueue.md:9** by [@mimowo](https://github.com/mimowo) — 2025-09-11T16:47:59Z
```diff
@@ -0,0 +1,9 @@
+---
+title: "Multikueue"
+date: 
+weight: 
+description: >
+  Multi-cluster, quota-governed job scheduling.
+---
+
+MultiKueue is a component of Kueue that enables distributed, multi-cluster job scheduling by federating Kueue instances across Kubernetes clusters.
```

  ```suggestion
  [MultiKueue](link to the MK concept) [AdmissionCheck](link to the admission check page) is the central 
  component responsible for admission of workloads created on the management cluster, based on their
  admission of the workloads on the selected worker cluster.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2025-09-11T16:48:22Z

- **site/content/en/docs/concepts/admission_check/multikueue.md:9** by [@mimowo](https://github.com/mimowo) — 2025-09-11T16:48:22Z
```diff
@@ -0,0 +1,9 @@
+---
+title: "Multikueue"
+date: 
+weight: 
+description: >
+  Multi-cluster, quota-governed job scheduling.
+---
+
+MultiKueue is a component of Kueue that enables distributed, multi-cluster job scheduling by federating Kueue instances across Kubernetes clusters.
```

  Ultimately we want to describe how the AC works, but maybe that is enough as a starter

### Review by [@JasminPradhan](https://github.com/JasminPradhan) (commented) — 2025-09-13T04:13:23Z

- **site/content/en/docs/concepts/admission.md:29** by [@JasminPradhan](https://github.com/JasminPradhan) — 2025-09-13T04:13:23Z
```diff
@@ -18,53 +18,31 @@ Kueue implements this through a two-phase admission cycle:
 1. **Quota Reservation:** When a user submits a Workload, it enters a LocalQueue first. This LocalQueue points to a ClusterQueue which is responsible for managing the available resources. The Kueue checks if the targeted ClusterQueue's available quota and resource flavors can accomodate requested resources (CPU, memory, GPUs, etc.). If the quota is available, the Kueue reserves resources for this Workload and prevents other Workloads from using the same resources. This phase also includes checking the availability of physical resources when 
     Topology-Aware Scheduling is enabled.
 
-2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. can be either built-in like [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/admission-check-controllers/provisioning/), or are the pluggable
-controllers that can perform validations such as policy checks, compliance, etc.
-The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
-
-## Provisioning AdmissionCheck 
-
-When AdmissionChecks or [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were not configured, Admissions were mainly based on quota checks - if sufficient quota existed, Kueue admitted the Workload. While quota reservation confirmed logical resource availability, it did't guarantee that physical resources existed to schedule all Pods successfully. The [ProvisioningRequest AdmissionCheck](/docs/admission-check-controllers/provisioning/) addresses this in cluster-autoscaler environments.
-
-Kueue's enhanced admission requires two sequential checks:
-
-1. **Quota Reservation:** Kueue validates the resource requests against ClusterQueue's available quota and resource flavors, reserves the required resources if available and locks the quota to prevent other Workloads from claiming it. This step verifies logical resource availability.
-2. **Capacity Guarantee:** This step uses ProvisioningRequest and [Cluster Autoscaler](https://github.com/kubernetes/autoscaler/tree/master/cluster-autoscaler) (CA) to verify physical resource availability. 
-    - The Kueue controller creates a ProvisioningRequest object by attaching the Workload's PodTemplates(optionally merged via [PodSetMergePolicy](/docs/admission-check-controllers/provisioning/#podset-merge-policy)) , applying [ProvisioningRequestConfig](/docs/admission-check-controllers/provisioning/#provisioningrequest-configuration) settings, and setting owner reference to Workload.
-    - Cluster Autoscaler receives ProvisioningRequest, checks actual cluster capacity, triggers scaling if needed and updates ProvisioningRequest status with this possible states: 
-      - `Provisioned=true`: CA provisioned the capacity and it's ready to use
-      - `Provisioned=false`: Provisioning in progress
-      - `Failed=true`:  CA couldn't provision the capacity
-      - `BookingExpired=true`: CA stopped booking the capacity, it will scale down if there are no Pods running on it  
-      - `CapacityRevoked=true`: CA revokes the capacity, if a Workload is running on it, it will be evicted
-  
-    These conditions only affect non-admitted Workloads. Once admitted, they are ignored.
-
+2. **Admission Checks:** Await for [AdmissionChecks](/docs/concepts/admission_check) configured in the ClusterQueue. Checks can be either built-in such as [MultiKueue](/docs/concepts/multikueue/) or [ProvisioningRequest](/docs/concepts/admission_check/provisioning_request/), or user-created plugins.
 
-Let's understand this with a real-world usage - GPU Workload:
-
-Scenario: *AI training job requiring 16 GPUs :*
-
-- **Step 1** *(Quota Reservation)*: ClusterQueue has 32 GPU quota available. Kueue reserves 16 GPUs from this quota.
+The Workload is admitted once all [AdmissionCheckStates](/docs/concepts/admission_check/#admissioncheckstates) are in the `Ready` state.
 
-- **Step 2** *(Admission Check)*: Kueue creates a ProvisioningRequest requesting for 16 GPUs. 
-  - Cluster Autoscaler checks cloud provider GPU inventory and initiates scaling of 4x GPU nodes (4 GPUs each). It sets `Provisioned=true` when nodes are ready.
+## [Provisioning AdmissionCheck ](docs/concepts/admission_check/provisioning_request)
 
-  - Kueue sees the `Provisioned=true` proceeds to mark the AdmissionCheck `Ready` and admits workload.
+When neither AdmissionChecks nor [TopologyAwareScheduling](docs/concepts/topology_aware_scheduling/) were configured, Admissions were mainly based on quota checks. The [ProvisioningRequest AdmissionCheck](/docs/concepts/admission_check/provisioning_request/#provisioningrequestconfig) addresses this in cluster-autoscaler environments through the following sequence of checks:
+- First reserving ClusterQueue resources (**Quota Reservation**),
+- Then confirming the physical capacity via ProvisioningRequest and Cluster Autoscaler(CA) (**Capacity Guarantee**)
```

  Got it!
