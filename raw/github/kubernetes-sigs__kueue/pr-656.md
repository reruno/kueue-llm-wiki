# PR #656: Update release notes for 0.3

**Summary**: Update release notes for 0.3

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/656

**Last updated**: 2023-03-23T02:14:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `v0.3-notes` → `main`
- **Created**: 2023-03-20T19:48:01Z
- **Updated**: 2023-03-23T02:14:23Z
- **Merged**: 2023-03-23T02:14:23Z
- **Closed**: 2023-03-23T02:14:23Z
- **Labels**: `lgtm`, `tide/merge-method-squash`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@kerthcet](https://github.com/kerthcet), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: _none_
- **Changed files**: 3   **+52 / -9**

## Description

#### What type of PR is this?

/kind documentation

#### What this PR does / why we need it:

Updates release notes from the PRs tagged as `kind/feature` or `kind/bug` that are user-facing.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

Let me know if I missed a PR.

In the future we should be more diligent adding release notes (I'm guilty too). And we should probably add milestones too.

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-20T19:48:06Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/656#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-20T19:48:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9402429e9e69d21de7aac3a5e4f7fc23aaec50bd |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/641b6c904c4a7a0008a85287 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-20T19:48:49Z

cc @kannon92 @kerthcet @tenzen-y @mimowo as people who worked on lots of PRs during this release

### Comment by [@kannon92](https://github.com/kannon92) — 2023-03-20T19:54:13Z

> cc @kannon92 @kerthcet @tenzen-y @mimowo as people who worked on lots of PRs during this release

Congratulations! Sounds like a lot of good things in this release!

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-20T19:57:48Z

Congrats!

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-22T21:01:18Z

/label tide/merge-method-squash

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-22T21:22:19Z

Thanks!
/lgtm
/hold for @kerthcet

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-03-23T02:12:33Z

/lgtm
/hold cancel

Thanks @alculquicondor

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-20T20:06:17Z

Thank you for updating the CHANGELOG.
I left a comment for a nit.

- **CHANGELOG/CHANGELOG-0.3.md:18** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-20T20:01:18Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
```

  ```suggestion
      - Refactor `quota.min` and `quota.max` into `nominalQuota` and `borrowingLimit`.
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-03-20T20:10:31Z

- **CHANGELOG/CHANGELOG-0.3.md:52** by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-20T20:10:30Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
+    - Swap hieararchy between `resources` and `flavors`.
+    - Group flavors and resources into `spec.resourceGroups` to make
+      co-dependent resources explicit.
+    - Move `admission` from `spec` to `status`.
+    - Add `conditions` field to `status`.
+  - `LocalQueue`:
+    - Add `admitted` field in `status`.
+    - Add `conditions` field to `status`.
+  - `Workload`:
+    - Add `metadata` to `podSet` templates.
+  - `ResourceFlavor`:
+    - Introduce `spec` to hold all fields.
+    - Rename `labels` to `nodeLabels`.
+    - Rename `taints` to `nodeTaints`.
+- Obtain queue names from label `kueue.x-k8s.io/queue-name`. The annotation with
+  the same name is still supported, but it's now deprecated.
+- Multiplatform support for `linux/amd64` and `linux/arm64`.
+- Validating webhook for `batch/v1.Job` validates kueue-specific labels and
+  annotations.
+- Use of Server-Side-Apply for updating conditions.
+- Sequential admission of jobs https://kueue.sigs.k8s.io/docs/tasks/setup_sequential_admission/
+- Preemption within ClusterQueue and cohort https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#preemption
+- Library for integrating job-like CRDs (controller and webhooks) https://sigs.k8s.io/kueue/pkg/controller/jobframework
+
+
+## Production Readiness
+
+- E2E tests for kubernetes 1.24, 1.25 1.26 on Kind
+- Improve readability and code location in logging #14
+- Optimized configuration for small size clusters with higher API QPS and number
+  of workers.
+- Reproducible load tests https://sigs.k8s.io/kueue/test/performance
+- Documentation website https://kueue.sigs.k8s.io/docs/
 
 ### Bug fixes
```

  I'm on the fence about whether we should add #565 to this section.
  wdyt?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-03-21T06:16:14Z

- **CHANGELOG/CHANGELOG-0.3.md:41** by [@kerthcet](https://github.com/kerthcet) — 2023-03-21T06:04:37Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
+    - Swap hieararchy between `resources` and `flavors`.
+    - Group flavors and resources into `spec.resourceGroups` to make
+      co-dependent resources explicit.
+    - Move `admission` from `spec` to `status`.
+    - Add `conditions` field to `status`.
+  - `LocalQueue`:
+    - Add `admitted` field in `status`.
+    - Add `conditions` field to `status`.
+  - `Workload`:
+    - Add `metadata` to `podSet` templates.
+  - `ResourceFlavor`:
+    - Introduce `spec` to hold all fields.
+    - Rename `labels` to `nodeLabels`.
+    - Rename `taints` to `nodeTaints`.
+- Obtain queue names from label `kueue.x-k8s.io/queue-name`. The annotation with
+  the same name is still supported, but it's now deprecated.
+- Multiplatform support for `linux/amd64` and `linux/arm64`.
+- Validating webhook for `batch/v1.Job` validates kueue-specific labels and
+  annotations.
+- Use of Server-Side-Apply for updating conditions.
+- Sequential admission of jobs https://kueue.sigs.k8s.io/docs/tasks/setup_sequential_admission/
+- Preemption within ClusterQueue and cohort https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#preemption
+- Library for integrating job-like CRDs (controller and webhooks) https://sigs.k8s.io/kueue/pkg/controller/jobframework
```

  limitRange https://github.com/kubernetes-sigs/kueue/issues/541 should be included.

- **CHANGELOG/CHANGELOG-0.3.md:38** by [@kerthcet](https://github.com/kerthcet) — 2023-03-21T06:06:08Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
+    - Swap hieararchy between `resources` and `flavors`.
+    - Group flavors and resources into `spec.resourceGroups` to make
+      co-dependent resources explicit.
+    - Move `admission` from `spec` to `status`.
+    - Add `conditions` field to `status`.
+  - `LocalQueue`:
+    - Add `admitted` field in `status`.
+    - Add `conditions` field to `status`.
+  - `Workload`:
+    - Add `metadata` to `podSet` templates.
+  - `ResourceFlavor`:
+    - Introduce `spec` to hold all fields.
+    - Rename `labels` to `nodeLabels`.
+    - Rename `taints` to `nodeTaints`.
+- Obtain queue names from label `kueue.x-k8s.io/queue-name`. The annotation with
+  the same name is still supported, but it's now deprecated.
+- Multiplatform support for `linux/amd64` and `linux/arm64`.
+- Validating webhook for `batch/v1.Job` validates kueue-specific labels and
+  annotations.
+- Use of Server-Side-Apply for updating conditions.
```

  I think we can remove it as it's not user-facing.

- **CHANGELOG/CHANGELOG-0.3.md:41** by [@kerthcet](https://github.com/kerthcet) — 2023-03-21T06:10:22Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
+    - Swap hieararchy between `resources` and `flavors`.
+    - Group flavors and resources into `spec.resourceGroups` to make
+      co-dependent resources explicit.
+    - Move `admission` from `spec` to `status`.
+    - Add `conditions` field to `status`.
+  - `LocalQueue`:
+    - Add `admitted` field in `status`.
+    - Add `conditions` field to `status`.
+  - `Workload`:
+    - Add `metadata` to `podSet` templates.
+  - `ResourceFlavor`:
+    - Introduce `spec` to hold all fields.
+    - Rename `labels` to `nodeLabels`.
+    - Rename `taints` to `nodeTaints`.
+- Obtain queue names from label `kueue.x-k8s.io/queue-name`. The annotation with
+  the same name is still supported, but it's now deprecated.
+- Multiplatform support for `linux/amd64` and `linux/arm64`.
+- Validating webhook for `batch/v1.Job` validates kueue-specific labels and
+  annotations.
+- Use of Server-Side-Apply for updating conditions.
+- Sequential admission of jobs https://kueue.sigs.k8s.io/docs/tasks/setup_sequential_admission/
+- Preemption within ClusterQueue and cohort https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#preemption
+- Library for integrating job-like CRDs (controller and webhooks) https://sigs.k8s.io/kueue/pkg/controller/jobframework
```

  Also support scheduler leader election for HA.

- **CHANGELOG/CHANGELOG-0.3.md:58** by [@kerthcet](https://github.com/kerthcet) — 2023-03-21T06:15:04Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
+    - Swap hieararchy between `resources` and `flavors`.
+    - Group flavors and resources into `spec.resourceGroups` to make
+      co-dependent resources explicit.
+    - Move `admission` from `spec` to `status`.
+    - Add `conditions` field to `status`.
+  - `LocalQueue`:
+    - Add `admitted` field in `status`.
+    - Add `conditions` field to `status`.
+  - `Workload`:
+    - Add `metadata` to `podSet` templates.
+  - `ResourceFlavor`:
+    - Introduce `spec` to hold all fields.
+    - Rename `labels` to `nodeLabels`.
+    - Rename `taints` to `nodeTaints`.
+- Obtain queue names from label `kueue.x-k8s.io/queue-name`. The annotation with
+  the same name is still supported, but it's now deprecated.
+- Multiplatform support for `linux/amd64` and `linux/arm64`.
+- Validating webhook for `batch/v1.Job` validates kueue-specific labels and
+  annotations.
+- Use of Server-Side-Apply for updating conditions.
+- Sequential admission of jobs https://kueue.sigs.k8s.io/docs/tasks/setup_sequential_admission/
+- Preemption within ClusterQueue and cohort https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#preemption
+- Library for integrating job-like CRDs (controller and webhooks) https://sigs.k8s.io/kueue/pkg/controller/jobframework
+
+
+## Production Readiness
+
+- E2E tests for kubernetes 1.24, 1.25 1.26 on Kind
+- Improve readability and code location in logging #14
+- Optimized configuration for small size clusters with higher API QPS and number
+  of workers.
+- Reproducible load tests https://sigs.k8s.io/kueue/test/performance
+- Documentation website https://kueue.sigs.k8s.io/docs/
 
 ### Bug fixes
 
-- Fix job controller ClusterRole for clusters that enable OwnerReferencesPermissionEnforcement admission control validation
+- Fix job controller ClusterRole for clusters that enable OwnerReferencesPermissionEnforcement admission control validation #392
+- Fix race condition when admission attempt and requeuing happen at the same time #427
+- Atomically release quota and requeue previously inadmissible workloads #512
+- Fix support for leader election #580
```

  Oh, you added here, maybe more like a feature?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-22T21:01:05Z

- **CHANGELOG/CHANGELOG-0.3.md:18** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-22T20:54:46Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
```

  Done

- **CHANGELOG/CHANGELOG-0.3.md:38** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-22T20:55:21Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
+    - Swap hieararchy between `resources` and `flavors`.
+    - Group flavors and resources into `spec.resourceGroups` to make
+      co-dependent resources explicit.
+    - Move `admission` from `spec` to `status`.
+    - Add `conditions` field to `status`.
+  - `LocalQueue`:
+    - Add `admitted` field in `status`.
+    - Add `conditions` field to `status`.
+  - `Workload`:
+    - Add `metadata` to `podSet` templates.
+  - `ResourceFlavor`:
+    - Introduce `spec` to hold all fields.
+    - Rename `labels` to `nodeLabels`.
+    - Rename `taints` to `nodeTaints`.
+- Obtain queue names from label `kueue.x-k8s.io/queue-name`. The annotation with
+  the same name is still supported, but it's now deprecated.
+- Multiplatform support for `linux/amd64` and `linux/arm64`.
+- Validating webhook for `batch/v1.Job` validates kueue-specific labels and
+  annotations.
+- Use of Server-Side-Apply for updating conditions.
```

  It should improve performance, but sure, we can remove it.

- **CHANGELOG/CHANGELOG-0.3.md:52** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-22T20:58:46Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
+    - Swap hieararchy between `resources` and `flavors`.
+    - Group flavors and resources into `spec.resourceGroups` to make
+      co-dependent resources explicit.
+    - Move `admission` from `spec` to `status`.
+    - Add `conditions` field to `status`.
+  - `LocalQueue`:
+    - Add `admitted` field in `status`.
+    - Add `conditions` field to `status`.
+  - `Workload`:
+    - Add `metadata` to `podSet` templates.
+  - `ResourceFlavor`:
+    - Introduce `spec` to hold all fields.
+    - Rename `labels` to `nodeLabels`.
+    - Rename `taints` to `nodeTaints`.
+- Obtain queue names from label `kueue.x-k8s.io/queue-name`. The annotation with
+  the same name is still supported, but it's now deprecated.
+- Multiplatform support for `linux/amd64` and `linux/arm64`.
+- Validating webhook for `batch/v1.Job` validates kueue-specific labels and
+  annotations.
+- Use of Server-Side-Apply for updating conditions.
+- Sequential admission of jobs https://kueue.sigs.k8s.io/docs/tasks/setup_sequential_admission/
+- Preemption within ClusterQueue and cohort https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#preemption
+- Library for integrating job-like CRDs (controller and webhooks) https://sigs.k8s.io/kueue/pkg/controller/jobframework
+
+
+## Production Readiness
+
+- E2E tests for kubernetes 1.24, 1.25 1.26 on Kind
+- Improve readability and code location in logging #14
+- Optimized configuration for small size clusters with higher API QPS and number
+  of workers.
+- Reproducible load tests https://sigs.k8s.io/kueue/test/performance
+- Documentation website https://kueue.sigs.k8s.io/docs/
 
 ### Bug fixes
```

  Absolutely

- **CHANGELOG/CHANGELOG-0.3.md:58** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-22T20:59:33Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
+    - Swap hieararchy between `resources` and `flavors`.
+    - Group flavors and resources into `spec.resourceGroups` to make
+      co-dependent resources explicit.
+    - Move `admission` from `spec` to `status`.
+    - Add `conditions` field to `status`.
+  - `LocalQueue`:
+    - Add `admitted` field in `status`.
+    - Add `conditions` field to `status`.
+  - `Workload`:
+    - Add `metadata` to `podSet` templates.
+  - `ResourceFlavor`:
+    - Introduce `spec` to hold all fields.
+    - Rename `labels` to `nodeLabels`.
+    - Rename `taints` to `nodeTaints`.
+- Obtain queue names from label `kueue.x-k8s.io/queue-name`. The annotation with
+  the same name is still supported, but it's now deprecated.
+- Multiplatform support for `linux/amd64` and `linux/arm64`.
+- Validating webhook for `batch/v1.Job` validates kueue-specific labels and
+  annotations.
+- Use of Server-Side-Apply for updating conditions.
+- Sequential admission of jobs https://kueue.sigs.k8s.io/docs/tasks/setup_sequential_admission/
+- Preemption within ClusterQueue and cohort https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#preemption
+- Library for integrating job-like CRDs (controller and webhooks) https://sigs.k8s.io/kueue/pkg/controller/jobframework
+
+
+## Production Readiness
+
+- E2E tests for kubernetes 1.24, 1.25 1.26 on Kind
+- Improve readability and code location in logging #14
+- Optimized configuration for small size clusters with higher API QPS and number
+  of workers.
+- Reproducible load tests https://sigs.k8s.io/kueue/test/performance
+- Documentation website https://kueue.sigs.k8s.io/docs/
 
 ### Bug fixes
 
-- Fix job controller ClusterRole for clusters that enable OwnerReferencesPermissionEnforcement admission control validation
+- Fix job controller ClusterRole for clusters that enable OwnerReferencesPermissionEnforcement admission control validation #392
+- Fix race condition when admission attempt and requeuing happen at the same time #427
+- Atomically release quota and requeue previously inadmissible workloads #512
+- Fix support for leader election #580
```

  In on the fence. It should have worked from the beginning because we are using controller-runtime, but we didn't add the scheduler to the manager.

- **CHANGELOG/CHANGELOG-0.3.md:41** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-22T21:00:40Z
```diff
@@ -4,12 +4,55 @@ Changes since `v0.2.1`:
 
 ### Features
 
-- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1alpha2`. `v1alpha1` is no longer supported.
-  `v1alpha2` includes the following changes:
-  - Add Namespace to propagate the namespace where kueue is deployed to the webhook certificate.
-  - Add InternalCertManagement with fields Enable, WebhookServiceName and WebhookSecretName.
-  - Remove EnableInternalCertManagement. Use InternalCertManagement.Enable instead.
+- Support for kubeflow's MPIJob (v2beta1)
+- Upgrade the `config.kueue.x-k8s.io` API version from `v1alpha1` to `v1beta1`. `v1alpha1` is no longer supported.
+  `v1beta1` includes the following changes:
+  - Add `namespace` to propagate the namespace where kueue is deployed to the webhook certificate.
+  - Add `internalCertManagement` with fields `enable`, `webhookServiceName` and `webhookSecretName`.
+  - Remove `enableInternalCertManagement`. Use `internalCertManagement.enable` instead.
+- Upgrade the `kueue.x-k8s.io` API version from `v1alpha2` to `v1beta1`.
+  `v1alpha2` is no longer supported.
+  `v1beta1` includes the following changes:
+  - `ClusterQueue`:
+    - Immutability of `spec.queueingStrategy`.
+    - Refactor `quota.min` and `quota.max` into `nominalQuota and `borrowingLimit`.
+    - Swap hieararchy between `resources` and `flavors`.
+    - Group flavors and resources into `spec.resourceGroups` to make
+      co-dependent resources explicit.
+    - Move `admission` from `spec` to `status`.
+    - Add `conditions` field to `status`.
+  - `LocalQueue`:
+    - Add `admitted` field in `status`.
+    - Add `conditions` field to `status`.
+  - `Workload`:
+    - Add `metadata` to `podSet` templates.
+  - `ResourceFlavor`:
+    - Introduce `spec` to hold all fields.
+    - Rename `labels` to `nodeLabels`.
+    - Rename `taints` to `nodeTaints`.
+- Obtain queue names from label `kueue.x-k8s.io/queue-name`. The annotation with
+  the same name is still supported, but it's now deprecated.
+- Multiplatform support for `linux/amd64` and `linux/arm64`.
+- Validating webhook for `batch/v1.Job` validates kueue-specific labels and
+  annotations.
+- Use of Server-Side-Apply for updating conditions.
+- Sequential admission of jobs https://kueue.sigs.k8s.io/docs/tasks/setup_sequential_admission/
+- Preemption within ClusterQueue and cohort https://kueue.sigs.k8s.io/docs/concepts/cluster_queue/#preemption
+- Library for integrating job-like CRDs (controller and webhooks) https://sigs.k8s.io/kueue/pkg/controller/jobframework
```

  Added LimitRange
