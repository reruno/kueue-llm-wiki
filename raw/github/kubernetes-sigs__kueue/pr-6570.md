# PR #6570: TAS: Filter out Node updates with LastHeartbeatTime changes

**Summary**: TAS: Filter out Node updates with LastHeartbeatTime changes

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6570

**Last updated**: 2025-08-21T13:08:25Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@utam0k](https://github.com/utam0k)
- **Branch**: `tas-filter-events` → `main`
- **Created**: 2025-08-13T12:51:59Z
- **Updated**: 2025-08-21T13:08:25Z
- **Merged**: 2025-08-21T13:07:09Z
- **Closed**: 2025-08-21T13:07:09Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@amy](https://github.com/amy), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@amy](https://github.com/amy), [@mimowo](https://github.com/mimowo), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+311 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind bug

#### What this PR does / why we need it:

This PR adds filtering for Node update events in the TAS (Topology-Aware Scheduling) controller to skip reconciliation when only `LastHeartbeatTime` is updated.

Previously, the TAS controller would trigger reconciliation for every Node update, including the periodic heartbeat updates.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Ref: #6551

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Fix a bug where new Workloads starve, caused by inadmissible workloads frequently requeueing due to unrelated Node LastHeartbeatTime update events.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-08-13T12:52:07Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 04fa579d897d5138527bc9023dba0d642e4dd2d2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68a70e6829125600088154b0 |

### Comment by [@amy](https://github.com/amy) — 2025-08-16T22:42:21Z

/assign

### Comment by [@amy](https://github.com/amy) — 2025-08-16T22:55:07Z

Added a nit here: https://github.com/kubernetes-sigs/kueue/pull/6570#discussion_r2280635993
Approving because its a nit. Will let approvers assess the nit if my assessment is wrong. Thank you!

/approve
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-16T22:55:13Z

LGTM label has been added.  <details>Git tree hash: 2f8a14316703c502dc7c05e70f3ab3e332300fbc</details>

### Comment by [@amy](https://github.com/amy) — 2025-08-18T14:17:37Z

/assign @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-19T23:30:22Z

/release-note-edit

```release-note
TAS: Fix a bug where new Workloads starve, caused by inadmissible workloads frequently requeueing due to unrelated Node LastHeartbeatTime update events.
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T12:06:47Z

@utam0k Remaining unresolved tasks are https://github.com/kubernetes-sigs/kueue/pull/6570#discussion_r2290839862 and https://github.com/kubernetes-sigs/kueue/pull/6570#discussion_r2290836587.
Otherwise, LGTM

### Comment by [@utam0k](https://github.com/utam0k) — 2025-08-21T12:18:16Z

> @utam0k Remaining unresolved tasks are [#6570 (comment)](https://github.com/kubernetes-sigs/kueue/pull/6570#discussion_r2290839862) and [#6570 (comment)](https://github.com/kubernetes-sigs/kueue/pull/6570#discussion_r2290836587). Otherwise, LGTM

Thanks for your quick review 🙏

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-21T12:22:46Z

LGTM label has been added.  <details>Git tree hash: 855c723ab1cd9737637a48024c0f32c0ed1a71b3</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-21T12:22:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6570#pullrequestreview-3126096820" title="Approved">amy</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6570#pullrequestreview-3140424141" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6570#" title="Author self-approved">utam0k</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T12:23:02Z

/cherry-pick release-0.12
/cherry-pick release-0.13

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-21T12:23:05Z

@tenzen-y: once the present PR merges, I will cherry-pick it on top of `release-0.12`, `release-0.13` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6570#issuecomment-3210381059):

>/cherry-pick release-0.12
>/cherry-pick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-21T13:07:48Z

@tenzen-y: new pull request created: #6636

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6570#issuecomment-3210381059):

>/cherry-pick release-0.12
>/cherry-pick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2025-08-21T13:08:24Z

@tenzen-y: new pull request created: #6637

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/6570#issuecomment-3210381059):

>/cherry-pick release-0.12
>/cherry-pick release-0.13


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@amy](https://github.com/amy) (commented) — 2025-08-16T22:53:27Z

- **pkg/controller/tas/resource_flavor.go:218** by [@amy](https://github.com/amy) — 2025-08-16T22:53:27Z
```diff
@@ -203,3 +211,19 @@ func nodeBelongsToFlavor(node *corev1.Node, nodeLabels map[string]string, levels
 	}
 	return true
 }
+
+// checkNodeSchedulingPropertiesChanged checks if the node update affects TAS scheduling.
+func checkNodeSchedulingPropertiesChanged(oldNode, newNode *corev1.Node) bool {
+	oldCopy := oldNode.DeepCopy()
+	newCopy := newNode.DeepCopy()
```

  nit: do we need to deepcopy the entire node? Wondering if there's a subset of the node that scheduling cares about.

### Review by [@amy](https://github.com/amy) (approved) — 2025-08-16T22:55:25Z

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-19T23:11:32Z

Thank you for working on this.

- **pkg/controller/tas/resource_flavor.go:138** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-19T23:05:36Z
```diff
@@ -109,6 +111,12 @@ func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workque
 	if !isOldNode || !isNewNode {
 		return
 	}
+
+	if !checkNodeSchedulingPropertiesChanged(oldNode, newNode) {
+		ctrl.LoggerFrom(ctx).V(5).Info("Skipping node update as it doesn't affect TAS scheduling", "node", newNode.Name)
+		return
+	}
```

  Could you replace this check with a more efficient way? (w/o DeepCopy)
  
  ```go
  var nodeSemantic = conversion.EqualitiesOrDir (nodeConditionEqual)
  
  func nodeConditionEqual(a, b corev1.NodeCondition) bool {
    return a.Type == b.Type && a.Status == b.Status && a.LastTransitionTime.Time.Equal(b.LastTransitionTime.Time) && a.Reason == b.Reason && a.Message == b.Message
  }
  ```
  
  ```suggestion
  	if !nodeSemantic.DeepEqual(oldNode, newNode) {
  		ctrl.LoggerFrom(ctx).V(5).Info("Skipping node update as new Node is semantically same as old Node", "node", newNode.Name)
  		return
  	}
  ```

- **pkg/controller/tas/resource_flavor_test.go:28** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-19T23:07:54Z
```diff
@@ -0,0 +1,210 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+func TestNodeSchedulingPropertiesChanged(t *testing.T) {
```

  ```suggestion
  func TestNodeHandler_Update(t *testing.T) {
  ```
  
  We want to implement UTs for exposed function to resuce Test maintainance costs. So, could you implement UTs for `Update` function?

- **pkg/controller/tas/resource_flavor_test.go:32** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-19T23:10:27Z
```diff
@@ -0,0 +1,210 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+func TestNodeSchedulingPropertiesChanged(t *testing.T) {
+	now := metav1.Now()
+	later := metav1.NewTime(now.Add(10 * time.Second))
+
+	baseNode := &corev1.Node{
```

  We should leverage NodeWrapper: https://github.com/kubernetes-sigs/kueue/blob/5106fb28c399ad05de55ca313db0238caa060c3a/pkg/util/testingjobs/node/wrappers.go

- **pkg/controller/tas/resource_flavor_test.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-19T23:11:15Z
```diff
@@ -0,0 +1,210 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+func TestNodeSchedulingPropertiesChanged(t *testing.T) {
+	now := metav1.Now()
+	later := metav1.NewTime(now.Add(10 * time.Second))
+
+	baseNode := &corev1.Node{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "test-node",
+			Labels: map[string]string{
+				"topology.kubernetes.io/zone": "zone-a",
+				"node-role":                   "worker",
+			},
+			Annotations: map[string]string{
+				"test-annotation": "value",
+			},
+		},
+		Spec: corev1.NodeSpec{
+			Unschedulable: false,
+			Taints: []corev1.Taint{
+				{
+					Key:    "test-taint",
+					Value:  "value",
+					Effect: corev1.TaintEffectNoSchedule,
+				},
+			},
+		},
+		Status: corev1.NodeStatus{
+			Allocatable: corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("8"),
+				corev1.ResourceMemory: resource.MustParse("32Gi"),
+			},
+			Conditions: []corev1.NodeCondition{
+				{
+					Type:               corev1.NodeReady,
+					Status:             corev1.ConditionTrue,
+					LastHeartbeatTime:  now,
+					LastTransitionTime: now,
+				},
+				{
+					Type:               corev1.NodeMemoryPressure,
+					Status:             corev1.ConditionFalse,
+					LastHeartbeatTime:  now,
+					LastTransitionTime: now,
+				},
+			},
+		},
+	}
+
+	testCases := []struct {
+		name        string
+		oldNode     *corev1.Node
+		newNode     *corev1.Node
+		wantChanged bool
+	}{
+		{
+			name:    "LastHeartbeatTime changed",
+			oldNode: baseNode.DeepCopy(),
+			newNode: func() *corev1.Node {
+				n := baseNode.DeepCopy()
+				n.Status.Conditions[0].LastHeartbeatTime = later
+				n.Status.Conditions[1].LastHeartbeatTime = later
+				return n
+			}(),
```

  Instead of anonymous functions, we can clone the base node and chain new parameters.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-19T23:32:48Z

- **pkg/controller/tas/resource_flavor_test.go:75** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-19T23:32:48Z
```diff
@@ -0,0 +1,210 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+func TestNodeSchedulingPropertiesChanged(t *testing.T) {
+	now := metav1.Now()
+	later := metav1.NewTime(now.Add(10 * time.Second))
+
+	baseNode := &corev1.Node{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "test-node",
+			Labels: map[string]string{
+				"topology.kubernetes.io/zone": "zone-a",
+				"node-role":                   "worker",
+			},
+			Annotations: map[string]string{
+				"test-annotation": "value",
+			},
+		},
+		Spec: corev1.NodeSpec{
+			Unschedulable: false,
+			Taints: []corev1.Taint{
+				{
+					Key:    "test-taint",
+					Value:  "value",
+					Effect: corev1.TaintEffectNoSchedule,
+				},
+			},
+		},
+		Status: corev1.NodeStatus{
+			Allocatable: corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("8"),
+				corev1.ResourceMemory: resource.MustParse("32Gi"),
+			},
+			Conditions: []corev1.NodeCondition{
+				{
+					Type:               corev1.NodeReady,
+					Status:             corev1.ConditionTrue,
+					LastHeartbeatTime:  now,
+					LastTransitionTime: now,
+				},
+				{
+					Type:               corev1.NodeMemoryPressure,
+					Status:             corev1.ConditionFalse,
+					LastHeartbeatTime:  now,
+					LastTransitionTime: now,
+				},
+			},
+		},
+	}
+
+	testCases := []struct {
```

  ```suggestion
  	testCases := map[string]struct {
  ```
  
  Additionally, we use a map instead of a slice so that we can avoid executing order-dependent tests.

### Review by [@utam0k](https://github.com/utam0k) (commented) — 2025-08-20T12:41:26Z

- **pkg/controller/tas/resource_flavor.go:138** by [@utam0k](https://github.com/utam0k) — 2025-08-20T12:41:26Z
```diff
@@ -109,6 +111,12 @@ func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workque
 	if !isOldNode || !isNewNode {
 		return
 	}
+
+	if !checkNodeSchedulingPropertiesChanged(oldNode, newNode) {
+		ctrl.LoggerFrom(ctx).V(5).Info("Skipping node update as it doesn't affect TAS scheduling", "node", newNode.Name)
+		return
+	}
```

  Yes, but I was worried that we might forget to address something if a new field is added to a Node. However, it seems like people prefer to avoid DeepCopy, so I'll go with that.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-20T12:54:48Z

- **pkg/controller/tas/resource_flavor.go:138** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-20T12:54:48Z
```diff
@@ -109,6 +111,12 @@ func (h *nodeHandler) Update(ctx context.Context, e event.UpdateEvent, q workque
 	if !isOldNode || !isNewNode {
 		return
 	}
+
+	if !checkNodeSchedulingPropertiesChanged(oldNode, newNode) {
+		ctrl.LoggerFrom(ctx).V(5).Info("Skipping node update as it doesn't affect TAS scheduling", "node", newNode.Name)
+		return
+	}
```

  That's fair. In that case, we could use the following to mitigate risk and avoid high-computation deepcopy.
  (The following DeepCopy will be executed only for NodeCondition).
  
  ```go
  var nodeSemantic = conversion.EqualitiesOrDir(nodeConditionEqual)
  
  func nodeConditionEqual(a, b corev1.NodeCondition) bool {
    aCopy, bCopy := a.DeepCopy(), b.DeepCopy()
    aCopy.LastHeartbeatTime, bCopy.LastHeartbeatTime = metav1.Time{}, metav1.Time{}
    return equality.Semantic.DeepEqual(aCopy, bCopy)
  }
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-21T03:47:07Z

- **pkg/controller/tas/resource_flavor.go:55** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T03:16:08Z
```diff
@@ -43,6 +47,34 @@ import (
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
+var nodeSemantic = conversion.EqualitiesOrDie(
+	nodeConditionEqual,
+	// Handle metav1.Time comparison to avoid panic on unexported fields
+	func(a, b metav1.Time) bool {
+		return a.Time.Equal(b.Time)
+	},
+	// Handle *metav1.Time pointer comparison (e.g., Taint.TimeAdded)
+	func(a, b *metav1.Time) bool {
+		if a == nil && b == nil {
+			return true
+		}
+		if a == nil || b == nil {
+			return false
+		}
+		return a.Time.Equal(b.Time)
+	},
```

  Do we really need this cmp func? As I checked locally, even after I removed this cmp func, there are no errors.

- **pkg/controller/tas/resource_flavor.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T03:16:28Z
```diff
@@ -43,6 +47,34 @@ import (
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
+var nodeSemantic = conversion.EqualitiesOrDie(
+	nodeConditionEqual,
+	// Handle metav1.Time comparison to avoid panic on unexported fields
+	func(a, b metav1.Time) bool {
+		return a.Time.Equal(b.Time)
+	},
+	// Handle *metav1.Time pointer comparison (e.g., Taint.TimeAdded)
+	func(a, b *metav1.Time) bool {
+		if a == nil && b == nil {
+			return true
+		}
+		if a == nil || b == nil {
+			return false
+		}
+		return a.Time.Equal(b.Time)
+	},
+	// Handle resource.Quantity comparison to avoid panic on unexported fields
+	func(a, b resource.Quantity) bool {
+		return a.Cmp(b) == 0
```

  ```suggestion
  		return a.Equal(b)
  ```
  
  We have `Equal` function.

- **pkg/controller/tas/resource_flavor_test.go:246** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T03:37:53Z
```diff
@@ -0,0 +1,169 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+)
+
+func TestNodeHandler_Update(t *testing.T) {
+	now := metav1.Now()
+	later := metav1.NewTime(now.Add(10 * time.Second))
+
+	baseNode := testingnode.MakeNode("test-node").
+		Annotation("test-annotation", "value").
+		Label("topology.kubernetes.io/zone", "zone-a").
+		Label("node-role", "worker").
+		Taints(corev1.Taint{
+			Key:    "test-taint",
+			Value:  "value",
+			Effect: corev1.TaintEffectNoSchedule,
+		}).
+		StatusAllocatable(corev1.ResourceList{
+			corev1.ResourceCPU:    resource.MustParse("8"),
+			corev1.ResourceMemory: resource.MustParse("32Gi"),
+		}).
+		StatusConditions(
+			corev1.NodeCondition{
+				Type:               corev1.NodeReady,
+				Status:             corev1.ConditionTrue,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+			corev1.NodeCondition{
+				Type:               corev1.NodeMemoryPressure,
+				Status:             corev1.ConditionFalse,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+		)
+
+	testCases := map[string]struct {
+		oldNode     *corev1.Node
+		newNode     *corev1.Node
+		wantChanged bool
+	}{
+		"LastHeartbeatTime changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionHeartbeat(corev1.NodeReady, later).
+				ConditionHeartbeat(corev1.NodeMemoryPressure, later).
+				Obj(),
+			wantChanged: false,
+		},
+		"Annotation changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Annotation("new-annotation", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Label changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Label("new-label", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Node Ready status changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionStatus(corev1.NodeReady, corev1.ConditionFalse, later).
+				Obj(),
+			wantChanged: true,
+		},
+		"Allocatable resources changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("16"),
+				corev1.ResourceMemory: resource.MustParse("32Gi"),
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Taints changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:    "new-taint",
+				Value:  "new-value",
+				Effect: corev1.TaintEffectNoExecute,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Taints with TimeAdded": {
+			oldNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:       "test",
+				Value:     "value",
+				Effect:    corev1.TaintEffectNoExecute,
+				TimeAdded: &now,
+			}).Obj(),
+			newNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:       "test",
+				Value:     "value",
+				Effect:    corev1.TaintEffectNoExecute,
+				TimeAdded: &later,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Unschedulable changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Unschedulable().Obj(),
+			wantChanged: true,
+		},
+		"Update Multiple properties": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionHeartbeat(corev1.NodeReady, later).
+				ConditionHeartbeat(corev1.NodeMemoryPressure, later).
+				Annotation("another-annotation", "another-value").
+				ResourceVersion("12345").
+				Obj(),
+			wantChanged: true,
+		},
+		"New condition type added": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().StatusConditions(corev1.NodeCondition{
+				Type:               corev1.NodeDiskPressure,
+				Status:             corev1.ConditionTrue,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Condition removed": {
+			oldNode: baseNode.Clone().StatusConditions(corev1.NodeCondition{
+				Type:               corev1.NodeDiskPressure,
+				Status:             corev1.ConditionFalse,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			}).Obj(),
+			newNode:     baseNode.Clone().Obj(),
+			wantChanged: true,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			got := checkNodeSchedulingPropertiesChanged(tc.oldNode, tc.newNode)
```

  Oh, I got it. Ideally, we want to call `Update` function here.
  But, we need to implement the fake workqueue RateLimiter for that.
  
  So, I'm ok with testing for `checkNodeSchedulingPropertiesChanged` since I think that implementing fake workqueue is extra work this PR PoV.

- **pkg/controller/tas/resource_flavor_test.go:88** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T03:44:35Z
```diff
@@ -0,0 +1,169 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+)
+
+func TestNodeHandler_Update(t *testing.T) {
+	now := metav1.Now()
+	later := metav1.NewTime(now.Add(10 * time.Second))
+
+	baseNode := testingnode.MakeNode("test-node").
+		Annotation("test-annotation", "value").
+		Label("topology.kubernetes.io/zone", "zone-a").
+		Label("node-role", "worker").
+		Taints(corev1.Taint{
+			Key:    "test-taint",
+			Value:  "value",
+			Effect: corev1.TaintEffectNoSchedule,
+		}).
+		StatusAllocatable(corev1.ResourceList{
+			corev1.ResourceCPU:    resource.MustParse("8"),
+			corev1.ResourceMemory: resource.MustParse("32Gi"),
+		}).
+		StatusConditions(
+			corev1.NodeCondition{
+				Type:               corev1.NodeReady,
+				Status:             corev1.ConditionTrue,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+			corev1.NodeCondition{
+				Type:               corev1.NodeMemoryPressure,
+				Status:             corev1.ConditionFalse,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+		)
+
+	testCases := map[string]struct {
+		oldNode     *corev1.Node
+		newNode     *corev1.Node
+		wantChanged bool
+	}{
+		"LastHeartbeatTime changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionHeartbeat(corev1.NodeReady, later).
+				ConditionHeartbeat(corev1.NodeMemoryPressure, later).
+				Obj(),
+			wantChanged: false,
+		},
+		"Annotation changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Annotation("new-annotation", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Label changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Label("new-label", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Node Ready status changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionStatus(corev1.NodeReady, corev1.ConditionFalse, later).
```

  ```suggestion
  				StatusConditions(
  					corev1.NodeCondition{
  						Type:               corev1.NodeReady,
  						Status:             corev1.ConditionFalse,
  						LastHeartbeatTime:  later,
  						LastTransitionTime: later,
  					},
  				).
  ```
  
  We want to remove `StatusConditions` call from baseNode, then call that in every test case.
  We follow the DUMP principle to make tests as descriptive as possible.
  
  Note: The `ConditionHeartbeat` is still better since it overwrites only one field and is less programmable.

- **pkg/controller/tas/resource_flavor_test.go:59** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T03:46:18Z
```diff
@@ -0,0 +1,169 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+)
+
+func TestNodeHandler_Update(t *testing.T) {
+	now := metav1.Now()
+	later := metav1.NewTime(now.Add(10 * time.Second))
+
+	baseNode := testingnode.MakeNode("test-node").
+		Annotation("test-annotation", "value").
+		Label("topology.kubernetes.io/zone", "zone-a").
+		Label("node-role", "worker").
+		Taints(corev1.Taint{
+			Key:    "test-taint",
+			Value:  "value",
+			Effect: corev1.TaintEffectNoSchedule,
+		}).
+		StatusAllocatable(corev1.ResourceList{
+			corev1.ResourceCPU:    resource.MustParse("8"),
+			corev1.ResourceMemory: resource.MustParse("32Gi"),
+		}).
+		StatusConditions(
+			corev1.NodeCondition{
+				Type:               corev1.NodeReady,
+				Status:             corev1.ConditionTrue,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+			corev1.NodeCondition{
+				Type:               corev1.NodeMemoryPressure,
+				Status:             corev1.ConditionFalse,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
```

  Do we need multiple conditions?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-21T03:52:11Z

- **pkg/controller/tas/resource_flavor_test.go:148** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T03:52:11Z
```diff
@@ -0,0 +1,169 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+)
+
+func TestNodeHandler_Update(t *testing.T) {
+	now := metav1.Now()
+	later := metav1.NewTime(now.Add(10 * time.Second))
+
+	baseNode := testingnode.MakeNode("test-node").
+		Annotation("test-annotation", "value").
+		Label("topology.kubernetes.io/zone", "zone-a").
+		Label("node-role", "worker").
+		Taints(corev1.Taint{
+			Key:    "test-taint",
+			Value:  "value",
+			Effect: corev1.TaintEffectNoSchedule,
+		}).
+		StatusAllocatable(corev1.ResourceList{
+			corev1.ResourceCPU:    resource.MustParse("8"),
+			corev1.ResourceMemory: resource.MustParse("32Gi"),
+		}).
+		StatusConditions(
+			corev1.NodeCondition{
+				Type:               corev1.NodeReady,
+				Status:             corev1.ConditionTrue,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+			corev1.NodeCondition{
+				Type:               corev1.NodeMemoryPressure,
+				Status:             corev1.ConditionFalse,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+		)
+
+	testCases := map[string]struct {
+		oldNode     *corev1.Node
+		newNode     *corev1.Node
+		wantChanged bool
+	}{
+		"LastHeartbeatTime changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionHeartbeat(corev1.NodeReady, later).
+				ConditionHeartbeat(corev1.NodeMemoryPressure, later).
+				Obj(),
+			wantChanged: false,
+		},
+		"Annotation changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Annotation("new-annotation", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Label changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Label("new-label", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Node Ready status changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionStatus(corev1.NodeReady, corev1.ConditionFalse, later).
+				Obj(),
+			wantChanged: true,
+		},
+		"Allocatable resources changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("16"),
+				corev1.ResourceMemory: resource.MustParse("32Gi"),
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Taints changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:    "new-taint",
+				Value:  "new-value",
+				Effect: corev1.TaintEffectNoExecute,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Taints with TimeAdded": {
+			oldNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:       "test",
+				Value:     "value",
+				Effect:    corev1.TaintEffectNoExecute,
+				TimeAdded: &now,
+			}).Obj(),
+			newNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:       "test",
+				Value:     "value",
+				Effect:    corev1.TaintEffectNoExecute,
+				TimeAdded: &later,
+			}).Obj(),
+			wantChanged: true,
+		},
```

  Additionally, I want to add a case where oldNode has a null TimeAdd and newNode has a proper TimeAdd.

### Review by [@utam0k](https://github.com/utam0k) (commented) — 2025-08-21T11:15:24Z

- **pkg/controller/tas/resource_flavor_test.go:246** by [@utam0k](https://github.com/utam0k) — 2025-08-21T11:15:24Z
```diff
@@ -0,0 +1,169 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+)
+
+func TestNodeHandler_Update(t *testing.T) {
+	now := metav1.Now()
+	later := metav1.NewTime(now.Add(10 * time.Second))
+
+	baseNode := testingnode.MakeNode("test-node").
+		Annotation("test-annotation", "value").
+		Label("topology.kubernetes.io/zone", "zone-a").
+		Label("node-role", "worker").
+		Taints(corev1.Taint{
+			Key:    "test-taint",
+			Value:  "value",
+			Effect: corev1.TaintEffectNoSchedule,
+		}).
+		StatusAllocatable(corev1.ResourceList{
+			corev1.ResourceCPU:    resource.MustParse("8"),
+			corev1.ResourceMemory: resource.MustParse("32Gi"),
+		}).
+		StatusConditions(
+			corev1.NodeCondition{
+				Type:               corev1.NodeReady,
+				Status:             corev1.ConditionTrue,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+			corev1.NodeCondition{
+				Type:               corev1.NodeMemoryPressure,
+				Status:             corev1.ConditionFalse,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+		)
+
+	testCases := map[string]struct {
+		oldNode     *corev1.Node
+		newNode     *corev1.Node
+		wantChanged bool
+	}{
+		"LastHeartbeatTime changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionHeartbeat(corev1.NodeReady, later).
+				ConditionHeartbeat(corev1.NodeMemoryPressure, later).
+				Obj(),
+			wantChanged: false,
+		},
+		"Annotation changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Annotation("new-annotation", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Label changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Label("new-label", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Node Ready status changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionStatus(corev1.NodeReady, corev1.ConditionFalse, later).
+				Obj(),
+			wantChanged: true,
+		},
+		"Allocatable resources changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("16"),
+				corev1.ResourceMemory: resource.MustParse("32Gi"),
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Taints changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:    "new-taint",
+				Value:  "new-value",
+				Effect: corev1.TaintEffectNoExecute,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Taints with TimeAdded": {
+			oldNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:       "test",
+				Value:     "value",
+				Effect:    corev1.TaintEffectNoExecute,
+				TimeAdded: &now,
+			}).Obj(),
+			newNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:       "test",
+				Value:     "value",
+				Effect:    corev1.TaintEffectNoExecute,
+				TimeAdded: &later,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Unschedulable changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Unschedulable().Obj(),
+			wantChanged: true,
+		},
+		"Update Multiple properties": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionHeartbeat(corev1.NodeReady, later).
+				ConditionHeartbeat(corev1.NodeMemoryPressure, later).
+				Annotation("another-annotation", "another-value").
+				ResourceVersion("12345").
+				Obj(),
+			wantChanged: true,
+		},
+		"New condition type added": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().StatusConditions(corev1.NodeCondition{
+				Type:               corev1.NodeDiskPressure,
+				Status:             corev1.ConditionTrue,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Condition removed": {
+			oldNode: baseNode.Clone().StatusConditions(corev1.NodeCondition{
+				Type:               corev1.NodeDiskPressure,
+				Status:             corev1.ConditionFalse,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			}).Obj(),
+			newNode:     baseNode.Clone().Obj(),
+			wantChanged: true,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			got := checkNodeSchedulingPropertiesChanged(tc.oldNode, tc.newNode)
```

  Should I create the issue before creating the extra PR?

### Review by [@utam0k](https://github.com/utam0k) (commented) — 2025-08-21T11:26:07Z

- **pkg/controller/tas/resource_flavor.go:55** by [@utam0k](https://github.com/utam0k) — 2025-08-21T11:26:07Z
```diff
@@ -43,6 +47,34 @@ import (
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
+var nodeSemantic = conversion.EqualitiesOrDie(
+	nodeConditionEqual,
+	// Handle metav1.Time comparison to avoid panic on unexported fields
+	func(a, b metav1.Time) bool {
+		return a.Time.Equal(b.Time)
+	},
+	// Handle *metav1.Time pointer comparison (e.g., Taint.TimeAdded)
+	func(a, b *metav1.Time) bool {
+		if a == nil && b == nil {
+			return true
+		}
+		if a == nil || b == nil {
+			return false
+		}
+		return a.Time.Equal(b.Time)
+	},
```

  I revisited it and I confirmed that this func isn't needed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-21T11:38:49Z

- **pkg/controller/tas/resource_flavor_test.go:246** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T11:38:49Z
```diff
@@ -0,0 +1,169 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package tas
+
+import (
+	"testing"
+	"time"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	testingnode "sigs.k8s.io/kueue/pkg/util/testingjobs/node"
+)
+
+func TestNodeHandler_Update(t *testing.T) {
+	now := metav1.Now()
+	later := metav1.NewTime(now.Add(10 * time.Second))
+
+	baseNode := testingnode.MakeNode("test-node").
+		Annotation("test-annotation", "value").
+		Label("topology.kubernetes.io/zone", "zone-a").
+		Label("node-role", "worker").
+		Taints(corev1.Taint{
+			Key:    "test-taint",
+			Value:  "value",
+			Effect: corev1.TaintEffectNoSchedule,
+		}).
+		StatusAllocatable(corev1.ResourceList{
+			corev1.ResourceCPU:    resource.MustParse("8"),
+			corev1.ResourceMemory: resource.MustParse("32Gi"),
+		}).
+		StatusConditions(
+			corev1.NodeCondition{
+				Type:               corev1.NodeReady,
+				Status:             corev1.ConditionTrue,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+			corev1.NodeCondition{
+				Type:               corev1.NodeMemoryPressure,
+				Status:             corev1.ConditionFalse,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			},
+		)
+
+	testCases := map[string]struct {
+		oldNode     *corev1.Node
+		newNode     *corev1.Node
+		wantChanged bool
+	}{
+		"LastHeartbeatTime changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionHeartbeat(corev1.NodeReady, later).
+				ConditionHeartbeat(corev1.NodeMemoryPressure, later).
+				Obj(),
+			wantChanged: false,
+		},
+		"Annotation changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Annotation("new-annotation", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Label changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Label("new-label", "new-value").Obj(),
+			wantChanged: true,
+		},
+		"Node Ready status changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionStatus(corev1.NodeReady, corev1.ConditionFalse, later).
+				Obj(),
+			wantChanged: true,
+		},
+		"Allocatable resources changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().StatusAllocatable(corev1.ResourceList{
+				corev1.ResourceCPU:    resource.MustParse("16"),
+				corev1.ResourceMemory: resource.MustParse("32Gi"),
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Taints changed": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:    "new-taint",
+				Value:  "new-value",
+				Effect: corev1.TaintEffectNoExecute,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Taints with TimeAdded": {
+			oldNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:       "test",
+				Value:     "value",
+				Effect:    corev1.TaintEffectNoExecute,
+				TimeAdded: &now,
+			}).Obj(),
+			newNode: baseNode.Clone().Taints(corev1.Taint{
+				Key:       "test",
+				Value:     "value",
+				Effect:    corev1.TaintEffectNoExecute,
+				TimeAdded: &later,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Unschedulable changed": {
+			oldNode:     baseNode.Clone().Obj(),
+			newNode:     baseNode.Clone().Unschedulable().Obj(),
+			wantChanged: true,
+		},
+		"Update Multiple properties": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().
+				ConditionHeartbeat(corev1.NodeReady, later).
+				ConditionHeartbeat(corev1.NodeMemoryPressure, later).
+				Annotation("another-annotation", "another-value").
+				ResourceVersion("12345").
+				Obj(),
+			wantChanged: true,
+		},
+		"New condition type added": {
+			oldNode: baseNode.Clone().Obj(),
+			newNode: baseNode.Clone().StatusConditions(corev1.NodeCondition{
+				Type:               corev1.NodeDiskPressure,
+				Status:             corev1.ConditionTrue,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			}).Obj(),
+			wantChanged: true,
+		},
+		"Condition removed": {
+			oldNode: baseNode.Clone().StatusConditions(corev1.NodeCondition{
+				Type:               corev1.NodeDiskPressure,
+				Status:             corev1.ConditionFalse,
+				LastHeartbeatTime:  now,
+				LastTransitionTime: now,
+			}).Obj(),
+			newNode:     baseNode.Clone().Obj(),
+			wantChanged: true,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			got := checkNodeSchedulingPropertiesChanged(tc.oldNode, tc.newNode)
```

  If you can create an issue, I would appreciate that.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-21T11:39:11Z

- **pkg/controller/tas/resource_flavor.go:55** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T11:39:10Z
```diff
@@ -43,6 +47,34 @@ import (
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
+var nodeSemantic = conversion.EqualitiesOrDie(
+	nodeConditionEqual,
+	// Handle metav1.Time comparison to avoid panic on unexported fields
+	func(a, b metav1.Time) bool {
+		return a.Time.Equal(b.Time)
+	},
+	// Handle *metav1.Time pointer comparison (e.g., Taint.TimeAdded)
+	func(a, b *metav1.Time) bool {
+		if a == nil && b == nil {
+			return true
+		}
+		if a == nil || b == nil {
+			return false
+		}
+		return a.Time.Equal(b.Time)
+	},
```

  Awesome.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-21T12:03:18Z

- **pkg/controller/tas/resource_flavor.go:58** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T12:03:15Z
```diff
@@ -43,6 +47,34 @@ import (
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
+var nodeSemantic = conversion.EqualitiesOrDie(
+	nodeConditionEqual,
+	// Handle metav1.Time comparison to avoid panic on unexported fields
+	func(a, b metav1.Time) bool {
+		return a.Time.Equal(b.Time)
+	},
+	// Handle *metav1.Time pointer comparison (e.g., Taint.TimeAdded)
+	func(a, b *metav1.Time) bool {
+		if a == nil && b == nil {
+			return true
+		}
+		if a == nil || b == nil {
+			return false
+		}
+		return a.Time.Equal(b.Time)
+	},
+	// Handle resource.Quantity comparison to avoid panic on unexported fields
+	func(a, b resource.Quantity) bool {
+		return a.Cmp(b) == 0
```

  @utam0k This is still not fixed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-21T12:04:15Z

- **pkg/util/testingjobs/node/wrappers.go:141** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T12:04:14Z
```diff
@@ -107,3 +116,32 @@ func (n *NodeWrapper) Unschedulable() *NodeWrapper {
 	n.Spec.Unschedulable = true
 	return n
 }
+
+// ConditionHeartbeat updates the LastHeartbeatTime of an existing condition.
+func (n *NodeWrapper) ConditionHeartbeat(conditionType corev1.NodeConditionType, heartbeat metav1.Time) *NodeWrapper {
+	for i := range n.Status.Conditions {
+		if n.Status.Conditions[i].Type == conditionType {
+			n.Status.Conditions[i].LastHeartbeatTime = heartbeat
+			break
+		}
+	}
+	return n
+}
+
+// ConditionStatus updates the Status and LastTransitionTime of an existing condition.
+func (n *NodeWrapper) ConditionStatus(conditionType corev1.NodeConditionType, status corev1.ConditionStatus, transitionTime metav1.Time) *NodeWrapper {
+	for i := range n.Status.Conditions {
+		if n.Status.Conditions[i].Type == conditionType {
+			n.Status.Conditions[i].Status = status
+			n.Status.Conditions[i].LastTransitionTime = transitionTime
+			break
+		}
+	}
+	return n
+}
```

  ```suggestion
  ```
  
  This is no longer used anywhere.

### Review by [@utam0k](https://github.com/utam0k) (commented) — 2025-08-21T12:16:43Z

- **pkg/controller/tas/resource_flavor.go:58** by [@utam0k](https://github.com/utam0k) — 2025-08-21T12:16:42Z
```diff
@@ -43,6 +47,34 @@ import (
 	"sigs.k8s.io/kueue/pkg/queue"
 )
 
+var nodeSemantic = conversion.EqualitiesOrDie(
+	nodeConditionEqual,
+	// Handle metav1.Time comparison to avoid panic on unexported fields
+	func(a, b metav1.Time) bool {
+		return a.Time.Equal(b.Time)
+	},
+	// Handle *metav1.Time pointer comparison (e.g., Taint.TimeAdded)
+	func(a, b *metav1.Time) bool {
+		if a == nil && b == nil {
+			return true
+		}
+		if a == nil || b == nil {
+			return false
+		}
+		return a.Time.Equal(b.Time)
+	},
+	// Handle resource.Quantity comparison to avoid panic on unexported fields
+	func(a, b resource.Quantity) bool {
+		return a.Cmp(b) == 0
```

  🙏

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-21T12:22:39Z

Thank you!
/lgtm
/approve
