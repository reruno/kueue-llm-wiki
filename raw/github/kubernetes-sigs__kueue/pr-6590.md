# PR #6590: feat: add deactivated workload metric

**Summary**: feat: add deactivated workload metric

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/6590

**Last updated**: 2025-09-30T12:30:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `feat/deactivated-cause-metrics` → `main`
- **Created**: 2025-08-15T08:29:58Z
- **Updated**: 2025-09-30T12:30:38Z
- **Merged**: 2025-08-27T10:24:08Z
- **Closed**: 2025-08-27T10:24:08Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-action-required`, `approved`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 12   **+88 / -61**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Use a more streamlined approach for deactivated workload metrics

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #6359 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Replace "DeactivatedXYZ" "reason" label values with "Deactivated" and introduce "underlying_cause" label to the following metrics:
- "pods_ready_to_evicted_time_seconds"
- "evicted_workloads_total"
- "local_queue_evicted_workloads_total"
- "evicted_workloads_once_total"

ACTION REQUIRED: If you rely on the "DeactivatedXYZ" "reason" label values, you can migrate to the "Deactivated" "reason" label value and the following "underlying_cause" label values:
- ""
- "WaitForStart"
- "WaitForRecovery"
- "AdmissionCheck"
- "MaximumExecutionTimeExceeded"
- "RequeuingLimitExceeded"
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2025-08-15T08:30:04Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 0e0153f0977068a7b5e48cefd697e1c203a22607 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/68aeced161e6b200083d46d5 |

### Comment by [@mykysha](https://github.com/mykysha) — 2025-08-21T11:42:03Z

@tenzen-y could you please take a look?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T11:45:31Z

> @tenzen-y could you please take a look?

ACK

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-27T09:31:31Z

/release-note-edit

```release-note
Replace "DeactivatedXYZ" "reason" label values with "Deactivated" and introduce "underlying_cause" label to the following metrics:
- "pods_ready_to_evicted_time_seconds"
- "evicted_workloads_total"
- "local_queue_evicted_workloads_total"
- "evicted_workloads_once_total"

ACTION REQUIRED
If you rely on the "DeactivatedXYZ" "reason" label values, you can migrate to the "Deactivated" "reason" label value and the following "underlying_cause" label values:
- ""
- "WaitForStart"
- "WaitForRecovery"
- "AdmissionCheck"
- "MaximumExecutionTimeExceeded"
- "RequeuingLimitExceeded"
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-27T09:32:05Z

LGTM label has been added.  <details>Git tree hash: eecb825e601a2153b6b13e32ab67ac73792e341b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-08-27T09:32:07Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/6590#" title="Author self-approved">mykysha</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/6590#pullrequestreview-3159061059" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mykysha](https://github.com/mykysha) — 2025-08-27T09:47:40Z

/retest-required

seems to be another occurrence of #6674

### Comment by [@mykysha](https://github.com/mykysha) — 2025-08-27T10:06:13Z

/test pull-kueue-test-unit-main

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-30T12:29:59Z

/release-note-edit

```release-note
Replace "DeactivatedXYZ" "reason" label values with "Deactivated" and introduce "underlying_cause" label to the following metrics:
- "pods_ready_to_evicted_time_seconds"
- "evicted_workloads_total"
- "local_queue_evicted_workloads_total"
- "evicted_workloads_once_total"

ACTION REQUIRED: If you rely on the "DeactivatedXYZ" "reason" label values, you can migrate to the "Deactivated" "reason" label value and the following "underlying_cause" label values:
- ""
- "WaitForStart"
- "WaitForRecovery"
- "AdmissionCheck"
- "MaximumExecutionTimeExceeded"
- "RequeuingLimitExceeded"
```

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2025-09-30T12:30:27Z

The `ACTION REQUIRED` must be started with `ACTION REQUIRED:` to generate correct CHANGELOG

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-08-15T15:40:56Z

- **pkg/workload/workload.go:1086** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-08-15T15:40:56Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
```

  ```suggestion
  	if underlyingCause != "" {
  ```
  Maybe like this?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-08-15T15:45:57Z

- **pkg/workload/workload.go:1086** by [@mykysha](https://github.com/mykysha) — 2025-08-15T15:45:57Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
```

  Perhaps, but then all the other reasons that did not have DueTo will have one now, which will affect conditions in the workloads, which I'm not sure is ok in the scope of this PR and task. If such changes are indeed ok, will commonise this for all reason - cause pairs.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-08-15T15:55:06Z

- **pkg/workload/workload.go:1086** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-08-15T15:55:06Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
```

  Only if underlyingCause set. The DueTo suffix is added only for deactivation, right?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-08-15T15:57:08Z

- **pkg/workload/workload.go:1086** by [@mykysha](https://github.com/mykysha) — 2025-08-15T15:57:07Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
```

  In the current logic, yes, but underlyingCause is not exclusive to the deactivation, so it will be set for other eviction reasons if we remove the deactivation-only check from the if clause

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-08-15T15:58:46Z

- **pkg/workload/workload.go:1086** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-08-15T15:58:46Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
```

  Shouldn't we add this suffix for other cases?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-08-15T16:02:10Z

- **pkg/workload/workload.go:1086** by [@mykysha](https://github.com/mykysha) — 2025-08-15T16:02:10Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
```

  Not sure, and not sure whether that should be done in the current scope. Can definitely do, if needed, but as of now tried to make the PR not to affect things outside of the deactivation metrics.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2025-08-15T16:08:20Z

- **pkg/workload/workload.go:1086** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2025-08-15T16:08:20Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
```

  @tenzen-y WDYT?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-21T18:28:32Z

- **pkg/workload/workload.go:1086** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T18:28:32Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
```

  As I mentioned in https://github.com/kubernetes-sigs/kueue/issues/6359#issuecomment-3211642692, I want to change only schedulingStats, and I want to keep conditions so that we can narrow down the status.conditions state machine matrix.
  
  So, let's keep @mykysha approach.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-21T18:45:30Z

Thank you for moving this forward.

- **pkg/workload/workload.go:1084** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T18:33:19Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
```

  Uhm, this `func Evict(..., reason, underlyingCause, msg string, ...)` is unsafe since we have 3 string (same typed) input. Ideally, we should have a dedicated type for `underlyingCause` to the API like `WorkloadEvictionUnderlyingCause`. Let's revisit there after this PR.

- **pkg/controller/core/workload_controller_test.go:690** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T18:38:20Z
```diff
@@ -677,16 +677,17 @@ func TestReconcile(t *testing.T) {
 				).
 				SchedulingStatsEviction(
 					kueue.WorkloadSchedulingStatsEviction{
-						Reason: "DeactivatedDueToAdmissionCheck",
-						Count:  1,
+						Reason:          "Deactivated",
+						UnderlyingCause: "AdmissionCheck",
+						Count:           1,
 					},
 				).
 				Obj(),
 			wantEvents: []utiltesting.EventRecord{
 				{
 					Key:       types.NamespacedName{Namespace: "ns", Name: "wl"},
 					EventType: "Normal",
-					Reason:    "EvictedDueToDeactivatedDueToAdmissionCheck",
+					Reason:    "EvictedDueToDeactivated",
```

  Can we keep the event reason?
  The new event reason lost the AdmissionCheck semantic.

- **pkg/metrics/metrics.go:351** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-21T18:45:13Z
```diff
@@ -349,6 +337,18 @@ The label 'reason' can have the following values:
 		}, []string{"preempting_cluster_queue", "reason"},
 	)
 
+	DeactivatedWorkloadsTotal = prometheus.NewCounterVec(
+		prometheus.CounterOpts{
+			Subsystem: constants.KueueName,
+			Name:      "deactivated_workloads_total",
+			Help: `The number of deactivated workloads per 'cluster_queue',
+The label 'reason' can have the following values:
+- "AdmissionCheck" means that the workload was deactivated because at least one admission check transitioned to False.
+- "MaximumExecutionTimeExceeded" means that the workload was deactivated due to maximum execution time exceeded.
+- "RequeuingLimitExceeded" means that the workload was deactivated due to requeuing limit exceeded.`,
+		}, []string{"cluster_queue", "reason"},
+	)
+
```

  I assumed introducing `underlying_cause` to each existing metric instead of adding a dedicated one.
  If there are not any `underlying_cause`, we can use an empty label ("").
  
  See: https://github.com/kubernetes-sigs/kueue/pull/6332/files#r2248078820

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-08-22T15:43:40Z

- **pkg/metrics/metrics.go:351** by [@mykysha](https://github.com/mykysha) — 2025-08-22T15:43:40Z
```diff
@@ -349,6 +337,18 @@ The label 'reason' can have the following values:
 		}, []string{"preempting_cluster_queue", "reason"},
 	)
 
+	DeactivatedWorkloadsTotal = prometheus.NewCounterVec(
+		prometheus.CounterOpts{
+			Subsystem: constants.KueueName,
+			Name:      "deactivated_workloads_total",
+			Help: `The number of deactivated workloads per 'cluster_queue',
+The label 'reason' can have the following values:
+- "AdmissionCheck" means that the workload was deactivated because at least one admission check transitioned to False.
+- "MaximumExecutionTimeExceeded" means that the workload was deactivated due to maximum execution time exceeded.
+- "RequeuingLimitExceeded" means that the workload was deactivated due to requeuing limit exceeded.`,
+		}, []string{"cluster_queue", "reason"},
+	)
+
```

  Removed the dedicated metric, introduced `underlying_cause` to the existing ones

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-08-22T15:45:23Z

- **pkg/controller/core/workload_controller_test.go:690** by [@mykysha](https://github.com/mykysha) — 2025-08-22T15:45:23Z
```diff
@@ -677,16 +677,17 @@ func TestReconcile(t *testing.T) {
 				).
 				SchedulingStatsEviction(
 					kueue.WorkloadSchedulingStatsEviction{
-						Reason: "DeactivatedDueToAdmissionCheck",
-						Count:  1,
+						Reason:          "Deactivated",
+						UnderlyingCause: "AdmissionCheck",
+						Count:           1,
 					},
 				).
 				Obj(),
 			wantEvents: []utiltesting.EventRecord{
 				{
 					Key:       types.NamespacedName{Namespace: "ns", Name: "wl"},
 					EventType: "Normal",
-					Reason:    "EvictedDueToDeactivatedDueToAdmissionCheck",
+					Reason:    "EvictedDueToDeactivated",
```

  Updated, but done in the same way as with conditions, where Reason would include DueTo and underlying_cause only for Deactivated metric. Can remove the exclusivity and include underlying_cause for all eviction types if needed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-26T11:35:22Z

@mykysha Overall LGTM, thanks!

- **pkg/metrics/metrics.go:169** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-26T11:24:46Z
```diff
@@ -166,12 +166,9 @@ The label 'reason' can have the following values:
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
 - "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
 - "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.
-- "Deactivated" means that the workload was evicted because spec.active is set to false.
-- "DeactivatedDueToAdmissionCheck" means that the workload was evicted and deactivated by Kueue due to a rejected admission check.
-- "DeactivatedDueToMaximumExecutionTimeExceeded" means that the workload was evicted and deactivated by Kueue due to maximum execution time exceeded.
-- "DeactivatedDueToRequeuingLimitExceeded" means that the workload was evicted and deactivated by Kueue due to requeuing limit exceeded.`,
+- "Deactivated" means that the workload was evicted because spec.active is set to false.`,
```

  @mykysha Could you list the possible values for underlying_cause like the following?
  
  ```suggestion
  - "Deactivated" means that the workload was evicted because spec.active is set to false.
  The label 'underlying_cause' can have the following values:
  - "" means that the value in 'reason' label is the root cause for eviction,
  - "AdmissionCheck" means that the AdmissionCheck state is the behind cause of eviction
  AND MORE...`,
  ```
  
  Same comment on ohter metrics as well.

- **pkg/workload/workload.go:1088** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-26T11:28:52Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
+		prepareForEviction(wl, clock.Now(), fmt.Sprintf("%sDueTo%s", reason, underlyingCause), msg)
+	} else {
+		prepareForEviction(wl, clock.Now(), reason, msg)
+	}
```

  ```suggestion
  	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
  		reason = fmt.Sprintf("%sDueTo%s", reason, underlyingCause)
  	}
  	prepareForEviction(wl, clock.Now(), reason, msg)
  ```
  
  The differences between those are only `reason`. So, the overriding reason makes more sense.

- **pkg/workload/workload.go:1131** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-26T11:32:23Z
```diff
@@ -1112,15 +1116,19 @@ func resetClusterNomination(w *kueue.Workload) {
 	w.Status.NominatedClusterNames = nil
 }
 
-func reportEvictedWorkload(recorder record.EventRecorder, wl *kueue.Workload, cqName kueue.ClusterQueueReference, reason, message string) {
-	metrics.ReportEvictedWorkloads(cqName, reason)
+func reportEvictedWorkload(recorder record.EventRecorder, wl *kueue.Workload, cqName kueue.ClusterQueueReference, reason, underlyingCause, message string) {
+	metrics.ReportEvictedWorkloads(cqName, reason, underlyingCause)
 	if podsReadyToEvictionTime := workloadsWithPodsReadyToEvictedTime(wl); podsReadyToEvictionTime != nil {
-		metrics.PodsReadyToEvictedTimeSeconds.WithLabelValues(string(cqName), reason).Observe(podsReadyToEvictionTime.Seconds())
+		metrics.PodsReadyToEvictedTimeSeconds.WithLabelValues(string(cqName), reason, underlyingCause).Observe(podsReadyToEvictionTime.Seconds())
 	}
 	if features.Enabled(features.LocalQueueMetrics) {
-		metrics.ReportLocalQueueEvictedWorkloads(metrics.LQRefFromWorkload(wl), reason)
+		metrics.ReportLocalQueueEvictedWorkloads(metrics.LQRefFromWorkload(wl), reason, underlyingCause)
+	}
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
+		recorder.Event(wl, corev1.EventTypeNormal, fmt.Sprintf("%sDueTo%sDueTo%s", kueue.WorkloadEvicted, reason, underlyingCause), message)
+	} else {
+		recorder.Event(wl, corev1.EventTypeNormal, fmt.Sprintf("%sDueTo%s", kueue.WorkloadEvicted, reason), message)
 	}
```

  ```suggestion
  	eventReason := fmt.Sprintf("%sDueTo%s", kueue.WorkloadEvicted, reason)
  	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
  		eventReason = fmt.Sprintf("%sDueTo%s", eventReason, underlyingCause)
  	}
  	recorder.Event(wl, corev1.EventTypeNormal, eventReason, message)
  ```
  
  Same comments as above.

- **pkg/workload/workload.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-26T11:35:14Z
  This is for follow-up comment.
  It would be nice to address the following:
  
  - Make `DueTo` a constant.
  - Introduce `WorkloadEvictionUnderlyingCause` typed to `underlyingCause` API to avoid unsafe function usage: https://github.com/kubernetes-sigs/kueue/pull/6590/files#r2291844495

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-08-27T08:40:34Z

- **pkg/metrics/metrics.go:169** by [@mykysha](https://github.com/mykysha) — 2025-08-27T08:40:34Z
```diff
@@ -166,12 +166,9 @@ The label 'reason' can have the following values:
 - "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.
 - "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
 - "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.
-- "Deactivated" means that the workload was evicted because spec.active is set to false.
-- "DeactivatedDueToAdmissionCheck" means that the workload was evicted and deactivated by Kueue due to a rejected admission check.
-- "DeactivatedDueToMaximumExecutionTimeExceeded" means that the workload was evicted and deactivated by Kueue due to maximum execution time exceeded.
-- "DeactivatedDueToRequeuingLimitExceeded" means that the workload was evicted and deactivated by Kueue due to requeuing limit exceeded.`,
+- "Deactivated" means that the workload was evicted because spec.active is set to false.`,
```

  Added label descriptions

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-08-27T08:41:33Z

- **pkg/workload/workload.go:1088** by [@mykysha](https://github.com/mykysha) — 2025-08-27T08:41:33Z
```diff
@@ -1081,7 +1081,11 @@ func AdmissionChecksForWorkload(log logr.Logger, wl *kueue.Workload, admissionCh
 }
 
 func Evict(ctx context.Context, c client.Client, recorder record.EventRecorder, wl *kueue.Workload, reason, underlyingCause, msg string, clock clock.Clock) error {
-	prepareForEviction(wl, clock.Now(), reason, msg)
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
+		prepareForEviction(wl, clock.Now(), fmt.Sprintf("%sDueTo%s", reason, underlyingCause), msg)
+	} else {
+		prepareForEviction(wl, clock.Now(), reason, msg)
+	}
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-08-27T08:41:45Z

- **pkg/workload/workload.go:1131** by [@mykysha](https://github.com/mykysha) — 2025-08-27T08:41:45Z
```diff
@@ -1112,15 +1116,19 @@ func resetClusterNomination(w *kueue.Workload) {
 	w.Status.NominatedClusterNames = nil
 }
 
-func reportEvictedWorkload(recorder record.EventRecorder, wl *kueue.Workload, cqName kueue.ClusterQueueReference, reason, message string) {
-	metrics.ReportEvictedWorkloads(cqName, reason)
+func reportEvictedWorkload(recorder record.EventRecorder, wl *kueue.Workload, cqName kueue.ClusterQueueReference, reason, underlyingCause, message string) {
+	metrics.ReportEvictedWorkloads(cqName, reason, underlyingCause)
 	if podsReadyToEvictionTime := workloadsWithPodsReadyToEvictedTime(wl); podsReadyToEvictionTime != nil {
-		metrics.PodsReadyToEvictedTimeSeconds.WithLabelValues(string(cqName), reason).Observe(podsReadyToEvictionTime.Seconds())
+		metrics.PodsReadyToEvictedTimeSeconds.WithLabelValues(string(cqName), reason, underlyingCause).Observe(podsReadyToEvictionTime.Seconds())
 	}
 	if features.Enabled(features.LocalQueueMetrics) {
-		metrics.ReportLocalQueueEvictedWorkloads(metrics.LQRefFromWorkload(wl), reason)
+		metrics.ReportLocalQueueEvictedWorkloads(metrics.LQRefFromWorkload(wl), reason, underlyingCause)
+	}
+	if reason == kueue.WorkloadDeactivated && underlyingCause != "" {
+		recorder.Event(wl, corev1.EventTypeNormal, fmt.Sprintf("%sDueTo%sDueTo%s", kueue.WorkloadEvicted, reason, underlyingCause), message)
+	} else {
+		recorder.Event(wl, corev1.EventTypeNormal, fmt.Sprintf("%sDueTo%s", kueue.WorkloadEvicted, reason), message)
 	}
```

  Changed here as well

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-27T09:20:20Z

Overall LGTM

- **pkg/metrics/metrics.go:174** by [@tenzen-y](https://github.com/tenzen-y) — 2025-08-27T09:20:04Z
```diff
@@ -167,11 +167,12 @@ The label 'reason' can have the following values:
 - "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
 - "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.
 - "Deactivated" means that the workload was evicted because spec.active is set to false.
-- "DeactivatedDueToAdmissionCheck" means that the workload was evicted and deactivated by Kueue due to a rejected admission check.
-- "DeactivatedDueToMaximumExecutionTimeExceeded" means that the workload was evicted and deactivated by Kueue due to maximum execution time exceeded.
-- "DeactivatedDueToRequeuingLimitExceeded" means that the workload was evicted and deactivated by Kueue due to requeuing limit exceeded.`,
+The label 'underlying_cause' can have the following values:
+- "AdmissionCheck" means that the workload was evicted by Kueue due to a rejected admission check.
+- "MaximumExecutionTimeExceeded" means that the workload was evicted by Kueue due to maximum execution time exceeded.
+- "RequeuingLimitExceeded" means that the workload was evicted by Kueue due to requeuing limit exceeded.`,
```

  ```suggestion
  - "RequeuingLimitExceeded" means that the workload was evicted by Kueue due to requeuing limit exceeded.
  - "" means that the value in 'reason' label is the root cause for eviction`,
  ```
  
  @mykysha Can we add a description for an empty label?
  Same comments for other metrics.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2025-08-27T09:24:52Z

- **pkg/metrics/metrics.go:174** by [@mykysha](https://github.com/mykysha) — 2025-08-27T09:24:52Z
```diff
@@ -167,11 +167,12 @@ The label 'reason' can have the following values:
 - "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.
 - "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.
 - "Deactivated" means that the workload was evicted because spec.active is set to false.
-- "DeactivatedDueToAdmissionCheck" means that the workload was evicted and deactivated by Kueue due to a rejected admission check.
-- "DeactivatedDueToMaximumExecutionTimeExceeded" means that the workload was evicted and deactivated by Kueue due to maximum execution time exceeded.
-- "DeactivatedDueToRequeuingLimitExceeded" means that the workload was evicted and deactivated by Kueue due to requeuing limit exceeded.`,
+The label 'underlying_cause' can have the following values:
+- "AdmissionCheck" means that the workload was evicted by Kueue due to a rejected admission check.
+- "MaximumExecutionTimeExceeded" means that the workload was evicted by Kueue due to maximum execution time exceeded.
+- "RequeuingLimitExceeded" means that the workload was evicted by Kueue due to requeuing limit exceeded.`,
```

  Added empty label descriptions

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2025-08-27T09:31:58Z

Thank you!
/lgtm
/approve
