# PR #660: Record original node selectors

**Summary**: Record original node selectors

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/660

**Last updated**: 2023-04-20T06:01:07Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `record_restore_original_node_selectors` → `main`
- **Created**: 2023-03-24T13:47:25Z
- **Updated**: 2023-04-20T06:01:07Z
- **Merged**: 2023-04-05T12:13:41Z
- **Closed**: 2023-04-05T12:13:41Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `tide/merge-method-squash`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: _none_
- **Changed files**: 14   **+412 / -25**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Records the original node selectors in a annotation `kueue.x-k8s.io/original-selectors`,  the content of this annotation is used to restore the "job's"  node selectors content if a workload is not present when a the job is suspended (`stopJob`), this being the case when a workload is deleted while it's admitted.


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #518

#### Special notes for your reviewer:

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-03-24T13:47:33Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 776aefa8073116f645859cacc2ee1b628c9b7722 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/642d0c9f721b160008668412 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-660--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-660--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTY2MC0ta3ViZXJuZXRlcy1zaWdzLWt1ZXVlLm5ldGxpZnkuYXBwIn0.weNAk2QvBvVSIpgroxG2SheeY2H92-eoil_2eYSBxlA)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site settings](https://app.netlify.com/sites/kubernetes-sigs-kueue/settings/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-03-24T13:47:35Z

Hi @trasc. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@trasc](https://github.com/trasc) — 2023-03-24T13:49:08Z

/cc @alculquicondor 

/cc @mwielgus 

/cc @mimowo

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-03-24T18:30:27Z

/ok-to-test

### Comment by [@trasc](https://github.com/trasc) — 2023-03-31T08:53:27Z

/test pull-kueue-test-integration-main

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-03T19:15:46Z

/approve
/lgtm
/hold
Feel free to lift the hold if you want to address the suggestions in a follow up.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-04-03T19:15:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/660#issuecomment-1494845137" title="LGTM">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/660#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-03T19:16:22Z

/label tide/merge-method-squash

### Comment by [@trasc](https://github.com/trasc) — 2023-04-05T05:54:52Z

/unhold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-05T12:12:28Z

/lgtm

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-27T10:41:41Z

Some comments around error handling though, and a question if there is a possibility of a malicious actor injecting unmarshalable node selectors. Either directly in the job annotation or via a recreated workload object.

- **pkg/controller/jobframework/constants.go:36** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:25:19Z
```diff
@@ -32,4 +32,6 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	OriginalSelectorsAnnotation = "kueue.x-k8s.io/original-selectors"
```

  I would suggest `original-node-selectors`. That name would better correspond with the field in name in pod. Also, in many other contexts we call it with `node`.

- **pkg/controller/jobframework/reconciler.go:40** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:25:39Z
```diff
@@ -34,6 +36,10 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
+var (
+	errSelectorsNotFound = errors.New(OriginalSelectorsAnnotation + " not found")
```

  ```suggestion
  	errNodeSelectorsNotFound = errors.New(OriginalNodeSelectorsAnnotation + " not found")
  ```

- **pkg/controller/jobframework/reconciler.go:502** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:31:44Z
```diff
@@ -442,3 +481,45 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func nodeSelectorClone(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// nodeSelectorsFromObject tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not fond or is unable to unmarshal
+func nodeSelectorsFromObject(obj client.Object) ([]map[string]string, error) {
+	str, found := obj.GetAnnotations()[OriginalSelectorsAnnotation]
+	if !found {
+		return nil, errSelectorsNotFound
+	}
+	// unmarshal
+	ret := []map[string]string{}
+	err := json.Unmarshal([]byte(str), &ret)
```

  ```suggestion
  	if err := json.Unmarshal([]byte(str), &ret); err != nil {
  ```

- **pkg/controller/jobframework/reconciler.go:362** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:38:19Z
```diff
@@ -333,8 +347,20 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
+	var originalSelectors []map[string]string
 	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+		originalSelectors = r.getNodeSelectorsFromPodSets(wl)
+	} else {
+		log.V(3).Info("restore node selectors from annotation")
+		selectors, err := nodeSelectorsFromObject(object)
+		if err != nil {
+			log.V(3).Info("Unable to get original node selectors", "error", err)
+		} else {
+			originalSelectors = selectors
+		}
+	}
+	if len(originalSelectors) > 0 {
```

  IIUC the only reason for not restoring the original node selectors would be an error during unmarhaling. I  suggest to directly check for the error here to express the intention.
  
  Is there a reproducible scenario when the unmarshaling fails, say can a malicious actor set node selectors which cannot unmarshalled? If so, it would be good to add an integration test for it, to verify the job gets suspended and there is no crash.

- **pkg/controller/jobframework/reconciler.go:495** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:40:11Z
```diff
@@ -442,3 +481,45 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func nodeSelectorClone(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// nodeSelectorsFromObject tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not fond or is unable to unmarshal
+func nodeSelectorsFromObject(obj client.Object) ([]map[string]string, error) {
```

  ```suggestion
  func getNodeSelectorsFromObjectAnnotation(obj client.Object) ([]map[string]string, error) {
  ```

- **pkg/controller/jobframework/reconciler.go:357** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:43:06Z
```diff
@@ -333,8 +347,20 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
+	var originalSelectors []map[string]string
 	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+		originalSelectors = r.getNodeSelectorsFromPodSets(wl)
+	} else {
+		log.V(3).Info("restore node selectors from annotation")
+		selectors, err := nodeSelectorsFromObject(object)
+		if err != nil {
+			log.V(3).Info("Unable to get original node selectors", "error", err)
```

  ```suggestion
  			log.V(3).Error(err, "Unable to get original node selectors")
  ```

- **pkg/controller/jobframework/reconciler.go:350** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:45:20Z
```diff
@@ -333,8 +347,20 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
+	var originalSelectors []map[string]string
```

  ```suggestion
  	var originalNodeSelectors []map[string]string
  ```

- **pkg/controller/jobframework/reconciler.go:485** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:46:15Z
```diff
@@ -442,3 +481,45 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func nodeSelectorClone(src map[string]string) map[string]string {
```

  I think `cloneNodeSelector` would read better.

- **pkg/controller/jobframework/reconciler.go:495** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:46:54Z
```diff
@@ -442,3 +481,45 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func nodeSelectorClone(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// nodeSelectorsFromObject tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not fond or is unable to unmarshal
+func nodeSelectorsFromObject(obj client.Object) ([]map[string]string, error) {
```

  Let's make the naming more consistent with the `getNodeSelectorsFromPodSets`  function.

- **pkg/controller/jobframework/reconciler.go:500** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:48:25Z
```diff
@@ -442,3 +481,45 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func nodeSelectorClone(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// nodeSelectorsFromObject tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not fond or is unable to unmarshal
+func nodeSelectorsFromObject(obj client.Object) ([]map[string]string, error) {
+	str, found := obj.GetAnnotations()[OriginalSelectorsAnnotation]
+	if !found {
+		return nil, errSelectorsNotFound
+	}
+	// unmarshal
+	ret := []map[string]string{}
+	err := json.Unmarshal([]byte(str), &ret)
+	if err != nil {
+		return nil, err
+	}
+	return ret, nil
+}
+
+// nodeSelectorsSetToObject - sets an annotation containing the provided node selectors into
+// a job object, even if very unlikely it could return an error related to json.marshaling
+func nodeSelectorsSetToObject(obj client.Object, nodeSelectors []map[string]string) error {
```

  I suggest calling it `SetNodeSelectorsInObjectAnnotation`.

- **test/integration/controller/job/job_controller_test.go:881** by [@mimowo](https://github.com/mimowo) — 2023-03-27T09:52:07Z
```diff
@@ -831,4 +831,65 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			initialSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
```

  Is there no race condition here, if the workload is recreated before the localQueue is deleted? Shouldn't we first delete the queue? 
  
  Or, maybe, create a second higher priority workload before deleting this one admitted, to get the second workload admitted and block resources so that this one will not get re-admitted.

- **pkg/controller/jobframework/reconciler.go:352** by [@mimowo](https://github.com/mimowo) — 2023-03-27T10:23:23Z
```diff
@@ -333,8 +347,20 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
+	var originalSelectors []map[string]string
 	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+		originalSelectors = r.getNodeSelectorsFromPodSets(wl)
```

  I'm wondering, is there a reason why we need the lookup to workload here, can't the annotations be the single source of through now?

- **test/integration/controller/mpijob/mpijob_controller_test.go:595** by [@mimowo](https://github.com/mimowo) — 2023-03-27T10:25:34Z
```diff
@@ -561,4 +561,74 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 
 	})
 
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			job := testingmpijob.MakeMPIJob(jobName, ns.Name).Queue(localQueue.Name).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "3").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "4").
+				Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &kubeflow.MPIJob{}
+
+			nodeSelectors := func(j *kubeflow.MPIJob) map[kubeflow.MPIReplicaType]map[string]string {
+				ret := map[kubeflow.MPIReplicaType]map[string]string{}
+				for k := range j.Spec.MPIReplicaSpecs {
+					ret[k] = j.Spec.MPIReplicaSpecs[k].Template.Spec.NodeSelector
+				}
+				return ret
+			}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the node selectors
+			initialSelector := nodeSelectors(createdJob)
```

  origina
  ```suggestion
  			originalNodeSelectors := nodeSelectors(createdJob)
  ```

- **test/integration/controller/mpijob/mpijob_controller_test.go:595** by [@mimowo](https://github.com/mimowo) — 2023-03-27T10:25:41Z
```diff
@@ -561,4 +561,74 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 
 	})
 
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			job := testingmpijob.MakeMPIJob(jobName, ns.Name).Queue(localQueue.Name).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "3").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "4").
+				Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &kubeflow.MPIJob{}
+
+			nodeSelectors := func(j *kubeflow.MPIJob) map[kubeflow.MPIReplicaType]map[string]string {
+				ret := map[kubeflow.MPIReplicaType]map[string]string{}
+				for k := range j.Spec.MPIReplicaSpecs {
+					ret[k] = j.Spec.MPIReplicaSpecs[k].Template.Spec.NodeSelector
+				}
+				return ret
+			}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the node selectors
+			initialSelector := nodeSelectors(createdJob)
```

  For consistency

- **test/integration/controller/mpijob/mpijob_controller_test.go:626** by [@mimowo](https://github.com/mimowo) — 2023-03-27T10:25:53Z
```diff
@@ -561,4 +561,74 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 
 	})
 
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			job := testingmpijob.MakeMPIJob(jobName, ns.Name).Queue(localQueue.Name).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "3").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "4").
+				Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &kubeflow.MPIJob{}
+
+			nodeSelectors := func(j *kubeflow.MPIJob) map[kubeflow.MPIReplicaType]map[string]string {
+				ret := map[kubeflow.MPIReplicaType]map[string]string{}
+				for k := range j.Spec.MPIReplicaSpecs {
+					ret[k] = j.Spec.MPIReplicaSpecs[k].Template.Spec.NodeSelector
+				}
+				return ret
+			}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the node selectors
+			initialSelector := nodeSelectors(createdJob)
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[kubeflow.MPIReplicaType]map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return nodeSelectors(createdJob)
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
```

  ```suggestion
  			ginkgo.By("the node selectors should be restored", func() {
  ```

- **test/integration/controller/mpijob/mpijob_controller_test.go:622** by [@mimowo](https://github.com/mimowo) — 2023-03-27T10:26:18Z
```diff
@@ -561,4 +561,74 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 
 	})
 
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			job := testingmpijob.MakeMPIJob(jobName, ns.Name).Queue(localQueue.Name).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "3").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "4").
+				Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &kubeflow.MPIJob{}
+
+			nodeSelectors := func(j *kubeflow.MPIJob) map[kubeflow.MPIReplicaType]map[string]string {
+				ret := map[kubeflow.MPIReplicaType]map[string]string{}
+				for k := range j.Spec.MPIReplicaSpecs {
+					ret[k] = j.Spec.MPIReplicaSpecs[k].Template.Spec.NodeSelector
+				}
+				return ret
+			}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the node selectors
+			initialSelector := nodeSelectors(createdJob)
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[kubeflow.MPIReplicaType]map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return nodeSelectors(createdJob)
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
```

  Here I'm also wondering if the race condition is possible.

- **pkg/controller/jobframework/constants.go:36** by [@mimowo](https://github.com/mimowo) — 2023-03-27T10:32:17Z
```diff
@@ -32,4 +32,6 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	OriginalSelectorsAnnotation = "kueue.x-k8s.io/original-selectors"
```

  Please add a comment describing the annotation

- **pkg/controller/jobframework/reconciler.go:313** by [@mimowo](https://github.com/mimowo) — 2023-03-27T10:34:22Z
```diff
@@ -300,7 +306,14 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 
 // startJob will unsuspend the job, and also inject the node affinity.
 func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) error {
-	nodeSelectors, err := r.getNodeSelectors(ctx, wl)
+	log := ctrl.LoggerFrom(ctx)
+	//get the original selectors and store them in the job object
+	originalSelectors := r.getNodeSelectorsFromPodSets(wl)
+	if err := nodeSelectorsSetToObject(object, originalSelectors); err != nil {
+		log.V(3).Error(err, "Unable to set original node selectors")
```

  Shouldn't we return with error here? I'm concerned that we may start the job and later not be able to restore the node selectors.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-27T15:32:03Z

- **pkg/controller/jobframework/constants.go:36** by [@trasc](https://github.com/trasc) — 2023-03-27T11:24:38Z
```diff
@@ -32,4 +32,6 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	OriginalSelectorsAnnotation = "kueue.x-k8s.io/original-selectors"
```

  done

- **pkg/controller/jobframework/constants.go:36** by [@trasc](https://github.com/trasc) — 2023-03-27T11:30:02Z
```diff
@@ -32,4 +32,6 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	OriginalSelectorsAnnotation = "kueue.x-k8s.io/original-selectors"
```

  done

- **pkg/controller/jobframework/reconciler.go:40** by [@trasc](https://github.com/trasc) — 2023-03-27T11:31:22Z
```diff
@@ -34,6 +36,10 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
+var (
+	errSelectorsNotFound = errors.New(OriginalSelectorsAnnotation + " not found")
```

  done

- **pkg/controller/jobframework/reconciler.go:313** by [@trasc](https://github.com/trasc) — 2023-03-27T11:45:46Z
```diff
@@ -300,7 +306,14 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 
 // startJob will unsuspend the job, and also inject the node affinity.
 func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) error {
-	nodeSelectors, err := r.getNodeSelectors(ctx, wl)
+	log := ctrl.LoggerFrom(ctx)
+	//get the original selectors and store them in the job object
+	originalSelectors := r.getNodeSelectorsFromPodSets(wl)
+	if err := nodeSelectorsSetToObject(object, originalSelectors); err != nil {
+		log.V(3).Error(err, "Unable to set original node selectors")
```

  I don't think it could help.
  - An error here can only be related to marshaling the the slice, which in my opinion, given the data type we are using, is impossible. 
  - If some marshaling error somehow is generated,  it's unlikely that at the same iteration (reconcile call) will succeed.
  - The data in the annotation should only be used if the workload is deleted.

- **pkg/controller/jobframework/reconciler.go:350** by [@trasc](https://github.com/trasc) — 2023-03-27T11:46:29Z
```diff
@@ -333,8 +347,20 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
+	var originalSelectors []map[string]string
```

  done

- **pkg/controller/jobframework/reconciler.go:352** by [@trasc](https://github.com/trasc) — 2023-03-27T14:29:39Z
```diff
@@ -333,8 +347,20 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
+	var originalSelectors []map[string]string
 	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+		originalSelectors = r.getNodeSelectorsFromPodSets(wl)
```

  It should be faster at runtime and provides an alternative source of old selectors if somehow the annotation is lost , however we could drop it for now.

- **pkg/controller/jobframework/reconciler.go:357** by [@trasc](https://github.com/trasc) — 2023-03-27T14:33:11Z
```diff
@@ -333,8 +347,20 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
+	var originalSelectors []map[string]string
 	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+		originalSelectors = r.getNodeSelectorsFromPodSets(wl)
+	} else {
+		log.V(3).Info("restore node selectors from annotation")
+		selectors, err := nodeSelectorsFromObject(object)
+		if err != nil {
+			log.V(3).Info("Unable to get original node selectors", "error", err)
```

  done

- **pkg/controller/jobframework/reconciler.go:362** by [@trasc](https://github.com/trasc) — 2023-03-27T14:36:46Z
```diff
@@ -333,8 +347,20 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
+	var originalSelectors []map[string]string
 	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+		originalSelectors = r.getNodeSelectorsFromPodSets(wl)
+	} else {
+		log.V(3).Info("restore node selectors from annotation")
+		selectors, err := nodeSelectorsFromObject(object)
+		if err != nil {
+			log.V(3).Info("Unable to get original node selectors", "error", err)
+		} else {
+			originalSelectors = selectors
+		}
+	}
+	if len(originalSelectors) > 0 {
```

  > IIUC the only reason for not restoring the original node selectors would be an error during unmarhaling. I suggest to directly check for the error here to express the intention.
  
  Not only, at the initial stop, it's expected for the annotation to not be found.
  
  
  
  > Is there a reproducible scenario when the unmarshaling fails, say can a malicious actor set node selectors which cannot unmarshalled? If so, it would be good to add an integration test for it, to verify the job gets suspended and there is no crash.
  
  I'll add one

- **pkg/controller/jobframework/reconciler.go:485** by [@trasc](https://github.com/trasc) — 2023-03-27T14:38:14Z
```diff
@@ -442,3 +481,45 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func nodeSelectorClone(src map[string]string) map[string]string {
```

  done

- **pkg/controller/jobframework/reconciler.go:495** by [@trasc](https://github.com/trasc) — 2023-03-27T14:40:12Z
```diff
@@ -442,3 +481,45 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func nodeSelectorClone(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// nodeSelectorsFromObject tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not fond or is unable to unmarshal
+func nodeSelectorsFromObject(obj client.Object) ([]map[string]string, error) {
```

  done

- **pkg/controller/jobframework/reconciler.go:502** by [@trasc](https://github.com/trasc) — 2023-03-27T14:41:02Z
```diff
@@ -442,3 +481,45 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func nodeSelectorClone(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// nodeSelectorsFromObject tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not fond or is unable to unmarshal
+func nodeSelectorsFromObject(obj client.Object) ([]map[string]string, error) {
+	str, found := obj.GetAnnotations()[OriginalSelectorsAnnotation]
+	if !found {
+		return nil, errSelectorsNotFound
+	}
+	// unmarshal
+	ret := []map[string]string{}
+	err := json.Unmarshal([]byte(str), &ret)
```

  done

- **pkg/controller/jobframework/reconciler.go:500** by [@trasc](https://github.com/trasc) — 2023-03-27T14:41:41Z
```diff
@@ -442,3 +481,45 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func nodeSelectorClone(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// nodeSelectorsFromObject tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not fond or is unable to unmarshal
+func nodeSelectorsFromObject(obj client.Object) ([]map[string]string, error) {
+	str, found := obj.GetAnnotations()[OriginalSelectorsAnnotation]
+	if !found {
+		return nil, errSelectorsNotFound
+	}
+	// unmarshal
+	ret := []map[string]string{}
+	err := json.Unmarshal([]byte(str), &ret)
+	if err != nil {
+		return nil, err
+	}
+	return ret, nil
+}
+
+// nodeSelectorsSetToObject - sets an annotation containing the provided node selectors into
+// a job object, even if very unlikely it could return an error related to json.marshaling
+func nodeSelectorsSetToObject(obj client.Object, nodeSelectors []map[string]string) error {
```

  done

- **test/integration/controller/job/job_controller_test.go:881** by [@trasc](https://github.com/trasc) — 2023-03-27T14:51:47Z
```diff
@@ -831,4 +831,65 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			initialSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
```

  Deleting the queue or creating something with higher priority might stop the job while the workload is in canceling admission and  not because the workload gets deleted. However since now we no longer restore from the podSets it will make no difference.

- **test/integration/controller/mpijob/mpijob_controller_test.go:595** by [@trasc](https://github.com/trasc) — 2023-03-27T14:58:09Z
```diff
@@ -561,4 +561,74 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 
 	})
 
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			job := testingmpijob.MakeMPIJob(jobName, ns.Name).Queue(localQueue.Name).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "3").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "4").
+				Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &kubeflow.MPIJob{}
+
+			nodeSelectors := func(j *kubeflow.MPIJob) map[kubeflow.MPIReplicaType]map[string]string {
+				ret := map[kubeflow.MPIReplicaType]map[string]string{}
+				for k := range j.Spec.MPIReplicaSpecs {
+					ret[k] = j.Spec.MPIReplicaSpecs[k].Template.Spec.NodeSelector
+				}
+				return ret
+			}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the node selectors
+			initialSelector := nodeSelectors(createdJob)
```

  done

- **test/integration/controller/mpijob/mpijob_controller_test.go:626** by [@trasc](https://github.com/trasc) — 2023-03-27T14:58:51Z
```diff
@@ -561,4 +561,74 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 
 	})
 
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			job := testingmpijob.MakeMPIJob(jobName, ns.Name).Queue(localQueue.Name).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "3").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "4").
+				Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &kubeflow.MPIJob{}
+
+			nodeSelectors := func(j *kubeflow.MPIJob) map[kubeflow.MPIReplicaType]map[string]string {
+				ret := map[kubeflow.MPIReplicaType]map[string]string{}
+				for k := range j.Spec.MPIReplicaSpecs {
+					ret[k] = j.Spec.MPIReplicaSpecs[k].Template.Spec.NodeSelector
+				}
+				return ret
+			}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the node selectors
+			initialSelector := nodeSelectors(createdJob)
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[kubeflow.MPIReplicaType]map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return nodeSelectors(createdJob)
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-27T15:34:20Z

- **test/integration/controller/mpijob/mpijob_controller_test.go:622** by [@trasc](https://github.com/trasc) — 2023-03-27T15:34:19Z
```diff
@@ -561,4 +561,74 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 
 	})
 
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			job := testingmpijob.MakeMPIJob(jobName, ns.Name).Queue(localQueue.Name).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "3").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "4").
+				Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &kubeflow.MPIJob{}
+
+			nodeSelectors := func(j *kubeflow.MPIJob) map[kubeflow.MPIReplicaType]map[string]string {
+				ret := map[kubeflow.MPIReplicaType]map[string]string{}
+				for k := range j.Spec.MPIReplicaSpecs {
+					ret[k] = j.Spec.MPIReplicaSpecs[k].Template.Spec.NodeSelector
+				}
+				return ret
+			}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the node selectors
+			initialSelector := nodeSelectors(createdJob)
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[kubeflow.MPIReplicaType]map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return nodeSelectors(createdJob)
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
```

  the order is not relevant now

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-03-28T10:04:10Z

- **pkg/controller/jobframework/constants.go:40** by [@kerthcet](https://github.com/kerthcet) — 2023-03-28T10:04:06Z
```diff
@@ -32,4 +32,10 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	// OriginalNodeSelectorsAnnotation is the annotation in which the original
+	// node selectors are recorded upon a workload admission. This information,
+	// if present, will be used to restore them if a workload is deleted while
+	// it is admitted. The content is a json marshaled slice of selectors.
+	OriginalNodeSelectorsAnnotation = "kueue.x-k8s.io/original-node-selectors"
```

  Have we weighted the suggestion? https://github.com/kubernetes-sigs/kueue/issues/518#issuecomment-1461454684

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-28T10:10:42Z

- **pkg/controller/jobframework/constants.go:40** by [@trasc](https://github.com/trasc) — 2023-03-28T10:10:42Z
```diff
@@ -32,4 +32,10 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	// OriginalNodeSelectorsAnnotation is the annotation in which the original
+	// node selectors are recorded upon a workload admission. This information,
+	// if present, will be used to restore them if a workload is deleted while
+	// it is admitted. The content is a json marshaled slice of selectors.
+	OriginalNodeSelectorsAnnotation = "kueue.x-k8s.io/original-node-selectors"
```

  Yes, but it will unnecessary complicate the workload lifecycle. Also setting the annotation is done without any extra api calls.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-28T10:34:47Z

- **pkg/controller/jobframework/reconciler.go:313** by [@mimowo](https://github.com/mimowo) — 2023-03-28T10:34:46Z
```diff
@@ -300,7 +306,14 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 
 // startJob will unsuspend the job, and also inject the node affinity.
 func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) error {
-	nodeSelectors, err := r.getNodeSelectors(ctx, wl)
+	log := ctrl.LoggerFrom(ctx)
+	//get the original selectors and store them in the job object
+	originalSelectors := r.getNodeSelectorsFromPodSets(wl)
+	if err := nodeSelectorsSetToObject(object, originalSelectors); err != nil {
+		log.V(3).Error(err, "Unable to set original node selectors")
```

  It might be only caused by marshaling today, but it is not obvious when calling the function. I think it is quite possible the `nodeSelectorsSetToObject` function is modified in the future and can error for a different reason resulting in a subtle bug. I think the intention is to keep the job suspended in case of any errors. 
  
  - ` it's unlikely that at the same iteration (reconcile call) will succeed.` I'm not sure I understand, why would it fail if we silence the error?
  
  - `The data in the annotation should only be used if the workload is deleted.`. How is this relevant here? IIUC the current code will lookup the annotation on `stopJob` even if the workload isn't deleted. Second, a malicious actor could delete the workload if knowing this would cause the annotation lookup when the annotation isn't set.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-28T10:51:19Z

- **test/integration/controller/job/job_controller_test.go:881** by [@mimowo](https://github.com/mimowo) — 2023-03-28T10:51:19Z
```diff
@@ -831,4 +831,65 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			initialSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
```

  IIUC it is possible that the workload is re-admitted between ""delete the workload" and "delete the localQueue, prevent readmission" steps. Thus, making the "delete the workload" step unnecessary. Would the test pass if you delete it?
  
  > creating something with higher priority might stop the job while the workload is in canceling admission and not because the workload gets deleted. 
  
  What I meant is to create another (second) job, before the "delete the workload" step. Then, when we delete the workload, and the first job is unadmitted, scheduler will pick the second job with the higher priority. The second job admitted would block admission of the first job because it would use all resources. This way we will be able to check the node selectors of the first (unadmitted due to deleted workload) job without a risk of race conditions.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-28T11:03:28Z

- **test/integration/controller/job/job_controller_test.go:933** by [@mimowo](https://github.com/mimowo) — 2023-03-28T11:01:05Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelector))
+			})
+		})
+
+		ginkgo.It("Should continue to use the admission node selectors if the annotation is corrupted", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			runningNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("change the annotation", func() {
```

  I'm wondering if this might be problematic. If a malicious user can update the annotation while the job is running and delete the workload, then the `stopJob` function will revert the `nodeSelectors` to the malicious value. 
  
  It seems we should extend webhooks to make the annotation immutable while the job is unsuspended. WDYT @alculquicondor

- **pkg/controller/jobframework/reconciler.go:353** by [@mimowo](https://github.com/mimowo) — 2023-03-28T11:03:02Z
```diff
@@ -333,8 +347,16 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		if errors.Is(err, errNodeSelectorsNotFound) {
+			log.V(3).Info("No original node selectors found")
```

  Why do we distiguish the two cases? It seems the absence of the annotation is an indication of an error? Is there any scenario under which the annotation might be not set?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-28T11:22:12Z

- **pkg/controller/jobframework/reconciler.go:353** by [@trasc](https://github.com/trasc) — 2023-03-28T11:22:12Z
```diff
@@ -333,8 +347,16 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		if errors.Is(err, errNodeSelectorsNotFound) {
+			log.V(3).Info("No original node selectors found")
```

  Yes, at the initial `stopJob`, before any workload is created, or if the workload is deleted before being accepted.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-28T11:58:21Z

- **test/integration/controller/job/job_controller_test.go:881** by [@trasc](https://github.com/trasc) — 2023-03-28T11:58:21Z
```diff
@@ -831,4 +831,65 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			initialSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
```

  > Thus, making the "delete the workload" step unnecessary. Would the test pass if you delete it?
  
  No it will not, the workload will not be deleted in one `util.Timeout` interval.
  
  However , we just need the job to be stopped and stay in a suspended  state, and it works.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-28T12:05:05Z

- **pkg/controller/jobframework/reconciler.go:313** by [@trasc](https://github.com/trasc) — 2023-03-28T12:05:05Z
```diff
@@ -300,7 +306,14 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 
 // startJob will unsuspend the job, and also inject the node affinity.
 func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) error {
-	nodeSelectors, err := r.getNodeSelectors(ctx, wl)
+	log := ctrl.LoggerFrom(ctx)
+	//get the original selectors and store them in the job object
+	originalSelectors := r.getNodeSelectorsFromPodSets(wl)
+	if err := nodeSelectorsSetToObject(object, originalSelectors); err != nil {
+		log.V(3).Error(err, "Unable to set original node selectors")
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-28T18:30:43Z

- **test/integration/controller/job/job_controller_test.go:933** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-28T18:30:43Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelector))
+			})
+		})
+
+		ginkgo.It("Should continue to use the admission node selectors if the annotation is corrupted", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			runningNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("change the annotation", func() {
```

  > It seems we should extend webhooks to make the annotation immutable while the job is unsuspended
  
  that's a good suggestion

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-29T07:56:38Z

- **test/integration/controller/job/job_controller_test.go:933** by [@trasc](https://github.com/trasc) — 2023-03-29T07:56:37Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelector))
+			})
+		})
+
+		ginkgo.It("Should continue to use the admission node selectors if the annotation is corrupted", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			runningNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("change the annotation", func() {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-29T10:10:41Z

- **test/integration/controller/job/job_controller_test.go:893** by [@mimowo](https://github.com/mimowo) — 2023-03-29T09:56:15Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelector))
+			})
+		})
+
+		ginkgo.It("Should continue to use the admission node selectors if the annotation is corrupted", func() {
```

  IIUC, we don't need this test in the current form since when the webhook is registered the update would fail. 
  
  We may add another intergration test for the update being rejected in mpijob_webhook_test.go. Analogous to the suite for Job, where we have job_webhook_test.go.

- **test/integration/controller/job/job_controller_test.go:835** by [@mimowo](https://github.com/mimowo) — 2023-03-29T10:01:20Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
```

  IIUC we don't need the scheduler in this test, and in fact its presence complicates reasoning (see another comment where the localQueue is deleted for non-obvious reasons). The modified code does not touch the scheduler, so, I suggest to simplify the tests and moving them to the set of tests without the scheduler.

- **test/integration/controller/job/job_controller_test.go:881** by [@mimowo](https://github.com/mimowo) — 2023-03-29T10:10:35Z
```diff
@@ -831,4 +831,65 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			initialSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
```

  There is a race condition though in the test. Just insert
  ```golang 
  time.Sleep(time.Second)
  ```
  between "delete the workload" and "delete the localQueue, prevent readmission". I assume this could be prevented by the second workload. 
  
  But on the other hand, do we benefit from scheduler in this test? I assume we can simplify the test and move to the section without scheduler (see related comment).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-29T10:35:49Z

- **test/integration/controller/job/job_controller_test.go:835** by [@trasc](https://github.com/trasc) — 2023-03-29T10:35:49Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
```

  We just need the workload to be admitted, we don't check any scheduler specific conditions. Forcing a manual admission will make the test code tightly coupled with workload internals like admission structure content  and status condition management.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-29T11:13:09Z

- **test/integration/controller/job/job_controller_test.go:835** by [@mimowo](https://github.com/mimowo) — 2023-03-29T11:13:09Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
```

  Indeed, we would need to admit the workload manually like here: https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/controller/job/job_controller_test.go#L149-L157. 
  
  I think it is ok for integration tests to depend on the workload API. Anyway, the tests in the current form need to "know" about a workload as they delete it in the "delete the workload" step. What's more, they need to know about scheduler as they delete the local queue (but even then resulting in the race conditions as pointed out). Thus, I think the dependency on the workload admission structure is lighter.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-29T11:35:45Z

- **test/integration/controller/job/job_controller_test.go:881** by [@trasc](https://github.com/trasc) — 2023-03-29T11:35:45Z
```diff
@@ -831,4 +831,65 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			initialSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
```

  I've changed the order of the operations and try to describe the steps better. I rally don't believe that adding an extra workload with higher priority to keep the first one from being admitted again is a more clear approach.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-29T12:01:33Z

- **test/integration/controller/job/job_controller_test.go:881** by [@mimowo](https://github.com/mimowo) — 2023-03-29T12:01:33Z
```diff
@@ -831,4 +831,65 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			initialSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(initialSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
```

  Thanks, this should help with the race condition. 
  
  While I find it acceptable, I still think though we will be better of by dropping the scheduler from the tests in the first place. And the fact we got this bug is to me an argument that the dependency on scheduler is conceptually heavier dependency than than using workload API for admission. WDYT @alculquicondor ?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-29T12:46:26Z

- **test/integration/controller/job/job_controller_test.go:835** by [@trasc](https://github.com/trasc) — 2023-03-29T12:46:25Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
```

  That approach is hard to maintain. A good number of the tests doing it needed manual rework when I worked on #653.  
  
  The scheduler is already part of this integration test suite and doing it's job. I strongly believe we should keep it as is.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-30T15:54:33Z

- **pkg/controller/jobframework/constants.go:38** by [@mimowo](https://github.com/mimowo) — 2023-03-30T15:31:39Z
```diff
@@ -32,4 +32,10 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	// OriginalNodeSelectorsAnnotation is the annotation in which the original
+	// node selectors are recorded upon a workload admission. This information,
+	// if present, will be used to restore them if a workload is deleted while
```

  Maybe "when suspending the job" instead of "if a workload is deleted while it is admitted"? 
  
  Note that, the annotation is used to restore the node selectors not only in case if the workload is deleted, but also on regular job suspend.

- **pkg/controller/jobframework/reconciler.go:353** by [@mimowo](https://github.com/mimowo) — 2023-03-30T15:49:06Z
```diff
@@ -333,8 +347,16 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		if errors.Is(err, errNodeSelectorsNotFound) {
+			log.V(3).Info("No original node selectors found")
```

  Can you elaborate more on these scenarios, possibly indicate the code paths that reach to calling stopJob.
  I can see two invocations of stopJob in code, but not sure which scenario goes via which path:
  1. https://github.com/kubernetes-sigs/kueue/blob/ee2a9a2634489c7ad449b390d8640aa7a739258e/pkg/controller/jobframework/reconciler.go#L259
  2. https://github.com/kubernetes-sigs/kueue/blob/ee2a9a2634489c7ad449b390d8640aa7a739258e/pkg/controller/jobframework/reconciler.go#L195
  
  Note that we have a webhook that makes a newly created job suspended: https://github.com/kubernetes-sigs/kueue/blob/ee2a9a2634489c7ad449b390d8640aa7a739258e/pkg/controller/jobs/job/job_webhook.go#L74

- **pkg/controller/jobframework/reconciler.go:489** by [@mimowo](https://github.com/mimowo) — 2023-03-30T15:49:52Z
```diff
@@ -442,3 +476,44 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func cloneModeSelector(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// getNodeSelectorsFromObjectAnnotation tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not fond or is unable to unmarshal
```

  ```suggestion
  // object's annotations fails if it's not found or is unable to unmarshal
  ```

- **test/integration/controller/job/job_controller_test.go:835** by [@mimowo](https://github.com/mimowo) — 2023-03-30T15:52:08Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
```

  I see pros and cons, but I'm still leaning towards decoupling the test from scheduler. However, both approaches are acceptable to me.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-31T07:26:23Z

Generally LGTM, but would like to better understand if there is a scenario (and what it is) that the annotation might be missing on `stopJob`: https://github.com/kubernetes-sigs/kueue/pull/660/files#r1150412190. IIUC if we set the annotation on unsuspend and it is immutable, then it should be present when `stopJob` is called. If there is such a scenario, maybe we could close the gap and eliminate it.

- **pkg/controller/jobframework/reconciler.go:353** by [@mimowo](https://github.com/mimowo) — 2023-03-31T07:20:45Z
```diff
@@ -333,8 +347,16 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		if errors.Is(err, errNodeSelectorsNotFound) {
+			log.V(3).Info("No original node selectors found")
```

  I don't think this is resolved. Please provide more details for the scenarios when the annotation is missing when `stopJob` is called (sequence of steps). 
  
  If there is such a scenario, please add a comment in code so that it is clear for future readers. 
  
  If we don't have a scenario then there is no need for a special case for the Info log - use error always. Also, the we can clean up the comment for `OriginalNodeSelectorsAnnotation`.

- **pkg/controller/jobframework/constants.go:38** by [@mimowo](https://github.com/mimowo) — 2023-03-31T07:21:47Z
```diff
@@ -32,4 +32,10 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	// OriginalNodeSelectorsAnnotation is the annotation in which the original
+	// node selectors are recorded upon a workload admission. This information,
+	// if present, will be used to restore them the job is suspended.
```

  ```suggestion
  	// if present, will be used to restore them when the job is suspended.
  ```
  Also, we could drop `if present` if there is no scenario the the annotation is empty.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-31T08:37:46Z

- **pkg/controller/jobframework/constants.go:38** by [@trasc](https://github.com/trasc) — 2023-03-31T08:37:46Z
```diff
@@ -32,4 +32,10 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	// OriginalNodeSelectorsAnnotation is the annotation in which the original
+	// node selectors are recorded upon a workload admission. This information,
+	// if present, will be used to restore them the job is suspended.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-31T08:38:05Z

- **pkg/controller/jobframework/reconciler.go:353** by [@trasc](https://github.com/trasc) — 2023-03-31T08:38:05Z
```diff
@@ -333,8 +347,16 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		if errors.Is(err, errNodeSelectorsNotFound) {
+			log.V(3).Info("No original node selectors found")
```

  The one case in which the missing annotation is acceptable is when the job is created unsuspended (in my opinion is guaranteed the the web-hook will always work for any kind of job, also the MPIJob integration tests do mot register webhooks) . However let's make them all errors.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-03-31T13:30:39Z

A couple more non-blocking nits. @alculquicondor over to you.

- **pkg/controller/jobs/job/job_webhook_test.go:162** by [@mimowo](https://github.com/mimowo) — 2023-03-31T13:13:25Z
```diff
@@ -156,6 +158,20 @@ func TestValidateUpdate(t *testing.T) {
 				field.Forbidden(parentWorkloadKeyPath, "this annotation is immutable"),
 			},
 		},
+		{
+			name:    "original node selectors can be set while suspended",
```

  ```suggestion
  			name:    "original node selectors can be set while unsuspending",
  ```
  nit: please also add a test case for injecting new annotations while suspending

- **pkg/controller/jobframework/validation.go:79** by [@mimowo](https://github.com/mimowo) — 2023-03-31T13:26:39Z
```diff
@@ -71,3 +73,14 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForOriginalNodeSelectors(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	if !oldJob.IsSuspended() && !newJob.IsSuspended() {
```

  nit: I don't think it matters for business cases, but we could make the job immutable unless it is suspending or unsuspending, it could be expressed easily:
  ```golang
  if oldJob.IsSuspended() != newJob.IsSuspended() {
  ```
  If you go for this, please also adjust the error message accordingly, for example: "this annotation is immutable unless the job is being suspended or unsuspended" (started or stopped)

- **test/integration/controller/job/job_controller_test.go:893** by [@mimowo](https://github.com/mimowo) — 2023-03-31T13:28:02Z
```diff
@@ -831,4 +831,131 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 			return createdProdJob.Spec.Suspend
 		}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+			job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "2").Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &batchv1.Job{}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the podSet's node selector
+			originalNodeSelector := createdJob.Spec.Template.Spec.NodeSelector
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selector should be updated", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelector))
+			})
+
+			ginkgo.By("delete the workload", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the localQueue, prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selector should be restored", func() {
+				gomega.Eventually(func() map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Template.Spec.NodeSelector
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelector))
+			})
+		})
+
+		ginkgo.It("Should continue to use the admission node selectors if the annotation is corrupted", func() {
```

  nit: I would suggest deleting this test case as it shows a scenario which is blocked by webhooks.

- **test/integration/controller/mpijob/mpijob_controller_test.go:634** by [@mimowo](https://github.com/mimowo) — 2023-03-31T13:28:20Z
```diff
@@ -561,4 +561,154 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 
 	})
 
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			job := testingmpijob.MakeMPIJob(jobName, ns.Name).Queue(localQueue.Name).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "3").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "4").
+				Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &kubeflow.MPIJob{}
+
+			nodeSelectors := func(j *kubeflow.MPIJob) map[kubeflow.MPIReplicaType]map[string]string {
+				ret := map[kubeflow.MPIReplicaType]map[string]string{}
+				for k := range j.Spec.MPIReplicaSpecs {
+					ret[k] = j.Spec.MPIReplicaSpecs[k].Template.Spec.NodeSelector
+				}
+				return ret
+			}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the node selectors
+			originalNodeSelectors := nodeSelectors(createdJob)
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selectors should be updated", func() {
+				gomega.Eventually(func() map[kubeflow.MPIReplicaType]map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return nodeSelectors(createdJob)
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelectors))
+			})
+
+			ginkgo.By("delete the localQueue to prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the workload to stop the job", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selectors should be restored", func() {
+				gomega.Eventually(func() map[kubeflow.MPIReplicaType]map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return nodeSelectors(createdJob)
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelectors))
+			})
+		})
+
+		ginkgo.It("Should continue to use the admission node selectors if the annotation is corrupted", func() {
```

  nit: I would suggest deleting this test case as it shows a scenario which is blocked by webhooks.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-31T15:53:11Z

- **pkg/controller/jobframework/constants.go:40** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T15:53:11Z
```diff
@@ -32,4 +32,10 @@ const (
 	// ignores this Job from admission, and takes control of its suspension
 	// status based on the admission status of the parent workload.
 	ParentWorkloadAnnotation = "kueue.x-k8s.io/parent-workload"
+
+	// OriginalNodeSelectorsAnnotation is the annotation in which the original
+	// node selectors are recorded upon a workload admission. This information,
+	// if present, will be used to restore them if a workload is deleted while
+	// it is admitted. The content is a json marshaled slice of selectors.
+	OriginalNodeSelectorsAnnotation = "kueue.x-k8s.io/original-node-selectors"
```

  The annotation is also useful as a record for users to look at

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-31T16:16:59Z

- **pkg/controller/jobframework/reconciler.go:40** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T15:55:35Z
```diff
@@ -34,6 +36,10 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
+var (
+	errNodeSelectorsNotFound = errors.New(OriginalNodeSelectorsAnnotation + " not found")
```

  ```suggestion
  	errNodeSelectorsNotFound = fmt.Errorf("annotation %s not found", OriginalNodeSelectorsAnnotation)
  ```

- **pkg/controller/jobframework/reconciler.go:353** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T16:04:16Z
```diff
@@ -333,8 +346,12 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		log.V(3).Error(err, "Unable to get original node selectors")
+	} else {
+		job.RestoreNodeAffinity(selectors)
```

  we could fallback into getting the selectors from the workload object

- **pkg/controller/jobframework/reconciler.go:476** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T16:08:09Z
```diff
@@ -442,3 +472,44 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func cloneModeSelector(src map[string]string) map[string]string {
```

  ```suggestion
  func cloneNodeSelector(src map[string]string) map[string]string {
  ```

- **pkg/controller/jobframework/validation.go:79** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T16:11:04Z
```diff
@@ -71,3 +73,14 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForOriginalNodeSelectors(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
```

  Also validate that the selectors are valid

- **pkg/controller/jobs/mpijob/mpijob_controller.go:133** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T16:15:42Z
```diff
@@ -125,11 +125,10 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	}
 }
 
-func (j *MPIJob) RestoreNodeAffinity(podSets []kueue.PodSet) {
+func (j *MPIJob) RestoreNodeAffinity(nodeSelectors []map[string]string) {
 	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
-	for index := range podSets {
+	for index, nodeSelector := range nodeSelectors {
 		replicaType := orderedReplicaTypes[index]
```

  the serialization is assuming that podsets can be ordered. While this works for Job and MPIJob, I prefer not to make that assumption in general. Let's add the name of the podset to the serialization.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-31T18:02:33Z

- **pkg/controller/jobframework/reconciler.go:353** by [@trasc](https://github.com/trasc) — 2023-03-31T18:02:33Z
```diff
@@ -333,8 +346,12 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		log.V(3).Error(err, "Unable to get original node selectors")
+	} else {
+		job.RestoreNodeAffinity(selectors)
```

  It was my original approach but was change during the review.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-31T18:08:20Z

- **pkg/controller/jobframework/validation.go:79** by [@trasc](https://github.com/trasc) — 2023-03-31T18:08:20Z
```diff
@@ -71,3 +73,14 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForOriginalNodeSelectors(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
```

  The added value is very limited this being the same validation that makes sure that the annotation can not be set while the job is not suspended, the cost being a reflection based u marshaling.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-31T18:26:14Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:133** by [@trasc](https://github.com/trasc) — 2023-03-31T18:26:14Z
```diff
@@ -125,11 +125,10 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	}
 }
 
-func (j *MPIJob) RestoreNodeAffinity(podSets []kueue.PodSet) {
+func (j *MPIJob) RestoreNodeAffinity(nodeSelectors []map[string]string) {
 	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
-	for index := range podSets {
+	for index, nodeSelector := range nodeSelectors {
 		replicaType := orderedReplicaTypes[index]
```

  The same approach is used to provide the pod sets to the workload. Sure it could be done but it will require the change of the source for original selectors (get them from them from the job not workload) ... Maybe we can create a follow-up issue for this.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-31T19:07:18Z

- **pkg/controller/jobframework/validation.go:79** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T19:07:18Z
```diff
@@ -71,3 +73,14 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForOriginalNodeSelectors(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
```

  you can just validate that the annotation correctly unmarshals.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-31T19:09:29Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:133** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T19:09:29Z
```diff
@@ -125,11 +125,10 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	}
 }
 
-func (j *MPIJob) RestoreNodeAffinity(podSets []kueue.PodSet) {
+func (j *MPIJob) RestoreNodeAffinity(nodeSelectors []map[string]string) {
 	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
-	for index := range podSets {
+	for index, nodeSelector := range nodeSelectors {
 		replicaType := orderedReplicaTypes[index]
```

  ```golang
  type PodSetNodeSelector struct {
    Name string `json:"name"`
    NodeSelector map[string]string `json:"nodeSelector"`
  }
  ```
  
  Then you serialize/deserialize `[]PodSetNodeSelector`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-31T19:11:12Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:133** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T19:11:11Z
```diff
@@ -125,11 +125,10 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	}
 }
 
-func (j *MPIJob) RestoreNodeAffinity(podSets []kueue.PodSet) {
+func (j *MPIJob) RestoreNodeAffinity(nodeSelectors []map[string]string) {
 	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
-	for index := range podSets {
+	for index, nodeSelector := range nodeSelectors {
 		replicaType := orderedReplicaTypes[index]
```

  These kind of changes cannot be easily done in follow ups if we want to provide backwards compatibility guarantees.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-03-31T19:22:48Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:133** by [@trasc](https://github.com/trasc) — 2023-03-31T19:22:47Z
```diff
@@ -125,11 +125,10 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	}
 }
 
-func (j *MPIJob) RestoreNodeAffinity(podSets []kueue.PodSet) {
+func (j *MPIJob) RestoreNodeAffinity(nodeSelectors []map[string]string) {
 	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
-	for index := range podSets {
+	for index, nodeSelector := range nodeSelectors {
 		replicaType := orderedReplicaTypes[index]
```

  The name of the podset cold be change within a workload, by the defaulting web hook, there is no guarantee tht the name you get from the workload can be used in restore.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-03-31T19:30:34Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:133** by [@alculquicondor](https://github.com/alculquicondor) — 2023-03-31T19:30:34Z
```diff
@@ -125,11 +125,10 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	}
 }
 
-func (j *MPIJob) RestoreNodeAffinity(podSets []kueue.PodSet) {
+func (j *MPIJob) RestoreNodeAffinity(nodeSelectors []map[string]string) {
 	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
-	for index := range podSets {
+	for index, nodeSelector := range nodeSelectors {
 		replicaType := orderedReplicaTypes[index]
```

  if a defaulting webhook changes the name that's ok.
  
  You should have the name defaulted at the time when the workload is admitted.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-04-03T05:46:55Z

- **pkg/controller/jobframework/reconciler.go:353** by [@mimowo](https://github.com/mimowo) — 2023-04-03T05:46:55Z
```diff
@@ -333,8 +346,12 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		log.V(3).Error(err, "Unable to get original node selectors")
+	} else {
+		job.RestoreNodeAffinity(selectors)
```

  IIRC in the original approach you first looked up the workload object and then fallback to annotation. 
  
  Anyway, I think that once we made the annotation immutable, we can fully rely on it, unless I'm missing some scenario. 
  
  If such a scenario exists my point was that we should add a comment why this is done (what is the scenario). Otherwise we will end up in a suspicious code which no-one remembers / knows why needed.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-03T06:12:23Z

- **pkg/controller/jobframework/reconciler.go:353** by [@trasc](https://github.com/trasc) — 2023-04-03T06:12:23Z
```diff
@@ -333,8 +346,12 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		log.V(3).Error(err, "Unable to get original node selectors")
+	} else {
+		job.RestoreNodeAffinity(selectors)
```

  The order was inverted due to performance reasons, getting the selectors from the workload  wold not have needed additional un-marshaling.
  
  The scenario wold be when a job is missing the annotation,  could happen if the job (other than core.Job or MPIJob) in question is not blocking the change of the annotation while running.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-03T06:16:48Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:133** by [@trasc](https://github.com/trasc) — 2023-04-03T06:16:48Z
```diff
@@ -125,11 +125,10 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	}
 }
 
-func (j *MPIJob) RestoreNodeAffinity(podSets []kueue.PodSet) {
+func (j *MPIJob) RestoreNodeAffinity(nodeSelectors []map[string]string) {
 	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
-	for index := range podSets {
+	for index, nodeSelector := range nodeSelectors {
 		replicaType := orderedReplicaTypes[index]
```

  But that means that you cannot (100% reliably ) use the name in the restore logic.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-04-03T06:20:44Z

- **pkg/controller/jobframework/reconciler.go:353** by [@mimowo](https://github.com/mimowo) — 2023-04-03T06:20:44Z
```diff
@@ -333,8 +346,12 @@ func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object clie
 		}
 	}
 
-	if wl != nil {
-		job.RestoreNodeAffinity(wl.Spec.PodSets)
+	log.V(3).Info("restore node selectors from annotation")
+	selectors, err := getNodeSelectorsFromObjectAnnotation(object)
+	if err != nil {
+		log.V(3).Error(err, "Unable to get original node selectors")
+	} else {
+		job.RestoreNodeAffinity(selectors)
```

  Yes, but we have a webhook to prevent the change. So, the only scenario is that the webhook is malfunctioning for some reason and we have a bad actor. 
  
  However, if the webhook is malfunctioning and we have a bad actor, the actor could both modify the annotation and delete the workload so the fallback would not work either. So, iiuc, it would not be bullet proof either, just misleadingly making that impression.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-03T07:28:16Z

- **pkg/controller/jobframework/reconciler.go:40** by [@trasc](https://github.com/trasc) — 2023-04-03T06:20:19Z
```diff
@@ -34,6 +36,10 @@ import (
 	"sigs.k8s.io/kueue/pkg/workload"
 )
 
+var (
+	errNodeSelectorsNotFound = errors.New(OriginalNodeSelectorsAnnotation + " not found")
```

  done

- **pkg/controller/jobframework/reconciler.go:476** by [@trasc](https://github.com/trasc) — 2023-04-03T06:22:01Z
```diff
@@ -442,3 +472,44 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func cloneModeSelector(src map[string]string) map[string]string {
```

  done

- **pkg/controller/jobframework/validation.go:79** by [@trasc](https://github.com/trasc) — 2023-04-03T06:31:51Z
```diff
@@ -71,3 +73,14 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForOriginalNodeSelectors(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
```

  done

- **pkg/controller/jobframework/validation.go:79** by [@trasc](https://github.com/trasc) — 2023-04-03T06:40:05Z
```diff
@@ -71,3 +73,14 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForOriginalNodeSelectors(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	if !oldJob.IsSuspended() && !newJob.IsSuspended() {
```

  done

- **pkg/controller/jobs/job/job_webhook_test.go:162** by [@trasc](https://github.com/trasc) — 2023-04-03T07:03:59Z
```diff
@@ -156,6 +158,20 @@ func TestValidateUpdate(t *testing.T) {
 				field.Forbidden(parentWorkloadKeyPath, "this annotation is immutable"),
 			},
 		},
+		{
+			name:    "original node selectors can be set while suspended",
```

  done

- **test/integration/controller/mpijob/mpijob_controller_test.go:634** by [@trasc](https://github.com/trasc) — 2023-04-03T07:17:40Z
```diff
@@ -561,4 +561,154 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", func() {
 
 	})
 
+	ginkgo.When("The workload is deleted while it's admitted", func() {
+		ginkgo.It("Should restore the original node selectors", func() {
+
+			localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+			job := testingmpijob.MakeMPIJob(jobName, ns.Name).Queue(localQueue.Name).
+				Request(kubeflow.MPIReplicaTypeLauncher, corev1.ResourceCPU, "3").
+				Request(kubeflow.MPIReplicaTypeWorker, corev1.ResourceCPU, "4").
+				Obj()
+			lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+			createdJob := &kubeflow.MPIJob{}
+
+			nodeSelectors := func(j *kubeflow.MPIJob) map[kubeflow.MPIReplicaType]map[string]string {
+				ret := map[kubeflow.MPIReplicaType]map[string]string{}
+				for k := range j.Spec.MPIReplicaSpecs {
+					ret[k] = j.Spec.MPIReplicaSpecs[k].Template.Spec.NodeSelector
+				}
+				return ret
+			}
+
+			ginkgo.By("create a job", func() {
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be suspend", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(true)))
+			})
+
+			// backup the the node selectors
+			originalNodeSelectors := nodeSelectors(createdJob)
+
+			ginkgo.By("create a localQueue", func() {
+				gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("job should be unsuspended", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(pointer.Bool(false)))
+			})
+
+			ginkgo.By("the node selectors should be updated", func() {
+				gomega.Eventually(func() map[kubeflow.MPIReplicaType]map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return nodeSelectors(createdJob)
+				}, util.Timeout, util.Interval).ShouldNot(gomega.Equal(originalNodeSelectors))
+			})
+
+			ginkgo.By("delete the localQueue to prevent readmission", func() {
+				gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("delete the workload to stop the job", func() {
+				wl := &kueue.Workload{}
+				wlKey := types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(job.Name), Namespace: job.Namespace}
+				gomega.Expect(k8sClient.Get(ctx, wlKey, wl)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkload(ctx, k8sClient, wl)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("the node selectors should be restored", func() {
+				gomega.Eventually(func() map[kubeflow.MPIReplicaType]map[string]string {
+					gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+					return nodeSelectors(createdJob)
+				}, util.Timeout, util.Interval).Should(gomega.Equal(originalNodeSelectors))
+			})
+		})
+
+		ginkgo.It("Should continue to use the admission node selectors if the annotation is corrupted", func() {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-03T09:19:55Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:133** by [@trasc](https://github.com/trasc) — 2023-04-03T09:19:54Z
```diff
@@ -125,11 +125,10 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	}
 }
 
-func (j *MPIJob) RestoreNodeAffinity(podSets []kueue.PodSet) {
+func (j *MPIJob) RestoreNodeAffinity(nodeSelectors []map[string]string) {
 	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
-	for index := range podSets {
+	for index, nodeSelector := range nodeSelectors {
 		replicaType := orderedReplicaTypes[index]
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-04-03T18:02:03Z

- **pkg/controller/jobframework/validation.go:78** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-03T17:47:00Z
```diff
@@ -71,3 +74,19 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForOriginalNodeSelectors(oldJob, newJob GenericJob) field.ErrorList {
```

  add unit tests

- **pkg/controller/jobframework/reconciler.go:512** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-03T17:52:55Z
```diff
@@ -442,3 +483,44 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func cloneNodeSelector(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// getNodeSelectorsFromObjectAnnotation tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not found or is unable to unmarshal
+func getNodeSelectorsFromObjectAnnotation(obj client.Object) ([]PodSetNodeSelector, error) {
+	str, found := obj.GetAnnotations()[OriginalNodeSelectorsAnnotation]
+	if !found {
+		return nil, errNodeSelectorsNotFound
+	}
+	// unmarshal
+	ret := []PodSetNodeSelector{}
+	if err := json.Unmarshal([]byte(str), &ret); err != nil {
+		return nil, err
+	}
+	return ret, nil
+}
+
+// nodeSelectorsSetToObject - sets an annotation containing the provided node selectors into
+// a job object, even if very unlikely it could return an error related to json.marshaling
+func nodeSelectorsSetToObject(obj client.Object, nodeSelectors []PodSetNodeSelector) error {
```

  ```suggestion
  func setNodeSelectorsInAnnotation(obj client.Object, nodeSelectors []PodSetNodeSelector) error {
  ```

- **pkg/controller/jobs/mpijob/mpijob_controller.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2023-04-03T17:57:50Z
```diff
@@ -113,26 +113,25 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	for index := range nodeSelectors {
```

  Add a comment that this function uses the same order of replicaTypes as `PodSets`, so that's why they match.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-05T05:42:25Z

- **pkg/controller/jobframework/validation.go:78** by [@trasc](https://github.com/trasc) — 2023-04-05T05:42:25Z
```diff
@@ -71,3 +74,19 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForOriginalNodeSelectors(oldJob, newJob GenericJob) field.ErrorList {
```

  This is tested by the job and mpijob unit tests.
  
   Maybe it could make sense to have a GenericJob test double that could be used in unit tests for this package, however this should be done in a follow-up and should cover more that `ValidateUpdateForOriginalNodeSelectors`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-05T05:53:09Z

- **pkg/controller/jobframework/reconciler.go:512** by [@trasc](https://github.com/trasc) — 2023-04-05T05:53:09Z
```diff
@@ -442,3 +483,44 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 		Message: message,
 	}
 }
+
+func cloneNodeSelector(src map[string]string) map[string]string {
+	ret := make(map[string]string, len(src))
+	for k, v := range src {
+		ret[k] = v
+	}
+	return ret
+}
+
+// getNodeSelectorsFromObjectAnnotation tries to retrieve a node selectors slice from the
+// object's annotations fails if it's not found or is unable to unmarshal
+func getNodeSelectorsFromObjectAnnotation(obj client.Object) ([]PodSetNodeSelector, error) {
+	str, found := obj.GetAnnotations()[OriginalNodeSelectorsAnnotation]
+	if !found {
+		return nil, errNodeSelectorsNotFound
+	}
+	// unmarshal
+	ret := []PodSetNodeSelector{}
+	if err := json.Unmarshal([]byte(str), &ret); err != nil {
+		return nil, err
+	}
+	return ret, nil
+}
+
+// nodeSelectorsSetToObject - sets an annotation containing the provided node selectors into
+// a job object, even if very unlikely it could return an error related to json.marshaling
+func nodeSelectorsSetToObject(obj client.Object, nodeSelectors []PodSetNodeSelector) error {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-04-05T05:53:21Z

- **pkg/controller/jobs/mpijob/mpijob_controller.go:115** by [@trasc](https://github.com/trasc) — 2023-04-05T05:53:20Z
```diff
@@ -113,26 +113,25 @@ func (j *MPIJob) RunWithNodeAffinity(nodeSelectors []map[string]string) {
 	for index := range nodeSelectors {
```

  done
